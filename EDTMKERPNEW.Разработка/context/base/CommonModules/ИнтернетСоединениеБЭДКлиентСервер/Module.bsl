
#Область СлужебныйПрограммныйИнтерфейс

// Возвращает вид ошибки, возникающей при проблемах с интернет-соединением.
// 
// Возвращаемое значение:
// 	См. ОбработкаНеисправностейБЭДКлиентСервер.НовоеОписаниеВидаОшибки
Функция ВидОшибкиИнтернетСоединение() Экспорт
	
	ОбработчикВыполненияДиагностики = "ОбработкаНеисправностейБЭДКлиент.ОткрытьМастерДиагностики";
	
	ВидОшибки = ОбработкаНеисправностейБЭДКлиентСервер.НовоеОписаниеВидаОшибки();
	ВидОшибки.Идентификатор = ИдентификаторВидаОшибкиИнтернетСоединение();
	ВидОшибки.ВыполнятьОбработчикАвтоматически = Истина;
	ВидОшибки.АвтоматическиВыполняемыйОбработчик = ОбработчикВыполненияДиагностики;
	ВидОшибки.ЗаголовокПроблемы = НСтр("ru = 'Ошибка интернет-соединения';
										|en = 'Internet connection error'");
	ОписаниеРешения = НСтр("ru = '<a href = ""%1"">Выполните</a> диагностику интернет-соединения';
							|en = '<a href = ""%1"">Diagnose</a> Internet connection'");
	ВидОшибки.ОписаниеРешения = СтрШаблон(ОписаниеРешения, "Выполните");
	ВидОшибки.ОбработчикиНажатия.Вставить("Выполните", ОбработчикВыполненияДиагностики);
	
	Возврат ВидОшибки;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Возвращаемое значение:
//  Строка
Функция ИдентификаторВидаОшибкиИнтернетСоединение() Экспорт
	
	Возврат "ОшибкаИнтернетСоединения";
	
КонецФункции

#КонецОбласти