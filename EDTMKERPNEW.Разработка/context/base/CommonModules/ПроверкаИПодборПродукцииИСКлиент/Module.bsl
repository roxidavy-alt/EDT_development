#Область ПрограммныйИнтерфейс

#Область ОткрытиеФормыПроверкиИПодбора

// Открывает форму проверки и подбора маркируемой продукции
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - Источник события
//  ПараметрыУказанияСерий - Произвольный - параметры указания серий источника для корректного отображения серий.
//  Сценарий - Число - сценарий точки вызова
//
Процедура ОткрытьФормуПроверкиИПодбора(Форма, ПараметрыУказанияСерий, Сценарий = 0) Экспорт
	
	ТребуетсяВопрос = Ложь;
	
	ПараметрыОткрытияФормыПроверки = ПараметрыОткрытияФормыПроверкиИПодбора(Форма, Сценарий);
	
	Если НЕ ПустаяСтрока(ПараметрыОткрытияФормыПроверки.ИмяРеквизитаФормыОбъект) Тогда
		Объект = Форма[ПараметрыОткрытияФормыПроверки.ИмяРеквизитаФормыОбъект];

		Если НЕ ПараметрыОткрытияФормыПроверки.ПроверятьМодифицированность Тогда
		ИначеЕсли Объект.Ссылка.Пустая() Тогда
			ТребуетсяВопрос = Истина;
			ТекстВопроса    = НСтр("ru = 'Сканирование маркируемой продукции возможно только в записанном документе. Записать?';
									|en = 'Сканирование маркируемой продукции возможно только в записанном документе. Записать?'");
		ИначеЕсли Форма.Модифицированность Тогда
			ТребуетсяВопрос  = Истина;
			ПровестиЗаписать = ?(Объект.Проведен, НСтр("ru = 'Провести';
														|en = 'Провести'"), НСтр("ru = 'Записать';
																				|en = 'Записать'"));
			ТекстВопроса     = СтрШаблон(НСтр("ru = 'Документ был изменен. %1?';
												|en = 'Документ был изменен. %1?'"), ПровестиЗаписать);
		КонецЕсли;
	КонецЕсли;
	
	Если ТребуетсяВопрос Тогда
		
		ПараметрыВопроса = Новый Структура();
		ПараметрыВопроса.Вставить("Форма", Форма);
		ПараметрыВопроса.Вставить("ПараметрыОткрытияФормыПроверки", ПараметрыОткрытияФормыПроверки);
		ПараметрыВопроса.Вставить("ПараметрыУказанияСерий", ПараметрыУказанияСерий);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ОткрытьФормуПроверкиИПодбораПриОтветеНаВопрос",
			ЭтотОбъект, ПараметрыВопроса);
		
		ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
		
	Иначе
		
		ОткрытьФормуПроверкиИПодбораМаркируемойПродукции(Форма, ПараметрыОткрытияФормыПроверки, ПараметрыУказанияСерий);
		
	КонецЕсли;
	
КонецПроцедуры

// Выполняет переопределяемую команду
//
// Параметры:
//  Форма                   - ФормаКлиентскогоПриложения - форма, в которой расположена команда
//  Команда                 - КомандаФормы     - команда формы
//  ДополнительныеПараметры - Структура        - дополнительные параметры.
//
Процедура ВыполнитьПереопределяемуюКоманду(Форма, Команда, ДополнительныеПараметры) Экспорт
	
	Если СтрНачинаетсяС(Команда.Имя, "ПодобратьИПроверитьПродукциюГосИС")
		Или СтрНачинаетсяС(Команда.Имя, "ПроверитьАкцизныеМарки") Тогда
		ПараметрыУказанияСерий = ИнтеграцияИСКлиент.ПараметрыУказанияСерий(Форма);
	КонецЕсли;
	
	Если СтрНачинаетсяС(Команда.Имя, "ПодобратьИПроверитьПродукциюГосИС") Тогда
		
		ОткрытьФормуПроверкиИПодбора(Форма, ПараметрыУказанияСерий, Число(Прав(Команда.Имя, 1)));
		
	ИначеЕсли СтрНачинаетсяС(Команда.Имя, "ПроверитьАкцизныеМарки") Тогда
		
		ОткрытьФормуПроверкиИПодбора(Форма, ПараметрыУказанияСерий, 0);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СтатусыПроверкиПодбораСтрок

// Обновляет статус проверки маркируемой продукции при изменении количества/состава строк по кешированным данным
//   состава табличной части "Штрихкоды упаковок".
// 
// Параметры:
//   Форма - ФормаКлиентскогоПриложения - редактируемая форма.
//   ТабличнаяЧастьТовары - ДанныеФормыКоллекция - редактируемая таблица.
//   ДанныеСтроки - ДанныеФормыЭлементКоллекции - редактируемая строка.
//   ДанныеКешаСтроки - Структура - данные строки перед редактированием.
//   ВсеТоварыМаркируемые - Булево - Обработка всех маркируемых товаров.
//   ДополнительныеКлючи - Строка - дополнительные ключи связи строк товаров и кеша штрихкодов упаковок.
//   ИмяКолонкиКоличество - Строка - имя колонки "Количество" в табличной части документа.
//   Сценарий - Число - сценарий обработки (таблица штрихкодов упаковок и редактируемая таблица).
// Возвращаемое значение:
//   Булево - требуется пересчет кеша для всей табличной части.
Функция ПрименитьКешПоСтроке(Форма, ТабличнаяЧастьТовары, ДанныеСтроки, ДанныеКешаСтроки, ВсеТоварыМаркируемые = Ложь, ДополнительныеКлючи = "", ИмяКолонкиКоличество = "Количество", Сценарий = 0) Экспорт
	
	КлючСвязиИзменен = ДанныеСтроки.Номенклатура <> ДанныеКешаСтроки.Номенклатура
		ИЛИ (ДанныеКешаСтроки.Свойство("Характеристика")
			И ДанныеСтроки.Характеристика <> ДанныеКешаСтроки.Характеристика)
		ИЛИ (ДанныеКешаСтроки.Свойство("Серия")
			И ДанныеСтроки.Серия <> ДанныеКешаСтроки.Серия);
	Для Каждого КлючСвязи Из СтрРазделить(ДополнительныеКлючи, ",", Ложь) Цикл
		КлючСвязиИзменен = КлючСвязиИзменен ИЛИ ДанныеСтроки[СокрЛП(КлючСвязи)] <> ДанныеКешаСтроки[СокрЛП(КлючСвязи)];
	КонецЦикла;
	
	ОбновитьПризнакиКонтроляОборотаМарокПоСтроке(Форма, ДанныеСтроки, Сценарий);
	
	ЕстьКолонкаПроверятьВозможностьОборотаНемаркированныхОстатковКеш = ДанныеКешаСтроки.Свойство("РазрешенОборотНемаркированныхОстатков");
	ЕстьКолонкаДействуетКонтрольОборотаМарокКеш                      = ДанныеКешаСтроки.Свойство("ДействуетКонтрольОборотаМарок");
	
	ЕстьКолонкаИспользоватьСтатусОСУ                              = ДанныеСтроки.Свойство("АвтоматическийОСУИС");
	ЕстьКолонкаПроверятьВозможностьОборотаНемаркированныхОстатков = ДанныеСтроки.Свойство("РазрешенОборотНемаркированныхОстатков");
	ЕстьКолонкаДействуетКонтрольОборотаМарок                      = ДанныеСтроки.Свойство("ДействуетКонтрольОборотаМарок");
	
	ПроверятьВозможностьОборотаНемаркированныхОстатковКеш = ?(ЕстьКолонкаПроверятьВозможностьОборотаНемаркированныхОстатковКеш, ДанныеКешаСтроки.РазрешенОборотНемаркированныхОстатков, Ложь);
	ДействуетКонтрольОборотаМарокКеш                      = ?(ЕстьКолонкаДействуетКонтрольОборотаМарокКеш, ДанныеКешаСтроки.ДействуетКонтрольОборотаМарок, Истина);
	ИспользоватьСтатусОСУ                                 = ?(ЕстьКолонкаИспользоватьСтатусОСУ, ДанныеСтроки.АвтоматическийОСУИС, Ложь);
	ПроверятьВозможностьОборотаНемаркированныхОстатков    = ?(ЕстьКолонкаПроверятьВозможностьОборотаНемаркированныхОстатков, ДанныеСтроки.РазрешенОборотНемаркированныхОстатков, Ложь);
	ДействуетКонтрольОборотаМарок                         = ?(ЕстьКолонкаДействуетКонтрольОборотаМарок, ДанныеСтроки.ДействуетКонтрольОборотаМарок, Истина);
	
	МассивКлючейСвязи = Новый Массив;
	МассивКлючейСвязи.Добавить("Номенклатура");
	Если ДанныеКешаСтроки.Свойство("Характеристика") Тогда
		МассивКлючейСвязи.Добавить("Характеристика");
	КонецЕсли;
	Если ДанныеКешаСтроки.Свойство("Серия") Тогда
		МассивКлючейСвязи.Добавить("Серия");
	КонецЕсли;
	Для Каждого КлючСвязи Из СтрРазделить(ДополнительныеКлючи, ",", Ложь) Цикл
		МассивКлючейСвязи.Добавить(СокрЛП(КлючСвязи));
	КонецЦикла;
	
	СтруктураПоиска = Новый Структура(СтрСоединить(МассивКлючейСвязи, ","));
	
	// Дополнительное поле сравнения
	ЕстьКоличествоПотребительскихКодов = ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДанныеСтроки, "КоличествоПотребительскихУпаковок");
	
	// Легенда:
	// 0 - пустая иконка.               Продукция немаркируемая.
	// 1 - зеленая иконка.              Продукция маркируемая, отсканировано столько марок, сколько требуется единиц товара.
	// 2 - красная иконка.              Продукция маркируемая, марок нет совсем или отсканировано недостаточно.
	// 3 - голубая иконка.              Продукция требует взвешивания.
	// 4 - серая иконка.                Марки отгружаются по ОСУ. Конкретные коды ОСУ не указаны.
	// 5 - ?
	// 6 - иконка с вопросом.           Разрешен оборот немаркируемых остатков, марки не отсканированы.
	// 7 - иконка с вопросом и зеленым. Разрешен оборот немаркируемых остатков, отсканированы марки или подобраны по ОСУ, но не по всему количеству.
	
	Если КлючСвязиИзменен И (ВсеТоварыМаркируемые Или ДанныеКешаСтроки.МаркируемаяПродукция) Тогда
		
		ЗаполнитьЗначенияСвойств(СтруктураПоиска, ДанныеКешаСтроки);
		
		СтруктураПоискаКеш = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(СтруктураПоиска);
		СтруктураПоискаКеш.Вставить("Сценарий", Сценарий);
		СтрокиКеша = Форма.ДанныеШтрихкодовУпаковокГосИС.НайтиСтроки(СтруктураПоискаКеш);
		
		Если СтрокиКеша.Количество() Тогда
			
			СтрокиТовары = ТабличнаяЧастьТовары.НайтиСтроки(СтруктураПоиска);
			Если НЕ СтрокиТовары.Количество() Тогда
				Возврат Истина; // Требуется удалить штрихкоды из табличной части штрихкодов и пересчитать кеш
			КонецЕсли;
			
		КонецЕсли;
		
		Если СтрокиКеша.Количество() И ДействуетКонтрольОборотаМарокКеш Тогда
			
			КоличествоПоКлючу = 0;
			КоличествоКодовПоКлючу = 0;
			Для Каждого СтрокаПоКлючу Из СтрокиТовары Цикл
				КоличествоПоКлючу = КоличествоПоКлючу + СтрокаПоКлючу[ИмяКолонкиКоличество];
				Если ЕстьКоличествоПотребительскихКодов Тогда
					КоличествоКодовПоКлючу = КоличествоКодовПоКлючу + СтрокаПоКлючу.КоличествоПотребительскихУпаковок;
				КонецЕсли;
			КонецЦикла;
			
			Если КоличествоПоКлючу = СтрокиКеша[0].Количество
				И (Не ЕстьКоличествоПотребительскихКодов Или КоличествоКодовПоКлючу = СтрокиКеша[0].КоличествоПотребительскихУпаковок)Тогда
				СтатусПроверкиГосИС = 1;
			ИначеЕсли СтрокиКеша[0].БезКоличества И Не ЕстьКоличествоПотребительскихКодов Тогда
				СтатусПроверкиГосИС = 3;
			ИначеЕсли (ПроверятьВозможностьОборотаНемаркированныхОстатковКеш Или Не ДействуетКонтрольОборотаМарок)
				И КоличествоПоКлючу > СтрокиКеша[0].Количество
				И СтрокиКеша[0].Количество > 0
				И (Не ЕстьКоличествоПотребительскихКодов Или КоличествоКодовПоКлючу > СтрокиКеша[0].КоличествоПотребительскихУпаковок) Тогда
				СтатусПроверкиГосИС = 7;
			ИначеЕсли ДействуетКонтрольОборотаМарокКеш Тогда
				СтатусПроверкиГосИС = 2;
			Иначе
				СтатусПроверкиГосИС = 0;
			КонецЕсли;
			Для Каждого СтрокаПоКлючу Из СтрокиТовары Цикл
				СтрокаПоКлючу.СтатусПроверкиГосИС = СтатусПроверкиГосИС;
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ВсеТоварыМаркируемые Или ДанныеСтроки.МаркируемаяПродукция Тогда
		
		ЗаполнитьЗначенияСвойств(СтруктураПоиска, ДанныеСтроки);
		СтруктураПоискаКеш = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(СтруктураПоиска);
		СтруктураПоискаКеш.Вставить("Сценарий", Сценарий);
		СтрокиКеша = Форма.ДанныеШтрихкодовУпаковокГосИС.НайтиСтроки(СтруктураПоискаКеш);
		СтрокиТовары = ТабличнаяЧастьТовары.НайтиСтроки(СтруктураПоиска);
		
		КоличествоПоКлючу = 0;
		КоличествоКодовПоКлючу = 0;
		Для Каждого СтрокаПоКлючу Из СтрокиТовары Цикл
			КоличествоПоКлючу = КоличествоПоКлючу + СтрокаПоКлючу[ИмяКолонкиКоличество];
			Если ЕстьКоличествоПотребительскихКодов Тогда
				КоличествоКодовПоКлючу = КоличествоКодовПоКлючу + СтрокаПоКлючу.КоличествоПотребительскихУпаковок;
			КонецЕсли;
		КонецЦикла;
		
		Если СтрокиКеша.Количество() = 0 Тогда
			// подобранных марок нет, автоОСУ
			Если ИспользоватьСтатусОСУ Тогда
				СтатусПроверкиГосИС = 4;
			// подобранных марок нет, действует контроль, но не обязателен
			ИначеЕсли ДействуетКонтрольОборотаМарок И ПроверятьВозможностьОборотаНемаркированныхОстатков Тогда
				СтатусПроверкиГосИС = 6;
			// действует контроль, марки обязательны
			ИначеЕсли ДействуетКонтрольОборотаМарок Тогда
				СтатусПроверкиГосИС = 2;
			КонецЕсли;
		// подобрано столько марок, сколько нужно
		ИначеЕсли (КоличествоПоКлючу = СтрокиКеша[0].Количество)
				И (Не ЕстьКоличествоПотребительскихКодов Или КоличествоКодовПоКлючу = СтрокиКеша[0].КоличествоПотребительскихУпаковок) Тогда
			СтатусПроверкиГосИС = 1;
		// марки без количества
		ИначеЕсли СтрокиКеша[0].БезКоличества И Не ЕстьКоличествоПотребительскихКодов Тогда
			СтатусПроверкиГосИС = 3;
		// марки есть, но контроль не обязателен или разрешен оборот остатков
		ИначеЕсли (ПроверятьВозможностьОборотаНемаркированныхОстатков Или Не ДействуетКонтрольОборотаМарок)
			И КоличествоПоКлючу > СтрокиКеша[0].Количество
			И СтрокиКеша[0].Количество > 0
			И (Не ЕстьКоличествоПотребительскихКодов Или КоличествоКодовПоКлючу > СтрокиКеша[0].КоличествоПотребительскихУпаковок) Тогда
			СтатусПроверкиГосИС = 7;
		// марки обязательны, их сколько нужно
		ИначеЕсли ДействуетКонтрольОборотаМарок Тогда
			СтатусПроверкиГосИС = 2;
		Иначе
			СтатусПроверкиГосИС = 0;
		КонецЕсли;
		Для Каждого СтрокаПоКлючу Из СтрокиТовары Цикл
			СтрокаПоКлючу.СтатусПроверкиГосИС = СтатусПроверкиГосИС;
		КонецЦикла;
		
	Иначе
		ДанныеСтроки.СтатусПроверкиГосИС = 0;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

#Область РасчетХешСумм

Процедура ЗаполнитьЗначенияСтрокДереваДляРасчетаХешСумм(ЗначенияСтрокДерева, СтрокиДереваУпаковок) Экспорт
	
	Для Каждого СтрокаДереваУпаковок Из СтрокиДереваУпаковок Цикл
		
		ДанныеСтроки = Новый Структура;
		ДанныеСтроки.Вставить("ТипУпаковки");
		ДанныеСтроки.Вставить("ВидУпаковки"); // Для расчета хеша для упаковок ОСУ
		ДанныеСтроки.Вставить("СтатусПроверки");
		ДанныеСтроки.Вставить("Штрихкод");
		ДанныеСтроки.Вставить("ХешСуммаНормализации");
		
		ДанныеСтроки.Вставить("Номенклатура");                                 // Для расчета хеша для упаковок ОСУ
		ДанныеСтроки.Вставить("Характеристика");                               // Для расчета хеша для упаковок ОСУ
		ДанныеСтроки.Вставить("Серия");                                        // Для расчета хеша для упаковок ОСУ
		ДанныеСтроки.Вставить("КоличествоПодчиненныхПотребительскихУпаковок"); // Для расчета хеша для упаковок ОСУ
		ДанныеСтроки.Вставить("Количество");                                   // Для расчета хеша для упаковок ОСУ
		ДанныеСтроки.Вставить("КоличествоПодчиненныхОтсутствует");             // Для расчета хеша для упаковок ОСУ 
		ДанныеСтроки.Вставить("КоличествоПодчиненныхВсего");                   // Для расчета хеша для упаковок ОСУ
		
		ЗаполнитьЗначенияСвойств(ДанныеСтроки, СтрокаДереваУпаковок);
		
		ДанныеСтроки.Вставить("ИдентификаторСтроки", СтрокаДереваУпаковок.ПолучитьИдентификатор());
		ДанныеСтроки.Вставить("ХешСумма",            "");
		ДанныеСтроки.Вставить("Строки",              Новый Массив());
		
		ПодчиненныеСтрокиДереваУпаковок = СтрокаДереваУпаковок.ПолучитьЭлементы();
		
		Если ПодчиненныеСтрокиДереваУпаковок.Количество() > 0 Тогда
			ЗаполнитьЗначенияСтрокДереваДляРасчетаХешСумм(ДанныеСтроки.Строки, ПодчиненныеСтрокиДереваУпаковок);
		КонецЕсли;
		
		ЗначенияСтрокДерева.Добавить(ДанныеСтроки);

	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьХешСуммыВСтрокахДереваУпаковок(ЗначенияСтрокДерева, ДеревоУпаковок) Экспорт
	
	Для Каждого ДанныеСтроки Из ЗначенияСтрокДерева Цикл
		СтрокаДереваУпаковок = ДеревоУпаковок.НайтиПоИдентификатору(ДанныеСтроки.ИдентификаторСтроки);
		
		Если СтрокаДереваУпаковок <> Неопределено Тогда
			СтрокаДереваУпаковок.ХешСумма = ДанныеСтроки.ХешСумма;
		КонецЕсли;
		
		Если ДанныеСтроки.Строки.Количество() > 0 Тогда
			ЗаполнитьХешСуммыВСтрокахДереваУпаковок(ДанныеСтроки.Строки, ДеревоУпаковок);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область ОткрытиеФормыПроверкиИПодбора

#Область СтруктурыДанных

// Предназначена для установки параметров открытия формы проверки и подбора маркируемой продукции
//
// Параметры:
//   Форма - ФормаКлиентскогоПриложения - форма, в который встраивается функциональность библиотеки ГосИС
//   Сценарий - Число - сценарий вызова формы проверки
// Возвращаемое значение:
//  Структура - значения, используемые для управления открытием формы проверки и подбора:
//  * ИмяРеквизитаФормыОбъект              - Строка - имя реквизита формы документа, содержащего объект документа. Для открытия не из формы документа должен быть пустой строкой
//  * ИмяРеквизитаОрганизация              - Строка - имя реквизита документа или реквизита формы, содержащего организацию
//  * ИмяРеквизитаКонтрагент               - Строка - имя реквизита документа или реквизита формы, содержащего контрагента
//  * ИмяРеквизитаСклад                    - Строка - имя реквизита документа или реквизита формы, содержащего склад
//  * Склад                                - Произвольный - явное указание склада если имя реквизита недоступно
//  * ИмяРеквизитаДокументОснование        - Строка - имя реквизита документа, содержащего его основание. Если основания нет, должен быть пустой строкой.
//  * ПроверятьНеобходимостьПеремаркировки - Булево - признак необходимости контроля состава упаковок в форме проверки.
//  * РассчитыватьХешСуммуУпаковки          - Булево - признак необходимости расчета хеш-сумм упаковок.
//  * РедактированиеФормыНедоступно        - Булево - признак недоступности редактирования формы, из которой открывается форма проверки
//  * АдресПроверяемыхДанных               - Строка - адрес данных для загрузки в форму проверки, если передача данных происходит не через ссылку на документ
//  * ОписаниеОповещенияПриЗакрытии        - ОписаниеОповещения - описание процедуры, которая будет вызвана после закрытия формы проверки
//  * ПроверятьМодифицированность          - Булево - признак необходимости записи документа перед открытием формы проверки
//  * ПроверкаЭлектронногоДокумента        - Булево - признак режима проверки входящего электронного документа
//  * Сценарий                             - Число - сценарий проверки документа
//
Функция ПараметрыОткрытияФормыПроверкиИПодбора(Форма, Сценарий)
	
	ИспользоватьСтатусПроверкаЗавершена = Ложь;
	ПроверкаЭлектронногоДокумента = Ложь;
	
	ПараметрыИнтеграции = ПараметрыИнтеграции(Форма, Сценарий);
	Если ПараметрыИнтеграции <> Неопределено Тогда
		ИспользоватьСтатусПроверкаЗавершена = ПараметрыИнтеграции.ИспользоватьСтатусПроверкаЗавершена;
		ПроверкаЭлектронногоДокумента = ПараметрыИнтеграции.ЕстьЭлектронныйДокумент;
	КонецЕсли;
	
	ПараметрыОткрытия = Новый Структура;
	ПараметрыОткрытия.Вставить("ИмяРеквизитаФормыОбъект",              "Объект");
	ПараметрыОткрытия.Вставить("ИмяРеквизитаОрганизация",              "Организация");
	ПараметрыОткрытия.Вставить("ИмяРеквизитаКонтрагент",               "");
	ПараметрыОткрытия.Вставить("ИмяРеквизитаСклад",                    "Склад");
	ПараметрыОткрытия.Вставить("Склад",                                Неопределено);
	ПараметрыОткрытия.Вставить("ИмяРеквизитаДокументОснование",        "ДокументОснование");
	ПараметрыОткрытия.Вставить("ПроверятьНеобходимостьПеремаркировки", Истина);
	ПараметрыОткрытия.Вставить("РассчитыватьХешСуммуУпаковки",          Истина);
	ПараметрыОткрытия.Вставить("РедактированиеФормыНедоступно",        Ложь);
	ПараметрыОткрытия.Вставить("АдресПроверяемыхДанных",               "");
	ПараметрыОткрытия.Вставить("ПроверятьМодифицированность",          Истина);
	ПараметрыОткрытия.Вставить("ПроверкаЭлектронногоДокумента",        ПроверкаЭлектронногоДокумента);
	ПараметрыОткрытия.Вставить("РежимПодбораСуществующихУпаковок",     Истина);
	ПараметрыОткрытия.Вставить("ДопустимыйСпособВводаВОборот",         Неопределено);
	ПараметрыОткрытия.Вставить("ДанныеВыбораПоМаркируемойПродукции",   Неопределено);
	ПараметрыОткрытия.Вставить("СохраненВыборПоМаркируемойПродукции",  Неопределено);
	ПараметрыОткрытия.Вставить("ДоступноСозданиеНовыхУпаковок",        Ложь);
	ПараметрыОткрытия.Вставить("ИспользоватьСтатусПроверкаЗавершена",  ИспользоватьСтатусПроверкаЗавершена);
	ПараметрыОткрытия.Вставить("Сценарий",                             Сценарий);
	ПараметрыОткрытия.Вставить("РежимИсправленияОшибокПослеПередачиКодов", Ложь);
	ПараметрыОткрытия.Вставить("ОрганизацияЕГАИС",                     Неопределено);
	
	ПараметрыОткрытия.Вставить("ЕГАИС",  Ложь);
	ПараметрыОткрытия.Вставить("ИСМП",   Ложь);
	ПараметрыОткрытия.Вставить("Прочее", Ложь);
	
	ПараметрыЗакрытия = Новый Структура;
	ПараметрыЗакрытия.Вставить("Форма", Форма);
	
	ОповещениеПриЗакрытии = Новый ОписаниеОповещения(
		"ПриЗакрытииФормыПроверкиИПодбора",
		ЭтотОбъект, ПараметрыЗакрытия);
	
	ПараметрыОткрытия.Вставить("ОписаниеОповещенияПриЗакрытии", ОповещениеПриЗакрытии);
	
	ПриУстановкеПараметровОткрытияФормыПроверкиИПодбора(Форма, ПараметрыОткрытия);
	
	Возврат ПараметрыОткрытия;
	
КонецФункции

Функция ПараметрыИнтеграции(Форма, Сценарий)
	
	Результат = Неопределено;
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "ПараметрыИнтеграцииГосИС") Тогда
		ПараметрыИнтеграцииМассив = Форма.ПараметрыИнтеграцииГосИС.Получить("ФормаПроверкиИПодбора");
		Если ПараметрыИнтеграцииМассив <> Неопределено Тогда
			Для Каждого ЭлементМассива Из ПараметрыИнтеграцииМассив Цикл
				Если ЭлементМассива.Сценарий = Сценарий Тогда
					Результат = ЭлементМассива;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	Возврат Результат;
	
КонецФункции

#КонецОбласти

// Предназначена для открытия формы проверки и подбора маркируемой продукции
// 
// Параметры:
//   Форма - ФормаКлиентскогоПриложения - форма прикладного документа или общая форма, в который встраивается
//       функциональность библиотеки ГосИС.
//   ПараметрыОткрытияФормыПроверки - См. ПараметрыОткрытияФормыПроверкиИПодбора.
//   ПараметрыУказанияСерий - Произвольный - параметры указания серий источника для корректного отображения серий.
//
Процедура ОткрытьФормуПроверкиИПодбораМаркируемойПродукции(Форма, Знач ПараметрыОткрытияФормыПроверки, Знач ПараметрыУказанияСерий)

	ОчиститьСообщения();
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("РедактированиеФормыНедоступно",        ПараметрыОткрытияФормыПроверки.РедактированиеФормыНедоступно);
	ПараметрыФормы.Вставить("ПроверятьНеобходимостьПеремаркировки", ПараметрыОткрытияФормыПроверки.ПроверятьНеобходимостьПеремаркировки);
	ПараметрыФормы.Вставить("РассчитыватьХешСуммуУпаковки",          ПараметрыОткрытияФормыПроверки.РассчитыватьХешСуммуУпаковки);
	ПараметрыФормы.Вставить("АдресПроверяемыхДанных",               ПараметрыОткрытияФормыПроверки.АдресПроверяемыхДанных);
	ПараметрыФормы.Вставить("ДопустимыйСпособВводаВОборот",         ПараметрыОткрытияФормыПроверки.ДопустимыйСпособВводаВОборот);
	ПараметрыФормы.Вставить("ДанныеВыбораПоМаркируемойПродукции",   ПараметрыОткрытияФормыПроверки.ДанныеВыбораПоМаркируемойПродукции);
	ПараметрыФормы.Вставить("СохраненВыборПоМаркируемойПродукции",  ПараметрыОткрытияФормыПроверки.СохраненВыборПоМаркируемойПродукции);
	ПараметрыФормы.Вставить("ПроверкаЭлектронногоДокумента",        ПараметрыОткрытияФормыПроверки.ПроверкаЭлектронногоДокумента);
	ПараметрыФормы.Вставить("ДоступноСозданиеНовыхУпаковок",        ПараметрыОткрытияФормыПроверки.ДоступноСозданиеНовыхУпаковок);
	ПараметрыФормы.Вставить("ИспользоватьСтатусПроверкаЗавершена",  ПараметрыОткрытияФормыПроверки.ИспользоватьСтатусПроверкаЗавершена);
	ПараметрыФормы.Вставить("Сценарий",                             ПараметрыОткрытияФормыПроверки.Сценарий);
	ПараметрыФормы.Вставить("ПараметрыУказанияСерий",               ПараметрыУказанияСерий);
	
	ПараметрыФормы.Вставить("РежимИсправленияОшибокПослеПередачиКодов", ПараметрыОткрытияФормыПроверки.РежимИсправленияОшибокПослеПередачиКодов);
	
	ПараметрыФормы.Вставить("ПроверкаНеПоДокументу",            Истина);
	ПараметрыФормы.Вставить("ПроверяемыйДокумент",              Неопределено);
	ПараметрыФормы.Вставить("РежимПодбораСуществующихУпаковок", ПараметрыОткрытияФормыПроверки.РежимПодбораСуществующихУпаковок);
	ПараметрыФормы.Вставить("Организация",                      Неопределено);
	ПараметрыФормы.Вставить("ОрганизацияЕГАИС",                 ПараметрыОткрытияФормыПроверки.ОрганизацияЕГАИС);
	ПараметрыФормы.Вставить("БезУчетаОрганизацииЕГАИС",         Ложь);
	ПараметрыФормы.Вставить("Склад",                            ПараметрыОткрытияФормыПроверки.Склад);
	ПараметрыФормы.Вставить("Контрагент",                       Неопределено);
	
	ПараметрыФормы.Вставить("ЕГАИС",  ПараметрыОткрытияФормыПроверки.ЕГАИС);
	ПараметрыФормы.Вставить("ИСМП",   ПараметрыОткрытияФормыПроверки.ИСМП);
	ПараметрыФормы.Вставить("Прочее", ПараметрыОткрытияФормыПроверки.Прочее);
	
	Отказ = Ложь;
	
	ПередОткрытиемФормыПроверкиПодбора(
		Форма, ПараметрыОткрытияФормыПроверки, ПараметрыФормы, Отказ);
	
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	ОткрытьФорму("Обработка.ПроверкаИПодборПродукцииИС.Форма.ПроверкаИПодбор",
		ПараметрыФормы,
		Форма,
		Форма.УникальныйИдентификатор, , ,
		ПараметрыОткрытияФормыПроверки.ОписаниеОповещенияПриЗакрытии,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

// Установка параметров формы проверки по параметрам открытия
Процедура ПередОткрытиемФормыПроверкиПодбора(Форма, ПараметрыОткрытияФормыПроверки, ПараметрыФормы, Отказ)
	
	Если ПараметрыОткрытияФормыПроверки.ЕГАИС Тогда
		ПроверкаИПодборПродукцииЕГАИСКлиент.ПередОткрытиемФормыПроверкиПодбора(Форма, ПараметрыОткрытияФормыПроверки, ПараметрыФормы, Отказ);
	КонецЕсли;
	
	Если ПустаяСтрока(ПараметрыОткрытияФормыПроверки.ИмяРеквизитаФормыОбъект) Тогда
		
		ПараметрыФормы.Организация = Форма[ПараметрыОткрытияФормыПроверки.ИмяРеквизитаОрганизация];
		
	Иначе
		
		Объект = Форма[ПараметрыОткрытияФормыПроверки.ИмяРеквизитаФормыОбъект];
		ПараметрыФормы.ПроверяемыйДокумент = Объект.Ссылка;
		ПараметрыФормы.ПроверкаНеПоДокументу = Ложь;
		
		Если Не ПустаяСтрока(ПараметрыОткрытияФормыПроверки.ИмяРеквизитаОрганизация) Тогда
			ПараметрыФормы.Организация = Объект[ПараметрыОткрытияФормыПроверки.ИмяРеквизитаОрганизация];
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПараметрыОткрытияФормыПроверки.ИмяРеквизитаСклад) Тогда
			ПараметрыФормы.Склад = Объект[ПараметрыОткрытияФормыПроверки.ИмяРеквизитаСклад];
		КонецЕсли;
		
		Если НЕ ПустаяСтрока(ПараметрыОткрытияФормыПроверки.ИмяРеквизитаДокументОснование) Тогда
			ПараметрыФормы.Вставить("КонтролироватьСканируемуюПродукциюПоДокументуОснованию",
				ЗначениеЗаполнено(Объект[ПараметрыОткрытияФормыПроверки.ИмяРеквизитаДокументОснование]));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПараметрыОткрытияФормыПроверки.ИмяРеквизитаКонтрагент) Тогда
			ПараметрыФормы.Вставить("Контрагент", Объект[ПараметрыОткрытияФормыПроверки.ИмяРеквизитаКонтрагент]);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ШтрихкодированиеИСКлиентСервер.ЭтоКонтекстОбъекта(Форма, "Документ.ОтчетОбИмпортеЕГАИС")
		Или ШтрихкодированиеИСКлиентСервер.ЭтоКонтекстОбъекта(Форма, "Документ.ОтчетОПроизводствеЕГАИС") Тогда
		ПараметрыФормы.Организация = Форма.Организация;
	КонецЕсли;
	
	ПроверкаИПодборПродукцииИСКлиентПереопределяемый.ПередОткрытиемФормыПроверкиПодбора(
		Форма, ПараметрыОткрытияФормыПроверки, ПараметрыФормы, Отказ);
	
КонецПроцедуры

Процедура ПриУстановкеПараметровОткрытияФормыПроверкиИПодбора(Форма, ПараметрыОткрытия)
	
	ПроверкаИПодборПродукцииЕГАИСКлиент.ПриУстановкеПараметровОткрытияФормыПроверкиИПодбора(Форма, ПараметрыОткрытия);
	ПроверкаИПодборПродукцииИСМПКлиент.ПриУстановкеПараметровОткрытияФормыПроверкиИПодбора(Форма, ПараметрыОткрытия);
	ПроверкаИПодборПродукцииИСКлиентПереопределяемый.ПриУстановкеПараметровОткрытияФормыПроверкиИПодбора(Форма, ПараметрыОткрытия);
	
КонецПроцедуры

Процедура ОткрытьФормуПроверкиИПодбораПриОтветеНаВопрос(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	Форма = ДополнительныеПараметры.Форма;
	ПараметрыОткрытияФормыПроверки = ДополнительныеПараметры.ПараметрыОткрытияФормыПроверки;
	
	Объект = Форма[ПараметрыОткрытияФормыПроверки.ИмяРеквизитаФормыОбъект];
	
	СтандартнаяОбработка = Ложь;
	ДействиеПослеЗаписи = Новый ОписаниеОповещения("ОткрытьФормуПроверкиИПодбораПослеЗаписиОбъекта", ЭтотОбъект, ДополнительныеПараметры);
	ИнтеграцияИСКлиентПереопределяемый.ВыполнитьЗаписьОбъектаВФорме(Форма, Объект, ДействиеПослеЗаписи, СтандартнаяОбработка);
	
	Если Не СтандартнаяОбработка Тогда
		Возврат;
	КонецЕсли;
	
	РезультатЗаписи = Ложь;
	Если Объект.Проведен Тогда
		Если Форма.ПроверитьЗаполнение() Тогда
			РезультатЗаписи = Форма.Записать(Новый Структура("РежимЗаписи", РежимЗаписиДокумента.Проведение));
		КонецЕсли;
	Иначе
		РезультатЗаписи = Форма.Записать(Новый Структура("РежимЗаписи", РежимЗаписиДокумента.Запись));
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ДействиеПослеЗаписи, РезультатЗаписи);
	
КонецПроцедуры

Процедура ОткрытьФормуПроверкиИПодбораПослеЗаписиОбъекта(РезультатЗаписи, ДополнительныеПараметры) Экспорт
	
	Если Не РезультатЗаписи Тогда
		Возврат;
	КонецЕсли;
	
	Форма = ДополнительныеПараметры.Форма;
	ПараметрыОткрытияФормыПроверки = ДополнительныеПараметры.ПараметрыОткрытияФормыПроверки;
	ПараметрыУказанияСерий = ДополнительныеПараметры.ПараметрыУказанияСерий;
	ОткрытьФормуПроверкиИПодбораМаркируемойПродукции(Форма, ПараметрыОткрытияФормыПроверки, ПараметрыУказанияСерий);
	
КонецПроцедуры

Процедура ПриЗакрытииФормыПроверкиИПодбора(РезультатЗакрытия, ДополнительныеПараметры) Экспорт
	
	Если РезультатЗакрытия = Неопределено Тогда
		ДополнительныеПараметры.Форма.Прочитать();
	КонецЕсли;
	ПроверкаИПодборПродукцииИСКлиентПереопределяемый.ПриЗакрытииФормыПроверкиИПодбора(РезультатЗакрытия, ДополнительныеПараметры);
	
КонецПроцедуры

#КонецОбласти

Процедура ОбновитьПризнакиКонтроляОборотаМарокПоСтроке(Форма, ДанныеСтроки, Сценарий = 0)
	
	Если Не ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДанныеСтроки, "ДействуетКонтрольОборотаМарок")
		Или Не ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДанныеСтроки, "РазрешенОборотНемаркированныхОстатков") Тогда
		Возврат;
	КонецЕсли;
	
	РазрешенОборотНемаркированныхОстатков = Ложь;
	ДействуетКонтрольОборотаМарок         = Истина;
	
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "ПараметрыИнтеграцииГосИС") Тогда
		
		Если Форма.ПараметрыИнтеграцииГосИС.Получить("ИСМП") <> Неопределено Тогда
			
			СтруктураКонтроля = ПроверкаИПодборПродукцииИСМПКлиент.ОбновитьПризнакиКонтроляОборотаМарокПоСтроке(Форма, ДанныеСтроки, Сценарий);
			
			ДействуетКонтрольОборотаМарок         = СтруктураКонтроля.ДействуетКонтрольОборотаМарок;
			РазрешенОборотНемаркированныхОстатков = СтруктураКонтроля.РазрешенОборотНемаркированныхОстатков;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ДанныеСтроки.ДействуетКонтрольОборотаМарок         = ДействуетКонтрольОборотаМарок;
	ДанныеСтроки.РазрешенОборотНемаркированныхОстатков = РазрешенОборотНемаркированныхОстатков;
	
КонецПроцедуры

#КонецОбласти