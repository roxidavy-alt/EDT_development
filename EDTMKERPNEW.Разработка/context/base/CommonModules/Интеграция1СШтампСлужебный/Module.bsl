
#Область СлужебныйПрограммныйИнтерфейс

// См. Интеграция1СШтамп.РезультатУстановкиШтамповВДокумент
Функция РезультатУстановкиШтамповВДокумент(
	Знач ПараметрыУстановкиШтампов,
	Знач ПользовательскийСценарий,
	Знач ТаймаутПолученияСтатусаВыполненияЗаданияВСервисе = Неопределено) Экспорт
	
	КонтекстПроверки = "Интеграция1СШтампСлужебный.РезультатУстановкиШтамповВДокумент";
	
	ОбщегоНазначенияКлиентСервер.ПроверитьПараметр(
		КонтекстПроверки,
		"ПараметрыУстановкиШтампов",
		ПараметрыУстановкиШтампов,
		Тип("Структура"));
		
	ФорматРезультата = ПараметрыУстановкиШтампов.ПараметрыРезультата.ФорматРезультата;
	ИсходныйФормат   = ПараметрыУстановкиШтампов.ПараметрыДокумента.Расширение;
		
	МассивТиповТаймаут = Новый Массив;
	МассивТиповТаймаут.Добавить(Тип("Неопределено"));
	МассивТиповТаймаут.Добавить(Тип("Число"));
		
	ОбщегоНазначенияКлиентСервер.ПроверитьПараметр(
		КонтекстПроверки,
		"ТаймаутПолученияСтатусаВыполненияЗаданияВСервисе",
		ТаймаутПолученияСтатусаВыполненияЗаданияВСервисе,
		МассивТиповТаймаут);
	
	ОбщегоНазначенияКлиентСервер.ПроверитьПараметр(
		КонтекстПроверки,
		"ПользовательскийСценарий",
		ПользовательскийСценарий,
		Тип("Строка"));
		
	КоличествоСимволов = Интеграция1СШтампСлужебныйКлиентСервер.МаксимальноеКоличествоСимволовВОписанииПользовательскогоСценария();
		
	ТекстОшибки = СтрШаблон(
						Нстр("ru = 'Описание пользовательского сценария превышает %1 символов';
							|en = 'User scenario description exceeds %1 characters'",
							ОбщегоНазначения.КодОсновногоЯзыка()),
							КоличествоСимволов);
		
	ОбщегоНазначенияКлиентСервер.Проверить(
		СтрДлина(ПользовательскийСценарий) <= КоличествоСимволов,
		ТекстОшибки,
		КонтекстПроверки);
		
	ТекстОшибки = Нстр("ru = 'Не указано расширение документа';
						|en = 'The document extension is not specified'",
							ОбщегоНазначения.КодОсновногоЯзыка());
		
	ОбщегоНазначенияКлиентСервер.Проверить(
		Не ПустаяСтрока(ИсходныйФормат),
		ТекстОшибки,
		КонтекстПроверки);
		
	ТекстОшибки = Нстр("ru = 'Не указаны двоичные данные документа';
						|en = 'Binary document data is not specified'",
							ОбщегоНазначения.КодОсновногоЯзыка());
		
	ОбщегоНазначенияКлиентСервер.Проверить(
		ЗначениеЗаполнено(ПараметрыУстановкиШтампов.ПараметрыДокумента.ДвоичныеДанные),
		ТекстОшибки,
		КонтекстПроверки);
		
	ТекстОшибки = Нстр("ru = 'Не указаны параметры установки картинок штампов в документ';
						|en = 'Не указаны параметры установки картинок штампов в документ'",
							ОбщегоНазначения.КодОсновногоЯзыка());
		
	ОбщегоНазначенияКлиентСервер.Проверить(
		ЗначениеЗаполнено(ПараметрыУстановкиШтампов.ПараметрыШтампов),
		ТекстОшибки,
		КонтекстПроверки);
	
	ТекстОшибки = Нстр("ru = 'Не указан формат получения результата';
						|en = 'The format for getting the result is not specified'",
							ОбщегоНазначения.КодОсновногоЯзыка());
		
	ОбщегоНазначенияКлиентСервер.Проверить(
		Не ПустаяСтрока(ФорматРезультата),
		ТекстОшибки,
		КонтекстПроверки);
		
	РасширениеФайлаРезультата = РасширениеФайлаБезУчетаВерсииФормата(ФорматРезультата);
	
	Если НРег(РасширениеФайлаРезультата) <> Интеграция1СШтампСлужебныйКлиентСервер.РасширениеФайлаPDF() Тогда
	
		ТекстОшибки = СтрШаблон(
						Нстр("ru = 'Для документа с расширением %1 получение результата в исходном формате не поддерживается';
							|en = 'For a document with the %1 extension, receiving the result in the original format is not supported'",
							ОбщегоНазначения.КодОсновногоЯзыка()),
							ИсходныйФормат);
			
		ОбщегоНазначенияКлиентСервер.Проверить(
			Интеграция1СШтампСлужебныйКлиентСервер.РасширениеОсновногоФайлаПоддерживаетШтампированиеПоТегам(
				ИсходныйФормат),
			ТекстОшибки,
			КонтекстПроверки);
			
	КонецЕсли;
	
	ПараметрыУстановкиШтампов.ПараметрыРезультата.ФорматРезультата = РасширениеФайлаРезультата;
	СлужебнаяИнформацияЗапроса = СлужебнаяИнформацияЗапроса(ПользовательскийСценарий, НовыйИдентификаторТрассировки());
	
	// Создаем задание в очередь в сервисе
	ОтветСервиса = РезультатВыполненияЗапросаСоздатьЗаданиеВСервисеНаУстановкуШтамповВДокумент(
		ПараметрыУстановкиШтампов,
		СлужебнаяИнформацияЗапроса);
	
	Результат = НовыйРезультатОбработкиДокумента();
	
	Если Не ОтветСервиса.КодСостояния = Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияОжидание() Тогда
		Результат.ЕстьОшибки = Истина;
		Результат.ИнформацияОбОшибках = ОтветСервиса;
		Возврат Результат;
	КонецЕсли;
		
	Интервал     = ОтветСервиса.Интервал;
	URLРедиректа = ОтветСервиса.URLРедиректа;
	
	ДлительностьВыполненияЗапроса = ТаймаутПолученияСтатусаВыполненияЗаданияВСервисе;
	МаксимальныйТаймаут = МаксимальнаяДлительностьВыполненияЗапроса();
	
	Если Не ЗначениеЗаполнено(ТаймаутПолученияСтатусаВыполненияЗаданияВСервисе)
		Или ТаймаутПолученияСтатусаВыполненияЗаданияВСервисе > МаксимальныйТаймаут Тогда
				ДлительностьВыполненияЗапроса = МаксимальныйТаймаут;
	КонецЕсли;
	
	ГраничноеВремя = ТекущаяДатаСеанса() + ДлительностьВыполненияЗапроса;
	ОтветСервиса = РезультатПолученияСтатусаВыполненияЗадания(ГраничноеВремя, URLРедиректа, Интервал, СлужебнаяИнформацияЗапроса);
	
	Если ОтветСервиса = Неопределено Тогда
		
		ОписаниеОшибки = НСтр("ru = 'Превышена длительность ожидания получения результата добавления штампов в документ.';
								|en = 'Timeout for receiving the results of adding stamps to the document is exceeded.'",
			ОбщегоНазначения.КодОсновногоЯзыка());
		
		Результат.ЕстьОшибки = Истина;
		Результат.ИнформацияОбОшибках.ТекстОшибки = ОписаниеОшибки;
				
		Возврат Результат;
		
	КонецЕсли;
	
	Если Не ОтветСервиса.КодСостояния = Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияВыполнено()
		И Не ОтветСервиса.КодСостояния = Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияРедиректаЗапроса() Тогда
			Результат.ЕстьОшибки = Истина;
			Результат.ИнформацияОбОшибках = ОтветСервиса;
		Возврат Результат;
	КонецЕсли;
	
	URLРедиректа = ОтветСервиса.URLРедиректа;
	
	ОтветСервиса = РезультатВыполненияЗапросаПолучитьРезультатВыполненияЗаданияУстановкиШтамповВДокумент(
		URLРедиректа, СлужебнаяИнформацияЗапроса);
	
	Если ОтветСервиса.КодСостояния <> Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияСоздано() Тогда
		
		Результат.ЕстьОшибки = Истина;
		Результат.ИнформацияОбОшибках = ОтветСервиса;
		
		Возврат Результат;
		
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ОтветСервиса.Данные) Тогда
		
		ОписаниеОшибки = НСтр("ru = 'Получен пустой результат ответа.';
								|en = 'An empty response result is received.'",
			ОбщегоНазначения.КодОсновногоЯзыка());
		ЗаписьЖурналаРегистрации(НСтр("ru = 'Взаимодействие с сервисом 1С:Штамп.';
										|en = 'Interaction with 1C:Stamp service'",
			ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка, , ,
			ОписаниеОшибки);
		
		Результат.ЕстьОшибки = Истина;
		Результат.ИнформацияОбОшибках = ОтветСервиса;
		Результат.ИнформацияОбОшибках.ТекстОшибки = ОписаниеОшибки;
				
		Возврат Результат;
		
	КонецЕсли;
	
	ПроштампированныйДокумент = ИзвлеченныйФайлИзАрхива(ОтветСервиса.Данные);
	
	Если Интеграция1СШтампКлиентСервер.ЭтоПодмножествоФорматаPDF(ФорматРезультата)
		И ИсходныйФормат <> ФорматРезультата Тогда
		
		ТипыФайловPDF = Интеграция1СШтампКлиентСервер.ТипыФайловPDFДокумента();
		ТипФайла = ТипыФайловPDF.Получить(ФорматРезультата);
		
		Если ТипФайла = Неопределено Тогда
			
			ОписаниеОшибки = СтрШаблон(
				НСтр("ru = 'Неподдерживаемая версия формата PDF: %1';
					|en = 'Unsupported PDF format version: %1'", ОбщегоНазначения.КодОсновногоЯзыка()),
				ФорматРезультата);
			
			Результат.ЕстьОшибки = Истина;
			Результат.ИнформацияОбОшибках.ТекстОшибки = ОписаниеОшибки;
				
			Возврат Результат;
			
		КонецЕсли;
		
		ПреобразоватьPDFДокумент(ТипФайла, ПроштампированныйДокумент);
		
	КонецЕсли;
	
	Результат.ЕстьРезультат              = Истина;
	Результат.ИнформацияОбОшибках        = ОтветСервиса;
	Результат.ДанныеФайла.ДвоичныеДанные = ПроштампированныйДокумент;
	Результат.ДанныеФайла.Расширение     = РасширениеФайлаРезультата;
	
	Возврат Результат;
	
КонецФункции

// См. Интеграция1СШтампПовтИсп.ПараметрыСоединения
Функция ПараметрыСоединения(Знач АдресСервиса) Экспорт
	
	Если Не ЗначениеЗаполнено(АдресСервиса) Тогда
		ВызватьИсключение НСтр("ru = 'Не указан адрес сервиса';
								|en = 'The service address is not specified'", ОбщегоНазначения.КодОсновногоЯзыка());
	КонецЕсли;
	
	Результат = НовыеПараметрыСоединения(АдресСервиса, 443, Истина);
	Результат.Аутентификация = Ложь;
	Результат.Таймаут = 120;

	Возврат Результат;

КонецФункции

// См. Интеграция1СШтампПовтИсп.СлужебнаяИнформация
Функция СлужебнаяИнформация() Экспорт
	
	Информация = Новый Структура;
	
	СистемнаяИнформация = Новый СистемнаяИнформация;
	
	Информация.Вставить("ОперационнаяСистема" , Строка(СистемнаяИнформация.ТипПлатформы));
	Информация.Вставить("ВерсияПриложения"    , СистемнаяИнформация.ВерсияПриложения);
	Информация.Вставить("ИмяКонфигурации"     , Метаданные.Имя);
	Информация.Вставить("ВерсияКонфигурации"  , Метаданные.Версия);
	Информация.Вставить("ИдентификаторИБ"     , СтандартныеПодсистемыСервер.ИдентификаторИнформационнойБазы());
	Информация.Вставить("ВерсияБЭД"           , ОбновлениеИнформационнойБазыБЭД.ВерсияБиблиотеки());
	
	Возврат Информация;
	
КонецФункции

#КонецОбласти

#Область ДляВызоваИзДругихПодсистем

// См. СтандартныеПодсистемыСервер.ПриОпределенииМетодовРазрешенныхДляВызоваКакПроизвольныйКод
Процедура ПриОпределенииМетодовРазрешенныхДляВызоваКакПроизвольныйКод(Методы) Экспорт
	
	Методы.Вставить("Пауза", Истина);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область МетодыОбращенияКСервису

// Возвращает результат создания задания в очереди сервиса.
//
// Параметры:
//  ПараметрыДобавленияШтампов - См. Интеграция1СШтампКлиентСервер.НовыеПараметрыДобавленияШтамповВДокумент
//  СлужебнаяИнформацияЗапроса - См. СлужебнаяИнформацияЗапроса
// 
// Возвращаемое значение:
//  См. НовыйРезультатВыполненияЗапроса
//
Функция РезультатВыполненияЗапросаСоздатьЗаданиеВСервисеНаУстановкуШтамповВДокумент(
	Знач  ПараметрыДобавленияШтампов, Знач СлужебнаяИнформацияЗапроса)
	
	ДвоичныеДанные = ДвоичныеДанныеАрхиваСФайламиДляШтампированияДокумента(ПараметрыДобавленияШтампов);
	Результат = РезультатВыполненияПрикладныхПроверокПередОтправкойЗапроса(ДвоичныеДанные);
	
	Если Не Результат.КодСостояния = Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияВыполнено() Тогда
		Возврат Результат;
	КонецЕсли;
	
	ЗапросКСервису = ЗапросКСервисуДобавитьШтампыВДокумент(ДвоичныеДанные, СлужебнаяИнформацияЗапроса);
	Результат = ВыполнитьЗапрос(ЗапросКСервису);
	
	Возврат Результат;
	
КонецФункции

// Возвращает статус задания в сервисе.
//
// Параметры:
//  АдресКРесурсу - Строка - URL для выполнения rest запроса.
//  СлужебнаяИнформацияЗапроса - См. СлужебнаяИнформацияЗапроса
// 
// Возвращаемое значение:
//  См. НовыйРезультатВыполненияЗапроса
//
Функция РезультатВыполненияЗапросаПолучитьСтатусЗаданияУстановкиШтамповВДокумент(
	Знач АдресКРесурсу, Знач СлужебнаяИнформацияЗапроса)
	
	ЗапросКСервису = ЗапросКСервисуПолучитьСтатусВыполнения(АдресКРесурсу, СлужебнаяИнформацияЗапроса);
	Результат = ВыполнитьЗапрос(ЗапросКСервису);
	
	Возврат Результат;
	
КонецФункции

// Возвращает результат выполнения задания в сервисе.
//
// Параметры:
//  АдресКРесурсу - Строка - URL для выполнения rest запроса.
//  СлужебнаяИнформацияЗапроса - См. СлужебнаяИнформацияЗапроса
// 
// Возвращаемое значение:
//  См. НовыйРезультатВыполненияЗапроса
//
Функция РезультатВыполненияЗапросаПолучитьРезультатВыполненияЗаданияУстановкиШтамповВДокумент(
	Знач АдресКРесурсу, Знач СлужебнаяИнформацияЗапроса)
	
	ЗапросКСервису = ЗапросКСервисуПолучитьРезультатВыполнения(АдресКРесурсу, СлужебнаяИнформацияЗапроса);
	Результат = ВыполнитьЗапрос(ЗапросКСервису);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ВыполнениеЗапросов

Функция ЗапросКСервисуДобавитьШтампыВДокумент(Знач ДвоичныеДанные, Знач СлужебнаяИнформацияЗапроса)
	
	ЗапросСервиса = НовыйЗапросСервиса();
	ЗапросСервиса.СтрокаЗапроса            = АдресЗапросаДобавитьШтамп();
	ЗапросСервиса.ТипПередаваемогоКонтента = ТипИсходящегоКонтентаДанныеДокументаИШтампов();
	ЗапросСервиса.Метод                    = "POST";
	ЗапросСервиса.Данные                   = ДвоичныеДанные;
	ЗапросСервиса.Наименование             = НСтр(
		"ru = 'Добавление штампов в документ в сервисе 1С:Штамп';
		|en = 'Adding stamps to the document in 1C:Stamp'",
		ОбщегоНазначения.КодОсновногоЯзыка());
		
	ЗапросСервиса.ДопустимыеКодыСостояния.Добавить(Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияОжидание());
	УстановитьЗаголовкиЗапроса(ЗапросСервиса, СлужебнаяИнформацияЗапроса);
		
	Возврат ЗапросСервиса;
	
КонецФункции

Функция ЗапросКСервисуПолучитьСтатусВыполнения(Знач АдресОбращения, Знач СлужебнаяИнформацияЗапроса)
	
	ЗапросСервиса = НовыйЗапросСервиса();
	ЗапросСервиса.СтрокаЗапроса            = АдресОбращения;
	ЗапросСервиса.Метод                    = "GET";
	ЗапросСервиса.Наименование             = НСтр(
		"ru = 'Получение статуса добавления штампов в документ в сервисе 1С:Штамп';
		|en = 'Receiving the status of adding stamps to the document in 1C:Stamp'",
		ОбщегоНазначения.КодОсновногоЯзыка());
	
	ЗапросСервиса.ДопустимыеКодыСостояния.Добавить(Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияВыполнено());
	ЗапросСервиса.ДопустимыеКодыСостояния.Добавить(Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияРедиректаЗапроса());
	УстановитьЗаголовкиЗапроса(ЗапросСервиса, СлужебнаяИнформацияЗапроса);
		
	Возврат ЗапросСервиса;
	
КонецФункции

Функция ЗапросКСервисуПолучитьРезультатВыполнения(Знач АдресОбращения, Знач СлужебнаяИнформацияЗапроса)
	
	ЗапросСервиса = НовыйЗапросСервиса();
	ЗапросСервиса.СтрокаЗапроса            = АдресОбращения;
	ЗапросСервиса.ТипВозвращаемогоКонтента = ТипВходящегоКонтентаРезультатШтампированияАрхивСДокументом();
	ЗапросСервиса.Метод                    = "GET";
	ЗапросСервиса.Наименование             = НСтр(
		"ru = 'Получение результата добавления штампов в документ в сервисе 1С:Штамп';
		|en = 'Receiving the results of adding stamps to the document in 1C:Stamp'",
		ОбщегоНазначения.КодОсновногоЯзыка());
		
	ЗапросСервиса.ДопустимыеКодыСостояния.Добавить(Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияСоздано());
	УстановитьЗаголовкиЗапроса(ЗапросСервиса, СлужебнаяИнформацияЗапроса);
		
	Возврат ЗапросСервиса;
	
КонецФункции

Процедура УстановитьЗаголовкиЗапроса(ЗапросСервиса, Знач СлужебнаяИнформацияЗапроса)
	
	Информация = СлужебнаяИнформацияЗапроса.СлужебнаяИнформация;
	
	ЗапросСервиса.Заголовки.Вставить(ИмяЗаголовкаСлужебныйВерсияПлатформы()    , Информация.ВерсияПриложения);
	ЗапросСервиса.Заголовки.Вставить(ИмяЗаголовкаСлужебныйИмяКонфигурации()    , СтроковыеФункции.СтрокаЛатиницей(Информация.ИмяКонфигурации));
	ЗапросСервиса.Заголовки.Вставить(ИмяЗаголовкаСлужебныйВерсияКонфигурации() , Информация.ВерсияКонфигурации);
	ЗапросСервиса.Заголовки.Вставить(ИмяЗаголовкаСлужебныйВерсияБЭД()          , Информация.ВерсияБЭД);
	ЗапросСервиса.Заголовки.Вставить(ИмяЗаголовкаСлужебныйИдентификаторИБ()    , Информация.ИдентификаторИБ);
	ЗапросСервиса.Заголовки.Вставить(ИмяЗаголовкаСлужебныйОперационнаяСистема(), Информация.ОперационнаяСистема);
	ЗапросСервиса.Заголовки.Вставить(ИмяЗаголовкаИдентификаторТрассировки()    , СлужебнаяИнформацияЗапроса.ИдентификаторТрассировки);
	
	ПользовательскийСценарий = СлужебнаяИнформацияЗапроса.ПользовательскийСценарий;
	
	Если Не ПустаяСтрока(ПользовательскийСценарий) Тогда
		ЗапросСервиса.Заголовки.Вставить(
			ИмяЗаголовкаСлужебныйСценарийИспользования(),
			СтроковыеФункции.СтрокаЛатиницей(ПользовательскийСценарий));
	КонецЕсли;
	
КонецПроцедуры

Функция ВыполнитьЗапрос(Знач ЗапросСервиса)
	
	Отказ = Ложь;
	ТекстОшибки  ="";
	
	РезультатВыполнения = НовыйРезультатВыполненияЗапроса();
		
	// Установка параметров соединения.
	
	ПараметрыСоединения = Интеграция1СШтампПовтИсп.ПараметрыСоединения(ЗапросСервиса.АдресСервиса);
	
	// Установка времени ожидания ответа.
	Если ЗапросСервиса.Свойство("Таймаут") И ЗапросСервиса.Таймаут <> 0 Тогда
		Таймаут = ЗапросСервиса.Таймаут;
	Иначе
		Таймаут = ПараметрыСоединения.Таймаут;
	КонецЕсли;
	
	// Инициализация соединения с сервисом.
	Попытка
		Соединение = Новый HTTPСоединение(ПараметрыСоединения.Сервер, ПараметрыСоединения.Порт,,,
			ПараметрыСоединения.Прокси, Таймаут, ПараметрыСоединения.ЗащищенноеСоединение);
	Исключение
		ТекстОшибки = НСтр("ru = 'Отсутствует соединение с сервисом 1С:Штамп. Подробности см. в журнале регистрации.';
							|en = 'No connection with 1С:Stamp. For more information, see the Event log.'",
			ОбщегоНазначения.КодОсновногоЯзыка());
		РезультатВыполнения.ТекстОшибки  = ЗафиксироватьОшибкуВЗапросе(ПараметрыСоединения, ЗапросСервиса, ТекстОшибки);
		РезультатВыполнения.КодСостояния = Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияСервисНеОтвечает();
		Возврат РезультатВыполнения;
	КонецПопытки;

	// Установка параметров запроса.
	HTTPЗапрос = Новый HTTPЗапрос(ЗапросСервиса.СтрокаЗапроса, ЗапросСервиса.Заголовки);
	
	// Установка заголовков
	Если ЗначениеЗаполнено(ЗапросСервиса.ТипПередаваемогоКонтента) Тогда
		ИмяЗаголовкаТипПередаваемогоКонтента = НРег(ИмяЗаголовкаТипПередаваемогоКонтента());
		HTTPЗапрос.Заголовки.Вставить(ИмяЗаголовкаТипПередаваемогоКонтента,
			ЗапросСервиса.ТипПередаваемогоКонтента);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЗапросСервиса.ТипВозвращаемогоКонтента) Тогда
		ИмяЗаголовкаТипВозвращаемогоКонтента = НРег(ИмяЗаголовкаТипВозвращаемогоКонтента());
		HTTPЗапрос.Заголовки.Вставить(ИмяЗаголовкаТипВозвращаемогоКонтента,
			ЗапросСервиса.ТипВозвращаемогоКонтента);
	КонецЕсли;
		
	// Установка тела запроса.
	Если ЗначениеЗаполнено(ЗапросСервиса.Данные) Тогда
		Если ТипЗнч(ЗапросСервиса.Данные) = Тип("ДвоичныеДанные") Тогда
			HTTPЗапрос.УстановитьТелоИзДвоичныхДанных(ЗапросСервиса.Данные);
		Иначе
			HTTPЗапрос.УстановитьТелоИзСтроки(ЗапросСервиса.Данные, КодировкаТекста.UTF8,
				ИспользованиеByteOrderMark.НеИспользовать);
		КонецЕсли;
	КонецЕсли;
	
	// Выполнение запроса
	Попытка
		HTTPОтвет = Соединение.ВызватьHTTPМетод(ЗапросСервиса.Метод, HTTPЗапрос);
	Исключение
		ПодробноеОписаниеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		РезультатВыполнения.ТекстОшибки  = ЗафиксироватьОшибкуВЗапросе(Соединение, ЗапросСервиса, ПодробноеОписаниеОшибки, Отказ);
		РезультатВыполнения.КодСостояния = Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияСервисНеОтвечает();
		Возврат РезультатВыполнения;
	КонецПопытки;
	
	// Разбор тела ответа
	ОбработатьОтветСервиса(HTTPОтвет, ЗапросСервиса, РезультатВыполнения, Отказ);
	
	Если Не ПустаяСтрока(РезультатВыполнения.ТекстОшибки) Тогда
		ОбщегоНазначенияБЭД.ЗаписатьВЖурналРегистрации(РезультатВыполнения.ТекстОшибки, ИмяПодсистемы());
	КонецЕсли;
	
	Если Отказ Тогда
		РезультатВыполнения.ТекстОшибки = ЗафиксироватьОшибкуВЗапросе(ПараметрыСоединения, ЗапросСервиса, РезультатВыполнения.ТекстОшибки);
		Возврат РезультатВыполнения;
	КонецЕсли;
	
	Возврат РезультатВыполнения;
	
КонецФункции

Функция НовыйЗапросСервиса()
	
	ЗапросСервиса = Новый Структура;
	ЗапросСервиса.Вставить("АдресСервиса"                 , АдресСервиса());
	ЗапросСервиса.Вставить("Наименование"                 , "");
	ЗапросСервиса.Вставить("СтрокаЗапроса"                , "");
	ЗапросСервиса.Вставить("Метод"                        , "");
	ЗапросСервиса.Вставить("Данные"                       , Неопределено);
	ЗапросСервиса.Вставить("Ошибки"                       , Новый Соответствие);
	ЗапросСервиса.Вставить("БлокироватьСообщенияОбОшибках", Ложь);
	ЗапросСервиса.Вставить("Заголовки"                    , Новый Соответствие);
	ЗапросСервиса.Вставить("ТипПередаваемогоКонтента"     , "");
	ЗапросСервиса.Вставить("ТипВозвращаемогоКонтента"     , "");
	ЗапросСервиса.Вставить("ДопустимыеКодыСостояния"      , Новый Массив());
	
	Возврат ЗапросСервиса;
	
КонецФункции

Функция ДвоичныеДанныеАрхиваСФайламиДляШтампированияДокумента(Знач ПараметрыДобавленияШтампов)
	
	КорневойКаталог = ПолучитьИмяВременногоФайла();
	СоздатьКаталог(КорневойКаталог);
	
	ДвоичныеДанныеДокумента = ПараметрыДобавленияШтампов.ПараметрыДокумента.ДвоичныеДанные;
	РасширениеДокумента     = ПараметрыДобавленияШтампов.ПараметрыДокумента.Расширение;
	
	ИмяФайлаДокумента = СтрШаблон(ШаблонНаименованияФайлаДокумента(), РасширениеДокумента);
		
	ПутьКДокументу = КорневойКаталог
		+ ПолучитьРазделительПути()
		+ ИмяФайлаДокумента;
		
	Попытка
		ЗаписатьДвоичныеДанныеВФайл(ДвоичныеДанныеДокумента, ПутьКДокументу);
	Исключение
		ФайловаяСистема.УдалитьВременныйФайл(КорневойКаталог);
		ВызватьИсключение;
	КонецПопытки;
	
	ИмяКаталогаШтампов = КорневойКаталог
		+ ПолучитьРазделительПути()
		+ НаименованиеКаталогаСКартинкамиШтамповВнутриАрхива();
		
	СоздатьКаталог(ИмяКаталогаШтампов);
		
	ТаблицаШтампов = Новый ТаблицаЗначений;
	
	ТаблицаШтампов.Колонки.Добавить("ХешСумма"       , Новый ОписаниеТипов("Строка"));
	ТаблицаШтампов.Колонки.Добавить("ИмяФайла"       , Новый ОписаниеТипов("Строка"));
	ТаблицаШтампов.Колонки.Добавить("ДвоичныеДанные" , Новый ОписаниеТипов("ДвоичныеДанные"));
	ТаблицаШтампов.Колонки.Добавить("Расширение"     , Новый ОписаниеТипов("Строка"));
	
	ПараметрыДобавленияШтамповСлужебная = ОбщегоНазначения.СкопироватьРекурсивно(ПараметрыДобавленияШтампов);
	ДобавитьШтампыВТаблицу(ПараметрыДобавленияШтамповСлужебная.ПараметрыШтампов, ТаблицаШтампов);
	
	ТаблицаШтампов.Сортировать("ХешСумма Возр");
	
	Счетчик = 1;
	
	Для Каждого СтрокаШтампа Из ТаблицаШтампов Цикл
		
		ДвоичныеДанныеКартинки = СтрокаШтампа.ДвоичныеДанные;
		РасширениеКартинки     = СтрокаШтампа.Расширение;
		
		ИмяФайла = СтрШаблон(ШаблонНаименованияКартинкиШтампа(), Счетчик, РасширениеКартинки);
		
		ПутьКФайлуШтампа = ИмяКаталогаШтампов
			+ ПолучитьРазделительПути()
			+ ИмяФайла;
		
		СтрокаШтампа.ИмяФайла = НаименованиеКаталогаСКартинкамиШтамповВнутриАрхива()
			+ ПолучитьРазделительПути() 
			+ ИмяФайла;
			
		Попытка
			ЗаписатьДвоичныеДанныеВФайл(ДвоичныеДанныеКартинки, ПутьКФайлуШтампа);
		Исключение
			ФайловаяСистема.УдалитьВременныйФайл(КорневойКаталог);
			ВызватьИсключение;
		КонецПопытки;
		
		Счетчик = Счетчик + 1;
		
	КонецЦикла;
	
	ФайлИнструкций = ФайлИнструкцийУстановкиШтамповВДокумент(ТаблицаШтампов, ПараметрыДобавленияШтамповСлужебная, ИмяФайлаДокумента, РасширениеДокумента);
	
	ИмяФайлаИнструкций = КорневойКаталог
		+ ПолучитьРазделительПути()
		+ НаименованиеФайлаИнструкций();
		
	Попытка
		ЗаписатьДвоичныеДанныеВФайл(ФайлИнструкций, ИмяФайлаИнструкций);
	Исключение
		ФайловаяСистема.УдалитьВременныйФайл(КорневойКаталог);
		ВызватьИсключение;
	КонецПопытки;
		
	МассивПутейКФайлам = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
		КорневойКаталог + ПолучитьРазделительПути() + "*");
	
	ПараметрыФормирования = РаботаСФайламиБЭД.НовыеПараметрыФормированияАрхива();
	ПараметрыФормирования.РежимОбработкиПодкаталогов = РежимОбработкиПодкаталоговZIP.ОбрабатыватьРекурсивно;
	ПараметрыФормирования.РежимСохраненияПутей       = РежимСохраненияПутейZIP.СохранятьОтносительныеПути;
	
	Попытка
		ZIPАрхив = РаботаСФайламиБЭД.СформироватьАрхивФайлов(МассивПутейКФайлам, ,ПараметрыФормирования);
	Исключение
		ФайловаяСистема.УдалитьВременныйФайл(КорневойКаталог);
		ВызватьИсключение;
	КонецПопытки;
	
	ФайловаяСистема.УдалитьВременныйФайл(КорневойКаталог);
	
	Возврат ZIPАрхив;
	
КонецФункции

Процедура ДобавитьШтампыВТаблицу(Знач Штампы, ТаблицаШтампов)
	
	Для Каждого ДанныеШтампа Из Штампы Цикл
		
		ДвоичныеДанные = ДанныеШтампа.ПараметрыФайла.ДвоичныеДанные;
		ХешСумма = ОбщегоНазначения.КонтрольнаяСуммаСтрокой(ДвоичныеДанные, ХешФункция.MD5);
		
		ДанныеШтампа.ПараметрыФайла.Вставить("ХешСумма", ХешСумма);
		
		Отбор = Новый Структура("ХешСумма", ХешСумма);
		МассивСтрок = ТаблицаШтампов.НайтиСтроки(Отбор);
		
		Если МассивСтрок.Количество() > 0 Тогда
			Продолжить;
		КонецЕсли;
		
		Строка = ТаблицаШтампов.Добавить();
		Строка.ХешСумма       = ХешСумма;
		Строка.ДвоичныеДанные = ДвоичныеДанные;
		Строка.Расширение     = ДанныеШтампа.ПараметрыФайла.Расширение;
		
	КонецЦикла;

КонецПроцедуры

Функция ШаблонНаименованияКартинкиШтампа()
	
	Возврат "stamp%1.%2";
	
КонецФункции

Функция ШаблонНаименованияФайлаДокумента()
	
	Возврат "document.%1";
	
КонецФункции

Функция НаименованиеКаталогаСКартинкамиШтамповВнутриАрхива()
	
	Возврат "stamps";
	
КонецФункции

Функция НаименованиеФайлаИнструкций()
	
	Возврат "meta.json";
	
КонецФункции

Функция ФайлИнструкцийУстановкиШтамповВДокумент(Знач ТаблицаШтампов, Знач ПараметрыДобавленияШтампов, Знач ИмяФайлаДокумента, Знач РасширениеДокумента)
	
	КонтекстПроверки = "Интеграция1СШтампСлужебный.ФайлИнструкцийУстановкиШтамповВДокумент";
	
	СтруктураТелаЗапроса = НоваяСтруктураТелаJSON();
	
	СтруктураТелаЗапроса.document.path      = ИмяФайлаДокумента;
	СтруктураТелаЗапроса.document.extension = РасширениеДокумента;
	
	СловарьЗначенийСпособовРазмещенияНаСтранице  = СловарьЗначенийСпособовРазмещенияНаСтранице();
	СловарьЗначенийСпособовРазмещенияНаСтраницах = СловарьЗначенийСпособовРазмещенияНаСтраницах();
	СловарьСпособовУстановкиШтампаПоТегу         = СловарьСпособовУстановкиШтампаПоТегу();
	СловарьЗначенийСмещенийПоГоризонтали         = СловарьЗначенийСмещенийПоГоризонтали();
	СловарьЗначенийСтратегийРазмещенияШтампов    = СловарьЗначенийСтратегийРазмещенияШтампов();
	
	Для Каждого Данные Из ПараметрыДобавленияШтампов.ПараметрыШтампов Цикл
		
		ТекстОшибки = Нстр("ru = 'Необходимо указать параметры установки штампов по позиционированию либо по тегам';
							|en = 'Необходимо указать параметры установки штампов по позиционированию либо по тегам'",
						ОбщегоНазначения.КодОсновногоЯзыка());
		
		ОбщегоНазначенияКлиентСервер.Проверить(
				Данные.ПараметрыУстановки.ПоПозиционированию.Позиции.Количество() > 0
				Или Данные.ПараметрыУстановки.ПоТегам.Теги.Количество() > 0,
				ТекстОшибки,
				КонтекстПроверки);
		
		Если ЗначениеЗаполнено(Данные.ПараметрыУстановки.ПоТегам.Теги) Тогда
			
			ТекстОшибки = СтрШаблон(
								Нстр("ru = 'Для документа с расширением %1 установка штампов по тегам не поддерживается';
									|en = 'For a document with the %1 extension, setting stamps by tags is not supported'",
									ОбщегоНазначения.КодОсновногоЯзыка()),
									РасширениеДокумента);
				
			ОбщегоНазначенияКлиентСервер.Проверить(
				Интеграция1СШтампСлужебныйКлиентСервер.РасширениеОсновногоФайлаПоддерживаетШтампированиеПоТегам(
					РасширениеДокумента),
					ТекстОшибки,
					КонтекстПроверки);
				
		КонецЕсли;
		
		ПараметрыФайла = Данные.ПараметрыФайла;
		
		Отбор = Новый Структура("ХешСумма", ПараметрыФайла.ХешСумма);
		МассивСтрок = ТаблицаШтампов.НайтиСтроки(Отбор);
		
		ОбщегоНазначенияКлиентСервер.Проверить(
			МассивСтрок.Количество() = 1,
			НСтр("ru = 'Не удалось сформировать файл инструкций meta по причине: ""Не найдена картинка штампа в служебной таблице ""ТаблицаШтампов""""';
				|en = 'Cannot generate the meta instruction file. Reason: ""No stamp picture is found in the ""Stamp table"" internal table""'",
				ОбщегоНазначения.КодОсновногоЯзыка()),
			КонтекстПроверки);
		
		НайденнаяСтрока = МассивСтрок[0];
		
		СтруктураСвойствШтампа = НоваяСтруктураСвойствПараметровУстановкиШтампа();
		
		СтруктураСвойствШтампа.file.path                        = НайденнаяСтрока.ИмяФайла;
		СтруктураСвойствШтампа.file.extension                   = ПараметрыФайла.Расширение;
		СтруктураСвойствШтампа.transformation.coefficient       = Данные.Настройки.КоэффициентИзмененияРазмера;
		СтруктураСвойствШтампа.transformation.transparencyLevel = Данные.Настройки.УровеньПрозрачности;
		
		ЗаполнитьРазмещениеШтампов(Данные.ПараметрыУстановки, СтруктураСвойствШтампа.placements);
		
		СтруктураТелаЗапроса.stamps.Добавить(СтруктураСвойствШтампа);
		
	КонецЦикла;
	
	Для Каждого СтратегияРазмещения Из ПараметрыДобавленияШтампов.СтратегииРазмещения Цикл
		
		СтруктураСтратегииРазмещения = НоваяСтруктураСвойствСтратегииРазмещения();
		
		Если ЗначениеЗаполнено(СтратегияРазмещения.Размещение.НаСтранице) Тогда
			СтруктураСтратегииРазмещения.placement.position = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
				СловарьЗначенийСпособовРазмещенияНаСтранице,
				СтратегияРазмещения.Размещение.НаСтранице);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтратегияРазмещения.Размещение.НаСтраницах.СпособРасположения) Тогда
			СтруктураСтратегииРазмещения.placement.pages.rule = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
				СловарьЗначенийСпособовРазмещенияНаСтраницах,
				СтратегияРазмещения.Размещение.НаСтраницах.СпособРасположения);
		КонецЕсли;
			
		Если ЗначениеЗаполнено(СтратегияРазмещения.Размещение.НаСтраницах.НомерСтраницы) Тогда
			СтруктураСтратегииРазмещения.placement.pages.number = СтратегияРазмещения.Размещение.НаСтраницах.НомерСтраницы;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтратегияРазмещения.Статегия.Направление) Тогда
			СтруктураСтратегииРазмещения.strategy.route = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
				СловарьЗначенийСтратегийРазмещенияШтампов,
				СтратегияРазмещения.Статегия.Направление);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтратегияРазмещения.Статегия.СмещениеПоГоризонтали) Тогда
			СтруктураСтратегииРазмещения.strategy.horizontalVisit = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
				СловарьЗначенийСмещенийПоГоризонтали,
				СтратегияРазмещения.Статегия.СмещениеПоГоризонтали);
		КонецЕсли;
		
		СтруктураТелаЗапроса.placementStrategies.Добавить(СтруктураСтратегииРазмещения);
		
	КонецЦикла;
	
	СтруктураТелаЗапроса.result.extension = ПараметрыДобавленияШтампов.ПараметрыРезультата.ФорматРезультата;
	
	НормализоватьСтруктуруТелаЗапроса(СтруктураТелаЗапроса);
	
	JSONСтрока = ОбщегоНазначенияБЭД.JSONСтрока(СтруктураТелаЗапроса);
	
	Возврат ПолучитьДвоичныеДанныеИзСтроки(JSONСтрока, КодировкаТекста.UTF8, Ложь);
	
КонецФункции

Процедура ЗаполнитьРазмещениеШтампов(Знач ПараметрыУстановки, СтруктураСвойствШтампа, УровеньВложенности = 1)
	
	КонтекстПроверки = "Интеграция1СШтампСлужебный.ЗаполнитьРазмещениеШтампов";
	
	СловарьЗначенийСпособовРазмещенияНаСтранице  = СловарьЗначенийСпособовРазмещенияНаСтранице();
	СловарьЗначенийСпособовРазмещенияНаСтраницах = СловарьЗначенийСпособовРазмещенияНаСтраницах();
	СловарьСпособовУстановкиШтампаПоТегу         = СловарьСпособовУстановкиШтампаПоТегу();
	
	Для Каждого ПараметрыУстановкиШтамповПоПозиционированию Из ПараметрыУстановки.ПоПозиционированию.Позиции Цикл
		
		ОтступОтГраниц = ПараметрыУстановкиШтамповПоПозиционированию.НаСтранице.ОтступОтГраниц;
		ОтступСверху   = ПараметрыУстановкиШтамповПоПозиционированию.НаСтранице.Координаты.Верх;
		ОтступСлева    = ПараметрыУстановкиШтамповПоПозиционированию.НаСтранице.Координаты.Лево;
		НомерСтраницы  = ПараметрыУстановкиШтамповПоПозиционированию.НаСтраницах.НомерСтраницы;
		
		ТекстОшибки = НСтр("ru = 'Не удалось сформировать файл инструкций meta по причине: ""В качестве значения отступа от границ документа может быть указано только целое число""';
							|en = 'Не удалось сформировать файл инструкций meta по причине: ""В качестве значения отступа от границ документа может быть указано только целое число""'",
			ОбщегоНазначения.КодОсновногоЯзыка());
		
		ПроверитьЦелоеЧисло(ОтступОтГраниц, ТекстОшибки, КонтекстПроверки);
		
		ТекстОшибки = НСтр("ru = 'Не удалось сформировать файл инструкций meta по причине: ""В качестве значения отступа от верхней границы документа может быть указано только целое число""';
							|en = 'Не удалось сформировать файл инструкций meta по причине: ""В качестве значения отступа от верхней границы документа может быть указано только целое число""'",
			ОбщегоНазначения.КодОсновногоЯзыка());
				
		ПроверитьЦелоеЧисло(ОтступСверху, ТекстОшибки, КонтекстПроверки);
		
		ТекстОшибки = НСтр("ru = 'Не удалось сформировать файл инструкций meta по причине: ""В качестве значения отступа от левой границы документа может быть указано только целое число""';
							|en = 'Не удалось сформировать файл инструкций meta по причине: ""В качестве значения отступа от левой границы документа может быть указано только целое число""'",
			ОбщегоНазначения.КодОсновногоЯзыка());
			
		ПроверитьЦелоеЧисло(ОтступСлева, ТекстОшибки, КонтекстПроверки);
		
		ТекстОшибки = НСтр("ru = 'Не удалось сформировать файл инструкций meta по причине: ""В качестве значения номера страницы документа может быть указано только целое число""';
							|en = 'Не удалось сформировать файл инструкций meta по причине: ""В качестве значения номера страницы документа может быть указано только целое число""'",
			ОбщегоНазначения.КодОсновногоЯзыка());
			
		ПроверитьЦелоеЧисло(НомерСтраницы, ТекстОшибки, КонтекстПроверки);
		
		ПараметрыУстановкиШтампа = НоваяСтруктураСвойствУстановкиШтампаПоПозиции();
		
		Если ЗначениеЗаполнено(ПараметрыУстановкиШтамповПоПозиционированию.НаСтраницах.Расположение) Тогда
			ПараметрыУстановкиШтампа.location.pages.rule = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
				СловарьЗначенийСпособовРазмещенияНаСтраницах,
				ПараметрыУстановкиШтамповПоПозиционированию.НаСтраницах.Расположение);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПараметрыУстановкиШтамповПоПозиционированию.НаСтранице.Расположение) Тогда
			ПараметрыУстановкиШтампа.location.sheet.position = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
				СловарьЗначенийСпособовРазмещенияНаСтранице,
				ПараметрыУстановкиШтамповПоПозиционированию.НаСтранице.Расположение);
		КонецЕсли;
			
		ПараметрыУстановкиШтампа.location.pages.number = НомерСтраницы;
		
		ПараметрыУстановкиШтампа.location.sheet.coordinates.top  = ОтступСверху;
		ПараметрыУстановкиШтампа.location.sheet.coordinates.left = ОтступСлева;
		ПараметрыУстановкиШтампа.location.sheet.indentation      = ОтступОтГраниц;
		
		СтруктураСвойствШтампа.byPosition.positions.Добавить(ПараметрыУстановкиШтампа);
		
	КонецЦикла;
	
	Для Каждого ПараметрыУстановкиШтампаПоТегу Из ПараметрыУстановки.ПоТегам.Теги Цикл
		
		ОбщегоНазначенияКлиентСервер.Проверить(
			ЗначениеЗаполнено(ПараметрыУстановкиШтампаПоТегу.ЗначениеТега),
			НСтр("ru = 'Не удалось сформировать файл инструкций meta по причине: ""Не указано значение тега для установки картинки штампа""';
				|en = 'Cannot generate the meta instruction file. Reason: ""No tag value is specified for setting stamp picture""'",
				ОбщегоНазначения.КодОсновногоЯзыка()),
			КонтекстПроверки);
				
		КоличествоСимволов = Интеграция1СШтампСлужебныйКлиентСервер.МинимальноеКоличествоСимволовТекстаТега();
		
		ОбщегоНазначенияКлиентСервер.Проверить(
			СтрДлина(ПараметрыУстановкиШтампаПоТегу.ЗначениеТега) >= КоличествоСимволов,
			СтрШаблон(
				Нстр("ru = 'Не удалось сформировать файл инструкций meta по причине:""Количество символов в значении текста тега не может быть меньше, чем %1 символов""';
					|en = 'Не удалось сформировать файл инструкций meta по причине:""Количество символов в значении текста тега не может быть меньше, чем %1 символов""'",
					ОбщегоНазначения.КодОсновногоЯзыка()),
				КоличествоСимволов),
			КонтекстПроверки);
		
		ПараметрыУстановкиШтампа = НоваяСтруктураСвойствУстановкиШтампаПоТегу();
		
		ПараметрыУстановкиШтампа.value   = ПараметрыУстановкиШтампаПоТегу.ЗначениеТега;
		ПараметрыУстановкиШтампа.replace = ПараметрыУстановкиШтампаПоТегу.ЗамещатьЗначениеТега;
		
		Если ЗначениеЗаполнено(ПараметрыУстановкиШтампаПоТегу.СпособУстановкиШтампа) Тогда
			ПараметрыУстановкиШтампа.location = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
				СловарьСпособовУстановкиШтампаПоТегу,
				ПараметрыУстановкиШтампаПоТегу.СпособУстановкиШтампа);
		КонецЕсли;
			
		СтруктураСвойствШтампа.byTags.tags.Добавить(ПараметрыУстановкиШтампа);
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(ПараметрыУстановки.ПоТегам.АльтернативныйСпособУстановки) Тогда
		
		ОбщегоНазначенияКлиентСервер.Проверить(
			УровеньВложенности < Интеграция1СШтампСлужебныйКлиентСервер.ДопустимыйУровеньВложенностиПараметровРазмещенияШтампов(),
			НСтр("ru = 'Не удалось сформировать файл инструкций meta по причине: ""Превышен максимальный размер уровня вложенности параметров размещениия картинок штампов.""';
				|en = 'Не удалось сформировать файл инструкций meta по причине: ""Превышен максимальный размер уровня вложенности параметров размещениия картинок штампов.""'",
				ОбщегоНазначения.КодОсновногоЯзыка()),
			КонтекстПроверки);
		
		СтруктураСвойствШтампа.byTags.ifNotFound = НоваяСтруктураСвойствРазмещенияШтампов();
		
		УровеньВложенности = УровеньВложенности + 1;
		
		ЗаполнитьРазмещениеШтампов(
			ПараметрыУстановки.ПоТегам.АльтернативныйСпособУстановки,
			СтруктураСвойствШтампа.byTags.ifNotFound,
			УровеньВложенности);
			
	КонецЕсли;
	
КонецПроцедуры

Процедура НормализоватьСтруктуруТелаЗапроса(Структура)
	
	Для Каждого Свойство Из Структура Цикл
		
		Если ТипЗнч(Свойство.Значение) = Тип("Массив") И ЗначениеЗаполнено(Свойство.Значение) Тогда
			Для Каждого Элемент Из Свойство.Значение Цикл
				НормализоватьСтруктуруТелаЗапроса(Элемент);
			КонецЦикла;
		ИначеЕсли ТипЗнч(Свойство.Значение) = Тип("Структура") Тогда
			НормализоватьСтруктуруТелаЗапроса(Свойство.Значение);
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(Свойство.Значение)
			Или Свойство.Ключ = "coefficient"
			И Свойство.Значение = 1
			Или Свойство.Ключ = "transparencyLevel"
			И Свойство.Значение = Интеграция1СШтампСлужебныйКлиентСервер.ЗначениеКоэффициентаПрозрачностиШтампаНепрозрачный() Тогда
				Структура.Удалить(Свойство.Ключ);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаписатьДвоичныеДанныеВФайл(Знач ДвоичныеДанные, Знач ПутьКФайлу)
	
	Попытка
		ДвоичныеДанные.Записать(ПутьКФайлу);
	Исключение
		
		ПодробноеОписаниеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ОбщегоНазначенияБЭД.ЗаписатьВЖурналРегистрации(ПодробноеОписаниеОшибки, ИмяПодсистемы());
		
		ВызватьИсключение;
		
	КонецПопытки;
	
КонецПроцедуры

Функция ЗафиксироватьОшибкуВЗапросе(Знач ПараметрыСоединения, Знач ЗапросСервиса,
	Знач ТекстОшибки, ЕстьОшибки = Неопределено)
	
	ЕстьОшибки = Истина;
	ПодробноеОписаниеОшибки = "";
	
	ПредставлениеОшибки = Новый Массив;
	ПредставлениеОшибки.Добавить(СтрШаблон(НСтр("ru = 'Ошибка взаимодействия с сервисом 1С:Штамп';
												|en = '1С:Stamp interaction error'")));
	ПредставлениеОшибки.Добавить(СтрШаблон(НСтр("ru = 'Хост: %1';
												|en = 'Host: %1'"), ПараметрыСоединения.Сервер));
	ПредставлениеОшибки.Добавить(СтрШаблон(НСтр("ru = 'Запрос: %1 %2.';
												|en = 'Request: %1 %2.'"), ЗапросСервиса.Метод,
		ЗапросСервиса.СтрокаЗапроса));
	ПредставлениеОшибки.Добавить(СтрШаблон(НСтр("ru = 'Текст ошибки: %1';
												|en = 'Error text: %1'"), ТекстОшибки));
	
	ПодробноеОписаниеОшибки = СтрСоединить(ПредставлениеОшибки, Символы.ПС);

	ОбщегоНазначенияБЭД.ЗаписатьВЖурналРегистрации(ПодробноеОписаниеОшибки, ИмяПодсистемы());
	
	ПредставлениеОшибкиДляПользователя = НСтр("ru = 'Произошла ошибка работы с сервисом 1С:Штамп.
										|Подробности см. в журнале регистрации.';
										|en = 'An error occurred when using 1C:Stamp.
										|For more information, see the Event log.'");
	
	ОбщегоНазначения.СообщитьПользователю(ПредставлениеОшибкиДляПользователя);

	Возврат ПодробноеОписаниеОшибки;

КонецФункции

Функция ОбработатьОтветСервиса(Знач HTTPОтвет, Знач ЗапросСервиса, Результат, Отказ)
	
	ТекстОшибки = "";
	Результат.КодСостояния = HTTPОтвет.КодСостояния;
	
	Если Не ЗапросВыполненКорректно(ЗапросСервиса, HTTPОтвет, ТекстОшибки) Тогда
		Результат.ТекстОшибки = ТекстОшибки;
		Отказ = Истина;
		Возврат Результат;
	КонецЕсли;
	
	Если Результат.КодСостояния =  Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияВыполнено()
		Или Результат.КодСостояния =  Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияОжидание() Тогда
			Результат.Интервал = ИнтервалПовторногоОбращения(HTTPОтвет, ТекстОшибки);
			Результат.URLРедиректа = URLРедиректа(HTTPОтвет, ТекстОшибки);
	ИначеЕсли Результат.КодСостояния =  Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияРедиректаЗапроса() Тогда
		Результат.URLРедиректа = URLРедиректа(HTTPОтвет, ТекстОшибки);
	КонецЕсли;
	
	Результат.ТекстОшибки = ТекстОшибки;
	Результат.Данные = HTTPОтвет.ПолучитьТелоКакДвоичныеДанные();
	
	Возврат Результат;
	
КонецФункции

Функция ЗапросВыполненКорректно(Знач ЗапросСервиса, Знач HTTPОтвет, ТекстОшибки = "")
	
	КодСостояния = HTTPОтвет.КодСостояния;
	
	Если ЗапросСервиса.ДопустимыеКодыСостояния.Количество() = 0
		Или ЗапросСервиса.ДопустимыеКодыСостояния.Найти(КодСостояния) <> Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	
	Если ЗапросСервиса.Ошибки.Получить(КодСостояния) <> Неопределено Тогда
		ТекстОшибки = ЗапросСервиса.Ошибки.Получить(КодСостояния);
		Возврат Ложь;
	КонецЕсли;
	
	КодЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
	
	ПодстрокиОшибки = Новый Массив;
	Если ЕстьПодробноеОписаниеОшибки(HTTPОтвет) Тогда
		ОписаниеОшибки = ОбщегоНазначенияБЭД.JSONЗначение(HTTPОтвет.ПолучитьТелоКакПоток());
		
		ПодстрокиОшибки.Добавить(СтрШаблон(НСтр("ru = 'Код состояния %1 - %2';
												|en = 'State code %1 - %2'", КодЯзыка),
			XMLСтрока(КодСостояния), ОписаниеОшибки.title));
		
		ИдентификаторТрассировки = ЗначениеКоллекцииПоАбсолютномуПути(ОписаниеОшибки, "traceId");
		Если ЗначениеЗаполнено(ИдентификаторТрассировки) Тогда
			ПодстрокиОшибки.Добавить(СтрШаблон(НСтр("ru = 'Идентификатор трассировки: %1';
													|en = 'Tracing ID: %1'", КодЯзыка),
			ИдентификаторТрассировки));
		КонецЕсли;
		
	Иначе
		ТекстОшибки = ИнтернетСоединениеБЭД.РасшифровкаКодаСостоянияHTTP(КодСостояния);
		Если Не ЗначениеЗаполнено(ТекстОшибки) Тогда
			Если КодСостояния >= 500 Тогда
				ТекстОшибки = НСтр("ru = 'внутренняя ошибка сервиса';
									|en = 'internal service error'", КодЯзыка);
			ИначеЕсли КодСостояния >= 400 Тогда
				ТекстОшибки = НСтр("ru = 'ошибка параметров запроса';
									|en = 'request parameter error'", КодЯзыка);
			Иначе
				ТекстОшибки = НСтр("ru = 'неизвестная ошибка';
									|en = 'unknown error'", КодЯзыка);
			КонецЕсли;
		КонецЕсли;
		
		ПодстрокиОшибки.Добавить(СтрШаблон(НСтр("ru = 'Код состояния %1 - %2';
												|en = 'State code %1 - %2'", КодЯзыка),
			XMLСтрока(КодСостояния), ТекстОшибки));
		
		Тело = "";
		Попытка
			Тело = HTTPОтвет.ПолучитьТелоКакСтроку();
			Если Не ПустаяСтрока(Тело) Тогда
				ПодстрокиОшибки.Добавить(СтрШаблон(НСтр("ru = 'Тело ответа: %1';
														|en = 'Response body: %1'", КодЯзыка), Тело));
			КонецЕсли;
		Исключение
			ПодробноеОписаниеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ОбщегоНазначенияБЭД.ЗаписатьВЖурналРегистрации(ПодробноеОписаниеОшибки, ИмяПодсистемы());
		КонецПопытки;
	КонецЕсли;
	
	ТекстОшибки = СтрСоединить(ПодстрокиОшибки, Символы.ПС);
	
	Возврат Ложь;
	
КонецФункции

Функция ЕстьПодробноеОписаниеОшибки(Знач HTTPОтвет)
	
	ИмяЗаголовкаТипВозвращаемогоКонтента = НРег(ИмяЗаголовкаТипВозвращаемогоКонтента());
	Возврат HTTPОтвет.Заголовки.Получить(ИмяЗаголовкаТипВозвращаемогоКонтента) = ТипВходящегоКонтентаОшибка();
	
КонецФункции

Функция URLРедиректа(Знач HTTPОтвет, ТекстОшибки = Неопределено)
	
	ИмяЗаголовкаРедиректа = НРег(ИмяЗаголовкаРедиректа());
	
	Результат = HTTPОтвет.Заголовки.Получить(ИмяЗаголовкаРедиректа);

	Если Результат <> Неопределено Тогда
		Результат = СокрЛП(Результат);
		Если ПустаяСтрока(Результат) Тогда
			Результат = Неопределено;
		КонецЕсли;
	КонецЕсли;

	Если Результат = Неопределено Тогда
		ТекстОшибки = СтрШаблон(
			НСтр("ru = 'Ошибка при выполнении запроса %1. Некорректное перенаправление,
				 | отсутствует HTTP-заголовок ответа ""location"".';
				 |en = 'An error occurred when executing the %1 request. Incorrect redirection.
				 | No HTTP location response header.'", ОбщегоНазначения.КодОсновногоЯзыка()), HTTPОтвет);
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

Функция ИнтервалПовторногоОбращения(Знач HTTPОтвет, ТекстОшибки = Неопределено)
	
	ИмяЗаголовкаПериодОжидания = НРег(ИмяЗаголовкаПериодОжидания());
	Интервал = Интеграция1СШтампСлужебныйКлиентСервер.ЗначениеИнтервалаПовторногоОбращенияКСервисуПоУмолчанию();
	
	Результат = HTTPОтвет.Заголовки.Получить(ИмяЗаголовкаПериодОжидания);
	
	Если Результат = Неопределено Тогда
		ТекстОшибки = СтрШаблон(
			НСтр("ru = 'Ошибка при выполнении запроса %1.
				 | Отсутствует HTTP-заголовок ответа ""retry-after"".';
				 |en = 'An error occurred when executing the %1 request.
				 | No HTTP header for the ""retry-after"" response.'", ОбщегоНазначения.КодОсновногоЯзыка()), HTTPОтвет);
		Возврат Интервал;
	Иначе
		Результат = СокрЛП(Результат);
		Если ПустаяСтрока(Результат) Тогда
			Возврат Интервал;
		КонецЕсли;
	КонецЕсли;
	
	ЧисловойТип = Новый ОписаниеТипов("Число");
	Результат = ЧисловойТип.ПривестиЗначение(Результат);
	
	Возврат Результат;
	
КонецФункции

Функция НовыйИдентификаторТрассировки()
	
	Возврат Строка(Новый УникальныйИдентификатор());
	
КонецФункции

// Возвращает конструктор параметров результата выполнения запроса
//
// Возвращаемое значение:
// 	Структура: 
//    * СлужебнаяИнформация - См. СлужебнаяИнформация.
//    * ПользовательскийСценарий - Строка - Строковое описание без пробелов (максимум 250 символов), информирующее
//      о месте использования механики 1С:Штамп внутри конфигурации.
//    * ИдентификаторТрассировки - Строка - Строковое представление уникального идентификатора.
//
Функция СлужебнаяИнформацияЗапроса(Знач ПользовательскийСценарий, Знач ИдентификаторТрассировки)
	
	Информация = Новый Структура;
	Информация.Вставить("СлужебнаяИнформация", СлужебнаяИнформация());
	Информация.Вставить("ПользовательскийСценарий", ПользовательскийСценарий);
	Информация.Вставить("ИдентификаторТрассировки", ИдентификаторТрассировки);
	
	Возврат Информация;
	
КонецФункции

// Возвращает конструктор параметров результата выполнения запроса
//
// Возвращаемое значение:
// 	Структура: 
//    * КодСостояния - Число     - Код состояния http ответа.
//    * Данные       - Структура - Данные ответа сервиса.
//    * URLРедиректа - Строка    - URL адрес для повторного обращения.
//    * Интервал     - Число     - Интервал (в секундах) для повторного обращения к сервису.
//    * ТекстОшибки  - Строка    - Текст ошибки на стороне сервиса.
//
Функция НовыйРезультатВыполненияЗапроса()
	
	Результат = Новый Структура;
	Результат.Вставить("КодСостояния"  , 200);
	Результат.Вставить("Данные"      );
	Результат.Вставить("URLРедиректа"  , "");
	Результат.Вставить("Интервал"      , 0);
	Результат.Вставить("ТекстОшибки"   , "");
	
	Возврат Результат;
	
КонецФункции

// Возвращает конструктор параметров результата выполнения запроса
//
// Возвращаемое значение:
// 	Структура: 
//    * ЕстьОшибки          - Булево         - Признак наличия ошибок.
//    * ИнформацияОбОшибках - см. НовыйРезультатВыполненияЗапроса
//    * ЕстьРезультат       - Булево         - Признак успешного выполнения запроса.
//    * ДвоичныеДанные      - ДвоичныеДанные - Двоичные данные результата.
//
Функция НовыйРезультатОбработкиДокумента() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ЕстьОшибки"         , Ложь);
	Результат.Вставить("ИнформацияОбОшибках", НовыйРезультатВыполненияЗапроса());
	Результат.Вставить("ЕстьРезультат"      , Истина);
	Результат.Вставить("ДанныеФайла"        , Интеграция1СШтампКлиентСервер.НовыеПараметрыФайла());
	
	Возврат Результат;
	
КонецФункции

Функция РезультатПолученияСтатусаВыполненияЗадания(Знач ГраничноеВремя, Знач URLРедиректа, Знач Интервал, Знач СлужебнаяИнформацияЗапроса)
		
	Пока Истина Цикл
		
		Пауза(Интервал);
		
		Результат = РезультатВыполненияЗапросаПолучитьСтатусЗаданияУстановкиШтамповВДокумент(URLРедиректа, СлужебнаяИнформацияЗапроса);
		
		Если Результат.КодСостояния <> Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияВыполнено() Тогда
			Прервать;
		КонецЕсли;
		
		Если ГраничноеВремя <= ТекущаяДатаСеанса() Тогда
			ОписаниеОшибки = НСтр("ru = 'Превышена длительность ожидания получения файла.';
									|en = 'File receipt timeout is exceeded.'");
			ЗаписьЖурналаРегистрации(НСтр("ru = 'Взаимодействие с сервисом 1С:Штамп.';
											|en = 'Interaction with 1C:Stamp service'",
				ОбщегоНазначения.КодОсновногоЯзыка()),
				УровеньЖурналаРегистрации.Ошибка, , ,
				ОписаниеОшибки);
			Возврат Неопределено;
		КонецЕсли;
		
		Таймаут = ТаймаутОжидания();
		Если Интервал > Таймаут Тогда
			Интервал = Таймаут;
		КонецЕсли;
		
		Интервал     = Результат.Интервал;
		URLРедиректа = Результат.URLРедиректа;
		
	КонецЦикла;
		
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ПараметрыАдресаКСервису

Функция АдресСервиса()
	
	Возврат "stamp-api.1c.ru";
	
КонецФункции

Функция ИмяПодсистемы()
	
	Возврат ОбщегоНазначенияБЭДКлиентСервер.ПодсистемыБЭД().СервисШтампированияФайлов;
	
КонецФункции

Функция АдресЗапросаДобавитьШтамп()
	
	Возврат "stamp";
	
КонецФункции 

#КонецОбласти

#Область Словарь

Функция ТипИсходящегоКонтентаДанныеДокументаИШтампов()
	
	Возврат "application/vnd.1cstamp.stamp.v5+zip";
	
КонецФункции

Функция ТипВходящегоКонтентаРезультатШтампированияАрхивСДокументом()
	
	Возврат "application/vnd.1cstamp.stampResult+zip";
	
КонецФункции

Функция ТипВходящегоКонтентаОшибка()
	
	Возврат "application/problem+json";
	
КонецФункции

Функция ИмяЗаголовкаРедиректа()
	
	Возврат "Location";
	
КонецФункции

Функция ИмяЗаголовкаПериодОжидания()
	
	Возврат "Retry-After";
	
КонецФункции

Функция ИмяЗаголовкаТипПередаваемогоКонтента()
	
	Возврат "Content-Type";
	
КонецФункции

Функция ИмяЗаголовкаТипВозвращаемогоКонтента()
	
	Возврат "Accept";
	
КонецФункции

Функция ИмяЗаголовкаСлужебныйВерсияПлатформы()
	
	Возврат "x-vnd-1cstamp-platform-version";
	
КонецФункции

Функция ИмяЗаголовкаСлужебныйИмяКонфигурации()
	
	Возврат "x-vnd-1cstamp-app-name";
	
КонецФункции

Функция ИмяЗаголовкаСлужебныйВерсияКонфигурации()
	
	Возврат "x-vnd-1cstamp-app-version";
	
КонецФункции

Функция ИмяЗаголовкаСлужебныйВерсияБЭД()
	
	Возврат "x-vnd-1cstamp-led-version";
	
КонецФункции

Функция ИмяЗаголовкаСлужебныйСценарийИспользования()
	
	Возврат "x-vnd-1cstamp-scenario";
	
КонецФункции

Функция ИмяЗаголовкаСлужебныйИдентификаторИБ()
	
	Возврат "x-vnd-1cstamp-endpointId";
	
КонецФункции

Функция ИмяЗаголовкаСлужебныйОперационнаяСистема()
	
	Возврат "x-vnd-1cstamp-os";
	
КонецФункции

Функция ИмяЗаголовкаИдентификаторТрассировки()
	
	Возврат "x-trace-id";
	
КонецФункции

#КонецОбласти

// Извлекает двоичные данные файла из архива.
//
// Параметры:
//  ДвоичныеДанные - ДвоичныеДанные - Двоичные данные архива.
// 
// Возвращаемое значение:
//  ДвоичныеДанные - Двоичные данные файла в архиве.
//
Функция ИзвлеченныйФайлИзАрхива(ДвоичныеДанные)
	
	Событие = НСтр("ru = 'Извлечение файла из архива. 1С:Штамп';
					|en = 'Extract the file from the archive. 1C:Stamp'", ОбщегоНазначения.КодОсновногоЯзыка());
	
	Каталог = ПолучитьИмяВременногоФайла();
	
	Попытка
		
		МассивФайлов = РаботаСФайламиБЭД.РаспаковатьАрхив(ДвоичныеДанные, Каталог, Неопределено);
		КоличествоФайлов = МассивФайлов.Количество();
		
		Если КоличествоФайлов = 0 Тогда
			ОписаниеОшибки = НСтр(
					"ru = 'Не найдены файлы в архиве.';
					|en = 'No files are found in the archive.'",
					ОбщегоНазначения.КодОсновногоЯзыка());
					
			ВызватьИсключение ОписаниеОшибки;
			
		КонецЕсли;
		
		Если КоличествоФайлов > 1 Тогда
			ОписаниеОшибки = НСтр(
					"ru = 'Обнаружено более одного файла в архиве.';
					|en = 'More than one file is found in the archive.'",
					ОбщегоНазначения.КодОсновногоЯзыка());
					
			ВызватьИсключение ОписаниеОшибки;
			
		КонецЕсли;
		
		ИмяФайла = МассивФайлов[0].Имя;
		
	Исключение
		
		ОписаниеОшибки = СтрШаблон(НСтр(
					"ru = 'Не удалось извлечь файл из архива по причине: %1.';
					|en = 'Cannot extract the file from the archive. Reason: %1.'",
					ОбщегоНазначения.КодОсновногоЯзыка()),
					ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
					
		ЗаписьЖурналаРегистрации(Событие,
			УровеньЖурналаРегистрации.Ошибка, , , ОписаниеОшибки);
			
		ВызватьИсключение ОписаниеОшибки;
		
	КонецПопытки;
	
	ДанныеФайла = Новый ДвоичныеДанные(Каталог + ПолучитьРазделительПути() + ИмяФайла);
	
	Событие = НСтр("ru = 'Удаление временных файлов';
					|en = 'Delete the temporary files'", ОбщегоНазначения.КодОсновногоЯзыка());
	
	Попытка
		УдалитьФайлы(Каталог);
	Исключение
		
		ОписаниеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
					
		ЗаписьЖурналаРегистрации(Событие,
			УровеньЖурналаРегистрации.Ошибка, , , ОписаниеОшибки);
			
	КонецПопытки;

	Возврат ДанныеФайла;
	
КонецФункции

// Возвращает структуру тела запроса JSON.
//
// Возвращаемое значение:
//   Структура: 
//    * document            - см. НоваяСтруктураСвойствФайла.
//    * stamps              - Массив из см. Интеграция1СШтампКлиентСервер.НовыеПараметрыШтампа.
//    * placementStrategies - Массив из см. Интеграция1СШтампКлиентСервер.НоваяСтратегияРазмещенияШтампов.
//    * result              - Структура - Параметры получения результата:
//      ** extension - Строка - Расширение файла результата.
//
Функция НоваяСтруктураТелаJSON()
	
	СвойстваРезультата = Новый Структура;
	СвойстваРезультата.Вставить("extension", "");
	
	СтруктураТела = Новый Структура;
	
	СтруктураТела.Вставить("document"           , НоваяСтруктураСвойствФайла());
	СтруктураТела.Вставить("stamps"             , Новый Массив);
	СтруктураТела.Вставить("placementStrategies", Новый Массив);
	СтруктураТела.Вставить("result"             , СвойстваРезультата);
	
	Возврат СтруктураТела;
	
КонецФункции

// Возвращает структуру свойств параметров установки штампа.
//
// Возвращаемое значение:
//   Структура: 
//    * file           - см. НоваяСтруктураСвойствФайла.
//    * placements     - см. НоваяСтруктураСвойствРазмещенияШтампов.
//    * transformation - см. НоваяСтруктураСвойствКартинкиШтампа.
//
Функция НоваяСтруктураСвойствПараметровУстановкиШтампа()
	
	СтруктураПараметров = Новый Структура;
	
	СтруктураПараметров.Вставить("file"          , НоваяСтруктураСвойствФайла());
	СтруктураПараметров.Вставить("placements"    , НоваяСтруктураСвойствРазмещенияШтампов());
	СтруктураПараметров.Вставить("transformation", НоваяСтруктураСвойствКартинкиШтампа());
	
	Возврат СтруктураПараметров;
	
КонецФункции

// Возвращает структуру свойств установки штампа по позиции.
//
// Возвращаемое значение:
//   Структура: 
//    * location - Структура - Параметры размещения картинки штампа в документе:
//      ** pages - Структура - Параметры расположения на страницах:
//         *** rule       - Строка - Способ расположения картинки штампа на страницах документа.
//         *** pageNumber - Число  - Номер страницы документа для расположения картинки штампа.
//      ** sheet - Структура - Параметры расположения на странице документа:
//        *** position    - Строка    - Способ размещения картинки штампа на странице документа.
//        *** indentation - Число     - Отступ от границ страницы документа в мм при установке картинки штампа.
//        *** coordinates - Структура - Данные координат размещения в мм:
//           **** top  - Число - Значение отступа в мм от верхней границы страницы документа.
//           **** left - Число - Значение отступа в мм от левой границы страницы документа.
//
Функция НоваяСтруктураСвойствУстановкиШтампаПоПозиции()
	
	СтруктураКоординаты = Новый Структура;
	
	СтруктураКоординаты.Вставить("top", 0);
	СтруктураКоординаты.Вставить("left", 0);
	
	СтруктураРазмещениеНаСтранице = Новый Структура;
	
	СтруктураРазмещениеНаСтранице.Вставить("position"   , "");
	СтруктураРазмещениеНаСтранице.Вставить("indentation", 10);
	СтруктураРазмещениеНаСтранице.Вставить("coordinates", СтруктураКоординаты);
	
	СтруктураРазмещениеНаСтраницах = Новый Структура;
	
	СтруктураРазмещениеНаСтраницах.Вставить("rule"  , "");
	СтруктураРазмещениеНаСтраницах.Вставить("number", 0);
	
	СтруктураСвойствРазмещения = Новый Структура;
	
	СтруктураСвойствРазмещения.Вставить("pages", СтруктураРазмещениеНаСтраницах);
	СтруктураСвойствРазмещения.Вставить("sheet", СтруктураРазмещениеНаСтранице);
	
	СтруктураШтампа = Новый Структура;
	СтруктураШтампа.Вставить("location", СтруктураСвойствРазмещения);
	
	Возврат СтруктураШтампа;
	
КонецФункции

// Возвращает структуру свойств размещения штампов по тегам.
//
// Возвращаемое значение:
//   Структура: 
//    * value    - Строка - Текстовое значение тега.
//    * replace  - Булево - Признак замещения тега картинкой штампа.
//    * location - Строка - Способ установки штампа в текстовое содержимое.
//
Функция НоваяСтруктураСвойствУстановкиШтампаПоТегу()
	
	СтруктураШтампа = Новый Структура;
	
	СтруктураШтампа.Вставить("value"   , "");
	СтруктураШтампа.Вставить("replace" , Ложь);
	СтруктураШтампа.Вставить("location", "");
	
	Возврат СтруктураШтампа;
	
КонецФункции

// Возвращает структуру свойств стратегии размещения.
//
// Возвращаемое значение:
//   Структура:
//    * placement - Структура - Параметры размещения:
//       ** position - Строка    - Способ размещения картинок штампов на странице документа.
//       ** pages    - Структура - Параметры расположения картинок штампов на страницах документа:
//            *** rule       - Строка - Способ расположения картинок штампов на страницах документа.
//            *** pageNumber - Число  - Номер страницы документа для расположения картинок штампов.
//    * strategy - Структура - Стратегия размещения:
//      ** route           - Строка - Направление при последовательной установке картинок штампов.
//      ** horizontalVisit - Строка - Смещение по горизонтали.
//
Функция НоваяСтруктураСвойствСтратегииРазмещения()
	
	СтруктураРазмещениеНаСтраницах = Новый Структура;
	
	СтруктураРазмещениеНаСтраницах.Вставить("rule"  , "");
	СтруктураРазмещениеНаСтраницах.Вставить("number", 0);
	
	СтруктураРасположение = Новый Структура;
	
	СтруктураРасположение.Вставить("position", "");
	СтруктураРасположение.Вставить("pages"   , СтруктураРазмещениеНаСтраницах);
	
	СтруктураСтратегия = Новый Структура;
	
	СтруктураСтратегия.Вставить("route"          , "");
	СтруктураСтратегия.Вставить("horizontalVisit", "");
	
	СтруктураСтратегииРазмещения = Новый Структура;
	СтруктураСтратегииРазмещения.Вставить("placement", СтруктураРасположение);
	СтруктураСтратегииРазмещения.Вставить("strategy" , СтруктураСтратегия);
	
	Возврат СтруктураСтратегииРазмещения;
	
КонецФункции

// Возвращает структуру свойств файла.
//
// Возвращаемое значение:
//   Структура:
//    * path      - Строка - Путь к файлу в папке zip архива.
//    * extension - Строка - Значение расширения файла.
//
Функция НоваяСтруктураСвойствФайла()
	
	СтруктураСвойствФайла = Новый Структура;
	
	СтруктураСвойствФайла.Вставить("path", "");
	СтруктураСвойствФайла.Вставить("extension", "");
	
	Возврат СтруктураСвойствФайла;
	
КонецФункции

// Возвращает структуру параметров файла картинки штампа.
//
// Возвращаемое значение:
//   Структура:
//    * coefficient       - Число - Коэффициент изменения размера картинки штампа.
//      Значение по умолчанию: 1
//    * transparencyLevel - Число - Значение уровня прозрачности от 0 до 255, где 0 - 100% значение прозрачности.
//      Значение по умолчанию: 255
//
Функция НоваяСтруктураСвойствКартинкиШтампа()
	
	СтруктураСвойствКартинкиШтампа = Новый Структура;
	
	СтруктураСвойствКартинкиШтампа.Вставить("coefficient"       , 1);
	СтруктураСвойствКартинкиШтампа.Вставить("transparencyLevel" , 255);
	
	Возврат СтруктураСвойствКартинкиШтампа;
	
КонецФункции

// Возвращает структуру параметров размещения картинки штампа.
//
// Возвращаемое значение:
//   Структура:
//    * byPosition - Структура - Параметры размещения по позиционированию:
//      ** positions - Массив из см. НоваяСтруктураСвойствУстановкиШтампаПоПозиции.
//    * byTags    - Структура  - Параметры размещения по тегам:
//      ** tags       - Массив из см. НоваяСтруктураСвойствУстановкиШтампаПоТегу 
//      ** ifNotFound - см. НоваяСтруктураСвойствРазмещенияШтампов()
//                    - Неопределено - Значение не инициализировано.
// 
Функция НоваяСтруктураСвойствРазмещенияШтампов()
	
	СтруктураПараметров = Новый Структура;
	
	СтруктураПараметров.Вставить("byPosition", Новый Структура("positions"       , Новый Массив));
	СтруктураПараметров.Вставить("byTags"    , Новый Структура("tags, ifNotFound", Новый Массив, Неопределено));
	
	Возврат СтруктураПараметров;
	
КонецФункции

Функция СловарьЗначенийСпособовРазмещенияНаСтранице()
	
	Соответствие = Новый Структура;
	
	Соответствие.Вставить("ЛевыйВерхнийУгол" , "leftTop");
	Соответствие.Вставить("ПравыйВерхнийУгол", "rightTop");
	Соответствие.Вставить("ВверхуПоЦентру"   , "centerTop");
	Соответствие.Вставить("ЛевыйНижнийУгол"  , "leftBottom");
	Соответствие.Вставить("ПравыйНижнийУгол" , "rightBottom");
	Соответствие.Вставить("ВнизуПоЦентру"    , "centerBottom");
	Соответствие.Вставить("ПоКоординатам"    , "byCoordinates");
	Соответствие.Вставить("ПоТегам"          , "byTags");
	
	Возврат Соответствие;
	
КонецФункции 

Функция СловарьЗначенийСпособовРазмещенияНаСтраницах()
	
	Соответствие = Новый Структура;
	
	Соответствие.Вставить("НаПервойСтранице"               , "firstPage");
	Соответствие.Вставить("НаПоследнейСтранице"            , "lastPage");
	Соответствие.Вставить("НаКаждойСтранице"               , "everyPage");
	Соответствие.Вставить("НаКаждойСтраницеКромеПоследней" , "everyPageExceptTheLast");
	Соответствие.Вставить("НаКонкретнойСтранице"           , "specificPage");
	Соответствие.Вставить("НаНовойСтранице"                , "newPage");
	
	Возврат Соответствие;
	
КонецФункции

Функция СловарьЗначенийСмещенийПоГоризонтали()
	
	Соответствие = Новый Структура;
	
	Соответствие.Вставить("Авто" , "auto");
	Соответствие.Вставить("Право", "right");
	Соответствие.Вставить("Лево" , "left");
	
	Возврат Соответствие;
	
КонецФункции

Функция СловарьЗначенийСтратегийРазмещенияШтампов()
	
	Соответствие = Новый Структура;
	
	Соответствие.Вставить(Интеграция1СШтампКлиентСервер.СтратегияРазмещенияГоризонтально(), "horizontal");
	Соответствие.Вставить(Интеграция1СШтампКлиентСервер.СтратегияРазмещенияВертикально(), "vertical");
	
	Возврат Соответствие;
	
КонецФункции

Функция СловарьСпособовУстановкиШтампаПоТегу()
	
	Соответствие = Новый Структура;
	
	Соответствие.Вставить("РаздвигаяТекст", "breakText");
	Соответствие.Вставить("НадТекстом"    , "inBackground");
	
	Возврат Соответствие;
	
КонецФункции

Процедура ПроверитьЦелоеЧисло(Знач ПроверяемоеЗначение, Знач ТекстОшибки, Знач КонтекстПроверки)
	
	ОбщегоНазначенияКлиентСервер.Проверить(
			Цел(ПроверяемоеЗначение)= ПроверяемоеЗначение,
			ТекстОшибки,
			КонтекстПроверки);
	
КонецПроцедуры

Процедура ПреобразоватьPDFДокумент(Знач ТипФайлаPDF, ДвоичныеДанные)
	
	ПотокДанныхФайла = Новый ПотокВПамяти;
	
	Попытка
	
		ДокументPDF = Новый ДокументPDF;
		
		ДокументPDF.Прочитать(ДвоичныеДанные.ОткрытьПотокДляЧтения());
		ДокументPDF.Записать(ПотокДанныхФайла, , ТипФайлаPDF);
		
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ПодробныйТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Преобразование PDF документа в 1С:Штамп.';
				|en = 'Convert PDF document in 1C:Stamp.'", ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка,,, ПодробныйТекстОшибки);
		ВызватьИсключение;
	КонецПопытки;
	
	ДвоичныеДанные = ПотокДанныхФайла.ЗакрытьИПолучитьДвоичныеДанные();
	
КонецПроцедуры

Функция РасширениеФайлаБезУчетаВерсииФормата(Знач ВерсияФайла)
	
	РасширениеФайлаPDF = Интеграция1СШтампСлужебныйКлиентСервер.РасширениеФайлаPDF();
	
	СоответствиеВерсийФормату = Новый Соответствие;
	
	СоответствиеВерсийФормату.Вставить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_1(), РасширениеФайлаPDF);
	СоответствиеВерсийФормату.Вставить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_2(), РасширениеФайлаPDF);
	СоответствиеВерсийФормату.Вставить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_3(), РасширениеФайлаPDF);
	
	ФорматФайла = СоответствиеВерсийФормату[ВерсияФайла];
	
	Возврат ?(ФорматФайла = Неопределено, ВерсияФайла, ФорматФайла)
	
КонецФункции

// Возвращает параметры соединения.
// 
// Возвращаемое значение:
//  Структура - параметры соединения:
//   * Сервер - Строка - имя сервера.
//   * Порт - Число - порт соединения.
//   * Аутентификация - Булево - требуется аутентификация по токену.
//   * Таймаут - Число - длительность ожидания ответа в секундах.
//   * ЗащищенноеСоединение - ЗащищенноеСоединениеOpenSSL - объект защищенного соединения OpenSSL.
//                          - Неопределено - объект защищенного соединения не установлен.
//   * Прокси - ИнтернетПрокси - параметры прокси-сервера.
//
Функция НовыеПараметрыСоединения(Сервер = "", Порт = 80, ЗащищенноеСоединение = Ложь)

	Результат = Новый Структура;
	Результат.Вставить("Сервер"              , Сервер);
	Результат.Вставить("Порт"                , Порт);
	Результат.Вставить("Аутентификация"      , Ложь);
	Результат.Вставить("Таймаут"             , 30);
	Результат.Вставить("Прокси"              , Новый ИнтернетПрокси);
	Результат.Вставить("ЗащищенноеСоединение", Неопределено);
	
	Если ЗащищенноеСоединение Тогда
		Результат.ЗащищенноеСоединение = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение(,
			Новый СертификатыУдостоверяющихЦентровОС);
		Результат.Прокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси("https");
	Иначе
		Результат.Прокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси("http");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Останавливает выполнение кода на заданное время. 
//
// Параметры:
//  Секунд - Число - время ожидания в секундах.
//
Процедура Пауза(Секунд) Экспорт
	
	ТекущийСеансИнформационнойБазы = ПолучитьТекущийСеансИнформационнойБазы();
	ФоновоеЗадание = ТекущийСеансИнформационнойБазы.ПолучитьФоновоеЗадание();
	
	Если ФоновоеЗадание = Неопределено Тогда
		Параметры = Новый Массив;
		Параметры.Добавить(Секунд);
		ФоновоеЗадание = ФоновыеЗадания.Выполнить("Интеграция1СШтампСлужебный.Пауза", Параметры);
	КонецЕсли;
		
	ФоновоеЗадание.ОжидатьЗавершенияВыполнения(Секунд);
	
КонецПроцедуры

// Защита от непредсказуемого интервала, который может быть получен из сервиса
// Максимальный таймаут для повторного обращения - 300 сек.
//
Функция ТаймаутОжидания()
	
	Возврат 300;
	
КонецФункции

// Защита от бесконечных проверок статуса задания
// Максимальная длительность запроса - 5 мин.
//
Функция МаксимальнаяДлительностьВыполненияЗапроса()
	
	Возврат 300;
	
КонецФункции

#Область ПрикладныеПроверки

// Возвращает результат проверок перед отправкой запроса в сервис.
//
// Параметры:
//  ТелоЗапроса  - Строка - JSON строка тела запроса.
//
// Возвращаемое значение:
//   См. НовыйРезультатВыполненияЗапроса
//
Функция РезультатВыполненияПрикладныхПроверокПередОтправкойЗапроса(Знач ДвоичныеДанные)
	
	Результат = НовыйРезультатВыполненияЗапроса();
	
	// Проверка превышения допустимого размера тела запроса
	Если ПревышенДопустимыйРазмерТелаЗапроса(ДвоичныеДанные) Тогда
		
		ОписаниеОшибки = НСтр(
					"ru = 'Тело запроса превышает допустимый размер.';
					|en = 'The query body exceeds the allowed size.'",
					ОбщегоНазначения.КодОсновногоЯзыка());
		
		Результат.КодСостояния = Интеграция1СШтампСлужебныйКлиентСервер.КодСостоянияСлишкомБольшойРазмерТелаЗапроса();
		Результат.ТекстОшибки  = ОписаниеОшибки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает признак превышения допустимого размера тела запроса.
//
// Параметры:
//  ТелоЗапроса  - Строка - JSON строка тела запроса.
//
// Возвращаемое значение:
//   Булево - Признак превышения размера.
//
Функция ПревышенДопустимыйРазмерТелаЗапроса(Знач ДвоичныеДанные)
	
	РазмерТела = ДвоичныеДанные.Размер();
	
	Возврат РазмерТела > Интеграция1СШтампСлужебныйКлиентСервер.ДопустимыйРазмерИсходящегоТелаЗапроса();
	
КонецФункции

#КонецОбласти

#Область РаботаСКоллекцией

// Получить значение данных коллекции в рекурсии.
//
// Параметры:
//  Значение - Произвольный - значения обработки.
//  СвойстваПути - Массив Из Строка - элементы пути, например pathToRoot[0]; categories.
//  ЕстьОшибки - Булево - признак ошибки при разборе.
//
// Возвращаемое значение:
//  Произвольный - Значение свойства по указанному пути
Функция ЗначениеСвойстваКоллекцииВРекурсии(Знач Значение, Знач СвойстваПути, ЕстьОшибки = Ложь)
	
	Свойство = СвойстваПути[0];
	МеткаКоллекции = СтрНайти(Свойство, "[");
	ИндексКоллекции = Неопределено;
	Если МеткаКоллекции <> 0 Тогда
		ИндексКоллекции = Сред(Свойство, МеткаКоллекции + 1);
		ИндексКоллекции = Лев(ИндексКоллекции, СтрДлина(ИндексКоллекции) - 1);
		Свойство = Лев(Свойство, МеткаКоллекции - 1);
	КонецЕсли;
	
	// Проверка свойства структуры.
	Если Не ЭтоКоллекция(Значение) Тогда
		ЕстьОшибки = Истина;
		
		Возврат Неопределено;
	КонецЕсли;

	Если ТипЗнч(Значение) = Тип("Структура") И Не Значение.Свойство(Свойство) 
		Или ТипЗнч(Значение) = Тип("Соответствие") И Значение.Получить(Свойство) = Неопределено Тогда
		ЕстьОшибки = Истина;
		
		Возврат Неопределено;
	КонецЕсли;
	
	// Поиск нового элемента
	Если МеткаКоллекции = 0 Тогда
		Значение = Значение[Свойство];
	Иначе
		Значение = Значение[Свойство][Число(ИндексКоллекции)];
	КонецЕсли;
	
	Если СвойстваПути.Количество() = 1 Тогда
		// Последний элемент пути.
		Возврат Значение;
	Иначе
		СвойстваПути.Удалить(0);
		Возврат ЗначениеСвойстваКоллекцииВРекурсии(Значение, СвойстваПути, ЕстьОшибки);
	КонецЕсли;
	
КонецФункции

// Получает значение свойства объекта по указанному пути
// 
// Параметры:
//  Значение   - Структура, Соответствие Из Произвольный - Объект, свойство которого требуется получить
//  Путь       - Строка                                  - Путь к свойству
//  ЕстьОшибки - Булево                                  - Признак ошибки при получении свойства
// 
// Возвращаемое значение:
//  Произвольный - Значение свойства по указанному пути
Функция ЗначениеКоллекцииПоАбсолютномуПути(Знач Значение, Знач Путь, ЕстьОшибки = Ложь) Экспорт
	
	СвойстваПути = СтрРазделить(Путь, ".", Ложь);
	
	Попытка
		Результат = ЗначениеСвойстваКоллекцииВРекурсии(Значение, СвойстваПути, ЕстьОшибки);
	Исключение
		Результат = Неопределено;
		ЕстьОшибки = Истина;
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоКоллекция(Знач Значение)
	
	ТипЗначения = ТипЗнч(Значение);
	
	Возврат ТипЗначения = Тип("Структура") Или ТипЗначения = Тип("Соответствие");
	
КонецФункции

#КонецОбласти

#КонецОбласти
