#Область ПрограммныйИнтерфейс

// Версия библиотеки
// 
// Возвращаемое значение:
//  Строка - Версия библиотеки
Функция ВерсияБиблиотеки() Экспорт
	
	Возврат "2.0.6.6";
	
КонецФункции

// Версия подсистемы
// 
// Параметры:
//  Редакция - Строка, Неопределено - Редакция
// 
// Возвращаемое значение:
//  Строка - Версия подсистемы
Функция ВерсияПодсистемы(Редакция = Неопределено) Экспорт
	
	Если Редакция = Неопределено Тогда
		Редакция = РедакцияБиблиотеки();
	КонецЕсли;
	
	Разделитель = ".";
	ВерсияБиблиотеки = ВерсияБиблиотеки();
	ЧастиВерсииБиблиотеки = СтрРазделить(ВерсияБиблиотеки, Разделитель);
	
	ЧастиВерсииБиблиотеки[0] = Редакция;
	
	Возврат СтрСоединить(ЧастиВерсииБиблиотеки, Разделитель);
	
КонецФункции

// см. ОбщегоНазначенияПереопределяемый.ПриДобавленииПараметровРаботыКлиента
Процедура ПриДобавленииПараметровРаботыКлиента(Параметры) Экспорт
	
	ДобавитьПараметрВыборТипПодписиБСП = Ложь;
	
	Если ПодсистемаСуществует("ИСМП") Тогда
		ДобавитьПараметрВыборТипПодписиБСП = Истина;
		МодульОбщегоНазначенияИСМП = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияИСМП");
		МодульОбщегоНазначенияИСМП.ПриДобавленииПараметровРаботыКлиента(Параметры);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ЗЕРНО") Тогда
		
		ДобавитьПараметрВыборТипПодписиБСП = Истина;
		
		МодульИнтеграцияЗЕРНО = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияЗЕРНО");
		МодульИнтеграцияЗЕРНО.ПриДобавленииПараметровРаботыКлиента(Параметры);
		
	КонецЕсли;
	
	Если ДобавитьПараметрВыборТипПодписиБСП Тогда
		
		ВерсияБСП = СтандартныеПодсистемыСервер.ВерсияБиблиотеки();
		
		ИменаПараметров = ОбщегоНазначенияИСКлиентСерверПовтИсп.ИменаПараметровРаботыКлиентаВыборТипаПодписиБСП();
		Если ОбщегоНазначенияКлиентСервер.СравнитьВерсии(ВерсияБСП, "3.1.7.247") >= 0 Тогда
			Параметры.Вставить(ИменаПараметров.ИмяПараметраПриложения, ИменаПараметров.ЗначениеПараметраВыборТипаПодписиИспользуетКонструктор);
		ИначеЕсли ОбщегоНазначенияКлиентСервер.СравнитьВерсии(ВерсияБСП, "3.1.7.61") >= 0 Тогда
			Параметры.Вставить(ИменаПараметров.ИмяПараметраПриложения, ИменаПараметров.ЗначениеПараметраВыборТипаПодписиЗадаетсяПеречислением);
		Иначе
			Параметры.Вставить(ИменаПараметров.ИмяПараметраПриложения, ИменаПараметров.ЗначениеПараметраВыборТипаПодписиНеПоддерживается);
		КонецЕсли;
	
	КонецЕсли;
	
КонецПроцедуры

// Формирует и выводит сообщение, которое может быть связано с элементом 
// управления формы.
//
// Параметры:
//  ИдентификаторНазначения    - УникальныйИдентификатор, Неопределено - уникальный идентификатор формы для показа сообщения.
//  ТекстСообщенияПользователю - Строка - текст сообщения.
//  КлючДанных                 - ЛюбаяСсылка - объект или ключ записи информационной базы, к которому это сообщение относится.
//  Поле                       - Строка - наименование реквизита формы.
//  ПутьКДанным                - Строка - путь к данным (путь к реквизиту формы).
//  Отказ                      - Булево - выходной параметр, всегда устанавливается в значение Истина.
//
// Примеры : см ОбщегоНазначения.СообщитьПользователю.
//
//@skip-check method-too-many-params
Процедура СообщитьПользователюВФорму(
		Знач ИдентификаторНазначения = Неопределено,
		Знач ТекстСообщенияПользователю,
		Знач КлючДанных = Неопределено,
		Знач Поле = "",
		Знач ПутьКДанным = "",
		Отказ = Ложь) Экспорт
	
	Сообщение = Новый СообщениеПользователю;
	Если ИдентификаторНазначения <> Неопределено Тогда
		Сообщение.ИдентификаторНазначения = ИдентификаторНазначения;
	КонецЕсли;
	
	Сообщение.Текст = ТекстСообщенияПользователю;
	Сообщение.Поле = Поле;
	
	ЭтоОбъект = Ложь;
	
	Если КлючДанных <> Неопределено
		И XMLТипЗнч(КлючДанных) <> Неопределено Тогда
		ТипЗначенияСтрокой = XMLТипЗнч(КлючДанных).ИмяТипа;
		ЭтоОбъект = СтрНайти(ТипЗначенияСтрокой, "Object.") > 0;
	КонецЕсли;
	
	Если ЭтоОбъект Тогда
		Сообщение.УстановитьДанные(КлючДанных);
	Иначе
		Сообщение.КлючДанных = КлючДанных;
	КонецЕсли;
	
	Если НЕ ПустаяСтрока(ПутьКДанным) Тогда
		Сообщение.ПутьКДанным = ПутьКДанным;
	КонецЕсли;
		
	Сообщение.Сообщить();
	
	Отказ = Истина;
	
КонецПроцедуры

// Заполняет представление регламентного задания (реквизиты ЗаданиеАктивно, ТекстРасписания)
//
// Параметры:
//  Задание				 - РегламентноеЗадание, Неопределено - регламентное задание представление которого необходимо получить
//  ТекстРасписания		 - Строка - представление расписания (выходной)
//  РасписаниеАктивно	 - Булево - использование задания (выходной)
//
Процедура ПолучитьТекстЗаголовкаИРасписанияРегламентнойНастройки(Задание, ТекстРасписания, РасписаниеАктивно) Экспорт
	
	РасписаниеАктивно = Ложь;
	
	Если Задание = Неопределено Тогда
		
		ТекстРасписания = НСтр("ru = '<Расписание не задано>';
								|en = '<Расписание не задано>'");
		
	Иначе
		
		Если Задание.Использование Тогда
			РасписаниеАктивно = Истина;
			ТекстРасписания = СтрШаблон(НСтр("ru = 'Расписание: %1';
											|en = 'Расписание: %1'"), Строка(Задание.Расписание));
		Иначе
			ТекстРасписания = СтрШаблон(НСтр("ru = 'Расписание (НЕ АКТИВНО): %1';
											|en = 'Расписание (НЕ АКТИВНО): %1'"), Строка(Задание.Расписание));
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// Возвращает менеджер (модуль менеджера или общий модуль) по полному имени метаданных
// 
// Параметры:
//   ПолноеИмя - Строка - полное имя объекта для получения модуля менеджера.
//
// Возвращаемое значение:
//   СправочникМенеджер, ДокументМенеджер, ОбщийМодуль - менеджер объекта для библиотеки
Функция МенеджерОбъектаПоПолномуИмени(ПолноеИмя) Экспорт
	
	МенеджерОбъекта = Неопределено;
	ОбщегоНазначенияИСПереопределяемый.ПриОпределенииМенеджераОбъектаПоПолномуИмени(МенеджерОбъекта, ПолноеИмя);
	
	Если МенеджерОбъекта = Неопределено Тогда
		МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени(ПолноеИмя);
	КонецЕсли;
	
	Возврат МенеджерОбъекта;
	
КонецФункции

// Возвращает менеджер (модуль менеджера или общий модуль) объекта по ссылке на объект.
// Ограничение: не обрабатываются точки маршрутов бизнес-процессов.
// См. также ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени.
//
// Параметры:
//  Ссылка - ЛюбаяСсылка - объект, менеджер которого требуется получить.
//
// Возвращаемое значение:
//  СправочникМенеджер, ДокументМенеджер, ОбработкаМенеджер, РегистрСведенийМенеджер - менеджер объекта.
//
// Пример:
//  МенеджерСправочника = ОбщегоНазначения.МенеджерОбъектаПоСсылке(СсылкаНаОрганизацию);
//
Функция МенеджерОбъектаПоСсылке(Ссылка) Экспорт
	
	МенеджерОбъекта = Неопределено;
	ОбщегоНазначенияИСПереопределяемый.ПриОпределенииМенеджераОбъектаПоСсылке(МенеджерОбъекта, Ссылка);
	
	Если МенеджерОбъекта = Неопределено Тогда
		МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоСсылке(Ссылка);
	КонецЕсли;
	
	Возврат МенеджерОбъекта;
	
КонецФункции

// Право определяемого типа.
// 
// Параметры:
//  ОпределяемыйТип  - ОбъектМетаданныхОпределяемыйТип - Определяемый тип
//  Право - Строка - Право
// 
// Возвращаемое значение:
//  Булево - Право определяемого типа
Функция ПравоОпределяемогоТипа(ОпределяемыйТип, Право) Экспорт
	
	Для Каждого ВозможныйТип Из ОпределяемыйТип.Тип.Типы() Цикл
		МетаданныеТипа = Метаданные.НайтиПоТипу(ВозможныйТип);
		Если МетаданныеТипа <> Неопределено Тогда
			Если ПравоДоступа(Право, МетаданныеТипа) Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

// Хэш сумма строки.
// 
// Параметры:
//  СтрокаДляРасчетаХеша - Строка - Строка для расчета хеша
// 
// Возвращаемое значение:
//  Строка - Хэш сумма строки
Функция ХэшСуммаСтроки(СтрокаДляРасчетаХеша) Экспорт
	
	ХешированиеДанныхОбъект = Новый ХешированиеДанных(ХешФункция.SHA256);
	ХешированиеДанныхОбъект.Добавить(СтрокаДляРасчетаХеша);
	
	Возврат Base64Строка(ХешированиеДанныхОбъект.ХешСумма);
	
КонецФункции

// Текст исключения обработки статуса
//
// Параметры:
//  Документ - ДокументСсылка - Документ.
//  Операция - ПеречислениеСсылка - Операция.
// 
// Возвращаемое значение:
//  Строка - Текст исключения.
//
Функция ТекстИсключенияОбработкиСтатуса(Документ, Операция) Экспорт
	
	Возврат СтрШаблон(
		НСтр("ru = 'При изменении статуса документа %1 не обработана операция %2';
			|en = 'При изменении статуса документа %1 не обработана операция %2'"),
		Документ,
		Операция);
		
КонецФункции


// Это расширенная версия гос ИС.
// 
// Параметры:
//  Подсистема - Неопределено, Строка - Подсистема
// 
// Возвращаемое значение:
//  Булево - Это расширенная версия БГосИС
Функция ЭтоРасширеннаяВерсияГосИС(Подсистема = Неопределено) Экспорт
	
	Если Подсистема = Неопределено Тогда
		Подсистема = "БазоваяФункциональность";
	КонецЕсли;
	
	Возврат ОбщегоНазначения.ПодсистемаСуществует(СтрШаблон("ГосИС.%1.Расширенная", Подсистема));
	
КонецФункции

// Выполняет поиск организации предприятия по ИНН и/или КПП.
//
// Параметры:
//   ИНН - Строка - ИНН искомой организации,
//   КПП - Строка - КПП искомой организации.
// 
// Возвращаемое значение:
//   ОпределяемыйТип.ОрганизацияКонтрагентГосИС, Неопределено - найденная организация
//
Функция ОрганизацияПоИННКПП(ИНН, КПП) Экспорт
	
	Организация = Неопределено;
	ОбщегоНазначенияИСПереопределяемый.ЗаполнитьОрганизациюПоИННКПП(Организация, ИНН, КПП);
	Возврат Организация;
	
КонецФункции

// Возвращает структуру, содержащую ИНН, КПП, GLN организации.
//
// Параметры:
//  Организация - ОпределяемыйТип.Организация - Организация.
//  Подразделение - ОпределяемыйТип.Подразделение - Подразделение организации.
//
// Возвращаемое значение:
//  Структура - со свойствами:
//   * ИНН - Строка - ИНН организации.
//   * КПП - Строка - КПП организации.
//   * GLN - Строка - GLN организации.
Функция ИННКППGLNОрганизации(Организация, Подразделение) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ИНН", "");
	Результат.Вставить("КПП", "");
	Результат.Вставить("GLN", "");
	ОбщегоНазначенияИСПереопределяемый.ПриОпределенииИННКППGLNОрганизации(Результат, Организация, Подразделение);
	
	Возврат Результат;
	
КонецФункции

// Получает список доступных для использования организаций.
//
//Возвращаемое значение:
//   СписокЗначений Из ОпределяемыйТип.Организация - ссылки на организации:
//    * Значение      - ОпределяемыйТип.Организация - ссылка на организацию.
//    * Представление - Строка - наименование организации.
Функция ДоступныеОрганизации() Экспорт
	
	СписокОрганизаций = Новый Массив;
	ОбщегоНазначенияИСПереопределяемый.ПриОпределенииСпискаОрганизацийНедоступныхДляВыбора(СписокОрганизаций);
	
	Организации = Новый СписокЗначений;
	
	ШаблонЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Организации.Ссылка       КАК Ссылка,
	|	Организации.Наименование КАК Наименование
	|ИЗ
	|	%1 КАК Организации
	|ГДЕ
	|	НЕ (Организации.Ссылка В (&СписокОрганизаций))
	|";
	
	Объединение = "
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|";

	Порядок = "
	|УПОРЯДОЧИТЬ ПО
	|	Организации.Наименование";
	
	ТекстЗапроса = "";
	ТипыОрганизаций = Метаданные.ОпределяемыеТипы.Организация.Тип.Типы();
	ПерваяИтерация = Истина;
	
	Для Каждого Тип Из ТипыОрганизаций Цикл
		
		ПолноеИмя = Метаданные.НайтиПоТипу(Тип).ПолноеИмя();
		ТекстЗапроса = ТекстЗапроса + СтрШаблон(ШаблонЗапроса, ПолноеИмя) + ?(Не ПерваяИтерация, Объединение, "");
		ПерваяИтерация = Ложь;
		
	КонецЦикла;
	
	ТекстЗапроса = ТекстЗапроса + Порядок;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("СписокОрганизаций", СписокОрганизаций);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Организации.Добавить(Выборка.Ссылка, Выборка.Наименование);
	КонецЦикла;
	
	Возврат Организации;
	
КонецФункции

// Характеристики используются.
// 
// Возвращаемое значение:
//  Булево - Характеристики используются
Функция ХарактеристикиИспользуются() Экспорт
	
	Используются = Ложь;
	ОбщегоНазначенияИСПереопределяемый.ИспользованиеХарактеристик(Используются, Неопределено);
	Возврат Используются;
	
КонецФункции

// Серии используются.
// 
// Возвращаемое значение:
//  Булево - Серии используются
Функция СерииИспользуются() Экспорт
	
	Используются = Ложь;
	ОбщегоНазначенияИСПереопределяемый.ИспользованиеСерий(Используются, Неопределено);
	Возврат Используются;
	
КонецФункции

// Возвращает значение по умолчанию переданного определяемого типа.
// 
// Параметры:
//  ОпределяемыйТипИлиИмяТипа - Строка, ОбъектМетаданныхОпределяемыйТип - Определяемый тип или имя типа
// 
// Возвращаемое значение:
//  Произвольный, Неопределено - Пустое значение определяемого типа
Функция ПустоеЗначениеОпределяемогоТипа(ОпределяемыйТипИлиИмяТипа) Экспорт
	
	// Обход ошибки платформы:
	// в некоторых случаях в фоновых заданиях для ссылочных типов определяемого типа
	// приведение значения выполняется не корректно
	// Справочники.СерииНоменклатуры.ПустаяСсылка() <> Метаданные.ОпределяемыеТипы.СерияНоменклатуры.Тип.ПривестиЗначение()
	
	Если ТипЗнч(ОпределяемыйТипИлиИмяТипа) = Тип("Строка") Тогда
		ОпределяемыйТип = Метаданные.ОпределяемыеТипы[ОпределяемыйТипИлиИмяТипа];
	Иначе
		ОпределяемыйТип = ОпределяемыйТипИлиИмяТипа;
	КонецЕсли;
	
	ТипыОпределяемогоТипа = ОпределяемыйТип.Тип.Типы();
	
	Если ТипыОпределяемогоТипа.Количество() = 1 Тогда
		
		ТипОпределяемогоТипа = ТипыОпределяемогоТипа[0];
		
		Если ТипОпределяемогоТипа = Неопределено Тогда
			Возврат Неопределено;
		ИначеЕсли ОбщегоНазначения.ЭтоСсылка(ТипОпределяемогоТипа) Тогда
			Возврат Новый (ТипОпределяемогоТипа)
		КонецЕсли;
		
		Возврат ОпределяемыйТип.Тип.ПривестиЗначение();
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

// Определяет необходимость указания серии для маркируемой продукции.
// 
// Параметры:
//  Товар - Структура - данные строки номенклатуры. Содержит обязательные поля:
//   * Номенклатура   - ОпределяемыйТип.Номенклатура               - номенклатура.
//        - ОпределяемыйТип.Номенклатура - номенклатура.
//  ПараметрыСканирования - (См. ШтрихкодированиеОбщегоНазначенияИСКлиент.ПараметрыСканирования).
// Возвращаемое значение:
//  Булево - Истина, если требуется указать серию.
Функция ТребуетсяВыборСерии(Товар, ПараметрыСканирования) Экспорт
	
	Если ПараметрыСканирования.ДополнительныеПараметры.Свойство("ДанныеУточнения")
		И ПараметрыСканирования.ДополнительныеПараметры.ДанныеУточнения.Свойство("Серия") Тогда
		Возврат ЗначениеЗаполнено(ПараметрыСканирования.ДополнительныеПараметры.ДанныеУточнения.Серия);
	КонецЕсли;
	
	ТребуетсяВыбор = Ложь;
	
	Операция = Неопределено;
	Если ШтрихкодированиеОбщегоНазначенияИС.ДопустимаПродукцияИСМП(ПараметрыСканирования)
		Или ШтрихкодированиеОбщегоНазначенияИС.ДопустимаТабачнаяПродукция(ПараметрыСканирования) Тогда
		Операция = ПараметрыСканирования.ВидОперацииИСМП;
	ИначеЕсли КонструкторИСОбщегоНазначенияКлиентСервер.ЭтоМаркируемаяПродукцияЕГАИС(ПараметрыСканирования.ДопустимыеВидыПродукции) Тогда
		Операция = ПараметрыСканирования.Операция;
	КонецЕсли;
	
	ПараметрыУказанияСерий = ПараметрыСканирования.ПараметрыУказанияСерий;
	
	ДанныеДляРасчетаСерии  = Новый Структура;
	ДанныеДляРасчетаСерии.Вставить("Склад",          ПараметрыСканирования.Склад);
	ДанныеДляРасчетаСерии.Вставить("Операция",       Операция);
	ДанныеДляРасчетаСерии.Вставить("Номенклатура",   Товар.Номенклатура);

	ОбщегоНазначенияИСПереопределяемый.ПриОпределенииНеобходимостиВыбораСерии(
		ДанныеДляРасчетаСерии, ПараметрыУказанияСерий, ТребуетсяВыбор, Неопределено, ПараметрыСканирования.КэшированныеЗначения);
	
	Возврат ТребуетсяВыбор;
	
КонецФункции

// Списывает из остатка вскрытых потребительских упаковок количество частично проданного товара.
// 
// Параметры:
//  ИсточникДанных - ДанныеФормыСтруктура, ДокументОбъект, Структура - объект формы, объект документа или произвольная структура,
//                   в которой находятся данные штрихкодов упаковок. Источник должен иметь поля и коллекции с именами,
//                   указанными в параметрах сканирования: ИмяТабличнойЧастиШтрихкодыУпаковок,
//                   ИмяКолонкиШтрихкодУпаковки, ИмяКолонкиЧастичноеВыбытиеКоличество
//  ПараметрыСканирования - см. ШтрихкодированиеОбщегоНазначенияИС.ПараметрыСканирования
Процедура СписатьОстатокПоВскрытымПотребительскимУпаковкам(ИсточникДанных, ПараметрыСканирования) Экспорт
	
	Если Не ШтрихкодированиеОбщегоНазначенияИС.ТребуетсяЧастичноеВыбытие(ПараметрыСканирования)
		Или Не ЗначениеЗаполнено(ПараметрыСканирования.ИмяТабличнойЧастиШтрихкодыУпаковок)
		Или Не ЗначениеЗаполнено(ПараметрыСканирования.ИмяКолонкиЧастичноеВыбытиеКоличество) Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	НоменклатураОписания      = Новый Массив;
	НоменклатураШтрихкодов    = Новый Соответствие();
	КодыМаркировкиДляСписания = Новый ТаблицаЗначений();
	КодыМаркировкиДляСписания.Колонки.Добавить("ШтрихкодУпаковки", Метаданные.РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.Измерения.КодМаркировки.Тип);
	КодыМаркировкиДляСписания.Колонки.Добавить("Количество",       Метаданные.РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.Ресурсы.Остаток.Тип);
	
	ШтрихкодыУпаковок = ИсточникДанных[ПараметрыСканирования.ИмяТабличнойЧастиШтрихкодыУпаковок];
	Для Каждого СтрокаДанных Из ШтрихкодыУпаковок Цикл
		
		КоличествоЧастичногоВыбытия = СтрокаДанных[ПараметрыСканирования.ИмяКолонкиЧастичноеВыбытиеКоличество];
		ШтрихкодУпаковки = СтрокаДанных[ПараметрыСканирования.ИмяКолонкиШтрихкодУпаковки];
		Если Не ЗначениеЗаполнено(КоличествоЧастичногоВыбытия)
			Или Не ЗначениеЗаполнено(ШтрихкодУпаковки) Тогда
			Продолжить;
		КонецЕсли;
		
		НоваяСтрока = КодыМаркировкиДляСписания.Добавить();
		НоваяСтрока.ШтрихкодУпаковки = ШтрихкодУпаковки;
		НоваяСтрока.Количество       = КоличествоЧастичногоВыбытия;
		
		Если ПараметрыСканирования.РежимБРМК Тогда
			Если СтрокаДанных[ПараметрыСканирования.ИмяКолонкиЧастичноеВыбытиеВариантУчета] = Перечисления.ВариантыУчетаЧастичногоВыбытияИС.НастроеннаяНоменклатура Тогда
				НоменклатураПоШтрихкоду = СтрокаДанных[ПараметрыСканирования.ИмяКолонкиЧастичноеВыбытиеНоменклатура];
			Иначе
				НоменклатураПоШтрихкоду = СтрокаДанных.Номенклатура;
			КонецЕсли;
			НоменклатураОписания.Добавить(НоменклатураПоШтрихкоду);
			НоменклатураШтрихкодов.Вставить(ШтрихкодУпаковки, НоменклатураПоШтрихкоду);
		КонецЕсли;
		
	КонецЦикла;
	
	Если КодыМаркировкиДляСписания.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	КодыМаркировкиДляСписания.Свернуть("ШтрихкодУпаковки", "Количество");
	
	Если Не ПараметрыСканирования.РежимБРМК Тогда
		НоменклатураШтрихкодов = ОбщегоНазначения.ЗначениеРеквизитаОбъектов(
			КодыМаркировкиДляСписания.ВыгрузитьКолонку("ШтрихкодУпаковки"), "Номенклатура");
		Для Каждого КлючИЗначение Из НоменклатураШтрихкодов Цикл
			НоменклатураОписания.Добавить(КлючИЗначение.Значение);
		КонецЦикла;
	КонецЕсли;
	
	ОписанияНоменклатуры = ОписаниеНоменклатуры(НоменклатураОписания);
	
	Блокировка = Новый БлокировкаДанных();
	
	ЭлементБлокировки = Блокировка.Добавить(Метаданные.РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ПолноеИмя());
	ЭлементБлокировки.ИсточникДанных = КодыМаркировкиДляСписания;
	ЭлементБлокировки.ИспользоватьИзИсточникаДанных(
		Метаданные.РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.Измерения.КодМаркировки.Имя, "ШтрихкодУпаковки");
	
	НачатьТранзакцию();
	
	Попытка
		
		Блокировка.Заблокировать();
		
		ДанныеПодключений = РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ВскрытыеПотребительскиеУпаковкиПоКодуМаркировки(
			КодыМаркировкиДляСписания.ВыгрузитьКолонку("ШтрихкодУпаковки"), Истина);
		
		Для Каждого СтрокаТаблицы Из КодыМаркировкиДляСписания Цикл
			
			ДанныеПодключения = ДанныеПодключений[СтрокаТаблицы.ШтрихкодУпаковки];
			
			Если ДанныеПодключения = Неопределено
				Или (КонструкторИСОбщегоНазначенияКлиентСервер.ЭтоМаркируемаяПродукцияЕГАИС(ДанныеПодключения.ВидПродукции)
					И ЭтоРасширеннаяВерсияГосИС())
				Или Не ЗначениеЗаполнено(ДанныеПодключения.ВидПродукции) Тогда
				Продолжить;
			КонецЕсли;
			
			Номенклатура         = НоменклатураШтрихкодов[СтрокаТаблицы.ШтрихкодУпаковки];
			ОписаниеНоменклатуры = ОписанияНоменклатуры[Номенклатура];
			
			Если ЗначениеЗаполнено(ОписаниеНоменклатуры.ЕмкостьПотребительскойУпаковки)
				И ЗначениеЗаполнено(ОписаниеНоменклатуры.КоличествоВПотребительскойУпаковке)
				И ОписаниеНоменклатуры.ВариантЧастичногоВыбытия = Перечисления.ВариантыУчетаЧастичногоВыбытияИС.ТекущаяНоменклатура Тогда
				Коэффициент = 1 / (ОписаниеНоменклатуры.КоличествоВПотребительскойУпаковке / ОписаниеНоменклатуры.ЕмкостьПотребительскойУпаковки);
			Иначе
				Коэффициент = 1;
			КонецЕсли;
			
			ДанныеЗаписиОстатка = РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ИнициализироватьДанныеВскрытияПотребительскойУпаковки();
			Если КонструкторИСМПОбщегоНазначенияКлиентСервер.ИспользоватьЧислительИЗнаменательПриЧастичномВыбытииНаККТ(
				ДанныеПодключения.ВидПродукции) Тогда
				КоличествоРасход = Окр(СтрокаТаблицы.Количество * Коэффициент);
			Иначе
				КоличествоРасход = СтрокаТаблицы.Количество * Коэффициент;
			КонецЕсли;
			ДанныеЗаписиОстатка.Остаток = ДанныеПодключения.Остаток - КоличествоРасход;
			
			Если ДанныеЗаписиОстатка.Остаток <= 0 Тогда
				Если КонструкторИСМПОбщегоНазначенияКлиентСервер.ПоддерживаетсяПодключениеКОборудованиюРозлива(ДанныеПодключения.ВидПродукции)
					Или Не ЗначениеЗаполнено(ДанныеПодключения.ВидПродукции) Тогда
					ДанныеЗаписиОстатка.Статус = Перечисления.СтатусыВскрытыхПотребительскихУпаковокИС.Отключено;
					РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ЗаписатьВскрытиеПотребительскойУпаковки(ДанныеЗаписиОстатка, ДанныеПодключения);
				Иначе
					РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.УдалитьВскрытыеПотребительскиеУпаковки(СтрокаТаблицы.ШтрихкодУпаковки);
				КонецЕсли;
			Иначе
				РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ЗаписатьВскрытиеПотребительскойУпаковки(ДанныеЗаписиОстатка, ДанныеПодключения);
			КонецЕсли;
			
		КонецЦикла;
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		
		ВызватьИсключение
		
	КонецПопытки;
	
КонецПроцедуры

// Определяет включено ли ведение учета продукции, подлежащей маркированию.
//
// Параметры:
// 	РасширеннаяВерсияГосИС - Булево
// Возвращаемое значение:
//  Булево - Истина, если учет ведется.
Функция ВедетсяУчетМаркируемойПродукции(РасширеннаяВерсияГосИС) Экспорт
	
	Возврат ОбщегоНазначенияИСПовтИсп.ВедетсяУчетМаркируемойПродукции(РасширеннаяВерсияГосИС);
	
КонецФункции

#Область ДляВызоваИзДругихПодсистем

#Область ВыгрузкаЗагрузкаДанных

// см. ВыгрузкаЗагрузкаДанныхПереопределяемый.ПриЗаполненииТиповОбщихДанныхПоддерживающихСопоставлениеСсылокПриЗагрузке
Процедура ПриЗаполненииТиповОбщихДанныхПоддерживающихСопоставлениеСсылокПриЗагрузке(Типы) Экспорт
	
	Типы.Добавить(Метаданные.Справочники.ЭлементыКонструктораВидовПродукцииИС);
	Типы.Добавить(Метаданные.Справочники.ВидыПродукцииИС);
	
	Если ПодсистемаСуществует("ИСМП") Тогда
		
		Типы.Добавить(Метаданные.Справочники.ТоварныеГруппыГИСМТ);
		Типы.Добавить(Метаданные.Справочники.ШаблоныКодовМаркировкиСУЗ);
		
		Если ЭтоРасширеннаяВерсияГосИС("ИСМП") Тогда
			Типы.Добавить(Метаданные.Справочники.ПричиныВыводаИзОборотаИСМП);
			Типы.Добавить(Метаданные.Справочники.ВидыВозвратаВОборотИСМП);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ПодсистемаСуществует("ЗЕРНО") Тогда
		МодульИнтеграцияЗЕРНО = ОбщегоНазначенияИСПовтИсп.ОбщийМодуль("ИнтеграцияЗЕРНО");
		МодульИнтеграцияЗЕРНО.ПриЗаполненииТиповОбщихДанныхПоддерживающихСопоставлениеСсылокПриЗагрузке(Типы);
	КонецЕсли;
	
КонецПроцедуры

// см. ВыгрузкаЗагрузкаДанныхПереопределяемый.ПередВыгрузкойДанных
Процедура ПередВыгрузкойДанных(Контейнер) Экспорт
	
	Возврат;
	
КонецПроцедуры

// см. ВыгрузкаЗагрузкаДанныхПереопределяемый.ПередЗагрузкойДанных
Процедура ПередЗагрузкойДанных(Контейнер) Экспорт
	
	Возврат;
	
КонецПроцедуры

// см. ВыгрузкаЗагрузкаДанныхПереопределяемый.ПослеЗагрузкиДанных
Процедура ПослеЗагрузкиДанных(Контейнер) Экспорт
	
	Если ОбщегоНазначения.РазделениеВключено() Тогда
		РегистрыСведений.ОписаниеНоменклатурыИС.ЗаполнитьОтсутствующиеКлючиПоИдентификаторамВидовПродукции();

	ИначеЕсли ВедетсяУчетПродукцииИС() Тогда
		ТребуемаяВерсияБиблиотекиБИП = МинимальнаяВерсияБиблиотекиИнтернетПоддержки();
		ТекущаяВерсияБиблиотекиБИП   = ИнтернетПоддержкаПользователейКлиентСервер.ВерсияБиблиотеки();
		СравнениеВерсийБиблиотекиБИП = ОбщегоНазначенияКлиентСервер.СравнитьВерсии(ТребуемаяВерсияБиблиотекиБИП,
			ТекущаяВерсияБиблиотекиБИП);

		ОписаниеКлассификаторов = Новый Массив;
		ЗаполнитьОписаниеКлассификаторов(ОписаниеКлассификаторов, Истина, Истина);

		Если СравнениеВерсийБиблиотекиБИП > 0 Тогда
			ДобавитьКлассификаторы(ОписаниеКлассификаторов);
		Иначе
			МодульРаботаСКлассификаторами = ОбщегоНазначения.ОбщийМодуль("РаботаСКлассификаторами");
			//@skip-check bsl-legacy-check-static-feature-access-for-unknown-left-part
			МодульРаботаСКлассификаторами.ДобавитьКлассификаторы(ОписаниеКлассификаторов);
		КонецЕсли;

		Если Не КлассификаторНастроекВидовПродукцийЗагружен() Тогда

			УстановитьПривилегированныйРежим(Истина);

			НачатьТранзакцию();

			Попытка
				РезультатОбновления = ПервоначальноеЗаполнениеКлассификаторовВидовПродукции(Истина, Истина);
				Если РезультатОбновления.УспешнаяЗагрузка Тогда
					ЗафиксироватьТранзакцию();
				Иначе
					ОтменитьТранзакцию();
					ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Обновление классификаторов';
					|en = 'Обновление классификаторов'"), УровеньЖурналаРегистрации.Ошибка, , ,
						РезультатОбновления.СообщениеОбОшибке);
				КонецЕсли;
			Исключение
				ОтменитьТранзакцию();
				ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Обновление классификаторов';
					|en = 'Обновление классификаторов'"), УровеньЖурналаРегистрации.Ошибка, , ,
					ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			КонецПопытки;

			Классификаторы = Новый Массив;
			Классификаторы.Добавить(ИдентификаторКлассификатораВидовПродукцииИС());

			РезультатОбновления = РаботаСКлассификаторами.ОбновитьКлассификаторы(Классификаторы);

			Если РезультатОбновления.КодОшибки = "НеизвестнаяОшибка"
				Или РезультатОбновления.КодОшибки = "НеОбработан"
				Или РезультатОбновления.КодОшибки = "ФайлНеЗагружен"
				Или РезультатОбновления.КодОшибки = "НеизвестныйКлассификаторИлиПрограмма" Тогда
				ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Обновление классификаторов';
					|en = 'Обновление классификаторов'"), УровеньЖурналаРегистрации.Ошибка, , ,
					РезультатОбновления.СообщениеОбОшибке);
			КонецЕсли;

		КонецЕсли;

	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область РаботаСЧасовымиПоясами

// Дата из строки.
// 
// Параметры:
//  Значение - Строка
//  Делитель - Число - Делитель
//  ПриводитьКМестномуВремени - Булево - Приводить к местному времени
// 
// Возвращаемое значение:
//  Дата - Дата из строки
Функция ДатаИзСтроки(Значение, Делитель = 1000, ПриводитьКМестномуВремени = Истина) Экспорт
	
	Если СтрНайти(Значение, "T") > 0 Тогда
		Возврат ДатаИзСтрокиISO(Значение);
	ИначеЕсли СтрНайти(Значение, "-") > 0 Тогда
		Возврат ПрочитатьДатуJSON(Значение, ФорматДатыJSON.ISO);
	ИначеЕсли СтрНайти(Значение, ".") > 0 Тогда
		Возврат СтрокаВДату(Значение);
	Иначе
		Возврат ОбщегоНазначенияИСКлиентСервер.ДатаИзСтрокиUNIX(Значение, Делитель, ПриводитьКМестномуВремени);
	КонецЕсли;
	
КонецФункции

// Преобразует исходную строку в дату. 
// Если дату не удалось распознать, то возвращается пустая дата (01.01.01 00:00:00).
//
// Параметры:
//  Значение - Строка - строка, которую необходимо привести к дате.
//                      Формат даты должен быть в виде "ДД.ММ.ГГГГ" или "ДД/ММ/ГГ" или "ДД-ММ-ГГ ЧЧ:ММ:CC",
//                      Например, "23.02.1980" или "23/02/80 09:15:45".
// Возвращаемое значение:
//  Дата
//
Функция СтрокаВДату(Значение) Экспорт
	
	КешРезультатовФункции = Неопределено;
	Результат = ОбщегоНазначенияИСКлиентСервер.ПрочитатьКеш(
		ОбщегоНазначенияИСКлиентСерверПовтИсп.ГлобальныйКеш(), "Функция.СтрокаВДату", Значение, КешРезультатовФункции);
	Если Результат <> Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = СтроковыеФункцииКлиентСервер.СтрокаВДату(Значение);
	
	Если КешРезультатовФункции <> Неопределено Тогда
		КешРезультатовФункции.Вставить(Значение, Результат);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Дата из строки ISO.
// 
// Параметры:
//  Значение - Дата, Строка - Значение
// 
// Возвращаемое значение:
//  Дата - Дата из строки ISO
Функция ДатаИзСтрокиISO(Значение) Экспорт
	
	Конвертер = РаботаСXMLИС.ОбъектXDTOПоИмениСвойства("http://gosis.1c.ru/Convertor", "query");
	Конвертер.ХранилищеДаты = РаботаСXMLИС.ОбъектXDTOПоИмениТипа(Конвертер, "ХранилищеДаты");
	
	Попытка
		Конвертер.ХранилищеДаты.Значение = Значение;
		Возврат Конвертер.ХранилищеДаты.Значение;
	Исключение
		Возврат Дата(1, 1, 1);
	КонецПопытки;
	
КонецФункции

// Дата в строку UNIX.
// 
// Параметры:
//  Дата - Дата
//  Делитель - Число - Делитель
// 
// Возвращаемое значение:
//  Число - Дата в строку UNIX
Функция ДатаВСтрокуUNIX(Дата, Делитель = 1000) Экспорт
	
	Значение = (УниверсальноеВремя(Дата) - '19700101') * Делитель;
	
	Возврат Значение;
	
КонецФункции

#КонецОбласти

#Область ОписаниеНоменклатуры

// Возвращает новые параметры описания номенклатуры.
//
// Возвращаемое значение:
//  Структура - Инициализировать данные описания:
// * Номенклатура                        - Неопределено, ОпределяемыйТип.Номенклатура                - Номенклатура.
// * КлючВидовПродукции                  - СправочникСсылка.КлючиВидовПродукцииИС                    - Ключ видов продукции.
// * ЕмкостьПотребительскойУпаковки      - Число                                                     - Емкость потребительской упаковки для частичного выбытия.
// * КоличествоВПотребительскойУпаковке  - Число                                                     - Количество учетных единиц товара в потребительской упаковке.
// * ВариантЧастичногоВыбытия            - ПеречислениеСсылка.ВариантыУчетаЧастичногоВыбытияИС       - Вариант частичного выбытия.
// * ВариантИспользованияЕдиницыХранения - ПеречислениеСсылка.ВариантыИспользованияЕдиницыХраненияИС - Вариант использования единицы измерения.
// * ПотребительскаяУпаковка             - Неопределено, ОпределяемыйТип.Упаковка                    - потребительская упаковка
// * УпаковкаЧастичногоВыбытия           - Неопределено, ОпределяемыйТип.Упаковка                    - упаковка частичного выбытия
// * ИдентификаторыВидовПродукции        - см. РегистрыСведений.ОписаниеНоменклатурыИС.ИдентикаторыВидовПродукцииКлюча
//                                       - Неопределено
Функция ИнициализироватьДанныеОписанияНоменклатуры() Экспорт

	ДанныеНоменклатуры = Новый Структура();
	ДанныеНоменклатуры.Вставить("Номенклатура",                        Неопределено);
	ДанныеНоменклатуры.Вставить("КлючВидовПродукции",                  Справочники.КлючиВидовПродукцииИС.ПустаяСсылка());
	ДанныеНоменклатуры.Вставить("ЕмкостьПотребительскойУпаковки",      0);
	ДанныеНоменклатуры.Вставить("КоличествоВПотребительскойУпаковке",  1);
	ДанныеНоменклатуры.Вставить("ВариантЧастичногоВыбытия",            Перечисления.ВариантыУчетаЧастичногоВыбытияИС.ПустаяСсылка());
	ДанныеНоменклатуры.Вставить("ВариантИспользованияЕдиницыХранения", Перечисления.ВариантыИспользованияЕдиницыХраненияИС.ПотребительскаяУпаковка);
	ДанныеНоменклатуры.Вставить("ПотребительскаяУпаковка",             Неопределено);
	ДанныеНоменклатуры.Вставить("УпаковкаЧастичногоВыбытия",           Неопределено);
	ДанныеНоменклатуры.Вставить("ПроизвольнаяЕдиницаУчета",            Ложь);
	ДанныеНоменклатуры.Вставить("ТребуетВзвешивания",                  Ложь);
	ДанныеНоменклатуры.Вставить("ИдентификаторыВидовПродукции",        Неопределено);
	
	Возврат ДанныеНоменклатуры;

КонецФункции

// Возвращает описание входящей номенклатуры. Разворачивает описание из подходящего класса номенклатуры при необходимости.
//
// Параметры:
//   Номенклатура - ОпределяемыйТип.ИсточникОписанияНоменклатурыИС, Массив - номенклатура (класс номенклатуры) для которой требуется получить описание ИС
//
// Возвращаемое значение:
//   Соответствие Из ОпределяемыйТип.ИсточникОписанияНоменклатурыИС, Структура - номенклатура с описанием:
//    * Ключ   - ОпределяемыйТип.ИсточникОписанияНоменклатурыИС - номенклатура,
//    * Значение - см. ИнициализироватьДанныеОписанияНоменклатуры.
Функция ОписаниеНоменклатуры(Знач Номенклатура) Экспорт
	
	ТекстЗапроса = "";
	
	ИнтеграцияИСПереопределяемый.ПриОпределенииТекстаЗапросаОписаниеНоменклатуры(ТекстЗапроса);
	
	Если ТекстЗапроса = "" Тогда
		ТекстЗапроса = 
		"ВЫБРАТЬ
		|	ОписаниеНоменклатурыИС.Номенклатура                        КАК Номенклатура,
		|	ОписаниеНоменклатурыИС.ЕмкостьПотребительскойУпаковки      КАК ЕмкостьПотребительскойУпаковки,
		|	ОписаниеНоменклатурыИС.КоличествоВПотребительскойУпаковке  КАК КоличествоВПотребительскойУпаковке,
		|	ОписаниеНоменклатурыИС.ВариантЧастичногоВыбытия            КАК ВариантЧастичногоВыбытия,
		|	ОписаниеНоменклатурыИС.ВариантИспользованияЕдиницыХранения КАК ВариантИспользованияЕдиницыХранения,
		|	ОписаниеНоменклатурыИС.ПотребительскаяУпаковка             КАК ПотребительскаяУпаковка,
		|	ОписаниеНоменклатурыИС.УпаковкаЧастичногоВыбытия           КАК УпаковкаЧастичногоВыбытия,
		|	ОписаниеНоменклатурыИС.КлючВидовПродукции                  КАК КлючВидовПродукции,
		|	НЕ НастройкиЧастичногоВыбытияПродукцииИС.НоменклатураЧастичногоВыбытия ЕСТЬ NULL
		|		ИЛИ ОписаниеНоменклатурыИС.КоличествоВПотребительскойУпаковке <> 1
		|		ИЛИ ОписаниеНоменклатурыИС.ЕмкостьПотребительскойУпаковки > 0 КАК ПроизвольнаяЕдиницаУчета,
		|	ОписаниеНоменклатурыИС.КоличествоВПотребительскойУпаковке = 0 КАК ТребуетВзвешивания,
		|	ОписаниеНоменклатурыИС.ИдентификаторыВидовПродукции           КАК ИдентификаторыВидовПродукции
		|ИЗ
		|	РегистрСведений.ОписаниеНоменклатурыИС КАК ОписаниеНоменклатурыИС
		|	ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиЧастичногоВыбытияПродукцииИС КАК НастройкиЧастичногоВыбытияПродукцииИС
		|		ПО НастройкиЧастичногоВыбытияПродукцииИС.НоменклатураЧастичногоВыбытия = ОписаниеНоменклатурыИС.Номенклатура
		|ГДЕ
		|	ОписаниеНоменклатурыИС.Номенклатура В (&Номенклатура)";
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	Результат = Новый Соответствие;
	
	Если ТипЗнч(Номенклатура) = Тип("Массив") Тогда
		МассивНоменклатур = Номенклатура;
	Иначе 
		МассивНоменклатур = Новый Массив;
		МассивНоменклатур.Добавить(Номенклатура);
	КонецЕсли;

	Для Каждого ЭлементМассива Из МассивНоменклатур Цикл
		ДанныеНоменклатуры = ИнициализироватьДанныеОписанияНоменклатуры();
		ДанныеНоменклатуры.Номенклатура = ЭлементМассива;
		Результат.Вставить(ЭлементМассива, ДанныеНоменклатуры);
	КонецЦикла;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	
	Запрос.УстановитьПараметр("Номенклатура", МассивНоменклатур);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		ОписаниеНоменклатуры = Результат[Выборка.Номенклатура];
		ЗаполнитьЗначенияСвойств(ОписаниеНоменклатуры, Выборка,, "ИдентификаторыВидовПродукции");
		ОписаниеНоменклатуры.ИдентификаторыВидовПродукции = Выборка.ИдентификаторыВидовПродукции.Получить();
	КонецЦикла;
	
	Возврат Результат;

КонецФункции

// Возвращает свойства маркируемой продукции по номенклатуре.
//   Значение возвращаемого признака "Маркируемая продукция" зависит от контекста (параметров сканирования),
//   если он не задан - считается как "маркируемая где-либо продукция"
// 
// Параметры:
//  Номенклатура - ОпределяемыйТип.Номенклатура - Номенклатура.
//  ПараметрыСканирования - См. ШтрихкодированиеОбщегоНазначенияИС.ПараметрыСканирования
// Возвращаемое значение:
//  Структура - Описание:
//	 * Номенклатура - ОпределяемыйТип.Номенклатура - Номенклатура.
//	 * МаркируемаяПродукция - Булево - Истина, если продукция маркируемая.
//	 * ВидПродукции - СправочникСсылка.КлючиВидовПродукцииИС - Вид маркируемой продукции.
Функция СвойстваМаркируемойПродукции(Номенклатура, ПараметрыСканирования = Неопределено) Экспорт
	
	ТекстЗапроса =
	"ВЫБРАТЬ
	|	&Номенклатура   КАК Номенклатура
	|ПОМЕСТИТЬ ДанныеШтрихкодовУпаковок
	|;
	|
	|/////////////////////////////////////////////
	|"
	+
	ШтрихкодированиеОбщегоНазначенияИС.ТекстЗапросаСвойстваМаркируемойПродукции(,ПараметрыСканирования)
	+
	"
	|;
	|/////////////////////////////////////////////
	|
	|ВЫБРАТЬ
	|	&Номенклатура                              КАК Номенклатура,
	|	ДанныеПоВидуПродукции.МаркируемаяПродукция КАК МаркируемаяПродукция,
	|	ДанныеПоВидуПродукции.ВидПродукции         КАК ВидПродукции
	|ИЗ
	|	СвойстваМаркируемойПродукции КАК ДанныеПоВидуПродукции
	|";
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("Номенклатура",   Номенклатура);
	ДанныеТаблица = Запрос.Выполнить().Выгрузить();
	
	Если ДанныеТаблица.Количество() = 0 Тогда
		ДанныеТаблица.Добавить();
	КонецЕсли;
	СвойстваМаркируемойПродукции = ОбщегоНазначения.СтрокаТаблицыЗначенийВСтруктуру(ДанныеТаблица[0]);
	
	//@skip-check constructor-function-return-section
	Возврат СвойстваМаркируемойПродукции;
	
КонецФункции

// Работа с регистром "ОписаниеНоменклатурыИС" в запросе
//
// Параметры:
//  ТекстЗапроса           - Строка - текст запроса с регистром "ОписаниеНоменклатурыИС" (исходящий),
//  ПутьКРегистру          - Строка - имя таблицы регистра "ОписаниеНоменклатурыИС" в запросе,
//  ПутьКПолюНоменклатура  - Строка - путь к номенклатуре в запросе
//  ПутьКИсточникуОписания - Строка - путь к источнику описания
Процедура ОбновитьТекстЗапросаСРегистромОписаниеНоменклатурыИС(ТекстЗапроса, ПутьКРегистру, ПутьКПолюНоменклатура, ПутьКИсточникуОписания = "") Экспорт
	
	// Определение источника описания
	ИмяПоляЗамены = "&ИсточникОписанияНоменклатуры";
	ИсточникОписанияНоменклатуры = "
	|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ОписаниеНоменклатурыИС КАК ОписаниеНоменклатурыИС
	|	ПО &ТоварыОписаниеНоменклатурыИС";
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, ИмяПоляЗамены, ИсточникОписанияНоменклатуры);
	// Определение пути к источнику описания
	ИмяПоляЗамены = "&ПутьКИсточникуОписания";
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, ИмяПоляЗамены, ПутьКИсточникуОписания);
	// Соединение с регистром
	ИмяПоляЗамены = "&ТоварыОписаниеНоменклатурыИС";
	ТекстЗамены = СтрШаблон("%2.Номенклатура = %1", ПутьКПолюНоменклатура, ПутьКРегистру);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, ИмяПоляЗамены, ТекстЗамены);
	// Получение признака Произвольная единица учета
	ИмяПоляЗамены = "&ПроизвольнаяЕдиницаУчета";
	ТекстЗамены = СтрШаблон("ЕСТЬNULL(%1.КоличествоВПотребительскойУпаковке, 1) <> 1
	|	ИЛИ ЕСТЬNULL(%1.ЕмкостьПотребительскойУпаковки, 1) > 1
	|	ИЛИ 1 В
	|		(ВЫБРАТЬ ПЕРВЫЕ 1
	|			1
	|		ИЗ
	|			РегистрСведений.НастройкиЧастичногоВыбытияПродукцииИС КАК НастройкиЧастичногоВыбытияПродукцииИС
	|		ГДЕ
	|			%2 = НастройкиЧастичногоВыбытияПродукцииИС.НоменклатураЧастичногоВыбытия)
	|", ПутьКРегистру, ПутьКПолюНоменклатура);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, ИмяПоляЗамены, ТекстЗамены);
	// Получение признака Требует взвешивания
	ИмяПоляЗамены = "&ТребуетВзвешивания";
	ТекстЗамены = СтрШаблон("ЕСТЬNULL(%1.КоличествоВПотребительскойУпаковке, 1) = 0", ПутьКРегистру);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, ИмяПоляЗамены, ТекстЗамены);
	// Получение признака Ключ видов продукции
	ИмяПоляЗамены = "&КлючВидовПродукции";
	ТекстЗамены = СтрШаблон("%1.КлючВидовПродукции", ПутьКРегистру);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, ИмяПоляЗамены, ТекстЗамены);
	
КонецПроцедуры

// Обновить текст запроса описания номенклатуры условием вида продукции.
// 
// Параметры:
//  ТекстЗапроса - Строка
//  ПутьКУсловию - Строка
//  ПутьКПараметруВидПродукции - Строка
Процедура ОбновитьТекстЗапросаОписанияНоменклатурыУсловиемВидаПродукции(ТекстЗапроса, ПутьКУсловию, ПутьКПараметруВидПродукции) Экспорт
	
	ТекстУсловия = СтрШаблон(
		"(ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииГИСМТ        В (%1)
		|	ИЛИ ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииВЕТИС  В (%1)
		|	ИЛИ ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииЕГАИС  В (%1)
		|	ИЛИ ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииСАТУРН В (%1)
		|	ИЛИ ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииЗЕРНО  В (%1))",
		ПутьКПараметруВидПродукции);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, ПутьКУсловию, ТекстУсловия);
	
КонецПроцедуры

// Используется произвольная единица учета.
// 
// Параметры:
//  ВидыПродукцииИС - Неопределено, СправочникСсылка.ВидыПродукцииИС, Массив Из СправочникСсылка.ВидыПродукцииИС - Виды продукции ИС
// 
// Возвращаемое значение:
//  Булево - Используется произвольная единица учета
Функция ИспользуетсяПроизвольнаяЕдиницаУчета(ВидыПродукцииИС = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Если ВидыПродукцииИС = Неопределено Тогда
		Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	1
		|ИЗ
		|	РегистрСведений.ОписаниеНоменклатурыИС КАК ОписаниеНоменклатурыИС
		|	ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиЧастичногоВыбытияПродукцииИС КАК НастройкиЧастичногоВыбытияПродукцииИС
		|		ПО НастройкиЧастичногоВыбытияПродукцииИС.НоменклатураЧастичногоВыбытия = ОписаниеНоменклатурыИС.Номенклатура
		|ГДЕ
		|	ОписаниеНоменклатурыИС.КоличествоВПотребительскойУпаковке <> 1
		|	ИЛИ ОписаниеНоменклатурыИС.ЕмкостьПотребительскойУпаковки > 1
		|	ИЛИ НЕ НастройкиЧастичногоВыбытияПродукцииИС.НоменклатураЧастичногоВыбытия ЕСТЬ NULL";
	Иначе
		Запрос.УстановитьПараметр("ВидыПродукцииИС", ВидыПродукцииИС);
		Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	1
		|ИЗ
		|	РегистрСведений.ОписаниеНоменклатурыИС КАК ОписаниеНоменклатурыИС
		|	ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиЧастичногоВыбытияПродукцииИС КАК НастройкиЧастичногоВыбытияПродукцииИС
		|		ПО НастройкиЧастичногоВыбытияПродукцииИС.НоменклатураЧастичногоВыбытия = ОписаниеНоменклатурыИС.Номенклатура
		|ГДЕ
		|	(ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииЕГАИС В(&ВидыПродукцииИС)
		|	ИЛИ ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииГИСМТ В(&ВидыПродукцииИС))
		|	И (ОписаниеНоменклатурыИС.КоличествоВПотребительскойУпаковке <> 1
		|	ИЛИ ОписаниеНоменклатурыИС.ЕмкостьПотребительскойУпаковки > 1
		|	ИЛИ НЕ НастройкиЧастичногоВыбытияПродукцииИС.НоменклатураЧастичногоВыбытия ЕСТЬ NULL)";
	КонецЕсли;
	
	Возврат Не Запрос.Выполнить().Пустой();
	
КонецФункции

// Используется частичное выбытие.
// 
// Параметры:
//  ВидыПродукцииИС - Неопределено, СправочникСсылка.ВидыПродукцииИС, Массив Из СправочникСсылка.ВидыПродукцииИС - Виды продукции ИС
// 
// Возвращаемое значение:
//  Булево - Используется частичное выбытие
Функция ИспользуетсяЧастичноеВыбытие(ВидыПродукцииИС = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Если ВидыПродукцииИС = Неопределено Тогда
		Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	1
		|ИЗ
		|	РегистрСведений.ОписаниеНоменклатурыИС КАК ОписаниеНоменклатурыИС
		|ГДЕ
		|	ОписаниеНоменклатурыИС.ВариантЧастичногоВыбытия <> ЗНАЧЕНИЕ(Перечисление.ВариантыУчетаЧастичногоВыбытияИС.ПустаяСсылка)";
	Иначе
		Запрос.УстановитьПараметр("ВидыПродукцииИС", ВидыПродукцииИС);
		Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	1
		|ИЗ
		|	РегистрСведений.ОписаниеНоменклатурыИС КАК ОписаниеНоменклатурыИС
		|ГДЕ
		|	(ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииЕГАИС В(&ВидыПродукцииИС)
		|	ИЛИ ОписаниеНоменклатурыИС.КлючВидовПродукции.ВидПродукцииГИСМТ В(&ВидыПродукцииИС))
		|	И ОписаниеНоменклатурыИС.ВариантЧастичногоВыбытия <> ЗНАЧЕНИЕ(Перечисление.ВариантыУчетаЧастичногоВыбытияИС.ПустаяСсылка)";
	КонецЕсли;
	
	Возврат Не Запрос.Выполнить().Пустой();
	
КонецФункции

// Есть продукция требующая взвешивания.
// 
// Возвращаемое значение:
//  Булево - Есть продукция требующая взвешивания
Функция ЕстьПродукцияТребующаяВзвешивания() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	1
	|ИЗ
	|	РегистрСведений.ОписаниеНоменклатурыИС КАК ОписаниеНоменклатурыИС
	|ГДЕ
	|	ОписаниеНоменклатурыИС.КоличествоВПотребительскойУпаковке = 0";
	
	Возврат Не Запрос.Выполнить().Пустой();
	
КонецФункции

#КонецОбласти

#Область Запросы

// Формирует пакет запросов и возвращает результат каждого запроса
//
// Параметры:
//  Запрос				 - Запрос - запрос, параметры которого предварительно установлены.
//  ТекстыЗапроса		 - СписокЗначений из Строка - в списке перечислены тексты запросов и их имена.
//  ВыгрузитьРезультат	 - Булево - Истина, если результат запроса нужно выгрузить в таблицы значений.
// 
// Возвращаемое значение:
//  Структура - структура в которую помещены полученные таблицы
//
Функция ВыполнитьПакетЗапросов(Запрос, ТекстыЗапроса, ВыгрузитьРезультат = Ложь) Экспорт

	Таблицы = Новый Структура;
	
	МассивТекстаЗапроса = Новый Массив;
	
	// Формирование текст запроса.
	Для Каждого ТекстЗапроса из ТекстыЗапроса Цикл
		Если ЗначениеЗаполнено(ТекстЗапроса.Представление) Тогда
			МассивТекстаЗапроса.Добавить("// " + ТекстЗапроса.Представление);
		КонецЕсли;
		МассивТекстаЗапроса.Добавить(ТекстЗапроса.Значение);
		МассивТекстаЗапроса.Добавить(ОбщегоНазначения.РазделительПакетаЗапросов());
		
	КонецЦикла;
	
	Запрос.Текст = СтрСоединить(МассивТекстаЗапроса, Символы.ПС);
	
	// Выполнение запроса.
	Результат = Запрос.ВыполнитьПакет();

	КоличествоЗапросовВПакете = 0;
	
	// Помещение результатов запроса в таблицы
	Для Каждого ТекстЗапроса из ТекстыЗапроса Цикл
		
		Если СтрЧислоВхождений(ТекстЗапроса.Значение, ";") > 0 Тогда
			Подзапросы = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(ТекстЗапроса.Значение, ";");
			КоличествоПодзапросов = Подзапросы.Количество();
			ИндексЗапросаРезультате = Неопределено;
			Для Индекс = -(КоличествоПодзапросов - 1) По 0 Цикл
				Если Найти(Подзапросы[-Индекс], "#РезультатЗапроса#") > 0 Тогда
					ИндексЗапросаРезультате = -Индекс;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			Если ИндексЗапросаРезультате = Неопределено Тогда
				ИндексЗапросаРезультате = КоличествоПодзапросов - 1;
			КонецЕсли;
		Иначе
			КоличествоПодзапросов = 1;
			ИндексЗапросаРезультате = 0;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ТекстЗапроса.Представление) Тогда
			РезультатТаблица = Результат[КоличествоЗапросовВПакете + ИндексЗапросаРезультате];
			РезультатТаблица = ?(ВыгрузитьРезультат, РезультатТаблица.Выгрузить(), РезультатТаблица);
			Таблицы.Вставить(ТекстЗапроса.Представление, РезультатТаблица);
		КонецЕсли;
		
		КоличествоЗапросовВПакете = КоличествоЗапросовВПакете + КоличествоПодзапросов;
		
	КонецЦикла;
	
	Возврат Таблицы;
	
КонецФункции

#КонецОбласти

#Область УсловноеОформлениеПоляВводаПоТабличному

// Применить условное оформление к полю.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//  ИмяПоля - Строка - Имя поля
Процедура ПрименитьУсловноеОформлениеКПолю(Форма, ИмяПоля) Экспорт
	
	ЗначенияОформления = Новый Соответствие;
	Для Каждого ЭлементОформления Из Форма.УсловноеОформление.Элементы Цикл
		// Найдем условное оформление поля 
		ЭтоПодходящийЭлементУправления = Ложь;
		Для Каждого Поле Из ЭлементОформления.Поля.Элементы Цикл
			Если Поле.Поле = Новый ПолеКомпоновкиДанных(ИмяПоля) Тогда
				ЭтоПодходящийЭлементУправления = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		Если Не ЭтоПодходящийЭлементУправления Тогда
			Продолжить;
		КонецЕсли;
		
		УсловиеОтбораВыполнено = ПроверитьУсловиеОтбора(Форма, ЭлементОформления);
		
		Для Каждого Оформление Из ЭлементОформления.Оформление.Элементы Цикл
			Если Оформление.Использование Тогда
				Если ЗначенияОформления.Получить(""+Оформление.Параметр) = Неопределено Тогда
					ЗначенияОформления.Вставить(""+Оформление.Параметр);
				КонецЕсли;
				Если УсловиеОтбораВыполнено Тогда
					ЗначенияОформления.Вставить(""+Оформление.Параметр, Оформление.Значение);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЦикла;
	УстановитьУсловноеОформлениеВНетабличноеПоле(Форма.Элементы[ИмяПоля], ЗначенияОформления);
	
КонецПроцедуры

#КонецОбласти

#Область ПараметрыСеанса

// см. ОбщегоНазначенияПереопределяемый.ПриДобавленииОбработчиковУстановкиПараметровСеанса
Процедура ПриДобавленииОбработчиковУстановкиПараметровСеанса(Обработчики) Экспорт
	
	Обработчики.Вставить("ПараметрСеансаИС",                   "ОбщегоНазначенияИС.УстановитьПараметрыСеанса");
	Обработчики.Вставить("НастройкиРазбораКодаМаркировкиИС",   "ОбщегоНазначенияИС.УстановитьПараметрыСеанса");
	Обработчики.Вставить("ИдентификаторСеансаЕГАИС" ,          "ИнтеграцияЕГАИС.УстановитьПараметрыСеанса");
	Обработчики.Вставить("ПараметрыЛогированияЗапросовИСМП" ,  "ОбщегоНазначенияИСМП.УстановитьПараметрыСеанса");
	Обработчики.Вставить("ПараметрыЛогированияЗапросовЗЕРНО" , "ИнтеграцияЗЕРНО.УстановитьПараметрыСеанса");
	Обработчики.Вставить("ПараметрыЛогированияЗапросовВЕТИС" , "ИнтеграцияВЕТИС.УстановитьПараметрыСеанса");
	Обработчики.Вставить("ПараметрыЛогированияЗапросовСАТУРН", "ИнтеграцияСАТУРН.УстановитьПараметрыСеанса");
	Обработчики.Вставить("ДанныеКлючаСессииИСМП",              "ОбщегоНазначенияИСМП.УстановитьПараметрыСеанса");
	Обработчики.Вставить("ДанныеКлючаСессииИСМПРозница",       "ОбщегоНазначенияИСМП.УстановитьПараметрыСеанса");
	Обработчики.Вставить("CDNПлощадкиСОшибкамиОбменаИСМП",     "ОбщегоНазначенияИСМП.УстановитьПараметрыСеанса");
	Обработчики.Вставить("ДанныеКлючаСессииСАТУРН",            "ИнтеграцияСАТУРН.УстановитьПараметрыСеанса");
	
КонецПроцедуры

#Область JWT

// Расшифровать токен JWT.
// 
// Параметры:
//  Токен - Строка
// ПреобразовыватьВСоответствие - Булево - Признак преобразования в соответствие.
// Возвращаемое значение:
//  Структура - данные токена JWT:
// * РезультатРасшифровки - Неопределено - 
// * ТекстОшибки - Строка - 
// * Данные - Неопределено - 
Функция РасшифроватьТокенJWT(Токен, ПреобразовыватьВСоответствие = Ложь) Экспорт
	
	ВозвращаемоеЗначение = Новый Структура;
	ВозвращаемоеЗначение.Вставить("РезультатРасшифровки", Неопределено);
	ВозвращаемоеЗначение.Вставить("ТекстОшибки",          "");
	ВозвращаемоеЗначение.Вставить("Данные",               Неопределено);
	
	ЭлементыТокена = СтрРазделить(Токен, ".");
	Если ЭлементыТокена.Count() <> 3 Тогда
		ВозвращаемоеЗначение.ТекстОшибки = НСтр("ru = 'Токен не соответствует формату JWT';
												|en = 'Токен не соответствует формату JWT'");
		Возврат ВозвращаемоеЗначение;
	КонецЕсли;
	
	ЭлементТокенаДанные = ЭлементыТокена[1];
	
	ВозвращаемоеЗначение.Данные = ОбщегоНазначенияИСКлиентСервер.ТекстJSONВОбъект(
		ПолучитьСтрокуИзДвоичныхДанных(
			ДвоичныеДанныеЭлементаТокенаJWT(ЭлементТокенаДанные)),
		ПреобразовыватьВСоответствие);
	
	Возврат ВозвращаемоеЗначение;
	
КонецФункции

#КонецОбласти

// Выполняет установку параметров сеанса. Вызывается из модуля сеанса.
//
// Параметры:
//  ИмяПараметра			 - Строка - имя параметра сеанса.
//  УстановленныеПараметры	 - Массив из Строка - все установленные параметры сеанса.
//
Процедура УстановитьПараметрыСеанса(ИмяПараметра, УстановленныеПараметры) Экспорт
	
	Если ИмяПараметра = Метаданные.ПараметрыСеанса.ПараметрСеансаИС.Имя Тогда
		ПараметрыСеанса.ПараметрСеансаИС = ПараметрСеансаИС(Истина);
		Если ТипЗнч(УстановленныеПараметры) = Тип("Массив") Тогда
			УстановленныеПараметры.Добавить(ИмяПараметра);
		ИначеЕсли ТипЗнч(УстановленныеПараметры) = Тип("Структура") Тогда
			УстановленныеПараметры.Вставить(ИмяПараметра);
		КонецЕсли;
	ИначеЕсли ИмяПараметра = Метаданные.ПараметрыСеанса.НастройкиРазбораКодаМаркировкиИС.Имя Тогда
		ПараметрыСеанса.НастройкиРазбораКодаМаркировкиИС =
			Новый ФиксированнаяСтруктура(
				РазборКодаМаркировкиИССлужебный.ИнициализироватьКэшНастроекРазбораКодаМаркировки());
		Если ТипЗнч(УстановленныеПараметры) = Тип("Массив") Тогда
			УстановленныеПараметры.Добавить(ИмяПараметра);
		ИначеЕсли ТипЗнч(УстановленныеПараметры) = Тип("Структура") Тогда
			УстановленныеПараметры.Вставить(ИмяПараметра);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

// Параметры сеанса.
// 
// Параметры:
//  Инициализация - Булево - Инициализация
// 
// Возвращаемое значение:
//  ФиксированнаяСтруктура - Параметры сеанса:
// * ТребуетсяОбработкаПриПереходеНаКонструктор - Соответствие из КлючИЗначение - 
Функция ПараметрСеансаИС(Инициализация = Ложь) Экспорт
	
	Если Инициализация Тогда
		ПараметрыСеансаИС = Новый Структура;
		ПараметрыСеансаИС.Вставить("ТребуетсяОбработкаПриПереходеНаКонструктор", Новый Соответствие);
		Возврат ОбщегоНазначения.СкопироватьРекурсивно(ПараметрыСеансаИС, Истина);
	Иначе
		//@skip-check constructor-function-return-section
		Возврат ПараметрыСеанса.ПараметрСеансаИС;
	КонецЕсли;
	
КонецФункции

#КонецОбласти

// см. ОбщегоНазначенияПереопределяемый.ПриДобавленииСерверныхОповещений
Процедура ПриДобавленииСерверныхОповещений(Оповещения) Экспорт
	
	Если ЭтоРасширеннаяВерсияГосИС() Тогда
		Если ПодсистемаСуществует("ЗЕРНО") Тогда
			МодульИнтеграцияЗЕРНО = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияЗЕРНО");
			МодульИнтеграцияЗЕРНО.ПриДобавленииСерверныхОповещений(Оповещения);
		КонецЕсли;
		Если ПодсистемаСуществует("ИСМП")
			И ЭтоРасширеннаяВерсияГосИС("ИСМП") Тогда
			МодульСоответствиеТребованиямГИСМТ = ОбщегоНазначения.ОбщийМодуль("СоответствиеТребованиямГИСМТ");
			МодульСоответствиеТребованиямГИСМТ.ПриДобавленииСерверныхОповещений(Оповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

// см. ПользователиПереопределяемый.ПриОпределенииНазначенияРолей
Процедура ПриОпределенииНазначенияРолей(НазначениеРолей) Экспорт

	НазначениеРолей.ТолькоДляВнешнихПользователей.Добавить("БазовыеПраваВнешнегоПользователяИС");
	НазначениеРолей.ТолькоДляПользователейСистемы.Добавить("ДобавлениеИзменениеЭлементовКонструктораВидовПродукцииИС");
	
	Если ПодсистемаСуществует("ИСМП") Тогда
		НазначениеРолей.ТолькоДляПользователейСистемы.Добавить("ДобавлениеИзменениеЭлементовКонструктораВидовПродукцииИСМП");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

// Настроить подключаемое оборудование.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//  ПоддерживаемыеТипыПодключаемогоОборудования - Строка - поддерживаемые типы подключаемого оборудования
//  ПрефиксыЭлементовФормы - Строка - Префиксы элементов формы
Процедура НастроитьПодключаемоеОборудование(Форма, ПоддерживаемыеТипыПодключаемогоОборудования, ПрефиксыЭлементовФормы = "Товары") Экспорт
	
	ОбщегоНазначенияИСПереопределяемый.НастроитьПодключаемоеОборудование(Форма, ПрефиксыЭлементовФормы);
	
	ВерсияБПО = МенеджерОборудования.ВерсияБиблиотеки();
	
	Если ОбщегоНазначенияКлиентСервер.СравнитьВерсии(ВерсияБПО, "3.2.1.0") >= 0 Тогда
		
		МодульМенеджерОборудованияКлиентСервер = ОбщегоНазначения.ОбщийМодуль("МенеджерОборудованияКлиентСервер");
		МодульМенеджерОборудования             = ОбщегоНазначения.ОбщийМодуль("МенеджерОборудования");

		ТипыОборудования = МодульМенеджерОборудованияКлиентСервер.ПараметрыТипыОборудования();
		ТипыОборудования.СканерШтрихкода = (СтрНайти(ПоддерживаемыеТипыПодключаемогоОборудования, "СканерШтрихкода") > 0);
		ТипыОборудования.СчитывательRFID = (СтрНайти(ПоддерживаемыеТипыПодключаемогоОборудования, "СчитывательRFID") > 0);

		МодульМенеджерОборудования.ПриСозданииНаСервере(Форма, ТипыОборудования);
		
	КонецЕсли;
	
КонецПроцедуры

// Если в объекте заполнено значение реквизита, а в списке выбора, связанном с данным реквизитом,
// значение отсутствует, тогда такое значение необходимо добавит в список выбора.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
Процедура НастроитьСпискиВыбораФормыОбъекта(Форма) Экспорт
	
	Если Не ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "Объект")
		Или Не ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма.Объект, "Ссылка")
		Или Форма.Объект.Ссылка.Пустая() Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого Элемент Из Форма.Элементы Цикл
		Если ТипЗнч(Элемент) = Тип("ПолеФормы")
			И Элемент.Вид = ВидПоляФормы.ПолеВвода
			И Элемент.РежимВыбораИзСписка
			И ТипЗнч(Элемент.Родитель) <> Тип("ТаблицаФормы") Тогда
			ПутьКДанным = Элемент.ПутьКДанным;
			Если Лев(ПутьКДанным, 6) = "Объект" Тогда
				Значение = Форма.Объект[Сред(Элемент.ПутьКДанным, 8)];
			Иначе
				Значение = Форма[Элемент.ПутьКДанным];
			КонецЕсли;
			Если ЗначениеЗаполнено(Значение)
				И Элемент.СписокВыбора.НайтиПоЗначению(Значение) = Неопределено Тогда
				Элемент.СписокВыбора.Добавить(Значение);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#Область КлассификаторНастроекВидовПродукций

// Классификатор настроек видов продукций загружен.
// 
// Возвращаемое значение:
//  Булево - Классификатор настроек видов продукций загружен
Функция КлассификаторНастроекВидовПродукцийЗагружен() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ЗначенияЭлементовКонструктораВидовПродукцииИС.ЭлементКонструктора
		|ИЗ
		|	РегистрСведений.ЗначенияЭлементовКонструктораВидовПродукцииИС КАК ЗначенияЭлементовКонструктораВидовПродукцииИС
		|ГДЕ
		|	НЕ ЗначенияЭлементовКонструктораВидовПродукцииИС.ВидПродукции.ПометкаУдаления
		|	И НЕ ЗначенияЭлементовКонструктораВидовПродукцииИС.ВидПродукции.Предопределенный
		|	И ЗначенияЭлементовКонструктораВидовПродукцииИС.ВидПродукции.ХешСуммаПоставляемыхДанных <> """"";
	
	Результат = Не Запрос.Выполнить().Пустой();
	
	Возврат Результат;
	
КонецФункции

// Загружает классификатор через интернет-поддержку или из макета.
// 
// Возвращаемое значение:
//  Строка - текст ошибки загрузки при наличии
Функция ЗагрузитьКлассификаторНастроекВидовПродукций() Экспорт
	
	Результат = "";
	
	Если ОбщегоНазначения.РазделениеВключено() Тогда
		Результат = НСтр("ru = 'Отсутствуют классификаторы настроек видов продукции. Обратитесь к администратору.';
						|en = 'Отсутствуют классификаторы настроек видов продукции. Обратитесь к администратору.'");
		Возврат Результат;
	КонецЕсли;
	
	ИдентификаторКлассификатора = ИдентификаторКлассификатораВидовПродукцииИС();
	ВерсияКлассификатора        = РаботаСКлассификаторами.ВерсияКлассификатора(ИдентификаторКлассификатора);
	
	Если ВерсияКлассификатора = Неопределено Тогда
		ТребуемаяВерсияБиблиотекиБИП = МинимальнаяВерсияБиблиотекиИнтернетПоддержки();
		ТекущаяВерсияБиблиотекиБИП   = ИнтернетПоддержкаПользователейКлиентСервер.ВерсияБиблиотеки();
		СравнениеВерсийБиблиотекиБИП = ОбщегоНазначенияКлиентСервер.СравнитьВерсии(ТребуемаяВерсияБиблиотекиБИП,
			ТекущаяВерсияБиблиотекиБИП);
			
		ОписаниеКлассификаторовИС = Новый Массив;
		КлассификаторыКДобавлению = Новый Массив;
		
		ЗаполнитьОписаниеКлассификаторов(ОписаниеКлассификаторовИС, Истина, Ложь);
		
		Для Каждого ОписаниеКлассификатора Из ОписаниеКлассификаторовИС Цикл
			Если ОписаниеКлассификатора.Идентификатор = ИдентификаторКлассификатора Тогда
				КлассификаторыКДобавлению.Добавить(ОписаниеКлассификатора);
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если СравнениеВерсийБиблиотекиБИП > 0 Тогда
			ДобавитьКлассификаторы(КлассификаторыКДобавлению);
		Иначе
			МодульРаботаСКлассификаторами = ОбщегоНазначения.ОбщийМодуль("РаботаСКлассификаторами");
		//@skip-check bsl-legacy-check-static-feature-access-for-unknown-left-part
			МодульРаботаСКлассификаторами.ДобавитьКлассификаторы(КлассификаторыКДобавлению);
		КонецЕсли;
		
	КонецЕсли;
	
	Классификаторы = Новый Массив;
	Классификаторы.Добавить(ИдентификаторКлассификатора);
	
	УстановитьПривилегированныйРежим(Истина);
	РезультатОбновления = РаботаСКлассификаторами.ОбновитьКлассификаторы(Классификаторы);
	
	Если ЗначениеЗаполнено(РезультатОбновления.КодОшибки) Тогда
		НачатьТранзакцию();
		
		Попытка
			РезультатОбновления = ПервоначальноеЗаполнениеКлассификаторовВидовПродукции(Истина);
			Если Не РезультатОбновления.УспешнаяЗагрузка Тогда
				Результат = РезультатОбновления.СообщениеОбОшибке;
				ОтменитьТранзакцию();
			Иначе
				ЗафиксироватьТранзакцию();
			КонецЕсли;

		Исключение
			Результат = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ОтменитьТранзакцию();
			
		КонецПопытки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Начать загрузку классификатора настроек видов продукции.
// 
// Параметры:
//  УникальныйИдентификатор - УникальныйИдентификатор
// 
// Возвращаемое значение:
//  см. ДлительныеОперации.ВыполнитьВФоне
Функция НачатьЗагрузкуКлассификатораНастроекВидовПродукции(УникальныйИдентификатор) Экспорт
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Загрузка классификатора настроек видов продукции';
															|en = 'Загрузка классификатора настроек видов продукции'");
	
	ПараметрыФоновогоЗадания = Новый Структура;
	
	Если ОбщегоНазначенияИСМП.ЭтоРасширеннаяВерсияГосИС() Тогда
		МодульИнтеграцияИСПереопределяемый = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияИСПереопределяемый");
		МодульИнтеграцияИСПереопределяемый.НастроитьДлительнуюОперацию(ПараметрыФоновогоЗадания, ПараметрыВыполнения);
	КонецЕсли;
	
	ДлительнаяОперация = ДлительныеОперации.ВыполнитьВФоне(
		"ОбщегоНазначенияИС.ЗагрузкаКлассификатораНастроекВидовПродукцииДлительнаяОперация",
		ПараметрыФоновогоЗадания, ПараметрыВыполнения);
	
	Возврат ДлительнаяОперация;
	
КонецФункции

// Загрузка классификатора настроек видов продукции длительная операция.
// 
// Параметры:
//  ПараметрыФоновогоЗадания - Структура
//  АдресРезультата - Строка
Процедура ЗагрузкаКлассификатораНастроекВидовПродукцииДлительнаяОперация(ПараметрыФоновогоЗадания, АдресРезультата) Экспорт
	
	ВозвращаемоеЗначение = Новый Структура;
	ВозвращаемоеЗначение.Вставить("ЕстьОшибки",                     Ложь);
	ВозвращаемоеЗначение.Вставить("ТекстОшибки",                    "");
	
	Если КлассификаторНастроекВидовПродукцийЗагружен() Тогда
		ПоместитьВоВременноеХранилище(ВозвращаемоеЗначение, АдресРезультата);
		Возврат;
	КонецЕсли;
	
	ОшибкаЗагрузкиКлассификатора = ЗагрузитьКлассификаторНастроекВидовПродукций();
	
	Если ЗначениеЗаполнено(ОшибкаЗагрузкиКлассификатора) Тогда
		ВозвращаемоеЗначение.ЕстьОшибки  = Истина;
		ВозвращаемоеЗначение.ТекстОшибки = ОшибкаЗагрузкиКлассификатора;
		ПоместитьВоВременноеХранилище(ВозвращаемоеЗначение, АдресРезультата);
		Возврат;
	КонецЕсли;
	
	ПоместитьВоВременноеХранилище(ВозвращаемоеЗначение, АдресРезультата);
	
КонецПроцедуры

#КонецОбласти

// Ведется учет продукции ИС.
// 
// Возвращаемое значение:
//  Булево - Ведется учет продукции ИС
Функция ВедетсяУчетПродукцииИС() Экспорт
		
	ВедетсяУчетПродукцииИС    = Ложь;
	ФункциональнаяОпцияВЕТИС  = "ВестиУчетПодконтрольныхТоваровВЕТИС";
	ФункциональнаяОпцияЗерно  = "ВестиУчетЗернаИПродуктовПереработкиЗЕРНО";
	ФункциональнаяОпцияСатурн = "ВестиУчетПестицидовАгрохимикатовТукосмесейСАТУРН";
	ФункциональнаяОпцияИСМП   = ?(ЭтоРасширеннаяВерсияГосИС(), "ВестиУчетМаркируемойПродукцииИСМП",
		"ВестиУчетМаркируемойПродукцииИСМПРМК");
	ФункциональнаяОпцияЕГАИС  = ?(ЭтоРасширеннаяВерсияГосИС(), "ВестиСведенияДляДекларацийПоАлкогольнойПродукции",
		"ВестиСведенияДляДекларацийПоАлкогольнойПродукцииРМК");
	
	Если ПодсистемаСуществует("ВЕТИС") И ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияВЕТИС)
		Или ПодсистемаСуществует("ЕГАИС") И ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияЕГАИС)
		Или ПодсистемаСуществует("ЗЕРНО") И ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияЗерно)
		Или ПодсистемаСуществует("ИСМП") И ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияИСМП)
		Или ПодсистемаСуществует("САТУРН") И ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияСатурн) Тогда
		ВедетсяУчетПродукцииИС = Истина;
	КонецЕсли;
	
	Возврат ВедетсяУчетПродукцииИС;
	
КонецФункции

// См. СтандартныеПодсистемыСервер.ПриОпределенииМетодовРазрешенныхДляВызоваКакПроизвольныйКод
Процедура ПриОпределенииМетодовРазрешенныхДляВызоваКакПроизвольныйКод(Методы) Экспорт

	Методы.Вставить("ЗагрузкаКлассификатораНастроекВидовПродукцииДлительнаяОперация", Истина);
	Методы.Вставить("Пауза", Истина);
	Методы.Вставить("УстановитьПараметрыСеанса");

КонецПроцедуры

#Область СправочникиИКлассификаторы

// Переопределяются алгоритмы обработки файла загруженного
// из сервиса классификаторов. После обработки файла нельзя удалять
// временного хранилища, т.к. при необходимости он будет сохранен в
// кэше для последующего использования.
//
// Параметры:
//  Идентификатор           - Строка - идентификатор классификатора в сервисе классификаторов.
//                            Определяется в процедуре ПриДобавленииКлассификаторов.
//  Версия                  - Число - номер загруженной версии;
//  Адрес                   - Строка - адрес двоичных данных файла обновления во
//                            временном хранилище;
//  Обработан               - Булево - если Ложь, при обработке файла обновления были ошибки
//                            и его необходимо загрузить повторно;
//  ДополнительныеПараметры - Структура - содержит дополнительные параметры обработки.
//                            Необходимо использовать для передачи значений в переопределяемый
//                            метод РаботаСКлассификаторамиВМоделиСервисаПереопределяемый.ПриОбработкеОбластиДанных
//                            и метод ИнтеграцияПодсистемБИП.ПриОбработкеОбластиДанных..
// Пример:
//	Если Идентификатор = "CentralBankRefinancingRate" Тогда
//		Обработан = РегистрыСведений.СтавкиРефинансирования.ОбновитьДанныеРегистраИзФайла(Адрес, ДополнительныеПараметры);
//	КонецЕсли;
//
//@skip-warning
Процедура ПриЗагрузкеКлассификатора(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры) Экспорт
	
	Если ПодсистемаСуществует("ЗЕРНО") Тогда
		МодульИнтеграцияЗЕРНО = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияЗЕРНО");
		МодульИнтеграцияЗЕРНО.ПриЗагрузкеКлассификатора(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ВетИС") Тогда
		МодульИнтеграцияВЕТИС = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияВЕТИС");
		МодульИнтеграцияВЕТИС.ПриЗагрузкеКлассификатора(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ЕГАИС") Тогда
		МодульОбщегоНазначенияЕГАИС = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияЕГАИС");
		МодульОбщегоНазначенияЕГАИС.ПриЗагрузкеКлассификатора(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры);
	КонецЕсли;
	
	ПриЗагрузкеКлассификатораИС(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры);
	
КонецПроцедуры

// Переопределяются номер версии классификатора, который уже загружен в информационную базу.
// При начале использовании подсистемы "РаботаСКлассификаторами" или при подключении нового
// классификатора к сервису, неизвестно какой номер версии классификатора загружен в ИБ,
// поэтому при очередной итерации обновления данных из сервиса данные будут загружены повторно.
// Чтобы избежать повторной загрузки, необходимо указать задать начальный номер версии.
// Метод будет вызван при попытке загрузить версию классификатора, у которого установлена
// версия равная 0.
//
// Параметры:
//  Идентификатор        - Строка - идентификатор классификатора в сервисе классификаторов.
//                         Определяется в процедуре ПриДобавленииКлассификаторов.
//  НачальныйНомерВерсии - Число - номер версии загруженного классификатора.
//
// Пример:
//	Если Идентификатор = "CentralBankRefinancingRate" Тогда
//		НачальныйНомерВерсии = РегистрыСведений.СтавкиРефинансирования.НомерЗагруженнойВерсии();
//	КонецЕсли;
//
//@skip-warning
Процедура ПриОпределенииНачальногоНомераВерсииКлассификатора(Идентификатор, НачальныйНомерВерсии) Экспорт
	
	Если ПодсистемаСуществует("ЗЕРНО") Тогда
		МодульИнтеграцияЗЕРНО = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияЗЕРНО");
		МодульИнтеграцияЗЕРНО.ПриОпределенииНачальногоНомераВерсииКлассификатора(Идентификатор, НачальныйНомерВерсии);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ВетИС") Тогда
		МодульИнтеграцияВЕТИС = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияВЕТИС");
		МодульИнтеграцияВЕТИС.ПриОпределенииНачальногоНомераВерсииКлассификатора(Идентификатор, НачальныйНомерВерсии);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ЕГАИС") Тогда
		МодульОбщегоНазначенияЕГАИС = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияЕГАИС");
		МодульОбщегоНазначенияЕГАИС.ПриОпределенииНачальногоНомераВерсииКлассификатора(Идентификатор, НачальныйНомерВерсии);
	КонецЕсли;
	
	ПриОпределенииНачальногоНомераВерсииКлассификатораИС(Идентификатор, НачальныйНомерВерсии);
	
КонецПроцедуры

// Переопределяется список и настройки классификаторов обновления которых необходимо
// загружать из сервиса классификаторов. Для получения идентификатора необходимо
// перевести наименование объекта метаданных, данные которого планируется обновлять,
// на английский язык. При переводе рекомендуется использовать профессиональные
// программы перевода текста, либо воспользоваться услугами переводчика, т.к. при
// обнаружении смысловых ошибок в идентификаторе потребуется заводить новый классификатор
// и изменять код конфигурации.
//
// Параметры:
//  Классификаторы  - Массив из Структура - содержит настройки загрузки классификаторов.
//                    Состав настроек см. функцию РаботаСКлассификаторами.ОписаниеКлассификатора.
//
// Пример:
//	Описатель = РаботаСКлассификаторами.ОписаниеКлассификатора();
//	Описатель.Наименование               = НСтр("ru = 'Ставки рефинансирования'");
//	Описатель.Идентификатор              = "CentralBankRefinancingRate";
//	Описатель.ОбновлятьАвтоматически     = Истина;
//	Описатель.ОбщиеДанные                = Истина;
//	Описатель.СохранятьФайлВКэш          = Ложь;
//	Описатель.ОбработкаРазделенныхДанных = Ложь;
//	Классификаторы.Добавить(Описатель);
//
//@skip-warning
Процедура ПриДобавленииКлассификаторов(Классификаторы) Экспорт
	
	Если ПодсистемаСуществует("ЗЕРНО") Тогда
		МодульИнтеграцияЗЕРНО = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияЗЕРНО");
		МодульИнтеграцияЗЕРНО.ПриДобавленииКлассификаторов(Классификаторы);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ВетИС") Тогда
		МодульИнтеграцияВЕТИС = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияВЕТИС");
		МодульИнтеграцияВЕТИС.ПриДобавленииКлассификаторов(Классификаторы);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ЕГАИС") Тогда
		МодульОбщегоНазначенияЕГАИС = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияЕГАИС");
		МодульОбщегоНазначенияЕГАИС.ПриДобавленииКлассификаторов(Классификаторы);
	КонецЕсли;
	
	ПриДобавленииКлассификаторовИС(Классификаторы);
	
КонецПроцедуры

// см. РаботаСКлассификаторамиВМоделиСервисаПереопределяемый.ПриОпределенииИдентификаторовКлассификаторов
Процедура ПриОпределенииИдентификаторовКлассификаторовВМоделиСервиса(Идентификаторы) Экспорт
	
	Если ПодсистемаСуществует("ЗЕРНО") Тогда
		МодульИнтеграцияЗЕРНО = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияЗЕРНО");
		МодульИнтеграцияЗЕРНО.ПриОпределенииИдентификаторовКлассификаторовВМоделиСервиса(Идентификаторы);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ВетИС") Тогда
		МодульИнтеграцияВЕТИС = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияВЕТИС");
		МодульИнтеграцияВЕТИС.ПриОпределенииИдентификаторовКлассификаторовВМоделиСервиса(Идентификаторы);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ЕГАИС") Тогда
		МодульОбщегоНазначенияЕГАИС = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияЕГАИС");
		МодульОбщегоНазначенияЕГАИС.ПриОпределенииИдентификаторовКлассификаторовВМоделиСервиса(Идентификаторы);
	КонецЕсли;
	
	ПриОпределенииИдентификаторовКлассификаторовВМоделиСервисаИС(Идентификаторы);
	
КонецПроцедуры

Функция ПервоначальноеЗаполнениеКлассификаторовВидовПродукции(БезусловноеЗаполнение=Ложь, ПолнаяЗагрузка=Ложь) Экспорт

    РезультатОбновления = ИнициализироватьРезультатыОбновленияКлассификаторовВидовПродукцииИС();
	РазделениеВключено  = ОбщегоНазначения.РазделениеВключено();
	
	Если Не БезусловноеЗаполнение Тогда
		Если Не РазделениеВключено И Не ВедетсяУчетПродукцииИС() Тогда
			Возврат РезультатОбновления;
		КонецЕсли;
	КонецЕсли;
	
	Идентификатор                   = ИдентификаторКлассификатораВидовПродукцииИС();
	СохраненнаяВерсияКлассификатора = РегистрыСведений.ПоставляемыеДанныеКлассификаторовВидовПродукцииИС.СохраненныеДанныеКлассификаторовВидовПродукцииИС(
		Идентификатор);
	
	Если СохраненнаяВерсияКлассификатора <> Неопределено Тогда
		АдресКлассификаторов = СохраненнаяВерсияКлассификатора;	
	Иначе
		ДвоичныеДанныеКлассификаторов = ПолучитьОбщийМакет("КлассификаторыВидовПродукцииИС");
		АдресКлассификаторов          = ПоместитьВоВременноеХранилище(ДвоичныеДанныеКлассификаторов);
	КонецЕсли;
	
	АдресДополнительногоКлассификатора = ДополнительныйМакетОбновленияКлассификатораВидовПродукцииИС();
	Если АдресДополнительногоКлассификатора <> Неопределено Тогда
		АдресКлассификаторов = АдресДополнительногоКлассификатора;
	КонецЕсли;
	
	ПараметрыЗагрузки = ПараметрыЗагрузкиКлассификаторовИС();
	
	ПараметрыЗагрузки.ПолнаяЗагрузка = ПолнаяЗагрузка;
	
	ОбновлениеКлассификаторовВидовПродукцииИС(АдресКлассификаторов, ПараметрыЗагрузки, РезультатОбновления);
	
	Если СохраненнаяВерсияКлассификатора <> Неопределено Тогда
		РегистрыСведений.ПоставляемыеДанныеКлассификаторовВидовПродукцииИС.ОбновитьПризнакЧастичнойЗагрузкиКлассификаторовВидовПродукцииИС(
			Идентификатор, РезультатОбновления.ДанныеЗагруженыЧастично);
	КонецЕсли;
	
	Возврат РезультатОбновления;
	
КонецФункции

// см. РаботаВМоделиСервисаПереопределяемый.ПриВключенииРазделенияПоОбластямДанных
Процедура ПриВключенииРазделенияПоОбластямДанных() Экспорт
	
	ТребуемаяВерсияБиблиотекиБИП = МинимальнаяВерсияБиблиотекиИнтернетПоддержки();
	ТекущаяВерсияБиблиотекиБИП   = ИнтернетПоддержкаПользователейКлиентСервер.ВерсияБиблиотеки();
	СравнениеВерсийБиблиотекиБИП = ОбщегоНазначенияКлиентСервер.СравнитьВерсии(ТребуемаяВерсияБиблиотекиБИП,
		ТекущаяВерсияБиблиотекиБИП);
	
	Классификаторы = Новый Массив;
	
	ЗаполнитьОписаниеКлассификаторов(Классификаторы, Истина, Ложь);
	
	Если ПодсистемаСуществует("ЕГАИС") Тогда
		МодульОбщегоНазначенияЕГАИС = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияЕГАИС");
		МодульОбщегоНазначенияЕГАИС.ЗаполнитьОписаниеКлассификаторов(Классификаторы, Истина);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ВетИС") Тогда
		МодульИнтеграцияВЕТИС = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияВЕТИС");
		МодульИнтеграцияВЕТИС.ЗаполнитьОписаниеКлассификаторов(Классификаторы, Истина);
	КонецЕсли;
	
	Если ПодсистемаСуществует("ЗЕРНО") Тогда
		МодульИнтеграцияЗЕРНО = ОбщегоНазначения.ОбщийМодуль("ИнтеграцияЗЕРНО");
		МодульИнтеграцияЗЕРНО.ЗаполнитьОписаниеКлассификаторов(Классификаторы, Истина);
	КонецЕсли;
	
	Если СравнениеВерсийБиблиотекиБИП > 0 Тогда
		ДобавитьКлассификаторы(Классификаторы);
	Иначе
		МодульРаботаСКлассификаторами = ОбщегоНазначения.ОбщийМодуль("РаботаСКлассификаторами");
		//@skip-check bsl-legacy-check-static-feature-access-for-unknown-left-part
		МодульРаботаСКлассификаторами.ДобавитьКлассификаторы(Классификаторы);
	КонецЕсли;
	
КонецПроцедуры

Функция МинимальнаяВерсияБиблиотекиИнтернетПоддержки()
	Возврат "2.8.3.0";
КонецФункции

Процедура ДобавитьКлассификаторы(Классификаторы)
	
	Для каждого Классификатор Из Классификаторы Цикл
		Запись = РегистрыСведений.ВерсииКлассификаторов.СоздатьМенеджерЗаписи();
		ЗаполнитьЗначенияСвойств(Запись, Классификатор);
		Запись.Записать();
	КонецЦикла;
	
КонецПроцедуры

#Область КлассификаторыИС

// Идентификаторы классификаторов.
// 
// Параметры:
//  УчитыватьОпции - Булево - Добавлять идентификаторы с учетом включенных опций
// 
// Возвращаемое значение:
//  Соответствие - Идентификаторы классификаторов
Функция ИдентификаторыКлассификаторов(Знач УчитыватьОпции=Ложь) Экспорт
	
	РазделениеВключено      = ОбщегоНазначения.РазделениеВключено();
	Идентификаторы          = Новый Соответствие();
	ВестиУчетПродукцииЕГАИС = Ложь;
	ВестиУчетПродукцииИСМП  = Ложь;
	
	Если РазделениеВключено Тогда
		УчитыватьОпции = Ложь;
	КонецЕсли;
	
	Если УчитыватьОпции Тогда
		Если ПодсистемаСуществует("ЕГАИС") Тогда
			МодульОбщегоНазначенияЕГАИС = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияЕГАИС");
			ВестиУчетПродукцииЕГАИС     = МодульОбщегоНазначенияЕГАИС.ИспользуетсяМаркируемаяПродукция();
		КонецЕсли;

		Если ПодсистемаСуществует("ИСМП") Тогда
			МодульОбщегоНазначенияИСМП = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияИСМП");
			ВестиУчетПродукцииИСМП     = МодульОбщегоНазначенияИСМП.ИспользуетсяМаркируемаяПродукция();
		КонецЕсли;
	КонецЕсли;
	
	Если Не УчитыватьОпции Или ВестиУчетПродукцииЕГАИС Или ВестиУчетПродукцииИСМП Тогда
		Идентификаторы.Вставить(
		"MinimalPricesForExcisableProducts", Новый Структура("Наименование, ВидКлассификатора", НСтр(
			"ru = 'Минимальные цены для алкогольной и табачной продукции ИС';
			|en = 'Минимальные цены для алкогольной и табачной продукции ИС'"),
			"РегистрСведений.МинимальныеЦеныПодакцизныхТоваровИС"));
	КонецЕсли;
	
	ИдентификаторыВидовПродукции = ИдентификаторыКлассификатораВидовПродукцииИС(УчитыватьОпции);
	Для Каждого Идентификатор Из ИдентификаторыВидовПродукции Цикл
		Идентификаторы.Вставить(Идентификатор.Ключ, Идентификатор.Значение);
	КонецЦикла;
	
	Возврат Идентификаторы;
	
КонецФункции

// см. РаботаСКлассификаторамиПереопределяемый.ПриДобавленииКлассификаторов
Процедура ПриДобавленииКлассификаторовИС(Классификаторы)
	
	РазделениеВключено = ОбщегоНазначения.РазделениеВключено();
	
	ЗаполнитьОписаниеКлассификаторов(Классификаторы, РазделениеВключено, Истина);
	
КонецПроцедуры

// см. РаботаСКлассификаторамиПереопределяемый.ПриЗагрузкеКлассификатора
Процедура ПриЗагрузкеКлассификатораИС(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры)
	
	ИдентификаторыКлассификаторов = ИдентификаторыКлассификаторов();
	ДанныеКлассификатора          = ИдентификаторыКлассификаторов[Идентификатор];
	
	Если ДанныеКлассификатора = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ИдентификаторВидовПродукцииИС = ИдентификаторКлассификатораВидовПродукцииИС();
	
	Если Идентификатор = ИдентификаторВидовПродукцииИС Тогда
		ПриЗагрузкеКлассификатораВидовПродукцииИС(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры);
	Иначе
		ДвоичныеДанные      = ПолучитьИзВременногоХранилища(Адрес); // - ДвоичныеДанные
		ВременныеФайлы      = Новый Массив;
		ФайлыКлассификатора = Новый Массив;

		Попытка

			ПотокЧтения = ДвоичныеДанные.ОткрытьПотокДляЧтения();

			ЧтениеZIP = Новый ЧтениеZIPФайла(ПотокЧтения);
		
		//@skip-check missing-temporary-file-deletion
			ВременныйКаталог = ПолучитьИмяВременногоФайла("cat");
			СоздатьКаталог(ВременныйКаталог);

			Для Каждого ЭлементАрхива Из ЧтениеZIP.Элементы Цикл
				ИмяВременногоФайла = ОбъединитьПути(ВременныйКаталог, ЭлементАрхива.ПолноеИмя);
				ФайлыКлассификатора.Добавить(ИмяВременногоФайла);
				ВременныеФайлы.Добавить(ИмяВременногоФайла);
				ЧтениеZIP.Извлечь(ЭлементАрхива, ВременныйКаталог);
			КонецЦикла;

			ЧтениеZIP.Закрыть();
			ПотокЧтения.Закрыть();

		Исключение
			ТекстОшибки = СтрШаблон(
			НСтр("ru = 'Ошибка подготовки исходных файлов для загрузки классификатора ИС %1:
				 |%2';
				 |en = 'Ошибка подготовки исходных файлов для загрузки классификатора ИС %1:
				 |%2'", ОбщегоНазначения.КодОсновногоЯзыка()), ДанныеКлассификатора.ВидКлассификатора,
				ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			ТекстОписанияОшибки = СтрШаблон(
			НСтр("ru = 'Ошибка подготовки исходных файлов для загрузки классификатора ИС %1:
				 |%2';
				 |en = 'Ошибка подготовки исходных файлов для загрузки классификатора ИС %1:
				 |%2'", ОбщегоНазначения.КодОсновногоЯзыка()), ДанныеКлассификатора.ВидКлассификатора,
				ОбработкаОшибок.КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
			ОбщегоНазначенияИСВызовСервера.ЗаписатьОшибкуВЖурналРегистрации(
			ТекстОшибки, НСтр("ru = 'Работа с классификаторами ИС';
								|en = 'Работа с классификаторами ИС'", ОбщегоНазначения.КодОсновногоЯзыка()));
			ВызватьИсключение ТекстОписанияОшибки;
		КонецПопытки;

		МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени(ДанныеКлассификатора.ВидКлассификатора);
		МенеджерОбъекта.ПриЗагрузкеКлассификатора(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры,
			ФайлыКлассификатора);

		Если ЗначениеЗаполнено(ВременныйКаталог) Тогда
			ВременныеФайлы.Добавить(ВременныйКаталог);
		КонецЕсли;
		Для Каждого ИмяВременногоФайла Из ВременныеФайлы Цикл
			УдалитьФайлы(ИмяВременногоФайла);
		КонецЦикла;

	КонецЕсли;
	
КонецПроцедуры

// см. РаботаСКлассификаторамиПереопределяемый.ПриОпределенииНачальногоНомераВерсииКлассификатора
Процедура ПриОпределенииНачальногоНомераВерсииКлассификатораИС(Идентификатор, НачальныйНомерВерсии)
	
	Возврат;
	
КонецПроцедуры

// см. РаботаСКлассификаторамиВМоделиСервисаПереопределяемый.ПриОпределенииИдентификаторовКлассификаторов
Процедура ПриОпределенииИдентификаторовКлассификаторовВМоделиСервисаИС(Идентификаторы)
	
	ИдентификаторыКлассификаторов = ИдентификаторыКлассификаторов();
	
	Для Каждого КлючИЗначение Из ИдентификаторыКлассификаторов Цикл
		
		Идентификаторы.Добавить(КлючИЗначение.Ключ);
		
	КонецЦикла;
	
КонецПроцедуры

// см. РаботаСКлассификаторами.ОписаниеКлассификатора
Процедура ЗаполнитьОписаниеКлассификаторов(Классификаторы, СохранятьФайлВКэш, УчитыватьОпции)
	
	ИдентификаторыКлассификаторов = ИдентификаторыКлассификаторов(УчитыватьОпции);
	
	Для Каждого КлючИЗначение Из ИдентификаторыКлассификаторов Цикл
		
		Описатель = РаботаСКлассификаторами.ОписаниеКлассификатора();
		Описатель.Наименование               = КлючИЗначение.Значение.Наименование;
		Описатель.Идентификатор              = КлючИЗначение.Ключ;
		Описатель.ОбновлятьАвтоматически     = Истина;
		Описатель.ОбщиеДанные                = Истина;
		Описатель.СохранятьФайлВКэш          = СохранятьФайлВКэш;
		Описатель.ОбработкаРазделенныхДанных = Ложь;
		
		Классификаторы.Добавить(Описатель);
		
	КонецЦикла;
	
КонецПроцедуры

// Текст запроса проверки необходимости загрузки классификаторов ИС.
// 
// Параметры:
//  Классификатор - Строка
// 
// Возвращаемое значение:
//  Строка - Текст запроса проверки необходимости загрузки классификаторов ИС
Функция ТекстЗапросаПроверкиНеобходимостиЗагрузкиКлассификаторовИС(Классификатор) Экспорт
	
	ТекстЗапроса = "";
	
	Если Классификатор = "MinimalPricesForExcisableProducts" Тогда
		ТекстЗапроса = "ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	МинимальныеЦеныПодакцизныхТоваровИС.МинимальнаяЦена КАК МинимальнаяЦена
		|ИЗ
		|	РегистрСведений.МинимальныеЦеныПодакцизныхТоваровИС КАК МинимальныеЦеныПодакцизныхТоваровИС";
	ИначеЕсли Классификатор = ИдентификаторКлассификатораВидовПродукцииИС() Тогда
		ТекстЗапроса = ТекстЗапросаПроверкиНеобходимостиЗагрузкиКлассификатораВидовПродукцииИС();
	КонецЕсли;
	
	Возврат ТекстЗапроса;
	
КонецФункции

#Область КлассификаторВидовПродукцииИС

// Возвращает идентификаторы классификатора видов продукции БГосИС с учетом включенного разделения данных и настроек учета продукции.
//
// Параметры:
//   УчитыватьОпции - Булево - признак необходимости учета функциональной опции использования ИС
//
// Возвращаемое значение:
//   Соответствие:
//     * Ключ - Строка - идентификатор классификатора
//     * Значение - Структура:
//         ** Наименование - Строка - наименование классификатора
//         ** ВидКлассификатора - Строка, Неопределено - вид классификатора (если указан)
Функция ИдентификаторыКлассификатораВидовПродукцииИС(Знач УчитыватьОпции=Ложь) Экспорт
	
	РазделениеВключено     = ОбщегоНазначения.РазделениеВключено();
	Идентификаторы         = Новый Соответствие;
	ВедетсяУчетПродукцииИС = Ложь;
	
	Если РазделениеВключено Тогда
		УчитыватьОпции = Ложь;
	КонецЕсли;
	
	Если УчитыватьОпции Тогда
		ВедетсяУчетПродукцииИС = ВедетсяУчетПродукцииИС();
	КонецЕсли;
	
	Если Не УчитыватьОпции Или ВедетсяУчетПродукцииИС Тогда
		Идентификаторы.Вставить(
		"ClassifierOfProductTypeSettingsBGosIS", Новый Структура("Наименование, ВидКлассификатора", НСтр(
			"ru = 'Классификатор настроек видов продукции БГосИС';
			|en = 'Классификатор настроек видов продукции БГосИС'")));
	КонецЕсли;
	
	Возврат Идентификаторы;
	
КонецФункции

// см. РаботаСКлассификаторамиПереопределяемый.ПриЗагрузкеКлассификатора
Процедура ПриЗагрузкеКлассификатораВидовПродукцииИС(Идентификатор, Версия, Адрес, Обработан, ДополнительныеПараметры) Экспорт
	
	ИдентификаторыКлассификаторов = ИдентификаторыКлассификаторов();
	ДанныеКлассификатора          = ИдентификаторыКлассификаторов[Идентификатор];
	
	Если ДанныеКлассификатора = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыЗагрузки   = ПараметрыЗагрузкиКлассификаторовИС();
	РезультатОбновления = ИнициализироватьРезультатыОбновленияКлассификаторовВидовПродукцииИС();
	
	Если ДополнительныеПараметры.Свойство("ПараметрыЗагрузки") Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыЗагрузки, ДополнительныеПараметры.ПараметрыЗагрузки);
	КонецЕсли;
	
	НачатьТранзакцию();
	
	Попытка
		ОбновлениеКлассификаторовВидовПродукцииИС(Адрес, ПараметрыЗагрузки, РезультатОбновления);
		
		Если Не РезультатОбновления.УспешнаяЗагрузка Тогда
			ВызватьИсключение РезультатОбновления.СообщениеОбОшибке;
		КонецЕсли;
		
		Обработан = Истина;
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		ОтменитьТранзакцию();
		
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'Не удалось загрузить классификаторы %1 по причине:
				 |%2';
				 |en = 'Не удалось загрузить классификаторы %1 по причине:
				 |%2'"), ДанныеКлассификатора.ВидКлассификатора, ОбработкаОшибок.ПодробноеПредставлениеОшибки(
				ИнформацияОбОшибке()));
				
		ВызватьИсключение ТекстСообщения;
		
	КонецПопытки;
	
	РегистрыСведений.ПоставляемыеДанныеКлассификаторовВидовПродукцииИС.ДобавитьПоставляемыеДанныеКлассификаторовВидовПродукцииИС(
		Идентификатор, Версия, Адрес, РезультатОбновления.ДанныеЗагруженыЧастично);
	
	ОбновитьПовторноИспользуемыеЗначения();
	
	Если Не ОбщегоНазначения.РазделениеВключено() Тогда
		РегистрыСведений.ОписаниеНоменклатурыИС.ЗаполнитьОтсутствующиеКлючиПоИдентификаторамВидовПродукции();
	КонецЕсли;
	
КонецПроцедуры

// Обновляет классификаторы видов продукции, товарных групп и справочников ИС из ZIP-архива.
//
// Параметры:
//   Адрес - Строка - адрес во временном хранилище ZIP-архива с классификаторами
//   ПараметрыЗагрузки - Структура - параметры обновления классификаторов
//   РезультатОбновления - Структура - структура для записи результатов обновления, содержит сообщения об ошибках и признак успешной загрузки
Процедура ОбновлениеКлассификаторовВидовПродукцииИС(Адрес, ПараметрыЗагрузки, РезультатОбновления)
	
	Если ОбщегоНазначения.ЭтоПодчиненныйУзелРИБ() Тогда
		Возврат;
	КонецЕсли;
	
	ДвоичныеДанные      = ПолучитьИзВременногоХранилища(Адрес);
	ВременныеФайлы      = Новый Массив();
	ФайлыКлассификатора = Новый Соответствие();
	ДанныеДляЗагрузки   = Новый Соответствие();
	РасширеннаяВерсия   = ЭтоРасширеннаяВерсияГосИС();
	
	Попытка
		ПотокЧтения = ДвоичныеДанные.ОткрытьПотокДляЧтения();

		ЧтениеZIP = Новый ЧтениеZIPФайла(ПотокЧтения);
		
		//@skip-check missing-temporary-file-deletion
		ВременныйКаталог = ПолучитьИмяВременногоФайла("cat");
		СоздатьКаталог(ВременныйКаталог);

		Для Каждого ЭлементАрхива Из ЧтениеZIP.Элементы Цикл
			ИмяВременногоФайла = ОбъединитьПути(ВременныйКаталог, ЭлементАрхива.ПолноеИмя);
			ФайлыКлассификатора.Вставить(ЭлементАрхива.ИмяБезРасширения, ИмяВременногоФайла);
			ВременныеФайлы.Добавить(ИмяВременногоФайла);
			ЧтениеZIP.Извлечь(ЭлементАрхива, ВременныйКаталог);
		КонецЦикла;

		ЧтениеZIP.Закрыть();
		ПотокЧтения.Закрыть();

	Исключение
		ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		РезультатОбновления.СообщениеОбОшибке = ТекстОшибки;
		РезультатОбновления.УспешнаяЗагрузка  = Ложь;
	КонецПопытки;
	
	Если Не ФайлыКлассификатора.Количество() Тогда
		Возврат;
	КонецЕсли;
	
	Попытка
		
		Для Каждого ФайлКлассификатора Из ФайлыКлассификатора Цикл
			Чтение = Новый ЧтениеJSON;
			Чтение.ОткрытьФайл(ФайлКлассификатора.Значение);	
			ДанныеКлассификатора = ПрочитатьJSON(Чтение);	
			ДанныеДляЗагрузки.Вставить(ФайлКлассификатора.Ключ, ДанныеКлассификатора);
			Чтение.Закрыть();
		КонецЦикла;
		
	Исключение
		ТекстОшибки = СтрШаблон(
			НСтр("ru = 'Внутренняя ошибка. Не удалось прочитать исходные данные классификатора %1
			|%2';
			|en = 'Внутренняя ошибка. Не удалось прочитать исходные данные классификатора %1
			|%2'"),
			"",
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		
		РезультатОбновления.СообщениеОбОшибке = ТекстОшибки;
		РезультатОбновления.УспешнаяЗагрузка  = Ложь;
	КонецПопытки;
	
	Если ЗначениеЗаполнено(ВременныйКаталог) Тогда
		ВременныеФайлы.Добавить(ВременныйКаталог);
	КонецЕсли;
	Для Каждого ИмяВременногоФайла Из ВременныеФайлы Цикл
		УдалитьФайлы(ИмяВременногоФайла);
	КонецЦикла;
	
	Если ДанныеДляЗагрузки.Количество() Тогда
		
		УстановитьПривилегированныйРежим(Истина);
		
		ДанныеОбновленияВидовПродукции = ДанныеДляЗагрузки["ProductTypes"];
		Если ДанныеОбновленияВидовПродукции <> Неопределено Тогда
			Справочники.ВидыПродукцииИС.ПриЗагрузкеКлассификатора(ДанныеОбновленияВидовПродукции, ПараметрыЗагрузки,
				РезультатОбновления);
		КонецЕсли;
		
		ДанныеОбновленияТоварныхГрупп = ДанныеДляЗагрузки["ProductGroups"];
		Если ДанныеОбновленияТоварныхГрупп <> Неопределено Тогда
			Справочники.ТоварныеГруппыГИСМТ.ПриЗагрузкеКлассификатора(ДанныеОбновленияТоварныхГрупп, ПараметрыЗагрузки,
				РезультатОбновления);
		КонецЕсли;
		
		ДанныеОбновленияПричиныВывода = ДанныеДляЗагрузки["WithdrawalReasons"];
		Если ДанныеОбновленияПричиныВывода <> Неопределено И РасширеннаяВерсия Тогда
			МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Справочник.ПричиныВыводаИзОборотаИСМП");
			МенеджерОбъекта.ПриЗагрузкеКлассификатора(ДанныеОбновленияПричиныВывода, ПараметрыЗагрузки,
				РезультатОбновления);
		КонецЕсли;

		ДанныеОбновленияВидовВозвратаВОборот = ДанныеДляЗагрузки["TypesOfReturns"];
		Если ДанныеОбновленияВидовВозвратаВОборот <> Неопределено И РасширеннаяВерсия Тогда
			МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Справочник.ВидыВозвратаВОборотИСМП");
			МенеджерОбъекта.ПриЗагрузкеКлассификатора(ДанныеОбновленияВидовВозвратаВОборот, ПараметрыЗагрузки,
				РезультатОбновления);
		КонецЕсли;

		ДанныеОбновленияШаблоновРазбораКМ = ДанныеДляЗагрузки["KMTemplates"];
		Если ДанныеОбновленияШаблоновРазбораКМ <> Неопределено Тогда
			Справочники.ШаблоныРазбораКодовМаркировкиИСМП.ПриЗагрузкеКлассификатора(ДанныеОбновленияШаблоновРазбораКМ, ПараметрыЗагрузки,
				РезультатОбновления);
		КонецЕсли;

		ДанныеОбновленияШаблоновКМСУЗ = ДанныеДляЗагрузки["SUZTemplates"];
		Если ДанныеОбновленияШаблоновКМСУЗ <> Неопределено Тогда
			Справочники.ШаблоныКодовМаркировкиСУЗ.ПриЗагрузкеКлассификатора(ДанныеОбновленияШаблоновКМСУЗ, ПараметрыЗагрузки,
				РезультатОбновления);
		КонецЕсли;

		ДанныеОбновленияМестПрименения = ДанныеДляЗагрузки["PlacesOfApplication"];
		Если ДанныеОбновленияМестПрименения <> Неопределено Тогда
			Справочники.МестаПримененияЭлементовКонструктораВидовПродукцииИС.ПриЗагрузкеКлассификатора(
				ДанныеОбновленияМестПрименения, ПараметрыЗагрузки, РезультатОбновления);
		КонецЕсли;

		ДанныеОбновленияМестПримененияЭК = ДанныеДляЗагрузки["PlacesOfConstructorElements"];
		Если ДанныеОбновленияМестПримененияЭК <> Неопределено Тогда
			РегистрыСведений.МестаПримененияЭлементовКонструктораВидовПродукцииИС.ПриЗагрузкеКлассификатора(
				ДанныеОбновленияМестПримененияЭК, ПараметрыЗагрузки, РезультатОбновления);
		КонецЕсли;

		ДанныеОбновленияДоступныхЗначений = ДанныеДляЗагрузки["AvailableValues"];
		Если ДанныеОбновленияДоступныхЗначений <> Неопределено Тогда
			РегистрыСведений.ДоступныеЗначенияЭлементовКонструктораВидовПродукцииИС.ПриЗагрузкеКлассификатора(
				ДанныеОбновленияДоступныхЗначений, ПараметрыЗагрузки, РезультатОбновления);
		КонецЕсли;

		ДанныеОбновленияМестЗначений = ДанныеДляЗагрузки["AvailableValuesPlaces"];
		Если ДанныеОбновленияМестЗначений <> Неопределено Тогда
			РегистрыСведений.ДоступныеЗначенияПоМестамПримененияЭлементовКонструктораВидовПродукцииИС.ПриЗагрузкеКлассификатора(
				ДанныеОбновленияМестЗначений, ПараметрыЗагрузки, РезультатОбновления);
		КонецЕсли;

		ДанныеОбновленияЭлементовКонструктора = ДанныеДляЗагрузки["ConstructorElements"];
		Если ДанныеОбновленияЭлементовКонструктора <> Неопределено Тогда
			Если ПараметрыЗагрузки.ЗагрузкаИзменений Тогда
				РегистрыСведений.ЗначенияПользователяЭлементовКонструктораВидовПродукцииИС.ПриЗагрузкеКлассификатора(
					ДанныеОбновленияЭлементовКонструктора, ПараметрыЗагрузки, РезультатОбновления);
			Иначе
				РегистрыСведений.ЗначенияЭлементовКонструктораВидовПродукцииИС.ПриЗагрузкеКлассификатора(
					ДанныеОбновленияЭлементовКонструктора, ПараметрыЗагрузки, РезультатОбновления);
			КонецЕсли;
		КонецЕсли;
		
		ДанныеОбновленияКодовТНВЭД = ДанныеДляЗагрузки["TNVED"];
		Если ДанныеОбновленияКодовТНВЭД <> Неопределено Тогда
			Справочники.ТНВЭДПоВидамПродукцииИС.ПриЗагрузкеКлассификатора(ДанныеОбновленияКодовТНВЭД,
				ПараметрыЗагрузки, РезультатОбновления);
		КонецЕсли;
		
		ДанныеОбновленияКодовОКПД = ДанныеДляЗагрузки["OKPD"];
		Если ДанныеОбновленияКодовОКПД <> Неопределено Тогда
			Справочники.ОКПД2ПоВидамПродукцииИС.ПриЗагрузкеКлассификатора(ДанныеОбновленияКодовОКПД, ПараметрыЗагрузки,
				РезультатОбновления);
		КонецЕсли;
		
		ДанныеОбновленияТНВЭДОКПД2 = ДанныеДляЗагрузки["TNVEDOKPD2"];
		Если ДанныеОбновленияТНВЭДОКПД2 <> Неопределено Тогда
			РегистрыСведений.СоответствиеТНВЭДиОКПД2ИС.ПриЗагрузкеКлассификатора(ДанныеОбновленияТНВЭДОКПД2, ПараметрыЗагрузки,
				РезультатОбновления);
		КонецЕсли;

		КонфликтыОбновления = ПараметрыЗагрузки.КонфликтыОбновления;

		Если КонфликтыОбновления.Количество() И ОбщегоНазначения.ПодсистемаСуществует("ПрикладнаяГосИС") Тогда
			МенеджерРегистраКонфликтов = ОбщегоНазначения.ОбщийМодуль("РегистрыСведений.КонфликтыОбновленияНастроекИС");
			МенеджерРегистраКонфликтов.ЗаписатьКонфликтыПриОбновлении(КонфликтыОбновления);
		КонецЕсли;
		
		Если Не ОбщегоНазначения.РазделениеВключено() Тогда
			
			Если Не ПараметрыЗагрузки.ЗагрузкаИзменений Тогда
				РегистрыСведений.ЗначенияПользователяЭлементовКонструктораВидовПродукцииИС.СинхронизироватьЗначенияСОсновнымРегистром();
			КонецЕсли;
			
			ИмяОповещения = "СтандартныеПодсистемы.БазоваяФункциональность.ПовторноИспользуемыеЗначенияУстарели";
			Попытка
				СерверныеОповещения.ОтправитьСерверноеОповещение(ИмяОповещения, Неопределено,, Истина);
			Исключение
				ТекстОшибки = СтрШаблон(
					"Ошибка при отправке серверного оповещения при обновлении классификаторов:
					|%1",
					ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
				ЗаписьЖурналаРегистрации(
					"ГБосИС.ОтправкаСерверногоОповещениПриОбновленииКлассификаторов",
					УровеньЖурналаРегистрации.Ошибка,,,
					ТекстОшибки);
			КонецПопытки;
			
		КонецЕсли;
		
	КонецЕсли;
		
КонецПроцедуры

// см. ОбщегоНазначенияИСКлиентСервер.ИдентификаторКлассификатораВидовПродукцииИС
Функция ИдентификаторКлассификатораВидовПродукцииИС() Экспорт
	
	Возврат ОбщегоНазначенияИСКлиентСервер.ИдентификаторКлассификатораВидовПродукцииИС();
	
КонецФункции

// Параметры загрузки классификаторов ИС.
// 
// Возвращаемое значение:
//  Структура - Параметры загрузки классификаторов ИС:
// * Идентификаторы - ТаблицаЗначений - Таблица идентификаторов классификаторов:
// ** ИмяТаблицы - Строка(100) - Имя таблицы метаданных
// ** Идентификатор - Строка(36) - Идентификатор элемента
// ** Наименование - Строка(150) - Наименование элемента
// ** Код - Строка(9) - Код элемента
// ** КлючЗначения - Строка(50) - Ключ значения
// ** Ссылка - СправочникСсылка.ЭлементыКонструктораВидовПродукцииИС - Ссылка на элемент справочника
// * ЗагрузкаИзменений - Булево - Признак загрузки изменений
// * КонфликтыОбновления - Соответствие - Соответствие конфликтов обновления
// * РежимОтладки - Булево - Признак режима отладки
// * ПолнаяЗагрузка - Булево - Обязательное обновление всех элементов справочников
Функция ПараметрыЗагрузкиКлассификаторовИС() Экспорт
	
	ПараметрыЗагрузки = Новый Структура;
	
	Идентификаторы = Новый ТаблицаЗначений;
	Идентификаторы.Колонки.Добавить("ИмяТаблицы",    ОбщегоНазначения.ОписаниеТипаСтрока(100));
	Идентификаторы.Колонки.Добавить("Идентификатор", ОбщегоНазначения.ОписаниеТипаСтрока(36));
	Идентификаторы.Колонки.Добавить("Наименование",  ОбщегоНазначения.ОписаниеТипаСтрока(150));
	Идентификаторы.Колонки.Добавить("Код",           ОбщегоНазначения.ОписаниеТипаСтрока(9));
	Идентификаторы.Колонки.Добавить("КлючЗначения",  ОбщегоНазначения.ОписаниеТипаСтрока(50));
	Идентификаторы.Колонки.Добавить("Ссылка",        Справочники.ТипВсеСсылки());
	
	ЭлементыКонструктора = Справочники.ЭлементыКонструктораВидовПродукцииИС.ПараметрыЭлементовКонструктора();
	ИмяТаблицыЭлементов  = Метаданные.Справочники.ЭлементыКонструктораВидовПродукцииИС.ПолноеИмя();
	
	Для каждого КлючИЗначение Из ЭлементыКонструктора Цикл
		ДанныеЭлемента = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(КлючИЗначение.Ключ, "ЭтоГруппа, ИмяПредопределенныхДанных");
		
		Если ДанныеЭлемента.ЭтоГруппа Тогда
			Продолжить;
		КонецЕсли;
		
		СтрокаИдентификаторов = Идентификаторы.Добавить();
		
		СтрокаИдентификаторов.ИмяТаблицы   = ИмяТаблицыЭлементов;
		СтрокаИдентификаторов.Наименование = ДанныеЭлемента.ИмяПредопределенныхДанных;
		СтрокаИдентификаторов.Ссылка       = КлючИЗначение.Ключ;
		
	КонецЦикла;
	
	Идентификаторы.Индексы.Добавить("ИмяТаблицы, Наименование");
	Идентификаторы.Индексы.Добавить("ИмяТаблицы, Идентификатор, Наименование");
	Идентификаторы.Индексы.Добавить("ИмяТаблицы, Идентификатор, Наименование, КлючЗначения");
	
	ПараметрыЗагрузки.Вставить("Идентификаторы",      Идентификаторы);
	ПараметрыЗагрузки.Вставить("ЗагрузкаИзменений",   Ложь);
	ПараметрыЗагрузки.Вставить("ПолнаяЗагрузка",      Ложь);
	ПараметрыЗагрузки.Вставить("КонфликтыОбновления", Новый Соответствие);
	ПараметрыЗагрузки.Вставить("РежимОтладки",        Ложь );
	
	Возврат ПараметрыЗагрузки;
	
КонецФункции

// Возвращает структуру для хранения результатов обновления классификаторов видов продукции ИС.
//
// Возвращаемое значение:
//   Структура:
//     * УспешнаяЗагрузка - Булево - признак успешной загрузки
//     * СообщениеОбОшибке - Строка - текст ошибки при неудачной загрузке
//     * ДанныеЗагруженыЧастично - Булево - признак частичной загрузки данных
Функция ИнициализироватьРезультатыОбновленияКлассификаторовВидовПродукцииИС() Экспорт
	
	РезультатОбновления = Новый Структура;
	РезультатОбновления.Вставить("УспешнаяЗагрузка",        Истина);
	РезультатОбновления.Вставить("СообщениеОбОшибке",       "");
	РезультатОбновления.Вставить("ДанныеЗагруженыЧастично", Ложь);
	
	Возврат РезультатОбновления;
		
КонецФункции

// Определяет действия при загрузке элементов классификатора на основе хеш-сумм и параметров загрузки.
//
// Параметры:
//   ПараметрыЗагрузки - Структура - параметры загрузки, должны содержать свойство ЗагрузкаИзменений
//   СуществующийОбъектСсылка - СправочникСсылка, Неопределено - ссылка на существующий объект или Неопределено
//   ХешСумма - Строка - текущая хеш-сумма объекта в информационной базе
//   ХешСуммаПоставляемыхДанных - Строка - хеш-сумма поставляемых данных
//   ХешСуммаЗагружаемыхДанных - Строка - хеш-сумма загружаемых данных
//
// Возвращаемое значение:
//   Структура - структура с флагами дальнейших действий:
//     * СоздатьЭлемент - Булево - признак необходимости создания нового элемента
//     * ТребуетсяОбновление - Булево - признак необходимости обновления элемента
//     * ЗафиксироватьКонфликт - Булево - признак необходимости фиксации конфликта
//     * КоллизияИзменений - Булево - признак наличия коллизии изменений
Функция ПараметрыОбновленияЭлементовПриЗагрузкеКлассификатора(ПараметрыЗагрузки, СуществующийОбъектСсылка, ХешСумма,
	ХешСуммаПоставляемыхДанных, ХешСуммаЗагружаемыхДанных) Экспорт

	ДальнейшиеДействия = Новый Структура;
	ДальнейшиеДействия.Вставить("СоздатьЭлемент",        Ложь);
	ДальнейшиеДействия.Вставить("ТребуетсяОбновление",   Ложь);
	ДальнейшиеДействия.Вставить("ЗафиксироватьКонфликт", Ложь);
	ДальнейшиеДействия.Вставить("КоллизияИзменений",     Ложь);

	Если СуществующийОбъектСсылка = Неопределено Тогда
		ДальнейшиеДействия.СоздатьЭлемент = Истина;
	Иначе
		Если ПараметрыЗагрузки.ПолнаяЗагрузка Тогда
			ДальнейшиеДействия.ТребуетсяОбновление = Истина;
			
		ИначеЕсли ПараметрыЗагрузки.ЗагрузкаИзменений Тогда
			Если ХешСумма <> ХешСуммаЗагружаемыхДанных Тогда
				Если ХешСумма <> ХешСуммаПоставляемыхДанных Тогда
					ДальнейшиеДействия.КоллизияИзменений = Истина;
				Иначе
					ДальнейшиеДействия.ТребуетсяОбновление = Истина;
				КонецЕсли;
			КонецЕсли;

		Иначе
			Если ХешСуммаПоставляемыхДанных = "" Тогда
				Если ХешСумма = "" Тогда
					ДальнейшиеДействия.ТребуетсяОбновление = Истина;
				ИначеЕсли ХешСумма <> ХешСуммаЗагружаемыхДанных Тогда
					ДальнейшиеДействия.ЗафиксироватьКонфликт = Истина;
				Иначе
					ДальнейшиеДействия.ТребуетсяОбновление = Истина;
				КонецЕсли;
			ИначеЕсли ХешСуммаПоставляемыхДанных <> ХешСуммаЗагружаемыхДанных Тогда
				Если ХешСуммаЗагружаемыхДанных = ХешСумма Тогда
					ДальнейшиеДействия.ТребуетсяОбновление = Истина;
				ИначеЕсли ХешСуммаПоставляемыхДанных <> ХешСумма Тогда
					ДальнейшиеДействия.ЗафиксироватьКонфликт = Истина;
				Иначе
					ДальнейшиеДействия.ТребуетсяОбновление = Истина;
				КонецЕсли;
			КонецЕсли;

		КонецЕсли;

	КонецЕсли;

	Возврат ДальнейшиеДействия;

КонецФункции

// Добавляет элемент в список конфликтов при обновлении с указанием загружаемых данных и хеш-суммы.
//
// Параметры:
//   СписокКонфликтов - Соответствие - список формируемых конфликтов обновления
//   ЭлементКонфликта - СправочникСсылка - ключ, по которому добавляется информация о конфликте
//   ЗагружаемыеДанные - Структура - данные, которые загружаются при обновлении
Процедура ДобавитьЭлементДляЗаписиКонфликтаПриОбновлении(СписокКонфликтов, ЭлементКонфликта, ЗагружаемыеДанные) Экспорт

	ПоставляемыеДанныеТекст = ОбщегоНазначенияИСКлиентСервер.ОбъектВТекстJSON(ЗагружаемыеДанные);
	
	Если ПоставляемыеДанныеТекст <> Неопределено Тогда
		СтруктураКонфликта = Новый Структура;
		СтруктураКонфликта.Вставить("ЗагружаемыеДанные", ПоставляемыеДанныеТекст);
		СтруктураКонфликта.Вставить("ХешСуммаПоставляемыхДанных", ЗагружаемыеДанные.ХешСуммаПоставляемыхДанных);

		СписокКонфликтов.Вставить(ЭлементКонфликта, СтруктураКонфликта);
		
	КонецЕсли;
	
КонецПроцедуры

// Список исключений подсистемы БРМК.
// 
// Возвращаемое значение:
//  Массив из Строка - Список объектов, которые отсутствуют в подсистеме БРМК
Функция СписокИсключенийПодсистемыБРМК() Экспорт
	
	МассивИсключений = Новый Массив;
	
	МассивИсключений.Добавить("Перечисление.ПричиныПеремаркировкиТоваровИСМП");
	МассивИсключений.Добавить("Перечисление.СпособыПолученияИзготовленныхКИЗГИСМ");
	МассивИсключений.Добавить("Справочник.ПричиныВыводаИзОборотаИСМП");
	МассивИсключений.Добавить("Перечисление.СпособыВводаВОборотСУЗ");
	МассивИсключений.Добавить("Перечисление.РазделыУчетаЗЕРНО");
	МассивИсключений.Добавить("Справочник.ВидыВозвратаВОборотИСМП");
	МассивИсключений.Добавить("Перечисление.ПричиныСписанияКодовМаркировкиИСМП");
	МассивИсключений.Добавить("Перечисление.ШаблоныКодовМаркировкиСУЗ");
	МассивИсключений.Добавить("Перечисление.ТипыМаркировкиККТ");
	
	Возврат МассивИсключений;
	
КонецФункции

// Требуется повторная загрузка классификатора после обновления.
// 
// Возвращаемое значение:
//  Булево - Истина, если добавлены новые элементы конструктора и требуется их заполнение
Функция ТребуетсяПовторнаяЗагрузкаКлассификатораИзМакетаПослеОбновления() Экспорт
	
	Возврат РегистрыСведений.ПоставляемыеДанныеКлассификаторовВидовПродукцииИС.ИмеютсяСохраненныеДанныеКлассификатораВидовПродукцииИС(
		ИдентификаторКлассификатораВидовПродукцииИС(),
		Истина);
	
КонецФункции

// Текст запроса проверки необходимости загрузки классификатора видов продукции ИС.
// 
// Возвращаемое значение:
//  Строка - Текст запроса проверки необходимости загрузки классификатора видов продукции ИС
Функция ТекстЗапросаПроверкиНеобходимостиЗагрузкиКлассификатораВидовПродукцииИС() Экспорт
	
	ТекстЗапроса = "ВЫБРАТЬ ПЕРВЫЕ 1
	|	ЗначенияЭлементовКонструктораВидовПродукцииИС.ЭлементКонструктора
	|ИЗ
	|	РегистрСведений.ЗначенияЭлементовКонструктораВидовПродукцииИС КАК ЗначенияЭлементовКонструктораВидовПродукцииИС";
	
	Возврат ТекстЗапроса;
	
КонецФункции

#КонецОбласти

#КонецОбласти

Функция ДополнительныйМакетОбновленияКлассификатораВидовПродукцииИС()
	Возврат Неопределено;
КонецФункции

#КонецОбласти

#Область Блокировки

// Устанавливает управляемую блокировку по значениям полей.
//
// Параметры:
//  Пространство - Строка - пространство блокировки.
//  Поля - Структура - ключ задает имя поля пространства блокировки, а значение:
//                     1. Источник задан: соответствующее поле источника, содержащее значения для блокировки.
//                     2. Источник не задан: непосредственно значение для блокировки.
//  Источник - РезультатЗапроса, ТабличнаяЧасть, ТаблицаЗначений  - источник данных.
//  Режим - РежимБлокировкиДанных - режим блокировки.
//        - Неопределено - значение по умолчанию "Исключительный".
//
Процедура УстановитьУправляемуюБлокировку(Знач Пространство, Знач Поля = Неопределено, Знач Источник = Неопределено, Знач Режим = Неопределено) Экспорт
	
	Если Режим = Неопределено Тогда
		Режим = РежимБлокировкиДанных.Исключительный;
	КонецЕсли;
	
	Если Поля = Неопределено Тогда
		Поля = Новый Структура;
	КонецЕсли;
	
	Блокировка = Новый БлокировкаДанных;
	ЭлементБлокировки = Блокировка.Добавить(Пространство);
	Если Источник = Неопределено Тогда
		Для каждого КлючЗначение Из Поля Цикл
			ЭлементБлокировки.УстановитьЗначение(КлючЗначение.Ключ, КлючЗначение.Значение);
		КонецЦикла;
	Иначе
		ЭлементБлокировки.ИсточникДанных = Источник;
		Для каждого КлючЗначение Из Поля Цикл
			ЭлементБлокировки.ИспользоватьИзИсточникаДанных(КлючЗначение.Ключ, КлючЗначение.Значение);
		КонецЦикла;
	КонецЕсли;
	ЭлементБлокировки.Режим = Режим;
	//@skip-check lock-out-of-try
	Блокировка.Заблокировать();
	
КонецПроцедуры

// Устанавливает управляемую блокировку по ссылке на объект.
//
// Параметры:
//  Ссылка - ЛюбаяСсылка - ссылка, по которой нужно установить блокировку.
//  Режим - РежимБлокировкиДанных - режим блокировки.
//        - Строка - допустимые значения "Разделяемый" и "Исключительный".
//        - Неопределено - значение по умолчанию "Исключительный".
//
Процедура УстановитьУправляемуюБлокировкуПоСсылке(Знач Ссылка, Знач Режим = Неопределено) Экспорт
	
	МетаданныеОбъекта = Ссылка.Метаданные();
	ПространствоБлокировки = МетаданныеОбъекта.ПолноеИмя();
	
	Поля = Новый Структура("Ссылка", Ссылка);
	
	УстановитьУправляемуюБлокировку(ПространствоБлокировки, Поля,, Режим);
	
КонецПроцедуры

// Устанавливает управляемую блокировку по набору записей.
// Используются установленные отборы в наборе.
//
// Параметры:
//  Набор - РегистрСведенийНаборЗаписей, РегистрНакопленияНаборЗаписей - набор, по которому нужно установить блокировку.
//  Режим - РежимБлокировкиДанных - режим блокировки.
//        - Строка - допустимые значения "Разделяемый" и "Исключительный".
//        - Неопределено - значение по умолчанию "Исключительный".
//
Процедура УстановитьУправляемуюБлокировкуПоНаборуЗаписей(Знач Набор, Знач Режим = Неопределено) Экспорт
	
	МетаданныеОбъекта = Набор.Метаданные();
	ПространствоБлокировки = МетаданныеОбъекта.ПолноеИмя();
	
	Поля = Новый Структура;
	Для каждого ЭлементОтбора Из Набор.Отбор Цикл
		Если ЭлементОтбора.Использование Тогда
			Поля.Вставить(ЭлементОтбора.ПутьКДанным, ЭлементОтбора.Значение);
		КонецЕсли;
	КонецЦикла;
	
	УстановитьУправляемуюБлокировку(ПространствоБлокировки, Поля,, Режим);
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

#Область ФункцииИспользующиеПереопределение

//Формирует представление строки номенклатуры. Переопределяется.
//   Значение по-умолчанию: непустые представления составляющих, через запятую.
//
//Параметры:
//   Номенклатура   - ОпределяемыйТип.Номенклатура               - ссылка на номенклатуру.
//   Характеристика - ОпределяемыйТип.ХарактеристикаНоменклатуры - ссылка на характеристику номенклатуры.
//   Упаковка       - ОпределяемыйТип.Упаковка                   - ссылка на упаковку.
//   Серия          - ОпределяемыйТип.СерияНоменклатуры          - ссылка на серию номенклатуры.
//   ПоУмолчанию    - Строка                                     - Значение, если номенклатура не заполнена.
//
//Возвращаемое значение:
//   Строка - представление строки номенклатуры
//
Функция ПредставлениеНоменклатуры(Номенклатура, Характеристика, Упаковка = Неопределено, Серия = Неопределено, ПоУмолчанию = Неопределено) Экспорт
	
	Представление = "";
	ОбщегоНазначенияИСПереопределяемый.ЗаполнитьПредставлениеНоменклатуры(
		Представление, Номенклатура, Характеристика, Упаковка, Серия);
	
	Если Представление = "" Тогда
		МассивФормированияПредставления = Новый Массив;
		Если ЗначениеЗаполнено(Номенклатура) Тогда
			МассивФормированияПредставления.Добавить(СокрЛП(Номенклатура));
		КонецЕсли;
		Если ЗначениеЗаполнено(Характеристика) Тогда
			МассивФормированияПредставления.Добавить(СокрЛП(Характеристика));
		КонецЕсли;
		Если ЗначениеЗаполнено(Упаковка) Тогда
			МассивФормированияПредставления.Добавить(СокрЛП(Упаковка));
		КонецЕсли;
		Если ЗначениеЗаполнено(Серия) Тогда
			МассивФормированияПредставления.Добавить(СокрЛП(Серия));
		КонецЕсли;
		Представление = СтрСоединить(МассивФормированияПредставления, ", ");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Представление) И ЗначениеЗаполнено(ПоУмолчанию) Тогда
		Представление = Строка(ПоУмолчанию);
	КонецЕсли;
	
	Возврат Представление;
	
КонецФункции

#КонецОбласти

// Серверное ожидание перед очередным действием в соответствии с ограничениями сервиса
//
// Параметры:
//  ВремяОжидания - Число, Неопределено - время ожидания
// Возвращаемое значение:
//  Число, Неопределено - время ожидания
Функция ВремяОжидания(ВремяОжидания) Экспорт
	
	Если ТипЗнч(ВремяОжидания) = Тип("Число") Тогда
		
		Возврат ?(ВремяОжидания <= 0, Неопределено, ВремяОжидания);
		
	Иначе
		
		Возврат Неопределено;
		
	КонецЕсли;
	
КонецФункции

Функция ОбъединитьПути(Путь1, Путь2, Путь3 = Неопределено) Экспорт
	
	РазделительПути = "\";
	
	МассивПутей = Новый Массив();
	МассивПутей.Добавить(Путь1);
	МассивПутей.Добавить(Путь2);
	МассивПутей.Добавить(Путь3);
	
	МассивПутейИтоговый = Новый Массив();
	
	Для Каждого Путь Из МассивПутей Цикл
		Если Не ЗначениеЗаполнено(Путь) Тогда
			Продолжить;
		КонецЕсли;
		Если Прав(Путь, 1) = "/"
			Или Прав(Путь, 1) = РазделительПути Тогда
			МассивПутейИтоговый.Добавить(Сред(Путь, СтрДлина(Путь) - 1));
		Иначе
			МассивПутейИтоговый.Добавить(Путь);
		КонецЕсли;
	КонецЦикла;
	
	Возврат СтрСоединить(МассивПутейИтоговый, РазделительПути);

КонецФункции

// Устанавливает признак есть ли право на изменение списка документов у текущего пользователя
// 
// Параметры:
//  ФормаСписка - ФормаКлиентскогоПриложения
//  ИмяРеквизита - Строка - Имя реквизита
Процедура УстановитьПризнакПравоИзмененияФормыСписка(ФормаСписка, ИмяРеквизита = "ПравоИзменения") Экспорт
	
	МассивРеквизитовФормы = ФормаСписка.ПолучитьРеквизиты();
	РеквизитНайден = Ложь;
	Для Каждого Реквизит Из МассивРеквизитовФормы Цикл
		Если Реквизит.Имя = ИмяРеквизита Тогда
			РеквизитНайден = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если Не РеквизитНайден Тогда
		Возврат;
	КонецЕсли;
	
	ИмяОсновнойТаблицыСписка = ФормаСписка.Список.ОсновнаяТаблица;
	ОбъектМетаданных = Метаданные.НайтиПоПолномуИмени(ИмяОсновнойТаблицыСписка);
	ФормаСписка[ИмяРеквизита] = ПравоДоступа("Изменение", ОбъектМетаданных);
	
КонецПроцедуры

// Определяет включение режима использования фоновых заданий при обмене с сервисами.
//
// Возвращаемое значение:
//  Булево - Ложь, если фоновые задания не используются.
//
Функция РежимРаботыБезФоновыхЗаданий() Экспорт
	
	ПараметрЗапускаПриложения = ПараметрыСеанса.ПараметрыКлиентаНаСервере.Получить("ПараметрЗапуска");
	
	Возврат СтрНайти(ВРег(ПараметрЗапускаПриложения), ВРег("РежимРаботыБезФоновыхЗаданийИС")) > 0;
	
КонецФункции

Процедура ПроверитьРасположениеФайлаВоВременномКаталоге(ПутьКФайлу) Экспорт
	
	Если Не ЗначениеЗаполнено(ПутьКФайлу) Тогда
		Возврат;
	КонецЕсли;
	
	Файл = Новый Файл(ПутьКФайлу);
	
	КаталогВременныхФайлов = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогВременныхФайлов());
	КаталогФайла = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(Файл.Путь);
	
	Если НРег(КаталогФайла) <> НРег(КаталогВременныхФайлов) Тогда
		ВызватьИсключение СтрШаблон(
			НСтр("ru = 'Каталог файла отличается от каталога временных файлов';
				|en = 'Каталог файла отличается от каталога временных файлов'"));
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Редакция библиотеки.
// 
// Возвращаемое значение:
//  Строка - Редакция библиотеки
Функция РедакцияБиблиотеки() Экспорт

	Возврат СтрРазделить(ВерсияБиблиотеки(), ".")[0];

КонецФункции

// Формирует массив пустых значений определяемого типа.
// 
// Параметры:
//  ОпределяемыйТипИлиИмяТипа - Строка, ОбъектМетаданныхОпределяемыйТип - определяемый тип, по которому надо получить пустые значения
//  по умолчанию
// Возвращаемое значение:
//  Массив из ЛюбаяСсылка - Пустые значения определяемого типа.
//
Функция НезаполненныеЗначенияОпределяемогоТипа(ОпределяемыйТипИлиИмяТипа) Экспорт
	
	Если ТипЗнч(ОпределяемыйТипИлиИмяТипа) = Тип("Строка") Тогда
		ОпределяемыйТип = Метаданные.ОпределяемыеТипы[ОпределяемыйТипИлиИмяТипа];
	Иначе
		ОпределяемыйТип = ОпределяемыйТипИлиИмяТипа;
	КонецЕсли;
	
	МассивПустыхЗначений = Новый Массив;
	Для Каждого ТипЗначения Из ОпределяемыйТип.Тип.Типы() Цикл
		МассивТипов = Новый Массив;
		МассивТипов.Добавить(ТипЗначения);
		ОписаниеТипа = Новый ОписаниеТипов(МассивТипов);
		МассивПустыхЗначений.Добавить(ОписаниеТипа.ПривестиЗначение());
	КонецЦикла;
	
	Если МассивПустыхЗначений.Количество() > 1 Тогда
		МассивПустыхЗначений.Добавить(Неопределено);
	КонецЕсли;
	
	Возврат МассивПустыхЗначений;
	
КонецФункции

// Возвращает пароль к сертификату, если доступен текущему пользователю.
// При вызове в привилегированном режиме текущий пользователь не учитывается.
//
// Параметры:
//  Сертификат - Неопределено - вернуть пароли ко всем сертификатам, доступным текущему пользователю.
//             - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования - вернуть пароль
//                 к указанному сертификату.
//              
// Возвращаемое значение:
//  Неопределено - пароль для указанного сертификата не указан.
//  Строка       - пароль для указанного сертификата.
//  Соответствие - все заданные пароли, доступные текущему пользователю
//                 в виде ключ - сертификат и значение - пароль.
//
Функция ПарольКСертификату(Сертификат = Неопределено) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	Данные = Константы.КонтекстРаботыИС.Получить().Получить();
	УстановитьПривилегированныйРежим(Ложь);
	
	РольДляПроверки = Неопределено;
	Если Метаданные.Роли.Найти("ДобавлениеИзменениеЭлектронныхПодписейИШифрование") <> Неопределено Тогда
		РольДляПроверки = "ДобавлениеИзменениеЭлектронныхПодписейИШифрование";
	ИначеЕсли Метаданные.Роли.Найти("ДобавлениеИзменениеСертификатовКлючейЭлектроннойПодписиИШифрования") <> Неопределено Тогда
		РольДляПроверки = "ДобавлениеИзменениеСертификатовКлючейЭлектроннойПодписиИШифрования";
	Иначе
		ВызватьИсключение НСтр("ru = 'Не определена роль для добавления и (или) изменения электронных подписей';
								|en = 'Не определена роль для добавления и (или) изменения электронных подписей'");
	КонецЕсли;
	
	Если Не Пользователи.РолиДоступны(РольДляПроверки) Тогда
		Если Сертификат <> Неопределено Тогда
			Возврат Неопределено;
		КонецЕсли;
		Возврат Новый СписокЗначений;
	КонецЕсли;
	
	Если Сертификат <> Неопределено Тогда
		Если ТипЗнч(Данные) <> Тип("Соответствие") Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Свойства = Данные.Получить(Сертификат);
		
		Если ТипЗнч(Свойства) = Тип("Структура")
		   И Свойства.Свойство("Пароль")
		   И ТипЗнч(Свойства.Пароль) = Тип("Строка")
		   И Свойства.Свойство("Пользователь")
		   И ТипЗнч(Свойства.Пользователь) = Тип("СправочникСсылка.Пользователи") Тогда
			
		   Если Свойства.Пользователь = Пользователи.ТекущийПользователь()
			   ИЛИ Свойства.Пользователь = Справочники.Пользователи.ПустаяСсылка() Тогда
				
				Возврат Свойства.Пароль;
			КонецЕсли;
		КонецЕсли;
		
		Возврат Неопределено;
	КонецЕсли;
	
	ПаролиСертификатов = Новый Соответствие;
	
	Если ТипЗнч(Данные) <> Тип("Соответствие") Тогда
		Возврат ПаролиСертификатов;
	КонецЕсли;
	
	Для Каждого КлючИЗначение Из Данные Цикл
		Свойства = КлючИЗначение.Значение;
		
		Если ТипЗнч(Свойства) = Тип("Структура")
		   И Свойства.Свойство("Пароль")
		   И ТипЗнч(Свойства.Пароль) = Тип("Строка")
		   И Свойства.Свойство("Пользователь")
		   И ТипЗнч(Свойства.Пользователь) = Тип("СправочникСсылка.Пользователи") Тогда
			
		   Если Свойства.Пользователь = Пользователи.ТекущийПользователь()
			   ИЛИ Свойства.Пользователь = Справочники.Пользователи.ПустаяСсылка()
			 Или ПривилегированныйРежим() Тогда
				ПаролиСертификатов.Вставить(КлючИЗначение.Ключ, Свойства.Пароль);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат ПаролиСертификатов;
	
КонецФункции

// Серверное ожидание перед очередным действием в соответствии с ограничениями сервиса
//
// Параметры:
//   ВремяОжидания   - Число     - длительность паузы
//   ПараметрыОбмена - Структура - параметры обмена с обязательным свойством:
//    * ФоновоеЗадание - Неопределено, ФоновоеЗадание - задание ожидания
//
Процедура Ожидать(ВремяОжидания, ПараметрыОбмена) Экспорт
	
	Если ВремяОжидания <= 0 Тогда
		Возврат;
	КонецЕсли;
	
	ФоновоеЗадание = Неопределено;
	Если ПараметрыОбмена.ФоновоеЗадание = Неопределено Тогда
		
		ТекущийСеансИнформационнойБазы = ПолучитьТекущийСеансИнформационнойБазы();
		ФоновоеЗадание = ТекущийСеансИнформационнойБазы.ПолучитьФоновоеЗадание();
		
		Если ФоновоеЗадание = Неопределено Тогда
			Если ОбщегоНазначения.РазделениеВключено() Тогда
				Пауза(ВремяОжидания);
				Возврат;
			КонецЕсли;
		Иначе
			ПараметрыОбмена.ФоновоеЗадание = ФоновоеЗадание;
		КонецЕсли;
		
	Иначе
		ФоновоеЗадание = ПараметрыОбмена.ФоновоеЗадание;
	КонецЕсли;
	
	Если ФоновоеЗадание <> Неопределено Тогда
		Попытка
			ФоновоеЗадание.ОжидатьЗавершенияВыполнения(ВремяОжидания);
		Исключение
			ВремяОжидания = 0;
		КонецПопытки;
	КонецЕсли;
	
	Если ВремяОжидания = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Если ОбщегоНазначения.ЭтоWindowsСервер() Тогда
		
		НастройкиПрокси = Новый ИнтернетПрокси(Ложь);
		НастройкиПрокси.НеИспользоватьПроксиДляЛокальныхАдресов = Истина;
		НастройкиПрокси.НеИспользоватьПроксиДляАдресов.Добавить("127.0.0.0");
		
		Попытка
			Loopback = Новый HTTPСоединение(
				"127.0.0.0",,,,НастройкиПрокси,
				ВремяОжидания);
			Loopback.Получить(Новый HTTPЗапрос());
		Исключение
			ВремяОжидания = 0;
		КонецПопытки;
		
	КонецЕсли;
	
КонецПроцедуры

// Останавливает выполнение кода на заданное время.
// Использовать можно только в фоновом задании.
//
// Параметры:
//  Секунд - Число - время ожидания в секундах.
//
Процедура Пауза(Секунд) Экспорт
	
	ТекущийСеансИнформационнойБазы = ПолучитьТекущийСеансИнформационнойБазы();
	ФоновоеЗадание = ТекущийСеансИнформационнойБазы.ПолучитьФоновоеЗадание();
	
	Если ФоновоеЗадание = Неопределено Тогда
		Параметры = Новый Массив;
		Параметры.Добавить(Секунд);
		ФоновоеЗадание = ФоновыеЗадания.Выполнить("ОбщегоНазначенияИС.Пауза", Параметры);
	КонецЕсли;
		
	ФоновоеЗадание.ОжидатьЗавершенияВыполнения(Секунд);
	
КонецПроцедуры

// Возвращает ссылку на общий модуль или модуль менеджера по имени.
//
// Параметры:
//  ИмяМодуля - Строка - имя общего модуля.
//  КешированныеЗначения - Неопределено, Структура - КешированныеЗначения
// Возвращаемое значение:
//   ОбщийМодуль
//   МодульМенеджераОбъекта
Функция ОбщийМодуль(ИмяМодуля, КешированныеЗначения) Экспорт
	
	Если КешированныеЗначения = Неопределено Тогда
		КешированныеЗначения = Новый Соответствие();
	КонецЕсли;
	
	ОписаниеМодуля = КешированныеЗначения[ИмяМодуля];
	Если ОписаниеМодуля = Неопределено Тогда
		ОписаниеМодуля = Новый Структура("Модуль");
		ОписаниеМодуля.Модуль = ОбщегоНазначения.ОбщийМодуль(ИмяМодуля);
		КешированныеЗначения[ИмяМодуля] = ОписаниеМодуля;
	КонецЕсли;
	
	Возврат ОписаниеМодуля.Модуль;
	
КонецФункции

Функция ПодсистемаСуществует(КраткоеИмяПодсистемы) Экспорт
	
	ПолноеИмяПодсистемы = ОбщегоНазначенияИСКлиентСервер.ПолноеИмяПодсистемы(КраткоеИмяПодсистемы);
	Возврат ОбщегоНазначения.ПодсистемаСуществует(ПолноеИмяПодсистемы);
	
КонецФункции

Функция ПодсистемаИспользуется(КраткоеИмяПодсистемы) Экспорт
	
	ИмяПодсистемы = ВРег(КраткоеИмяПодсистемы);
	Если ИмяПодсистемы = "ГИСМТ" Тогда
		ИмяПодсистемы = "ИСМП";
	ИначеЕсли ИмяПодсистемы = "ВЕТИС" Тогда
		ИмяПодсистемы = "ВетИС";
	КонецЕсли;
	
	ПолноеИмяПодсистемы = ОбщегоНазначенияИСКлиентСервер.ПолноеИмяПодсистемы(ИмяПодсистемы);
	Если Не ОбщегоНазначения.ПодсистемаСуществует(ПолноеИмяПодсистемы) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ФункциональнаяОпцияВЕТИС  = "ВестиУчетПодконтрольныхТоваровВЕТИС";
	ФункциональнаяОпцияЗерно  = "ВестиУчетЗернаИПродуктовПереработкиЗЕРНО";
	ФункциональнаяОпцияСатурн = "ВестиУчетПестицидовАгрохимикатовТукосмесейСАТУРН";
	ФункциональнаяОпцияИСМП   = ?(ЭтоРасширеннаяВерсияГосИС(), "ВестиУчетМаркируемойПродукцииИСМП",
		"ВестиУчетМаркируемойПродукцииИСМПРМК");
	ФункциональнаяОпцияЕГАИС  = ?(ЭтоРасширеннаяВерсияГосИС(), "ВестиСведенияДляДекларацийПоАлкогольнойПродукции",
		"ВестиСведенияДляДекларацийПоАлкогольнойПродукцииРМК");
		
	Если ИмяПодсистемы = "ИСМП" Тогда
		Возврат ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияИСМП);
	ИначеЕсли ИмяПодсистемы = "ВетИС" Тогда
		Возврат ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияВЕТИС);
	ИначеЕсли ИмяПодсистемы = "ЕГАИС" Тогда
		Возврат ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияЕГАИС);
	ИначеЕсли ИмяПодсистемы = "ЗЕРНО" Тогда
		Возврат ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияЗерно);
	ИначеЕсли ИмяПодсистемы = "САТУРН" Тогда
		Возврат ПолучитьФункциональнуюОпцию(ФункциональнаяОпцияСатурн);
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

// Записывает вскрытие потребительской упаковки и, при необходимости, готовит документ для передачи в ГИС МТ
// 
// Параметры:
//  ДанныеПодключения - см. РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ИнициализироватьДанныеВскрытияПотребительскойУпаковки
//  ДанныеОтключения  - Неопределено
//                    - см. РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ИнициализироватьДанныеВскрытияПотребительскойУпаковки
// Возвращаемое значение:
//  Структура - результат подключения кега к оборудованию розлива:
// * Документ - ДокументСсылка.ПодключениеКегаКОборудованиюРозливаИСМП - документ подключения кега
// * ТекстОшибки - Строка, Неопределено - текст ошибки подключения при наличии
Функция ВскрытьПотребительскуюУпаковку(ДанныеПодключения, ДанныеОтключения = Неопределено) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	ВозвращаемоеЗначение = Новый Структура;
	ВозвращаемоеЗначение.Вставить("Документ",    Документы.ПодключениеКегаКОборудованиюРозливаИСМП.ПустаяСсылка());
	ВозвращаемоеЗначение.Вставить("ТекстОшибки", Неопределено);
	
	Блокировка = Новый БлокировкаДанных();
	
	ЭлементБлокировки = Блокировка.Добавить(Метаданные.РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ПолноеИмя());
	ЭлементБлокировки.УстановитьЗначение("КодМаркировки", ДанныеПодключения.КодМаркировки);
	
	Если ДанныеОтключения <> Неопределено Тогда
		ЭлементБлокировки = Блокировка.Добавить(Метаданные.РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ПолноеИмя());
		ЭлементБлокировки.УстановитьЗначение("КодМаркировки", ДанныеОтключения.КодМаркировки);
	КонецЕсли;
	
	НачатьТранзакцию();
	
	Попытка
		
		Блокировка.Заблокировать();
		
		Если КонструкторИСМПОбщегоНазначенияКлиентСервер.ПоддерживаетсяПодключениеКОборудованиюРозлива(ДанныеПодключения.ВидПродукции) Тогда
			
			МенеджерПодключения = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Документ.ПодключениеКегаКОборудованиюРозливаИСМП");
			ДокументОбъект      = МенеджерПодключения.СоздатьДокумент();
			
			ЗаполнитьЗначенияСвойств(ДокументОбъект, ДанныеПодключения);
			ДокументОбъект.КодМаркировки = ДанныеПодключения.КодМаркировки;
			ДокументОбъект.Дата          = ТекущаяДатаСеанса();
			
			ДокументОбъект.Записать(РежимЗаписиДокумента.Проведение);
			
			ВозвращаемоеЗначение.Документ = ДокументОбъект.Ссылка;
			ДанныеПодключения.Документ    = ДокументОбъект.Ссылка;
		
		КонецЕсли;
		
		РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ЗаписатьВскрытиеПотребительскойУпаковки(ДанныеПодключения);
		
		Если ДанныеОтключения <> Неопределено Тогда
			
			ТаблицаДанных = РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.НоваяТаблицаОтключенияВскрытыхПотребительскихУпаковок();
			НоваяСтрока                  = ТаблицаДанных.Добавить();
			НоваяСтрока.КодМаркировки    = ДанныеОтключения.КодМаркировки;
			НоваяСтрока.УчитыватьОстаток = Истина;
			РегистрыСведений.ВскрытыеПотребительскиеУпаковкиИС.ОтключитьВскрытыеПотребительскиеУпаковки(ТаблицаДанных);
			
		КонецЕсли;
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		
		ИнформацияОбОшибке = ИнформацияОбОшибке();
				
		ВозвращаемоеЗначение.ТекстОшибки = СтрШаблон(
			НСтр("ru = 'Ошибка записи данных вскрытия потребительской упаковки %1:
				       |%2';
				       |en = 'Ошибка записи данных вскрытия потребительской упаковки %1:
				       |%2'"),
			ДанныеПодключения.КодМаркировки,
			ОбработкаОшибок.КраткоеПредставлениеОшибки(ИнформацияОбОшибке));
		
		ТекстОшибкиПодробно = СтрШаблон(
			НСтр("ru = 'Ошибка записи данных вскрытия потребительской упаковки %1:
				       |%2';
				       |en = 'Ошибка записи данных вскрытия потребительской упаковки %1:
				       |%2'"),
			ДанныеПодключения.КодМаркировки,
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		
		ОбщегоНазначенияИСВызовСервера.ЗаписатьОшибкуВЖурналРегистрации(ТекстОшибкиПодробно);
		
	КонецПопытки;
	
	Возврат ВозвращаемоеЗначение;
	
КонецФункции

// Обновить текст запроса с данными номенклатуры по коду маркировки.
// 
// Параметры:
//  ТекстЗапроса - Строка - текст запроса
//  ПсевдонимИсточникаДанных - Строка - псевдоним источника данных
Процедура ОбновитьТекстЗапросаСДаннымиНоменклатурыПоКодуМаркировки(ТекстЗапроса, ПсевдонимИсточникаДанных) Экспорт
	
	ОписаниеДанных = Новый Структура();
	ОписаниеДанных.Вставить("ИмяПоляНоменклатураШтрихкода",   "Номенклатура");
	ОписаниеДанных.Вставить("ИмяПоляХарактеристикаШтрихкода", "Характеристика");
	ОписаниеДанных.Вставить("ИмяПоляСерияШтрихкода",          "Серия");
	ОписаниеДанных.Вставить("ПутьКИсточникуДанныхШтрихкода",  "Справочник.ШтрихкодыУпаковокТоваров");
	ОписаниеДанных.Вставить("ИмяПоляКодМаркировки",           "Ссылка");
	
	ОбщегоНазначенияИСПереопределяемый.ПриОпределенииПараметровИсточникаДанныхНоменклатурыПоКодуМаркировки(ОписаниеДанных);
	
	ТекстЗапроса = СтрЗаменить(
		ТекстЗапроса,
		"&ИмяПоляНоменклатураШтрихкода",
		СтрШаблон("%1.%2", ПсевдонимИсточникаДанных, ОписаниеДанных.ИмяПоляНоменклатураШтрихкода));
	ТекстЗапроса = СтрЗаменить(
		ТекстЗапроса,
		"&ИмяПоляХарактеристикаШтрихкода",
		СтрШаблон("%1.%2", ПсевдонимИсточникаДанных, ОписаниеДанных.ИмяПоляХарактеристикаШтрихкода));
	ТекстЗапроса = СтрЗаменить(
		ТекстЗапроса,
		"&ИмяПоляСерияШтрихкода",
		СтрШаблон("%1.%2", ПсевдонимИсточникаДанных, ОписаниеДанных.ИмяПоляСерияШтрихкода));
	
	ТекстЗапроса = СтрЗаменить(
		ТекстЗапроса,
		"&ПутьКИсточникуДанныхШтрихкода",
		ОписаниеДанных.ПутьКИсточникуДанныхШтрихкода);
	
	ТекстЗапроса = СтрЗаменить(
		ТекстЗапроса,
		"&ПолеСвязиКодМаркировки",
		СтрШаблон("%1.%2", ПсевдонимИсточникаДанных, ОписаниеДанных.ИмяПоляКодМаркировки));
		
КонецПроцедуры

Функция КомментарийКонтактнойИнформации(КонтактнаяИнформация) Экспорт
	
	Возврат УправлениеКонтактнойИнформацией.КомментарийКонтактнойИнформации(КонтактнаяИнформация);
	
КонецФункции

Функция ДвоичныеДанныеЭлементаТокенаJWT(Знач Значение)
	
	Значение = СтрЗаменить(Значение, "-", "+");
	Значение = СтрЗаменить(Значение, "_", "/");
	
	Остаток = СтрДлина(Значение) % 4;

	Если Остаток = 1 Тогда
		Возврат Неопределено;
	ИначеЕсли Остаток = 2 Тогда
		Значение = Значение + "==";
	ИначеЕсли Остаток = 3 Тогда
		Значение = Значение + "=";
	КонецЕсли;
	
	Возврат Base64Значение(Значение);
	
КонецФункции

#Область Обмен

// Получает сертификаты организаций, предназначенных для подписания на сервере.
// 
// Возвращаемое значение:
//  Структура - Структура со свойствами:
//   * Сертификаты - ТаблицаЗначений - содержит сертификат и пароль.
//   * МенеджерыКриптографии - Соответствие из КлючИЗначение:
//      ** Ключ - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//      ** Значение - МенеджерКриптографии - менеджер криптографии.
Функция СертификатыДляПодписанияНаСервере() Экспорт
	
	Если Не ОбщегоНазначения.ИнформационнаяБазаФайловая()
		И Не ЭлектроннаяПодпись.СоздаватьЭлектронныеПодписиНаСервере() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	НастройкиОбмена = НастройкиОбменаГосИС();
	
	Если НастройкиОбмена = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если НастройкиОбмена.Колонки.Найти("Подразделение") = Неопределено Тогда
		НастройкиОбмена.Колонки.Добавить("Подразделение", Метаданные.ОпределяемыеТипы.Подразделение.Тип);
	КонецЕсли;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	Т.Организация   КАК Организация,
	|	Т.Подразделение КАК Подразделение,
	|	Т.Сертификат    КАК Сертификат
	|ПОМЕСТИТЬ ТаблицаДанных
	|ИЗ
	|	&Таблица КАК Т
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТаблицаДанных.Организация          КАК Организация,
	|	ТаблицаДанных.Подразделение        КАК Подразделение,
	|	ТаблицаДанных.Сертификат           КАК Сертификат,
	|	ТаблицаДанных.Сертификат.Отпечаток КАК Отпечаток,
	|	ТаблицаДанных.Сертификат.Программа КАК Программа
	|ИЗ
	|	ТаблицаДанных КАК ТаблицаДанных
	|ГДЕ
	|	НЕ ТаблицаДанных.Сертификат.Отозван
	|	И (ТаблицаДанных.Сертификат.ДействителенДо >= &ТекущаяДатаСеанса
	|	   ИЛИ ТаблицаДанных.Сертификат.ДействителенДо = ДАТАВРЕМЯ(1, 1, 1))
	|");
	
	Запрос.УстановитьПараметр("Таблица",           НастройкиОбмена);
	Запрос.УстановитьПараметр("ТекущаяДатаСеанса", ТекущаяДатаСеанса());
	
	ДанныеСертификатовИзНастроекОбмена = Запрос.Выполнить().Выгрузить();
	
	СертификатыОрганизацийДляОбменаНаСервере = Новый ТаблицаЗначений();
	СертификатыОрганизацийДляОбменаНаСервере.Колонки.Добавить("Организация");
	СертификатыОрганизацийДляОбменаНаСервере.Колонки.Добавить("Подразделение");
	СертификатыОрганизацийДляОбменаНаСервере.Колонки.Добавить("Сертификат");
	СертификатыОрганизацийДляОбменаНаСервере.Колонки.Добавить("Отпечаток");
	СертификатыОрганизацийДляОбменаНаСервере.Колонки.Добавить("СертификатКриптографии");
	СертификатыОрганизацийДляОбменаНаСервере.Колонки.Добавить("Пароль");
	
	СертификатыОрганизацийДляОбменаНаСервере.Индексы.Добавить("Организация");
	СертификатыОрганизацийДляОбменаНаСервере.Индексы.Добавить("Организация,Подразделение");
	
	МенеджерыКриптографии = Новый Соответствие();
	МенеджерыПоПрограммам = Новый Соответствие();
	
	Для Каждого ДанныеСертификата Из ДанныеСертификатовИзНастроекОбмена Цикл
		
		Пароль = ПарольКСертификату(ДанныеСертификата.Сертификат);
		
		СертификатКриптографии = ЭлектроннаяПодписьСлужебный.ПолучитьСертификатПоОтпечатку(
			ДанныеСертификата.Отпечаток,
			Ложь,
			Ложь);
		Если СертификатКриптографии = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		СтрокаТЧ = СертификатыОрганизацийДляОбменаНаСервере.Добавить();
		СтрокаТЧ.Организация            = ДанныеСертификата.Организация;
		СтрокаТЧ.Подразделение          = ДанныеСертификата.Подразделение;
		СтрокаТЧ.Сертификат             = ДанныеСертификата.Сертификат;
		СтрокаТЧ.Отпечаток              = ДанныеСертификата.Отпечаток;
		СтрокаТЧ.СертификатКриптографии = СертификатКриптографии;
		Если Пароль <> Неопределено Тогда
			СтрокаТЧ.Пароль = Пароль;
		Иначе
			СтрокаТЧ.Пароль = "";
		КонецЕсли;
		
		МенеджерКриптографии = МенеджерыПоПрограммам[ДанныеСертификата.Программа];
		Если МенеджерКриптографии = Неопределено Тогда
			Если ЗначениеЗаполнено(ДанныеСертификата.Программа) Тогда
				Программа = ДанныеСертификата.Программа;
			Иначе
				Программа = Неопределено;
			КонецЕсли;
			МенеджерКриптографии = ЭлектроннаяПодпись.МенеджерКриптографии("Подписание",,, Программа);
			МенеджерыПоПрограммам[ДанныеСертификата.Программа] = МенеджерКриптографии;
		КонецЕсли;
		
		МенеджерыКриптографии.Вставить(ДанныеСертификата.Сертификат, МенеджерКриптографии);
		
	КонецЦикла;
	
	Если СертификатыОрганизацийДляОбменаНаСервере.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	СертификатыДляПодписанияНаСервере = Новый Структура;
	СертификатыДляПодписанияНаСервере.Вставить("Сертификаты",           СертификатыОрганизацийДляОбменаНаСервере);
	СертификатыДляПодписанияНаСервере.Вставить("МенеджерыКриптографии", МенеджерыКриптографии);
	
	//@skip-check constructor-function-return-section
	Возврат СертификатыДляПодписанияНаСервере;
	
КонецФункции

// Получает доступные пользователю для подписи сертификаты по организации.
//
// Параметры:
//  Организация - ОпределяемыйТип.Организация - организация, чьи сертификаты получаются.
//  ВключатьСертификатыБезОрганизации - Булево - Включать сертификаты с незаполненной организацией, если отсутсвуют сертификаты организации.
//  ВключатьСертификатыФЛ_СМЧД - Булево - Включать сертификаты, оформленные на физических лиц, требующие МЧД
// Возвращаемое значение:
//   Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования - массив доступных сертификатов.
//
Функция СертификатыПользователяДляПодписиПоОрганизации(Организация, ВключатьСертификатыБезОрганизации = Ложь, ВключатьСертификатыФЛ_СМЧД = Истина) Экспорт
	
	Если ТипЗнч(Организация) = Тип("Массив") Тогда
		Организации = Организация;
	Иначе
		Организации = Новый Массив;
		Организации.Добавить(Организация);
	КонецЕсли;
	
	Возврат СертификатыПользователяДляПодписиПоОрганизациям(Организации, ВключатьСертификатыБезОрганизации, ВключатьСертификатыФЛ_СМЧД)[Организация];
	
КонецФункции

// Получает доступные пользователю для подписи сертификаты по организациям.
//
// Параметры:
//  Организации - Массив из ОпределяемыйТип.Организация - организация, чьи сертификаты получаются.
//  ВключатьСертификатыБезОрганизации - Булево - Включать сертификаты с незаполненной организацией, если отсутствуют сертификаты организации.
//  ВключатьСертификатыФЛ_СМЧД - Булево - Включать сертификаты, оформленные на физических лиц, 
// Возвращаемое значение:
//   Соответствие из КлючИЗначение:
//   * Ключ - ОпределяемыйТип.Организация - организация.
//   * Значение - Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования - массив доступных сертификатов.
//
Функция СертификатыПользователяДляПодписиПоОрганизациям(Организации, ВключатьСертификатыБезОрганизации = Ложь, ВключатьСертификатыФЛ_СМЧД = Истина) Экспорт
	
	Результат = Новый Соответствие;
	Для Каждого Организация Из Организации Цикл
		Результат.Вставить(Организация, Новый Массив);
	КонецЦикла;
	
	ЕстьПравоДоступаЧтение = ПравоДоступа("Чтение", Метаданные.Справочники.СертификатыКлючейЭлектроннойПодписиИШифрования);
	Если Не ЕстьПравоДоступаЧтение Тогда
		Возврат Результат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Сертификаты.Организация       КАК Организация,
	|	Сертификаты.Ссылка            КАК Сертификат,
	|	Сертификаты.ДанныеСертификата КАК ДанныеСертификата
	|ИЗ
	|	Справочник.СертификатыКлючейЭлектроннойПодписиИШифрования КАК Сертификаты
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.СертификатыКлючейЭлектроннойПодписиИШифрования.Пользователи КАК ПользователиСертификатов
	|		ПО Сертификаты.Ссылка = ПользователиСертификатов.Ссылка
	|ГДЕ
	|	Сертификаты.Организация В (&Организации)
	|	И НЕ Сертификаты.Отозван
	|	И НЕ Сертификаты.ПометкаУдаления
	|	И Сертификаты.Подписание
	|	И (Сертификаты.ДействителенДо >= &ТекущаяДатаСеанса
	|			ИЛИ Сертификаты.ДействителенДо = ДАТАВРЕМЯ(1, 1, 1))
	|	И (Сертификаты.Пользователь = &Пользователь
	|			ИЛИ ПользователиСертификатов.Пользователь = &Пользователь
	|			ИЛИ Сертификаты.Пользователь В (&ПустойПользователь)
	|				И ПользователиСертификатов.Пользователь ЕСТЬ NULL)
	|;
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	Сертификаты.Ссылка КАК Сертификат
	|ИЗ
	|	Справочник.СертификатыКлючейЭлектроннойПодписиИШифрования КАК Сертификаты
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.СертификатыКлючейЭлектроннойПодписиИШифрования.Пользователи КАК ПользователиСертификатов
	|		ПО Сертификаты.Ссылка = ПользователиСертификатов.Ссылка
	|ГДЕ
	|	ВЫБОР
	|		КОГДА &ВключатьСертификатыБезОрганизации
	|			ТОГДА Сертификаты.Организация В (&ПустаяОрганизация)
	|		ИНАЧЕ ЛОЖЬ
	|	КОНЕЦ
	|	И НЕ Сертификаты.Отозван
	|	И НЕ Сертификаты.ПометкаУдаления
	|	И Сертификаты.Подписание
	|	И (Сертификаты.ДействителенДо >= &ТекущаяДатаСеанса
	|			ИЛИ Сертификаты.ДействителенДо = ДАТАВРЕМЯ(1, 1, 1))
	|	И (Сертификаты.Пользователь = &Пользователь
	|			ИЛИ ПользователиСертификатов.Пользователь = &Пользователь
	|			ИЛИ Сертификаты.Пользователь В (&ПустойПользователь)
	|				И ПользователиСертификатов.Пользователь ЕСТЬ NULL)";
	
	Запрос.УстановитьПараметр("ТекущаяДатаСеанса",                 ТекущаяДатаСеанса());
	Запрос.УстановитьПараметр("ВключатьСертификатыБезОрганизации", ВключатьСертификатыБезОрганизации И ВключатьСертификатыФЛ_СМЧД);
	Запрос.УстановитьПараметр("Организации",                       Организации);
	Запрос.УстановитьПараметр("ПустаяОрганизация",                 НезаполненныеЗначенияОпределяемогоТипа("Организация"));
	Запрос.УстановитьПараметр("Пользователь",                      Пользователи.ТекущийПользователь());
	Запрос.УстановитьПараметр("ПустойПользователь",                НезаполненныеЗначенияОпределяемогоТипа("Пользователь"));
	Запрос.Параметры.ПустойПользователь.Добавить(Пользователи.СсылкаНеуказанногоПользователя());
	
	РезультатЗапроса = Запрос.ВыполнитьПакет();
	
	Выборка = РезультатЗапроса[0].Выбрать();
	Пока Выборка.Следующий() Цикл  
		
		СертификатыПоОрганизации = Результат[Выборка.Организация];
		Если СертификатыПоОрганизации.Найти(Выборка.Сертификат) <> Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Если Не ВключатьСертификатыФЛ_СМЧД Тогда
			
			Если ТребуетсяДоверенностьДляИспользованияСертификата(Выборка.Сертификат, Выборка.Организация) Тогда
				Продолжить;
			КонецЕсли;
			
		КонецЕсли;
		
		СертификатыПоОрганизации.Добавить(Выборка.Сертификат);
		
	КонецЦикла;
	
	Если ВключатьСертификатыБезОрганизации Тогда
		Выборка = РезультатЗапроса[1].Выбрать();
		Для Каждого КлючИЗначение Из Результат Цикл
			Если КлючИЗначение.Значение.Количество() = 0 Тогда
				Выборка.Сбросить();
				Пока Выборка.Следующий() Цикл
					КлючИЗначение.Значение.Добавить(Выборка.Сертификат);
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ФорматированиеJSON

// Форматирует текст сообщения в формате JSON
//
// Параметры:
//  ТекстСообщенияJSON           - Строка - текст сообщения, который.
//  УдалитьПробелыИПереносыСтрок - Булево - признак использования отступов, по умолчанию Ложь.
//  КлючиРазворачиванияBASE64 - Неопределено -
//                            - Соответствие Из КлючИЗначение:
//  * Ключ - Строка - Имя свойства для разворота BASE64
//  * Значение - Неопределено - не используется
// Возвращаемое значение:
//  Строка - отформатированная строка JSON.
//
Функция ФорматироватьJSON(ТекстСообщенияJSON, УдалитьПробелыИПереносыСтрок = Ложь, КлючиРазворачиванияBASE64 = Неопределено) Экспорт
	
	ЧтениеJSON = Новый ЧтениеJSON();
	ЧтениеJSON.УстановитьСтроку(ТекстСообщенияJSON);
	
	Данные = ПрочитатьJSON(ЧтениеJSON, Истина);
	ЧтениеJSON.Закрыть();
	
	Если КлючиРазворачиванияBASE64 <> Неопределено
		И ТипЗнч(Данные) <> Тип("Массив") Тогда
		Для Каждого КлючИЗначение Из КлючиРазворачиванияBASE64 Цикл
			ДанныеПоКлючу = Данные[КлючИЗначение.Ключ];
			Если ДанныеПоКлючу = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			Данные[КлючИЗначение.Ключ] = ОбщегоНазначенияИСКлиентСервер.ТекстJSONВОбъект(ШтрихкодированиеОбщегоНазначенияИСКлиентСервер.Base64ВШтрихкод(ДанныеПоКлючу), Истина);
		КонецЦикла;
	КонецЕсли;
	
	Возврат ОбщегоНазначенияИСКлиентСервер.ОбъектВТекстJSON(Данные, УдалитьПробелыИПереносыСтрок);
	
КонецФункции

#КонецОбласти

#Область ОбменСИспользованиемЭДО

// Получает структуру настроек обмена через ЭДО, извлекая из константы НастройкиОбменаГосИС.
// 
// Возвращаемое значение:
//  ТаблицаЗначений, Неопределено, Произвольный - Настройки обмена.
//
Функция НастройкиОбменаГосИС() Экспорт
	
	ХранилищеЗначения = Константы.НастройкиОбменаГосИС.Получить();
	
	Если ХранилищеЗначения = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	НастройкиОбмена = ХранилищеЗначения.Получить();
	
	Возврат НастройкиОбмена;
	
КонецФункции

Функция СертификатыОрганизаций() Экспорт
	
	СертификатыОрганизаций = Новый ТаблицаЗначений();
	СертификатыОрганизаций.Колонки.Добавить("Организация", Метаданные.ОпределяемыеТипы.Организация.Тип);
	
	НастройкиОбменаГосИС = НастройкиОбменаГосИС();
	
	Если НастройкиОбменаГосИС = Неопределено Тогда
		Возврат СертификатыОрганизаций;
	КонецЕсли;
	
	Для Каждого Строка Из НастройкиОбменаГосИС Цикл
		ЗаполнитьЗначенияСвойств(СертификатыОрганизаций.Добавить(), Строка);
	КонецЦикла;
	
	Возврат СертификатыОрганизаций;
	
КонецФункции

Функция ТребуетсяДоверенностьДляИспользованияСертификата(Сертификат, Организация, Знач ИНН = "") Экспорт
	
	Если Не ОбщегоНазначения.ПодсистемаСуществует("ЭлектронноеВзаимодействие.БазоваяФункциональность.Криптография") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	МодульКриптографияБЭД             = ОбщегоНазначения.ОбщийМодуль("КриптографияБЭД");
	МодульКриптографияБЭДКлиентСервер = ОбщегоНазначения.ОбщийМодуль("КриптографияБЭДКлиентСервер");
	
	СвойстваСубъектаСертификата = МодульКриптографияБЭД.СвойстваСубъектаСертификатаПоСсылке(Сертификат);
	СвойстваИздателяСертификата = МодульКриптографияБЭД.СвойстваИздателяСертификатаПоСсылке(Сертификат);
	
	ВидСертификата = 
		МодульКриптографияБЭД.ВидСертификатаПоДаннымСвойствСубъектаИИздателя(
			СвойстваСубъектаСертификата, СвойстваИздателяСертификата);
	ВидыСертификатов = МодульКриптографияБЭДКлиентСервер.ВидыСертификатовЭлектроннойПодписи();
	
	Если Не ЗначениеЗаполнено(ИНН) Тогда
		ИНН = РаботаСКонтрагентамиИСВызовСервера.ИННКПППоОрганизацииКонтрагенту(Организация).ИНН;
	КонецЕсли;
	
	ОрганизацияУчетнойЗаписиСовпадаетСВладельцемСертификата = 
		СтроковыеФункцииКлиентСервер.ДополнитьСтроку(ИНН, 12) = 
			СтроковыеФункцииКлиентСервер.ДополнитьСтроку(СвойстваСубъектаСертификата.ИНН, 12);
	
	ТребуетсяДоверенность = Ложь;
	Если ВидСертификата = ВидыСертификатов.СертификатДолжностногоЛицаГосОрганаУЦ Тогда
		ТребуетсяДоверенность = Ложь;
	ИначеЕсли ВидСертификата = ВидыСертификатов.СертификатЮЛ
		Или ВидСертификата = ВидыСертификатов.СертификатИП Тогда
		ТребуетсяДоверенность = Не ОрганизацияУчетнойЗаписиСовпадаетСВладельцемСертификата;
	ИначеЕсли ВидСертификата = ВидыСертификатов.СертификатФЛ Тогда
		ТребуетсяДоверенность = Истина;
	КонецЕсли;
	
	Возврат ТребуетсяДоверенность;
	
КонецФункции

#КонецОбласти

#Область ОрганизацииКонтрагенты

// Проверяет использование подразделений в информационной базе.
// 
// Возвращаемое значение:
//  Булево - Подразделения используются.
//
Функция ИспользоватьПодразделения() Экспорт
	
	Использовать = Ложь;
	ОбщегоНазначенияИСПереопределяемый.ИспользованиеПодразделений(Использовать);
	Возврат Использовать;
	
КонецФункции

// Определяет использование в информационной базе обособленных подразделений выделенных на отдельный баланс
// 
// Возвращаемое значение:
//  Булево - Используются подразделения, выделенных на обособленный баланс
//
Функция ИспользоватьОбособленныеПодразделенияВыделенныеНаБаланс() Экспорт
	
	Использовать = Ложь;
	ОбщегоНазначенияИСПереопределяемый.ИспользоватьОбособленныеПодразделенияВыделенныеНаБаланс(Использовать);
	Возврат Использовать;
	
КонецФункции

#КонецОбласти

#Область МинимальныеЦеныПодакцизныхТоваров

//Выполняет проверку минимальных цен подакцизных товаров
//
// Параметры:
//  ПараметрыСканирования - Структура - параметры проверки минимальных цен:
//   * Грузоотправитель   - ОпределяемыйТип.ОрганизацияКонтрагентГосИС, Неопределено -
//   * Грузополучатель    - ОпределяемыйТип.ОрганизацияКонтрагентГосИС, Неопределено -
//   * ВидДокумента       - ПеречислениеСсылка.ВидыДокументовЕГАИС
//   * ЭтоРеализация      - Булево 
//   
//  Товары - ТаблицаЗначений - таблица продукции для проверки минимальных цен алкгольной продукции:
//   * Номенклатура   - ОпределяемыйТип.Номенклатура
//   * Характеристика - ОпределяемыйТип.ХарактеристикаНоменклатуры
//   * Серия          - ОпределяемыйТип.СерияНоменклатуры
//  
//  ШтрихкодыУпаковок - ТаблицаЗначений - штриходы упаковок табачной продукции для проверки минимальных/максимальных цен
//   см. ОбщегоНазначенияИСМП.МинимальныеЦеныТабачнойПродукции
// 
// Возвращаемое значение:
//  Структура - результаты проверки минимальных цен подакцизных товаров:
//   * РезультатыПроверкиПоАлкогольнойПродукции - см. ОбщегоНазначенияЕГАИС.ПодобратьМинимальныеЦеныАлкогольнойПродукции 
//   * РезультатыПроверкиПоТабачнойПродукции - см. ОбщегоНазначенияИСМП.МинимальныеЦеныТабачнойПродукции
//
Функция МинимальныеЦеныПодакцизныхТоваров(ПараметрыСканирования=Неопределено, Товары=Неопределено, ШтрихкодыУпаковок=Неопределено) Экспорт
	
	РезультатыПроверкиЦен = Новый Структура;
	
	Если Товары <> Неопределено И ПодсистемаСуществует("ЕГАИС") Тогда
		МодульИнтеграцияЕГАИС = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияЕГАИС");
		ПараметрыПроверки     = МодульИнтеграцияЕГАИС.ИнициализироватьПараметрыПроверкиМинимальныхЦен();
		
		Если ПараметрыСканирования.ВидДокумента = Перечисления["ВидыДокументовЕГАИС"].ЧекККМ Тогда
			ПараметрыПроверки.РозничнаяПродажа      = Истина;
			ПараметрыПроверки.ПроверятьКонтрагентов = Ложь;
		Иначе
			Если ПараметрыСканирования.ЭтоРеализация Тогда
				ПараметрыПроверки.Реализация = Истина;
			Иначе
				ПараметрыПроверки.Поступление = Истина;
			КонецЕсли;
		КонецЕсли;
		
		ПараметрыПроверки.Организация = ПараметрыСканирования.Грузоотправитель;
		ПараметрыПроверки.Контрагент  = ПараметрыСканирования.Грузополучатель;
		
		МодульИнтеграцияЕГАИС.ПодобратьМинимальныеЦеныАлкогольнойПродукции(Товары, ПараметрыПроверки);
		РезультатыПроверкиЦен.Вставить("РезультатыПроверкиПоАлкогольнойПродукции", ПараметрыПроверки);	
	КонецЕсли;
	
	Если ШтрихкодыУпаковок <> Неопределено Тогда
		МодульИнтеграцияИСМП = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияИСМП");
		РезультатыПроверкиПоТабачнойПродукции = МодульИнтеграцияИСМП.МинимальныеЦеныТабачнойПродукции(ШтрихкодыУпаковок, ПараметрыСканирования);
		РезультатыПроверкиЦен.Вставить("РезультатыПроверкиПоТабачнойПродукции", РезультатыПроверкиПоТабачнойПродукции);	
	КонецЕсли;
	
	Возврат РезультатыПроверкиЦен;
	
КонецФункции

#КонецОбласти

#Область УсловноеОформлениеЭлементовШапкиСлужебный

Функция ПроверитьУсловиеОтбора(Форма, ЭлементОформления)
	
	Результат = Истина;
	
	Для Каждого ЭлементОтбора Из ЭлементОформления.Отбор.Элементы Цикл
		ЛевоеЗначение = ЭлементОтбора.ЛевоеЗначение;
		ПравоеЗначение = ЭлементОтбора.ПравоеЗначение;
		Если ТипЗнч(ЛевоеЗначение) = Тип("ПолеКомпоновкиДанных") Тогда
			ЛевоеЗначение = ПреобразоватьПолеВЗначение(Форма, "" + ЛевоеЗначение);
		КонецЕсли;
		Если ТипЗнч(ПравоеЗначение) = Тип("ПолеКомпоновкиДанных") Тогда
			ПравоеЗначение = ПреобразоватьПолеВЗначение(Форма, "" + ПравоеЗначение);
		КонецЕсли;
		
		Если ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Больше Тогда
			Результат = Результат И ЛевоеЗначение > ПравоеЗначение;
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.БольшеИлиРавно Тогда
			Результат = Результат И ЛевоеЗначение >= ПравоеЗначение;
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Меньше Тогда
			Результат = Результат И ЛевоеЗначение < ПравоеЗначение;
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.МеньшеИлиРавно Тогда
			Результат = Результат И ЛевоеЗначение <= ПравоеЗначение;
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно Тогда
			Результат = Результат И ЛевоеЗначение = ПравоеЗначение;
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.НеРавно Тогда
			Результат = Результат И ЛевоеЗначение <> ПравоеЗначение;
			
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Заполнено Тогда
			Результат = Результат И ЗначениеЗаполнено(ЛевоеЗначение);
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.НеЗаполнено Тогда
			Результат = Результат И Не ЗначениеЗаполнено(ЛевоеЗначение);
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.ВСписке Тогда
			Результат = Результат И ПравоеЗначение.НайтиПоЗначению(ЛевоеЗначение)<>Неопределено;
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.НеВСписке Тогда
			Результат = Результат И ПравоеЗначение.НайтиПоЗначению(ЛевоеЗначение)=Неопределено;
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.НачинаетсяС Тогда
			Результат = Результат И СтрНачинаетсяС(ЛевоеЗначение, ПравоеЗначение);
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.НеНачинаетсяС Тогда
			Результат = Результат И Не СтрНачинаетсяС(ЛевоеЗначение, ПравоеЗначение);
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура УстановитьУсловноеОформлениеВНетабличноеПоле(ЭлементФормы, ЗначенияОформления)
	
	Для Каждого КлючИЗначение Из ЗначенияОформления Цикл
		Если КлючИЗначение.Ключ = "ЦветТекста" Тогда
			Если КлючИЗначение.Значение = Неопределено Тогда
				ЭлементФормы[КлючИЗначение.Ключ] = ЦветаСтиля.ЦветТекстаПоля;
			Иначе
				ЭлементФормы[КлючИЗначение.Ключ] = КлючИЗначение.Значение;
			КонецЕсли;
		ИначеЕсли КлючИЗначение.Ключ = "ЦветФона" Тогда
			Если КлючИЗначение.Значение = Неопределено Тогда
				ЭлементФормы[КлючИЗначение.Ключ] = ЦветаСтиля.ЦветФонаПоля;
			Иначе
				ЭлементФормы[КлючИЗначение.Ключ] = КлючИЗначение.Значение;
			КонецЕсли;
		ИначеЕсли КлючИЗначение.Ключ = "Шрифт" Тогда
			Если КлючИЗначение.Значение = Неопределено Тогда
				ЭлементФормы[КлючИЗначение.Ключ] = ШрифтыСтиля.ОбычныйШрифтТекста;
			Иначе
				ЭлементФормы[КлючИЗначение.Ключ] = КлючИЗначение.Значение;
			КонецЕсли;
		ИначеЕсли КлючИЗначение.Ключ = "ТолькоПросмотр" Тогда
			Если КлючИЗначение.Значение = Неопределено Тогда
				ЭлементФормы[КлючИЗначение.Ключ] = Ложь;
			Иначе
				ЭлементФормы[КлючИЗначение.Ключ] = КлючИЗначение.Значение;
			КонецЕсли;
		ИначеЕсли КлючИЗначение.Ключ = "ОтметкаНезаполненного" Тогда
			ЭлементФормы.АвтоОтметкаНезаполненного = КлючИЗначение.Значение;
		ИначеЕсли КлючИЗначение.Ключ = "Текст" Тогда
			ЭлементФормы.ПодсказкаВвода = КлючИЗначение.Значение;
		ИначеЕсли КлючИЗначение.Ключ = "Видимость"
				Или КлючИЗначение.Ключ = "Доступность" Тогда
			Если КлючИЗначение.Значение = Неопределено Тогда
				ЭлементФормы[КлючИЗначение.Ключ] = Истина;
			Иначе
				ЭлементФормы[КлючИЗначение.Ключ] = КлючИЗначение.Значение;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
		
КонецПроцедуры

Функция ПреобразоватьПолеВЗначение(Форма, ПолеКомпоновкиДанныеСтрокой)
	
	Элемент = Форма.Элементы.Найти(ПолеКомпоновкиДанныеСтрокой);
	Если Элемент <> Неопределено Тогда
		
		Префикс = "Элементы.";
		Начало = СтрДлина(Префикс) + 1;
		Ключ = ".ТекущиеДанные.";
		ДлинаКлюча = СтрДлина(Ключ);
		ПозицияЭлементТаблицы = СтрНайти(Элемент.ПутьКДанным, Ключ);
		
		Если ПозицияЭлементТаблицы <> 0 Тогда
			ИмяТаблицы = Сред(Элемент.ПутьКДанным, Начало, ПозицияЭлементТаблицы - Начало);
			ИмяПоля = Сред(Элемент.ПутьКДанным, ПозицияЭлементТаблицы + ДлинаКлюча);
			ЭлементФормыТаблица = Форма.Элементы.Найти(ИмяТаблицы);
			ПутьКДаннымТаблицы = СтрРазделить(ЭлементФормыТаблица.ПутьКДанным, ".");
			Если ПутьКДаннымТаблицы.Количество() = 1 Тогда // Первая строка таблицы формы
				Возврат Форма[ПутьКДаннымТаблицы[0]][0][ИмяПоля];
			ИначеЕсли ПутьКДаннымТаблицы.Количество() = 2 Тогда // Первая строка таблицы объекта формы
				Возврат Форма[ПутьКДаннымТаблицы[0]][ПутьКДаннымТаблицы[1]][0][ИмяПоля];
			КонецЕсли;
		Иначе
			ПутьКДанным = СтрРазделить(Элемент.ПутьКДанным, ".");
			Если ПутьКДанным.Количество() = 1 Тогда // Реквизит формы
				Возврат Форма[ПутьКДанным[0]];
			ИначеЕсли ПутьКДанным.Количество() = 2 Тогда // Реквизит объекта формы
				Возврат Форма[ПутьКДанным[0]][ПутьКДанным[1]];
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	ЧастиПоляКомпоновки = СтрРазделить(ПолеКомпоновкиДанныеСтрокой, ".");
	Если ЧастиПоляКомпоновки.Количество() = 1 Тогда
		Возврат Форма[ПолеКомпоновкиДанныеСтрокой];
	ИначеЕсли ЧастиПоляКомпоновки.Количество() = 3 Тогда
		Возврат Форма[ЧастиПоляКомпоновки[0]][ЧастиПоляКомпоновки[1]][0][ЧастиПоляКомпоновки[2]];
	Иначе
		Попытка
			//таблица формы
			Возврат Форма[ЧастиПоляКомпоновки[0]][0][ЧастиПоляКомпоновки[1]];
		Исключение
			//реквизит объекта
			Возврат Форма[ЧастиПоляКомпоновки[0]][ЧастиПоляКомпоновки[1]];
		КонецПопытки;
	КонецЕсли;
КонецФункции

#КонецОбласти

#КонецОбласти
