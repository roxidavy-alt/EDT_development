
#Область ПрограммныйИнтерфейс

// Возвращает дату платежа по календарю с учетом сдвига.
// 
// Параметры:
//  Параметры - см. ЭтапыОплатыКлиентСервер.ПараметрыРасчетаДатыПлатежа
// 
// Возвращаемое значение:
//  Дата - Дата платежа по календарю
Функция ДатаПлатежаПоКалендарю(Параметры) Экспорт
	
	ДозаполнитьДатыЕслиНеЗаполнены(Параметры);
	
	Если НЕ ЗначениеЗаполнено(Параметры.ВариантОтсчета) Тогда
		ДатаПлатежа = Дата(1,1,1);
	ИначеЕсли Параметры.ВариантОтсчета = Перечисления.ВариантыОтсчетаДатыПлатежа.ФиксированнаяДата Тогда
		ДатаПлатежа = Параметры.ДатаПлатежа;
	ИначеЕсли Параметры.ВариантОтсчета = Перечисления.ВариантыОтсчетаДатыПлатежа.ОтДатыЗаказа И ЗначениеЗаполнено(Параметры.ДатаЗаказа) Тогда
		ДатаПлатежа = ДатаПоКалендарю(Параметры.Календарь, Параметры.ДатаЗаказа, Параметры.Сдвиг);
	ИначеЕсли Параметры.ВариантОтсчета = Перечисления.ВариантыОтсчетаДатыПлатежа.ОтДатыСогласования И ЗначениеЗаполнено(Параметры.ДатаСогласования) Тогда
		ДатаПлатежа = ДатаПоКалендарю(Параметры.Календарь, Параметры.ДатаСогласования, Параметры.Сдвиг);
	ИначеЕсли Параметры.ВариантОтсчета = Перечисления.ВариантыОтсчетаДатыПлатежа.ДоДатыОтгрузки И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		ДатаПлатежа = ДатаПоКалендарю(Параметры.Календарь, Параметры.ДатаОтгрузки, -Параметры.Сдвиг);
	ИначеЕсли Параметры.ВариантОтсчета = Перечисления.ВариантыОтсчетаДатыПлатежа.ОтДатыОтгрузки И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		ДатаПлатежа = ДатаПоКалендарю(Параметры.Календарь, Параметры.ДатаОтгрузки, Параметры.Сдвиг);
	ИначеЕсли Параметры.ВариантОтсчета = Перечисления.ВариантыОтсчетаДатыПлатежа.ОтДатыПоступления И ЗначениеЗаполнено(Параметры.ДатаПоступления) Тогда
		ДатаПлатежа = ДатаПоКалендарю(Параметры.Календарь, Параметры.ДатаПоступления, Параметры.Сдвиг);
	ИначеЕсли Параметры.ВариантОтсчета = Перечисления.ВариантыОтсчетаДатыПлатежа.ОтДатыПереходаПраваСобственности И ЗначениеЗаполнено(Параметры.ДатаПереходаПраваСобственности) Тогда
		ДатаПлатежа = ДатаПоКалендарю(Параметры.Календарь, Параметры.ДатаПереходаПраваСобственности, Параметры.Сдвиг);
	ИначеЕсли Параметры.ВариантОтсчета = Перечисления.ВариантыОтсчетаДатыПлатежа.Формула И ЗначениеЗаполнено(Параметры.Формула) Тогда
		ДатаПлатежа = ДатаПлатежаПоФормуле(Параметры);
	Иначе
		ДатаПлатежа = Дата(1,1,1);
	КонецЕсли;
	
	Возврат ДатаПлатежа
	
КонецФункции

// Возвращает расчетную дату платежа без учета производственного календаря.
// 
// Параметры:
//  Параметры - см. ЭтапыОплатыКлиентСервер.ПараметрыРасчетаДатыПлатежа
// 
// Возвращаемое значение:
//  Дата - Дата платежа (без учета календаря)
//
Функция ДатаПлатежаБезКалендаря(Параметры) Экспорт
	
	ДозаполнитьДатыЕслиНеЗаполнены(Параметры);
	
	Если НЕ ЗначениеЗаполнено(Параметры.ВариантОтсчета) Тогда
		ДатаПлатежа = Дата(1,1,1);
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ФиксированнаяДата") Тогда
		ДатаПлатежа = Параметры.ДатаПлатежа;
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыЗаказа") И ЗначениеЗаполнено(Параметры.ДатаЗаказа) Тогда
		ДатаПлатежа = Параметры.ДатаЗаказа + 86400 * Параметры.Сдвиг;
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыСогласования") И ЗначениеЗаполнено(Параметры.ДатаСогласования) Тогда
		ДатаПлатежа = Параметры.ДатаСогласования + 86400 * Параметры.Сдвиг;
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ДоДатыОтгрузки") И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		ДатаПлатежа = Параметры.ДатаОтгрузки - 86400 * Параметры.Сдвиг;
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыОтгрузки") И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		ДатаПлатежа = Параметры.ДатаОтгрузки + 86400 * Параметры.Сдвиг;
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыПоступления") И ЗначениеЗаполнено(Параметры.ДатаПоступления) Тогда
		ДатаПлатежа = Параметры.ДатаПоступления + 86400 * Параметры.Сдвиг;
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыПереходаПраваСобственности") И ЗначениеЗаполнено(Параметры.ДатаПереходаПраваСобственности) Тогда
		ДатаПлатежа = Параметры.ДатаПереходаПраваСобственности + 86400 * Параметры.Сдвиг;
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.Формула") И ЗначениеЗаполнено(Параметры.Формула) Тогда
		ДатаПлатежа = ДатаПлатежаПоФормуле(Параметры);
	Иначе
		ДатаПлатежа = Дата(1,1,1);
	КонецЕсли;
	
	Возврат ДатаПлатежа;
	
КонецФункции

// Возвращает сдвиг по календарю от даты платежа.
// 
// Параметры:
//  Параметры - см. ЭтапыОплатыКлиентСервер.ПараметрыРасчетаДатыПлатежа
// 
// Возвращаемое значение:
//  Число - Сдвиг по календарю
Функция СдвигПоКалендарю(Параметры) Экспорт
	
	ДозаполнитьДатыЕслиНеЗаполнены(Параметры);
	
	Если Параметры.ДатаПлатежа = Дата(1,1,1) ИЛИ Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ФиксированнаяДата") Тогда
		Сдвиг = 0;
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыЗаказа") 
		И ЗначениеЗаполнено(Параметры.ДатаЗаказа) Тогда
		Сдвиг = СдвигМеждуДатамиПоКалендарю(Параметры.Календарь, Параметры.ДатаЗаказа, Параметры.ДатаПлатежа);
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыСогласования") 
		И ЗначениеЗаполнено(Параметры.ДатаСогласования) Тогда
		Сдвиг = СдвигМеждуДатамиПоКалендарю(Параметры.Календарь, Параметры.ДатаСогласования, Параметры.ДатаПлатежа);
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ДоДатыОтгрузки") 
		И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		Сдвиг = СдвигМеждуДатамиПоКалендарю(Параметры.Календарь, Параметры.ДатаПлатежа, Параметры.ДатаОтгрузки);
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыОтгрузки")
		И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		Сдвиг = СдвигМеждуДатамиПоКалендарю(Параметры.Календарь, Параметры.ДатаОтгрузки, Параметры.ДатаПлатежа);
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыПоступления")
		И ЗначениеЗаполнено(Параметры.ДатаПоступления) Тогда
		Сдвиг = СдвигМеждуДатамиПоКалендарю(Параметры.Календарь, Параметры.ДатаПоступления, Параметры.ДатаПлатежа);
	ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыПереходаПраваСобственности")
		И ЗначениеЗаполнено(Параметры.ДатаПереходаПраваСобственности) Тогда
		Сдвиг = СдвигМеждуДатамиПоКалендарю(Параметры.Календарь, Параметры.ДатаПереходаПраваСобственности, Параметры.ДатаПлатежа);
	Иначе
		Сдвиг = 0;
	КонецЕсли;
	
	Возврат Сдвиг;
	
КонецФункции

// Возвращает расчетное количество дней от дата события до даты платежа без учета производственного календаря.
// 
// Параметры:
//  Параметры - см. ЭтапыОплатыКлиентСервер.ПараметрыРасчетаДатыПлатежа
// 
// Возвращаемое значение:
//  Число - Сдвиг, дней (без учета календаря)
//
Функция СдвигБезКалендаря(Параметры) Экспорт
	
	ДозаполнитьДатыЕслиНеЗаполнены(Параметры);
	
	Если НЕ ЗначениеЗаполнено(Параметры.ВариантОтсчета)
		Или НЕ ЗначениеЗаполнено(Параметры.ДатаПлатежа) 
		Или Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ФиксированнаяДата") Тогда
		Сдвиг = 0;
	Иначе
		Если Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыЗаказа") 
			И ЗначениеЗаполнено(Параметры.ДатаЗаказа) Тогда
			Сдвиг = (Параметры.ДатаПлатежа - НачалоДня(Параметры.ДатаЗаказа)) / 86400 ;
		ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыСогласования") 
			И ЗначениеЗаполнено(Параметры.ДатаСогласования) Тогда
			Сдвиг = (Параметры.ДатаПлатежа - Параметры.ДатаСогласования) / 86400 ;
		ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ДоДатыОтгрузки") 
			И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
			Сдвиг = (Параметры.ДатаОтгрузки - Параметры.ДатаПлатежа) / 86400 ;
		ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыОтгрузки")
			И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
			Сдвиг = (Параметры.ДатаПлатежа - Параметры.ДатаОтгрузки) / 86400 ;
		ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыПоступления")
			И ЗначениеЗаполнено(Параметры.ДатаПоступления) Тогда
			Сдвиг = (Параметры.ДатаПлатежа - Параметры.ДатаПоступления) / 86400;
		ИначеЕсли Параметры.ВариантОтсчета = ПредопределенноеЗначение("Перечисление.ВариантыОтсчетаДатыПлатежа.ОтДатыПереходаПраваСобственности")
			И ЗначениеЗаполнено(Параметры.ДатаПереходаПраваСобственности) Тогда
			Сдвиг = (Параметры.ДатаПлатежа - Параметры.ДатаПереходаПраваСобственности) / 86400 ;
		Иначе
			Сдвиг = 0;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Сдвиг;
	
КонецФункции

// Функция проверки наличия графика оплаты в соглашении
//
// Параметры:
// 		Соглашение - СправочникСсылка.СоглашенияСПоставщиками, СправочникСсылка.СоглашенияСКлиентами - 
//
// Возвращаемое значение:
// 		Булево - "Истина", если в графике есть хотя бы один этап и соглашения используются
//
Функция ГрафикСоглашенияЗаполнен(Соглашение) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Соглашение) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ТипЗнч(Соглашение) = Тип("СправочникСсылка.СоглашенияСКлиентами")
		И НЕ ПолучитьФункциональнуюОпцию("ИспользоватьСоглашенияСКлиентами")
		ИЛИ ТипЗнч(Соглашение) = Тип("СправочникСсылка.СоглашенияСПоставщиками")
		И НЕ ПолучитьФункциональнуюОпцию("ИспользоватьСоглашенияСПоставщиками") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ТипЗнч(Соглашение) = Тип("СправочникСсылка.СоглашенияСКлиентами") Тогда
		Запрос = Новый Запрос(
			"ВЫБРАТЬ ПЕРВЫЕ 1
			|	СоглашенияСКлиентамиЭтапыГрафикаОплаты.Ссылка
			|ИЗ
			|	Справочник.СоглашенияСКлиентами.ЭтапыГрафикаОплаты КАК СоглашенияСКлиентамиЭтапыГрафикаОплаты
			|ГДЕ
			|	СоглашенияСКлиентамиЭтапыГрафикаОплаты.Ссылка = &Соглашение");
	Иначе
		Запрос = Новый Запрос(
			"ВЫБРАТЬ ПЕРВЫЕ 1
			|	СоглашенияСПоставщикамиЭтапыГрафикаОплаты.Ссылка
			|ИЗ
			|	Справочник.СоглашенияСПоставщиками.ЭтапыГрафикаОплаты КАК СоглашенияСПоставщикамиЭтапыГрафикаОплаты
			|ГДЕ
			|	СоглашенияСПоставщикамиЭтапыГрафикаОплаты.Ссылка = &Соглашение");
	КонецЕсли;
	Запрос.УстановитьПараметр("Соглашение", Соглашение);
	
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

// В таблице этапов есть этапы от даты отгрузки.
// 
// Параметры:
//  ЭтапыГрафикаОплаты - ТаблицаЗначений - Этапы графика оплаты
// 
// Возвращаемое значение:
//  Булево - В таблице этапов есть этапы от даты отгрузки
Функция ВТаблицеЭтаповЕстьЭтапыОтДатыОтгрузки(ЗНАЧ ЭтапыГрафикаОплаты) Экспорт
	
	Для Каждого СтрокаЭтапа Из ЭтапыГрафикаОплаты Цикл
		Если ЭтапыОплатыПовтИсп.СтрокаЭтаповЗависитОтДатыОтгрузки(СтрокаЭтапа.ВариантОтсчета, СтрокаЭтапа.Формула) Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
КонецФункции

// Возвращает дату, которая отличается указанной даты ДатаОт на количество дней,
// входящих в указанный график или производственный календарь ГрафикРаботы.
//
// Параметры:
//   ГрафикРаботы	- СправочникСсылка.Календари
//	             	- СправочникСсылка.ПроизводственныеКалендари - график или 
//                    производственный календарь, который необходимо использовать для расчета даты.
//   ДатаОт			- Дата - дата, от которой нужно рассчитать количество дней.
//   КоличествоДней	- Число - количество дней, на которые нужно увеличить дату начала.
//   ВызыватьИсключение - Булево - если Истина, вызвать исключение в случае незаполненного графика.
//
// Возвращаемое значение:
//   Дата, Неопределено - дата, увеличенная на количество дней, входящих в график.
//	                      Если выбранный график не заполнен, и ВызыватьИсключение = Ложь, возвращается Неопределено.
//
Функция ДатаПоКалендарю(Знач ГрафикРаботы, Знач ДатаОт, Знач КоличествоДней, ВызыватьИсключение = Ложь) Экспорт
	
	Если Не ЗначениеЗаполнено(ГрафикРаботы) Тогда
		Если ВызыватьИсключение Тогда
			ВызватьИсключение НСтр("ru = 'Не указан график работы или производственный календарь.';
									|en = 'Work schedule or business calendar is not specified.'");
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	ДатаОт = НачалоДня(ДатаОт);
	
	Если КоличествоДней = 0 Тогда
		Возврат ДатаОт;
	КонецЕсли;
	
	МассивДней = Новый Массив;
	МассивДней.Добавить(КоличествоДней);
	
	МассивДат = ДатыПоКалендарю(ГрафикРаботы, ДатаОт, МассивДней, ВызыватьИсключение);
	
	Возврат ?(МассивДат <> Неопределено И МассивДат.Количество() > 0, МассивДат[0], Дата(1,1,1));
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Возвращает даты, которые отличаются от указанной даты ДатаОт на количество дней,
// входящих в указанный график ГрафикРаботы.
//
// Параметры:
//   ГрафикРаботы	- СправочникСсылка.Календари
//	             	- СправочникСсылка.ПроизводственныеКалендари - график или 
//                    производственный календарь, который необходимо использовать для расчета дат.
//   ДатаОт			- Дата - дата, от которой нужно рассчитать количество дней.
//   МассивДней		- Массив из Число - количество дней (Число), на которые нужно увеличить дату начала.
//   ВызыватьИсключение - Булево - если Истина, вызвать исключение в случае незаполненного графика.
//
// Возвращаемое значение:
//   Неопределено, Массив из Дата - массив дат, увеличенных на количество дней, входящих в график,
//	                        Если график ГрафикРаботы не заполнен, и ВызыватьИсключение = Ложь, возвращается Неопределено.
//
Функция ДатыПоКалендарю(Знач ГрафикРаботы, Знач ДатаОт, Знач МассивДней, ВызыватьИсключение = Истина) 
	
	Если Не ЗначениеЗаполнено(ГрафикРаботы) Тогда
		Если ВызыватьИсключение Тогда
			ВызватьИсключение НСтр("ru = 'Не указан график работы или производственный календарь.';
									|en = 'Work schedule or business calendar is not specified.'");
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	
	// По производственному календарю.
	Запрос.Текст =
	"ВЫБРАТЬ
	|	КалендарныеГрафики.Дата КАК ДатаГрафика
	|ИЗ
	|	РегистрСведений.ДанныеПроизводственногоКалендаря КАК КалендарныеГрафики
	|ГДЕ
	|	КалендарныеГрафики.ПроизводственныйКалендарь = &ГрафикРаботы
	|	И КалендарныеГрафики.ВидДня В (ЗНАЧЕНИЕ(Перечисление.ВидыДнейПроизводственногоКалендаря.Рабочий), ЗНАЧЕНИЕ(Перечисление.ВидыДнейПроизводственногоКалендаря.Предпраздничный))
	|УПОРЯДОЧИТЬ ПО
	|	КалендарныеГрафики.Дата УБЫВ";
	Запрос.УстановитьПараметр("ГрафикРаботы", ГрафикРаботы);
	
	МассивДат = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("ДатаГрафика");
	
	Для сч = 0 По МассивДат.ВГраница() Цикл
		Если МассивДат[сч] >= ДатаОт Тогда
			ИндексОт = сч;
		Иначе
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ИндексОт = Неопределено Тогда
		СообщениеОбОшибке = НСтр("ru = 'Производственный календарь «%1» не заполнен с даты %2 на указанное количество рабочих дней.';
								|en = 'Cannot determine the date because business calendar ""%1"" is not filled for the specified number of workdays after %2.'");
		СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(СообщениеОбОшибке, ГрафикРаботы, Формат(ДатаОт, "ДЛФ=D"));
		Если ВызыватьИсключение Тогда
			ВызватьИсключение СообщениеОбОшибке
		Иначе
			ОбщегоНазначения.СообщитьПользователю(СообщениеОбОшибке);
			Возврат Неопределено
		КонецЕсли;
	Иначе
		Если ДатаОт <> МассивДат[ИндексОт] Тогда
			Сдвиг = 1;
		Иначе
			Сдвиг = 0;
		КонецЕсли;
		
		СоответствиеДат = Новый Соответствие;
		Для сч = 0 По МассивДат.Количество()-1 Цикл
			СоответствиеДат.Вставить(ИндексОт - сч + Сдвиг, МассивДат[сч]);
		КонецЦикла;
	КонецЕсли;
	
	МассивДатРезультат = Новый Массив;
	
	Для Каждого СтрокаДней Из МассивДней Цикл
		Дата = СоответствиеДат.Получить(СтрокаДней);
		Если Дата = Неопределено Тогда
			СообщениеОбОшибке = НСтр("ru = 'Производственный календарь «%1» не заполнен с даты %2 на указанное количество рабочих дней.';
									|en = 'Cannot determine the date because business calendar ""%1"" is not filled for the specified number of workdays after %2.'");
			СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(СообщениеОбОшибке, ГрафикРаботы, Формат(Дата, "ДЛФ=D"));
			Если ВызыватьИсключение Тогда
				ВызватьИсключение СообщениеОбОшибке
			Иначе
				ОбщегоНазначения.СообщитьПользователю(СообщениеОбОшибке);
			КонецЕсли;
		Иначе
			МассивДатРезультат.Добавить(Дата);
		КонецЕсли;
	КонецЦикла;
	
	Возврат МассивДатРезультат;
	
КонецФункции

Функция СдвигМеждуДатамиПоКалендарю(Знач ГрафикРаботы, Знач ДатаОт, Знач ДатаДо, ВызыватьИсключение = Ложь) 
	
	Если Не ЗначениеЗаполнено(ГрафикРаботы) Тогда
		Если ВызыватьИсключение Тогда
			ВызватьИсключение НСтр("ru = 'Не указан график работы или производственный календарь.';
									|en = 'Work schedule or business calendar is not specified.'");
		КонецЕсли;
		Возврат 0;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	
	// По производственному календарю.
	Запрос.Текст =
	"ВЫБРАТЬ
	|	КалендарныеГрафики.Дата КАК ДатаГрафика
	|ИЗ
	|	РегистрСведений.ДанныеПроизводственногоКалендаря КАК КалендарныеГрафики
	|ГДЕ
	|	КалендарныеГрафики.ПроизводственныйКалендарь = &ГрафикРаботы
	|	И КалендарныеГрафики.ВидДня В (ЗНАЧЕНИЕ(Перечисление.ВидыДнейПроизводственногоКалендаря.Рабочий), ЗНАЧЕНИЕ(Перечисление.ВидыДнейПроизводственногоКалендаря.Предпраздничный))
	|УПОРЯДОЧИТЬ ПО
	|	КалендарныеГрафики.Дата УБЫВ";
	Запрос.УстановитьПараметр("ГрафикРаботы", ГрафикРаботы);
	МассивДат = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("ДатаГрафика");
	
	Если МассивДат.Количество() = 0 Или ДатаОт > МассивДат[0] Тогда
		СообщениеОбОшибке = НСтр("ru = 'Производственный календарь «%1» не заполнен на дату %2.';
								|en = 'Business calendar ""%1"" is not filled in on date %2.'");
		СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(СообщениеОбОшибке, ГрафикРаботы, Формат(ДатаОт, "ДЛФ=D"));
		Если ВызыватьИсключение Тогда
			ВызватьИсключение СообщениеОбОшибке;
		Иначе
			ОбщегоНазначения.СообщитьПользователю(СообщениеОбОшибке);
			Возврат 0;
		КонецЕсли;
	ИначеЕсли МассивДат.Количество() = 0 Или ДатаДо > МассивДат[0] Тогда
		СообщениеОбОшибке = НСтр("ru = 'Производственный календарь «%1» не заполнен на дату %2.';
								|en = 'Business calendar ""%1"" is not filled in on date %2.'");
		СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(СообщениеОбОшибке, ГрафикРаботы, Формат(ДатаДо, "ДЛФ=D"));
		Если ВызыватьИсключение Тогда
			ВызватьИсключение СообщениеОбОшибке;
		Иначе
			ОбщегоНазначения.СообщитьПользователю(СообщениеОбОшибке);
			Возврат 0;
		КонецЕсли;
	КонецЕсли;
	
	Индекс = 0;
	ДнейОт = Неопределено;
	ДнейДо = Неопределено;
	Для Каждого День Из МассивДат Цикл
		Если ДнейОт <> Неопределено И ДнейДо <> Неопределено Тогда
			Прервать;
		КонецЕсли;
		Если ДатаОт >= День И ДнейОт = Неопределено Тогда
			ДнейОт = Индекс;
		КонецЕсли;
		Если ДатаДо >= День И ДнейДо = Неопределено  Тогда
			ДнейДо = Индекс;
		КонецЕсли;
		Индекс = Индекс + 1;
	КонецЦикла;
	
	Возврат ДнейОт - ДнейДо;
	
КонецФункции

Функция ДатаПлатежаПоФормуле(Параметры)
	
	ШаблонФормулы = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Параметры.Формула, Метаданные.Справочники.ФормулыРасчетаДатыПлатежа.Реквизиты.Формула.Имя);
	
	Формула = ШаблонФормулы;
	МассивЗначенийПараметров = Новый Массив;
	ДеревоОперандов = ЭтапыОплатыСервер.ДеревоОперандовДляФормулы();
	Для Каждого СтрокаОперанда Из ДеревоОперандов.Строки Цикл
		Если СтрокаОперанда.Идентификатор = "Сдвиг" Или СтрокаОперанда.Идентификатор = "СрокПереходаПраваСобственности" Тогда
			МассивЗначенийПараметров.Добавить(Параметры[СтрокаОперанда.Идентификатор]*86400);
		Иначе
			МассивЗначенийПараметров.Добавить(Параметры[СтрокаОперанда.Идентификатор]);
		КонецЕсли;
		ОперандСоСкобками = РаботаСФормуламиКлиентСервер.ПолучитьТекстОперандаДляВставки(СтрокаОперанда.Идентификатор);
		СтрокаЗаменыВШаблоне = "Параметры[" + Строка(МассивЗначенийПараметров.Количество()-1) + "]";
		Формула = СтрЗаменить(Формула, ОперандСоСкобками, СтрокаЗаменыВШаблоне);
	КонецЦикла;
	
	Попытка
		ДатаПлатежа = ОбщегоНазначения.ВычислитьВБезопасномРежиме(Формула,МассивЗначенийПараметров);
	Исключение
		
		СообщениеОбОшибке = СтрШаблон(НСтр("ru = 'Невозможно рассчитать дату платежа по формуле %1 по причине: %2';
											|en = 'Cannot calculate the payment date using the %1 formula. Reason: %2'"),ШаблонФормулы,ОписаниеОшибки());
		ВызватьИсключение СообщениеОбОшибке;
		
	КонецПопытки;
	
	Возврат ДатаПлатежа;
	
КонецФункции

// Дозаполняет даты в параметрах расчета сдвига/даты платежа если они не заполнены.
Процедура ДозаполнитьДатыЕслиНеЗаполнены(Параметры)
	
	Если НЕ ЗначениеЗаполнено(Параметры.ДатаСогласования)  И ЗначениеЗаполнено(Параметры.ДатаЗаказа) Тогда
		Параметры.ДатаСогласования = Параметры.ДатаЗаказа;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Параметры.ДатаОтгрузки) И ЗначениеЗаполнено(Параметры.ДатаЗаказа) Тогда
		Параметры.ДатаОтгрузки = Параметры.ДатаЗаказа;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Параметры.ДатаПереходаПраваСобственности) И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		Параметры.ДатаПереходаПраваСобственности = Параметры.ДатаОтгрузки + Параметры.СрокПереходаПраваСобственности*86400;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Параметры.ДатаЗаказа) И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		Параметры.ДатаЗаказа = Параметры.ДатаОтгрузки;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Параметры.ДатаПоступления) И ЗначениеЗаполнено(Параметры.ДатаОтгрузки) Тогда
		Параметры.ДатаПоступления = Параметры.ДатаОтгрузки;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
