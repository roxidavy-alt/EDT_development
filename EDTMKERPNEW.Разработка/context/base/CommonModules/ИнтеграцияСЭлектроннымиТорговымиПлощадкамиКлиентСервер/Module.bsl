////////////////////////////////////////////////////////////////////////////////
// Подсистема "Интеграция с электронными торговыми площадками".
// ОбщийМодуль.ИнтеграцияСЭлектроннымиТорговымиПлощадкамиКлиентСервер.
////////////////////////////////////////////////////////////////////////////////

#Область СлужебныйПрограммныйИнтерфейс

// Описание представлений событий сервиса Бидзаар.
// 
// Возвращаемое значение:
//  Соответствие из строка - Состояния запроса коммерческих предложений Бидзаар:
// * Ключ - Число - значение состояния
// * Значение - Строка - представление состояния
Функция СостоянияЗапросаКоммерческогоПредложенияБидзаар() Экспорт
	
	Соответствие = Новый Соответствие;
	
	Соответствие.Вставить(0, СостояниеЧерновик());
	Соответствие.Вставить(1, СостояниеОпубликован());
	Соответствие.Вставить(2, СостояниеИзменен());
	Соответствие.Вставить(3, СостояниеСборПредложений());
	Соответствие.Вставить(4, СостояниеЭтапСбораПредложенийЗавершен());
	Соответствие.Вставить(5, СостояниеСборПредложенийАвтопродление());
	Соответствие.Вставить(6, СостояниеСборПредложений());
	Соответствие.Вставить(7, СостояниеСборПредложенийЗавершен());
	Соответствие.Вставить(8, СостояниеПодведениеИтогов());
	Соответствие.Вставить(9, СостояниеСборПредложений());
	Соответствие.Вставить(10, СостояниеСборПредложений());
	Соответствие.Вставить(11, СостояниеСборПредложений());
	Соответствие.Вставить(12, СостояниеСборПредложений());
	Соответствие.Вставить(13, СостояниеСборПредложений());
	Соответствие.Вставить(14, СостояниеСборПредложений());
	Соответствие.Вставить(15, СостояниеСборПредложений());
	Соответствие.Вставить(16, СостояниеСборПредложений());
	Соответствие.Вставить(17, СостояниеСборПредложений());
	Соответствие.Вставить(18, СостояниеСборПредложений());
	Соответствие.Вставить(19, СостояниеСборПредложений());
	Соответствие.Вставить(20, СостояниеСборПредложений());
	Соответствие.Вставить(21, СостояниеСборПредложений());
	Соответствие.Вставить(22, СостояниеСборПредложений());
	Соответствие.Вставить(23, СостояниеСборПредложений());
	Соответствие.Вставить(24, СостояниеСборПредложений());
	Соответствие.Вставить(25, СостояниеСборПредложений());
	Соответствие.Вставить(26, СостояниеСборПредложений());
	Соответствие.Вставить(27, СостояниеПеренесенВАрхив());
	Соответствие.Вставить(28, СостояниеВозвращенИзАрхива());
	Соответствие.Вставить(29, СостояниеСборПредложений());
	Соответствие.Вставить(30, СостояниеСборПредложений());
	Соответствие.Вставить(32, СостояниеСборПредложений());
	Соответствие.Вставить(33, СостояниеСборПредложений());
	Соответствие.Вставить(34, СостояниеСборПредложений());
	Соответствие.Вставить(35, СостояниеСборПредложений());
	Соответствие.Вставить(36, СостояниеСборПредложений());
	Соответствие.Вставить(37, СостояниеСборПредложений());
	Соответствие.Вставить(38, СостояниеСборПредложений());
	Соответствие.Вставить(53, СостояниеСборПредложений());
	Соответствие.Вставить(54, СостояниеСборПредложений());
	
	Возврат Соответствие;
	
КонецФункции

// Функция - Преобразует коллекцию ключевых слов в строку
//
// Параметры:
//  КоллекцияКлючевыхСлов - СписокЗначений из Строка - список ключевых слов для преобразования
//  				   - Массив из Строка - массив ключевых слов для преобразования
//
// Возвращаемое значение:
//  Строка - строка ключевых слов, разделенных символом разделителя
//
Функция СтрокаКлючевыхСлов(КоллекцияКлючевыхСлов) Экспорт
	МассивКлючевыхСлов = Новый Массив;
	Если ТипЗнч(КоллекцияКлючевыхСлов) = Тип("СписокЗначений") Тогда
		МассивКлючевыхСлов = КоллекцияКлючевыхСлов.ВыгрузитьЗначения();
	ИначеЕсли ТипЗнч(КоллекцияКлючевыхСлов) = Тип("Массив") Тогда
		МассивКлючевыхСлов = КоллекцияКлючевыхСлов;
	КонецЕсли;
	Возврат СтрСоединить(МассивКлючевыхСлов, РазделительКлючевыхСлов());
КонецФункции

// Функция - Преобразует строку ключевых слов в список значений
//
// Параметры:
//  СтрокаКлючевыхСлов - Строка - строка с ключевыми словами, разделенными символом разделителя
//
// Возвращаемое значение:
//  СписокЗначений из Строка - список ключевых слов с типом значения Строка (длина до 50 символов)
//
Функция СписокКлючевыхСлов(СтрокаКлючевыхСлов = "") Экспорт
	Результат = Новый СписокЗначений();
	Результат.ТипЗначения = Новый ОписаниеТипов("Строка",, Новый КвалификаторыСтроки(50));
	Если Не ПустаяСтрока(СтрокаКлючевыхСлов) Тогда
		МассивКлючевыхСлов = СтрРазделить(СтрокаКлючевыхСлов,РазделительКлючевыхСлов(),Ложь);
		Результат.ЗагрузитьЗначения(МассивКлючевыхСлов);
	КонецЕсли;
	Возврат Результат;
КонецФункции

// Функция - Преобразует строку ключевых слов в массив
//
// Параметры:
//  СтрокаКлючевыхСлов - Строка - строка с ключевыми словами, разделенными символом разделителя
//
// Возвращаемое значение:
//  Массив из Строка - массив ключевых слов с типом значения Строка
//
Функция МассивКлючевыхСлов(СтрокаКлючевыхСлов) Экспорт
	Возврат СтрРазделить(СтрокаКлючевыхСлов,РазделительКлючевыхСлов(),Ложь);
КонецФункции

// Функция - Возвращает структуру с данными результата подбора ключевых слов
//
// Возвращаемое значение:
//	Структура - Результат подбора ключевых слов. Содержит свойства:
//		* ПодборВыполнялся - булево - указывает, что подбор выполнялся
//		* КлючевыеСлова - СписокЗначений из Строка - список подобранных ключевых слов
//		* ТекстОшибки - Строка - текст ошибки, которая возникла при подборе ключевых слов
//
Функция РезультатПодбораКлючевыхСлов() Экспорт
	
	Результат = Новый Структура;
	
	Результат.Вставить("ПодборВыполнялся",Ложь);
	Результат.Вставить("КлючевыеСлова",СписокКлючевыхСлов());
	Результат.Вставить("ТекстОшибки","");
	
	Возврат Результат;

КонецФункции

// Возвращает текст сообщения о том, что данные отчета еще обновляются.
// 
// Возвращаемое значение:
//  Строка - Сообщение данные еще обновляются
Функция СообщениеДанныеЕщеОбновляются() Экспорт
	Возврат НСтр("ru = 'Не завершен переход на новую версию приложения. Если форма долгое время недоступна, обратитесь к администратору.';
				|en = 'Не завершен переход на новую версию приложения. Если форма долгое время недоступна, обратитесь к администратору.'");
КонецФункции

#Область Конструкторы

// Новый результат метода.
// 
// Возвращаемое значение:
//  Структура - Новый результат метода:
// * КодСостояния - Число - Код ответа. https://ru.wikipedia.org/wiki/Список_кодов_состояния_HTTP.
// * ТекстОшибки - Строка - Описание ошибки, возникшей в ходе выполнения метода
Функция НовыйРезультатМетода() Экспорт
	
	Результат = Новый Структура;
	
	Результат.Вставить("КодСостояния", 0);
	Результат.Вставить("ТекстОшибки", "");
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область Словарь

Функция СостояниеНеопределено() Экспорт
	
	Возврат Нстр("ru = 'Неопределено';
				|en = 'Undefined'");
	
КонецФункции

Функция СостояниеЧерновик() Экспорт
	
	Возврат Нстр("ru = 'Черновик';
				|en = 'Draft'");
	
КонецФункции

Функция СостояниеОпубликован() Экспорт
	
	Возврат Нстр("ru = 'Опубликован';
				|en = 'Published'");
	
КонецФункции

Функция СостояниеИзменен()
	
	Возврат Нстр("ru = 'Изменен в сервисе';
				|en = 'Changed in the service'");
	
КонецФункции

Функция СостояниеСборПредложений() Экспорт
	
	Возврат Нстр("ru = 'Сбор предложений';
				|en = 'Quotation collection'");
	
КонецФункции

Функция СостояниеСборПредложенийАвтопродление() Экспорт
	
	Возврат Нстр("ru = 'Сбор предложений (Автопродление)';
				|en = 'Quotation collection (Auto renewal)'");
	
КонецФункции

Функция СостояниеЭтапСбораПредложенийЗавершен()
	
	Возврат Нстр("ru = 'Этап сбора предложений завершен';
				|en = 'Quotation collection stage is completed'");
	
КонецФункции

Функция СостояниеСборПредложенийЗавершен() Экспорт
	
	Возврат Нстр("ru = 'Сбор предложений завершен';
				|en = 'Quotations are collected'");
	
КонецФункции

// Устарела: больше не используется.
// Возвращает строковое представление статуса завершения сбора предложений без выбора победителя.
//
// Возвращаемое значение:
//   Строка - текстовое описание статуса
Функция СостояниеСборПредложенийЗавершенБезПобедителя() Экспорт
	
	Возврат Нстр("ru = 'Сбор предложений завершен без выбора победителя';
				|en = 'Quotations are collected without a winner'");
	
КонецФункции

Функция СостояниеПодведениеИтогов() Экспорт
	
	Возврат Нстр("ru = 'Подведение итогов';
				|en = 'Summary'");
	
КонецФункции

Функция СостояниеПеренесенВАрхив() Экспорт
	
	Возврат Нстр("ru = 'Перенесен в архив';
				|en = 'Moved to archive'");
	
КонецФункции

Функция СостояниеВозвращенИзАрхива() Экспорт
	
	Возврат Нстр("ru = 'Возвращен из архива';
				|en = 'Returned from archive'");
	
КонецФункции

Функция ИмяСервисаБидзаар() Экспорт
	
	Возврат "Бидзаар";
	
КонецФункции

// Раздел учета продажи.
// 
// Возвращаемое значение:
//  Строка - Раздел учета поставщики
Функция РазделУчетаПоставщики() Экспорт
	
	Возврат "Поставщики";
	
КонецФункции

// Раздел учета закупки.
// 
// Возвращаемое значение:
//  Строка - Раздел учета покупатели
Функция РазделУчетаПокупатели() Экспорт
	
	Возврат "Покупатели";
	
КонецФункции

// Код ответа ОК.
// 
// Возвращаемое значение:
//  Число - Код ответа ОК
Функция КодОтветаОК() Экспорт
	
	Возврат 200;
	
КонецФункции

// Возвращает имена серверов торговой площадки Бидзаар
// 
// Параметры:
//  Рабочие - Булево - Рабочие
//  Тестовые - Булево - Тестовые
// 
// Возвращаемое значение:
//  СписокЗначений из Строка - Имена серверов бидзаар
Функция ИменаСерверовБидзаар(Рабочие = Истина, Тестовые = Истина) Экспорт
	
	Результат = Новый СписокЗначений;
	
	Если Рабочие Тогда
		Результат.Добавить("bidzaar.com", НСтр("ru = 'Рабочий';
												|en = 'Рабочий'"), Истина);	// Отметка рабочего сервера
	КонецЕсли;
	Если Тестовые Тогда
		Результат.Добавить("phoenix.bidzaar.com", НСтр("ru = 'Тестовый (phoenix)';
														|en = 'Тестовый (phoenix)'"), Ложь);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Имя метода общего модуля получения данных коммерческих предложений с торговой площадки
// 
// Возвращаемое значение:
//  Строка - имя метода
Функция МетодПолучениеДанныхКоммерческихПредложенийПоЗапросуБидзаар() Экспорт
	Возврат "ИнтеграцияСЭлектроннымиТорговымиПлощадками.ПолучениеДанныхКоммерческихПредложенийПоЗапросуБидзаар";
КонецФункции

// Имя метода получения данных участников закупочной процедуры на площадке Бидзаар.
// 
// Возвращаемое значение:
//  Строка - Имя метода
Функция МетодПолучениеДанныхУчастниковПоЗапросуБидзаар() Экспорт
	Возврат "ИнтеграцияСЭлектроннымиТорговымиПлощадками.ПолучениеДанныхУчастниковПоЗапросуБидзаар";
КонецФункции

#КонецОбласти // Словарь

#КонецОбласти // СлужебныйПрограммныйИнтерфейс

#Область СлужебныеПроцедурыИФункции

// Функция - Возвращает разделитель для ключевых слов
//
// Возвращаемое значение:
//  Строка - разделитель ключевых слов
//
Функция РазделительКлючевыхСлов()
	Возврат Символы.ПС;
КонецФункции

#КонецОбласти // СлужебныеПроцедурыИФункции