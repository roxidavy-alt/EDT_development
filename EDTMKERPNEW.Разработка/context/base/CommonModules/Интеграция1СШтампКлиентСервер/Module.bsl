
#Область ПрограммныйИнтерфейс

// Возвращает конструктор параметров установки картинок штампов в документ.
//
// Возвращаемое значение:
//  Структура:
//    * ПараметрыДокумента  - см. НовыеПараметрыФайла.
//    * ПараметрыШтампов    - Массив из см. НовыеПараметрыШтампа.
//    * СтратегииРазмещения - Массив из см. НоваяСтратегияРазмещенияШтампов.
//    * ПараметрыРезультата - см. НовыеПараметрыПолученияРезультата.
//
Функция НовыеПараметрыУстановкиШтамповВДокумент() Экспорт
	
	ПараметрыДобавления = Новый Структура;
	ПараметрыДобавления.Вставить("ПараметрыДокумента" , НовыеПараметрыФайла());
	ПараметрыДобавления.Вставить("ПараметрыШтампов"   , Новый Массив);
	ПараметрыДобавления.Вставить("СтратегииРазмещения", Новый Массив);
	ПараметрыДобавления.Вставить("ПараметрыРезультата", НовыеПараметрыПолученияРезультата());
	
	Возврат ПараметрыДобавления;
	
КонецФункции

// Возвращает конструктор параметров устанавливаемой картинки штампа.
//
// Возвращаемое значение:
//  Структура:
//    * ПараметрыФайла     - см. НовыеПараметрыФайла.
//    * Настройки          - см. НовыеНастройкиШтампа.
//    * ПараметрыУстановки - см. НовыеПараметрыУстановкиШтампа.
//
Функция НовыеПараметрыШтампа() Экспорт
	
	ПараметрыШтампа = Новый Структура;
	
	ПараметрыШтампа.Вставить("ПараметрыФайла"    , НовыеПараметрыФайла());
	ПараметрыШтампа.Вставить("Настройки"         , НовыеНастройкиШтампа());
	ПараметрыШтампа.Вставить("ПараметрыУстановки", НовыеПараметрыУстановкиШтампа());
	
	Возврат ПараметрыШтампа;
	
КонецФункции

// Возвращает конструктор параметров устанавливаемой картинки штампа.
//
// Возвращаемое значение:
//  Структура:
//    * ПоПозиционированию - Структура - Параметры установки штампов по позиционированию:
//       ** Позиции - Массив из см. НовыеПараметрыУстановкиШтампаПоПозиции.
//    * ПоТегам - Структура - Параметры установки штампов по тегам:
//       ** Теги - Массив из см. НовыеПараметрыТега.
//       ** АльтернативныйСпособУстановки - см. НовыеПараметрыУстановкиШтампа()
//                                        - Неопределено - Значение не инициализировано.
//            Параметры установки штампов, если теги в документе не найдены. Необязательное свойство.
//
Функция НовыеПараметрыУстановкиШтампа() Экспорт
	
	ПараметрыУстановкиШтампа = Новый Структура;
	
	ПараметрыУстановкиШтампаПоТегам = Новый Структура;
	ПараметрыУстановкиШтампаПоТегам.Вставить("Теги", Новый Массив);
	ПараметрыУстановкиШтампаПоТегам.Вставить("АльтернативныйСпособУстановки", Неопределено);
	
	ПараметрыУстановкиШтампаПоПозиции = Новый Структура;
	ПараметрыУстановкиШтампаПоПозиции.Вставить("Позиции", Новый Массив);
	
	ПараметрыУстановкиШтампа.Вставить("ПоПозиционированию", ПараметрыУстановкиШтампаПоПозиции);
	ПараметрыУстановкиШтампа.Вставить("ПоТегам"           , ПараметрыУстановкиШтампаПоТегам);
	
	Возврат ПараметрыУстановкиШтампа;
	
КонецФункции

// Возвращает конструктор параметров установки картинки штампа по позиционированию.
//
// Возвращаемое значение:
//  Структура:
//    * НаСтранице  - см. НовыеПараметрыРасположенияНаСтранице.
//    * НаСтраницах - см. НовыеПараметрыРасположенияНаСтраницах.
//
Функция НовыеПараметрыУстановкиШтампаПоПозиции() Экспорт
	
	ПараметрыШтампа = Новый Структура;
	
	ПараметрыШтампа.Вставить("НаСтранице" , НовыеПараметрыРасположенияНаСтранице());
	ПараметрыШтампа.Вставить("НаСтраницах", НовыеПараметрыРасположенияНаСтраницах());
	
	Возврат ПараметрыШтампа;
	
КонецФункции

// Возвращает конструктор параметров тега.
//
// Возвращаемое значение:
//  Структура:
//   * ЗначениеТега          - Строка - Текст, по позиции которого будет вставлена картинка штампа.
//   * ЗамещатьЗначениеТега  - Булево - Признак замещения тега картинкой штампа.
//   * СпособУстановкиШтампа - Строка - Способ установки картинки штампа в текстовое содержимое документа.
//       Значение по умолчанию: "НадТекстом".
//
Функция НовыеПараметрыТега() Экспорт
	
	ПараметрыШтампов = Новый Структура;
	ПараметрыШтампов.Вставить("ЗначениеТега"         , "");
	ПараметрыШтампов.Вставить("ЗамещатьЗначениеТега" , Ложь);
	ПараметрыШтампов.Вставить("СпособУстановкиШтампа", СпособУстановкиШтампаНадТекстом());
	
	Возврат ПараметрыШтампов;
	
КонецФункции

// Возвращает конструктор параметров множественного размещения картинок штампов по шаблонному значению позиционирования.
//
// Возвращаемое значение:
//   Размещение - см. НовоеРасположениеДляМножественногоРазмещения.
//   Статегия   - см. НоваяСтратегияРазмещения.
//
Функция НоваяСтратегияРазмещенияШтампов() Экспорт
	
	СтратегияРазмещения = Новый Структура;
	СтратегияРазмещения.Вставить("Размещение", НовоеРасположениеДляМножественногоРазмещения());
	СтратегияРазмещения.Вставить("Статегия"  , НоваяСтратегияРазмещения());
	
	Возврат СтратегияРазмещения;
	
КонецФункции

// Возвращает конструктор параметров файла.
//
// Возвращаемое значение:
//  Структура: 
//    * ДвоичныеДанные - ДвоичныеДанные - Двоичные данные файла.
//                     - Неопределено   - Значение не инициализировано.
//    * Расширение     - Строка         - Значение расширения файла.
//
Функция НовыеПараметрыФайла() Экспорт
	
	ПараметрыФайла = Новый Структура;
	
	ПараметрыФайла.Вставить("ДвоичныеДанные", Неопределено);
	ПараметрыФайла.Вставить("Расширение"    , "");
	
	Возврат ПараметрыФайла;
	
КонецФункции

#Область ЗначенияКонстантПоУмолчанию

// Возвращает значение отступа в миллиметрах по умолчанию.
//
// Возвращаемое значение:
//   Число - значение отступа в мм. Значение по умолчанию - 10 мм.
//
Функция ЗначениеОтступаОтКраевСтраницыПоУмолчанию() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.ЗначениеОтступаОтКраевСтраницыПоУмолчанию();
	
КонецФункции

// Возвращает коэффициент прозрачности картинки штампа по умолчанию.
// Градация следующая: при значении 0 - 100% прозрачность, при значении 255 - 0% прозрачность.
//
// Возвращаемое значение:
//   Число - значение коэффициента прозрачности от 0 до 255. Значение по умолчанию - 128.
//
Функция ЗначениеКоэффициентаПрозрачностиШтампаПоУмолчанию() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.ЗначениеКоэффициентаПрозрачностиШтампаПоУмолчанию();
	
КонецФункции

// Возвращает коэффициент прозрачности картинки штампа, если прозрачность не используется.
// Градация следующая: при значении 0 - 100% прозрачность, при значении 255 - 0% прозрачность.
//
// Возвращаемое значение:
//   Число - значение коэффициента 255.
//
Функция ЗначениеКоэффициентаПрозрачностиШтампаНепрозрачный() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.ЗначениеКоэффициентаПрозрачностиШтампаНепрозрачный();
	
КонецФункции

// Возвращает значение прозрачности картинки штампа, когда прозрачность используется.
//
// Возвращаемое значение:
//   Строка - значение прозрачности "Прозрачный".
//
Функция ЗначениеПрозрачностиПрозрачный() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.ЗначениеПрозрачностиПрозрачный();
	
КонецФункции

#КонецОбласти

#Область Словарь

// Возвращает способ размещения штампа в верхнем левом углу.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРазмещенияВверхуСлева() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРазмещенияВверхуСлева();
	
КонецФункции

// Возвращает способ размещения штампа в верхнем правом углу.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРазмещенияВверхуСправа() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРазмещенияВверхуСправа();
	
КонецФункции

// Возвращает способ размещения штампа вверху по центру страницы.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРазмещенияВверхуПоЦентру() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРазмещенияВверхуПоЦентру();
	
КонецФункции

// Возвращает способ размещения штампа в нижнем левом углу.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРазмещенияВнизуСлева() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРазмещенияВнизуСлева();
	
КонецФункции

// Возвращает способ размещения штампа в нижнем правом углу.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРазмещенияВнизуСправа() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРазмещенияВнизуСправа();
	
КонецФункции

// Возвращает способ размещения штампа внизу по центру страницы.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРазмещенияВнизуПоЦентру() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРазмещенияВнизуПоЦентру();
	
КонецФункции

// Возвращает способ размещения штампа произвольно по координатам.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРазмещенияПоКоординатам() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРазмещенияПоКоординатам();
	
КонецФункции

// Возвращает способ размещения штампа по тегам.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРазмещенияПоТегам() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРазмещенияПоТегам();
	
КонецФункции

// Возвращает способ расположения штампа на каждой странице.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРасположенияНаКаждойСтранице() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРасположенияНаКаждойСтранице();
	
КонецФункции

// Возвращает способ расположения штампа на первой странице.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРасположенияНаПервойСтранице() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРасположенияНаПервойСтранице();
	
КонецФункции

// Возвращает способ расположения штампа на последней странице.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРасположенияНаПоследнейСтранице() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРасположенияНаПоследнейСтранице();
	
КонецФункции

// Возвращает способ расположения штампа на каждой странице кроме последней.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРасположенияНаКаждойСтраницеКромеПоследней() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРасположенияНаКаждойСтраницеКромеПоследней();
	
КонецФункции

// Возвращает способ расположения штампа на конкретной странице.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРасположенияНаКонкретнойСтранице() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРасположенияНаКонкретнойСтранице();
	
КонецФункции

// Возвращает способ расположения штампа на новой созданной странице в конце документа.
//
// Возвращаемое значение:
//   Строка - Значение расположения.
//
Функция СпособРасположенияНаНовойСтранице() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособРасположенияНаНовойСтранице();
	
КонецФункции

// Возвращает способ установки штампа - раздвигая текст со всех сторон.
//
// Возвращаемое значение:
//   Строка - Значение способа установки.
//
Функция СпособУстановкиШтампаРаздвигаяТекст() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособУстановкиШтампаРаздвигаяТекст();
	
КонецФункции

// Возвращает способ установки штампа - над текстом.
//
// Возвращаемое значение:
//   Строка - Значение способа установки.
//
Функция СпособУстановкиШтампаНадТекстом() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СпособУстановкиШтампаНадТекстом();
	
КонецФункции

// Возвращает значение стратегии размещения горизонтально.
//
// Возвращаемое значение:
//   Строка - Значение стратегии размещения.
//
Функция СтратегияРазмещенияГоризонтально() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СтратегияРазмещенияГоризонтально();
	
КонецФункции

// Возвращает значение стратегии размещения вертикально.
//
// Возвращаемое значение:
//   Строка - Значение стратегии размещения.
//
Функция СтратегияРазмещенияВертикально() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СтратегияРазмещенияВертикально();
	
КонецФункции

// Возвращает значение горизонтального смещения вправо.
//
// Возвращаемое значение:
//   Строка - Значение направления размещения.
//
Функция СмещениеПоГоризонталиВправо() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СмещениеПоГоризонталиВправо();
	
КонецФункции

// Возвращает значение горизонтального смещения влево.
//
// Возвращаемое значение:
//   Строка - Значение направления размещения.
//
Функция СмещениеПоГоризонталиВлево() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СмещениеПоГоризонталиВлево();
	
КонецФункции

// Возвращает значение горизонтального смещения авто.
//
// Возвращаемое значение:
//   Строка - Значение направления размещения.
//
Функция СмещениеПоГоризонталиАвто() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.СмещениеПоГоризонталиАвто();
	
КонецФункции

#КонецОбласти

// Возвращает массив допустимых расширений файлов
//
// Возвращаемое значение:
//  Массив Из Строка - Массив строк расширений.
//
Функция ДопустимыеРасширенияФайловШтампов() Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.ДопустимыеРасширенияФайловШтампов();
	
КонецФункции

// См. Интеграция1СШтампСлужебныйКлиентСервер.РасширениеОсновногоФайлаПоддерживается
//
Функция РасширениеОсновногоФайлаПоддерживается(Расширение) Экспорт
	
	Возврат Интеграция1СШтампСлужебныйКлиентСервер.РасширениеОсновногоФайлаПоддерживается(Расширение);
	
КонецФункции

// Возвращает соответствие типов файлов документа PDF его строковым представлениям.
//
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//    * Ключ     - Строка               - Строковое представление формата PDF.
//    * Значение - ТипФайлаДокументаPDF - Системное перечисление типов файлов PDF.
//
Функция ТипыФайловPDFДокумента() Экспорт
	
	ТипыФайлов = Новый Соответствие;
	
	#Если Не МобильныйКлиент Тогда
	
		ТипыФайлов.Вставить(Интеграция1СШтампСлужебныйКлиентСервер.РасширениеФайлаPDF(), ТипФайлаДокументаPDF.PDF);
		ТипыФайлов.Вставить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_1(), ТипФайлаДокументаPDF.PDF_A_1);
		ТипыФайлов.Вставить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_2(), ТипФайлаДокументаPDF.PDF_A_2);
		ТипыФайлов.Вставить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_3(), ТипФайлаДокументаPDF.PDF_A_3);
		
	#КонецЕсли
	
	Возврат ТипыФайлов;
	
КонецФункции

// Проверяет, относится ли формат к семейству форматов PDF.
//
// Параметры:
//  ФорматРезультата - Строка - Расширение файла получаемого документа.
//
// Возвращаемое значение:
//   Булево - признак отнесения к семейству форматов PDF.
//
Функция ЭтоПодмножествоФорматаPDF(Знач ФорматРезультата) Экспорт
	
	МассивВерсийФормата = Новый Массив;
	
	МассивВерсийФормата.Добавить(Интеграция1СШтампСлужебныйКлиентСервер.РасширениеФайлаPDF());
	МассивВерсийФормата.Добавить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_1());
	МассивВерсийФормата.Добавить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_2());
	МассивВерсийФормата.Добавить(Интеграция1СШтампСлужебныйКлиентСервер.ВерсияФайлаPDF_A_3());
	
	Возврат МассивВерсийФормата.Найти(ФорматРезультата) <> Неопределено;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Возвращает конструктор параметров новой стратегии размещения картинок штампов.
//
// Возвращаемое значение:
//  Структура:
//    * Стратегия - Строка - Стратегия размещения картинок штампов.
//       Значение по умолчанию: Горизонтально.
//    * СмещениеПоГоризонтали - Строка - Смещение по горизонтали.
//       Значение по умолчанию: Авто.
//
Функция НоваяСтратегияРазмещения()
	
	СтратегияРазмещения = Новый Структура;
	
	СтратегияРазмещения.Вставить("Направление"          , СтратегияРазмещенияГоризонтально());
	СтратегияРазмещения.Вставить("СмещениеПоГоризонтали", СмещениеПоГоризонталиАвто());
	
	
	Возврат СтратегияРазмещения;
	
КонецФункции

// Возвращает конструктор параметров исходного размещения картинок штампов при множественной установке.
//
// Возвращаемое значение:
//  Структура:
//    * НаСтранице  - Строка - Способ размещения на странице документа.
//          Значение по умолчанию: ВверхуСлева.
//    * НаСтраницах - Строка - Способ расположения на страницах документа.
//          Значение по умолчанию: НаНовойСтранице.
//
Функция НовоеРасположениеДляМножественногоРазмещения()
	
	ПравилаРасположения = Новый Структура;
	
	ПравилаРасположения.Вставить("СпособРасположения" , СпособРасположенияНаНовойСтранице());
	ПравилаРасположения.Вставить("НомерСтраницы"      , 1);
	
	ПараметрыМножественногоРазмещения = Новый Структура;
	ПараметрыМножественногоРазмещения.Вставить("НаСтранице" , СпособРазмещенияВверхуСлева());
	ПараметрыМножественногоРазмещения.Вставить("НаСтраницах", ПравилаРасположения);
	
	Возврат ПараметрыМножественногоРазмещения;

КонецФункции

// Возвращает конструктор параметров получения результата.
//
// Возвращаемое значение:
//  Структура:
//    * ФорматРезультата - Строка - Расширение файла, в котором необходимо получить результат.
//          Значение по умолчанию: "pdf".
//       Список форматов см. Интеграция1СШтампСлужебныйКлиентСервер.ПоддерживаемыеРасширенияФайловДляШтампирования
//
Функция НовыеПараметрыПолученияРезультата()
	
	ПараметрыРезультата = Новый Структура;
	ПараметрыРезультата.Вставить("ФорматРезультата", Интеграция1СШтампСлужебныйКлиентСервер.РасширениеФайлаPDF());
	
	Возврат ПараметрыРезультата;
	
КонецФункции

// Возвращает конструктор параметров размещения картинки штампа на странице документа.
//
// Возвращаемое значение:
//  Структура:
//    * Координаты - Структура - Данные координат размещения в мм:
//      ** Верх - Число - Значение отступа в мм от верхней границы страницы документа.
//      ** Лево - Число - Значение отступа в мм от левой границы страницы документа.
//    * Расположение - Строка - Значение расположения картинки штампа на странице документа.
//          Значение по умолчанию: ВнизуПоЦентру.
//    * ОтступОтГраниц - Число - Отступ от границ страницы документа в мм при установке картинки штампа.
//        Значение по умолчанию: 10.
//
Функция НовыеПараметрыРасположенияНаСтранице()
	
	ПараметрыРасположения = Новый Структура;
	ПараметрыРасположения.Вставить("Координаты"    , Новый Структура("Верх, Лево",0,0));
	ПараметрыРасположения.Вставить("Расположение"  , СпособРазмещенияВнизуПоЦентру());
	ПараметрыРасположения.Вставить("ОтступОтГраниц", 10);
	
	Возврат ПараметрыРасположения;
	
КонецФункции

// Возвращает конструктор правил расположения картинки штампа на страницах документа.
//
// Возвращаемое значение:
//  Структура:
//    * Расположение - Строка - Значение способа расположения картинки штампа на страницах документа.
//        Значение по умолчанию: НаКаждойСтранице.
//    * НомерСтраницы - Число - Номер страницы документа для расположения картинки штампа.
//        Используется для правила размещения: НаКонкретнойСтранице.
//        Значение по умолчанию: 1.
//
Функция НовыеПараметрыРасположенияНаСтраницах()
	
	ПравилаРасположения = Новый Структура;
	
	ПравилаРасположения.Вставить("Расположение" , СпособРасположенияНаКаждойСтранице());
	ПравилаРасположения.Вставить("НомерСтраницы", 1);
	
	Возврат ПравилаРасположения;
	
КонецФункции

// Возвращает конструктор настройки картинки штампа.
//
// Возвращаемое значение:
//  Структура:
//   * УровеньПрозрачности         - Число - Значение уровня прозрачности от 0 до 255, где 0 - 100% значение прозрачности.
//   * КоэффициентИзмененияРазмера - Число - Коэффициент изменения размера картинки штампа.
//
Функция НовыеНастройкиШтампа()
	
	НастройкиШтампа = Новый Структура;
	НастройкиШтампа.Вставить("УровеньПрозрачности"        , 255);
	НастройкиШтампа.Вставить("КоэффициентИзмененияРазмера", 1);
	
	Возврат НастройкиШтампа;
	
КонецФункции

#КонецОбласти
