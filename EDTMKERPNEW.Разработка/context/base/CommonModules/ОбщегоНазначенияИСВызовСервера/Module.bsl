#Область ПрограммныйИнтерфейс

Функция ЭтоРасширеннаяВерсияГосИС(Подсистема = Неопределено) Экспорт
	
	Возврат ОбщегоНазначенияИС.ЭтоРасширеннаяВерсияГосИС(Подсистема);
	
КонецФункции

// Возвращает признак ведения учета маркируемой продукции.
// 
// Возвращаемое значение:
// 	Булево - Истина, если учет маркируемой продукции используется.
//
Функция ИспользуетсяМаркируемаяПродукция() Экспорт
	
	Возврат ОбщегоНазначенияИСМП.ИспользуетсяМаркируемаяПродукция();
	
КонецФункции

Функция ТекстJSONВОбъект(ТекстJSON, ПреобразовыватьВСоответствие) Экспорт
	Возврат ОбщегоНазначенияИСКлиентСервер.ТекстJSONВОбъект(ТекстJSON, ПреобразовыватьВСоответствие);	
КонецФункции

Функция ОбъектВТекстJSON(Структура, УдалитьПробелыИПереносыСтрок) Экспорт
	Возврат ОбщегоНазначенияИСКлиентСервер.ОбъектВТекстJSON(Структура, УдалитьПробелыИПереносыСтрок);
КонецФункции

// Проверка, является ли партнер индивидуальным предпринимателем.
// 
// Параметры:
//  Контрагент - СправочникСсылка.Контрагенты, СправочникСсылка.Организации - Партнер
// Возвращаемое значение:
//  Булево - Истина, если это индивидуальный предприниматель
Функция ПартнерЭтоИндивидуальныйПредприниматель(Контрагент) Экспорт
	
	ЭтоИндивидуальныйПредприниматель = Ложь;
	ОбщегоНазначенияИСПереопределяемый.ПартнерЭтоИндивидуальныйПредприниматель(Контрагент, ЭтоИндивидуальныйПредприниматель);
	
	Возврат ЭтоИндивидуальныйПредприниматель;
	
КонецФункции

// Проверка, является ли партнер индивидуальным предпринимателем.
// 
// Параметры:
//  Контрагент - СправочникСсылка.Контрагенты, СправочникСсылка.Организации - Партнер
// Возвращаемое значение:
//  Булево - Истина, если это индивидуальный предприниматель
Функция ПартнерОрганизацияЭтоИндивидуальныйПредприниматель(Контрагент) Экспорт
	
	ЭтоИндивидуальныйПредприниматель = Ложь;
	ОбщегоНазначенияИСПереопределяемый.ПартнерОрганизацияЭтоИндивидуальныйПредприниматель(Контрагент, ЭтоИндивидуальныйПредприниматель);
	
	Возврат ЭтоИндивидуальныйПредприниматель;
	
КонецФункции

// Дата из строки.
// 
// Параметры:
//  Значение - Строка
//  Делитель - Число - Делитель
//  ПриводитьКМестномуВремени - Булево - Приводить к местному времени
// 
// Возвращаемое значение:
//  Дата - Дата из строки
Функция ДатаИзСтроки(Значение, Делитель = 1000, ПриводитьКМестномуВремени = Истина) Экспорт
	Возврат ОбщегоНазначенияИС.ДатаИзСтроки(Значение, Делитель, ПриводитьКМестномуВремени);
КонецФункции

#Область ОбработкаОшибок

// Выполняет запись ошибки в журнал регистрации, добавляя имя события ГосИС если оно не задано.
//
// Параметры:
//  ТекстОшибки      - Строка           - текст ошибки.
//  ИмяСобытия       - Строка           - имя события.
//  ОбъектМетаданных - ОбъектМетаданных - объект метаданных с ошибкой.
//  Данные           - Произвольный     - объект данных с ошибкой.
//
Процедура ЗаписатьОшибкуВЖурналРегистрации(ТекстОшибки, ИмяСобытия = Неопределено, ОбъектМетаданных = Неопределено, Данные = Неопределено) Экспорт
	
	Если ИмяСобытия = Неопределено Тогда
		ИмяСобытия = НСтр("ru = 'ГосИС';
							|en = 'ГосИС'", ОбщегоНазначения.КодОсновногоЯзыка());
	КонецЕсли;
	
	ЗаписьЖурналаРегистрации(
		ИмяСобытия,
		УровеньЖурналаРегистрации.Ошибка,
		ОбъектМетаданных,
		Данные,
		ТекстОшибки);
	
КонецПроцедуры

#КонецОбласти

#Область ЧастичноеВыбытие

// Возвращает признак возможности для вида продукции участвовать в частичном выбытии.
// 
// Параметры:
//  ВидПродукции - СправочникСсылка.ВидыПродукцииИС                - вид маркируемой продукции.
//  ОперацияИС   - ПеречислениеСсылка.ВидыОперацийИСМП, ПеречислениеСсылка.ВидыДокументовЕГАИС, Неопределено - вид операции ИСМП.
// Возвращаемое значение:
//  Булево - Вид продукци может выбывать частично.
Функция ПоддерживаетсяЧастичноеВыбытие(ВидПродукции, ОперацияИС = Неопределено) Экспорт
	
	Если ОбщегоНазначенияИСПовтИсп.ЭтоМаркируемаяПродукцияЕГАИС(ВидПродукции) Тогда
		Если Не ОбщегоНазначения.ПодсистемаСуществует("ГосИС.ЕГАИС") Тогда
			Возврат Ложь;
		КонецЕсли;
		Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ПоддерживаетсяЧастичноеВыбытие(ВидПродукции, ОперацияИС);
	Иначе
		Если Не ОбщегоНазначенияИСПовтИсп.ЭтоПродукцияИСМП(ВидПродукции, Истина)
			Или Не ОбщегоНазначения.ПодсистемаСуществует("ГосИС.ИСМП") Тогда
			Возврат Ложь;
		КонецЕсли;
		Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ПоддерживаетсяЧастичноеВыбытие(ВидПродукции, ОперацияИС);
	КонецЕсли;
	
КонецФункции

Функция ВидыПродукцииИспользующиеВскрытыеПотребительскихУпаковок(ПродукцияИСМП = Истина, ПродукцияЕГАИС = Истина, ТолькоНастроеннаяНоменклатура = Истина) Экспорт
	
	ВозвращаемоеЗначение = Новый Массив();
	
	Если ПродукцияИСМП И ОбщегоНазначенияИС.ПодсистемаСуществует("ИСМП") Тогда
		
		МодульОбщегоНазначенияИСМПКлиентСерверПовтИсп = ОбщегоНазначенияИСПовтИсп.ОбщийМодуль("ОбщегоНазначенияИСМПКлиентСерверПовтИсп");
		Для Каждого ВидПродукции Из МодульОбщегоНазначенияИСМПКлиентСерверПовтИсп.УчитываемыеВидыМаркируемойПродукции() Цикл
			Если ОбщегоНазначенияИСКлиентСерверПовтИсп.ПоддерживаетсяЧастичноеВыбытие(ВидПродукции)
				И (Не ТолькоНастроеннаяНоменклатура Или ОбщегоНазначенияИС.ИспользуетсяЧастичноеВыбытие(ВидПродукции)) Тогда
				ВозвращаемоеЗначение.Добавить(ВидПродукции);
			КонецЕсли;
		КонецЦикла;
	
	КонецЕсли;
	
	ИмяФО = "ВестиСведенияДляДекларацийПоАлкогольнойПродукции";
	Если ПродукцияЕГАИС
		И ОбщегоНазначенияИС.ПодсистемаСуществует("ЕГАИС")
		И ПолучитьФункциональнуюОпцию(ИмяФО) Тогда
		
		ВидыПродукцииЕГАИС = КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииЕГАИСМаркируемые();
		Для Каждого ВидПродукцииАлкоголь Из ВидыПродукцииЕГАИС Цикл
			Если ОбщегоНазначенияИСКлиентСерверПовтИсп.ПоддерживаетсяЧастичноеВыбытие(ВидПродукцииАлкоголь)
				И(Не ТолькоНастроеннаяНоменклатура Или ОбщегоНазначенияИС.ИспользуетсяЧастичноеВыбытие(ВидПродукцииАлкоголь)) Тогда
					ВозвращаемоеЗначение.Добавить(ВидПродукцииАлкоголь);
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат ВозвращаемоеЗначение;
	
КонецФункции

#КонецОбласти

#Область КонструкторВидовПродукции

// Возвращает текущие элементы конструктора видов продукции ИС.
// 
// Возвращаемое значение:
//  см. Справочники.ЭлементыКонструктораВидовПродукцииИС.ЭлементыКонструктора
Функция ЭлементыКонструктора() Экспорт
	
	Возврат Справочники.ЭлементыКонструктораВидовПродукцииИС.ЭлементыКонструктора();
	
КонецФункции

// Возвращает значения элемента конструктора.
// 
// Параметры:
//  ЭлементКонструктора - СправочникСсылка.ЭлементыКонструктораВидовПродукцииИС - элемент конструктора, для которого требуется получить данные.
//  ВидыПродукции - Массив из СправочникСсылка.ВидыПродукцииИС - виды продукции, для которых требуется получить значение.
//  Значение - ОпределяемыйТип.ЗначениеЭлементаКонструктораВидовПродукцииИС - значение, для которого требуется получить атрибуты.
//             Не требуется, если элемент конструктора не Параметризуется.
// 
// Возвращаемое значение:
//  Структура:
//   * Значение             - Булево - Истина - задано "предопределенное" значение, Ложь - "предопределенное" значение задано не было.
//   * ЗначениеПользователя - Булево, Неопределено - Истина - пользователь установил значение, Ложь - пользователь удалил значение,
//                            Неопределено - пользователь не изменял значение.
//
Функция ЗначенияЭлементаКонструктора(ЭлементКонструктора, ВидыПродукции, Значение = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Значение", Ложь);
	Результат.Вставить("ЗначениеПользователя", Неопределено);
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ЭлементКонструктора", ЭлементКонструктора);
	Запрос.УстановитьПараметр("ВидыПродукции",       ВидыПродукции);
	Запрос.УстановитьПараметр("Значение",            Значение);
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	ИСТИНА
	|ИЗ
	|	РегистрСведений.ЗначенияЭлементовКонструктораВидовПродукцииИС КАК Значения
	|ГДЕ
	|	Значения.ЭлементКонструктора = &ЭлементКонструктора
	|	И Значения.ВидПродукции В (&ВидыПродукции)
	|	И Значения.Значение = &Значение
	|;
	|ВЫБРАТЬ ПЕРВЫЕ 1
	|	Значения.ПометкаУдаления
	|ИЗ
	|	РегистрСведений.ЗначенияПользователяЭлементовКонструктораВидовПродукцииИС КАК Значения
	|ГДЕ
	|	Значения.ЭлементКонструктора = &ЭлементКонструктора
	|	И Значения.ВидПродукции В (&ВидыПродукции)
	|	И Значения.Значение = &Значение";
	РезультатЗапроса = Запрос.ВыполнитьПакет();
	
	Выборка = РезультатЗапроса[0].Выбрать();
	Если Выборка.Следующий() Тогда
		Результат.Значение = Истина;
	КонецЕсли;
	
	Выборка = РезультатЗапроса[1].Выбрать();
	Если Выборка.Следующий() Тогда
		Результат.ЗначениеПользователя = Не Выборка.ПометкаУдаления;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает ключи видов продукции ИС.
// 
// Возвращаемое значение:
//  см. Справочники.КлючиВидовПродукцииИС.КлючиВидовПродукции
Функция КлючиВидовПродукции() Экспорт
	
	Возврат Справочники.КлючиВидовПродукцииИС.КлючиВидовПродукции();
	
КонецФункции

#КонецОбласти

// Классифицирует текущий сеанс, как сеанс, запущенный в фоновом задании в клиент-серверном варианте, в остальных
// случаях, сеанс имеет ту же файловую систему на стороне сервера, что и основной сеанс.
//	
// Возвращаемое значение:
// 	Булево - Описание
Функция ЭтоФоновоеЗаданиеНаСервере() Экспорт
	
	Если ПолучитьТекущийСеансИнформационнойБазы().ПолучитьФоновоеЗадание() <> Неопределено
		И Не ОбщегоНазначения.ИнформационнаяБазаФайловая() Тогда
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

// см. ОбщегоНазначенияИС.СертификатыПользователяДляПодписиПоОрганизации
Функция СертификатыПользователяДляПодписиПоОрганизации(Организация, ВключатьСертификатыБезОрганизации = Ложь, ВключатьСертификатыФЛ_СМЧД = Ложь) Экспорт
	Возврат ОбщегоНазначенияИС.СертификатыПользователяДляПодписиПоОрганизации(Организация, ВключатьСертификатыБезОрганизации, ВключатьСертификатыФЛ_СМЧД);
КонецФункции

// см. УправлениеКонтактнойИнформацией.КонтактнаяИнформацияПоПредставлению
Функция ЗначенияПолейКонтактнойИнформации(Представление, ОжидаемыйВид) Экспорт
	
	Возврат УправлениеКонтактнойИнформацией.КонтактнаяИнформацияПоПредставлению(
		Представление, ОжидаемыйВид);
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// см. ОбщегоНазначенияИС.КлассификаторНастроекВидовПродукцийЗагружен
Функция КлассификаторНастроекВидовПродукцийЗагружен() Экспорт
	
	Возврат ОбщегоНазначенияИС.КлассификаторНастроекВидовПродукцийЗагружен();
	
КонецФункции

#Область КонструкторВидовПродукции

Функция ПредставленияВидовУпаковокПоВидамПродукции() Экспорт
	
	Склонения = Новый Соответствие;
	
	ЭлементКонструктора        = ПредопределенноеЗначение("Справочник.ЭлементыКонструктораВидовПродукцииИС.ВидыУпаковок");
	ДанныеЭлемента             = КонструкторИСОбщегоНазначенияКлиентСервер.ДанныеЭлементаКонструктора(ЭлементКонструктора);
	АтрибутыВидовУпаковок      = КонструкторИСОбщегоНазначенияКлиентСервер.АтрибутыВидовУпаковок();
	ДанныеЭлемента             = ОбщегоНазначения.СкопироватьРекурсивно(ДанныеЭлемента);
	ДанныеЭлемента.ВидыПродукции.Вставить(Справочники.ВидыПродукцииИС.ПустаяСсылка(), Новый Соответствие());
	
	Кеш = Новый Соответствие;
	
	Для Каждого КлючИЗначение Из ДанныеЭлемента.ВидыПродукции Цикл
		
		ВидПродукции = КлючИЗначение.Ключ;
		Значения     = КлючИЗначение.Значение;
		
		Для Каждого ВидУпаковки Из Перечисления.ВидыУпаковокИС Цикл
			
			ЗначенияАтрибутовПоВидуУпаковки = Значения[ВидУпаковки];
			
			Для Каждого АтрибутыВидовУпаковки Из АтрибутыВидовУпаковок Цикл
				
				ИмяАтрибута       = АтрибутыВидовУпаковки.Ключ;
				ПараметрыАтрибута = АтрибутыВидовУпаковки.Значение;
				
				ПредставлениеВидаУпаковки = "";
				Если ЗначенияАтрибутовПоВидуУпаковки <> Неопределено
					И ЗначенияАтрибутовПоВидуУпаковки.Свойство(ИмяАтрибута)  Тогда
					ПредставлениеВидаУпаковки = ЗначенияАтрибутовПоВидуУпаковки[ИмяАтрибута];
				КонецЕсли;
				
				Если Не ЗначениеЗаполнено(ПредставлениеВидаУпаковки) Тогда
					ПредставлениеВидаУпаковки = КонструкторИСОбщегоНазначенияКлиентСервер.ПредставлениеВидаУпаковкиПоУмолчанию(
						ВидУпаковки,
						ПараметрыАтрибута.ГрамматическоеЧисло,
						ПараметрыАтрибута.ЭтоОписаниеКоличества);
				КонецЕсли;
				
				КлючКеша = ПредставлениеВидаУпаковки + "|" + ПараметрыАтрибута.Падеж;
				ЗначениеИзКеша = Кеш[КлючКеша];
				Если ЗначениеИзКеша <> Неопределено Тогда
					ПредставлениеВидаУпаковки = ЗначениеИзКеша;
				Иначе
					ЗначенияСклонений = ПолучитьСклоненияСтроки(
						ПредставлениеВидаУпаковки,
						"Л=ru_RU",
						СтрШаблон("ПД=%1", ПараметрыАтрибута.Падеж));
					Если ЗначенияСклонений.Количество() Тогда
						ПредставлениеВидаУпаковки = ЗначенияСклонений[0];
					КонецЕсли;
					Кеш.Вставить(КлючКеша, ПредставлениеВидаУпаковки);
				КонецЕсли;
				
				ЗначенияПути = Новый Массив;
				ЗначенияПути.Добавить(ВидПродукции);
				ЗначенияПути.Добавить(ВидУпаковки);
				ЗначенияПути.Добавить(ПараметрыАтрибута.Падеж);
				ЗначенияПути.Добавить(ПараметрыАтрибута.ГрамматическоеЧисло);
				ЗначенияПути.Добавить(ПараметрыАтрибута.ЭтоОписаниеКоличества);
				
				ОбщегоНазначенияИСКлиентСервер.УстановитьДанныеПоПутиВСоответствии(
					Склонения, ЗначенияПути, ПредставлениеВидаУпаковки);
					
			КонецЦикла;
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Склонения;
	
КонецФункции

#КонецОбласти

Функция ТребуетсяДоверенностьДляИспользованияСертификата(Сертификат, Организация, ИНН) Экспорт
	Возврат ОбщегоНазначенияИС.ТребуетсяДоверенностьДляИспользованияСертификата(Сертификат, Организация, ИНН);
КонецФункции

// см. РегистрыСведений.МестаОсуществленияДеятельностиИСМП.ИмеютсяПодконтрольныеОбъектыВЕТИС
// 
Функция ОрганизацияИмеетПодконтрольныеОбъектыВЕТИС(Организация) Экспорт
	Возврат РегистрыСведений.МестаОсуществленияДеятельностиИСМП.ИмеютсяПодконтрольныеОбъектыВЕТИС(Организация);	
КонецФункции

#КонецОбласти