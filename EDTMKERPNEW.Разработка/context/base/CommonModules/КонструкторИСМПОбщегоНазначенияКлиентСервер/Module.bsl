#Область ПрограммныйИнтерфейс

// Возвращает дату начала прослеживаемости перемещения между МОД
// для указанного вида продукции (товарной группы).
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - Строка, СправочникСсылка - вид продукции или товарная группа,
//                                для которой определяется дата начала прослеживаемости
//                                перемещения между МОД
//
// Возвращаемое значение:
//  Дата - дата начала прослеживаемости перемещения между МОД,
//  Неопределено - если дата не задана для указанного вида продукции
//
Функция ДатаНачалаПрослеживаемостиПеремещенияМеждуМОД(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаПрослеживаемостиПеремещенияМеждуМОД"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Проверяет, доступно ли поле "Минимальная розничная цена" для указанного вида продукции.
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
//
// Возвращаемое значение:
//  Булево - Истина, если поле МРЦ доступно
//
Функция ДоступноПолеМРЦ(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПолеМРЦ"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

// Возвращает виды продукции, для которых доступно поле "Минимальная розничная цена".
//
// Возвращаемое значение:
//  Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции, для которых доступно поле МРЦ
//
Функция ВидыПродукцииКоторымДоступноПолеМРЦ() Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииПоЭлементуКонструктора(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПолеМРЦ"));
	
КонецФункции

// Проверяет, нужно ли использовать числитель и знаменатель при частичном выбытии на ККТ
// для указанного вида продукции.
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//
// Возвращаемое значение:
//  Булево - Истина, если нужно использовать числитель и знаменатель при частичном выбытии на ККТ
//
Функция ИспользоватьЧислительИЗнаменательПриЧастичномВыбытииНаККТ(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ИспользоватьЧислительИЗнаменательПриЧастичномВыбытииНаККТ"),
			ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

// Проверяет, является ли продукция продукцией ЕГАИС
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
//
// Возвращаемое значение:
//  Булево - Истина, если продукция является продукцией ЕГАИС
//
Функция ЭтоПродукцияЕГАИС(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЭтоПродукцияЕГАИС(
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

// Проверяет, является ли продукция продукцией ИС МП
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
//  ВключатьТабачнуюПродукцию - Булево - включать ли табачную продукцию (по умолчанию Ложь)
//
// Возвращаемое значение:
//  Булево - Истина, если продукция является продукцией ИС МП
//
Функция ЭтоПродукцияИСМП(ВидПродукцииТоварнаяГруппа, ВключатьТабачнуюПродукцию = Ложь) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЭтоПродукцияИСМП(
		ВидыПродукции(ВидПродукцииТоварнаяГруппа), ВключатьТабачнуюПродукцию);
	
КонецФункции

// Проверяет, является ли вид продукции ГИС МТ связанным с ЕГАИС
//
// Параметры:
//  ВидПродукции - СправочникСсылка.ВидыПродукцииИС - Вид продукции
//               - Массив Из СправочникСсылка.ВидыПродукцииИС - Массив видов продукции
//               - СправочникСсылка.КлючиВидовПродукцииИС - Ключ видов продукции
//               - СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
// Возвращаемое значение:
//  Булево - Истина, если вид продукции ГИС МТ связан с подконтрольной ЕГАИС продукцией.
Функция ЭтоВидПродукцииИСМПЕГАИС(Знач ВидПродукции) Экспорт
	
	ВидыПродукцииПодконтрольныеЕГАИС = КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииИСМПЕГАИС();
	
	Если ТипЗнч(ВидПродукции) = Тип("Массив") Тогда
		Для Каждого ВидПродукцииИзМассива Из ВидПродукции Цикл
			Если ВидыПродукцииПодконтрольныеЕГАИС.Найти(ВидПродукцииИзМассива) <> Неопределено Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЦикла;
		Возврат Ложь;
	ИначеЕсли ТипЗнч(ВидПродукции) = Тип("СправочникСсылка.КлючиВидовПродукцииИС") Тогда
		ВидПродукции = КонструкторИСОбщегоНазначенияКлиентСервер.ВидПродукцииКлючаПоПодсистеме(ВидПродукции, "ГИСМТ");
	ИначеЕсли ТипЗнч(ВидПродукции) = Тип("СправочникСсылка.ТоварныеГруппыГИСМТ") Тогда
		Для Каждого ВидПродукцииИзМассива Из ВидыПродукции(ВидПродукции) Цикл
			Если ВидыПродукцииПодконтрольныеЕГАИС.Найти(ВидПродукцииИзМассива) <> Неопределено Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЦикла;
		Возврат Ложь;
	КонецЕсли;
	
	Возврат ВидыПродукцииПодконтрольныеЕГАИС.Найти(ВидПродукции) <> Неопределено;
	
КонецФункции

// Возвращает виды продукции, которые поддерживают подключение к оборудованию розлива
//
//Возвращаемое значение:
//  Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции, поддерживающие переменное количество
Функция ВидыПродукцииПоддерживающиеПодключениеКОборудованиюРозлива() Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииПоЭлементуКонструктора(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПоддерживаетсяПодключениеКОборудованиюРозлива"));
	
КонецФункции

// Возвращает товарную группу для указанного вида продукции
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
//
// Возвращаемое значение:
//  СправочникСсылка.ТоварныеГруппыГИСМТ - товарная группа,
//  Неопределено - если товарная группа не найдена
//
Функция ТоварнаяГруппаПоВидуПродукции(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	ТоварныеГруппы = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ТоварныеГруппыГИСМТ"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если ТоварныеГруппы.Количество() > 0 Тогда
		Возврат ТоварныеГруппы[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает список видов продукции для указанной товарной группы
//
// Параметры:
//  ТоварнаяГруппа - СправочникСсылка.ТоварныеГруппыГИСМТ - товарная группа
//
// Возвращаемое значение:
//  Массив из СправочникСсылка.ВидыПродукцииИС
//
Функция ВидыПродукцииПоТоварнойГруппе(ТоварнаяГруппа) Экспорт
	
	Если ЗначениеЗаполнено(ТоварнаяГруппа) Тогда
		Результат = КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииПоЗначениюЭлементаКонструктора(
			ПредопределенноеЗначение(
				"Справочник.ЭлементыКонструктораВидовПродукцииИС.ТоварныеГруппыГИСМТ"),
			ТоварнаяГруппа);
	Иначе
		Результат = Новый Массив;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает дату начала оформления УПД ОСУ для указанного вида продукции
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
//
// Возвращаемое значение:
//  Дата - дата начала оформления УПД ОСУ,
//  Неопределено - если дата не задана
//
Функция ДатаНачалаОформленияУПДОСУ(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаОформленияУПДОСУ"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает дату начала оформления УПД поштучно для указанного вида продукции
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
//
// Возвращаемое значение:
//  Дата - дата начала оформления УПД поштучно,
//  Неопределено - если дата не задана
//
Функция ДатаНачалаОформленияУПДПоштучно(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаОформленияУПДПоштучно"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает представление вида упаковки по виду продукции
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
//  ВидУпаковки - ПеречислениеСсылка.ВидыУпаковкиЕГАИС - вид упаковки
//  Падеж - Строка - падеж для склонения (по умолчанию "Именительный")
//  ГрамматическоеЧисло - Строка - грамматическое число (по умолчанию "Единственное")
//  ЭтоОписаниеКоличества - Булево - признак описания количества (по умолчанию Ложь)
//
// Возвращаемое значение:
//  Строка - представление вида упаковки
//
Функция ПредставлениеВидаУпаковкиПоВидуПродукции(ВидПродукцииТоварнаяГруппа, ВидУпаковки, Падеж = "Именительный", ГрамматическоеЧисло = "Единственное", ЭтоОписаниеКоличества = Ложь) Экспорт
	
	Если ВидПродукцииТоварнаяГруппа = ПредопределенноеЗначение("Справочник.ТоварныеГруппыГИСМТ.ПустаяСсылка")
		Или ВидПродукцииТоварнаяГруппа = ПредопределенноеЗначение("Справочник.КлючиВидовПродукцииИС.ПустаяСсылка") Тогда
		ВидыПродукции = ПредопределенноеЗначение("Справочник.ВидыПродукцииИС.ПустаяСсылка");
	Иначе
		ВидыПродукции = ВидыПродукции(ВидПродукцииТоварнаяГруппа);
	КонецЕсли;
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ПредставлениеВидаУпаковкиПоВидуПродукции(
		ВидыПродукции,
		ВидУпаковки,
		Падеж,
		ГрамматическоеЧисло,
		ЭтоОписаниеКоличества);
	
КонецФункции

// Проверяет, поддерживается ли подключение к оборудованию розлива для указанного вида продукции
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа
//
// Возвращаемое значение:
//  Булево - Истина, если поддерживается подключение к оборудованию розлива
//
Функция ПоддерживаетсяПодключениеКОборудованиюРозлива(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПоддерживаетсяПодключениеКОборудованиюРозлива"),
			ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

// Возвращает имя товарной группы по виду продукции или товарной группе.
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//
// Возвращаемое значение:
//  Строка - имя товарной группы,
//  Неопределено - если товарная группа не найдена.
//
Функция ИмяТоварнойГруппы(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Если ТипЗнч(ВидПродукцииТоварнаяГруппа) = Тип("СправочникСсылка.ТоварныеГруппыГИСМТ") Тогда
		ТоварнаяГруппа = ВидПродукцииТоварнаяГруппа;
	Иначе
		ТоварнаяГруппа = ТоварнаяГруппаПоВидуПродукции(ВидПродукцииТоварнаяГруппа);
	КонецЕсли;
	
	Если ТоварнаяГруппа <> Неопределено Тогда
		Результат = ОбщегоНазначенияИСМПКлиентСерверПовтИсп.ДанныеТоварнойГруппы(ТоварнаяГруппа).Имя;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

#Область КонструкторВидовПродукции

Функция ЭлементКонструктораСодержитЗначение(ЭлементКонструктора, ВидПродукцииТоварнаяГруппа, Значение = Неопределено) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЭлементКонструктораСодержитЗначение(
		ЭлементКонструктора,
		ВидыПродукции(ВидПродукцииТоварнаяГруппа),
		Значение);
	
КонецФункции

Функция ЗначениеАтрибутаЭлементаКонструктора(ИмяАтрибута, ЭлементКонструктора, ВидПродукцииТоварнаяГруппа,
	Значение = Неопределено, ВернутьПервоеЗначение = Истина) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		ИмяАтрибута,
		ЭлементКонструктора,
		ВидыПродукции(ВидПродукцииТоварнаяГруппа),
		Значение,
		ВернутьПервоеЗначение);
	
КонецФункции

#КонецОбласти

#Область ЭлементыКонструктораВидовПродукции

Функция ВидыПродукции(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Если ВидПродукцииТоварнаяГруппа = Неопределено Тогда
		Возврат Новый Массив;
	ИначеЕсли ТипЗнч(ВидПродукцииТоварнаяГруппа) = Тип("СправочникСсылка.ТоварныеГруппыГИСМТ") Тогда
		Возврат ВидыПродукцииПоТоварнойГруппе(ВидПродукцииТоварнаяГруппа);
	ИначеЕсли ТипЗнч(ВидПродукцииТоварнаяГруппа) = Тип("СправочникСсылка.КлючиВидовПродукцииИС") Тогда
		Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииКлюча(ВидПродукцииТоварнаяГруппа);
	Иначе
		Возврат ВидПродукцииТоварнаяГруппа;
	КонецЕсли;
	
КонецФункции

Функция ЭтоПродукцияВЕТИС(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЭтоПродукцияВЕТИС(
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

Функция ЭтоПродукцияМОТП(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЭтоПродукцияМОТП(
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

Функция ЭтоПродукцияМЕХ(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ВидыПродукцииМЕХ"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

Функция ЭтоПродукцияПоддерживающаяИндивидуализациюКИЗ(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ОперацияИндивидуализацияКИЗ"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

Функция ИменаДатНачалаРаботыСМаркируемойПродукцией(КакСтруктура = Ложь) Экспорт
	
	Если КакСтруктура Тогда
		Результат = Новый Структура;
		Результат.Вставить("ДатаНачалаПроизводства",                        "ДатаНачалаПроизводства");
		Результат.Вставить("ДатаНачалаОформленияУПДОСУ",                    "ДатаНачалаОформленияУПДОСУ");
		Результат.Вставить("ДатаНачалаОформленияУПДПоштучно",               "ДатаНачалаОформленияУПДПоштучно");
		Результат.Вставить("ДатаНачалаРозничныхПродаж",                     "ДатаНачалаРозничныхПродаж");
		Результат.Вставить("ДатаНачалаРазрешительногоРежима",               "ДатаНачалаРазрешительногоРежима");
		Результат.Вставить("ДатаНачалаИспользованияЛокальногоМодуля",       "ДатаНачалаИспользованияЛокальногоМодуля");
		Результат.Вставить("ДатаНачалаВыводаИзОборота",                     "ДатаНачалаВыводаИзОборота");
		Результат.Вставить("ДатаНачалаЗапретаПродажБезМарок",               "ДатаНачалаЗапретаПродажБезМарок");
		Результат.Вставить("ДатаНачалаПрослеживаемостиПеремещенияМеждуМОД", "ДатаНачалаПрослеживаемостиПеремещенияМеждуМОД");
		Результат.Вставить("ДатаНачалаКонтроляРазрешительныхДокументов",    "ДатаНачалаКонтроляРазрешительныхДокументов");
	Иначе
		Результат = Новый Массив;
		Результат.Добавить("ДатаНачалаПроизводства");
		Результат.Добавить("ДатаНачалаОформленияУПДОСУ");
		Результат.Добавить("ДатаНачалаОформленияУПДПоштучно");
		Результат.Добавить("ДатаНачалаРозничныхПродаж");
		Результат.Добавить("ДатаНачалаРазрешительногоРежима");
		Результат.Добавить("ДатаНачалаИспользованияЛокальногоМодуля");
		Результат.Добавить("ДатаНачалаВыводаИзОборота");
		Результат.Добавить("ДатаНачалаЗапретаПродажБезМарок");
		Результат.Добавить("ДатаНачалаПрослеживаемостиПеремещенияМеждуМОД");
		Результат.Добавить("ДатаНачалаКонтроляРазрешительныхДокументов");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИмяЭлементаКонструктораПоИмениДаты(Имя) Экспорт
	
	Возврат "Даты" + Сред(Имя, 5);
	
КонецФункции

Функция ЭлементКонструктораДляОпределенияДоступныхВидовПродукции(Объект = Неопределено) Экспорт
	
	Ссылка = Неопределено;
	ПолноеИмя = Неопределено;
	Если Объект <> Неопределено
		И ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Объект, "Ссылка") Тогда
		Ссылка = Объект.Ссылка;
	ИначеЕсли Объект <> Неопределено
		И ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Объект, "ПолноеИмя") Тогда
		ПолноеИмя = Объект.ПолноеИмя;
	КонецЕсли;
	
	#Если Сервер Или ВнешнееСоединение Или ТолстыйКлиентОбычноеПриложение Или ТолстыйКлиентУправляемоеПриложение Тогда
	Если ТипЗнч(Ссылка) = Тип("РегистрСведенийНаборЗаписей.ВскрытыеПотребительскиеУпаковкиИС") Тогда
		ПолноеИмя = "РегистрСведений.ВскрытыеПотребительскиеУпаковкиИС";
	КонецЕсли;
	#КонецЕсли
	
	Если ПолноеИмя = "РегистрСведений.ВскрытыеПотребительскиеУпаковкиИС" Тогда
		Результат = ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ЧастичноеВыбытие");
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ПодключениеКегаКОборудованиюРозливаИСМП") Тогда
		Результат = Новый Массив;
		Результат.Добавить(
			ПредопределенноеЗначение(
				"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПоддерживаетсяПодключениеКОборудованиюРозлива"));
		Результат.Добавить(
			ПредопределенноеЗначение(
				"Справочник.ЭлементыКонструктораВидовПродукцииИС.ЧастичноеВыбытие"));
	КонецЕсли;
	
	Если Результат = Неопределено
		И ОбщегоНазначенияИСКлиентСерверПовтИсп.ЭтоРасширеннаяВерсияГосИС() Тогда
		#Если Клиент Или ТонкийКлиент Или ВебКлиент Или МобильноеПриложениеКлиент Или МобильныйКлиент Тогда
			МодульКонструкторИСМПКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("КонструкторИСМПКлиентСервер");
		#Иначе
			МодульКонструкторИСМПКлиентСервер = ОбщегоНазначенияИСПовтИсп.ОбщийМодуль("КонструкторИСМПКлиентСервер");
		#КонецЕсли
		Результат = МодульКонструкторИСМПКлиентСервер.ЭлементКонструктораДляОпределенияДоступныхВидовПродукции(Объект);
	КонецЕсли;
	
	Если Результат = Неопределено Тогда
		Результат = ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ВидыПродукцииГИСМТ");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДоступныеВидыПродукции(Объект = Неопределено) Экспорт
	
	ЭлементКонструктораДляОпределенияДоступныхВидовПродукции = ЭлементКонструктораДляОпределенияДоступныхВидовПродукции(Объект);
	
	Если ТипЗнч(ЭлементКонструктораДляОпределенияДоступныхВидовПродукции) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементКонструктора Из ЭлементКонструктораДляОпределенияДоступныхВидовПродукции Цикл
			ОбщегоНазначенияКлиентСервер.ДополнитьМассив(
				Результат,
				КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииПоЭлементуКонструктора(ЭлементКонструктора),
				Истина);
		КонецЦикла;
	Иначе
		Результат = КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииПоЭлементуКонструктора(
			ЭлементКонструктораДляОпределенияДоступныхВидовПродукции);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДоступныеТоварныеГруппы(Объект = Неопределено) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ТоварныеГруппыГИСМТ"),
		ДоступныеВидыПродукции(Объект));
	
КонецФункции

Функция КодТоварнойГруппы(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Если ТипЗнч(ВидПродукцииТоварнаяГруппа) = Тип("СправочникСсылка.ТоварныеГруппыГИСМТ") Тогда
		ТоварнаяГруппа = ВидПродукцииТоварнаяГруппа;
	Иначе
		ТоварнаяГруппа = ТоварнаяГруппаПоВидуПродукции(ВидПродукцииТоварнаяГруппа);
	КонецЕсли;
	
	Если ТоварнаяГруппа <> Неопределено Тогда
		Результат = ОбщегоНазначенияИСМПКлиентСерверПовтИсп.ДанныеТоварнойГруппы(ТоварнаяГруппа).Код;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВидыПродукцииПоРеквизитуТоварнойГруппы(ИмяРеквизита, ЗначениеРеквизита) Экспорт
	
	Возврат ВидыПродукцииПоТоварнойГруппе(
		ОбщегоНазначенияИСМПКлиентСерверПовтИсп.ТоварнаяГруппаПоРеквизиту(ИмяРеквизита, ЗначениеРеквизита));
	
КонецФункции

Функция ДатаНачалаРозничныхПродаж(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаРозничныхПродаж"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДатаНачалаРазрешительногоРежима(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаРазрешительногоРежима"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДатаНачалаИспользованияЛокальногоМодуля(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаИспользованияЛокальногоМодуля"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДатаНачалаПроизводства(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаПроизводства"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДатаНачалаВыводаИзОборота(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаВыводаИзОборота"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДатаНачалаЗапретаПродажБезМарок(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаЗапретаПродажБезМарок"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДатаНачалаКонтроляРазрешительныхДокументов(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = Неопределено;
	
	Даты = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ДатыНачалаКонтроляРазрешительныхДокументов"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если Даты.Количество() > 0 Тогда
		Результат = Даты[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Функция возвращает описание структуры разделенных дат контроля маркируемой продукции
// 	по выбранному или по всем видам маркируемой продукции
// Параметры:
//  ВидМаркируемойПродукции - СправочникСсылка.ВидыПродукцииИС,
//  						  Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
// 
// Возвращаемое значение:
//  Массив Из см. ОбщегоНазначенияИСМПКлиентСервер.ИнициализироватьНастройкуДатРаздельногоКонтроля
Функция ДанныеДатРаздельногоКонтроля(ВидМаркируемойПродукции = Неопределено) Экспорт
	
	МассивНастроек = Новый Массив;
	ВидыПродукции  = Новый Массив;
	
	Если ВидМаркируемойПродукции = Неопределено Тогда
		ВидыПродукции = КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииИСМП(Истина);
	ИначеЕсли ТипЗнч(ВидМаркируемойПродукции) = Тип("Массив") Тогда
		ВидыПродукции = ВидМаркируемойПродукции;	
	Иначе
		ВидыПродукции.Добавить(ВидМаркируемойПродукции);
	КонецЕсли;
	
	Для Каждого ВидПродукции Из ВидыПродукции Цикл
		
		СтруктураДат = ОбщегоНазначенияИСМПКлиентСервер.ИнициализироватьНастройкуДатРаздельногоКонтроля();
		
		СтруктураДат.ВидПродукции                             = ВидПродукции;
		СтруктураДат.ДатаКонтрольПроизводства                 = ДатаНачалаПроизводства(ВидПродукции);
		СтруктураДат.ДатаКонтрольВыводИзОборота               = ДатаНачалаВыводаИзОборота(ВидПродукции);
		СтруктураДат.ДатаЗапретПродажиНемаркированныхОстатков = ДатаНачалаЗапретаПродажБезМарок(ВидПродукции);
		СтруктураДат.ДатаОптКонтрольОСУ                       = ДатаНачалаОформленияУПДОСУ(ВидПродукции);
		СтруктураДат.ДатаОптКонтрольПоштучно                  = ДатаНачалаОформленияУПДПоштучно(ВидПродукции);
		СтруктураДат.ДатаРозницаЛокальныйРазрешительныйРежим  = ДатаНачалаИспользованияЛокальногоМодуля(ВидПродукции);
		СтруктураДат.ДатаРозницаУведомительныйРежим           = ДатаНачалаРозничныхПродаж(ВидПродукции);
		СтруктураДат.ДатаРозницаРазрешительныйРежим           = ДатаНачалаРазрешительногоРежима(ВидПродукции);
		СтруктураДат.ДатаПрослеживаемостьПеремещенияМеждуМОД  = ДатаНачалаПрослеживаемостиПеремещенияМеждуМОД(ВидПродукции);
		СтруктураДат.ДатаКонтроляРазрешительныхДокументов     = ДатаНачалаКонтроляРазрешительныхДокументов(ВидПродукции);
		
		МассивНастроек.Добавить(СтруктураДат);
		
	КонецЦикла;
	
	Возврат МассивНастроек;
	
КонецФункции

// Функция возвращает описание структуры разделенных дат контроля маркируемой продукции
// 	по выбранному виду маркируемой продукции
// Параметры:
//  ВидМаркируемойПродукции - СправочникСсылка.ВидыПродукцииИС,
//  						  Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
// 
// Возвращаемое значение:
//  см. ОбщегоНазначенияИСМПКлиентСервер.ИнициализироватьНастройкуДатРаздельногоКонтроля
Функция ДатыРаздельногоКонтроляМаркируемойПродукции(ВидМаркируемойПродукции) Экспорт
	
	ДанныеНастроек = ДанныеДатРаздельногоКонтроля(ВидМаркируемойПродукции);
	
	Если ДанныеНастроек.Количество() Тогда
		СтруктураДат = ДанныеНастроек[0];
	Иначе
		СтруктураДат = ОбщегоНазначенияИСМПКлиентСервер.ИнициализироватьНастройкуДатРаздельногоКонтроля();
	КонецЕсли;
	
	Возврат СтруктураДат;
	
КонецФункции

Функция ИспользуетсяSSCC18(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.SSCC18"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

Функция ИспользуетсяSSCC20(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.SSCC20"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

Функция ИспользуетсяАТК(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.АТК"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

// Возвращает перечень маркируемой продукции, для которой обязательная онлайн проверка перед розничной продажей.
//
// Возвращаемое значение:
//   Массив Из СправочникСсылка.ВидыПродукцииИС - список видов маркируемой продукции.
//
Функция ВидыПродукцииОбязательнойОнлайнПроверкиПередРозничнойПродажей() Экспорт
	
	Возврат ВидыПродукцииОбязательнойОнлайнПроверкиПередРозничнойПродажейНаДату('00010101');
	
КонецФункции

// Возвращает перечень маркируемой продукции, для которой обязательная онлайн проверка перед розничной продажей на конкретную дату.
// 
// Параметры:
//  НаДату - Дата - дата проверки
//  
// Возвращаемое значение:
//   Массив Из СправочникСсылка.ВидыПродукцииИС - список видов маркируемой продукции.
//
Функция ВидыПродукцииОбязательнойОнлайнПроверкиПередРозничнойПродажейНаДату(НаДату = Неопределено) Экспорт
	
	МассивПродукции = Новый Массив();
	
	Если НаДату = Неопределено Тогда
		#Если Сервер Или ВнешнееСоединение Или МобильноеПриложениеСервер Или МобильныйАвтономныйСервер Тогда
			НаДату = ТекущаяДатаСеанса();
		#Иначе
			НаДату = ОбщегоНазначенияКлиент.ДатаСеанса();
		#КонецЕсли
	КонецЕсли;
	
	ВидыПродукции = ОбщегоНазначенияИСМПКлиентСерверПовтИсп.УчитываемыеВидыМаркируемойПродукции();
	Для Каждого ВидПродукции Из ВидыПродукции Цикл
		ДатаНачалаРазрешительногоРежима = ДатаНачалаРазрешительногоРежима(ВидПродукции);
		Если ЗначениеЗаполнено(ДатаНачалаРазрешительногоРежима)
			И (ДатаНачалаРазрешительногоРежима <= НачалоДня(НаДату) Или НаДату = '00010101') Тогда
			МассивПродукции.Добавить(ВидПродукции);
		КонецЕсли;
	КонецЦикла;
	
	Возврат МассивПродукции;
	
КонецФункции

// Возвращает перечень маркируемой продукции, для которой обязательная офлайн проверка перед розничной продажей на конкретную дату.
// 
// Параметры:
//  НаДату - Дата - дата проверки
//  
// Возвращаемое значение:
//   Массив Из СправочникСсылка.ВидыПродукцииИС - список видов маркируемой продукции.
//
Функция ВидыПродукцииОбязательнойОфлайнПроверкиПередРозничнойПродажейНаДату(НаДату = Неопределено) Экспорт
	
	МассивПродукции = Новый Массив();
	
	Если НаДату = Неопределено Тогда
		#Если Сервер Или ВнешнееСоединение Или МобильноеПриложениеСервер Или МобильныйАвтономныйСервер Тогда
			НаДату = ТекущаяДатаСеанса();
		#Иначе
			НаДату = ОбщегоНазначенияКлиент.ДатаСеанса();
		#КонецЕсли
	КонецЕсли;
	
	ВидыПродукции = ОбщегоНазначенияИСМПКлиентСерверПовтИсп.УчитываемыеВидыМаркируемойПродукции();
	Для Каждого ВидПродукции Из ВидыПродукции Цикл
		ДатаНачалаИспользованияЛокальногоМодуля = ДатаНачалаИспользованияЛокальногоМодуля(ВидПродукции);
		Если ЗначениеЗаполнено(ДатаНачалаИспользованияЛокальногоМодуля)
			И (ДатаНачалаИспользованияЛокальногоМодуля <= НачалоДня(НаДату) Или НаДату = '00010101') Тогда
			МассивПродукции.Добавить(ВидПродукции);
		КонецЕсли;
	КонецЦикла;
	
	Возврат МассивПродукции;
	
КонецФункции

// Признак использования объемно-сортового учета для вида продукции по виду операции.
// 
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа,
//                               СправочникСсылка.ВидыПродукцииИС - Вид продукции,
//                               СправочникСсылка.КлючиВидовПродукцииИС - ключ видов продукции,
//                               Массив из СправочникСсылка.ВидыПродукцииИС.
//  Операция - ПеречислениеСсылка.ВидыОперацийИСМП - Операция
//           - Массив из ПеречислениеСсылка.ВидыОперацийИСМП
// Возвращаемое значение:
//   Булево - Признак использования объемно-сортового учета для вида продукции.
//
Функция ПоддерживаетсяОбъемноСортовойУчет(ВидПродукцииТоварнаяГруппа, Операция) Экспорт
	
	ВидыПродукции = ВидыПродукции(ВидПродукцииТоварнаяГруппа);
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ИспользуетсяВидУпаковки(
		ВидыПродукции,
		ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.ОбъемноСортовойУчет"))
		И КонструкторИСОбщегоНазначенияКлиентСервер.ЭлементКонструктораСодержитЗначение(
			ПредопределенноеЗначение("Справочник.ЭлементыКонструктораВидовПродукцииИС.ПоддерживаетсяОСУ"),
			ВидыПродукции,
			Операция);
	
КонецФункции

Функция ДоступноПолеКоличествоФиксированныйВесПодсказка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПолеКоличествоФиксированныйВесПодсказка"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

Функция ПредставлениеТоварнойКатегории(ВидПродукции, Наименование) Экспорт
	
	Результат = "";
	
	ТоварнаяГруппа = ТоварнаяГруппаПоВидуПродукции(ВидПродукции);
	ВидыПродукции = ВидыПродукцииПоТоварнойГруппе(ТоварнаяГруппа);
	
	Если ВидыПродукции.Количество() = 1 Тогда
		Результат = ТоварнаяГруппа;
	Иначе
		Результат = СтрШаблон("%1 (%2)", Наименование, ТоварнаяГруппа);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолеКоличествоУпаковкаЦелоеЧисло(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПолеКоличествоУпаковкаЦелоеЧисло"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

// Возвращает Истина, если для вида продукции допустим переходный вид продукции.
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//  ПереходныйВидПродукции - СправочникСсылка.ВидыПродукцииИС - Вид продукции, Неопределено - Вид продукции
//Возвращаемое значение:
//  Булево - Истина, если для вида продукции допустим переходный вид продукции.
Функция ПереходныйВидПродукции(ВидПродукцииТоварнаяГруппа, ПереходныйВидПродукции = Неопределено) Экспорт
	
	Возврат ЭлементКонструктораСодержитЗначение(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПереходныеВидыПродукции"),
			ВидПродукцииТоварнаяГруппа,
			ПереходныйВидПродукции);
	
КонецФункции

// Вид продукции ИС МП с возможным, но необязательным указанием срока годности в операциях нанесения КМ
// 
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//  Операция - ПеречислениеСсылка.ВидыОперацийИСМП - Операция,
//           - Массив из ПеречислениеСсылка.ВидыОперацийИСМП.
// 
// Возвращаемое значение:
//  Булево - вид продукции не требует обязательного указания срока годности
Функция ЭтоВидПродукцииСОбязательнымУказаниемСроковГодности(ВидПродукцииТоварнаяГруппа, Операция = Неопределено) Экспорт
	
	Возврат ЭлементКонструктораСодержитЗначение(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПроверятьЗаполнениеПоляСрокГодности"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа),
		Операция);
	
КонецФункции

// Возвращает минимальную длину кода логистической упаковки
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
// Возвращаемое значение:
//  Число - Минимальная длина кода логистической упаковки.
Функция МинимальнаяДлинаКодаЛогистическойУпаковки(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Значения = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.МинимальнаяДлинаКодаЛогистическойУпаковки"),
			ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	МинимальноеЗначение = Неопределено;
	Для Каждого Значение Из Значения Цикл
		Если МинимальноеЗначение = Неопределено Тогда
			МинимальноеЗначение = Значение;
		Иначе
			МинимальноеЗначение = Мин(МинимальноеЗначение, Значение);
		КонецЕсли;
	КонецЦикла;
	
	Если МинимальноеЗначение = Неопределено Тогда
		МинимальноеЗначение = 0;
	КонецЕсли;
	
	Возврат МинимальноеЗначение;
	
КонецФункции

// Возвращает максимальную длину кода логистической упаковки
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
// Возвращаемое значение:
//  Число - Максамальная длина кода логистической упаковки.
Функция МаксимальнаяДлинаКодаЛогистическойУпаковки(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Значения = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.МаксимальнаяДлинаКодаЛогистическойУпаковки"),
			ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	МаксимальноеЗначение = Неопределено;
	Для Каждого Значение Из Значения Цикл
		Если МаксимальноеЗначение = Неопределено Тогда
			МаксимальноеЗначение = Значение;
		Иначе
			МаксимальноеЗначение = Макс(МаксимальноеЗначение, Значение);
		КонецЕсли;
	КонецЦикла;
	
	Если МаксимальноеЗначение = Неопределено Тогда
		МаксимальноеЗначение = 0;
	КонецЕсли;
	
	Возврат МаксимальноеЗначение;
	
КонецФункции

#Область ЧастичноеВыбытие

// Мера количества предмета расчета при выбытии на ККТ.
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//Возвращаемое значение:
//  ПеречислениеСсылка.МераКоличестваПредметаРасчетаККТ
Функция МераКоличестваПредметаРасчетаПриЧастичномВыбытии(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.МераКоличестваПредметаРасчетаПриЧастичномВыбытии"),
			ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
	Если ЗначениеЗаполнено(Результат) Тогда
		Возврат Результат[0];
	КонецЕсли;
	
КонецФункции

// Представление частичного выбытия по виду продукции.
// 
// Параметры:
//  ВидПродукции - СправочникСсылка.КлючиВидовПродукцииИС - Вид продукции
//  Падеж - Строка - Падеж для склонения строки (Именительный, Родительный, Винительный)
// Возвращаемое значение:
//  Строка - Представление частичного выбытия по виду продукции
Функция ПредставлениеПродажаЧастичногоВыбытияПоВидуПродукции(ВидПродукции, Падеж = "Именительный") Экспорт
	
	ИмяАтрибута = "ПродажаЧастичноеВыбытие" + Падеж;
	
	Результат = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		ИмяАтрибута,
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукции);
		
	Если Не ЗначениеЗаполнено(Результат) Тогда
		Если Падеж = "Родительный" Тогда
			Результат = НСтр("ru = 'продажи части продукции из вскрытой маркируемой потребительской упаковки';
							|en = 'продажи части продукции из вскрытой маркируемой потребительской упаковки'");
		ИначеЕсли Падеж = "Винительный" Тогда
			Результат = НСтр("ru = 'продажу части продукции из вскрытой маркируемой потребительской упаковки';
							|en = 'продажу части продукции из вскрытой маркируемой потребительской упаковки'");
		Иначе
			Результат = НСтр("ru = 'продажа части продукции из вскрытой маркируемой потребительской упаковки';
							|en = 'продажа части продукции из вскрытой маркируемой потребительской упаковки'");
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Представление вида продукции.
// 
// Параметры:
//  ВидПродукции - СправочникСсылка.КлючиВидовПродукцииИС - Вид продукции
// 
// Возвращаемое значение:
//  Строка, Неопределено - 
Функция ПредставлениеВидаПродукции(ВидПродукции, Падеж = "Именительный") Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПредставлениеВидПродукции",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукции);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	Иначе
		Результат = Строка(ВидПродукции);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Представление частичного выбытия по виду продукции.
// 
// Параметры:
//  ВидПродукции - СправочникСсылка.КлючиВидовПродукцииИС - Вид продукции
//  Падеж - Строка - Падеж для склонения строки
// Возвращаемое значение:
//  Строка - Представление частичного выбытия по виду продукции
Функция КраткаяФормаЧастичногоВыбытияПоВидуПродукции(ВидПродукции) Экспорт
	
	Результат = НСтр("ru = 'Поштучно';
					|en = 'Поштучно'");
	
	ИсключениеПродажаПоштучно = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ИсключениеПродажаПоштучно",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукции);
	
	Если ИсключениеПродажаПоштучно <> Неопределено Тогда
		Результат = ИсключениеПродажаПоштучно;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Представление частичного выбытия по виду продукции.
// 
// Параметры:
//  ВидПродукции - СправочникСсылка.КлючиВидовПродукцииИС - Вид продукции
//  Падеж - Строка - Падеж для склонения строки
// Возвращаемое значение:
//  Строка - Представление частичного выбытия по виду продукции
Функция ПредставлениеЧастичноеВыбытиеПоВидуПродукции(ВидПродукции, Падеж = "Именительный") Экспорт
	
	Результат = НСтр("ru = 'Поштучная';
					|en = 'Поштучная'");
	
	ИсключениеПродажаПоштучно = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ИсключениеПродажаПоштучно",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукции);
	
	Если ИсключениеПродажаПоштучно <> Неопределено Тогда
		Результат = ИсключениеПродажаПоштучно;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает коэффициент пересчета количества частичного выбытия для разрешительного режима.
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
// Возвращаемое значение:
//  Число - Коэффициент пересчета количества частичного выбытия для разрешительного режима.
Функция КоэффициентПересчетаКоличестваЧастичногоВыбытияДляРазрешительногоРежима(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.КоэффициентПересчетаКоличестваЧастичногоВыбытияДляРазрешительногоРежима"),
			ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

// Настройка номенклатуры текст подвала.
// 
// Параметры:
//  ВидПродукции - 
// 
// Возвращаемое значение:
//  Строка - Настройка номенклатуры текст подвала
Функция НастройкаНоменклатурыТекстПодвалаЧастичноеВыбытие(ВидПродукции) Экспорт
	
	Результат = "";
	
	НастройкаНоменклатурыТекстПодвала = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"НоменклатураТекстПодвалаЧастичноеВыбытие",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукции);
	
	Если НастройкаНоменклатурыТекстПодвала <> Неопределено Тогда
		Результат = НастройкаНоменклатурыТекстПодвала;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Настройка номенклатуры текст подвала.
// 
// Параметры:
//  ВидПродукции - 
// 
// Возвращаемое значение:
//  Строка - Настройка номенклатуры текст подвала
Функция НастройкаНоменклатурыТекстПодвала(ВидПродукции) Экспорт
	
	Результат = "";
	
	НастройкаНоменклатурыТекстПодвала = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"НоменклатураТекстПодвала",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукции);
	
	Если НастройкаНоменклатурыТекстПодвала <> Неопределено Тогда
		Результат = НастройкаНоменклатурыТекстПодвала;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолеКоличествоЧастичноеВыбытиеПодсказка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПолеКоличествоЧастичноеВыбытиеПодсказка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НоменклатураТекущаяПодсказка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"НоменклатураТекущаяПодсказка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПодсказкаНоменклатураБезДокументовСборкаРазборка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПодсказкаНоменклатураБезДокументовСборкаРазборка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПодсказкаНоменклатураОдинДокументСборкаРазборка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПодсказкаНоменклатураОдинДокументСборкаРазборка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПодсказкаНоменклатураМногоДокументовСборкаРазборка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПодсказкаНоменклатураМногоДокументовСборкаРазборка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолеКоличествоКоробЗаголовок(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПолеКоличествоКоробЗаголовок",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолеКоличествоКоробПодсказка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПолеКоличествоКоробПодсказка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолеКоличествоУпаковкаЗаголовок(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПолеКоличествоУпаковкаЗаголовок",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолеКоличествоУпаковкаПодсказка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПолеКоличествоУпаковкаПодсказка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗаголовокГруппыШтука(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ЗаголовокГруппыШтука",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗаголовокГруппыУпаковка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ЗаголовокГруппыУпаковка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВариантЕдиницыПотребительскаяПодсказка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ВариантЕдиницыПотребительскаяПодсказка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВариантЕдиницыКоробБлокПодсказка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ВариантЕдиницыКоробБлокПодсказка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВариантЕдиницыЧастьУпаковки(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ВариантЕдиницыЧастьУпаковки",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВариантЕдиницыЧастьУпаковкиПодсказка(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Результат = "";
	
	ЗначениеАтрибута = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ВариантЕдиницыЧастьУпаковкиПодсказка",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеАтрибута <> Неопределено Тогда
		Результат = ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПредставлениеЕдиницыЧастичногоВыбытия(ВидПродукцииТоварнаяГруппа) Экспорт
	
	ПредставлениеЕдиницы = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"ПредставлениеЕдиницыЧастичногоВыбытия",
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.НастройкаПредставлений"),
		ВидПродукцииТоварнаяГруппа);
	
	Если ЗначениеЗаполнено(ПредставлениеЕдиницы) Тогда
		Возврат ПредставлениеЕдиницы;
	Иначе
		Возврат НСтр("ru = 'шт.';
					|en = 'шт.'");
	КонецЕсли;
	
КонецФункции

#КонецОбласти

// Поддерживается взаимное признание кодов маркировки
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
// ДанныеШтрихкода - Структура - Данные кода маркировки
//
//Возвращаемое значение:
//  Булево - Истина, коды другой страны признаются в РФ
Функция ВзаимноеПризнаниеКодовМаркировки(ВидПродукцииТоварнаяГруппа, ДанныеШтрихкода) Экспорт
	
	КодСтраны = "";
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДанныеШтрихкода, "СоставКодаМаркировки") Тогда
		КодСтраны = Лев(ДанныеШтрихкода.СоставКодаМаркировки.СерийныйНомер, 1);
	КонецЕсли;
	
	Возврат ЭлементКонструктораСодержитЗначение(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ВзаимноеПризнаниеКодовМаркировки"),
			ВидПродукцииТоварнаяГруппа,
			КодСтраны);
	
КонецФункции

#КонецОбласти

// Возвращает виды продукции, коды маркировки которых содержат нулевой МРЦ
//
//Возвращаемое значение:
//  Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции, коды маркировки которых содержат нулевой МРЦ
Функция ВидыПродукцииСодержащиеВКодеМаркировкиНулевойМРЦ() Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииПоЭлементуКонструктора(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.КодМаркировкиВключаетНулевойМРЦ"));
	
КонецФункции

// Функция проверяет, доступно ли поле "Минимальная розничная цена" для указанного вида продукции.
// 
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа,
//                               СправочникСсылка.ВидыПродукцииИС - Вид продукции,
//                               Массив из СправочникСсылка.ВидыПродукцииИС.
// 
// Возвращаемое значение:
//  Булево - доступность поля
Функция ДоступноУказаниеМинимальнойРозничнойЦены(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ПолеМинимальнаяРозничнаяЦена"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

// Возвращает связанные с ВетИС виды продукции ГИС МТ.
//
// Возвращаемое значение:
//  Массив Из СправочникСсылка.ВидыПродукцииИС - Массив связанных видов продукции ГИСМТ.
Функция ВидыПродукцииИСМПВЕТИС() Экспорт
	
	Результат = Новый Массив;
	
	ВидыПродукцииИСМП = КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииИСМП();
	
	Для Каждого ВидПродукции Из КонструкторИСОбщегоНазначенияКлиентСервер.ЗависимыеВидыПродукцииИС(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ВидыПродукцииВЕТИС")) Цикл
		
		Если ВидыПродукцииИСМП.Найти(ВидПродукции) <> Неопределено Тогда
			Результат.Добавить(ВидПродукции);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Вид продукции ИС МП требует указание срока годности в операциях нанесения КМ
// 
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//  УчитыватьМолочнуюПродукцию - Булево - Флаг учета молочной продукции.
//  Операция - ПеречислениеСсылка.ВидыОперацийИСМП - Операция,
//           - Массив из ПеречислениеСсылка.ВидыОперацийИСМП.
// 
// Возвращаемое значение:
//  Булево - вид продукции требует указания срока годности
Функция ЭтоВидПродукцииСоСрокамиГодности(ВидПродукцииТоварнаяГруппа, УчитыватьМолочнуюПродукцию = Истина, Операция = Неопределено) Экспорт
	
	Возврат ЭлементКонструктораСодержитЗначение(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.СрокГодности"),
		ВидыПродукции(ВидПродукцииТоварнаяГруппа),
		Операция);
	
КонецФункции

// Возвращает признак необходимости контроля статусов кодов маркировки
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//  Операция - ПеречислениеСсылка.ВидыОперацийИСМП, Неопределено - Вид операции
//Возвращаемое значение:
//  Булево - Истина, если по виду продукции и виду операции требуется контролировать статусы
Функция КонтролироватьСтатусыКодовМаркировки(ВидПродукцииТоварнаяГруппа, Операция = Неопределено) Экспорт
	
	ЭлементКонструктора = ПредопределенноеЗначение(
		"Справочник.ЭлементыКонструктораВидовПродукцииИС.КонтролироватьСтатусыКодовМаркировки");
	СвязанныйЭлемент = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"СвязанныйЭлемент", ЭлементКонструктора, ВидПродукцииТоварнаяГруппа, Операция);
	
	Если ЗначениеЗаполнено(СвязанныйЭлемент) Тогда
		ДатыНачалаУчета = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
			СвязанныйЭлемент, ВидПродукцииТоварнаяГруппа);
		#Если Клиент Или ВебКлиент Или МобильноеПриложениеКлиент Или МобильныйКлиент Тогда
			ДатаСеанса = НачалоДня(ОбщегоНазначенияКлиент.ДатаСеанса());
		#Иначе
			ДатаСеанса = НачалоДня(ТекущаяДатаСеанса());
		#КонецЕсли
		Для Каждого ДатаНачалаУчета Из ДатыНачалаУчета Цикл
			Если ЗначениеЗаполнено(ДатаНачалаУчета) Тогда
				Если ДатаСеанса >= НачалоДня(ДатаНачалаУчета) Тогда
					Возврат Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		ЗначенияЭлементаКонструктора = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктора(
			ЭлементКонструктора,
			ВидыПродукции(ВидПродукцииТоварнаяГруппа),
			Операция);
		
		Если ЗначенияЭлементаКонструктора.ЗначениеПользователя = Неопределено Тогда
			ВыключенДоДатыДействияСвязанногоЭлемента = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
				"ВыключенДоДатыДействияСвязанногоЭлемента", ЭлементКонструктора, ВидПродукцииТоварнаяГруппа, Операция) = Истина;
			Если ВыключенДоДатыДействияСвязанногоЭлемента Тогда
				Возврат Ложь;
			Иначе
				Возврат Истина;
			КонецЕсли;
		Иначе
			Возврат ЗначенияЭлементаКонструктора.ЗначениеПользователя;
		КонецЕсли;
		
	Иначе
		
		Возврат ЭлементКонструктораСодержитЗначение(
			ЭлементКонструктора,
			ВидПродукцииТоварнаяГруппа,
			Операция);
		
	КонецЕсли;
	
КонецФункции

// Возвращает признак необходимости контроля владельцев кодов маркировки
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//  Операция - ПеречислениеСсылка.ВидыОперацийИСМП, Неопределено - Вид операции
//Возвращаемое значение:
//  Булево - Истина, если по виду продукции и виду операции требуется контролировать владельца
Функция КонтролироватьВладельцевКодовМаркировки(ВидПродукцииТоварнаяГруппа, Операция = Неопределено) Экспорт
	
	ЭлементКонструктора = ПредопределенноеЗначение(
		"Справочник.ЭлементыКонструктораВидовПродукцииИС.КонтролироватьВладельцевКодовМаркировки");
	СвязанныйЭлемент = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
		"СвязанныйЭлемент", ЭлементКонструктора, ВидПродукцииТоварнаяГруппа, Операция);
	
	Если ЗначениеЗаполнено(СвязанныйЭлемент) Тогда
		ДатыНачалаУчета = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
			СвязанныйЭлемент, ВидПродукцииТоварнаяГруппа);
		#Если Клиент Или ВебКлиент Или МобильноеПриложениеКлиент Или МобильныйКлиент Тогда
			ДатаСеанса = НачалоДня(ОбщегоНазначенияКлиент.ДатаСеанса());
		#Иначе
			ДатаСеанса = НачалоДня(ТекущаяДатаСеанса());
		#КонецЕсли
		Для Каждого ДатаНачалаУчета Из ДатыНачалаУчета Цикл
			Если ЗначениеЗаполнено(ДатаНачалаУчета) Тогда
				Если ДатаСеанса >= НачалоДня(ДатаНачалаУчета) Тогда
					Возврат Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		ЗначенияЭлементаКонструктора = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктора(
			ЭлементКонструктора,
			ВидыПродукции(ВидПродукцииТоварнаяГруппа),
			Операция);
		
		Если ЗначенияЭлементаКонструктора.ЗначениеПользователя = Неопределено Тогда
			ВключенДоДатыДействияСвязанногоЭлемента = КонструкторИСОбщегоНазначенияКлиентСервер.ЗначениеАтрибутаЭлементаКонструктора(
				"ВключенДоДатыДействияСвязанногоЭлемента", ЭлементКонструктора, ВидПродукцииТоварнаяГруппа, Операция) = Истина;
			Если ВключенДоДатыДействияСвязанногоЭлемента Тогда
				Возврат Истина;
			Иначе
				Возврат Ложь;
			КонецЕсли;
		Иначе
			Возврат ЗначенияЭлементаКонструктора.ЗначениеПользователя;
		КонецЕсли;
		
	Иначе
		
		Возврат ЭлементКонструктораСодержитЗначение(
			ЭлементКонструктора,
			ВидПродукцииТоварнаяГруппа,
			Операция);
		
	КонецЕсли;
	
КонецФункции

// Функция возвращает признак действия контроля разрешительных документов при обороте марок на указаннную дату
// Возможность оборота регулируется настройкой даты в рабочем месте Интеграция ИС МП (обувь, одежда, табак...)
//  и текущим законодательством.
// 
// Параметры:
//  ВидПродукции - СправочникСсылка.ВидыПродукцииИС - вид маркируемой продукции
//  НаДату       - Неопределено, Дата                 - дата контроля
// 
// Возвращаемое значение:
//  Булево - Истина, если контроль уже вступил в силу
Функция ДействуетКонтрольРазрешительныхДокументов(ВидПродукции, НаДату = Неопределено) Экспорт
	
	Если Не ЗначениеЗаполнено(НаДату) Тогда
		#Если Клиент Или ВебКлиент Или МобильноеПриложениеКлиент Или МобильныйКлиент Тогда
			НаДату = НачалоДня(ОбщегоНазначенияКлиент.ДатаСеанса());
		#Иначе
			НаДату = НачалоДня(ТекущаяДатаСеанса());
		#КонецЕсли
	КонецЕсли;
	
	СтруктураДатКонтроля = ДатыРаздельногоКонтроляМаркируемойПродукции(ВидПродукции);
	ДействуетКонтрольРД  = Ложь;
	
	Если ЗначениеЗаполнено(СтруктураДатКонтроля.ДатаКонтроляРазрешительныхДокументов)
		И СтруктураДатКонтроля.ДатаКонтроляРазрешительныхДокументов <= НаДату Тогда
		ДействуетКонтрольРД = Истина;
	КонецЕсли;
	
	Возврат ДействуетКонтрольРД;
	
КонецФункции

// Возвращает признак необходимости игнорирования проверки кодов маркировки на ККТ
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
//  Операция - ПеречислениеСсылка.ВидыОперацийИСМП, Неопределено - Вид операции
//Возвращаемое значение:
//  Булево - Истина, если по виду продукции и виду операции требуется контролировать владельца
Функция ИгнорироватьРезультатыПроверкиКодовМаркировкиНаККТ(ВидПродукцииТоварнаяГруппа, Операция = Неопределено) Экспорт
	
	Возврат ЭлементКонструктораСодержитЗначение(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.ИгнорироватьРезультатыПроверкиКодовМаркировкиНаККТ"),
			ВидПродукцииТоварнаяГруппа,
			Операция);
	
КонецФункции

// Возвращает признак необходимости контроля единой минимальной цены табачных изделий
//
// Параметры:
//  ВидПродукцииТоварнаяГруппа - СправочникСсылка.КлючиВидовПродукцииИС,
//                               СправочникСсылка.ВидыПродукцииИС,
//                               Массив из СправочникСсылка.ВидыПродукцииИС - Виды продукции,
//                               СправочникСсылка.ТоварныеГруппыГИСМТ - Товарная группа.
// Возвращаемое значение:
//  Булево - Истина, если необходим контроль ЕМЦ табачных изделий
Функция КонтролироватьЕдинуюМинимальнуюЦенуТабачныхИзделий(ВидПродукцииТоварнаяГруппа) Экспорт
	
	Возврат КонструкторИСОбщегоНазначенияКлиентСервер.ЗначенияЭлементаКонструктораПоВидуПродукции(
		ПредопределенноеЗначение(
			"Справочник.ЭлементыКонструктораВидовПродукцииИС.КонтролироватьЕМЦ"),
			ВидыПродукции(ВидПродукцииТоварнаяГруппа));
	
КонецФункции

#КонецОбласти