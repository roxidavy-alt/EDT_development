////////////////////////////////////////////////////////////////////////////////
// Модуль "Распределение запасов (движения)", содержит процедуры и функции
// обслуживающие движения документов по регистру накопления "Распределение запасов - Движения".
//
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// Формирует параметры для проведения документа по регистрам учетного механизма через общий механизм проведения.
//
// Параметры:
//  Документ - ДокументОбъект - записываемый документ
//  Свойства - ФиксированнаяСтруктура - свойства документа (См. ПроведениеДокументов.СвойстваДокумента).
//
//
// Возвращаемое значение:
//  Структура - См. ПроведениеДокументов.ПараметрыУчетногоМеханизма
//
Функция ПараметрыДляПроведенияДокумента(Документ, Свойства) Экспорт
	
	Параметры = ПроведениеДокументов.ПараметрыУчетногоМеханизма();
	Параметры.ЕстьПроизводныеДвижения = Истина;
	
	ТипДокумента = ТипЗнч(Документ);
	
	ДополнительныеДокументы = Новый Структура;
	ПроведениеДокументовЛокализация.ЗаполнитьЛокальныеДокументыОбеспечения(ДополнительныеДокументы);
	Параметры.КонтрольныеРегистрыЗаданий.Добавить(Метаданные.РегистрыНакопления.ЗапасыИПотребности);
	
	// Проведение
	Если Свойства.РежимЗаписи = РежимЗаписиДокумента.Проведение Тогда
		
		Параметры.ПодчиненныеРегистры.Добавить(Метаданные.РегистрыНакопления.ЗапасыИПотребности);
		
	КонецЕсли;
	
	// Контроль
	Если Свойства.РежимЗаписи <> РежимЗаписиДокумента.Запись Тогда

			ИсправительныйДокумент = ИсправлениеДокументов.ЭтоИсправительныйДокумент(Документ);
		
		Если Не ЗапасыСервер.ПроверитьНеобходимостьПерезаполненияВидовЗапасовДокумента(Документ)
			И (Не Документ.ДополнительныеСвойства.Свойство("КонтролироватьОбеспечение")
				Или Документ.ДополнительныеСвойства.КонтролироватьОбеспечение) Тогда
			
				Если ТипДокумента = Тип("ДокументОбъект.АктОРасхожденияхПослеОтгрузки")
					Или ТипДокумента = Тип("ДокументОбъект.АктОРасхожденияхПослеПеремещения")
					Или ТипДокумента = Тип("ДокументОбъект.ВнутреннееПотребление")
					Или ТипДокумента = Тип("ДокументОбъект.ВозвратТоваровПоставщику")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказКлиента")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказНаВнутреннееПотребление")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказНаПеремещение")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказНаСборку")
					Или ТипДокумента = Тип("ДокументОбъект.КорректировкаНазначенияТоваров")
						И Не Документ.ДополнительныеСвойства.Свойство("ПрограммноеСозданиеДокумента")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказПоставщику")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаявкаНаЗакупку")
					Или ТипДокумента = Тип("ДокументОбъект.КорректировкаРеализации")
					Или ТипДокумента = Тип("ДокументОбъект.ОтгрузкаТоваровКлиенту")
					Или ТипДокумента = Тип("ДокументОбъект.ОтгрузкаТоваровСХранения")
					//++ НЕ УТКА
						И Не Документ.ХозяйственнаяОперация = Перечисления.ХозяйственныеОперации.ПередачаДавальцу2_5
					Или ТипДокумента = Тип("ДокументОбъект.ОтгрузкаТоваровСХранения")
						И Не ПраваПользователяПовтИсп.ОтключитьКонтрольПередачиПродукцииДавальцу()
					//-- НЕ УТКА
					Или ТипДокумента = Тип("ДокументОбъект.ОтчетОРозничныхПродажах")
					Или ТипДокумента = Тип("ДокументОбъект.ПередачаТоваровХранителю")
					Или ТипДокумента = Тип("ДокументОбъект.ПеремещениеТоваров")
					Или ТипДокумента = Тип("ДокументОбъект.ПоступлениеТоваровНаСклад")
					Или ТипДокумента = Тип("ДокументОбъект.ПоступлениеТоваровОтКлиента")
					Или ТипДокумента = Тип("ДокументОбъект.ПоступлениеТоваровОтХранителя")
					Или ТипДокумента = Тип("ДокументОбъект.ПриемкаТоваровНаХранение")
					Или ТипДокумента = Тип("ДокументОбъект.ПриобретениеТоваровУслуг")
					Или ТипДокумента = Тип("ДокументОбъект.ПриходныйОрдерНаТовары")
					Или ТипДокумента = Тип("ДокументОбъект.ПрочееОприходованиеТоваров")
					Или ТипДокумента = Тип("ДокументОбъект.РеализацияТоваровУслуг")
					Или ТипДокумента = Тип("ДокументОбъект.СборкаТоваров")
					Или ТипДокумента = Тип("ДокументОбъект.ЧекККМ")
					Или ТипДокумента = Тип("ДокументОбъект.ЧекККМВозврат")
					Или ТипДокумента = Тип("ДокументОбъект.ЧекККМКоррекции")
					Или ТипДокумента = Тип("ДокументОбъект.АктКонтроляКачестваТоваров")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаявкаНаКонтрольКачестваТоваров")
						И Документ.ПричинаБлокировкиТовара = Справочники.ПричиныБлокировкиТоваров.ВходнойКонтрольКачестваТовара
					
					//++ НЕ УТ
					
					//++ Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ВозвратСырьяОтПереработчика")
					//-- Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ДвижениеПродукцииИМатериалов")
						И Не Документ.ХозяйственнаяОперация = Перечисления.ХозяйственныеОперации.ПередачаПродукцииИзКладовой
					Или ТипДокумента = Тип("ДокументОбъект.ДвижениеПродукцииИМатериалов")
						И Не ПраваПользователяПовтИсп.РазрешитьОформлениеВыпускаЗаднимЧислом()
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказМатериаловВПроизводство")
					//++ Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказПереработчику")
					//-- Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказПереработчику2_5")
					//++ НЕ УТКА
						И Не Документ.ГруппировкаЗатрат = Перечисления.ГруппировкиЗатратВЗаказеПереработчику.ПоЭтапамПроизводства
					//-- НЕ УТКА
					Или ТипДокумента = Тип("ДокументОбъект.ЗаявкаНаВозвратТоваровОтКлиента")
					//++ Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ПередачаСырьяПереработчику")
					Или ТипДокумента = Тип("ДокументОбъект.ПоступлениеОтПереработчика")
					//-- Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ПроизводствоБезЗаказа")
					//++ Устарело_Производство21
					Или ТипДокумента = Тип("ДокументОбъект.ВозвратМатериаловИзПроизводства")
					Или ТипДокумента = Тип("ДокументОбъект.ВыпускПродукции")
					Или ТипДокумента = Тип("ДокументОбъект.ПередачаМатериаловВПроизводство")
					//-- Устарело_Производство21
					
					//++ НЕ УТКА

					//++ Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ВозвратСырьяДавальцу")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказДавальца")
					//-- Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказДавальца2_5")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказНаПроизводство2_2")
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказНаРемонт")
					//++ Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ПоступлениеСырьяОтДавальца")
					//-- Устарело_Переработка24
					Или ТипДокумента = Тип("ДокументОбъект.ЭтапПроизводства2_2")
					Или ТипДокумента = Тип("ДокументОбъект.ОтчетДавальцу2_5")
					//++ Устарело_Производство21
					Или ТипДокумента = Тип("ДокументОбъект.ЗаказНаПроизводство")
					Или ТипДокумента = Тип("ДокументОбъект.КорректировкаЗаказаМатериаловВПроизводство")
					//-- Устарело_Производство21

					//-- НЕ УТКА

					//-- НЕ УТ
					Или ИсправительныйДокумент
						И НЕ Документ.ДополнительныеСвойства.Свойство("ПрограммноеСозданиеДокумента") 
					
					Или (ДополнительныеДокументы.Свойство("КонтрольныеРегистрыИзменений")
						И ДополнительныеДокументы.КонтрольныеРегистрыИзменений.Свойство("ЗапасыИПотребности")
						И ДополнительныеДокументы.КонтрольныеРегистрыИзменений.ЗапасыИПотребности.Найти(ТипДокумента)<>Неопределено)
					Тогда
						Параметры.КонтрольныеРегистрыИзменений.Добавить(Метаданные.РегистрыНакопления.ЗапасыИПотребности);
			КонецЕсли;
			
		КонецЕсли;
	
	КонецЕсли;
	
	Возврат Параметры;
	
КонецФункции

// Возвращает тексты запросов для сторнирования движений при исправлении документов
// 
// Параметры:
// 	МетаданныеДокумента - ОбъектМетаданныхДокумент - Метаданные документа, который проводится.
// 
// Возвращаемое значение:
// 	Соответствие - Соответствие полного имени регистра тексту запроса сторнирования
//
Функция ТекстыЗапросовСторнирования(МетаданныеДокумента) Экспорт
	
	ТекстыЗапросов = Новый Соответствие();
	
	МетаданныеРегистра = Метаданные.РегистрыНакопления.ЗапасыИПотребности;
	ДопУсловие = "ДанныеРегистра.ТипЗаписи = ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись)";
	ТекстСторнирования = ПроведениеДокументов.ТекстСторнирующегоЗапроса(МетаданныеРегистра, МетаданныеДокумента, ДопУсловие);
	ТекстыЗапросов.Вставить(МетаданныеРегистра.ПолноеИмя(), ТекстСторнирования);
	Возврат ТекстыЗапросов;
	
КонецФункции

// Дополняет текст запроса механизма проверки даты запрета по таблице изменений.
// 
// Параметры:
// 	Запрос - Запрос - используется для установки параметров запроса.
// 
// Возвращаемое значение:
//	Соответствие - соответствие имен таблиц изменения регистров и текстов запросов.
//	
Функция ТекстыЗапросовКонтрольДатыЗапретаПоТаблицеИзменений(Запрос) Экспорт

	СоответствиеТекстовЗапросов = Новый Соответствие();
	Возврат СоответствиеТекстовЗапросов;
	
КонецФункции

// Процедура формирования движений по подчиненным регистрам обеспечения.
//
// Параметры:
//	ТаблицыДляДвижений - Структура - таблицы данных документа
//	Движения - КоллекцияДвижений - коллекция наборов записей движений документа
//	Отказ - Булево - признак отказа от проведения документа.
//
Процедура ОтразитьДвижения(ТаблицыДляДвижений, Движения, Отказ) Экспорт
	
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	ПроведениеДокументов.ОтразитьДвижения(ТаблицыДляДвижений, Движения, "ЗапасыИПотребности");
	
КонецПроцедуры

// Формирует тексты запросов для контроля изменений записанных движений регистров.
//
// Параметры:
//  Запрос - Запрос - запрос, хранящий параметры используемые в списке запросов
//  ТекстыЗапроса - СписокЗначений Из Строка - список текстов запросов и их имен.
//  Документ - ДокументОбъект - записываемый документ.
//
Процедура ИнициализироватьДанныеКонтроляИзменений(Запрос, ТекстыЗапроса, Документ) Экспорт
	
	Если ПроведениеДокументов.ЕстьЗаписиВТаблице(Документ, "ЗапасыИПотребностиИзменение") Тогда
		
		ЗначениеПараметра = Пользователи.РолиДоступны(
			"ПроведениеНеподтвержденныхЗаказовСверхОбособленнойПотребности", Неопределено, Ложь);
		Запрос.УстановитьПараметр("КонтролироватьПодтвержденныйГрафикПоступления", ЗначениеПараметра);
		Запрос.УстановитьПараметр("КонтролироватьЗапасыТоваровПодлежащихРезервированиюПоМереПоступления",
			Константы.КонтролироватьЗапасыТоваровПодлежащихРезервированиюПоМереПоступления.Получить());
		Запрос.УстановитьПараметр("КонтролироватьТолькоФактическийОстаток",
			ТипЗнч(Документ.Ссылка) = Тип("ДокументСсылка.ЗаявкаНаКонтрольКачестваТоваров"));
		
		РаспределениеЗапасов.ДобавитьТекстЗапросаКонтроль(Запрос, ТекстыЗапроса, Документ);
		
	КонецЕсли;
	
КонецПроцедуры

// Выводит сообщения пользователю при наличии ошибок контроля изменений записанных движений регистров.
//
// Параметры:
//  РезультатыКонтроля - Структура - таблицы с результатами контроля изменений
//  Документ - ДокументОбъект - записываемый документ
//  Отказ - Булево - признак отказа от проведения документа.
//
Процедура СообщитьОРезультатахКонтроляИзменений(РезультатыКонтроля, Документ, Отказ) Экспорт
	
	Если ПроведениеДокументов.ЕстьЗаписиВТаблице(Документ, "ЗапасыИПотребностиИзменение") Тогда
		
		ТаблицаРезультатовКонтроля = РезультатыКонтроля.ОбеспечениеРезультатыКонтроля;
		РаспределениеЗапасов.СообщитьОбОшибкахПроведения(Документ, Отказ, ТаблицаРезультатовКонтроля);
		
		Если РезультатыКонтроля.Свойство("ОбеспечениеРезультатыКонтроляПревышенияОбособленногоОбеспечения") Тогда
			ТаблицаРезультатовКонтроля = РезультатыКонтроля.ОбеспечениеРезультатыКонтроляПревышенияОбособленногоОбеспечения;
			РаспределениеЗапасов.СообщитьОбОшибкахПроведенияПревышениеОбособленногоОбеспечения(Документ,
				Отказ, ТаблицаРезультатовКонтроля);
		КонецЕсли;
		
		Если РезультатыКонтроля.Свойство("ОбеспечениеРезультатыКонтроляЗапасаКонтролируемойПартииНаСкладе") Тогда
			ТаблицаРезультатовКонтроля = РезультатыКонтроля.ОбеспечениеРезультатыКонтроляЗапасаКонтролируемойПартииНаСкладе;
			РаспределениеЗапасов.СообщитьОбОшибкахПроведенияЗапасКонтролируемойПартииНаСкладе(Документ,
				Отказ, ТаблицаРезультатовКонтроля);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// Перераспределяет запасы при групповом изменении приоритета заказов на отгрузку.
// 
// Параметры:
// 	Заказы - Массив - ссылки на заказы.
//
Процедура ПриИзмененииПриоритета(Заказы) Экспорт
	
	Если Заказы.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц();
	Запрос = Новый Запрос();
	Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
	Запрос.УстановитьПараметр("Заказы", Заказы);
	Запрос.Текст =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Сведения.Номенклатура КАК Номенклатура,
		|	Сведения.Характеристика КАК Характеристика,
		|	Сведения.Склад КАК Склад,
		|	Сведения.Назначение КАК Назначение,
		|	НЕОПРЕДЕЛЕНО КАК Заказ,
		|	0 КАК ВНаличииРасход,
		|	0 КАК РезервироватьНаСкладе
		|ПОМЕСТИТЬ ЗапасыИПотребностиИзменение
		|ИЗ
		|	РегистрНакопления.ЗапасыИПотребности.Остатки(, Заказ В(&Заказы)) КАК Сведения
		|ГДЕ
		|	Сведения.КОбеспечениюОстаток <> 0
		|ИНДЕКСИРОВАТЬ ПО
		|	Номенклатура, Характеристика, Склад, Назначение";
	УстановитьПривилегированныйРежим(Истина);
	Запрос.Выполнить();
	Документ = ПроведениеДокументов.ЭмуляцияДокумента(Заказы[0], Неопределено); // нужна ссылка на заказ, для механизма состояний заказов клиентов
	
	НачатьТранзакцию();
	Попытка
		
		ЭмуляцияДокумента = ПроведениеДокументов.ЭмуляцияДокумента(Неопределено, Неопределено);
		ОтразитьЗаданияКРаспределениюЗапасов(ЭмуляцияДокумента, МенеджерВременныхТаблиц);
		Если Не РаспределениеЗапасов.ДосчитыватьРегистрРегламентнымЗаданием() Тогда
			СостоянияДокументов.ОтразитьСостоянияДокументов(Документ, МенеджерВременныхТаблиц);
			//++ НЕ УТКА
			СтруктураЗаказаПроведениеДокументов.СформироватьЗаданияКРасчетуСтруктурыЗаказаРаспределениеЗапасовПриЗаписиДокумента(Документ, МенеджерВременныхТаблиц);
			//-- НЕ УТКА
		КонецЕсли;
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		ТекстОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ТекстСообщения = НСтр("ru = 'Изменение приоритета заказа завершилось неудачно по причине: %Причина%';
								|en = 'Order priority change failed. Reason: %Причина%'");
		ТекстСообщения = СтрЗаменить(ТекстСообщения, "%Причина%", ТекстОшибки);
		СобытиеЖурнала = ОбновлениеИнформационнойБазы.СобытиеЖурналаРегистрации();
		ЗаписьЖурналаРегистрации(СобытиеЖурнала,
		                         УровеньЖурналаРегистрации.Ошибка,
		                         Метаданные.РегистрыСведений.ЗаданияКРаспределениюЗапасов,
		                         ,
		                         ТекстСообщения);
		ВызватьИсключение ТекстСообщения;
		
	КонецПопытки;
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// Шаблоны движений документов.

// Дополняет тексты запроса проведения документа текстами движений регистра ЗапасыИПотребности
// Отражает операцию движения товаров к отгрузке (списание из доступного остатка).
// Уменьшает фактический и свободный остаток товара на складе, работы в подразделении.
// Проведение документа может быть выполнено с контролем остатков.
// Если свободного остатка недостаточно могут быть задействованы запасы на складе,
// распределенные ранее на указанный документ планируемого расхода запаса.
//
// Параметры:
//  Запрос - Запрос - Запрос отражения документа.
//  ТекстыЗапроса - СписокЗначений Из Строка - Список текстов запроса отражения документа.
//  Регистры - Неопределено, Структура, Строка - Список регистров для отражения.
//  ТекстЗапросаТабЧасть - Строка - Текст запроса получения данных документа. Выражения выборки должны содержать следующие поля:
//   * Ссылка                                     - ДокументСсылка                        - отражаемый документ.
//   * Период                                     - Дата                                  - дата отражения операции (соответствует дате документа).
//   * Номенклатура                               - СправочникСсылка.Номенклатура         - списываемая из запаса номенклатура.
//   * Характеристика                             - СправочникСсылка.ХарактеристикиНоменклатуры - списываемая из запаса характеристика номенклатуры.
//   * Склад                                      - СправочникСсылка.Склады               - склад списания.
//   * Назначение                                 - СправочникСсылка.Назначения           - назначение номенклатуры.
//   * КонтролируемаяПартия                       - СправочникСсылка.КонтролируемыеПартии - партия, поступившая на контроль качества,
//                                                                                          которую отгружают по перемещению, списывают или успешно завершают контроль.
//   * Количество                                 - Число                                 - списываемое количество.
//   * ЗапланированныйРасходРаспределенногоЗапаса - ОпределяемыйТип.ОжидаемаяОтгрузка     - документ планирования расхода запасов,
//                                                                                          распределенные запасы которого будут израсходованы.
//   * КонтрольСвободногоОстатка                  - Булево                                - контроль свободного остатка при списании запасов.
//  ЕстьКонтролируемаяПартия                      - Булево                                - истина, если поле КонтролируемаяПартия в документе может быть непустым.
//  ТекстыШаблоновВременныхТаблиц - см. ПроведениеДокументов.ДополнитьЗапросОтраженияДокумента.ТекстыШаблоновВременныхТаблиц
Процедура РасходЗапаса(Запрос, ТекстыЗапроса, Регистры, ТекстЗапросаТабЧасть, ЕстьКонтролируемаяПартия, ТекстыШаблоновВременныхТаблиц = Неопределено) Экспорт
	
	ТекстЗапросаОперации =
		// Расход запаса.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтрольСвободногоОстатка
		|			ТОГДА ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
		|		ИНАЧЕ ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|	КОНЕЦ КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	НЕОПРЕДЕЛЕНО КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтрольСвободногоОстатка
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ -ТабЧасть.Количество
		|	КОНЕЦ КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	ТабЧасть.ЗапланированныйРасходРаспределенногоЗапаса КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
	Если ЕстьКонтролируемаяПартия Тогда
		
		Тексты = Новый Массив();
		Тексты.Добавить(ТекстЗапросаОперации);
		
		ТекстЗапросаОперации =
		// Расход заблокированного запаса.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтрольСвободногоОстатка
		|			ТОГДА ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
		|		ИНАЧЕ ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|	КОНЕЦ КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.КонтролируемаяПартия КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтрольСвободногоОстатка
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ -ТабЧасть.Количество
		|	КОНЕЦ КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	ТабЧасть.ЗапланированныйРасходРаспределенногоЗапаса КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.КонтролируемаяПартия <> ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
		Тексты.Добавить(ТекстЗапросаОперации);
		ТекстЗапросаОперации = СтрСоединить(Тексты, ОбщегоНазначенияУТ.РазделительЗапросовВОбъединении());
		
	КонецЕсли;
	
	ТекстыШаблоновОтражения = ТекстыШаблоновОтражения(Регистры, ТекстЗапросаОперации, "ЗапасыИПотребности");
	ТекстыЗапросовТаблицДокумента = Новый Структура;
	ТекстыЗапросовТаблицДокумента.Вставить("ТабЧасть", ТекстЗапросаТабЧасть);
	
	ПроведениеДокументов.ДополнитьЗапросОтраженияДокумента(Запрос, ТекстыЗапроса, ТекстыШаблоновОтражения,
		ТекстыЗапросовТаблицДокумента, ТекстыШаблоновВременныхТаблиц);
	
КонецПроцедуры

// Дополняет тексты запроса проведения документа текстами движений регистра ЗапасыИПотребности.
// Отражает операцию планируемого расхода товаров.
// Резервировать: уменьшает свободный остаток с контролем остатка, формирует резерв товара на складе по заказу.
// НеОбеспечивать: не формирует потребности и резервов, фиксирует факт приема заказа
// КОбеспечению, если есть свободный остаток на складе / в ожидаемых поступлениях: уменьшает свободный остаток,
// формирует резерв товара на складе по заказуКОбеспечению, если нет свободного остатка на складе / в ожидаемых поступлениях: формирует потребность в товаре / работах.
//
// Параметры:
//  Запрос - Запрос - Запрос отражения документа.
//  ТекстыЗапроса - СписокЗначений Из Строка - Список текстов запроса отражения документа.
//  Регистры - Неопределено, Структура, Строка - Список регистров для отражения.
//  ТекстЗапросаТабЧасть - Строка - Текст запроса получения данных документа. Выражения выборки должны содержать следующие поля:
//   * Ссылка                                     - ДокументСсылка                         - отражаемый документ.
//   * Период                                     - Дата                                   - дата отражения операции (соответствует дате документа).
//   * Номенклатура                               - СправочникСсылка.Номенклатура          - списываемая из запаса номенклатура.
//   * Характеристика                             - СправочникСсылка.ХарактеристикиНоменклатуры - списываемая из запаса характеристика номенклатуры.
//   * Склад                                      - СправочникСсылка.Склады                - склад списания.
//   * Назначение                                 - СправочникСсылка.Назначения            - назначение номенклатуры.
//   * КонтролируемаяПартия                       - СправочникСсылка.КонтролируемыеПартии  - партия товаров на контроле, планируемая к отгрузке по заказу на перемещение
//   * Количество                                 - Число                                  - заказываемое количество.
//   * ВариантОбеспечения                         - ПеречислениеСсылка.ВариантыОбеспечения - вариант обеспечения.
//   * Заказ                                      - ОпределяемыйТип.ОжидаемаяОтгрузка      - заказ.
//   * ЖелаемаяДатаОтгрузки                       - Дата                                   - планируемая дата списания запасов.
//   * ПоГрафику                                  - Булево - признак, что расход товара происходит по ранее запланированному в графике расходу.
//   * РаспоряжениеВГрафике                       - ОпределяемыйТип.ОжидаемаяОтгрузка      - документ планирования расхода запаса,
//                                                                                           запасы которого будут уменьшены.
//   * КоличествоВГрафике                         - Число                                  - количество запаса, на которое будет уменьшен запас
//                                                                                           ранее запланированного в графике расхода.
//  ЕстьПоГрафику                                 - Булево                                 - истина, если поле ПоГрафику в документе может быть истиной.
Процедура ЗапланироватьРасходЗапаса(Запрос, ТекстыЗапроса, Регистры, ТекстЗапросаТабЧасть, ЕстьПоГрафику) Экспорт
	
	ТекстЗапросаОперации =
		// График отгрузки.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия = ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА ТабЧасть.Заказ
		|		ИНАЧЕ ТабЧасть.КонтролируемаяПартия
		|	КОНЕЦ КАК Заказ,
		|	ВЫБОР
		|		КОГДА ТабЧасть.ЖелаемаяДатаОтгрузки = НЕОПРЕДЕЛЕНО
		|			ТОГДА ДАТАВРЕМЯ(1,1,1)
		|		ИНАЧЕ ТабЧасть.ЖелаемаяДатаОтгрузки
		|	КОНЕЦ КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия <> ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА 0
		|		КОГДА ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.СоСклада)
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК РезервироватьНаСкладе,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия <> ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА 0
		|		КОГДА ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.РезервироватьПоМереПоступления)
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	ВЫБОР
		|		КОГДА ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.КОбеспечению)
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК КОбеспечению,
		|	ВЫБОР
		|		КОГДА ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.НеТребуется)
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия = ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА 0
		|		КОГДА ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.СоСклада)
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК РезервироватьНаСкладеИзЗаблокированных,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия = ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА 0
		|		КОГДА ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.РезервироватьПоМереПоступления)
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.Количество <> 0
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
	Если ЕстьПоГрафику Тогда
		
		Тексты = Новый Массив();
		Тексты.Добавить(ТекстЗапросаОперации);
		
		ТекстЗапросаОперации =
		// Закрытие графика отгрузки.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия = ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА ТабЧасть.РаспоряжениеВГрафике
		|		ИНАЧЕ ТабЧасть.КонтролируемаяПартия
		|	КОНЕЦ КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоНаСкладе,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	ТабЧасть.КоличествоВГрафике КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.ПоГрафику
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
		Тексты.Добавить(ТекстЗапросаОперации);
		ТекстЗапросаОперации = СтрСоединить(Тексты, ОбщегоНазначенияУТ.РазделительЗапросовВОбъединении());
		
	КонецЕсли;
	
	ТекстыШаблоновОтражения = ТекстыШаблоновОтражения(Регистры, ТекстЗапросаОперации, "ЗапасыИПотребности");
	ТекстыЗапросовТаблицДокумента = Новый Структура;
	ТекстыЗапросовТаблицДокумента.Вставить("ТабЧасть", ТекстЗапросаТабЧасть);
	ПроведениеДокументов.ДополнитьЗапросОтраженияДокумента(Запрос, ТекстыЗапроса, ТекстыШаблоновОтражения, ТекстыЗапросовТаблицДокумента);
	
КонецПроцедуры

// Дополняет тексты запроса проведения документа текстами движений регистра ЗапасыИПотребности
// Отражает операцию отмены запланированного расхода товаров со склада.
//
// Параметры:
//  Запрос - Запрос - Запрос отражения документа.
//  ТекстыЗапроса - СписокЗначений Из Строка - Список текстов запроса отражения документа.
//  Регистры - Неопределено, Структура, Строка - Список регистров для отражения.
//  ТекстЗапросаТабЧасть - Строка - Текст запроса получения данных документа. Выражения выборки должны содержать следующие поля:
//   * Ссылка                                     - ДокументСсылка                         - отражаемый документ.
//   * Период                                     - Дата                                   - дата отражения операции (соответствует дате документа).
//   * Номенклатура                               - СправочникСсылка.Номенклатура          - списываемая из запаса номенклатура.
//   * Характеристика                             - СправочникСсылка.ХарактеристикиНоменклатуры - списываемая из запаса характеристика номенклатуры.
//   * Склад                                      - СправочникСсылка.Склады -              - склад списания.
//   * Назначение                                 - СправочникСсылка.Назначения -          - назначение номенклатуры.
//   * КонтролируемаяПартия                       - СправочникСсылка.КонтролируемыеПартии  - партия товаров на контроле, планируемая к отгрузке по заказу на перемещение
//   * Количество                                 - Число                                  - отменяемое количество.
//   * РаспоряжениеВГрафике                       - ОпределяемыйТип.ОжидаемаяОтгрузка      - документ планирования расхода запаса,
//                                                                                           запасы которого будут уменьшены.
Процедура ОтменитьЗапланированныйРасходЗапаса(Запрос, ТекстыЗапроса, Регистры, ТекстЗапросаТабЧасть) Экспорт
	
	ТекстЗапросаОперации =
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия = ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА ТабЧасть.РаспоряжениеВГрафике
		|		ИНАЧЕ ТабЧасть.КонтролируемаяПартия
		|	КОНЕЦ КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	ТабЧасть.Количество КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
	ТекстыШаблоновОтражения = ТекстыШаблоновОтражения(Регистры, ТекстЗапросаОперации, "ЗапасыИПотребности");
	ТекстыЗапросовТаблицДокумента = Новый Структура;
	ТекстыЗапросовТаблицДокумента.Вставить("ТабЧасть", ТекстЗапросаТабЧасть);
	ПроведениеДокументов.ДополнитьЗапросОтраженияДокумента(Запрос, ТекстыЗапроса, ТекстыШаблоновОтражения, ТекстыЗапросовТаблицДокумента);
	
КонецПроцедуры

// Дополняет тексты запроса проведения документа текстами движений регистра ЗапасыИПотребности
// и регистра ЗапасыИПотребности, отражающего операцию фактического поступления товаров на склад
// (пополнение доступного остатка).
// Увеличивает фактический и свободный остаток товара на складе, работы в подразделении.
// Если приход товара ранее был запланирован документом планового прихода запаса (приход по графику),
// то при проведении будет уменьшен запас и свободный остаток в данном ожидаемом приходе (распоряжении).
//
// Параметры:
//  Запрос - Запрос - Запрос отражения документа.
//  ТекстыЗапроса - СписокЗначений Из Строка - Список текстов запроса отражения документа.
//  Регистры - Неопределено, Структура, Строка - Список регистров для отражения.
//  ТекстЗапросаТабЧасть - Строка - Текст запроса получения данных документа. Выражения выборки должны содержать следующие поля:
//   * Ссылка                    - ДокументСсылка                        - отражаемый документ.
//   * Период                    - Дата                                  - дата отражения операции (соответствует дате документа).
//   * Номенклатура              - СправочникСсылка.Номенклатура         - приходуемая номенклатура.
//   * Характеристика            - СправочникСсылка.ХарактеристикиНоменклатуры - приходуемая характеристика номенклатуры.
//   * Склад                     - СправочникСсылка.Склады               - склад оприходования.
//   * Назначение                - СправочникСсылка.Назначения           - назначение приходуемой номенклатуры.
//   * КонтролируемаяПартия      - СправочникСсылка.КонтролируемыеПартии - партия поступившая на склад и подлежащая контролю качества.
//   * ПроконтролироватьКачество - Булево                                - необходимость блокировки поступившего товара.
//   * ПодлежитРазблокировке     - Булево                                - формирование графика разблокировки товара.
//   * ДатаПодтвержденияКачества - Дата                                  - планируемая Дата подтверждения качества после выполнения контроля качества 
//   * Количество                - Число                                 - приходуемое количество.
//   * ПоГрафику                 - Булево                                - признак, что приход товара происходит по ранее запланированному в графике поступлению.
//   * РаспоряжениеВГрафике      - ОпределяемыйТип.ОжидаемоеПоступление  - документ планирования прихода запаса,
//                                                                         запасы которого будут уменьшены на величину данного прихода запаса.
//  ЕстьПоГрафику                - Булево                                - истина, если поле ПоГрафику в документе может быть истиной.
//  ЕстьПроконтролироватьКачество - Булево                               - истина, если поле ПроконтролироватьКачество в документе может быть непустым.
//  ТекстыШаблоновВременныхТаблиц - см. ПроведениеДокументов.ДополнитьЗапросОтраженияДокумента.ТекстыШаблоновВременныхТаблиц
Процедура ПриходЗапаса(Запрос, ТекстыЗапроса, Регистры, ТекстЗапросаТабЧасть, ЕстьПоГрафику,
						ЕстьПроконтролироватьКачество, ТекстыШаблоновВременныхТаблиц = Неопределено) Экспорт
	
	ТекстЗапросаОперации =
		// Поступление товара на склад.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	НЕОПРЕДЕЛЕНО КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	ТабЧасть.Количество КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
	
	Если ЕстьПроконтролироватьКачество Тогда
		
		Тексты = Новый Массив();
		Тексты.Добавить(ТекстЗапросаОперации);
		
		ТекстЗапросаОперации =
		// Блокировка товара на складе.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.КонтролируемаяПартия КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	ТабЧасть.Количество КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ТабЧасть.Количество > 0 И ТабЧасть.ПодлежитРазблокировке КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.ПроконтролироватьКачество
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		// Планирование разблокировки поступившего товара, для положительных приходов.
		|ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.КонтролируемаяПартия КАК Заказ,
		|	ТабЧасть.ДатаПодтвержденияКачества КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	ТабЧасть.Количество КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.ПроконтролироватьКачество
		|		И ТабЧасть.ПодлежитРазблокировке
		|		И ТабЧасть.Количество > 0
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
		Тексты.Добавить(ТекстЗапросаОперации);
		ТекстЗапросаОперации = СтрСоединить(Тексты, ОбщегоНазначенияУТ.РазделительЗапросовВОбъединении());
	
	КонецЕсли;
	
	Если ЕстьПоГрафику Тогда
		
		Тексты = Новый Массив();
		Тексты.Добавить(ТекстЗапросаОперации);
		
		ТекстЗапросаОперации =
		// Закрытие графика поступления и графика разблокировки.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.РаспоряжениеВГрафике КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	ТабЧасть.Количество КАК ЗакрытьГрафикПоступления,
		|	ТабЧасть.ПроконтролироватьКачество КАК ПроконтролироватьКачество,
		|	ТабЧасть.ПодлежитРазблокировке КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.ПоГрафику
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
		Тексты.Добавить(ТекстЗапросаОперации);
		ТекстЗапросаОперации = СтрСоединить(Тексты, ОбщегоНазначенияУТ.РазделительЗапросовВОбъединении());
		
	КонецЕсли;
	
	ТекстыШаблоновОтражения = ТекстыШаблоновОтражения(Регистры, ТекстЗапросаОперации, "ЗапасыИПотребности");
	ТекстыЗапросовТаблицДокумента = Новый Структура;
	ТекстыЗапросовТаблицДокумента.Вставить("ТабЧасть", ТекстЗапросаТабЧасть);
	
	ПроведениеДокументов.ДополнитьЗапросОтраженияДокумента(Запрос, ТекстыЗапроса, ТекстыШаблоновОтражения,
		ТекстыЗапросовТаблицДокумента, ТекстыШаблоновВременныхТаблиц);
	
КонецПроцедуры

// Дополняет тексты запроса проведения документа текстами движений регистра ЗапасыИПотребности
// и регистра ЗапасыИПотребности, отражающего операцию фактического поступления товаров на склад
// (пополнение доступного остатка).
// Увеличивает фактический и свободный остаток товара на складе, работы в подразделении.
// Если приход товара ранее был запланирован документом планового прихода запаса (приход по графику),
// то при проведении будет уменьшен запас и свободный остаток в данном ожидаемом приходе (распоряжении).
//
// Параметры:
//  Запрос - Запрос - Запрос отражения документа.
//  ТекстыЗапроса - СписокЗначений Из Строка - Список текстов запроса отражения документа.
//  Регистры - Неопределено, Структура, Строка - Список регистров для отражения.
//  ТекстЗапросаТабЧасть - Строка - Текст запроса получения данных документа. Выражения выборки должны содержать следующие поля:
//   * Ссылка                    - ДокументСсылка                        - отражаемый документ.
//   * Период                    - Дата                                  - дата отражения операции (соответствует дате документа).
//   * Номенклатура              - СправочникСсылка.Номенклатура         - приходуемая номенклатура.
//   * Характеристика            - СправочникСсылка.ХарактеристикиНоменклатуры - приходуемая характеристика номенклатуры.
//   * Склад                     - СправочникСсылка.Склады               - склад оприходования.
//   * Назначение                - СправочникСсылка.Назначения           - назначение приходуемой номенклатуры.
//   * ПроконтролироватьКачество - Булево                                - необходимость блокировки товара, при его поступлении на склад.
//   * ПодлежитРазблокировке     - Булево                                - формирование графика разблокировки товара.
//   * ДатаПодтвержденияКачества - Дата                                  - планируемая Дата подтверждения качества после выполнения входного контроля качества 
//   * Количество                - Число                                 - приходуемое количество.
//   * Заказ                     - ОпределяемыйТип.ОжидаемоеПоступление  - заказ по которому планируется поступление.
//   * ДатаПоступления           - Булево                                - дата на которую планируется поступление.
//   * ДоступенДляРасхода        - Булево                                - признак, что данный запас можно использовать для обеспечения запланированного расхода запаса.
//   * ПоГрафику                 - Булево                                - признак, что приход товара происходит по ранее запланированному в графике поступлению.
//   * РаспоряжениеВГрафике      - ОпределяемыйТип.ОжидаемоеПоступление  - документ планирования прихода запаса,
//                                                                         запасы которого будут уменьшены.
//   * КоличествоВГрафике        - Число                                 - количество запаса, на которое будет уменьшен запас ранее запланированного в графике поступления.
//  ЕстьПоГрафику                - Булево                                - истина, если поле ПоГрафику в документе может быть истиной.
//  ЕстьПроконтролироватьКачество - Булево                               - истина, если поле ПроконтролироватьКачество в документе может быть непустым.
Процедура ЗапланироватьПриходЗапаса(Запрос, ТекстыЗапроса, Регистры, ТекстЗапросаТабЧасть, ЕстьПоГрафику, ЕстьПроконтролироватьКачество) Экспорт
	
	ТекстЗапросаОперации =
		// График поступления.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.Заказ КАК Заказ,
		|	ВЫБОР
		|		КОГДА ТабЧасть.ДоступенДляРасхода
		|			ТОГДА НАЧАЛОПЕРИОДА(ТабЧасть.ДатаПоступления, ДЕНЬ)
		|		ИНАЧЕ ДАТАВРЕМЯ(1, 1, 1)
		|	КОНЕЦ КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	ВЫБОР
		|		КОГДА ТабЧасть.ДоступенДляРасхода
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК Поступит,
		|	ТабЧасть.Количество КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	ВЫБОР
		|		КОГДА ТабЧасть.ДоступенДляРасхода И ТабЧасть.ПроконтролироватьКачество
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ТабЧасть.ПроконтролироватьКачество И ТабЧасть.ПодлежитРазблокировке КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.Количество <> 0
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
	Если ЕстьПроконтролироватьКачество Тогда
		
		Тексты = Новый Массив();
		Тексты.Добавить(ТекстЗапросаОперации);
		
		ТекстЗапросаОперации =
		// График разблокировки.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.Заказ КАК Заказ,
		|	ТабЧасть.ДатаПодтвержденияКачества КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	ТабЧасть.Количество КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.ДоступенДляРасхода
		|		И ТабЧасть.ПроконтролироватьКачество
		|		И ТабЧасть.ПодлежитРазблокировке
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
			
		Тексты.Добавить(ТекстЗапросаОперации);
		ТекстЗапросаОперации = СтрСоединить(Тексты, ОбщегоНазначенияУТ.РазделительЗапросовВОбъединении());
		
	КонецЕсли;
	
	Если ЕстьПоГрафику Тогда
		
		Тексты = Новый Массив();
		Тексты.Добавить(ТекстЗапросаОперации);
		
		ТекстЗапросаОперации =
		// Закрытие графика поступления.
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.РаспоряжениеВГрафике КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	ТабЧасть.КоличествоВГрафике КАК ЗакрытьГрафикПоступления,
		|	ТабЧасть.ПроконтролироватьКачество КАК ПроконтролироватьКачество,
		|	ТабЧасть.ПодлежитРазблокировке КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В (&Ссылка)
		|		И ТабЧасть.ПоГрафику
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
		
		Тексты.Добавить(ТекстЗапросаОперации);
		ТекстЗапросаОперации = СтрСоединить(Тексты, ОбщегоНазначенияУТ.РазделительЗапросовВОбъединении());
		
	КонецЕсли;
	
	ТекстыШаблоновОтражения = ТекстыШаблоновОтражения(Регистры, ТекстЗапросаОперации, "ЗапасыИПотребности");
	ТекстыЗапросовТаблицДокумента = Новый Структура;
	ТекстыЗапросовТаблицДокумента.Вставить("ТабЧасть", ТекстЗапросаТабЧасть);
	
	ПроведениеДокументов.ДополнитьЗапросОтраженияДокумента(Запрос, ТекстыЗапроса, ТекстыШаблоновОтражения, ТекстыЗапросовТаблицДокумента);
	
КонецПроцедуры

// Дополняет тексты запроса проведения документа текстами движений регистра ЗапасыИПотребности.
// Отражает операцию отмены запланированного прихода товаров на склад.
// Увеличивает фактический и свободный остаток товара на складе, работы в подразделении.
// Будет уменьшен запас и свободный остаток в ожидаемом приходе (распоряжении).
//
// Параметры:
//  Запрос - Запрос - Запрос отражения документа.
//  ТекстыЗапроса - СписокЗначений Из Строка - Список текстов запроса отражения документа.
//  Регистры - Неопределено, Структура, Строка - Список регистров для отражения.
//  ТекстЗапросаТабЧасть - Строка - Текст запроса получения данных документа. Выражения выборки должны содержать следующие поля:
//   * Ссылка                    - ДокументСсылка                        - отражаемый документ.
//   * Период                    - Дата                                  - дата отражения операции (соответствует дате документа).
//   * Номенклатура              - СправочникСсылка.Номенклатура         - приходуемая номенклатура.
//   * Характеристика            - СправочникСсылка.ХарактеристикиНоменклатуры - приходуемая характеристика номенклатуры.
//   * Склад                     - СправочникСсылка.Склады               - склад оприходования.
//   * Назначение                - СправочникСсылка.Назначения           - назначение приходуемой номенклатуры.
//   * ПроконтролироватьКачество - Булево                                - необходимость закрытия графика поступления заблокированного товара.
//   * ПодлежитРазблокировке     - Булево                                - необходимость закрытия графика разблокировки товара.
//   * Количество                - Число                                 - отменяемое количество.
//   * РаспоряжениеВГрафике      - ОпределяемыйТип.ОжидаемоеПоступление  - документ планирования прихода запаса,
//                                                                         запасы которого будут уменьшены.
Процедура ОтменитьЗапланированныйПриходЗапаса(Запрос, ТекстыЗапроса, Регистры, ТекстЗапросаТабЧасть) Экспорт
	
	ТекстЗапросаОперации =
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.РаспоряжениеВГрафике КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	ТабЧасть.Количество КАК ЗакрытьГрафикПоступления,
		|	ТабЧасть.ПроконтролироватьКачество КАК ПроконтролироватьКачество,
		|	ТабЧасть.ПодлежитРазблокировке КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка В(&Ссылка)
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
	
	ТекстыШаблоновОтражения = ТекстыШаблоновОтражения(Регистры, ТекстЗапросаОперации, "ЗапасыИПотребности");
	ТекстыЗапросовТаблицДокумента = Новый Структура;
	ТекстыЗапросовТаблицДокумента.Вставить("ТабЧасть", ТекстЗапросаТабЧасть);
	
	ПроведениеДокументов.ДополнитьЗапросОтраженияДокумента(Запрос, ТекстыЗапроса, ТекстыШаблоновОтражения, ТекстыЗапросовТаблицДокумента);
	
КонецПроцедуры

// Дополняет тексты запроса проведения документа текстами движений регистра ЗапасыИПотребности,
// отражающего операцию планирования графика отгрузки по датам, а также движения товаров к отгрузке.
//
// Параметры:
//  ТекстЗапросаТабЧасть - Строка - Текст запроса получения данных документа. Выражения выборки должны содержать следующие поля:
//   * Период                                     - Дата                                   - дата отражения операции (соответствует дате документа).
//   * Номенклатура                               - СправочникСсылка.Номенклатура          - списываемая из запаса номенклатура.
//   * Характеристика                             - СправочникСсылка.ХарактеристикиНоменклатуры - списываемая из запаса характеристика номенклатуры.
//   * Склад                                      - СправочникСсылка.Склады                - склад списания.
//   * Назначение                                 - СправочникСсылка.Назначения            - назначение номенклатуры.
//   * КонтролируемаяПартия                       - СправочникСсылка.КонтролируемыеПартии  - партия товара на контроле, которую нужно отгрузить, либо запланировать ее отгрузку.
//   * Количество                                 - Число                                  - заказываемое количество.
//   * ВариантОбеспечения                         - ПеречислениеСсылка.ВариантыОбеспечения - вариант обеспечения.
//   * ЗаказНаОтгрузку                            - ОпределяемыйТип.ОжидаемаяОтгрузка      - заказ.
//   * ЖелаемаяДатаОтгрузки                       - Дата                                   - планируемая дата списания запасов.
//   * ДатаПриема - Дата - 
//   * ПорядокПоПриоритету - Число - 
//   * ГотовКОбеспечению - Булево - 
//   * ГотовКОтгрузке - Булево - 
//  ИгнорироватьГотовностьКОбеспечению - Булево -
//
//	Возвращаемое значение:
//   Строка - Текст запроса.
Функция ТекстЗапросаРаспоряжениеНаОбеспечениеИОтгрузку(ТекстЗапросаТабЧасть, ИгнорироватьГотовностьКОбеспечению) Экспорт
	
	ТекстЗапросаОперации =
		"ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	НЕОПРЕДЕЛЕНО КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	ТабЧасть.Количество КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	ТабЧасть.ЗапланированныйРасходРаспределенногоЗапаса КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка = &Ссылка
		|		И ТабЧасть.Количество <> 0
		|		И ТабЧасть.ГотовКОтгрузке
		|		И ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.Отгрузить)
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ТабЧасть.КонтролируемаяПартия КАК Заказ,
		|	ДАТАВРЕМЯ(1,1,1) КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	0 КАК РезервироватьНаСкладе,
		|	0 КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	0 КАК КОбеспечению,
		|	0 КАК НеОбеспечивать,
		|	ТабЧасть.Количество КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	0 КАК РезервироватьНаСкладеИзЗаблокированных,
		|	0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	ТабЧасть.ЗапланированныйРасходРаспределенногоЗапаса КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка = &Ссылка
		|		И ТабЧасть.Количество <> 0
		|		И ТабЧасть.ГотовКОтгрузке
		|		И ТабЧасть.КонтролируемаяПартия <> ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|		И ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.Отгрузить)
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	ТабЧасть.Ссылка КАК Регистратор,
		|	ТабЧасть.Период КАК Период,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) КАК ВидДвижения,
		|	ТабЧасть.Номенклатура КАК Номенклатура,
		|	ТабЧасть.Характеристика КАК Характеристика,
		|	ТабЧасть.Склад КАК Склад,
		|	ТабЧасть.Назначение КАК Назначение,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия = ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА ТабЧасть.ЗаказНаОтгрузку
		|		ИНАЧЕ ТабЧасть.КонтролируемаяПартия
		|	КОНЕЦ КАК Заказ,
		|	ВЫБОР КОГДА ТабЧасть.ЖелаемаяДатаОтгрузки = НЕОПРЕДЕЛЕНО ТОГДА
		|				ДАТАВРЕМЯ(1,1,1)
		|			ИНАЧЕ
		|				ТабЧасть.ЖелаемаяДатаОтгрузки
		|		КОНЕЦ КАК ДатаСобытия,
		|	0 КАК ВНаличии,
		|	0 КАК Поступит,
		|	0 КАК Заказано,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия <> ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА 0
		|		КОГДА ТабЧасть.ВариантОбеспечения В(
		|				ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.СоСклада),
		|				ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.Отгрузить))
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК РезервироватьНаСкладе,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия <> ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА 0
		|		КОГДА ТабЧасть.ГотовКОбеспечению И ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.РезервироватьПоМереПоступления)
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК РезервироватьПоМереПоступления,
		|	0 КАК ОтложитьРезервирование,
		|	ВЫБОР
		|		КОГДА ТабЧасть.ГотовКОбеспечению И ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.КОбеспечению)
		|			ТОГДА ТабЧасть.Количество
		|			ИНАЧЕ 0
		|	КОНЕЦ КАК КОбеспечению,
		|	ВЫБОР
		|		КОГДА ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.НеТребуется)
		|				ИЛИ НЕ ТабЧасть.ГотовКОбеспечению И ТабЧасть.ВариантОбеспечения В(
		|						ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.РезервироватьПоМереПоступления),
		|						ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.КОбеспечению))
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК НеОбеспечивать,
		|	0 КАК ЗаблокированоВНаличии,
		|	0 КАК ЗаблокированоПоступит,
		|	0 КАК КРазблокировке,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия = ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА 0
		|		КОГДА ТабЧасть.ВариантОбеспечения В(
		|				ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.СоСклада),
		|				ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.Отгрузить))
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК РезервироватьНаСкладеИзЗаблокированных,
		|	ВЫБОР
		|		КОГДА ТабЧасть.КонтролируемаяПартия = ЗНАЧЕНИЕ(Справочник.КонтролируемыеПартии.ПустаяСсылка)
		|			ТОГДА 0
		|		КОГДА ТабЧасть.ГотовКОбеспечению И ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.РезервироватьПоМереПоступления)
		|			ТОГДА ТабЧасть.Количество
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	0 КАК ЗакрытьГрафикПоступления,
		|	ЛОЖЬ КАК ПроконтролироватьКачество,
		|	ЛОЖЬ КАК ПодлежитРазблокировке,
		|	0 КАК ЗакрытьГрафикОтгрузки,
		|	НЕОПРЕДЕЛЕНО КАК ОтгружаемыйЗаказ,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыЗаписейЗапасыИПотребности.ПервичнаяЗапись) КАК ТипЗаписи,
		|	ЛОЖЬ КАК Сторно
		|ИЗ
		|	#ТабЧасть КАК ТабЧасть
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ВсяНоменклатура
		|		ПО ВсяНоменклатура.Ссылка = ТабЧасть.Номенклатура
		|ГДЕ
		|	ТабЧасть.Ссылка = &Ссылка
		|		И ТабЧасть.Количество <> 0
		|		И НЕ (ТабЧасть.ГотовКОтгрузке
		|				И ТабЧасть.ВариантОбеспечения = ЗНАЧЕНИЕ(Перечисление.ВариантыОбеспечения.Отгрузить))
		|		И ВсяНоменклатура.ТипНоменклатуры В(
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара),
		|				ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))";
	
	Если ИгнорироватьГотовностьКОбеспечению Тогда
	
		ТекстЗапросаОперации = СтрЗаменить(ТекстЗапросаОперации, "ТабЧасть.ГотовКОбеспечению", "ИСТИНА");
	
	КонецЕсли;
	
	ТекстыЗапросовТаблицДокумента = Новый Структура;
	ТекстыЗапросовТаблицДокумента.Вставить("ТабЧасть", ТекстЗапросаТабЧасть);
	ПроведениеДокументов.ПодготовитьТекстЗапросаОтраженияДокумента(ТекстЗапросаОперации, ТекстыЗапросовТаблицДокумента);
	
	Возврат ТекстЗапросаОперации;
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

////////////////////////////////////////////////////////////////////////////////
// Процедуры и функции обслуживающие регистр накопления ЗапасыИПотребности

Процедура ПередЗаписьюДвиженийДокумента(Документ, МенеджерВременныхТаблиц, Отказ) Экспорт
	
	Набор = Документ.Движения.ЗапасыИПотребности;
	
	Если Набор.ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Запрос = Новый Запрос();
	Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц; // здесь есть временная таблица изменений регистра Реестр документов.
	
	// Для нового документа расчет и так идет по всем записям набора, так как все они есть в таблице изменений набора.
	ИзменилосьМестоВОчереди =
		Метаданные.ОпределяемыеТипы.ОжидаемаяОтгрузка.Тип.СодержитТип(ТипЗнч(Набор.Отбор.Регистратор.Значение))
		И Набор.ДополнительныеСвойства.Свойство("СвойстваДокумента") // при пакетном проведении этапов свойства документа могут быть не инициализированы.
		И Набор.ДополнительныеСвойства.СвойстваДокумента.РежимЗаписи = РежимЗаписиДокумента.Проведение
		И Не Набор.ДополнительныеСвойства.СвойстваДокумента.ЭтоНовый
		И ИзменилосьМестоВОчереди(Запрос);
		
	Если ИзменилосьМестоВОчереди Тогда
		Документ.ДополнительныеСвойства.Вставить("ИзменилосьМестоВОчередиРаспределенияЗапасов", Истина);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПослеЗаписиДвиженийДокумента(Документ, МенеджерВременныхТаблиц, Отказ) Экспорт
	
	Возврат; // в текущей конфигурации дополнительных действий не предусмотрено
	
КонецПроцедуры

Функция ТекстЗапросаИсходныхДвиженийЗапасыИПотребности(ПроверятьРегистраторИАктивность = Истина, ПроверятьНовый = Истина) Экспорт
	
	Текст =
		"ВЫБРАТЬ
		|	Таблица.Номенклатура КАК Номенклатура,
		|	Таблица.Характеристика КАК Характеристика,
		|	Таблица.Склад КАК Склад,
		|	Таблица.Назначение КАК Назначение,
		|	Таблица.Заказ КАК Заказ,
		|	Таблица.ОтгружаемыйЗаказ КАК ОтгружаемыйЗаказ,
		|	Таблица.ДатаСобытия КАК ДатаСобытия,
		|	Таблица.Сторно КАК Сторно,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.ВНаличии
		|		ИНАЧЕ -Таблица.ВНаличии
		|	КОНЕЦ КАК ВНаличии,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.Поступит
		|		ИНАЧЕ -Таблица.Поступит
		|	КОНЕЦ КАК Поступит,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.Заказано
		|		ИНАЧЕ -Таблица.Заказано
		|	КОНЕЦ КАК Заказано,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.РезервироватьНаСкладе
		|		ИНАЧЕ -Таблица.РезервироватьНаСкладе
		|	КОНЕЦ КАК РезервироватьНаСкладе,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.РезервироватьПоМереПоступления
		|		ИНАЧЕ -Таблица.РезервироватьПоМереПоступления
		|	КОНЕЦ КАК РезервироватьПоМереПоступления,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.ОтложитьРезервирование
		|		ИНАЧЕ -Таблица.ОтложитьРезервирование
		|	КОНЕЦ КАК ОтложитьРезервирование,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.КОбеспечению
		|		ИНАЧЕ -Таблица.КОбеспечению
		|	КОНЕЦ КАК КОбеспечению,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.НеОбеспечивать
		|		ИНАЧЕ -Таблица.НеОбеспечивать
		|	КОНЕЦ КАК НеОбеспечивать,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.ЗаблокированоВНаличии
		|		ИНАЧЕ -Таблица.ЗаблокированоВНаличии
		|	КОНЕЦ КАК ЗаблокированоВНаличии,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.ЗаблокированоПоступит
		|		ИНАЧЕ -Таблица.ЗаблокированоПоступит
		|	КОНЕЦ КАК ЗаблокированоПоступит,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.КРазблокировке
		|		ИНАЧЕ -Таблица.КРазблокировке
		|	КОНЕЦ КАК КРазблокировке,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.РезервироватьНаСкладеИзЗаблокированных
		|		ИНАЧЕ -Таблица.РезервироватьНаСкладеИзЗаблокированных
		|	КОНЕЦ КАК РезервироватьНаСкладеИзЗаблокированных,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.РезервироватьПоМереПоступленияИзЗаблокированных
		|		ИНАЧЕ -Таблица.РезервироватьПоМереПоступленияИзЗаблокированных
		|	КОНЕЦ КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.ЗакрытьГрафикПоступления
		|		ИНАЧЕ -Таблица.ЗакрытьГрафикПоступления
		|	КОНЕЦ КАК ЗакрытьГрафикПоступления,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) И Таблица.ПроконтролироватьКачество
		|			ТОГДА Таблица.ЗакрытьГрафикПоступления
		|		КОГДА Таблица.ПроконтролироватьКачество
		|			ТОГДА -Таблица.ЗакрытьГрафикПоступления
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК ЗакрытьГрафикПоступленияПроконтролироватьКачество,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) И Таблица.ПодлежитРазблокировке
		|			ТОГДА Таблица.ЗакрытьГрафикПоступления
		|		КОГДА Таблица.ПодлежитРазблокировке
		|			ТОГДА -Таблица.ЗакрытьГрафикПоступления
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК ЗакрытьГрафикПоступленияПодлежитРазблокировке,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.ЗакрытьГрафикОтгрузки
		|		ИНАЧЕ -Таблица.ЗакрытьГрафикОтгрузки
		|	КОНЕЦ КАК ЗакрытьГрафикОтгрузки,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
		|			ТОГДА Таблица.ВНаличии
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК ВНаличииРасход,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
		|			ТОГДА Таблица.ЗаблокированоВНаличии
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК ЗаблокированоВНаличииРасход,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.ВНаличии
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК ВНаличииПриход,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.ВНаличии
		|		ИНАЧЕ -Таблица.ВНаличии
		|	КОНЕЦ КАК ВНаличииМинусЗаблокировано,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
		|			ТОГДА Таблица.Поступит
		|		ИНАЧЕ -Таблица.Поступит
		|	КОНЕЦ КАК ПоступитМинусЗаблокировано,
		|	
		|	ВЫБОР
		|		КОГДА Таблица.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
		|			ТОГДА Таблица.ВНаличии
		|		ИНАЧЕ 0
		|	КОНЕЦ КАК ВНаличииРасходМинусЗаблокировано
		|ПОМЕСТИТЬ ЗапасыИПотребностиПередЗаписью
		|ИЗ
		|	РегистрНакопления.ЗапасыИПотребности КАК Таблица
		|ГДЕ
		|	Таблица.Регистратор В(&Регистратор)
		|		И НЕ &ЭтоНовый
		|		И Таблица.Активность";
	Если Не ПроверятьРегистраторИАктивность Тогда
		Текст = СтрЗаменить(Текст, "Таблица.Регистратор В(&Регистратор)", "ИСТИНА");
		Текст = СтрЗаменить(Текст, "Таблица.Активность", "ИСТИНА");
	КонецЕсли;
	Если Не ПроверятьНовый Тогда
		Текст = СтрЗаменить(Текст, "&ЭтоНовый", "ЛОЖЬ");
	КонецЕсли;
	
	Если ПолучитьФункциональнуюОпцию("ИспользоватьКонтрольКачестваТоваров") Тогда
		
		Тексты = Новый Массив();
		Текст = СтрЗаменить(Текст, "ЗапасыИПотребностиПередЗаписью", "ЗапасыИПотребностиПередЗаписьюПредварительный");
		Тексты.Добавить(Текст);
		
		Текст =
			"ВЫБРАТЬ
			|	Набор.Номенклатура КАК Номенклатура,
			|	Набор.Характеристика КАК Характеристика,
			|	Набор.Склад КАК Склад,
			|	Набор.Назначение КАК Назначение,
			|	Набор.Заказ КАК Заказ,
			|	Набор.ОтгружаемыйЗаказ КАК ОтгружаемыйЗаказ,
			|	Набор.ДатаСобытия КАК ДатаСобытия,
			|	Набор.Сторно КАК Сторно,
			|	СУММА(Набор.ВНаличии) КАК ВНаличии,
			|	СУММА(Набор.ЗаблокированоВНаличииСводно) КАК ЗаблокированоВНаличииСводно,
			|	СУММА(Набор.Поступит) КАК Поступит,
			|	СУММА(Набор.Заказано) КАК Заказано,
			|	СУММА(Набор.РезервироватьНаСкладе) КАК РезервироватьНаСкладе,
			|	СУММА(Набор.РезервироватьПоМереПоступления) КАК РезервироватьПоМереПоступления,
			|	СУММА(Набор.ОтложитьРезервирование) КАК ОтложитьРезервирование,
			|	СУММА(Набор.КОбеспечению) КАК КОбеспечению,
			|	СУММА(Набор.НеОбеспечивать) КАК НеОбеспечивать,
			|	СУММА(Набор.ЗаблокированоВНаличии) КАК ЗаблокированоВНаличии,
			|	СУММА(Набор.ЗаблокированоПоступит) КАК ЗаблокированоПоступит,
			|	СУММА(Набор.КРазблокировке) КАК КРазблокировке,
			|	СУММА(Набор.РезервироватьНаСкладеИзЗаблокированных) КАК РезервироватьНаСкладеИзЗаблокированных,
			|	СУММА(Набор.РезервироватьПоМереПоступленияИзЗаблокированных) КАК РезервироватьПоМереПоступленияИзЗаблокированных,
			|	СУММА(Набор.ЗакрытьГрафикПоступления) КАК ЗакрытьГрафикПоступления,
			|	СУММА(Набор.ЗакрытьГрафикПоступленияПроконтролироватьКачество) КАК ЗакрытьГрафикПоступленияПроконтролироватьКачество,
			|	СУММА(Набор.ЗакрытьГрафикПоступленияПодлежитРазблокировке) КАК ЗакрытьГрафикПоступленияПодлежитРазблокировке,
			|	СУММА(Набор.ЗакрытьГрафикОтгрузки) КАК ЗакрытьГрафикОтгрузки,
			|	СУММА(Набор.ВНаличииРасход) КАК ВНаличииРасход,
			|	СУММА(Набор.ЗаблокированоВНаличииРасход) КАК ЗаблокированоВНаличииРасход,
			|	СУММА(Набор.ВНаличииПриход) КАК ВНаличииПриход,
			|	
			|	ВЫБОР
			|		КОГДА СУММА(Набор.ВНаличии) > 0
			|				И СУММА(Набор.ВНаличии) < СУММА(Набор.ЗаблокированоВНаличииСводно)
			|			ТОГДА 0
			|		КОГДА СУММА(Набор.ВНаличии) > 0
			|				И СУММА(Набор.ЗаблокированоВНаличииСводно) > 0
			|			ТОГДА
			|				СУММА(Набор.ВНаличии) - СУММА(Набор.ЗаблокированоВНаличииСводно)
			|		КОГДА СУММА(Набор.ВНаличии) < 0
			|				И СУММА(Набор.ВНаличии) > СУММА(Набор.ЗаблокированоВНаличииСводно)
			|			ТОГДА 0
			|		КОГДА СУММА(Набор.ВНаличии) < 0
			|				И СУММА(Набор.ЗаблокированоВНаличииСводно) < 0
			|			ТОГДА СУММА(Набор.ВНаличии) - СУММА(Набор.ЗаблокированоВНаличииСводно)
			|		ИНАЧЕ СУММА(Набор.ВНаличии)
			|	КОНЕЦ КАК ВНаличииМинусЗаблокировано,
			|	
			|	ВЫБОР
			|		КОГДА СУММА(Набор.Поступит) > 0
			|				И СУММА(Набор.Поступит) < СУММА(Набор.ЗаблокированоПоступит)
			|			ТОГДА 0
			|		КОГДА СУММА(Набор.Поступит) > 0
			|				И СУММА(Набор.ЗаблокированоПоступит) > 0
			|			ТОГДА
			|				СУММА(Набор.Поступит) - СУММА(Набор.ЗаблокированоПоступит)
			|		КОГДА СУММА(Набор.Поступит) < 0
			|				И СУММА(Набор.Поступит) > СУММА(Набор.ЗаблокированоПоступит)
			|			ТОГДА 0
			|		КОГДА СУММА(Набор.Поступит) < 0
			|				И СУММА(Набор.ЗаблокированоПоступит) < 0
			|			ТОГДА СУММА(Набор.Поступит) - СУММА(Набор.ЗаблокированоПоступит)
			|		ИНАЧЕ СУММА(Набор.Поступит)
			|	КОНЕЦ КАК ПоступитМинусЗаблокировано,
			|	
			|	ВЫБОР
			|		КОГДА СУММА(Набор.ВНаличииРасход) > 0
			|				И СУММА(Набор.ВНаличииРасход) < СУММА(Набор.ЗаблокированоВНаличииРасходСводно)
			|			ТОГДА 0
			|		КОГДА СУММА(Набор.ВНаличииРасход) > 0
			|				И СУММА(Набор.ЗаблокированоВНаличииРасходСводно) > 0
			|			ТОГДА
			|				СУММА(Набор.ВНаличииРасход) - СУММА(Набор.ЗаблокированоВНаличииРасходСводно)
			|		КОГДА СУММА(Набор.ВНаличииРасход) < 0
			|				И СУММА(Набор.ВНаличииРасход) > СУММА(Набор.ЗаблокированоВНаличииРасходСводно)
			|			ТОГДА 0
			|		КОГДА СУММА(Набор.ВНаличииРасход) < 0
			|				И СУММА(Набор.ЗаблокированоВНаличииРасходСводно) < 0
			|			ТОГДА СУММА(Набор.ВНаличииРасход) - СУММА(Набор.ЗаблокированоВНаличииРасходСводно)
			|		ИНАЧЕ СУММА(Набор.ВНаличииРасход)
			|	КОНЕЦ КАК ВНаличииРасходМинусЗаблокировано
			|	
			|ПОМЕСТИТЬ ЗапасыИПотребностиПередЗаписью
			|ИЗ(
			|	ВЫБРАТЬ
			|		Таблица.Номенклатура КАК Номенклатура,
			|		Таблица.Характеристика КАК Характеристика,
			|		Таблица.Склад КАК Склад,
			|		Таблица.Назначение КАК Назначение,
			|		Таблица.Заказ КАК Заказ,
			|		Таблица.ОтгружаемыйЗаказ КАК ОтгружаемыйЗаказ,
			|		Таблица.ДатаСобытия КАК ДатаСобытия,
			|		Таблица.Сторно КАК Сторно,
			|		Таблица.ВНаличии КАК ВНаличии,
			|		Таблица.Поступит КАК Поступит,
			|		Таблица.Заказано КАК Заказано,
			|		Таблица.РезервироватьНаСкладе КАК РезервироватьНаСкладе,
			|		Таблица.РезервироватьПоМереПоступления КАК РезервироватьПоМереПоступления,
			|		Таблица.ОтложитьРезервирование КАК ОтложитьРезервирование,
			|		Таблица.КОбеспечению КАК КОбеспечению,
			|		Таблица.НеОбеспечивать КАК НеОбеспечивать,
			|		Таблица.ЗаблокированоВНаличии КАК ЗаблокированоВНаличии,
			|		Таблица.ЗаблокированоПоступит КАК ЗаблокированоПоступит,
			|		Таблица.КРазблокировке КАК КРазблокировке,
			|		Таблица.РезервироватьНаСкладеИзЗаблокированных КАК РезервироватьНаСкладеИзЗаблокированных,
			|		Таблица.РезервироватьПоМереПоступленияИзЗаблокированных КАК РезервироватьПоМереПоступленияИзЗаблокированных,
			|		Таблица.ЗакрытьГрафикПоступления КАК ЗакрытьГрафикПоступления,
			|		Таблица.ЗакрытьГрафикПоступленияПроконтролироватьКачество КАК ЗакрытьГрафикПоступленияПроконтролироватьКачество,
			|		Таблица.ЗакрытьГрафикПоступленияПодлежитРазблокировке КАК ЗакрытьГрафикПоступленияПодлежитРазблокировке,
			|		Таблица.ЗакрытьГрафикОтгрузки КАК ЗакрытьГрафикОтгрузки,
			|		Таблица.ВНаличииРасход КАК ВНаличииРасход,
			|		Таблица.ЗаблокированоВНаличииРасход КАК ЗаблокированоВНаличииРасход,
			|		Таблица.ВНаличииПриход КАК ВНаличииПриход,
			|		0 КАК ЗаблокированоВНаличииСводно,
			|		0 КАК ЗаблокированоВНаличииРасходСводно
			|	ИЗ
			|		ЗапасыИПотребностиПередЗаписьюПредварительный КАК Таблица
			|	
			|	ОБЪЕДИНИТЬ ВСЕ
			|	
			|	ВЫБРАТЬ
			|		Таблица.Номенклатура КАК Номенклатура,
			|		Таблица.Характеристика КАК Характеристика,
			|		Таблица.Склад КАК Склад,
			|		Таблица.Назначение КАК Назначение,
			|		НЕОПРЕДЕЛЕНО КАК Заказ,
			|		Таблица.ОтгружаемыйЗаказ КАК ОтгружаемыйЗаказ,
			|		Таблица.ДатаСобытия КАК ДатаСобытия,
			|		Таблица.Сторно КАК Сторно,
			|		0 КАК ВНаличии,
			|		0 КАК Поступит,
			|		0 КАК Заказано,
			|		0 КАК РезервироватьНаСкладе,
			|		0 КАК РезервироватьПоМереПоступления,
			|		0 КАК ОтложитьРезервирование,
			|		0 КАК КОбеспечению,
			|		0 КАК НеОбеспечивать,
			|		0 КАК ЗаблокированоВНаличии,
			|		0 КАК ЗаблокированоПоступит,
			|		0 КАК КРазблокировке,
			|		0 КАК РезервироватьНаСкладеИзЗаблокированных,
			|		0 КАК РезервироватьПоМереПоступленияИзЗаблокированных,
			|		0 КАК ЗакрытьГрафикПоступления,
			|		0 КАК ЗакрытьГрафикПоступленияПроконтролироватьКачество,
			|		0 КАК ЗакрытьГрафикПоступленияПодлежитРазблокировке,
			|		0 КАК ЗакрытьГрафикОтгрузки,
			|		0 КАК ВНаличииРасход,
			|		0 КАК ЗаблокированоВНаличииРасход,
			|		0 КАК ВНаличииПриход,
			|		Таблица.ЗаблокированоВНаличии КАК ЗаблокированоВНаличииСводно,
			|		Таблица.ЗаблокированоВНаличииРасход КАК ЗаблокированоВНаличииРасходСводно
			|	ИЗ
			|		ЗапасыИПотребностиПередЗаписьюПредварительный КАК Таблица) КАК Набор
			|СГРУППИРОВАТЬ ПО
			|	Набор.Номенклатура,
			|	Набор.Характеристика,
			|	Набор.Склад,
			|	Набор.Назначение,
			|	Набор.Заказ,
			|	Набор.ОтгружаемыйЗаказ,
			|	Набор.ДатаСобытия,
			|	Набор.Сторно";
		
		Тексты.Добавить(Текст);
		Тексты.Добавить("УНИЧТОЖИТЬ ЗапасыИПотребностиПередЗаписьюПредварительный");
		Текст = СтрСоединить(Тексты, ОбщегоНазначения.РазделительПакетаЗапросов());
		
	КонецЕсли;
	
	Возврат Текст;
	
КонецФункции

Функция ТекстЗапросаИзмененийДвиженийЗапасыИПотребности(ПроверятьРегистраторИАктивность, ИмяТаблицы = Неопределено) Экспорт
	
	Тексты = Новый Массив();
	
	Текст = ТекстЗапросаИсходныхДвиженийЗапасыИПотребности(ПроверятьРегистраторИАктивность, Ложь);
	Текст = СтрЗаменить(Текст, "ЗапасыИПотребностиПередЗаписью", "ЗапасыИПотребностиПриЗаписи");
	Если ИмяТаблицы <> Неопределено Тогда
		Текст = СтрЗаменить(Текст, "РегистрНакопления.ЗапасыИПотребности", ИмяТаблицы);
	КонецЕсли;
	Тексты.Добавить(Текст);
	
	Текст =
		"ВЫБРАТЬ
		|	ТаблицаИзменений.Номенклатура КАК Номенклатура,
		|	ТаблицаИзменений.Характеристика КАК Характеристика,
		|	ТаблицаИзменений.Склад КАК Склад,
		|	ТаблицаИзменений.Назначение КАК Назначение,
		|	ТаблицаИзменений.Заказ КАК Заказ,
		|	ТаблицаИзменений.ДатаСобытия КАК ДатаСобытия,
		|	МАКСИМУМ(ТаблицаИзменений.НаправлениеДеятельностиИзНазначения) КАК НаправлениеДеятельностиИзНазначения,
		|	МАКСИМУМ(ТаблицаИзменений.ЗаказИзНазначения) КАК ЗаказИзНазначения,
		|	МАКСИМУМ(ТаблицаИзменений.ПартнерИзНазначения) КАК ПартнерИзНазначения,
		|	МАКСИМУМ(ВЫБОР
		|		КОГДА ТаблицаИзменений.Назначение = ЗНАЧЕНИЕ(Справочник.Назначения.ПустаяСсылка)
		|			ТОГДА ЛОЖЬ
		//++ НЕ УТКА
		|		КОГДА ТИПЗНАЧЕНИЯ(ТаблицаИзменений.ЗаказИзНазначения) В(
		//++ Устарело_Переработка24
		|				ТИП(Документ.ЗаказДавальца),
		//-- Устарело_Переработка24
		|				ТИП(Документ.ЗаказДавальца2_5))
		|			ТОГДА ЛОЖЬ
		//-- НЕ УТКА
		
		//++ НЕ УТ
		|		КОГДА ТаблицаИзменений.ПартнерИзНазначения <> ЗНАЧЕНИЕ(Справочник.Партнеры.ПустаяСсылка)
		|			И ТаблицаИзменений.ЗаказИзНазначения = НЕОПРЕДЕЛЕНО
		|			ТОГДА ЛОЖЬ
		//-- НЕ УТ
		|		КОГДА ТаблицаИзменений.НаправлениеДеятельностиИзНазначения <> ЗНАЧЕНИЕ(Справочник.НаправленияДеятельности.ПустаяСсылка)
		|			И ТаблицаИзменений.ЗаказИзНазначения = НЕОПРЕДЕЛЕНО
		|			ТОГДА НЕ ТаблицаИзменений.КонтролироватьТолькоНаличие
		|		КОГДА &КонтролироватьПревышениеОбособленногоОбеспечения И ТаблицаИзменений.ЗаказИзНазначения <> НЕОПРЕДЕЛЕНО
		|			ТОГДА ИСТИНА
		|		ИНАЧЕ ЛОЖЬ
		|	КОНЕЦ) КАК КонтролироватьПревышениеОбособленногоОбеспечения,
		|	СУММА(ТаблицаИзменений.ВНаличии) КАК ВНаличии,
		|	СУММА(ТаблицаИзменений.Поступит) КАК Поступит,
		|	СУММА(ТаблицаИзменений.Заказано) КАК Заказано,
		|	СУММА(ТаблицаИзменений.РезервироватьНаСкладе) КАК РезервироватьНаСкладе,
		|	СУММА(ТаблицаИзменений.РезервироватьПоМереПоступления) КАК РезервироватьПоМереПоступления,
		|	СУММА(ТаблицаИзменений.ОтложитьРезервирование) КАК ОтложитьРезервирование,
		|	СУММА(ТаблицаИзменений.КОбеспечению) КАК КОбеспечению,
		|	СУММА(ТаблицаИзменений.НеОбеспечивать) КАК НеОбеспечивать,
		|	СУММА(ТаблицаИзменений.ЗаблокированоВНаличии) КАК ЗаблокированоВНаличии,
		|	СУММА(ТаблицаИзменений.ЗаблокированоПоступит) КАК ЗаблокированоПоступит,
		|	СУММА(ТаблицаИзменений.КРазблокировке) КАК КРазблокировке,
		|	СУММА(ТаблицаИзменений.РезервироватьНаСкладеИзЗаблокированных) КАК РезервироватьНаСкладеИзЗаблокированных,
		|	СУММА(ТаблицаИзменений.РезервироватьПоМереПоступленияИзЗаблокированных) КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|	СУММА(ТаблицаИзменений.ЗакрытьГрафикПоступления) КАК ЗакрытьГрафикПоступления,
		|	СУММА(ТаблицаИзменений.ЗакрытьГрафикПоступленияПроконтролироватьКачество) КАК ЗакрытьГрафикПоступленияПроконтролироватьКачество,
		|	СУММА(ТаблицаИзменений.ЗакрытьГрафикПоступленияПодлежитРазблокировке) КАК ЗакрытьГрафикПоступленияПодлежитРазблокировке,
		|	СУММА(ТаблицаИзменений.ЗакрытьГрафикОтгрузки) КАК ЗакрытьГрафикОтгрузки,
		|	СУММА(ТаблицаИзменений.ВНаличииРасход) КАК ВНаличииРасход,
		|	СУММА(ТаблицаИзменений.ЗаблокированоВНаличииРасход) КАК ЗаблокированоВНаличииРасход,
		|	СУММА(ТаблицаИзменений.ВНаличииПриход) КАК ВНаличииПриход,
		|	СУММА(ТаблицаИзменений.ВНаличииМинусЗаблокировано) КАК ВНаличииМинусЗаблокировано,
		|	СУММА(ТаблицаИзменений.ПоступитМинусЗаблокировано) КАК ПоступитМинусЗаблокировано,
		|	СУММА(ТаблицаИзменений.ВНаличииРасходМинусЗаблокировано) КАК ВНаличииРасходМинусЗаблокировано
		|ПОМЕСТИТЬ ЗапасыИПотребностиИзменение
		|ИЗ(
		|	ВЫБРАТЬ
		|		Таблица.Номенклатура КАК Номенклатура,
		|		Таблица.Характеристика КАК Характеристика,
		|		Таблица.Склад КАК Склад,
		|		Таблица.Назначение КАК Назначение,
		|		ВЫРАЗИТЬ(Таблица.Назначение КАК Справочник.Назначения).НаправлениеДеятельности КАК НаправлениеДеятельностиИзНазначения,
		|		ВЫРАЗИТЬ(Таблица.Назначение КАК Справочник.Назначения).Заказ КАК ЗаказИзНазначения,
		|		ВЫРАЗИТЬ(Таблица.Назначение КАК Справочник.Назначения).Партнер КАК ПартнерИзНазначения,
		|		ВЫРАЗИТЬ(Таблица.Назначение КАК Справочник.Назначения).КонтролироватьТолькоНаличие КАК КонтролироватьТолькоНаличие,
		|		ВЫБОР
		|			КОГДА Таблица.Заказ = НЕОПРЕДЕЛЕНО
		|				ТОГДА Таблица.ОтгружаемыйЗаказ
		|			ИНАЧЕ Таблица.Заказ
		|		КОНЕЦ КАК Заказ,
		|		Таблица.ДатаСобытия КАК ДатаСобытия,
		|		Таблица.ВНаличии КАК ВНаличии,
		|		Таблица.Поступит КАК Поступит,
		|		Таблица.Заказано КАК Заказано,
		|		Таблица.РезервироватьНаСкладе КАК РезервироватьНаСкладе,
		|		Таблица.РезервироватьПоМереПоступления КАК РезервироватьПоМереПоступления,
		|		Таблица.ОтложитьРезервирование КАК ОтложитьРезервирование,
		|		Таблица.КОбеспечению КАК КОбеспечению,
		|		Таблица.НеОбеспечивать КАК НеОбеспечивать,
		|		Таблица.ЗаблокированоВНаличии КАК ЗаблокированоВНаличии,
		|		Таблица.ЗаблокированоПоступит КАК ЗаблокированоПоступит,
		|		Таблица.КРазблокировке КАК КРазблокировке,
		|		Таблица.РезервироватьНаСкладеИзЗаблокированных КАК РезервироватьНаСкладеИзЗаблокированных,
		|		Таблица.РезервироватьПоМереПоступленияИзЗаблокированных КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|		Таблица.ЗакрытьГрафикПоступления КАК ЗакрытьГрафикПоступления,
		|		Таблица.ЗакрытьГрафикПоступленияПроконтролироватьКачество КАК ЗакрытьГрафикПоступленияПроконтролироватьКачество,
		|		Таблица.ЗакрытьГрафикПоступленияПодлежитРазблокировке КАК ЗакрытьГрафикПоступленияПодлежитРазблокировке,
		|		Таблица.ЗакрытьГрафикОтгрузки КАК ЗакрытьГрафикОтгрузки,
		|		Таблица.ВНаличииРасход КАК ВНаличииРасход,
		|		Таблица.ЗаблокированоВНаличииРасход КАК ЗаблокированоВНаличииРасход,
		|		Таблица.ВНаличииПриход КАК ВНаличииПриход,
		|		Таблица.ВНаличииМинусЗаблокировано КАК ВНаличииМинусЗаблокировано,
		|		Таблица.ПоступитМинусЗаблокировано КАК ПоступитМинусЗаблокировано,
		|		Таблица.ВНаличииРасходМинусЗаблокировано КАК ВНаличииРасходМинусЗаблокировано
		|	ИЗ
		|		ЗапасыИПотребностиПриЗаписи КАК Таблица
		|	
		|	ОБЪЕДИНИТЬ ВСЕ
		|	
		|	ВЫБРАТЬ
		|		Таблица.Номенклатура КАК Номенклатура,
		|		Таблица.Характеристика КАК Характеристика,
		|		Таблица.Склад КАК Склад,
		|		Таблица.Назначение КАК Назначение,
		|		ВЫРАЗИТЬ(Таблица.Назначение КАК Справочник.Назначения).НаправлениеДеятельности КАК НаправлениеДеятельностиИзНазначения,
		|		ВЫРАЗИТЬ(Таблица.Назначение КАК Справочник.Назначения).Заказ КАК ЗаказИзНазначения,
		|		ВЫРАЗИТЬ(Таблица.Назначение КАК Справочник.Назначения).Партнер КАК ПартнерИзНазначения,
		|		ВЫРАЗИТЬ(Таблица.Назначение КАК Справочник.Назначения).КонтролироватьТолькоНаличие КАК КонтролироватьТолькоНаличие,
		|		ВЫБОР
		|			КОГДА Таблица.Заказ = НЕОПРЕДЕЛЕНО
		|				ТОГДА Таблица.ОтгружаемыйЗаказ
		|			ИНАЧЕ Таблица.Заказ
		|		КОНЕЦ КАК Заказ,
		|		Таблица.ДатаСобытия КАК ДатаСобытия,
		|		-Таблица.ВНаличии КАК ВНаличии,
		|		-Таблица.Поступит КАК Поступит,
		|		-Таблица.Заказано КАК Заказано,
		|		-Таблица.РезервироватьНаСкладе КАК РезервироватьНаСкладе,
		|		-Таблица.РезервироватьПоМереПоступления КАК РезервироватьПоМереПоступления,
		|		-Таблица.ОтложитьРезервирование КАК ОтложитьРезервирование,
		|		-Таблица.КОбеспечению КАК КОбеспечению,
		|		-Таблица.НеОбеспечивать КАК НеОбеспечивать,
		|		-Таблица.ЗаблокированоВНаличии КАК ЗаблокированоВНаличии,
		|		-Таблица.ЗаблокированоПоступит КАК ЗаблокированоПоступит,
		|		-Таблица.КРазблокировке КАК КРазблокировке,
		|		-Таблица.РезервироватьНаСкладеИзЗаблокированных КАК РезервироватьНаСкладеИзЗаблокированных,
		|		-Таблица.РезервироватьПоМереПоступленияИзЗаблокированных КАК РезервироватьПоМереПоступленияИзЗаблокированных,
		|		-Таблица.ЗакрытьГрафикПоступления КАК ЗакрытьГрафикПоступления,
		|		-Таблица.ЗакрытьГрафикПоступленияПроконтролироватьКачество КАК ЗакрытьГрафикПоступленияПроконтролироватьКачество,
		|		-Таблица.ЗакрытьГрафикПоступленияПодлежитРазблокировке КАК ЗакрытьГрафикПоступленияПодлежитРазблокировке,
		|		-Таблица.ЗакрытьГрафикОтгрузки КАК ЗакрытьГрафикОтгрузки,
		|		-Таблица.ВНаличииРасход КАК ВНаличииРасход,
		|		-Таблица.ЗаблокированоВНаличииРасход КАК ЗаблокированоВНаличииРасход,
		|		-Таблица.ВНаличииПриход КАК ВНаличииПриход,
		|		-Таблица.ВНаличииМинусЗаблокировано КАК ВНаличииМинусЗаблокировано,
		|		-Таблица.ПоступитМинусЗаблокировано КАК ПоступитМинусЗаблокировано,
		|		-Таблица.ВНаличииРасходМинусЗаблокировано КАК ВНаличииРасходМинусЗаблокировано
		|	ИЗ
		|		ЗапасыИПотребностиПередЗаписью КАК Таблица) КАК ТаблицаИзменений
		|СГРУППИРОВАТЬ ПО
		|	ТаблицаИзменений.Номенклатура,
		|	ТаблицаИзменений.Характеристика,
		|	ТаблицаИзменений.Склад,
		|	ТаблицаИзменений.Назначение,
		|	ТаблицаИзменений.Заказ,
		|	ТаблицаИзменений.ДатаСобытия
		|ИМЕЮЩИЕ
		|	СУММА(ТаблицаИзменений.ВНаличии) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.Поступит) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.Заказано) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.РезервироватьНаСкладе) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.РезервироватьПоМереПоступления) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ОтложитьРезервирование) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.КОбеспечению) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.НеОбеспечивать) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ЗаблокированоВНаличии) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ЗаблокированоПоступит) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.КРазблокировке) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.РезервироватьНаСкладеИзЗаблокированных) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.РезервироватьПоМереПоступленияИзЗаблокированных) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ЗакрытьГрафикПоступления) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ЗакрытьГрафикПоступленияПроконтролироватьКачество) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ЗакрытьГрафикПоступленияПодлежитРазблокировке) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ЗакрытьГрафикОтгрузки) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ВНаличииРасход) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ЗаблокированоВНаличииРасход) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ВНаличииПриход) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ВНаличииМинусЗаблокировано) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ПоступитМинусЗаблокировано) <> 0
		|		ИЛИ СУММА(ТаблицаИзменений.ВНаличииРасходМинусЗаблокировано) <> 0";
		
	Тексты.Добавить(Текст);
	Текст = СтрСоединить(Тексты, ОбщегоНазначения.РазделительПакетаЗапросов());
	
	Возврат Текст;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Процедуры и функции для пакетной работы с регистром сведений "Распределение запасов"

////////////////////////////////////////////////////////////////////////////////
// Служебные

// Параметры:
//  Таблица - ТаблицаЗначений - таблица в которую нужно добавить колонки.
Процедура ДобавитьКолонкиРегистра(Таблица) Экспорт
	
	ТаблицаРегистра = РегистрыНакопления.ЗапасыИПотребности.СоздатьНаборЗаписей().ВыгрузитьКолонки(
		"Регистратор,ЗакрытьГрафикОтгрузки,ЗакрытьГрафикПоступления,Сторно");
	Если Таблица.Колонки.Найти("Регистратор") = Неопределено Тогда
		Таблица.Колонки.Добавить("Регистратор", ТаблицаРегистра.Колонки.Регистратор.ТипЗначения);
	КонецЕсли;
	Если Таблица.Колонки.Найти("ЗакрытьГрафикОтгрузки") = Неопределено Тогда
		Таблица.Колонки.Добавить("ЗакрытьГрафикОтгрузки", ТаблицаРегистра.Колонки.ЗакрытьГрафикОтгрузки.ТипЗначения);
	КонецЕсли;
	Если Таблица.Колонки.Найти("ЗакрытьГрафикПоступления") = Неопределено Тогда
		Таблица.Колонки.Добавить("ЗакрытьГрафикПоступления", ТаблицаРегистра.Колонки.ЗакрытьГрафикПоступления.ТипЗначения);
	КонецЕсли;
	Если Таблица.Колонки.Найти("Сторно") = Неопределено Тогда
		Таблица.Колонки.Добавить("Сторно", ТаблицаРегистра.Колонки.Сторно.ТипЗначения);
	КонецЕсли;
	
КонецПроцедуры

// Возвращаемое значение:
//  Структура - структура с полями:
//   * ТипыОжидаемоеПоступление - Массив из Тип -
Функция ЗначенияПараметровДляТекстовЗапросовПроведенияДокументов() Экспорт
	
	ЗначенияПараметров = Новый Структура;
	ЗначенияПараметров.Вставить("ТипыОжидаемоеПоступление",
		Метаданные.ОпределяемыеТипы.ОжидаемоеПоступление.Тип.Типы());
	
	Возврат ЗначенияПараметров;
	
КонецФункции

Процедура ОтразитьЗаданияКРаспределениюЗапасов(Документ, МенеджерВременныхТаблиц) Экспорт
	
	Если МенеджерВременныхТаблиц.Таблицы.Найти("ЗапасыИПотребностиИзменение") = Неопределено
			Или Документ.ДополнительныеСвойства.Свойство("БлокироватьРаспределениеЗапасов")
				И Документ.ДополнительныеСвойства.БлокироватьРаспределениеЗапасов Тогда
		Возврат;
	КонецЕсли;
	
	ОписаниеЗамера = ОценкаПроизводительности.НачатьЗамерДлительнойОперации("РаспределениеЗапасовДвижения.ОтразитьЗаданияКРаспределениюЗапасов");
	
	Если РаспределениеЗапасов.ДосчитыватьРегистрРегламентнымЗаданием() Тогда
		
		Текст =
			"ВЫБРАТЬ РАЗЛИЧНЫЕ
			|	ИзмененияДвижений.Номенклатура КАК Номенклатура,
			|	ИзмененияДвижений.Характеристика КАК Характеристика,
			|	ИзмененияДвижений.Склад КАК Склад,
			|	ИзмененияДвижений.Назначение КАК Назначение,
			|	НЕОПРЕДЕЛЕНО КАК ЗаказНаОтгрузку,
			|	0 КАК КСнятиюРезерва,
			|	0 КАК КСнятиюРезерваИзЗаблокированных
			|ИЗ
			|	ЗапасыИПотребностиИзменение КАК ИзмененияДвижений
			|ГДЕ
			|	НЕ ТИПЗНАЧЕНИЯ(ИзмененияДвижений.Заказ) В(&ТипыОжидаемаяОтгрузка)
			|
			|ОБЪЕДИНИТЬ ВСЕ
			|
			|ВЫБРАТЬ
			|	ИзмененияДвижений.Номенклатура КАК Номенклатура,
			|	ИзмененияДвижений.Характеристика КАК Характеристика,
			|	ИзмененияДвижений.Склад КАК Склад,
			|	ИзмененияДвижений.Назначение КАК Назначение,
			|	ИзмененияДвижений.Заказ КАК ЗаказНаОтгрузку,
			|	ВЫБОР
			|		КОГДА СУММА(ИзмененияДвижений.ВНаличииРасход + ИзмененияДвижений.РезервироватьНаСкладе) > 0
			|			ТОГДА СУММА(ИзмененияДвижений.ВНаличииРасход + ИзмененияДвижений.РезервироватьНаСкладе)
			|		ИНАЧЕ 0
			|	КОНЕЦ КАК КСнятиюРезерва,
			|	ВЫБОР
			|		КОГДА СУММА(ИзмененияДвижений.ЗаблокированоВНаличииРасход + ИзмененияДвижений.РезервироватьНаСкладеИзЗаблокированных) > 0
			|			ТОГДА СУММА(ИзмененияДвижений.ЗаблокированоВНаличииРасход + ИзмененияДвижений.РезервироватьНаСкладеИзЗаблокированных)
			|		ИНАЧЕ 0
			|	КОНЕЦ КАК КСнятиюРезерваИзЗаблокированных
			|ИЗ
			|	ЗапасыИПотребностиИзменение КАК ИзмененияДвижений
			|ГДЕ
			|	ТИПЗНАЧЕНИЯ(ИзмененияДвижений.Заказ) В(&ТипыОжидаемаяОтгрузка)
			|СГРУППИРОВАТЬ ПО
			|	ИзмененияДвижений.Номенклатура,
			|	ИзмененияДвижений.Характеристика,
			|	ИзмененияДвижений.Склад,
			|	ИзмененияДвижений.Назначение,
			|	ИзмененияДвижений.Заказ";
		
		Если Документ.Ссылка <> Неопределено И Документ.ДополнительныеСвойства.Свойство("ИзменилосьМестоВОчередиРаспределенияЗапасов") Тогда
			
			ТекстыЗапросов = Новый Массив();
			ТекстыЗапросов.Добавить(Текст);
			
			Текст =
				"ВЫБРАТЬ РАЗЛИЧНЫЕ
				|	Движения.Номенклатура КАК Номенклатура,
				|	Движения.Характеристика КАК Характеристика,
				|	Движения.Склад КАК Склад,
				|	Движения.Назначение КАК Назначение,
				|	Движения.Заказ КАК ЗаказНаОтгрузку,
				|	0 КАК КСнятиюРезерва,
				|	0 КАК КСнятиюРезерваИзЗаблокированных
				|ИЗ
				|	РегистрНакопления.ЗапасыИПотребности КАК Движения
				|ГДЕ
				|	Движения.Регистратор = &Регистратор
				|		И Движения.Активность";
				
			ТекстыЗапросов.Добавить(Текст);
			ТекстВложенногоЗапроса = СтрСоединить(ТекстыЗапросов, ОбщегоНазначенияУТ.РазделительЗапросовВОбъединении());
			Текст =
				"ВЫБРАТЬ
				|	Набор.Номенклатура КАК Номенклатура,
				|	Набор.Характеристика КАК Характеристика,
				|	Набор.Склад КАК Склад,
				|	Набор.Назначение КАК Назначение,
				|	Набор.ЗаказНаОтгрузку КАК ЗаказНаОтгрузку,
				|	МАКСИМУМ(Набор.КСнятиюРезерва) КАК КСнятиюРезерва,
				|	МАКСИМУМ(Набор.КСнятиюРезерваИзЗаблокированных) КАК КСнятиюРезерваИзЗаблокированных
				|ИЗ
				|	ТекстВложенногоЗапроса КАК Набор
				|СГРУППИРОВАТЬ ПО
				|	Набор.Номенклатура,
				|	Набор.Характеристика,
				|	Набор.Склад,
				|	Набор.Назначение,
				|	Набор.ЗаказНаОтгрузку";
			Текст = СтрЗаменить(Текст, "ТекстВложенногоЗапроса", СтрШаблон("(%1)", ТекстВложенногоЗапроса));
			
		КонецЕсли;
		
		Запрос = Новый Запрос();
		Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
		Запрос.УстановитьПараметр("Регистратор", Документ.Ссылка);
		Запрос.Текст = Текст;
		Запрос.УстановитьПараметр("ТипыОжидаемаяОтгрузка", Метаданные.ОпределяемыеТипы.ОжидаемаяОтгрузка.Тип.Типы());
		
		Таблица = Запрос.Выполнить().Выгрузить();
		Таблица.Колонки.Добавить("ДатаЗаписи");
		Таблица.ЗаполнитьЗначения(ТекущаяУниверсальнаяДата(), "ДатаЗаписи");
		//++ НЕ УТКА
		ОчередьРасчетаСтруктурыЗаказов = 0;
		Если Документ.ДополнительныеСвойства.Свойство("ОчередьРасчетаСтруктурыЗаказов", ОчередьРасчетаСтруктурыЗаказов) Тогда
			Таблица.Колонки.Добавить("ОчередьРасчетаСтруктурыЗаказов");
			Таблица.ЗаполнитьЗначения(ОчередьРасчетаСтруктурыЗаказов, "ОчередьРасчетаСтруктурыЗаказов");
		КонецЕсли;
		//-- НЕ УТКА
		
		ОтложенныеЗадания.ОбработатьЗаданияОчереди("ЗаданияКРаспределениюЗапасов", Таблица);
		
	Иначе
		
		Текст =
			"ВЫБРАТЬ РАЗЛИЧНЫЕ
			|	ИзмененияДвижений.Номенклатура КАК Номенклатура,
			|	ИзмененияДвижений.Характеристика КАК Характеристика,
			|	ИзмененияДвижений.Склад КАК Склад,
			|	ИзмененияДвижений.Назначение КАК Назначение
			|ПОМЕСТИТЬ ТоварыДляРаспределенияЗапасов
			|ИЗ
			|	ЗапасыИПотребностиИзменение КАК ИзмененияДвижений
			|ИНДЕКСИРОВАТЬ ПО
			|	Номенклатура,Характеристика,Склад,Назначение";
		
		Если Документ.Ссылка <> Неопределено И Документ.ДополнительныеСвойства.Свойство("ИзменилосьМестоВОчередиРаспределенияЗапасов") Тогда
			
			Текст =
				"ВЫБРАТЬ РАЗЛИЧНЫЕ
				|	ИзмененияДвижений.Номенклатура КАК Номенклатура,
				|	ИзмененияДвижений.Характеристика КАК Характеристика,
				|	ИзмененияДвижений.Склад КАК Склад,
				|	ИзмененияДвижений.Назначение КАК Назначение
				|ПОМЕСТИТЬ ТоварыДляРаспределенияЗапасов
				|ИЗ
				|	ЗапасыИПотребностиИзменение КАК ИзмененияДвижений
				|
				|ОБЪЕДИНИТЬ
				|
				|ВЫБРАТЬ РАЗЛИЧНЫЕ
				|	Движения.Номенклатура КАК Номенклатура,
				|	Движения.Характеристика КАК Характеристика,
				|	Движения.Склад КАК Склад,
				|	Движения.Назначение КАК Назначение
				|ИЗ
				|	РегистрНакопления.ЗапасыИПотребности КАК Движения
				|ГДЕ
				|	Движения.Регистратор = &Регистратор
				|		И Движения.Активность
				|ИНДЕКСИРОВАТЬ ПО
				|	Номенклатура,Характеристика,Склад,Назначение";
				
		КонецЕсли;
		
		Запрос = Новый Запрос();
		Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
		Запрос.УстановитьПараметр("Регистратор", Документ.Ссылка);
		Запрос.Текст = Текст;
		Запрос.Выполнить();
		
		ЗапросСостояний = Новый Запрос();
		ЗапросСостояний.МенеджерВременныхТаблиц =  МенеджерВременныхТаблиц;
		РаспределениеЗапасов.ДобавитьВременныеТаблицыАктуализацииСостояний(ЗапросСостояний, "ТоварыДляРаспределенияЗапасов", "ПередЗаписью");
		
		Запрос.Текст =
			"ВЫБРАТЬ
			|	ИзмененияДвижений.Номенклатура КАК Номенклатура,
			|	ИзмененияДвижений.Характеристика КАК Характеристика,
			|	ИзмененияДвижений.Склад КАК Склад,
			|	ИзмененияДвижений.Назначение КАК Назначение,
			|	ИзмененияДвижений.Заказ КАК ЗаказНаОтгрузку,
			|	СУММА(ИзмененияДвижений.ВНаличииРасход + ИзмененияДвижений.РезервироватьНаСкладе) КАК Отгрузка
			|ПОМЕСТИТЬ ИзменениеОстаткаПоТоварамДляКонтроляОстатков
			|ИЗ
			|	ЗапасыИПотребностиИзменение КАК ИзмененияДвижений
			|СГРУППИРОВАТЬ ПО
			|	ИзмененияДвижений.Номенклатура,
			|	ИзмененияДвижений.Характеристика,
			|	ИзмененияДвижений.Склад,
			|	ИзмененияДвижений.Назначение,
			|	ИзмененияДвижений.Заказ
			|ИМЕЮЩИЕ
			|	СУММА(ИзмененияДвижений.ВНаличииРасход + ИзмененияДвижений.РезервироватьНаСкладе) > 0
			|ИНДЕКСИРОВАТЬ ПО
			|	Номенклатура, Характеристика, Склад, Назначение, Заказ";
		Запрос.Выполнить();
		РаспределениеЗапасов.ЗаписатьРаспределениеЗапасовВТранзакцииПроведения(Запрос);
		Запрос.Текст = "УНИЧТОЖИТЬ ИзменениеОстаткаПоТоварамДляКонтроляОстатков";
		Запрос.Выполнить();
		
		РаспределениеЗапасов.ДобавитьВременныеТаблицыАктуализацииСостояний(ЗапросСостояний, "ТоварыДляРаспределенияЗапасов", "ПриЗаписи");
		РаспределениеЗапасов.ТаблицаИзмененийРаспределениеЗапасовДляРасчетаСостояний(ЗапросСостояний);
		
	КонецЕсли;
	
	ОценкаПроизводительности.ЗакончитьЗамерДлительнойОперации(ОписаниеЗамера, 1);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ДобавитьПустуюТаблицуИзменениеОстаткаПоТоварамДляКонтроляОстатков(Запрос) Экспорт
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	NULL КАК Номенклатура,
		|	NULL КАК Характеристика,
		|	NULL КАК Склад,
		|	NULL КАК Назначение,
		|	NULL КАК ЗаказНаОтгрузку,
		|	0 КАК Отгрузка
		|ПОМЕСТИТЬ ИзменениеОстаткаПоТоварамДляКонтроляОстатков
		|ГДЕ
		|	ЛОЖЬ";
	Запрос.Выполнить();
	
КонецПроцедуры

Функция ТекстыШаблоновОтражения(Регистры, ТекстЗапросаОперации, ИмяРегистра)
	
	ТекстыШаблоновОтражения = Новый Структура();
	Если ПроведениеДокументов.ТребуетсяТаблицаДляДвижений(ИмяРегистра, Регистры) Тогда
		ТекстыШаблоновОтражения.Вставить(ИмяРегистра, ТекстЗапросаОперации);
	КонецЕсли;
	Возврат ТекстыШаблоновОтражения;
	
КонецФункции

Функция ИзменилосьМестоВОчереди(Запрос)
	
	Результат = Ложь;
	Если Запрос.МенеджерВременныхТаблиц.Таблицы.Найти("РеестрДокументовИзменения") <> Неопределено Тогда
		
		Запрос.Текст =
			"ВЫБРАТЬ ПЕРВЫЕ 1
			|	ИСТИНА КАК ИзменилосьМестоВОчереди
			|ИЗ
			|	РеестрДокументовИзменения КАК Таблица
			|ГДЕ
			|	НЕ Таблица.ДополнительнаяЗаписьДоИзменения
			|		ИЛИ НЕ Таблица.ДополнительнаяЗапись
			|СГРУППИРОВАТЬ ПО
			|	Таблица.Ссылка
			|ИМЕЮЩИЕ
			|	НЕ МАКСИМУМ(Таблица.ПриоритетДоИзменения) ЕСТЬ NULL
			|		И НЕ МАКСИМУМ(Таблица.Приоритет) ЕСТЬ NULL
			|		И (МАКСИМУМ(Таблица.ПриоритетДоИзменения) <> МАКСИМУМ(Таблица.Приоритет)
			|			ИЛИ МАКСИМУМ(Таблица.ДатаДокументаИБ) <> МИНИМУМ(Таблица.ДатаДокументаИБ))";
		Результат = Не Запрос.Выполнить().Пустой();
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти
