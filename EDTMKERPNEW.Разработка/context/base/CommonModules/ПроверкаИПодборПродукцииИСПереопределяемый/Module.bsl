#Область СлужебныйПрограммныйИнтерфейс

// Получает сформированный ранее Акт о расхождениях для переданного документа.
// 
// Параметры:
//  Документ         - ДокументСсылка - ссылка на документ, для которого необходимо получить Акт о расхождениях:
//  АктОРасхождениях - ДокументСсылка - ссылка на Акт о расхождениях:
Процедура ПолучитьСформированныйАктОРасхождениях(Документ, АктОРасхождениях) Экспорт
	
	АктОРасхождениях = ИнтеграцияИСМПУТ.СформированныйАктОРасхождениях(Документ);
	
КонецПроцедуры

// Заполняет через третий параметр таблицу товаров документа (для определенного сценария),
//   либо дополняет ее значениями GTIN.
//
// Параметры:
//  Контекст  - ФормаКлиентскогоПриложения, ДокументСсылка - документ, маркируемую продукцию которого необходимо получить.
//  Сценарий  - Число - номер сценария заполнения.
//  ТаблицаПродукции - См. ПроверкаИПодборПродукцииИС.ТаблицаПродукцииДокумента
Процедура ПриОпределенииПродукцииДокумента(Контекст, Сценарий, ТаблицаПродукции) Экспорт
	
	ПроверкаИПодборПродукцииИСМПУТ.ЗаполнитьПродукциюДокумента(Контекст, ТаблицаПродукции, Сценарий);
	
КонецПроцедуры

// Обработать изменения в табличной части Подобранная продукция формы проверки.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - Форма проверки и подбора:
//   * ПараметрыСканирования - Структура - Параметры сканирования:
//     ** Склад - ОпределяемыйТип.Склад - Склад
//   * ЕГАИС - Булево - доступна проверка продукции ЕГАИС
//   * ИСМП - Булево - доступна проверка продукции ГИСМТ
//  СтрокиПодобраннойПродукции - Соответствие из ДанныеФормыЭлементКоллекции - Строки подобранной продукции
//  Действия - Соответствие из Строка - Требуемые действия (для обработки прикладным решением,
// может содержать элементы ОбработатьИзменениеНоменклатуры, ОбработатьИзменениеКоличества).
//  КэшированныеЗначения - Произвольный - Кэшированные значения
//  ПараметрыУказанияСерий - Произвольный - Параметры указания серий
Процедура ОбработатьПодобраннуюПродукцию(
	Форма, СтрокиПодобраннойПродукции, Действия, КэшированныеЗначения, ПараметрыУказанияСерий) Экспорт
	
	Если Действия["ОбработатьИзменениеНоменклатуры"] = Истина Тогда
		
		ПараметрыУказанияСерийКопия = ОбщегоНазначения.СкопироватьРекурсивно(ПараметрыУказанияСерий, Ложь);
		Поле = ПараметрыУказанияСерийКопия.ИменаПолейДополнительные.Найти("КоличествоПодобрано");
		Если Поле <> Неопределено Тогда
			ПараметрыУказанияСерийКопия.ИменаПолейДополнительные.Удалить(Поле);
		КонецЕсли;
		ИмяТабличнойЧасти = ПроверкаИПодборПродукцииИСМПУТ.ИмяТабличнойЧасти(Форма.ПараметрыСканирования);
		ПараметрыУказанияСерийКопия.ИмяТЧТовары = ИмяТабличнойЧасти;
		ПараметрыУказанияСерийКопия.ИмяТЧСерии = ИмяТабличнойЧасти;
		
		ПроверкаИПодборПродукцииИСМПУТ.ПроверитьДополнитьКолонкиСтрокиПодобраннойПродукции(Форма);
		
		ОбработчикЗаполнитьПризнакМаркируемаяПродукция = "ЗаполнитьПризнакМаркируемаяПродукция";
		ИсточникНастроек = Форма;
		Если ТипЗнч(Форма) = Тип("Структура") Тогда
			ИсточникНастроек = Форма.ПараметрыСканирования;
		КонецЕсли;
		Если Не ИсточникНастроек.ИСМП Тогда
			ОбработчикЗаполнитьПризнакМаркируемаяПродукция = "ЗаполнитьПризнакМаркируемаяПродукцияЕГАИС";
		ИначеЕсли Не ИсточникНастроек.ЕГАИС Тогда
			ОбработчикЗаполнитьПризнакМаркируемаяПродукция = "ЗаполнитьПризнакМаркируемаяПродукцияГИСМТ";
		КонецЕсли;
		
		СтруктураДействий = Новый Структура;
		СтруктураДействий.Вставить("ЗаполнитьПризнакХарактеристикиИспользуются",
			Новый Структура("Номенклатура", "ХарактеристикиИспользуются"));
		СтруктураДействий.Вставить("ЗаполнитьПризнакТипНоменклатуры",
			Новый Структура("Номенклатура", "ТипНоменклатуры"));
		СтруктураДействий.Вставить("ЗаполнитьПризнакТребуетВзвешивания",
			Новый Структура("Номенклатура", "ТребуетВзвешивания"));
		СтруктураДействий.Вставить("ЗаполнитьПризнакПроизвольнаяЕдиницаУчета",
			Новый Структура("Номенклатура", "ПроизвольнаяЕдиницаУчета"));
		СтруктураДействий.Вставить("ЗаполнитьПризнакСкоропортящаясяПродукция",
			Новый Структура("Номенклатура", "СкоропортящаясяПродукция"));
		СтруктураДействий.Вставить(ОбработчикЗаполнитьПризнакМаркируемаяПродукция,
			Новый Структура("Номенклатура", "МаркируемаяПродукция"));
		СтруктураДействий.Вставить("ЗаполнитьВидПродукцииИС",
			Новый Структура("Номенклатура", "ВидПродукции"));
		СтруктураДействий.Вставить("ПроверитьСериюРассчитатьСтатус",
			Новый Структура("Склад, ПараметрыУказанияСерий", Форма.ПараметрыСканирования.Склад, ПараметрыУказанияСерийКопия));
		
		МассивСтрок = Новый Массив;
		Для Каждого ЭлементСоответствия Из СтрокиПодобраннойПродукции Цикл
			МассивСтрок.Добавить(ЭлементСоответствия.Ключ);
		КонецЦикла;
		ПакетнаяОбработкаТабличнойЧастиСервер.ОбработатьСтрокиТЧ(
			МассивСтрок, СтруктураДействий, Форма.ПодобраннаяМаркируемаяПродукция);
	КонецЕсли;
	
КонецПроцедуры

#Область ЗавершениеПроверкиИПодбора

// Вызывается при заверешении проверки и подбора для отражения данных в документе.
// Предназначена для сохранения связей номенклатуры с GTIN, с учетом коээффициентов групповых упаковок. 
// 
// Параметры:
//  ТаблицаОписанияGTIN - см. ПроверкаИПодборПродукцииИС.ПустаяТаблицаОписанияGTIN.
Процедура СохранитьШтрихкодыНоменклатуры(ТаблицаОписанияGTIN) Экспорт
	
	ИнтеграцияИСМПУТ.ЗафиксироватьОписаниеGTIN(ТаблицаОписанияGTIN);
	
КонецПроцедуры

// Отражает результаты проверки и подбора в документе, из которого была вызвана соответствующая форма.
// 
// Параметры:
// 	ПараметрыОкончанияПроверки - Структура
Процедура ЗафиксироватьРезультатПроверкиИПодбора(ПараметрыОкончанияПроверки) Экспорт
	
	ИнтеграцияИСМПУТ.ОтразитьРезультатыСканированияВДокументе(ПараметрыОкончанияПроверки);
	
КонецПроцедуры

#КонецОбласти

#Область ПараметрыИнтеграцииФормыПроверкиИПодбора

// Заполняет специфику интеграции формы проверки и подбора в конкретную форму.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - форма для которой настраиваются параметры интеграции:
//  *Объект - ДокументОбъект
//  ПараметрыИнтеграции - См. ПроверкаИПодборПродукцииИС.ПараметрыИнтеграцииФормПроверкиИПодбора.
//
Процедура ПриОпределенииПараметровИнтеграцииФормыПроверкиИПодбора(Форма, ПараметрыИнтеграции) Экспорт
	
	ЭлементНастроек = ПроверкаИПодборПродукцииИС.ПараметрыИнтеграцииФормПроверкиИПодбора();
	
	Если ПолучитьФункциональнуюОпцию("ИспользоватьПередачиТоваровМеждуОрганизациями") Тогда
		ЭлементНастроек.Вставить("ИспользоватьПередачиТоваровМеждуОрганизациями");
	КонецЕсли;
	
	Если ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "РеализацияТоваровУслуг") Тогда
	
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ДоступноЧастичноеВыбытие                   = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ОтображатьОшибкиПроверкиСредствамиККТ      = Истина;
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ВозвратТоваровПоставщику")
		Или ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ОтгрузкаТоваровСХранения")
	Тогда
	
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ОтображатьОшибкиПроверкиСредствамиККТ      = Истина;
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "РасходныйОрдерНаТовары") Тогда
	
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ОтгружаемыеТоварыПодменюЗаполнить";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		
		ЭлементНастроек.ИмяТабличнойЧастиТовары                    = "ОтгружаемыеТовары";
		ЭлементНастроек.ИмяЭлементаФормыТовары                     = "ОтгружаемыеТовары";
		ЭлементНастроек.ИмяСледующейКолонки                        = "ОтгружаемыеТоварыНоменклатураАртикул";
		ЭлементНастроек.ИмяТабличнойЧастиСерии                     = "";
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ОтгружаемыеТоварыПодменюЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ОтборРазмещениеТоваров") Тогда
	
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТоварыОтбор";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		
		ЭлементНастроек.ИмяТабличнойЧастиТовары                    = "ТоварыОтбор";
		ЭлементНастроек.ИмяЭлементаФормыТовары                     = "ТоварыОтбор";
		ЭлементНастроек.ИмяСледующейКолонки                        = "ТоварыОтборНоменклатура";
		ЭлементНастроек.ИмяПоследующегоЭлементаФормы               = "ТоварыОтбор";
		ЭлементНастроек.ИмяТабличнойЧастиСерии                     = "";
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыОтбор.КоманднаяПанель.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПриходныйОрдерНаТовары") Тогда
	
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ЗаполнитьПоРаспоряжению";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Ложь;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		
		ЭлементНастроек.ИмяТабличнойЧастиТовары                    = "Товары";
		ЭлементНастроек.ИмяЭлементаФормыТовары                     = "Товары";
		ЭлементНастроек.ИмяСледующейКолонки                        = "ТоварыНоменклатураТипНоменклатуры";
		ЭлементНастроек.ИмяПоследующегоЭлементаФормы               = "ГруппаРежимПросмотраНавигация";
		ЭлементНастроек.ИмяТабличнойЧастиСерии                     = "";
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = "ЗаполнитьПоРаспоряжению";
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли СтрНачинаетсяС(Форма.ИмяФормы,"Документ.КорректировкаРеализации.Форма.ФормаДокументаДоВводаОстатков") Тогда
		
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.ИмяТабличнойЧастиСерии                     = "";
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ГруппаТаблицаКорректировкиЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "КорректировкаРеализации") Тогда
		
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ДоступноЧастичноеВыбытие                   = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = ЗначениеЗаполнено(Форма.Объект.Склад);
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ИмяТабличнойЧастиСерии                     = "";
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.КорректировкаРеализации);
		ЭлементНастроек.ОтображатьОшибкиПроверкиСредствамиККТ      = Истина;
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ГруппаТоварыЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ВозвратТоваровОтКлиента") Тогда
		
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.ВозвратТоваровОтКлиента);
		ЭлементНастроек.ЭтоДокументПриобретения                    = Истина;
		ЭлементНастроек.ЕстьЭлектронныйДокумент                    = ЭлектронноеВзаимодействиеИСМП.ДокументСвязанСЭлектронным(Форма.Объект.Ссылка);
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Не ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.ДоступноЧастичноеВыбытие                   = Истина;
		ЭлементНастроек.ИмяСледующейКолонки                        = "ТоварыНомерСтроки";
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Не ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.ОтображатьОшибкиПроверкиСредствамиККТ      = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПриобретениеТоваровУслуг") Тогда
		
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ЭтоДокументПриобретения                    = Истина;
		ЭлементНастроек.ЕстьЭлектронныйДокумент                    = ЭлектронноеВзаимодействиеИСМП.ДокументСвязанСЭлектронным(Форма.Объект.Ссылка);
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Не ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПриемкаТоваровНаХранение") Тогда
		
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Не ЗначениеЗаполнено(Форма.Объект.ИсправляемыйДокумент);
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ЭтоДокументПриобретения                    = Истина;
		ЭлементНастроек.ЕстьЭлектронныйДокумент                    = ЭлектронноеВзаимодействиеИСМП.ДокументСвязанСЭлектронным(Форма.Объект.Ссылка);
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = ЭлементНастроек.РазмещатьЭлементыИнтерфейса И Не ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПоступлениеТоваровОтКлиента") Тогда
		
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Не ЗначениеЗаполнено(Форма.Объект.ИсправляемыйДокумент);
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ЭтоДокументПриобретения                    = Истина;
		ЭлементНастроек.ЕстьЭлектронныйДокумент                    = ЭлектронноеВзаимодействиеИСМП.ДокументСвязанСЭлектронным(Форма.Объект.Ссылка);
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = ЭлементНастроек.РазмещатьЭлементыИнтерфейса И Не ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.ИмяЭлементаФормыТовары                     = "ТоварыГруппаРанееОтгруженныйТовар";
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПоступлениеТоваровОтХранителя") Тогда
		
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Не ЗначениеЗаполнено(Форма.Объект.ИсправляемыйДокумент);
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ЭтоДокументПриобретения                    = Истина;
		ЭлементНастроек.ЕстьЭлектронныйДокумент                    = ЭлектронноеВзаимодействиеИСМП.ДокументСвязанСЭлектронным(Форма.Объект.Ссылка);
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = ЭлементНастроек.РазмещатьЭлементыИнтерфейса И Не ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = ЭлементНастроек.ЕстьЭлектронныйДокумент;
		ЭлементНастроек.ИмяЭлементаФормыТовары                     = "ТоварыГруппаРанееОтгруженныйТовар";
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли СтрНачинаетсяС(Форма.ИмяФормы,"Документ.ЧекККМ.Форма.ФормаДокументаРМК")
		Или СтрНачинаетсяС(Форма.ИмяФормы,"Документ.ЧекККМВозврат.Форма.ФормаДокументаРМК") Тогда
		
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ДоступноЧастичноеВыбытие                   = Истина;
		ЭлементНастроек.ИспользоватьСтатусПроверкиПодбораДокумента = Ложь;
		ЭлементНастроек.ИмяТабличнойЧастиШтрихкодыУпаковок         = "АкцизныеМарки";
		ЭлементНастроек.ИмяКолонкиШтрихкодУпаковки                 = "АкцизнаяМарка";
		ЭлементНастроек.ОтображатьОшибкиПроверкиСредствамиККТ      = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПрочееОприходованиеТоваров") Тогда
		
		//++ НЕ УТ
		Возврат;
		//-- НЕ УТ
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = ОбщегоНазначенияИС.ЕстьПродукцияТребующаяВзвешивания()
			И ИнтеграцияИСМПКлиентСерверПовтИсп.НастройкиСканированияКодовМаркировки().ЗапрашиватьКоличествоМерногоТовара;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.ПрочееОприходованиеТоваров);
		ЭлементНастроек.ЭтоДокументПриобретения                    = Ложь;
		ЭлементНастроек.ЕстьЭлектронныйДокумент                    = Ложь;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
//++ НЕ УТ
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПроизводствоБезЗаказа") Тогда
		
		Сценарий = ОбщегоНазначения.СкопироватьРекурсивно(ЭлементНастроек);
		Сценарий.ИмяРодительскойГруппыФормы                 = "ГруппаПродукция";
		Сценарий.ИмяПоследующегоЭлементаФормы               = "ВыходныеИзделия";
		Сценарий.ИмяТабличнойЧастиТовары                    = "ВыходныеИзделия";
		Сценарий.ИмяТабличнойЧастиСерии                     = "ВыходныеИзделияСерии";
		Сценарий.РазмещатьЭлементыИнтерфейса                = ОбщегоНазначенияИС.ЕстьПродукцияТребующаяВзвешивания()
			И ИнтеграцияИСМПКлиентСерверПовтИсп.НастройкиСканированияКодовМаркировки().ЗапрашиватьКоличествоМерногоТовара;
		Сценарий.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.ПроизводствоБезЗаказа);
		Сценарий.ЭтоДокументПриобретения                    = Ложь;
		Сценарий.ЕстьЭлектронныйДокумент                    = Ложь;
		Сценарий.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		Сценарий.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		Сценарий.ИмяКоманднойПанели                         = Форма.Элементы.ВыходныеИзделияГруппаЗаполнить.Имя;
		ПараметрыИнтеграции.Добавить(Сценарий);
		
		Сценарий = ОбщегоНазначения.СкопироватьРекурсивно(ЭлементНастроек);
		Сценарий.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		Сценарий.ИспользоватьБезМаркируемойПродукции        = Истина;
		Сценарий.ИмяРодительскойГруппыФормы                 = "ГруппаМатериалыИРаботы";
		Сценарий.ИмяПоследующегоЭлементаФормы               = "МатериалыИРаботы";
		Сценарий.ИмяТабличнойЧастиТовары                    = "МатериалыИРаботы";
		Сценарий.ИмяЭлементаФормыТовары                     = "МатериалыИРаботы";
		Сценарий.ИмяСледующейКолонки                        = "МатериалыИРаботыНоменклатура";
		Сценарий.ИмяТабличнойЧастиСерии                     = "";
		Сценарий.ИмяТабличнойЧастиШтрихкодыУпаковок         = "ШтрихкодыУпаковокМатериалы";
		Сценарий.РазмещатьЭлементыИнтерфейса                = Истина;
		Сценарий.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.ПроизводствоБезЗаказа);
		Сценарий.ЭтоДокументПриобретения                    = Ложь;
		Сценарий.ЕстьЭлектронныйДокумент                    = Ложь;
		Сценарий.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		Сценарий.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		Сценарий.Сценарий                                   = 1;
		Сценарий.ИмяКоманднойПанели                         = Форма.Элементы.МатериалыИРаботыЗаполнить.Имя;
		ПараметрыИнтеграции.Добавить(Сценарий);
		
//-- НЕ УТ

//++ НЕ УТКА

	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ЭтапПроизводства2_2") Тогда
		
		Сценарий = ОбщегоНазначения.СкопироватьРекурсивно(ЭлементНастроек);
		Сценарий.ИмяРодительскойГруппыФормы                 = "ГруппаПродукция";
		Сценарий.ИмяПоследующегоЭлементаФормы               = "ГруппаВыходныеИзделия";
		Сценарий.ИмяТабличнойЧастиТовары                    = "ВыходныеИзделия";
		Сценарий.ИмяТабличнойЧастиСерии                     = "ВыходныеИзделияСерии";
		Сценарий.РазмещатьЭлементыИнтерфейса                = ОбщегоНазначенияИС.ЕстьПродукцияТребующаяВзвешивания()
			И ИнтеграцияИСМПКлиентСерверПовтИсп.НастройкиСканированияКодовМаркировки().ЗапрашиватьКоличествоМерногоТовара;
		Сценарий.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.ЭтапПроизводства2_2);
		Сценарий.ЭтоДокументПриобретения                    = Ложь;
		Сценарий.ЕстьЭлектронныйДокумент                    = Ложь;
		Сценарий.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		Сценарий.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		Сценарий.ИмяКоманднойПанели                         = Форма.Элементы.ВыходныеИзделия_Заполнить.Имя;
		ПараметрыИнтеграции.Добавить(Сценарий);
		
		Сценарий = ОбщегоНазначения.СкопироватьРекурсивно(ЭлементНастроек);
		Сценарий.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		Сценарий.ИспользоватьБезМаркируемойПродукции        = Истина;
		Сценарий.ИмяРодительскойГруппыФормы                 = "ТаблицаИзрасходованныеМатериалы";
		Сценарий.ИмяПоследующегоЭлементаФормы               = "РасходМатериаловИРабот";
		Сценарий.ИмяТабличнойЧастиТовары                    = "РасходМатериаловИРабот";
		Сценарий.ИмяЭлементаФормыТовары                     = "РасходМатериаловИРабот";
		Сценарий.ИмяСледующейКолонки                        = "РасходМатериаловИРаботНоменклатура";
		Сценарий.ИмяТабличнойЧастиСерии                     = ""; 
		Сценарий.ИмяТабличнойЧастиШтрихкодыУпаковок         = "ШтрихкодыУпаковокМатериалы";
		Сценарий.РазмещатьЭлементыИнтерфейса                = Истина;
		Сценарий.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.ЭтапПроизводства2_2);
		Сценарий.ЭтоДокументПриобретения                    = Ложь;
		Сценарий.ЕстьЭлектронныйДокумент                    = Ложь;
		Сценарий.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		Сценарий.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		Сценарий.Сценарий                                   = 1;
		Сценарий.ИмяКоманднойПанели                         = Форма.Элементы.РасходМатериаловИРаботГруппаЗаполнить.Имя;
		ПараметрыИнтеграции.Добавить(Сценарий);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПроизводственнаяОперация2_2") Тогда
		
		Сценарий = ОбщегоНазначения.СкопироватьРекурсивно(ЭлементНастроек);
		Сценарий.ИмяРодительскойГруппыФормы                 = "СтраницаВыходныеИзделия";
		Сценарий.ИмяПоследующегоЭлементаФормы               = "ВыходныеИзделия";
		Сценарий.ИмяТабличнойЧастиТовары                    = "ВыходныеИзделия";
		Сценарий.ИмяТабличнойЧастиСерии                     = "ВыходныеИзделияСерии";
		Сценарий.ИмяСледующейКолонки                        = "ВыходныеИзделияНоменклатура";
		Сценарий.РазмещатьЭлементыИнтерфейса                = ОбщегоНазначенияИС.ЕстьПродукцияТребующаяВзвешивания()
			И ИнтеграцияИСМПКлиентСерверПовтИсп.НастройкиСканированияКодовМаркировки().ЗапрашиватьКоличествоМерногоТовара;
		Сценарий.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.ПроизводственнаяОперация2_2);
		Сценарий.ЭтоДокументПриобретения                    = Ложь;
		Сценарий.ЕстьЭлектронныйДокумент                    = Ложь;
		Сценарий.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		Сценарий.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		Сценарий.ИмяКоманднойПанели                         = Форма.Элементы.ВыходныеИзделия.КоманднаяПанель.Имя;
		ПараметрыИнтеграции.Добавить(Сценарий);
		
		Сценарий = ОбщегоНазначения.СкопироватьРекурсивно(ЭлементНастроек);
		Сценарий.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		Сценарий.ИспользоватьБезМаркируемойПродукции        = Истина;
		Сценарий.ИмяРодительскойГруппыФормы                 = "СтраницаМатериалыИРаботы";
		Сценарий.ИмяПоследующегоЭлементаФормы               = "МатериалыИРаботы";
		Сценарий.ИмяТабличнойЧастиТовары                    = "МатериалыИРаботы";
		Сценарий.ИмяЭлементаФормыТовары                     = "МатериалыИРаботы";
		Сценарий.ИмяСледующейКолонки                        = "МатериалыИРаботыНоменклатура";
		Сценарий.ИмяТабличнойЧастиСерии                     = ""; 
		Сценарий.ИмяТабличнойЧастиШтрихкодыУпаковок         = "ШтрихкодыУпаковокМатериалы";
		Сценарий.РазмещатьЭлементыИнтерфейса                = Истина;
		Сценарий.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Метаданные.Документы.ПроизводственнаяОперация2_2);
		Сценарий.ЭтоДокументПриобретения                    = Ложь;
		Сценарий.ЕстьЭлектронныйДокумент                    = Ложь;
		Сценарий.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		Сценарий.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		Сценарий.Сценарий                                   = 1;
		Сценарий.ИмяКоманднойПанели                         = Форма.Элементы.МатериалыИРаботыГруппаЗаполнить.Имя;
		ПараметрыИнтеграции.Добавить(Сценарий);
		
//-- НЕ УТКА
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПеремещениеТоваров") Тогда
		
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПередачаТоваровМеждуОрганизациями") Тогда
		
		ЭлементНастроек.ИмяТабличнойЧастиСерии                     = "";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ВозвратТоваровМеждуОрганизациями") Тогда
		
		ЭлементНастроек.ИмяТабличнойЧастиСерии                     = "";
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ВнутреннееПотребление") Тогда
		
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПередачаТоваровХранителю")
		Или ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ОтгрузкаТоваровКлиенту") Тогда
		
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "СборкаТоваров") Тогда
	
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Ложь; //динамически менять при разборке не получится
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыГруппаЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
//++ НЕ УТ
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ДвижениеПродукцииИМатериалов") Тогда
	
		ЭлементНастроек.РазмещатьЭлементыИнтерфейса                = Истина;
		ЭлементНастроек.ИмяРодительскойГруппыФормы                 = "ГруппаТовары";
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Истина;
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		ЭлементНастроек.БлокироватьТабличнуюЧастьТоварыПриПроверке = Ложь;
		ЭлементНастроек.ИспользоватьСтатусПроверкаЗавершена        = Ложь;
		ЭлементНастроек.ЕстьПравоИзменение                         = ПравоДоступа("Изменение", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ДоступныОбъемноСортовыеКоды                = Истина;
		ЭлементНастроек.ИмяКоманднойПанели                         = Форма.Элементы.ТоварыЗаполнить.Имя;
		
		ПараметрыИнтеграции.Добавить(ЭлементНастроек);
		
//-- НЕ УТ
	
	КонецЕсли;
	
	// ЕГАИС
	Если ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "РеализацияТоваровУслуг")
			Или ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ВозвратТоваровПоставщику")
			Или ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ВнутреннееПотребление")
			Или ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПередачаТоваровХранителю") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "ГруппаТовары", Форма.Объект.Ссылка.Метаданные());
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "РасходныйОрдерНаТовары") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "ГруппаРежимПросмотраНавигацияМаркировка", Метаданные.Документы.РасходныйОрдерНаТовары);
		ЭлементНастроек.ИмяТабличнойЧастиТовары      = "ОтгружаемыеТовары";
		ЭлементНастроек.ИмяЭлементаФормыТовары       = "ОтгружаемыеТовары";
		ЭлементНастроек.ИмяСледующейКолонки          = "ОтгружаемыеТоварыНоменклатураАртикул";
		ЭлементНастроек.ИмяПоследующегоЭлементаФормы = "ГруппаРежимПросмотраНавигация";
		ЭлементНастроек.ИмяТабличнойЧастиСерии       = "";
		ЭлементНастроек.ИспользоватьБезМаркируемойПродукции        = Истина;
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ОтборРазмещениеТоваров") Тогда
	
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "ГруппаТоварыОтбор", Метаданные.Документы.ОтборРазмещениеТоваров);
		ЭлементНастроек.ИмяТабличнойЧастиТовары      = "ТоварыОтбор";
		ЭлементНастроек.ИмяЭлементаФормыТовары       = "ТоварыОтбор";
		ЭлементНастроек.ИмяСледующейКолонки          = "ТоварыОтборНоменклатура";
		ЭлементНастроек.ИмяПоследующегоЭлементаФормы = "ТоварыОтбор";
		ЭлементНастроек.ИмяТабличнойЧастиСерии       = "";
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПеремещениеТоваров") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "", Метаданные.Документы.ПеремещениеТоваров);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "СборкаТоваров") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "ГруппаТовары", Метаданные.Документы.СборкаТоваров);
		ЭлементНастроек.ИспользоватьКолонкуСтатусаПроверкиПодбора  = Ложь; //динамически менять при разборке не получится

	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПередачаТоваровМеждуОрганизациями")
		Или ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ВозвратТоваровМеждуОрганизациями") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "", Форма.Объект.Ссылка.Метаданные());
		ЭлементНастроек.ИмяТабличнойЧастиСерии       = "";
		
//++ НЕ УТ
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ДвижениеПродукцииИМатериалов") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "ГруппаТовары", Метаданные.Документы.ДвижениеПродукцииИМатериалов);
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПроизводствоБезЗаказа") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "ГруппаМатериалыИРаботы", Метаданные.Документы.ПроизводствоБезЗаказа);
		ЭлементНастроек.ИмяПоследующегоЭлементаФормы       = "МатериалыИРаботы";
		ЭлементНастроек.ИмяТабличнойЧастиТовары            = "МатериалыИРаботы";
		ЭлементНастроек.ИмяЭлементаФормыТовары             = "МатериалыИРаботы";
		ЭлементНастроек.ИмяСледующейКолонки                = "МатериалыИРаботыНоменклатура";
		ЭлементНастроек.ИмяТабличнойЧастиСерии             = "";
		ЭлементНастроек.ИмяТабличнойЧастиШтрихкодыУпаковок = "ШтрихкодыУпаковокМатериалы";
		ЭлементНастроек.Сценарий                           = 1;

//-- НЕ УТ

//++ НЕ УТКА

	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ЭтапПроизводства2_2") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "ТаблицаИзрасходованныеМатериалы", Метаданные.Документы.ЭтапПроизводства2_2);
		ЭлементНастроек.ИмяПоследующегоЭлементаФормы       = "РасходМатериаловИРабот";
		ЭлементНастроек.ИмяТабличнойЧастиТовары            = "РасходМатериаловИРабот";
		ЭлементНастроек.ИмяЭлементаФормыТовары             = "РасходМатериаловИРабот";
		ЭлементНастроек.ИмяСледующейКолонки                = "РасходМатериаловИРаботНоменклатура";
		ЭлементНастроек.ИмяТабличнойЧастиСерии             = "";
		ЭлементНастроек.ИмяТабличнойЧастиШтрихкодыУпаковок = "ШтрихкодыУпаковокМатериалы";
		ЭлементНастроек.Сценарий                           = 1;
		
	ИначеЕсли ИнтеграцияИСУТКлиентСервер.ЭтоДокументПоНаименованию(Форма, "ПроизводственнаяОперация2_2") Тогда
		
		ПроверкаИПодборПродукцииИСМПУТ.НастроитьРасходныйДокументДляЕГАИС(
			ЭлементНастроек, "СтраницаМатериалыИРаботы", Метаданные.Документы.ПроизводственнаяОперация2_2);
		ЭлементНастроек.ИмяПоследующегоЭлементаФормы       = "МатериалыИРаботы";
		ЭлементНастроек.ИмяТабличнойЧастиТовары            = "МатериалыИРаботы";
		ЭлементНастроек.ИмяЭлементаФормыТовары             = "МатериалыИРаботы";
		ЭлементНастроек.ИмяСледующейКолонки                = "МатериалыИРаботыНоменклатура";
		ЭлементНастроек.ИмяТабличнойЧастиСерии             = "";
		ЭлементНастроек.ИмяТабличнойЧастиШтрихкодыУпаковок = "ШтрихкодыУпаковокМатериалы";
		ЭлементНастроек.Сценарий                           = 1;

//-- НЕ УТКА

	КонецЕсли;
	
КонецПроцедуры

// Переопределение параметров интеграции ЕГАИС (расположения команды проверки и подбора)
//
// Параметры:
//   Форма                - ФормаКлиентскогоПриложения - прикладная форма для встраивания форматированной строки
//   СтандартнаяОбработка - Булево - стандартная работа с элементами проверки подбора
//   ПараметрыИнтеграции  - Соответствие - автоматически заданные параметры интеграции
//
Процедура ПриОпределенииПараметровИнтеграции(Форма, СтандартнаяОбработка, ПараметрыИнтеграции) Экспорт
	
	Если Форма.ИмяФормы = "Документ.АктОРасхожденияхПослеПриемки.Форма.ФормаДокумента"
		Или Форма.ИмяФормы = "Документ.АктОРасхожденияхПослеОтгрузки.Форма.ФормаДокумента"
		Тогда
			СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

// Заполняет специфику применения интеграции формы проверки и подбора в конкретную форму.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - форма для которой применяются параметры интеграции.
//
Процедура ПриПримененииПараметровИнтеграцииФормыПроверкиИПодбора(Форма) Экспорт
	
	Возврат;
	
КонецПроцедуры

// Получение организации для складских документов "через точку". 
// Параметры:
// ПроверяемыйДокумент - ДокументСсылка
// Организация - ОпределяемыйТип.Организация - возвращаемое значение.
Процедура ПриОпределенииОрганизации(ПроверяемыйДокумент, Организация) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Параметры.Вставить("ТекДокумент", ПроверяемыйДокумент);
	
	Если ТипЗнч(ПроверяемыйДокумент) = Тип("ДокументСсылка.РасходныйОрдерНаТовары") Тогда
		
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	МАКСИМУМ(РасходныйОрдерНаТоварыТоварыПоРаспоряжениям.Распоряжение.Организация) КАК Организация
		|ИЗ
		|	Документ.РасходныйОрдерНаТовары.ТоварыПоРаспоряжениям КАК РасходныйОрдерНаТоварыТоварыПоРаспоряжениям
		|ГДЕ
		|	РасходныйОрдерНаТоварыТоварыПоРаспоряжениям.Ссылка = &ТекДокумент";
		
	ИначеЕсли ТипЗнч(ПроверяемыйДокумент) = Тип("ДокументСсылка.ПриходныйОрдерНаТовары") Тогда
		
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	МАКСИМУМ(ПредварительныйОтбор.Организация) КАК Организация
		|ИЗ
		|	(ВЫБРАТЬ
		|		МАКСИМУМ(ПриходныйОрдерНаТоварыТовары.ДокументОтгрузки.Организация) КАК Организация
		|	ИЗ
		|		Документ.ПриходныйОрдерНаТовары.Товары КАК ПриходныйОрдерНаТоварыТовары
		|	ГДЕ
		|		ПриходныйОрдерНаТоварыТовары.Ссылка = &ТекДокумент
		|		И
		|			ПриходныйОрдерНаТоварыТовары.Ссылка.СкладскаяОперация = ЗНАЧЕНИЕ(Перечисление.СкладскиеОперации.ВозвратНеПринятыхТоваров)
		|
		|	ОБЪЕДИНИТЬ ВСЕ
		|
		|	ВЫБРАТЬ
		|		ПриходныйОрдерНаТовары.Распоряжение.Организация КАК Организация
		|	ИЗ
		|		Документ.ПриходныйОрдерНаТовары КАК ПриходныйОрдерНаТовары
		|	ГДЕ
		|		ПриходныйОрдерНаТовары.Ссылка = &ТекДокумент
		|		И НЕ
		|			ПриходныйОрдерНаТовары.СкладскаяОперация = ЗНАЧЕНИЕ(Перечисление.СкладскиеОперации.ВозвратНеПринятыхТоваров)) КАК
		|		ПредварительныйОтбор";
		
	ИначеЕсли ТипЗнч(ПроверяемыйДокумент) = Тип("ДокументСсылка.ОтборРазмещениеТоваров") Тогда
		
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	МАКСИМУМ(РасходныйОрдерНаТоварыТоварыПоРаспоряжениям.Распоряжение.Организация) КАК Организация
		|ИЗ
		|	Документ.ОтборРазмещениеТоваров КАК ОтборРазмещениеТоваров
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РасходныйОрдерНаТовары.ТоварыПоРаспоряжениям КАК РасходныйОрдерНаТоварыТоварыПоРаспоряжениям
		|		ПО ОтборРазмещениеТоваров.Распоряжение = РасходныйОрдерНаТоварыТоварыПоРаспоряжениям.Ссылка
		|ГДЕ
		|	ОтборРазмещениеТоваров.Ссылка = &ТекДокумент";

	Иначе
		Возврат;
	КонецЕсли;

	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	Если Выборка.Следующий() Тогда
		Организация = Выборка.Организация;
	КонецЕсли;
	
КонецПроцедуры

// Возвращает через третий параметр признак наличия маркируемой продукции.
//
// Параметры:
//  Объект                   - ДанныеФормыСтруктура - объект в котором проходит проверка
//  Коллекция                - Строка - ТЧ с товарами.
//  ЕстьМаркируемаяПродукция - Булево - Исходящий, признак наличия маркируемой продукции.
//
Процедура ЕстьМаркируемаяПродукция(Объект, Коллекция, ЕстьМаркируемаяПродукция) Экспорт
	
	ВидыПродукции = Новый Массив;
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВидыПродукции, КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииЕГАИСМаркируемые());
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВидыПродукции, КонструкторИСОбщегоНазначенияКлиентСервер.ВидыПродукцииИСМП(Истина));
	
	ПроверкаИПодборПродукцииИСМПУТ.ЕстьМаркируемаяПродукцияВКоллекции(
		Объект[Коллекция],
		ВидыПродукции,
		ЕстьМаркируемаяПродукция);
	
КонецПроцедуры

#КонецОбласти

#Область ПараметрыФормыПроверкиИПодбора

// Вызывается при открытии формы проверки и подбора для явного задания ее параметров, которые не были переданы заранее.
// 
// Параметры:
//  Форма - См. Обработка.ПроверкаИПодборПродукцииИС.Форма.ПроверкаИПодбор
//  ПараметрыСканирования - См. ШтрихкодированиеОбщегоНазначенияИС.ПараметрыСканирования
Процедура ПриЗаполненииПараметровФормыПроверкиИПодбора(Форма, ПараметрыСканирования) Экспорт
	
	ПроверяемыйДокумент = Форма.ПроверяемыйДокумент;
	Запрос = Новый Запрос;
	Запрос.Параметры.Вставить("ТекДокумент", ПроверяемыйДокумент);
	
	Если ТипЗнч(ПроверяемыйДокумент) = Тип("ДокументСсылка.РасходныйОрдерНаТовары") Тогда
		
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	МАКСИМУМ(РасходныйОрдерНаТоварыТоварыПоРаспоряжениям.Распоряжение.Организация) КАК Организация
		|ИЗ
		|	Документ.РасходныйОрдерНаТовары.ТоварыПоРаспоряжениям КАК РасходныйОрдерНаТоварыТоварыПоРаспоряжениям
		|ГДЕ
		|	РасходныйОрдерНаТоварыТоварыПоРаспоряжениям.Ссылка = &ТекДокумент";
		
	ИначеЕсли ТипЗнч(ПроверяемыйДокумент) = Тип("ДокументСсылка.ПриходныйОрдерНаТовары") Тогда
		
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	МАКСИМУМ(ПредварительныйОтбор.Организация) КАК Организация
		|ИЗ
		|	(ВЫБРАТЬ
		|		МАКСИМУМ(ПриходныйОрдерНаТоварыТовары.ДокументОтгрузки.Организация) КАК Организация
		|	ИЗ
		|		Документ.ПриходныйОрдерНаТовары.Товары КАК ПриходныйОрдерНаТоварыТовары
		|	ГДЕ
		|		ПриходныйОрдерНаТоварыТовары.Ссылка = &ТекДокумент
		|		И
		|			ПриходныйОрдерНаТоварыТовары.Ссылка.СкладскаяОперация = ЗНАЧЕНИЕ(Перечисление.СкладскиеОперации.ВозвратНеПринятыхТоваров)
		|
		|	ОБЪЕДИНИТЬ ВСЕ
		|
		|	ВЫБРАТЬ
		|		ПриходныйОрдерНаТовары.Распоряжение.Организация КАК Организация
		|	ИЗ
		|		Документ.ПриходныйОрдерНаТовары КАК ПриходныйОрдерНаТовары
		|	ГДЕ
		|		ПриходныйОрдерНаТовары.Ссылка = &ТекДокумент
		|		И НЕ
		|			ПриходныйОрдерНаТовары.СкладскаяОперация = ЗНАЧЕНИЕ(Перечисление.СкладскиеОперации.ВозвратНеПринятыхТоваров)) КАК
		|		ПредварительныйОтбор";
		
	ИначеЕсли ТипЗнч(ПроверяемыйДокумент) = Тип("ДокументСсылка.ОтборРазмещениеТоваров") Тогда
		
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	МАКСИМУМ(РасходныйОрдерНаТоварыТоварыПоРаспоряжениям.Распоряжение.Организация) КАК Организация
		|ИЗ
		|	Документ.ОтборРазмещениеТоваров КАК ОтборРазмещениеТоваров
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РасходныйОрдерНаТовары.ТоварыПоРаспоряжениям КАК РасходныйОрдерНаТоварыТоварыПоРаспоряжениям
		|		ПО ОтборРазмещениеТоваров.Распоряжение = РасходныйОрдерНаТоварыТоварыПоРаспоряжениям.Ссылка
		|ГДЕ
		|	ОтборРазмещениеТоваров.Ссылка = &ТекДокумент";

	Иначе
		Возврат;
	КонецЕсли;

	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	Если Выборка.Следующий() Тогда
		Форма.Организация = Выборка.Организация;
	КонецЕсли;
	
КонецПроцедуры

// Определяет соответствие проверяемому типу документа, результат возвращает через второй параметр.
//
// Параметры:
//  ИмяДокумента    - Строка - Контекст для определения типа документа
//  ЭтоПриобретение - Булево - исходящий признак входящего (загруженного) документа.
Процедура ЭтоДокументПриобретения(ИмяДокумента, ЭтоПриобретение) Экспорт
	
	Если ИмяДокумента = "ПриобретениеТоваровУслуг" Тогда
		ЭтоПриобретение = Истина;
	ИначеЕсли ИмяДокумента = "ПриемкаТоваровНаХранение" Тогда
		ЭтоПриобретение = Истина;
	ИначеЕсли ИмяДокумента = "ПоступлениеТоваровОтХранителя" Тогда
		ЭтоПриобретение = Истина;
	ИначеЕсли ИмяДокумента = "ПоступлениеТоваровОтКлиента" Тогда
		ЭтоПриобретение = Истина;
	ИначеЕсли ИмяДокумента = "ВозвратТоваровОтКлиента" Тогда
		ЭтоПриобретение = Истина;
	ИначеЕсли ИмяДокумента = "КорректировкаПриобретения" Тогда
		ЭтоПриобретение = Истина;
	КонецЕсли;
	
КонецПроцедуры

// Определяет соответствие проверяемому типу документа, результат возвращает через второй параметр.
//
// Параметры:
//  ИмяДокумента - Строка - Контекст для определения типа документа
//  ЭтоАкт       - Булево - исходящий признак акта о расхождениях.
Процедура ЭтоАктОРасхождениях(ИмяДокумента, ЭтоАкт) Экспорт
	
	Если ИмяДокумента = "АктОРасхожденияхПослеОтгрузки" Тогда
		ЭтоАкт = Истина;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти