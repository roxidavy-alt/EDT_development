
#Область СлужебныйПрограммныйИнтерфейс

#Область ФормаПроверкиИПодбора

Функция НеобходимоОбращениеКСервису(ПараметрыСканирования, ПараметрыПроверкиКодовМаркировки) Экспорт
	
	ОтключитьЗапросПоВидуПродукцииИОперации = Не ПараметрыПроверкиКодовМаркировки.КонтролироватьВладельцевКодовМаркировки И Не ПараметрыПроверкиКодовМаркировки.КонтролироватьСтатусыКодовМаркировки;
	
	// Если включена настройка проверки розничным методом по ТГ или не включено Запрашивать данные сервиса, то токен не нужен
	Если Не ОбщегоНазначенияИСМПКлиентСервер.ЗначениеПараметраСканированияЗапрашиватьДанныеСервисаИСМП(ПараметрыСканирования, ОтключитьЗапросПоВидуПродукцииИОперации)
		И Не ОбщегоНазначенияИСМПКлиентСервер.ЗначениеПараметраСканированияЗапрашиватьДанныеНеизвестныхУпаковокИСМП(ПараметрыСканирования, ОтключитьЗапросПоВидуПродукцииИОперации)
		Или ПараметрыПроверкиКодовМаркировки.ПроверятьЗаполнениеИдентификатораГИСМТ Тогда
		Возврат Ложь;
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#Область ПредставлениеПолейДереваМаркированнойПродукции

// Формирует признак допустимости и представление проверки данных ИСМП для строки дерева, содержащего данные упаковки.
// 
// Параметры:
//  ТекущаяСтрока - ДанныеФормыЭлементДерева - строка дерева, содержащая данные упаковки.
//  ПараметрыПроверкиКодовМаркировки - Структура - со свойствами:
//   * ДопустимыеСтатусыКодовМаркировки - Массив Из ПеречислениеСсылка.СтатусыКодовМаркировкиИСМП - допустимые статусы кода маркировки для текущей хоз.операции.
//   * КонтролироватьВладельцевКодовМаркировки - Булево - необходимость контроля кода маркировки по владельцу.
//   * ИННВладельца - Строка - ИНН владельца кода маркировки по данным ИС ИСМП.
//   * КонтролироватьВложенностьУпаковок - Булево - необходимость контроля типов вложенных упаковок.
//  ЕстьПодчиненныеСтроки - Булево, Неопределено - Признак наличия подчиненных строк
//  ЭтоОСУ - Булево - Признак принадлежности упаковки к объемно-сортовому учету
//
Процедура ПроверитьДопустимостьКодаМаркировки(ТекущаяСтрока, ПараметрыПроверкиКодовМаркировки, ЕстьПодчиненныеСтроки = Неопределено, ЭтоОСУ = Ложь) Экспорт
	
	Если ТекущаяСтрока.ТипУпаковки = ТипУпаковкиПотребительскиеУпаковкиБезУпаковки()
		Или ТекущаяСтрока.ТипУпаковки = ТипУпаковкиГрупповыеУпаковкиБезКоробки()
		Или ТекущаяСтрока.ТипУпаковки = ТипУпаковкиОбъемноСортовойУчет()
		Или ТекущаяСтрока.ТипУпаковки = ТипУпаковкиГруппировкаОбъемноСортовогоУчетаПоGTIN() Тогда
		
		ТекущаяСтрока.НедопустимыйКодМаркировки = Ложь;
		ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = "";
		
	ИначеЕсли Не ПараметрыПроверкиКодовМаркировки.ЗапрашиватьДанныеСервиса
		Или ОбщегоНазначенияИСМПКлиентСерверПовтИсп.ДействуетАварийныйРежимДляРозничныхПродаж() Тогда
		
		ТекущаяСтрока.НедопустимыйКодМаркировки = Ложь;
		ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = "";
		
	ИначеЕсли ПараметрыПроверкиКодовМаркировки.ОтсутствуетПодключениеИСМП Тогда
		
		ТекущаяСтрока.НедопустимыйКодМаркировки = Ложь;
		ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = НСтр("ru = 'Статус неопределен';
																|en = 'Статус неопределен'");
		
	Иначе
		
		ТекущаяСтрока.НедопустимыйКодМаркировки           = Ложь;
		ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = "";
		
		// Приоритет 1. Статус
		Если ТекущаяСтрока.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Логистическая") Тогда
			ДопустимыеСтатусыКодовМаркировки = ПараметрыПроверкиКодовМаркировки.ДопустимыеСтатусыУпаковок;
		ИначеЕсли ТекущаяСтрока.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Набор")
			И ПараметрыПроверкиКодовМаркировки.ДопустимыеСтатусыНаборов <> Неопределено Тогда
			ДопустимыеСтатусыКодовМаркировки = ПараметрыПроверкиКодовМаркировки.ДопустимыеСтатусыНаборов;
		ИначеЕсли ТекущаяСтрока.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Групповая")
			И ПараметрыПроверкиКодовМаркировки.ДопустимыеСтатусыНаборов <> Неопределено Тогда
			ДопустимыеСтатусыКодовМаркировки = ПараметрыПроверкиКодовМаркировки.ДопустимыеСтатусыНаборов;
		Иначе
			ДопустимыеСтатусыКодовМаркировки = ПараметрыПроверкиКодовМаркировки.ДопустимыеСтатусыКодовМаркировки;
		КонецЕсли;
		
		ИсключенияПроверокРазрешительногоРежима = Ложь;
		
		Если ЗначениеЗаполнено(ТекущаяСтрока.РазрешительныйРежимДатаЗапросаГИСМТ)
			И Не ЗначениеЗаполнено(ТекущаяСтрока.РазрешительныйРежимИдентификаторЗапросаГИСМТ)
			Или ТекущаяСтрока.ПродажаПоДаннымОффлайнРежима Тогда
			
			// ответ от ГИС МТ не был получен, продажа разрешена без проверок
			ИсключенияПроверокРазрешительногоРежима = Истина;
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ТекущаяСтрока.СтатусКодаМаркировки)
			И ПараметрыПроверкиКодовМаркировки.КонтролироватьСтатусыКодовМаркировки 
			И ДопустимыеСтатусыКодовМаркировки.Найти(ТекущаяСтрока.СтатусКодаМаркировки) = Неопределено
			И Не ИсключенияПроверокРазрешительногоРежима
			И Не ЭтоОСУ
			И НастройкаПараметровСканированияСлужебныйКлиентСерверПовтИсп.КонтролироватьСтатусыКодовМаркировки(
				ТекущаяСтрока.ВидПродукции, ПараметрыПроверкиКодовМаркировки.ВидОперацииИСМП) Тогда
			ТекущаяСтрока.НедопустимыйКодМаркировки           = Истина;
			ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = СтрШаблон(НСтр("ru = 'Статус: %1';
																				|en = 'Статус: %1'"), ТекущаяСтрока.СтатусКодаМаркировки);
		КонецЕсли;
		
		// Приоритет 2. Вложения. Пустая упаковка
		Если Не ТекущаяСтрока.НедопустимыйКодМаркировки
			И ПараметрыПроверкиКодовМаркировки.КонтролироватьСтандартнуюВложенность
			И ТекущаяСтрока.ВидУпаковки <> ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Потребительская")
			И ТекущаяСтрока.ВидУпаковки <> ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.ОбъемноСортовойУчет")
			И (ЕстьПодчиненныеСтроки = Ложь И ТекущаяСтрока.КоличествоПодчиненныхПотребительскихУпаковок = 0) Тогда
				ТекущаяСтрока.НедопустимыйКодМаркировки           = Истина;
				ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = НСтр("ru = 'Пустая упаковка';
																		|en = 'Пустая упаковка'");
		КонецЕсли;
		
		// Приоритет 3. Вложенность стандартных упаковок
		Если Не ТекущаяСтрока.НедопустимыйКодМаркировки
			И ПараметрыПроверкиКодовМаркировки.КонтролироватьСтандартнуюВложенность
			И ТекущаяСтрока.ВидУпаковки <> ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Потребительская")
			И ТекущаяСтрока.КоличествоПодчиненныхПотребительскихУпаковок <> ТекущаяСтрока.ПлановоеКоличествоПотребительскихУпаковок И ТекущаяСтрока.ПлановоеКоличествоПотребительскихУпаковок > 0 Тогда
			ТекущаяСтрока.НедопустимыйКодМаркировки           = Истина;
			ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = СтрШаблон(
				НСтр("ru = 'По данным ГИС МТ в упаковке %2 ед. из %1';
					|en = 'По данным ГИС МТ в упаковке %2 ед. из %1'"),
				ТекущаяСтрока.ПлановоеКоличествоПотребительскихУпаковок, ТекущаяСтрока.КоличествоПодчиненныхПотребительскихУпаковок);
		КонецЕсли;
		
		// Приоритет 4. Блокировка КМ по данным ГИС МТ
		Если ПараметрыПроверкиКодовМаркировки.КонтролироватьСтатусыКодовМаркировки
			И ЗначениеЗаполнено(ТекущаяСтрока.ОрганыБлокировкиКМ) Тогда
			ТекущаяСтрока.НедопустимыйКодМаркировки           = Истина;
			ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки
				+ ?(ПустаяСтрока(ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки), "", ", ")
				+ СтрШаблон(НСтр("ru = 'Заблокирован по решению %1';
								|en = 'Заблокирован по решению %1'"), ТекущаяСтрока.ОрганыБлокировкиКМ);
		КонецЕсли;
		
		// Приоритет 5. Проверка разрешительной документации
		Если ПараметрыПроверкиКодовМаркировки.КонтролироватьРазрешительныеДокументыКодовМаркировки
			И (ПараметрыПроверкиКодовМаркировки.КонтролироватьСтатусыКодовМаркировки Или ПараметрыПроверкиКодовМаркировки.КонтролироватьВладельцевКодовМаркировки)
			И РазрешительнаяДокументацияТребуетПроверкиПоДатеПроизводства(ТекущаяСтрока, ПараметрыПроверкиКодовМаркировки)
			И Не ШтрихкодированиеОбщегоНазначенияИСМПКлиентСервер.ГруппаСтатусаРазрешительнойДокументацииРазрешенаДляОборота(ТекущаяСтрока.СтатусРД) Тогда
			
			Если ТипЗнч(ТекущаяСтрока) = Тип("ДанныеФормыЭлементДерева") Тогда
				РодительТекущейСтроки = ТекущаяСтрока.ПолучитьРодителя();
			Иначе
				РодительТекущейСтроки = ТекущаяСтрока.Родитель;
			КонецЕсли;
			
			ПропуститьВложенныеУпаковкиГрупповойЛогистическойУпаковки = РодительТекущейСтроки <> Неопределено
				И ТипЗнч(РодительТекущейСтроки.ТипУпаковки) = Тип("ПеречислениеСсылка.ТипыУпаковок");
			
			Если Не ПропуститьВложенныеУпаковкиГрупповойЛогистическойУпаковки Тогда
				
				ТекущаяСтрока.НедопустимыйКодМаркировки           = Истина;
				ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки
					+ ?(ПустаяСтрока(ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки), "", ", ")
					+ СтрШаблон(НСтр("ru = 'Некоректный статус РД - %1';
									|en = 'Некоректный статус РД - %1'"), ?(ЗначениеЗаполнено(ТекущаяСтрока.СтатусРД), ТекущаяСтрока.СтатусРД, НСтр("ru = 'статус неизвестен';
																																					|en = 'статус неизвестен'")));
					
			КонецЕсли;
			
		КонецЕсли;
		
		Если ТекущаяСтрока.СтатусКодаМаркировки <> ПредопределенноеЗначение("Перечисление.СтатусыКодовМаркировкиИСМП.Неопределен")
			И ЗначениеЗаполнено(ТекущаяСтрока.СтатусКодаМаркировки)
			И Не ЭтоОСУ Тогда
			
			Если ПараметрыПроверкиКодовМаркировки.КонтролироватьВладельцевКодовМаркировки
				И НастройкаПараметровСканированияСлужебныйКлиентСерверПовтИсп.КонтролироватьВладельцевКодовМаркировки(
					ТекущаяСтрока.ВидПродукции, ПараметрыПроверкиКодовМаркировки.ВидОперацииИСМП) Тогда
				
				Владельцы = Новый Соответствие();
				Если ЗначениеЗаполнено(ТекущаяСтрока.ИННВладельца) Тогда
					Владельцы.Вставить(
						ТекущаяСтрока.ИННВладельца,
						СтрШаблон(НСтр("ru = 'ИНН владельца: %1';
										|en = 'ИНН владельца: %1'"), ТекущаяСтрока.ИННВладельца));
				КонецЕсли;
				
				ДанныеОВладельце = Владельцы.Получить(ПараметрыПроверкиКодовМаркировки.ИННВладельца);
				Если Владельцы.Количество() = 0 Тогда
						
					Если Не ИсключенияПроверокРазрешительногоРежима
						И Не ВладелецМожетОтличатьсяПоДатеПроизводства(ТекущаяСтрока, ПараметрыПроверкиКодовМаркировки) Тогда
						
						ТекущаяСтрока.НедопустимыйКодМаркировки = Истина;
						ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки
							+ ?(ПустаяСтрока(ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки), "", ", ")
							+ НСтр("ru = 'Принадлежит другой организации';
									|en = 'Принадлежит другой организации'");
							
					КонецЕсли;
				
				ИначеЕсли ДанныеОВладельце = Неопределено
					И ПараметрыПроверкиКодовМаркировки.ОрганизацииИнтеркампани[ТекущаяСтрока.ИННВладельца] = Неопределено
					И Не ВладелецМожетОтличатьсяПоДатеПроизводства(ТекущаяСтрока, ПараметрыПроверкиКодовМаркировки) Тогда
					
					ДанныеСтрокиОВладельце = Новый Массив();
					Для Каждого КлючИЗначение Из Владельцы Цикл
						ДанныеСтрокиОВладельце.Добавить(КлючИЗначение.Значение);
					КонецЦикла;
					СтрокаОВладельце = СтрСоединить(ДанныеСтрокиОВладельце, ", ");
					ТекущаяСтрока.НедопустимыйКодМаркировки = Истина;
					ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки +
						?(ПустаяСтрока(ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки), "", ", ") +
						СтрокаОВладельце;
					
				КонецЕсли;
				
			КонецЕсли;
			
			Если Не ТекущаяСтрока.НедопустимыйКодМаркировки
				И ПараметрыПроверкиКодовМаркировки.ЗапрашиватьДанныеСервиса
				И ПараметрыПроверкиКодовМаркировки.ДопустимыйСпособВводаВОборот.Количество()
				И ЗначениеЗаполнено(ТекущаяСтрока.СпособВводаВОборот)
				И ПараметрыПроверкиКодовМаркировки.ДопустимыйСпособВводаВОборот.Найти(ТекущаяСтрока.СпособВводаВОборот) = Неопределено Тогда
				
				ТекущаяСтрока.НедопустимыйКодМаркировки           = Истина;
				ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки
					+ ?(ПустаяСтрока(ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки), "", ", ")
					+ СтрШаблон(
						НСтр("ru = 'Способ ввода в оборот: %1';
							|en = 'Способ ввода в оборот: %1'"),
						ТекущаяСтрока.СпособВводаВОборот);
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ПараметрыПроверкиКодовМаркировки.КонтролироватьВложенностьУпаковок
		И ПараметрыПроверкиКодовМаркировки.ДопустимыеВложенияАгрегатов <> Неопределено Тогда
		
		ПроверитьДопустимуюВложенностьУпаковок(ТекущаяСтрока, ПараметрыПроверкиКодовМаркировки);
		
	КонецЕсли;
	
	Если ТекущаяСтрока.ОбработанСОшибкойВГИСМТ Тогда
		
		ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки
			+ ?(ПустаяСтрока(ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки), "", ", ")
			+ ТекущаяСтрока.ТекстОбработанСОшибкойВГИСМТ;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ДопустимыеВложенияАгрегатовПоВидамПродукции(ТоварнаяГруппаИлиВидПродукции) Экспорт
	
	ДопустимыеВложенияАгрегатов = Новый Соответствие;
	
	ДопустимыеВложенияАгрегатов.Вставить(
		ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Логистическая"),
		МаркировкаТоваровКлиентСервер.ДопустимыеВидыУпаковокВнутриЛогистическойУпаковки(ТоварнаяГруппаИлиВидПродукции));
	
	ДопустимыеТипыШтрихкодовВнутриSSCC = МаркировкаТоваровКлиентСервер.ДопустимыеТипыШтрихкодовВнутриSSCC(ТоварнаяГруппаИлиВидПродукции);
	ДопустимыеВложенияАгрегатов.Вставить(
		ПредопределенноеЗначение("Перечисление.ТипыШтрихкодов.SSCC"),
		ДопустимыеТипыШтрихкодовВнутриSSCC);
	ДопустимыеВложенияАгрегатов.Вставить(
		ПредопределенноеЗначение("Перечисление.ТипыШтрихкодов.SSCC18"),
		ДопустимыеТипыШтрихкодовВнутриSSCC);
	ДопустимыеВложенияАгрегатов.Вставить(
		ПредопределенноеЗначение("Перечисление.ТипыШтрихкодов.SSCC20"),
		ДопустимыеТипыШтрихкодовВнутриSSCC);
	
	ДопустимыеВложенияАгрегатов.Вставить(
		ПредопределенноеЗначение("Перечисление.ТипыШтрихкодов.GS1_128"), 
		МаркировкаТоваровКлиентСервер.ДопустимыеТипыШтрихкодовВнутриGS1(ТоварнаяГруппаИлиВидПродукции));
	
	Возврат ДопустимыеВложенияАгрегатов;
	
КонецФункции

Процедура ПроверитьСоставАгрегата(ТекущаяСтрока, ПараметрыПроверкиКодовМаркировки) Экспорт
	
	Если ТекущаяСтрока = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ПараметрыПроверкиКодовМаркировки.КонтролироватьСоставАгрегата
		Или ПараметрыПроверкиКодовМаркировки.КонтролироватьСоставАгрегатаПриИзмененииСтрокиДерева
		И ТекущаяСтрока.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Логистическая") Тогда
		
		СтрокаОшибки = "";
		
		Если ТипЗнч(ТекущаяСтрока) = Тип("ДанныеФормыЭлементДерева") Тогда
			ВложенныеСтроки = ТекущаяСтрока.ПолучитьЭлементы();
		Иначе
			ВложенныеСтроки = ТекущаяСтрока.Строки;
		КонецЕсли;
		
		ТекущийВидУпаковки = Неопределено;
		
		Для Каждого ВложеннаяСтрокаДерева Из ВложенныеСтроки Цикл
			
			Если ТекущийВидУпаковки = Неопределено Тогда
				ТекущийВидУпаковки = ВложеннаяСтрокаДерева.ВидУпаковки;
				Продолжить;
			КонецЕсли;
			
			Если ВложеннаяСтрокаДерева.ВидУпаковки <> ТекущийВидУпаковки Тогда
				
				СтрокаОшибки = СтрШаблон(
					НСтр("ru = '%1 упаковка может содержать только один вид упаковок (логистические, групповые или потребительские)';
						|en = '%1 упаковка может содержать только один вид упаковок (логистические, групповые или потребительские)'"),
					ТекущаяСтрока.ВидУпаковки);
				Прервать;
				
			КонецЕсли;
			
		КонецЦикла;
		
		Если ЗначениеЗаполнено(СтрокаОшибки) Тогда
			
			ТекущаяСтрока.НедопустимыйКодМаркировки = Истина;
			ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки
				+ ?(ПустаяСтрока(ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки), "", ", ")
				+ СтрокаОшибки;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ПараметрыПроверкиКодовМаркировки.КонтролироватьСоставГрупповойУпаковки
		Или ПараметрыПроверкиКодовМаркировки.КонтролироватьСоставГрупповойУпаковкиПриИзмененииСтрокиДерева
		И ТекущаяСтрока.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Групповая") Тогда
		
		ТекущийТипШтрихкода = Неопределено;
		СтрокаОшибки = "";
		
		Если ТипЗнч(ТекущаяСтрока) = Тип("ДанныеФормыЭлементДерева") Тогда
			ВложенныеСтрокиГрупповойУпаковки = ТекущаяСтрока.ПолучитьЭлементы();
		Иначе
			ВложенныеСтрокиГрупповойУпаковки = ТекущаяСтрока.Строки;
		КонецЕсли;
		
		Для Каждого ВложеннаяСтрокаДерева Из ВложенныеСтрокиГрупповойУпаковки Цикл
			
			Если ТекущийТипШтрихкода = Неопределено Тогда
				ТекущийТипШтрихкода = ВложеннаяСтрокаДерева.ТипШтрихкода;
				Продолжить;
			КонецЕсли;
			
			Если ВложеннаяСтрокаДерева.ТипШтрихкода <> ТекущийТипШтрихкода Тогда
				
				СтрокаОшибки = СтрШаблон(
					НСтр("ru = '%1 упаковка не может содержать разные шаблоны кодов маркировки';
						|en = '%1 упаковка не может содержать разные шаблоны кодов маркировки'"),
					ТекущаяСтрока.ВидУпаковки);
				Прервать;
				
			КонецЕсли;
			
		КонецЦикла;
		
		Если ЗначениеЗаполнено(СтрокаОшибки) Тогда
			
			ТекущаяСтрока.НедопустимыйКодМаркировки = Истина;
			ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки = ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки
				+ ?(ПустаяСтрока(ТекущаяСтрока.ПредставлениеПроверкиКодаМаркировки), "", ", ")
				+ СтрокаОшибки;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СерииНоменклатуры

Функция СериюМожноУказать(ТаблицаПродукции, ИмяКолонкиКоличество = "КоличествоПодобрано") Экспорт
	
	СтатусыСерийСериюМожноУказать = ИнтеграцияИСКлиентСервер.СтатусыСерийСериюМожноУказать();
	
	Для Каждого СтрокаПродукции Из ТаблицаПродукции Цикл
		
		Если СтрокаПродукции[ИмяКолонкиКоличество] = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		Если СтатусыСерийСериюМожноУказать.Найти(СтрокаПродукции.СтатусУказанияСерий) <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

#КонецОбласти

Функция ТекстОшибкиНеСоответствуютДокументуОснованию(ДанныеШтрихкода) Экспорт
	
	ЧастиТекстаОшибки = Новый Массив;
	
	ЧастиТекстаОшибки.Добавить(НСтр("ru = 'Не удалось сопоставить данным документа следующие данные:';
									|en = 'Не удалось сопоставить данным документа следующие данные:'"));
	ЧастиТекстаОшибки.Добавить(" ");
	ЧастиТекстаОшибки.Добавить(
		СтрШаблон(НСтр("ru = 'номенклатура - %1';
						|en = 'номенклатура - %1'"),
			?(ЗначениеЗаполнено(ДанныеШтрихкода.Номенклатура),
				ДанныеШтрихкода.Номенклатура,
				НСтр("ru = 'Не определена';
					|en = 'Не определена'"))));
				
	Если ЗначениеЗаполнено(ДанныеШтрихкода.Характеристика) Тогда
		ЧастиТекстаОшибки.Добавить(", ");
		ЧастиТекстаОшибки.Добавить(
			СтрШаблон(НСтр("ru = 'Характеристика - %1';
							|en = 'Характеристика - %1'"), ДанныеШтрихкода.Характеристика));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДанныеШтрихкода.Серия) Тогда
		ЧастиТекстаОшибки.Добавить(", ");
		ЧастиТекстаОшибки.Добавить(
			СтрШаблон(НСтр("ru = 'Серия - %1';
							|en = 'Серия - %1'"), ДанныеШтрихкода.Серия));
	КонецЕсли;
	
	Возврат СтрСоединить(ЧастиТекстаОшибки);
	
КонецФункции

#Область ОпределениеХозяйственнойОперации

Функция ЭтоДокументРеализации(Контекст) Экспорт
	
	ЭтоРеализация = Ложь;
	
	ПроверкаИПодборПродукцииИСМПКлиентСерверПереопределяемый.ЭтоДокументРеализации(Контекст, ЭтоРеализация);
	
	Возврат ЭтоРеализация;
	
КонецФункции

Функция ЭтоЧекККМ(Контекст) Экспорт
	
	ЭтоЧек = Ложь;
	
	ПроверкаИПодборПродукцииИСМПКлиентСерверПереопределяемый.ЭтоЧекККМ(Контекст, ЭтоЧек);
	
	Возврат ЭтоЧек;
	
КонецФункции

Функция ЭтоЧекККМВозврат(Контекст) Экспорт
	
	ЭтоЧекВозврат = Ложь;
	
	ПроверкаИПодборПродукцииИСМПКлиентСерверПереопределяемый.ЭтоЧекККМВозврат(Контекст, ЭтоЧекВозврат);
	
	Возврат ЭтоЧекВозврат;
	
КонецФункции

#КонецОбласти

Функция НоменклатурыСтрокСовпадают(ИсходнаяСтрока, СтрокаИзменений, ПолноеСовпадение = Истина) Экспорт
	
	Возврат ИсходнаяСтрока.Номенклатура = СтрокаИзменений.Номенклатура
		И СвойствоСтрокСовпадает(ИсходнаяСтрока, СтрокаИзменений, "Характеристика")
		И СвойствоСтрокСовпадает(ИсходнаяСтрока, СтрокаИзменений, "Серия", ПолноеСовпадение);
	
КонецФункции

Функция СвойствоСтрокСовпадает(ИсходнаяСтрока, СтрокаИзменений, ИмяСвойства, ПолноеСовпадение = Истина) Экспорт
	
	Свойство1 = ИсходнаяСтрока[ИмяСвойства];
	Свойство2 = СтрокаИзменений[ИмяСвойства];
	
	Если Свойство1 = Свойство2 Тогда
		Возврат Истина;
	ИначеЕсли Не ЗначениеЗаполнено(Свойство1) И Не ЗначениеЗаполнено(Свойство2) Тогда
		Возврат Истина;
	ИначеЕсли ПолноеСовпадение Тогда
		Возврат Ложь;
	Иначе
		Возврат Не ЗначениеЗаполнено(Свойство2);
	КонецЕсли;
	
КонецФункции

Функция GTINРавны(GTIN1, GTIN2) Экспорт
	
	Если GTIN1 = GTIN2 Тогда
		Возврат Истина;
	ИначеЕсли СтрДлина(GTIN1) = СтрДлина(GTIN2) Тогда
		Возврат Ложь;
	КонецЕсли;
	EAN1 = РазборКодаМаркировкиИССлужебныйКлиентСервер.ШтрихкодEANИзGTIN(GTIN1);
	EAN2 = РазборКодаМаркировкиИССлужебныйКлиентСервер.ШтрихкодEANИзGTIN(GTIN2);
	Возврат EAN1 = EAN2;
	
КонецФункции

Процедура ДополнитьДанныеШтрихкодаПолнымКодомМаркировки(ДанныеШтрихкода, ДанныеРазбора, ПараметрыСканирования) Экспорт
	
	Если ДанныеРазбора = Неопределено
		Или ДанныеШтрихкода.Свойство("ПолныйКодМаркировки")
		Или Не ДанныеРазбора.Свойство("СоставКодаМаркировки") Тогда
		Возврат;
	КонецЕсли;
	
	Если (ДанныеРазбора.СоставКодаМаркировки.Свойство("ВключаетКлючПроверки")
		И ДанныеРазбора.СоставКодаМаркировки.ВключаетКлючПроверки)
		Или (ДанныеРазбора.СоставКодаМаркировки.Свойство("ВключаетКриптоХвост")
		И ДанныеРазбора.СоставКодаМаркировки.ВключаетКриптоХвост) Тогда
		ДанныеШтрихкода.Вставить(
			"ПолныйКодМаркировки",
			ШтрихкодированиеОбщегоНазначенияИСКлиентСервер.ШтрихкодВBase64(ДанныеШтрихкода.Штрихкод));
		ДанныеШтрихкода.Вставить("ЕстьВПулеКодовМаркировки", Ложь);
		ДанныеШтрихкода.Вставить("ДанныеРазбора",            ДанныеРазбора);
		Если ДанныеРазбора.Свойство("ВидПродукции") И ЗначениеЗаполнено(ДанныеРазбора.ВидПродукции) Тогда
			Если ТипЗнч(ДанныеРазбора.ВидПродукции) = Тип("СправочникСсылка.ВидыПродукцииИС") Тогда
				ДанныеШтрихкода.Вставить("ВидПродукции", ДанныеРазбора.ВидПродукции);
			ИначеЕсли ТипЗнч(ДанныеРазбора.ВидПродукции) = Тип("Массив") И ДанныеРазбора.ВидПродукции.Количество() = 1 Тогда
				ДанныеШтрихкода.Вставить("ВидПродукции", ДанныеРазбора.ВидПродукции[0]);
			КонецЕсли;
		КонецЕсли;
		Если Не ДанныеШтрихкода.Свойство("ВидУпаковки") Тогда
			ДанныеШтрихкода.Вставить("ВидУпаковки", ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.ПустаяСсылка"));
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры

Процедура ОбработатьТребованиеПолногоКода(ДанныеШтрихкода, СтрокаДанных, ПараметрыСканирования) Экспорт
	
	Если СтрокаДанных.СостояниеТребованияПолногоКодаККТ <> 1 Тогда
		Возврат;
	КонецЕсли;
	
	Если ДанныеШтрихкода.Свойство("ПолныйКодМаркировки") Тогда
		
		СтрокаДанных.СостояниеТребованияПолногоКодаККТ = 2;
		СтрокаДанных.ТекстОшибкиПроверкиСредствамиККТ  = НСтр("ru = 'Будет выполнена перед пробитием чека';
																|en = 'Будет выполнена перед пробитием чека'");
		
		Если Не ДанныеШтрихкода.ЕстьВПулеКодовМаркировки Тогда
			
			ПроверкаИПодборПродукцииИСМПВызовСервера.СохранитьПолныйКодМаркировкиВПулПриСканированииСуществующего(
				ДанныеШтрихкода,
				ПараметрыСканирования);
				
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ТипУпаковкиПотребительскиеУпаковкиБезУпаковки() Экспорт
	Возврат ПредопределенноеЗначение("Перечисление.ПрочиеЗоныПересчетаПродукцииИС.ПачкиБезБлока");
КонецФункции

Функция ТипУпаковкиГрупповыеУпаковкиБезКоробки() Экспорт
	Возврат ПредопределенноеЗначение("Перечисление.ПрочиеЗоныПересчетаПродукцииИС.БлокиБезКоробки");
КонецФункции

Функция ТипУпаковкиОбъемноСортовойУчет() Экспорт
	Возврат ПредопределенноеЗначение("Перечисление.ПрочиеЗоныПересчетаПродукцииИС.ОбъемноСортовойУчет");
КонецФункции

Функция ТипУпаковкиГруппировкаОбъемноСортовогоУчетаПоGTIN() Экспорт
	Возврат ПредопределенноеЗначение("Перечисление.ПрочиеЗоныПересчетаПродукцииИС.ГруппировкаОбъемноСортовогоУчетаПоGTIN");
КонецФункции

Функция РекомендуемыйРежимДобавленияКодовМаркировки(Форма) Экспорт
	
	Если Форма.ПроверкаНеПоДокументу
		Или Не Форма.ПоддерживаетсяОбъемноСортовойУчет Тогда
		
		Возврат ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.Поштучно");
		
	ИначеЕсли Форма.ЭтоДокументПриобретения Тогда
		
		Возврат ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.ОСУ");
		
	ИначеЕсли Не ЭтоДокументРеализации(Форма.ПроверяемыйДокумент) Тогда
		
		Возврат ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.Поштучно");
		
	ИначеЕсли Не ЗначениеЗаполнено(Форма.РежимДобавленияКодовМаркировки) Тогда
		
		Возврат ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.Поштучно");
		
	ИначеЕсли Форма.РежимДобавленияКодовМаркировки = ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.ОСУ") Тогда
		
		Возврат Форма.РежимДобавленияКодовМаркировки;
		
	ИначеЕсли Форма.ПараметрыПроверкиКодовМаркировки.ОтсутствуетПодключениеИСМП
		Или Не Форма.ПараметрыПроверкиКодовМаркировки.ЗапрашиватьДанныеСервиса Тогда
		
		Возврат ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.Поштучно");
		
	КонецЕсли;
	
	Возврат Форма.РежимДобавленияКодовМаркировки;
	
КонецФункции

Функция ДопустимыеРежимыДобавленияКодовМаркировки(Форма) Экспорт
	
	Если Не Форма.ПоддерживаетсяОбъемноСортовойУчет Или Форма.ПроверкаНеПоДокументу Тогда
		
		Возврат ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
			ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.Поштучно"));
		
	ИначеЕсли Форма.ЭтоДокументПриобретения Тогда
		
		Возврат ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
			ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.ОСУ"));
		
	ИначеЕсли Не ЭтоДокументРеализации(Форма.ПроверяемыйДокумент) Тогда
		
		Возврат ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
			ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.Поштучно"));
		
	КонецЕсли;
	
	ДопустимыеРежимы = Новый Массив;
	ДопустимыеРежимы.Добавить(ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.Поштучно"));
	ДопустимыеРежимы.Добавить(ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.ОСУ"));
	
	Если Не Форма.ПараметрыПроверкиКодовМаркировки.ОтсутствуетПодключениеИСМП
		И Форма.ПараметрыПроверкиКодовМаркировки.ЗапрашиватьДанныеСервиса Тогда
		
		ДопустимыеРежимы.Добавить(ПредопределенноеЗначение("Перечисление.РежимДобавленияКодовМаркировкиИС.ПоВладельцу"));
		
	КонецЕсли;
	
	Возврат ДопустимыеРежимы;
	
КонецФункции

Функция ДокументИспользуетДанныеВЕТИС(ДокументСсылка) Экспорт
	Возврат ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.МаркировкаТоваровИСМП")
		Или ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.УточнениеСведенийОКодахМаркировкиИСМП");
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область ПодобраннаяМаркируемаяПродукция

Процедура ЗаполнитьСвойствоВключаетМРЦ(ВидПродукции, НоваяСтрока, СтрокаИсточника) Экспорт
	
	Если Не КонструкторИСМПОбщегоНазначенияКлиентСервер.ДоступноПолеМРЦ(ВидПродукции) Тогда
		Возврат;
	КонецЕсли;
	
	НоваяСтрока.ВключаетМРЦ = (НоваяСтрока.МРЦ <> 0);
	
	Если НоваяСтрока.ВключаетМРЦ Или СтрокаИсточника.ДанныеРазбора = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если СтрокаИсточника.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Потребительская")
		Или СтрокаИсточника.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Групповая") Тогда
		
		НоваяСтрока.ВключаетМРЦ = СтрокаИсточника.ДанныеРазбора.СоставКодаМаркировки.Свойство("ВключаетМРЦ")
			И СтрокаИсточника.ДанныеРазбора.СоставКодаМаркировки.ВключаетМРЦ;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

Функция ПоддерживаетсяОбъемноСортовойУчет(ПараметрыСканирования, ВидПродукции) Экспорт
	
	Возврат ПараметрыСканирования.ПоддерживаетсяОбъемноСортовойУчет
		И КонструкторИСМПОбщегоНазначенияКлиентСервер.ПоддерживаетсяОбъемноСортовойУчет(
			ВидПродукции, ПараметрыСканирования.ВидОперацииИСМП);
	
КонецФункции

Функция ВладелецМожетОтличатьсяПоДатеПроизводства(СтрокаДанных, ПараметрыПроверкиКодовМаркировки)
	
	Если Не (СтрокаДанных.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Потребительская")
			Или СтрокаДанных.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Групповая")
			Или СтрокаДанных.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Набор"))
		Или Не ОбщегоНазначенияИСМПКлиентСерверПовтИсп.ВидОперацииПоддерживаетПроверкуВладельцаПоДатеПроизводства(ПараметрыПроверкиКодовМаркировки.ВидОперацииИСМП) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ДатаКонтроляВладельцаПоДатеПроизводства = ОбщегоНазначенияИСМПКлиентСерверПовтИсп.ДатаНачалаКонтроляПриПродажеОптомПоштучно(
		СтрокаДанных.ВидПродукции);
		
	Если ЗначениеЗаполнено(ДатаКонтроляВладельцаПоДатеПроизводства) Тогда
		
		Если ЗначениеЗаполнено(СтрокаДанных.ДатаВводаВОборот) Тогда
			ДатаКонтроля = СтрокаДанных.ДатаВводаВОборот;
		Иначе
			ДатаКонтроля = СтрокаДанных.ДатаПроизводства;
		КонецЕсли;
		
		ДатаНачалаКонтроляПриПродажеОптомПоштучно = ДатаКонтроляВладельцаПоДатеПроизводства;
		
		Возврат (ЗначениеЗаполнено(ДатаКонтроля) И НачалоДня(ДатаКонтроля) < ДатаНачалаКонтроляПриПродажеОптомПоштучно);
	
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Процедура ПроверитьДопустимуюВложенностьУпаковок(ТекущаяСтрока, ПараметрыПроверкиКодовМаркировки)
	
	Если ТипЗнч(ТекущаяСтрока) = Тип("ДанныеФормыЭлементДерева") Тогда
		РодительТекущейСтроки = ТекущаяСтрока.ПолучитьРодителя();
	Иначе
		РодительТекущейСтроки = ТекущаяСтрока.Родитель;
	КонецЕсли;
	
	Если РодительТекущейСтроки <> Неопределено И ТипЗнч(РодительТекущейСтроки.ТипУпаковки) = Тип("ПеречислениеСсылка.ТипыУпаковок") Тогда
		
		ДопустимыеВложенияАгрегатов = ПараметрыПроверкиКодовМаркировки.ДопустимыеВложенияАгрегатов;
		
		ДопустимыеВложенияПоВидуУпаковки  = ДопустимыеВложенияАгрегатов.Получить(РодительТекущейСтроки.ВидУпаковки);
		ДопустимыеВложенияПоТипуШтрихкода = ДопустимыеВложенияАгрегатов.Получить(РодительТекущейСтроки.ТипШтрихкода);
		
		СтрокаОшибки = "";
		
		Если ДопустимыеВложенияПоВидуУпаковки <> Неопределено
			И ДопустимыеВложенияПоВидуУпаковки.Найти(ТекущаяСтрока.ВидУпаковки) = Неопределено Тогда
			
			СтрокаОшибки = СтрШаблон(
				НСтр("ru = '%1 упаковка не может содержать %2';
					|en = '%1 упаковка не может содержать %2'"),
				РодительТекущейСтроки.ВидУпаковки,
				ТекущаяСтрока.ВидУпаковки);
			
		ИначеЕсли ТекущаяСтрока.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Логистическая") Тогда
			
			Если ДопустимыеВложенияПоТипуШтрихкода <> Неопределено
				И ДопустимыеВложенияПоТипуШтрихкода.Найти(ТекущаяСтрока.ТипШтрихкода) = Неопределено Тогда
				
				СтрокаОшибки = СтрШаблон(
					НСтр("ru = 'Упаковка %1 не может содержать %2';
						|en = 'Упаковка %1 не может содержать %2'"),
					РодительТекущейСтроки.ТипШтрихкода,
					ТекущаяСтрока.ТипШтрихкода);
				
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтрокаОшибки) Тогда
			
			РодительТекущейСтроки.НедопустимыйКодМаркировки = Истина;
			РодительТекущейСтроки.ПредставлениеПроверкиКодаМаркировки = СтрокаОшибки;
			
		КонецЕсли;
		
		Если ПараметрыПроверкиКодовМаркировки.Свойство("КонтролироватьСоставГрупповойУпаковкиПриИзмененииСтрокиДерева")
			И ПараметрыПроверкиКодовМаркировки.КонтролироватьСоставГрупповойУпаковкиПриИзмененииСтрокиДерева Тогда
			
			ПроверитьСоставАгрегата(РодительТекущейСтроки, ПараметрыПроверкиКодовМаркировки);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция РазрешительнаяДокументацияТребуетПроверкиПоДатеПроизводства(СтрокаДанных, ПараметрыПроверкиКодовМаркировки) Экспорт
	
	ДатаНачалаКонтроляРазрешительнойДокументации = ОбщегоНазначенияИСМПКлиентСерверПовтИсп.ДатаКонтроляРазрешительнойДокументации(
		СтрокаДанных.ВидПродукции);
	
	Если Не ЗначениеЗаполнено(ДатаНачалаКонтроляРазрешительнойДокументации)
		Или Не СтрокаДанных.ВидУпаковки = ПредопределенноеЗначение("Перечисление.ВидыУпаковокИС.Потребительская") Тогда
		Возврат Ложь;
	Иначе
		
		Если ЗначениеЗаполнено(СтрокаДанных.ДатаВводаВОборот) Тогда
			ДатаКонтроля = СтрокаДанных.ДатаВводаВОборот;
		Иначе
			ДатаКонтроля = СтрокаДанных.ДатаПроизводства;
		КонецЕсли;
		
		Возврат (ЗначениеЗаполнено(ДатаКонтроля) И ДатаНачалаКонтроляРазрешительнойДокументации < НачалоДня(ДатаКонтроля));
		
	КонецЕсли;
	
КонецФункции

#КонецОбласти
