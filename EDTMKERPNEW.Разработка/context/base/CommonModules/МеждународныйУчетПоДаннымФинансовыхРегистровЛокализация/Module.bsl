#Область СлужебныйПрограммныйИнтерфейс

// Дополняет соответствие объектов финансового учета статьям активов и пассивов.
// Параметры:
//  ИсточникиДанныхОбъектовУчета - см. Перечисления.УдалитьОбъектыФинансовогоУчета.СоответствиеОбъектовФинансовогоУчетаСтатьямАктивовПассивов
//
Процедура ДополнитьСоответствиеОбъектовФинансовогоУчетаСтатьямАктивовПассивов(СоответствиеОбъектовСтатьям) Экспорт
	
	//++ Локализация
	
	ОбъектыФинансовогоУчета = Перечисления.УдалитьОбъектыФинансовогоУчета;
	СтатьиАктивовПассивов = ПланыВидовХарактеристик.СтатьиАктивовПассивов;
	
	СоответствиеОбъектовСтатьям.Вставить(ОбъектыФинансовогоУчета.ДенежныеДокументы, СтатьиАктивовПассивов.ДенежныеДокументы);
	СоответствиеОбъектовСтатьям.Вставить(ОбъектыФинансовогоУчета.РезервыПоВознаграждениям, СтатьиАктивовПассивов.РезервыПоВознаграждениям);
	СоответствиеОбъектовСтатьям.Вставить(ОбъектыФинансовогоУчета.РезервыПоСтраховымВзносам, СтатьиАктивовПассивов.РезервыПоСтраховымВзносам);
	
	//-- Локализация
	
КонецПроцедуры

// Дополняет соответствие объектов финансового учета и источников их данных - имен балансовых регистров накопления.
// Параметры:
//  ИсточникиДанныхОбъектовУчета - см. Перечисления.УдалитьОбъектыФинансовогоУчета.ИсточникиДанныхОбъектовФинансовогоУчета
//
Процедура ДополнитьИсточникиДанныхОбъектовФинансовогоУчета(ИсточникиДанныхОбъектовУчета) Экспорт
	
	//++ Локализация
	
	ОбъектыФинансовогоУчета = Перечисления.УдалитьОбъектыФинансовогоУчета;
	РегистрыНакопленияМетаданные = Метаданные.РегистрыНакопления;
	
	ИсточникиДанныхОбъектовУчета.Вставить(ОбъектыФинансовогоУчета.ДенежныеДокументы, РегистрыНакопленияМетаданные.ДенежныеДокументы.Имя);
	ИсточникиДанныхОбъектовУчета.Вставить(ОбъектыФинансовогоУчета.РезервыПоВознаграждениям, РегистрыНакопленияМетаданные.ДвиженияПоПрочимАктивамПассивам.Имя);
	ИсточникиДанныхОбъектовУчета.Вставить(ОбъектыФинансовогоУчета.РезервыПоСтраховымВзносам, РегистрыНакопленияМетаданные.ДвиженияПоПрочимАктивамПассивам.Имя);
	
	//-- Локализация
	
КонецПроцедуры

// Добавляет записи в таблицу описания объектов финансового учета.
// 
// Параметры:
// 	ОбъектыФинансовогоУчета - см.Перечисления.УдалитьОбъектыФинансовогоУчета.ОписаниеОбъектовФинансовогоУчета
// 	НастройкиПроводок - см. Справочники.НастройкиФормированияПроводокМеждународногоУчета.НастройкиФормированияПроводок
// 
Процедура ДополнитьОписаниеОбъектовФинансовогоУчета(ОбъектыФинансовогоУчета, НастройкиПроводок) Экспорт
	
	//++ Локализация
	
	СтрокаДенежныеДокументы = ОбъектыФинансовогоУчета.Добавить();
	СтрокаДенежныеДокументы.ОбъектУчета = Перечисления.УдалитьОбъектыФинансовогоУчета.ДенежныеДокументы;
	СтрокаДенежныеДокументы.ФункциональныеОпции = Метаданные.ФункциональныеОпции.ИспользоватьРеглУчет.Имя;
	
	СтрокаРезервыПоВознаграждениям = ОбъектыФинансовогоУчета.Добавить();
	СтрокаРезервыПоВознаграждениям.ОбъектУчета = Перечисления.УдалитьОбъектыФинансовогоУчета.РезервыПоВознаграждениям;
	ФункциональныеОпции = Новый Массив();
	ФункциональныеОпции.Добавить(Метаданные.ФункциональныеОпции.ИспользоватьРезервыПоОплатеТруда.Имя);
	ФункциональныеОпции.Добавить(Метаданные.ФункциональныеОпции.ИспользоватьРасчетРезервовОтпусков.Имя);
	СтрокаРезервыПоВознаграждениям.ФункциональныеОпции = ФункциональныеОпции;
	СтрокаРезервыПоВознаграждениям.ПутьКДаннымОбъектНастройки = "Аналитика";
	ОтборДанных = Новый Соответствие();
	ОтборДанных.Вставить("Статья", ПланыВидовХарактеристик.СтатьиАктивовПассивов.РезервыПоВознаграждениям);
	СтрокаРезервыПоВознаграждениям.ОтборДанных = ОтборДанных;
	
	СтрокаРезервыПоСтраховымВзносам = ОбъектыФинансовогоУчета.Добавить();
	СтрокаРезервыПоСтраховымВзносам.ОбъектУчета = Перечисления.УдалитьОбъектыФинансовогоУчета.РезервыПоСтраховымВзносам;
	ФункциональныеОпции = Новый Массив();
	ФункциональныеОпции.Добавить(Метаданные.ФункциональныеОпции.ИспользоватьРезервыПоОплатеТруда.Имя);
	ФункциональныеОпции.Добавить(Метаданные.ФункциональныеОпции.ИспользоватьРасчетРезервовОтпусков.Имя);
	СтрокаРезервыПоСтраховымВзносам.ФункциональныеОпции = ФункциональныеОпции;
	СтрокаРезервыПоСтраховымВзносам.ПутьКДаннымОбъектНастройки = "Аналитика";
	ОтборДанных = Новый Соответствие();
	ОтборДанных.Вставить("Статья", ПланыВидовХарактеристик.СтатьиАктивовПассивов.РезервыПоСтраховымВзносам);
	СтрокаРезервыПоСтраховымВзносам.ОтборДанных = ОтборДанных;
	
	//-- Локализация
	
КонецПроцедуры

// Добавляет элементы в соответствие расширенного описания объектов финансового учета.
// 
// Параметры:
//  ОписаниеОбъектовУчетаДляНастройкиФорм - Соответствие Из КлючИЗначение - данные расширенного описания объектов финансового учета:
//  	* Ключ - ПеречислениеСсылка.УдалитьОбъектыФинансовогоУчета - объект финансового учета
//  	* Значение - см. Перечисления.УдалитьОбъектыФинансовогоУчета.ОписаниеОбъектаУчетаДляНастройкиФорм
//  ГруппыОбъектовУчета - см. Перечисления.УдалитьОбъектыФинансовогоУчета.ГруппыОбъектовУчета
//  НастройкиПроводок - см. Справочники.НастройкиФормированияПроводокМеждународногоУчета.НастройкиФормированияПроводок
//
Процедура ДополнитьРасширенноеОписаниеОбъектовУчетаДляНастройки(
	ОписаниеОбъектовУчетаДляНастройкиФорм, ГруппыОбъектовУчета, НастройкиПроводок) Экспорт
	
	//++ Локализация
	
	ОписаниеОбъектаУчета = Перечисления.УдалитьОбъектыФинансовогоУчета.ОписаниеОбъектаУчетаДляНастройкиФорм();
	ОписаниеОбъектаУчета.Группа = ГруппыОбъектовУчета.ДережныеСредства;
	ОписаниеОбъектаУчета.Представление = НСтр("ru = 'В денежных документах';
												|en = 'In financial documents'");
	ОписаниеОбъектаУчета.ЗаголовокТаблицыНастройкиСчетов = НСтр("ru = 'Счета учета денежных документов';
																|en = 'Ledger accounts of financial documents'");
	ОписаниеОбъектаУчета.ОписаниеТиповОбъектНастройки = Неопределено;
	ОписаниеОбъектаУчета.ОписаниеТиповМестоУчета = Новый ОписаниеТипов();
	ОписаниеОбъектовУчетаДляНастройкиФорм.Вставить(Перечисления.УдалитьОбъектыФинансовогоУчета.ДенежныеДокументы, ОписаниеОбъектаУчета);
	
	ОписаниеОбъектаУчета = Перечисления.УдалитьОбъектыФинансовогоУчета.ОписаниеОбъектаУчетаДляНастройкиФорм();
	ОписаниеОбъектаУчета.Группа = ГруппыОбъектовУчета.РезервыПоОплатеТруда;
	ОписаниеОбъектаУчета.Представление = НСтр("ru = 'Резервы по вознаграждениям';
												|en = 'Remuneration provisions'");
	ОписаниеОбъектаУчета.ЗаголовокТаблицыНастройкиСчетов = НСтр("ru = 'Счета учета оценочных обязательств по вознаграждениям';
																|en = 'GL accounts of provisions for remunerations'");
	ОписаниеОбъектаУчета.ОписаниеТиповОбъектНастройки = Новый ОписаниеТипов("СправочникСсылка.Резервы");
	ОписаниеОбъектаУчета.ОтборОбъектовНастройки = Новый Структура(
		Метаданные.Справочники.Резервы.Реквизиты.ТипРезерва.Имя,
		Перечисления.ТипыРезервовПредстоящихРасходов.ОплатаТруда);
	ОписаниеОбъектаУчета.ОписаниеТиповМестоУчета = Новый ОписаниеТипов();
	ОписаниеОбъектовУчетаДляНастройкиФорм.Вставить(Перечисления.УдалитьОбъектыФинансовогоУчета.РезервыПоВознаграждениям, ОписаниеОбъектаУчета);
	
	ОписаниеОбъектаУчета = Перечисления.УдалитьОбъектыФинансовогоУчета.ОписаниеОбъектаУчетаДляНастройкиФорм();
	ОписаниеОбъектаУчета.Группа = ГруппыОбъектовУчета.РезервыПоОплатеТруда;
	ОписаниеОбъектаУчета.Представление = НСтр("ru = 'Резервы по страховым взносам';
												|en = 'Insurance contribution provisions'");
	ОписаниеОбъектаУчета.ЗаголовокТаблицыНастройкиСчетов = НСтр("ru = 'Счета учета оценочных обязательств по страховым взносам';
																|en = 'GL accounts of provisions for insurance contributions'");
	ОписаниеОбъектаУчета.ОписаниеТиповОбъектНастройки = Новый ОписаниеТипов("СправочникСсылка.Резервы");
	ОписаниеОбъектаУчета.ОтборОбъектовНастройки = Новый Структура(
		Метаданные.Справочники.Резервы.Реквизиты.ТипРезерва.Имя,
		Перечисления.ТипыРезервовПредстоящихРасходов.ОплатаТруда);
	ОписаниеОбъектаУчета.ОписаниеТиповМестоУчета = Новый ОписаниеТипов();
	ОписаниеОбъектовУчетаДляНастройкиФорм.Вставить(Перечисления.УдалитьОбъектыФинансовогоУчета.РезервыПоСтраховымВзносам, ОписаниеОбъектаУчета);
	
	//-- Локализация
	
КонецПроцедуры

#КонецОбласти