#Область ПрограммныйИнтерфейс

// Возвращает структуру параметров отражения записей регистров накопления в финансовом учете.
// Если какой-либо параметр является выражением языка запросов, то описания полей в нем должны содержать 
// псевдоним источника данных, переданный через параметр ПсевдонимИсточникаДанных. Если параметр является 
// только именем поля, в том числе разыменованным через точку, его можно указывать без псевдонима источника данных.
// 
// Параметры:
//  РегистрацияКОтражению - Булево - Признак получения параметров для регистрации к отражению в учете
// 
// Возвращаемое значение:
//  Структура - Описание:
//  * ИнверсияДвижений - Булево - Только для регистров остатков. Признак соответствия движений регистра правилу Приход = Кредит, Расход = Дебет.
//  * ВыделениеДолгосрочныхАктивовОбязательств - Булево - Признак деления учетных данных регистра на краткосрочные и долгосрочные.
//  * ПутьКДаннымОрганизация - Строка - Путь выборки значения организации из данных регистра
//  * ПутьКДаннымРегистратор - Строка - Путь выборки значения документа движения из данных регистра
//  * ПутьКДаннымОбъектНастройки - Строка - Путь выборки данных регистра, соответствующих объектам настроек, в разрезе которых указываются счета учета. Может быть переопределено в описании объектов бух.учета
//  * ПутьКДаннымМестоУчета - Строка - Путь выборки данных регистра, соответствующих местам учета, в разрезе которых указываются исключения счетов учета
//  * ПутьКДаннымИдентификаторФинЗаписи - Строка - Путь выборки значения идентификатора записи регистра
//  * ПутьКДаннымНастройкаХозОперации - Строка - Путь выборки значения настройки хозяйственной операции
//  * ПутьКДаннымНаправлениеДеятельности - Строка - Путь выборки значения направления деятельности из данных регистра
//  * ПутьКДаннымИдентификаторСтроки - Строка - Путь выборки значения идентификатора строки из данных регистра. Может быть переопределено в описании объектов бух.учета
//  * ПутьКДаннымВидДеятельностиНДС - Строка - Путь выборки значения вида деятельности по учету НДС из данных регистра
//  * ПутьКДаннымПодразделение - Строка - Путь выборки значения подразделения из данных регистра
//  * ПутьКДаннымДатаПогашения - Строка - Путь выборки данных регистра, соответствующих датам погашения обязательств
//  * ПутьКДаннымВалюта - Строка - Путь выборки значения валюты из данных регистра, пустая строка если в регистре не ведется валютный учет
//  * ПутьКДаннымСторно - Строка - Путь выборки признака исправительной записи корректирующего документа
//  * ОписаниеИзмерений - Структура Из КлючИЗначение - Описание:
//   ** Ключ - Строка - Имя измерения регистра или реквизита измерения если оно является ключем аналитики
//   ** Значение - см. ОписаниеПоляИсточникаДанных
//  * ОписаниеРеквизитов - Структура Из КлючИЗначение - Описание:
//   ** Ключ - Строка - Имя реквизита регистра
//   ** Значение - см. ОписаниеПоляИсточникаДанных
//  * РесурсыУпр - Массив из Строка - Массив строк с именами всех используемых ресурсов, хранящих сумму операции в валюте управленческого учета. Может быть переопределено в описании объектов бух.учета. Может быть пустым при отсутствии упр. учета в регистре
//  * РесурсыРегл - Массив из Строка - Массив строк с именами всех используемых ресурсов, хранящих сумму операции в валюте регламентированного учета. Может быть переопределено в описании объектов бух.учета. Может быть пустым при отсутствии регл. учета в регистре
//  * РесурсыВал - Массив из Строка - Массив строк с именами всех используемых ресурсов, хранящих валютную сумму операции, если в регистре ведется валютный учет. Может быть переопределено в описании объектов бух.учета. Может быть пустым при отсутствии вал. учета в регистре
//  * РесурсыКоличество - Массив из Строка - Массив строк с именами ресурсов, хранящих количество операции. Может быть пустым при отсутствии количественного учета в регистре
//  * УсловиеДебет - Строка - Условие на языке запросов для определения признака дебета движения в оборотных регистрах и остаточных регистрах со сложной логикой
//  * УсловиеКредит - Строка - Условие на языке запросов для определения признака кредита движения в оборотных регистрах и остаточных регистрах со сложной логикой
//  * ПсевдонимИсточникаДанных - Строка - псевдоним источника данных, используемый в параметрах, являющимися выражениями языка запросов.
//  * ПсевдонимИсточникаДанных - Строка - псевдоним источника данных, используемый в параметрах, являющимися выражениями языка запросов.
//  * ДополнительныйОтбор - Строка - Условие на языке запросов для дополнительного отбора данных регистра.
//  * СодержитНеБалансовыеДанные - Булево - Признак наличия в регистре не балансовых данных, которые не могут быть отфильтрованы через свойство ДополнительныйОтбор, а фильтруются через отборы данных статей активов и пассивов. 
// 
Функция ПараметрыОтраженияДвиженийВФинансовомУчете(РегистрацияКОтражению = Ложь) Экспорт
	
	ПараметрыОтражения = Новый Структура();
	ПараметрыОтражения.Вставить("ПутьКДаннымОрганизация", "Организация");
	ПараметрыОтражения.Вставить("ПутьКДаннымРегистратор", "Регистратор");
	ПараметрыОтражения.Вставить("ПсевдонимИсточникаДанных", ПсевдонимИсточникаДанныхПоУмолчанию());
	
	Если РегистрацияКОтражению Тогда
		Возврат ПараметрыОтражения;
	КонецЕсли;
	
	ПараметрыОтражения.Вставить("ИнверсияДвижений", Ложь);
	ПараметрыОтражения.Вставить("ВыделениеДолгосрочныхАктивовОбязательств", Ложь);
	ПараметрыОтражения.Вставить("ПутьКДаннымОбъектНастройки", "");
	ПараметрыОтражения.Вставить("ПутьКДаннымМестоУчета", "");
	ПараметрыОтражения.Вставить("ПутьКДаннымИдентификаторФинЗаписи", "ИдентификаторФинЗаписи");
	ПараметрыОтражения.Вставить("ПутьКДаннымНастройкаХозОперации", "НастройкаХозяйственнойОперации");
	ПараметрыОтражения.Вставить("ПутьКДаннымНаправлениеДеятельности", "");
	ПараметрыОтражения.Вставить("ПутьКДаннымИдентификаторСтроки", "");
	ПараметрыОтражения.Вставить("ПутьКДаннымВидДеятельностиНДС", "");
	ПараметрыОтражения.Вставить("ПутьКДаннымПодразделение", "");
	ПараметрыОтражения.Вставить("ПутьКДаннымДатаПогашения", "");
	ПараметрыОтражения.Вставить("ПутьКДаннымВалюта", "");
	ПараметрыОтражения.Вставить("ПутьКДаннымСторно", "");
	ПараметрыОтражения.Вставить("ОписаниеИзмерений", Новый Структура);
	ПараметрыОтражения.Вставить("ОписаниеРеквизитов", Новый Структура);
	ПараметрыОтражения.Вставить("РесурсыУпр", Новый Массив());
	ПараметрыОтражения.Вставить("РесурсыРегл", Новый Массив());
	ПараметрыОтражения.Вставить("РесурсыВал", Новый Массив());
	ПараметрыОтражения.Вставить("РесурсыКоличество", Новый Массив());
	ПараметрыОтражения.Вставить("УсловиеДебет", "");
	ПараметрыОтражения.Вставить("УсловиеКредит", "");
	ПараметрыОтражения.Вставить("ДополнительныйОтбор", "");
	ПараметрыОтражения.Вставить("СодержитНеБалансовыеДанные", Ложь);
	
	//++ НЕ УТ
	
	ФактическиеДанныеБюджетированияСервер.ДополнитьПараметрыОтраженияДвиженийВФинансовомУчете(ПараметрыОтражения);
	
	//-- НЕ УТ
	
	Возврат ПараметрыОтражения;
	
КонецФункции

// Заполняет параметры отражения движений в фин.учете по метаданным регистра накопления
//
// Параметры:
//  ПараметрыОтражения - см. ПараметрыОтраженияДвиженийВФинансовомУчете
//  МетаданныеРегистра - ОбъектМетаданныхРегистрНакопления - Метаданные регистра накопления
//	НедоступныеПоля - Массив - Массив имен полей, которые не доступны в качестве источников заполнения субконто
//
Процедура ЗаполнитьПараметрыОтраженияПоМетаданнымРегистра(ПараметрыОтражения, МетаданныеРегистра, НедоступныеПоля = Неопределено) Экспорт
	
	Для каждого Измерение Из МетаданныеРегистра.Измерения Цикл
		Если НедоступныеПоля <> Неопределено И НедоступныеПоля.Найти(Измерение.Имя) <> Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Если СтрНачинаетсяС(Измерение.Имя, "Удалить") Тогда
			Продолжить;
		КонецЕсли;
		
		ОписаниеПоляРегистра = ОписаниеПоляИсточникаДанных(Измерение.Имя, Измерение.Синоним, Измерение.Тип);
		ПараметрыОтражения.ОписаниеИзмерений.Вставить(Измерение.Имя, ОписаниеПоляРегистра);
		
		Если Измерение.Имя = "ВидДеятельностиНДС" Тогда
			ПараметрыОтражения.ПутьКДаннымВидДеятельностиНДС = Измерение.Имя;
		КонецЕсли;
	КонецЦикла;
	
	ДобавитьСоставКлючейАналитикиВОписаниеИзмерений(ПараметрыОтражения.ОписаниеИзмерений, МетаданныеРегистра);
	
	РегистраторЭтоРеквизит = Ложь;
	
	Для Каждого Реквизит Из МетаданныеРегистра.Реквизиты Цикл
		Если НедоступныеПоля <> Неопределено И НедоступныеПоля.Найти(Реквизит.Имя) <> Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Если СтрНачинаетсяС(Реквизит.Имя, "Удалить")
		 ИЛИ Реквизит.Тип.СодержитТип(Тип("ХранилищеЗначения")) Тогда
			Продолжить;
		КонецЕсли;
		
		Если Реквизит.Имя = ПараметрыОтражения.ПутьКДаннымРегистратор Тогда
			ИмяПоля = "Регистратор";
			СинонимПоля = МетаданныеРегистра.СтандартныеРеквизиты.Регистратор.Синоним;
			РегистраторЭтоРеквизит = Истина;
		Иначе
			ИмяПоля = Реквизит.Имя;
			СинонимПоля = Реквизит.Синоним
		КонецЕсли;
		
		ОписаниеПоляРегистра = ОписаниеПоляИсточникаДанных(Реквизит.Имя, СинонимПоля, Реквизит.Тип);
		ПараметрыОтражения.ОписаниеРеквизитов.Вставить(ИмяПоля, ОписаниеПоляРегистра);
		
		Если Реквизит.Имя = "ИдентификаторСтроки" Тогда
			ПараметрыОтражения.ПутьКДаннымИдентификаторСтроки = Реквизит.Имя;
		ИначеЕсли Реквизит.Имя = "Сторно" Тогда
			ПараметрыОтражения.ПутьКДаннымСторно = Реквизит.Имя;
		КонецЕсли;
	КонецЦикла;
	
	Если НЕ РегистраторЭтоРеквизит Тогда
		ОписаниеПоляРегистра = ОписаниеПоляИсточникаДанных(
			МетаданныеРегистра.СтандартныеРеквизиты.Регистратор.Имя,
			МетаданныеРегистра.СтандартныеРеквизиты.Регистратор.Синоним,
			МетаданныеРегистра.СтандартныеРеквизиты.Регистратор.Тип);
		ПараметрыОтражения.ОписаниеРеквизитов.Вставить("Регистратор", ОписаниеПоляРегистра);
	КонецЕсли;
	
КонецПроцедуры

// Конструктор описания поля регистра - источника субконто или отбора движений
//
// Параметры:
//  ПутьКДанным - Строка - Полный путь к полю источнику субконто
//  Заголовок - Строка - Заголовок поля источника субконто
//  Тип - ОписаниеТипов - Тип данных, который хранится в поле
//
// Возвращаемое значение:
// 	Структура - Описание:
// 		* ПутьКДанным - Строка - Полный путь к полю источнику субконто
// 		* Заголовок - Строка - Заголовок поля источника субконто
// 		* Тип - ОписаниеТипов - Тип данных, который хранится в поле
//
Функция ОписаниеПоляИсточникаДанных(ПутьКДанным = "", Заголовок = "", Тип = Неопределено) Экспорт
	
	Возврат Новый Структура(
		"ПутьКДанным, Заголовок, Тип",
		ПутьКДанным,
		Заголовок,
		?(Тип = Неопределено, Новый ОписаниеТипов(), Тип));
	
КонецФункции

// Возвращает строку - текст выборки значения ГФУ номенклатуры из регистров ВыручкаИСебестоимостьПродаж и СебестоимостьТоваров
//
// Параметры:
//  ПсевдонимИсточника - Строка - псевдоним источника данных в запросе
//
// Возвращаемое значение:
//  Строка - текст выборки значения ГФУ номенклатуры
//
Функция ПолеВыборкиГФУНоменклатуры(ПсевдонимИсточника = Неопределено) Экспорт
	
	Если ПсевдонимИсточника = Неопределено Тогда
		ПсевдонимИсточника = ПсевдонимИсточникаДанныхПоУмолчанию();
	КонецЕсли;
	
	ПолеВыборкиГФУ = "ВЫБОР
	|		КОГДА ПсевдонимИсточника.АналитикаУчетаНоменклатуры.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа)
	|			ТОГДА ПсевдонимИсточника.АналитикаУчетаНоменклатуры.Номенклатура.ГруппаФинансовогоУчета
	|		КОГДА &ФормироватьВидыЗапасовПоГруппамФинансовогоУчета И ПсевдонимИсточника.ВидЗапасов <> ЗНАЧЕНИЕ(Справочник.ВидыЗапасов.ПустаяСсылка)
	|			ТОГДА ЕСТЬNULL(ПсевдонимИсточника.ВидЗапасов.ГруппаФинансовогоУчета, ЗНАЧЕНИЕ(Справочник.ГруппыФинансовогоУчетаНоменклатуры.ПустаяСсылка))
	|		ИНАЧЕ ПсевдонимИсточника.АналитикаУчетаНоменклатуры.Номенклатура.ГруппаФинансовогоУчета
	|	КОНЕЦ";
	
	ПолеВыборкиГФУ = СтрЗаменить(ПолеВыборкиГФУ, "ПсевдонимИсточника", ПсевдонимИсточника);
	
	Возврат ПолеВыборкиГФУ;
	
КонецФункции

// Возвращает описание примитивных типов данных
//
// Возвращаемое значение:
//  ОписаниеТипов
//
Функция ОписаниеПримитивныхТипов() Экспорт
	
	Возврат Новый ОписаниеТипов("Строка, Число, Дата, Булево, Тип, Неопределено, УникальныйИдентификатор");
	
КонецФункции

// Рекурсивно копирует отбор компоновки данных из источника в приемник с добавлением префикса и/или постфикса
// к имени поля компоновки данных.
//
// Параметры:
//  ОтборПриемник - ОтборКомпоновкиДанных - отбор, в который копируются элементы отбора
//  ОтборИсточник - ОтборКомпоновкиДанных - отбор, из которого копируются элементы отбора
//  Префикс - Строка - Символы, добавляемые слева к имени поля компоновки данных
//  Постфикс - Строка - Символы, добавляемые справа к имени поля компоновки данных.
//
Процедура СкопироватьОтборКомпоновкиДанных(ОтборПриемник, ОтборИсточник, Префикс = "", Постфикс = "") Экспорт
	
	Для Каждого ЭлементОтбораИсточника Из ОтборИсточник.Элементы Цикл
		ТипЭлементаОтбора = ТипЗнч(ЭлементОтбораИсточника);
		ЭлементОтбораПриемника = ОтборПриемник.Элементы.Добавить(ТипЭлементаОтбора);
		Если ТипЭлементаОтбора = Тип("ЭлементОтбораКомпоновкиДанных") Тогда
			ПутьКДанным = Строка(ЭлементОтбораИсточника.ЛевоеЗначение);
			Если НЕ ПустаяСтрока(Префикс) ИЛИ НЕ ПустаяСтрока(Постфикс) Тогда
				ЧастиПутиКДанным = СтрРазделить(ПутьКДанным, ".");
				ЧастиПутиКДанным[0] = Префикс + ЧастиПутиКДанным[0] + Постфикс;
				ПутьКДанным = СтрСоединить(ЧастиПутиКДанным, ".");
			КонецЕсли;
			ЗаполнитьЗначенияСвойств(ЭлементОтбораПриемника, ЭлементОтбораИсточника,, "ЛевоеЗначение");
			ЭлементОтбораПриемника.ЛевоеЗначение = Новый ПолеКомпоновкиДанных(ПутьКДанным);
		Иначе
			ЗаполнитьЗначенияСвойств(ЭлементОтбораПриемника, ЭлементОтбораИсточника);
			СкопироватьОтборКомпоновкиДанных(ЭлементОтбораПриемника, ЭлементОтбораИсточника, Префикс, Постфикс);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

// Возвращает имена балансных полей источников данных - регистров накопления оперативного учета,
// используемых для связи между записями и формирования корреспонденций.
// 
// Возвращаемое значение:
//  Массив из Строка
//
Функция БалансныеПоляИсточниковДанных() Экспорт
	
	БалансныеПоля = Новый Массив();
	БалансныеПоля.Добавить("Период");
	БалансныеПоля.Добавить("Регистратор");
	БалансныеПоля.Добавить("Организация");
	БалансныеПоля.Добавить("ИдентификаторФинЗаписи");
	БалансныеПоля.Добавить("НастройкаХозяйственнойОперации");
	БалансныеПоля.Добавить("Сторно");
	
	Возврат БалансныеПоля;
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Добавляет в описание измерений регистра накопления реквизиты измерений - ключей аналитики
//
// Параметры:
//  ОписаниеИзмерений - Структура Из КлючИЗначение - Источники заполнения субконто:
//   * Ключ - Строка - Имя реквизита измерения - ключа аналитики
//   * Значение - см. ОписаниеПоляИсточникаДанных
//	МетаданныеРегистра - ОбъектМетаданныхРегистрНакопления - Метаданные регистра накопления
//
Процедура ДобавитьСоставКлючейАналитикиВОписаниеИзмерений(ОписаниеИзмерений, МетаданныеРегистра) Экспорт
	
	ТипыКлючейАналитики = Новый Массив();
	ТипыКлючейАналитики.Добавить(Тип("СправочникСсылка.КлючиАналитикиУчетаПартий"));
	ТипыКлючейАналитики.Добавить(Тип("СправочникСсылка.КлючиАналитикиУчетаНоменклатуры"));
	ТипыКлючейАналитики.Добавить(Тип("СправочникСсылка.КлючиАналитикиУчетаПоПартнерам"));
	
	Для Каждого ТипКлючаАналитики Из ТипыКлючейАналитики Цикл
		Для каждого Измерение Из МетаданныеРегистра.Измерения Цикл
			Если СтрНачинаетсяС(Измерение.Имя, "Удалить") Тогда
			ИначеЕсли Измерение.Тип.СодержитТип(ТипКлючаАналитики) Тогда
				МетаданныеКлючаАналитики = Метаданные.НайтиПоТипу(ТипКлючаАналитики);
				Для каждого Реквизит Из МетаданныеКлючаАналитики.Реквизиты Цикл
					Если НЕ СтрНачинаетсяС(Реквизит.Имя, "Удалить") Тогда
						ОписаниеИсточникаАналитики = ОписаниеПоляИсточникаДанных();
						ОписаниеИсточникаАналитики.ПутьКДанным = СтрШаблон("%1.%2", Измерение.Имя, Реквизит.Имя);
						ОписаниеИсточникаАналитики.Заголовок = СтрШаблон("%1 (%2)", Реквизит.Синоним, Измерение.Синоним);
						ОписаниеИсточникаАналитики.Тип = Реквизит.Тип;
						ОписаниеИзмерений.Вставить(СтрШаблон("%1_%2", Измерение.Имя, Реквизит.Имя), ОписаниеИсточникаАналитики);
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

// Возвращает структуру параметров для передачи в процедуру выполнения оффлайновых расчетов.
// 
// Возвращаемое значение:
//  Структура - Описание:
//   * ДатаОкончания - Дата - Конечная дата выполнения расчетов
//   * ДокументыКРасчету - Массив из ДокументСсылка - Документы, движения которых необходимо доформировать оффлайн
//   * КоличествоПотоков - Число - Количество сеансов обработки документов, выполняющихся одновременно
//   * ПересчитатьРегистрыОборотов - Булево - Признак необходимости перерасчета движений оборотных регистров
//
Функция ПараметрыВыполненияОффлайновыхРасчетов() Экспорт
	
	Параметры = Новый Структура();
	Параметры.Вставить("ДатаОкончания", '00010101');
	Параметры.Вставить("ДокументыКРасчету", Новый Массив());
	Параметры.Вставить("КоличествоПотоков", 1);
	Параметры.Вставить("ПересчитатьРегистрыОборотов", Ложь);
	Параметры.Вставить("ВидЗадания", Справочники.ВидыНеоперативныхЗаданий.ПустаяСсылка());
	
	Возврат Параметры;
	
КонецФункции

// Выполняет запуск процедур доформирования движений документов по регистрам
// 
// Параметры:
//  ПараметрыВыполнения - См. ПараметрыВыполненияОффлайновыхРасчетов
//
Процедура ВыполнитьОффлайновыеРасчеты(ПараметрыВыполнения) Экспорт
	
	ПроцедурыРасчетов = Новый ТаблицаЗначений();
	ПроцедурыРасчетов.Колонки.Добавить("ИмяМетода", ОбщегоНазначения.ОписаниеТипаСтрока(250));
	ПроцедурыРасчетов.Колонки.Добавить("МногопоточноеВыполнение", Новый ОписаниеТипов("Булево"));
	ПроцедурыРасчетов.Колонки.Добавить("ДополнительныеПараметры", Новый ОписаниеТипов("Структура"));
	
	Если ПолучитьФункциональнуюОпцию("НоваяАрхитектураВзаиморасчетов") И ПланыОбмена.ГлавныйУзел() = Неопределено
		И ЗначениеЗаполнено(ПараметрыВыполнения.ВидЗадания) Тогда
		НеоперативнаяОчередьЗаданий.ВыполнитьЗаданияНеоперативнойОчередиПоСпискуДокументов(
			ПараметрыВыполнения.ДокументыКРасчету, ПараметрыВыполнения.ВидЗадания);
	Иначе
		ПроцедураРасчета = ПроцедурыРасчетов.Добавить();
		ПроцедураРасчета.ИмяМетода = "РассчитатьСуммыДокументовВВалютахУчета";
		ПроцедураРасчета.МногопоточноеВыполнение = Истина;
		ПроцедураРасчета.ДополнительныеПараметры.Вставить("ПересчитатьРегистрыОборотов", ПараметрыВыполнения.ПересчитатьРегистрыОборотов);
	КонецЕсли;
	
	//++ НЕ УТ
	
	ПроцедураРасчета = ПроцедурыРасчетов.Добавить();
	ПроцедураРасчета.ИмяМетода = "СформироватьДвиженияПоВНА";
	ПроцедураРасчета.МногопоточноеВыполнение = Ложь;
	
	//-- НЕ УТ
	
	ПроцедураРасчета = ПроцедурыРасчетов.Добавить();
	ПроцедураРасчета.ИмяМетода = "ОтразитьДокументыВУчетеНДС";
	ПроцедураРасчета.МногопоточноеВыполнение = Истина;
	
	Для Каждого ПроцедураРасчета Из ПроцедурыРасчетов Цикл
		ВыполнитьОффлайновыйРасчет(ПараметрыВыполнения, ПроцедураРасчета);
	КонецЦикла;
	
КонецПроцедуры

// Возвращает строку условия на языке запросов на основании описания отбора, переданного в виде отбора
// компоновки данных, структуры, соответствия, массива или фиксированного массива.
// Элементы отбора, переданные в структуре или соответствии, складываются в условии по "И".
// Элементы отбора, переданные в массиве или фиксированном массиве, складываются в условии по "ИЛИ".
// 
// Параметры:
//  ОтборДанных - ОтборКомпоновкиДанных, Массив из Структура, Структура, Соответствие - Описание:
//  	* Ключ - Строка - Путь к данным, на которые накладывается отбор
//  	* Значение - Произвольный - Значение накладываемого отбора
//  ПараметрыЗапроса - Структура - параметры запроса, в которые будут добавлены значения отбора, которые невозможно описать языком запросов
//  ПсевдонимИсточника - Строка - псевдоним источника данных в запросе
//
// Возвращаемое значение:
//  Строка - текст условия отбора на языке запросов, заключенный в скобки
//
Функция СтрокаУсловияВыборкиДанных(ОтборДанных, ПараметрыЗапроса = Неопределено, ПсевдонимИсточника = Неопределено) Экспорт
	
	ТипОтбораДанных = ТипЗнч(ОтборДанных);
	ЗаключитьВСкобки = Ложь;
	
	ЭтоЛогическоеУмножение = ТипОтбораДанных = Тип("Структура")
		ИЛИ ТипОтбораДанных = Тип("Соответствие")
		ИЛИ ТипОтбораДанных = Тип("ОтборКомпоновкиДанных")
		ИЛИ (ТипОтбораДанных = Тип("ГруппаЭлементовОтбораКомпоновкиДанных")
		И ОтборДанных.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ);
	
	ЭтоЛогическоеСложение = ТипОтбораДанных = Тип("Массив")
		ИЛИ ТипОтбораДанных = Тип("ФиксированныйМассив")
		ИЛИ (ТипОтбораДанных = Тип("ГруппаЭлементовОтбораКомпоновкиДанных")
		И ОтборДанных.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли);
	
	ЭтоЭлементОтбора = ТипОтбораДанных = Тип("КлючИЗначение")
		ИЛИ ТипОтбораДанных = Тип("ЭлементОтбораКомпоновкиДанных");
	
	Если ЭтоЛогическоеУмножение Тогда
		Если ТипОтбораДанных = Тип("Структура")
		 ИЛИ ТипОтбораДанных = Тип("Соответствие") Тогда
			ЭлементыОтбора = ОтборДанных;
		Иначе
			ЭлементыОтбора = ОтборДанных.Элементы;
		КонецЕсли;
		
		СтрокиИ = Новый Массив();
		Для Каждого ЭлементОтбора Из ЭлементыОтбора Цикл
			СтрокиИ.Добавить(СтрокаУсловияВыборкиДанных(ЭлементОтбора, ПараметрыЗапроса, ПсевдонимИсточника));
		КонецЦикла;
		
		СтрокаУсловия = СтрСоединить(СтрокиИ, " И "); //@Query-part
		ЗаключитьВСкобки = Истина;
	ИначеЕсли ЭтоЛогическоеСложение Тогда
		Если ТипОтбораДанных = Тип("Массив")
		 ИЛИ ТипОтбораДанных = Тип("ФиксированныйМассив") Тогда
			ЭлементыОтбора = ОтборДанных;
		Иначе
			ЭлементыОтбора = ОтборДанных.Элементы;
		КонецЕсли;
		
		СтрокиИли = Новый Массив();
		Для Каждого ЭлементОтбора Из ЭлементыОтбора Цикл
			СтрокиИли.Добавить(СтрокаУсловияВыборкиДанных(ЭлементОтбора, ПараметрыЗапроса, ПсевдонимИсточника));
		КонецЦикла;
		
		СтрокаУсловия = СтрСоединить(СтрокиИли, " ИЛИ "); //@Query-part
		ЗаключитьВСкобки = Истина;
	ИначеЕсли ЭтоЭлементОтбора Тогда
		Если ТипОтбораДанных = Тип("КлючИЗначение") Тогда
			ПутьКДанным = ОтборДанных.Ключ;
			ЗначениеОтбора = ОтборДанных.Значение;
			ЭтоИнверсия = Ложь;
		Иначе
			ПутьКДанным = Строка(ОтборДанных.ЛевоеЗначение);
			ЗначениеОтбора = ОтборДанных.ПравоеЗначение;
			ЭтоИнверсия = ОтборДанных.ВидСравнения = ВидСравненияКомпоновкиДанных.НеРавно
				ИЛИ ОтборДанных.ВидСравнения = ВидСравненияКомпоновкиДанных.НеВСписке;
		КонецЕсли;
		
		СтрокаВыборкиЗначения = СтрокаВыборкиЗначения(ЗначениеОтбора);
		ЭтоПараметр = СтрНачинаетсяС(СтрокаВыборкиЗначения, "Параметр");
		ЭтоКоллекция = ТипЗнч(ЗначениеОтбора) = Тип("Массив")
			ИЛИ ТипЗнч(ЗначениеОтбора) = Тип("СписокЗначений");
		
		Если ЭтоПараметр Тогда
			Если ПараметрыЗапроса <> Неопределено Тогда
				ПараметрыЗапроса.Вставить(СтрокаВыборкиЗначения, ЗначениеОтбора);
			Иначе
				ТекстыИсключения = Новый Массив();
				ТекстыИсключения.Добавить(НСтр("ru = 'Значение не может быть преобразовано в выражение запроса.';
												|en = 'The value cannot be converted to a query expression.'"));
				ТекстыИсключения.Добавить(НСтр("ru = 'Требуется передача параметров запроса.';
												|en = 'Pass the query parameters.'"));
				ТекстыИсключения.Добавить(СтрШаблон("(%1)", ЗначениеОтбора));
				ВызватьИсключение СтрСоединить(ТекстыИсключения, " ");
			КонецЕсли;
		КонецЕсли;
		
		ЧастиСтроки = Новый Массив();
		ЧастиСтроки.Добавить(?(ЭтоИнверсия, "НЕ ", "")); //@Query-part
		ЧастиСтроки.Добавить(ПутьКДаннымСПсевдонимомИсточника(ПутьКДанным, ПсевдонимИсточника));
		ЧастиСтроки.Добавить(?(ЭтоКоллекция, " В (", " = ")); //@Query-part
		ЧастиСтроки.Добавить(?(ЭтоПараметр, "&", "")); //@Query-part
		ЧастиСтроки.Добавить(СтрокаВыборкиЗначения);
		ЧастиСтроки.Добавить(?(ЭтоКоллекция, ")", "")); //@Query-part
		
		Если ЭтоИнверсия И СтрЧислоВхождений(ПутьКДанным, ".") > 1 Тогда
			ЧастиСтроки.Добавить(" ИЛИ "); //@Query-part
			ЧастиСтроки.Добавить(ПутьКДаннымСПсевдонимомИсточника(ПутьКДанным, ПсевдонимИсточника));
			ЧастиСтроки.Добавить(" ЕСТЬ NULL"); //@Query-part
			ЗаключитьВСкобки = Истина;
		КонецЕсли;
		
		СтрокаУсловия = СтрСоединить(ЧастиСтроки);
	КонецЕсли;
	
	Если ЗаключитьВСкобки Тогда
		СтрокаУсловия = "(" + СтрокаУсловия + ")";
	КонецЕсли;
	
	Возврат СтрокаУсловия;
	
КонецФункции

// Возвращает псевдоним источника данных в запросе по умолчанию.
// Указание псевдонимов источников обязательно при выборке из временных таблиц.
// 
// Возвращаемое значение:
// 	Строка - псевдоним источника данных в запросе
//
Функция ПсевдонимИсточникаДанныхПоУмолчанию() Экспорт
	
	Возврат "ТаблицаРегистра";
	
КонецФункции

// Возвращает имена доступных полей источника данных.
// 
// Параметры:
//  ОписаниеПолейИсточника - Структура Из КлючИЗначение - Описание:
//   * Ключ - Строка - Имя измерения регистра, имя реквизита измерения - ключа аналитики или имя реквизита регистра
//   * Значение - см. ОписаниеПоляИсточникаДанных
//  
// Возвращаемое значение:
//  Массив Из Строка - имена доступных полей источника данных
//
Функция ДоступныеПоляИсточникаДанных(ОписаниеПолейИсточника) Экспорт
	
	ДоступныеПоля = Новый Массив();
	
	Для Каждого КлючИЗначение Из ОписаниеПолейИсточника Цикл
		ЧастиПутиКДанным = СтрРазделить(КлючИЗначение.Значение.ПутьКДанным, ".");
		ИмяПоляИсточника = ЧастиПутиКДанным[0];
		
		Если ДоступныеПоля.Найти(ИмяПоляИсточника) = Неопределено Тогда
			ДоступныеПоля.Добавить(ИмяПоляИсточника);
		КонецЕсли;
	КонецЦикла;
	
	Возврат ДоступныеПоля;
	
КонецФункции

// Возвращает массив имен документов, не используемых при получении данных финансовых регистров
// 
// Возвращаемое значение:
// 	Массив из Строка - имена неиспользуемых документов
//
Функция ИменаНеиспользуемыхДокументов() Экспорт
	
	ИменаДокументов = Новый Массив();
	МетаданныеДокументы = Метаданные.Документы;
	
	ИменаДокументов.Добавить(МетаданныеДокументы.ВводОстатков.Имя);
	ИменаДокументов.Добавить(МетаданныеДокументы.КорректировкаРегистров.Имя);
	
	ФинансовыйУчетПоДаннымБалансовыхРегистровЛокализация.ДополнитьИменаНеиспользуемыхДокументов(ИменаДокументов);
	
	Возврат ИменаДокументов;
	
КонецФункции

// Возвращает массив имен регистров накопления, в корреспонденции которых всегда учитываются показатели
//
// Возвращаемое значение:
//  Массив Из Строка - имена регистров накопления
//
Функция РегистрыСОтборомПоПоказателям() Экспорт
	
	ИменаРегистров = Новый Массив();
	ИменаРегистров.Добавить(Метаданные.РегистрыНакопления.ВыручкаИСебестоимостьПродаж.Имя);
	ИменаРегистров.Добавить(Метаданные.РегистрыНакопления.СебестоимостьТоваров.Имя);
	ИменаРегистров.Добавить(Метаданные.РегистрыНакопления.ДвиженияПоАкцизам.Имя);
	
	Возврат ИменаРегистров;
	
КонецФункции

// Возвращает описания путей к данным для общих полей балансовых регистров
//
// Параметры:
//  ПараметрыОтраженияДвижений - см. ПараметрыОтраженияДвиженийВФинансовомУчете
//  ПсевдонимИсточника - Неопределено, Строка - имя таблицы балансового регистра
//
// Возвращаемое значение:
//  Структура:
//   *Ключ - Строка - имя поля
//   *Значение - Строка - путь к данным поля
Функция ОписанияПутейКДаннымПолейРегистра(ПараметрыОтраженияДвижений, ПсевдонимИсточника = Неопределено) Экспорт
		
	ПутиКДаннымПолей = Новый Структура;
	
	Если ПустаяСтрока(ПараметрыОтраженияДвижений.ПутьКДаннымПодразделение) Тогда
		ПутиКДаннымПолей.Вставить("Подразделение", СтрокаВыборкиЗначения(Справочники.СтруктураПредприятия.ПустаяСсылка()));
	Иначе
		ПутиКДаннымПолей.Вставить("Подразделение", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымПодразделение, ПсевдонимИсточника));
	КонецЕсли;

	Если ПустаяСтрока(ПараметрыОтраженияДвижений.ПутьКДаннымНаправлениеДеятельности) Тогда
		ПутиКДаннымПолей.Вставить("НаправлениеДеятельности", СтрокаВыборкиЗначения(Справочники.НаправленияДеятельности.ПустаяСсылка()));
	Иначе
		ПутиКДаннымПолей.Вставить("НаправлениеДеятельности", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымНаправлениеДеятельности, ПсевдонимИсточника));
	КонецЕсли;

	ПутиКДаннымПолей.Вставить("НастройкаХозОперации", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымНастройкаХозОперации, ПсевдонимИсточника));

	Если ПустаяСтрока(ПараметрыОтраженияДвижений.ПутьКДаннымДопНастройкаХозОперации) Тогда
		ПутиКДаннымПолей.Вставить("ДопНастройкаХозОперации", ПутиКДаннымПолей.НастройкаХозОперации);
	Иначе
		ПутиКДаннымПолей.Вставить("ДопНастройкаХозОперации", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымДопНастройкаХозОперации, ПсевдонимИсточника));
	КонецЕсли;

	Если ПустаяСтрока(ПараметрыОтраженияДвижений.ПутьКДаннымВалюта) Тогда
		ПутиКДаннымПолей.Вставить("Валюта", СтрокаВыборкиЗначения(Справочники.Валюты.ПустаяСсылка()));
	Иначе
		ПутиКДаннымПолей.Вставить("Валюта", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымВалюта, ПсевдонимИсточника));
	КонецЕсли;

	Если ПустаяСтрока(ПараметрыОтраженияДвижений.ПутьКДаннымСторно) Тогда
		ПутиКДаннымПолей.Вставить("РеквизитСторно", СтрокаВыборкиЗначения(Ложь));
	Иначе
		ПутиКДаннымПолей.Вставить("РеквизитСторно", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымСторно, ПсевдонимИсточника));
	КонецЕсли;

	ПутиКДаннымПолей.Вставить("Регистратор", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымРегистратор, ПсевдонимИсточника));
	ПутиКДаннымПолей.Вставить("Организация", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымОрганизация, ПсевдонимИсточника));
	
	Если ПустаяСтрока(ПараметрыОтраженияДвижений.ПутьКДаннымВидДеятельностиНДС) Тогда
		ПутиКДаннымПолей.Вставить("ВидДеятельностиНДС", СтрокаВыборкиЗначения(Перечисления.ТипыНалогообложенияНДС.ПустаяСсылка()));
	Иначе
		ПутиКДаннымПолей.Вставить("ВидДеятельностиНДС", ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений.ПутьКДаннымВидДеятельностиНДС, ПсевдонимИсточника));
	КонецЕсли;
	
	Возврат ПутиКДаннымПолей;

КонецФункции

#Область ТекстыЗапросовФормированияКорреспонденций

// Возвращает структуру для установки значений параметров запроса данных финансовых регистров.
// 
// Возвращаемое значение:
//  Структура:
//   * ДатаНачала - Дата - дата начала построения отчета
//   * ДатаОкончания - Дата - дата окончания построения отчета
//   * Регистратор - ДокументСсылка, Массив из ДокументСсылка - отбор по документу или документам
//   * Организация - СправочникСсылка.Организации, Массив из СправочникСсылка.Организации - отбор по организации или организациям
//   * ОбъектУчетаДт - ПланВидовХарактеристикСсылка.СтатьиАктивовПассивов - отбор по объекту учета дебета
//   * ОбъектУчетаКт - ПланВидовХарактеристикСсылка.СтатьиАктивовПассивов - отбор по объекту учета кредита
//   * ТипРегистратора - Массив из Тип - отбор по коллекциям движений типов документов
//   * ДополнительныйОтбор - ОтборКомпоновкиДанных - отбор фиксированных настроек компоновщика настроек компоновки данных
//
Функция ПараметрыЗапросаДанныхФинансовыхРегистров() Экспорт
	
	ПараметрыЗапроса = Новый Структура();
	
	ПараметрыЗапроса.Вставить("ДатаНачала", '00010101');
	ПараметрыЗапроса.Вставить("ДатаОкончания", '00010101');
	ПараметрыЗапроса.Вставить("Регистратор", Неопределено);
	ПараметрыЗапроса.Вставить("Организация", Неопределено);
	ПараметрыЗапроса.Вставить("ОбъектУчетаДт", Неопределено);
	ПараметрыЗапроса.Вставить("ОбъектУчетаКт", Неопределено);
	ПараметрыЗапроса.Вставить("ТипРегистратора", Неопределено);
	ПараметрыЗапроса.Вставить("ДополнительныйОтбор", Неопределено);
	
	Возврат ПараметрыЗапроса;
	
КонецФункции

// Возвращает запрос, использующийся в формировании корреспонденций балансовых регистров
//
// Параметры:
//  ПараметрыЗапроса - Структура - исходные параметры запроса
//  МенеджерВременныхТаблиц - МенеджерВременныхТаблиц
//
// Возвращаемое значение:
//  Запрос
//
Функция ЗапросДанныхФинансовыхРегистров(ПараметрыЗапроса, МенеджерВременныхТаблиц) Экспорт
	
	Запрос = Новый Запрос();
	Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
	
	Для Каждого КлючИЗначение Из ПараметрыЗапроса Цикл
		Запрос.УстановитьПараметр(КлючИЗначение.Ключ, КлючИЗначение.Значение);
	КонецЦикла;
	
	ИсходныеВременныеТаблицы = Новый Массив();
	Для Каждого ВременнаяТаблица Из МенеджерВременныхТаблиц.Таблицы Цикл
		ИсходныеВременныеТаблицы.Добавить(ВременнаяТаблица.ПолноеИмя);
	КонецЦикла;
	Запрос.УстановитьПараметр("ИсходныеВременныеТаблицы", ИсходныеВременныеТаблицы);
	
	Запрос.УстановитьПараметр("ВалютаУправленческогоУчета", ЗначениеНастроекПовтИсп.ВалютаУправленческогоУчета());
	Запрос.УстановитьПараметр("ИдентификаторНеиспользуемойФинЗаписи", ПроведениеДокументов.ИдентификаторНеиспользуемойФинЗаписи());
	Запрос.УстановитьПараметр("ФормироватьВидыЗапасовПоГруппамФинансовогоУчета", ПолучитьФункциональнуюОпцию("ФормироватьВидыЗапасовПоГруппамФинансовогоУчета"));
	Запрос.УстановитьПараметр("ХозОперацииБезУчетаВВалюте", ХозяйственныеОперацииБезУчетаВВалюте());
	Запрос.УстановитьПараметр("ТипыНалогообложенияБезУчетаНДС", ТипыНалогообложенияБезУчетаНДС());
	Запрос.УстановитьПараметр("ТипыДокументовВводаОстатков", ТипыДокументовВводаОстатков());
	Запрос.УстановитьПараметр("ТипыДокументовРозничныхПродаж", ТипыДокументовРозничныхПродаж());
	Запрос.УстановитьПараметр("ТипыДокументовРозничныхВозвратов", ТипыДокументовРозничныхВозвратов());
	Запрос.УстановитьПараметр("ТипыДокументовВзаимозачетаЗадолженности", ТипыДокументовВзаимозачетаЗадолженности());
	
	ТипыРегистраторовСлужебныхОбъектовУчета = ОбщегоНазначения.СкопироватьРекурсивно(Запрос.Параметры.ТипыДокументовВводаОстатков);
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ТипыРегистраторовСлужебныхОбъектовУчета, Запрос.Параметры.ТипыДокументовРозничныхПродаж);
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ТипыРегистраторовСлужебныхОбъектовУчета, Запрос.Параметры.ТипыДокументовРозничныхВозвратов);
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ТипыРегистраторовСлужебныхОбъектовУчета, Запрос.Параметры.ТипыДокументовВзаимозачетаЗадолженности);
	Запрос.УстановитьПараметр("ТипыРегистраторовСлужебныхОбъектовУчета", ТипыРегистраторовСлужебныхОбъектовУчета);
	Запрос.УстановитьПараметр("ИмяРегистраСлужебныхОбъектовУчета", ИмяРегистраСлужебныхОбъектовУчета());
	
	КорИсточникиДанных = Новый ТаблицаЗначений();
	КорИсточникиДанных.Колонки.Добавить("ОбъектУчета", Новый ОписаниеТипов("ПланВидовХарактеристикСсылка.СтатьиАктивовПассивов"));
	КорИсточникиДанных.Колонки.Добавить("КорИсточникДанных", ОбщегоНазначения.ОписаниеТипаСтрока(150)); // имя регистра, корреспондирующего с объектом учета
	КорИсточникиДанных.Колонки.Добавить("Идентификатор", ОбщегоНазначения.ОписаниеТипаСтрока(50)); // идентификатор всех регистров, корреспондирующих с объектом учета
	КорИсточникиДанных.Колонки.Добавить("КорИдентификатор", ОбщегоНазначения.ОписаниеТипаСтрока(50)); // идентификатор для группировки всех корреспондирующих регистров
	Запрос.УстановитьПараметр("КорИсточникиДанных", КорИсточникиДанных);
	
	Запрос.УстановитьПараметр("ОбъектыУчетаНДС", Новый Массив());
	Запрос.УстановитьПараметр("ОбъектыУчетаСНДС", Новый Массив());
	
	Запрос.УстановитьПараметр("РегистрыБезСуммыУпр", Новый Массив());
	Запрос.УстановитьПараметр("РегистрыБезСуммыРегл", Новый Массив());
	Запрос.УстановитьПараметр("РегистрыВыборкиАналитики", Новый Структура());
	
	Запрос.УстановитьПараметр("Дт", "Дт");
	Запрос.УстановитьПараметр("Кт", "Кт");
	
	Запрос.УстановитьПараметр("СложныеПроводкиСУказаниемКорИсточников", Ложь);
	
	Запрос.УстановитьПараметр("ВосстановитьРасчетыПоСторно", Ложь);
	Запрос.УстановитьПараметр("РегистрыВосстановленияРасчетовПоСторно", РегистрыВосстановленияРасчетовПоСторно());
	
	ИмяРегистраПрочиеАктивыПассивы = Метаданные.РегистрыНакопления.ДвиженияПоПрочимАктивамПассивам.Имя;
	Запрос.УстановитьПараметр("ИмяРегистраПрочиеАктивыПассивы", ИмяРегистраПрочиеАктивыПассивы);
	
	Запрос.УстановитьПараметр("РегистрыЗаменыСторноНаРеверс", Новый Массив());
	Запрос.УстановитьПараметр("РегистрыЗаменыРеверсаНаСторно", Новый Массив());
	
	КвалификаторыДенежнойСуммы = Метаданные.ОпределяемыеТипы.ДенежнаяСуммаЛюбогоЗнака.Тип.КвалификаторыЧисла;
	Запрос.УстановитьПараметр("ДлинаСуммы", КвалификаторыДенежнойСуммы.Разрядность);
	Запрос.УстановитьПараметр("ТочностьСуммы", КвалификаторыДенежнойСуммы.РазрядностьДробнойЧасти);
	
	Запрос.УстановитьПараметр("ДлинаКоличества", 15);
	Запрос.УстановитьПараметр("ТочностьКоличества", 3);
	
	Запрос.УстановитьПараметр("ТаблицаКорреспонденцийДвиженийРегистров", "втДвиженияРегистровДебетКредит");
	Запрос.УстановитьПараметр("ТаблицаДетальныхДвиженийСИтогамиДебет", "втДетальныеДвиженияСИтогамиДебет");
	Запрос.УстановитьПараметр("ТаблицаДетальныхДвиженийСИтогамиКредит", "втДетальныеДвиженияСИтогамиКредит");
	
	Возврат Запрос;
	
КонецФункции

Процедура УничтожитьВременныеТаблицы(Запрос, ТекстыЗапросов) Экспорт
	
	ИсходныеВременныеТаблицы = Запрос.Параметры.ИсходныеВременныеТаблицы; // Массив -
	
	Для Каждого ВременнаяТаблица Из Запрос.МенеджерВременныхТаблиц.Таблицы Цикл
		ИмяВременнойТаблицы = ВременнаяТаблица.ПолноеИмя;
		Если ИсходныеВременныеТаблицы.Найти(ИмяВременнойТаблицы) = Неопределено Тогда
			ДобавитьЗапросУдаленияВременнойТаблицы(ТекстыЗапросов, ИмяВременнойТаблицы);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ТекстыЗапросовОбщегоНазначения

Процедура ВыполнитьЗаменуИменТаблицИПолейДвижений(ТекстЗапроса, ДтКт) Экспорт
	
	Если ДтКт = "Дт" Тогда
		ЭтоДтКт = "ЭтоДебет";
		ЭтоКорДтКт = "ЭтоКредит";
		ОбъектУчетаДтКт = "ОбъектУчетаДт";
		ОбъектУчетаКорДтКт = "ОбъектУчетаКт";
		втДвиженияДтКт = "втДвиженияДебет";
		втКорДвиженияДтКт = "втДвиженияКредит";
		втДвиженияСИтогамиДтКт = "втДвиженияСИтогамиДебет";
		втДвиженияРегистровДтКт = "втДвиженияРегистровДебет";
		втСвернутыеДвиженияДтКт = "втСвернутыеДвиженияДебет";
		втДвиженияРегистровРеверсДтКт = "втДвиженияРегистровРеверсДебет";
		втКорДвиженияРегистровРеверсДтКт = "втДвиженияРегистровРеверсКредит";
		втКорИдентификаторыОперацийДтКт = "втИдентификаторыОперацийКредит";
	Иначе
		ЭтоДтКт = "ЭтоКредит";
		ЭтоКорДтКт = "ЭтоДебет";
		ОбъектУчетаДтКт = "ОбъектУчетаКт";
		ОбъектУчетаКорДтКт = "ОбъектУчетаДт";
		втДвиженияДтКт = "втДвиженияКредит";
		втКорДвиженияДтКт = "втДвиженияДебет";
		втДвиженияСИтогамиДтКт = "втДвиженияСИтогамиКредит";
		втДвиженияРегистровДтКт = "втДвиженияРегистровКредит";
		втСвернутыеДвиженияДтКт = "втСвернутыеДвиженияКредит";
		втДвиженияРегистровРеверсДтКт = "втДвиженияРегистровРеверсКредит";
		втКорДвиженияРегистровРеверсДтКт = "втДвиженияРегистровРеверсДебет";
		втКорИдентификаторыОперацийДтКт = "втИдентификаторыОперацийДебет";
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ЭтоДтКт", ЭтоДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ЭтоКорДтКт", ЭтоКорДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ОбъектУчетаДтКт", ОбъектУчетаДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ОбъектУчетаКорДтКт", ОбъектУчетаКорДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "втДвиженияДтКт", втДвиженияДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "втКорДвиженияДтКт", втКорДвиженияДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "втДвиженияСИтогамиДтКт", втДвиженияСИтогамиДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "втДвиженияРегистровДтКт", втДвиженияРегистровДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "втСвернутыеДвиженияДтКт", втСвернутыеДвиженияДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "втДвиженияРегистровРеверсДтКт", втДвиженияРегистровРеверсДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "втКорДвиженияРегистровРеверсДтКт", втКорДвиженияРегистровРеверсДтКт);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "втКорИдентификаторыОперацийДтКт", втКорИдентификаторыОперацийДтКт);
	
КонецПроцедуры

Процедура ДобавитьЗапросУдаленияВременнойТаблицы(ТекстыЗапросов, ИмяТаблицы) Экспорт
	
	Если ТипЗнч(ИмяТаблицы) = Тип("Массив") Тогда
		Для Каждого УдаляемаяТаблица Из ИмяТаблицы Цикл
			ДобавитьЗапросУдаленияВременнойТаблицы(ТекстыЗапросов, УдаляемаяТаблица);
		КонецЦикла;
	Иначе
		ТекстыЗапросов.Добавить("
		|УНИЧТОЖИТЬ " + ИмяТаблицы);
	КонецЕсли;
	
КонецПроцедуры

Функция ЕстьЗаписиВоВременнойТаблице(Запрос, ТекстыЗапросов, ИмяТаблицы) Экспорт
	
	ТекстЗапроса = "
	|ВЫБРАТЬ ПЕРВЫЕ 1
	|	ИСТИНА
	|ИЗ
	|	&ИмяТаблицы КАК ВременнаяТаблица
	|";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ИмяТаблицы", ИмяТаблицы);
	ТекстыЗапросов.Добавить(ТекстЗапроса);
	
	Запрос.Текст = СтрСоединить(ТекстыЗапросов, ОбщегоНазначения.РазделительПакетаЗапросов());
	Результаты = Запрос.ВыполнитьПакет();
	ТекстыЗапросов.Очистить();
	
	Возврат НЕ Результаты[Результаты.ВГраница()].Пустой();
	
КонецФункции

#КонецОбласти

#Область ТекстыЗапросовВосстановленияРасчетовПоСторно

Функция РегистрыВосстановленияРасчетовПоСторно() Экспорт
	
	ИменаРегистров = Новый Массив();
	ИменаРегистров.Добавить(Метаданные.РегистрыНакопления.РасчетыСКлиентамиПоСрокам.Имя);
	ИменаРегистров.Добавить(Метаданные.РегистрыНакопления.РасчетыСПоставщикамиПоСрокам.Имя);
	
	Возврат ИменаРегистров;
	
КонецФункции

#КонецОбласти

#Область ТекстыЗапросовРаспределенияСуммИКоличеств

Процедура ДобавитьТаблицуОшибокОкругленияСуммы(ТекстыЗапросов, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс) Экспорт
	
	ТекстЗапроса = "
	|ВЫБРАТЬ
	|	РаспределениеНебалансовых.НомерЗаписи КАК НомерЗаписи,
	|	РаспределениеНебалансовых.НомерСложнойЗаписи КАК НомерСложнойЗаписи,
	|	МАКСИМУМ(РаспределениеНебалансовых.РаспределяемыйРесурс) - СУММА(РаспределениеНебалансовых.РаспределенныйРесурс) КАК ОшибкаОкругления,
	|	МАКСИМУМ(ВЫБОР
	|		КОГДА РаспределениеНебалансовых.ЕстьРаспределениеСуммы
	|			ТОГДА РаспределениеНебалансовых.РаспределенныйРесурс
	|		ИНАЧЕ 0
	|	КОНЕЦ) КАК МаксимальныйРаспределенныйРесурс
	|ПОМЕСТИТЬ втОшибкиОкругления
	|ИЗ
	|	втРаспределениеНебалансовых КАК РаспределениеНебалансовых
	|ГДЕ
	|	РаспределениеНебалансовых.СложнаяПроводка
	|	И РаспределениеНебалансовых.Валюта <> ЗНАЧЕНИЕ(Справочник.Валюты.ПустаяСсылка)
	|СГРУППИРОВАТЬ ПО
	|	РаспределениеНебалансовых.НомерЗаписи,
	|	РаспределениеНебалансовых.НомерСложнойЗаписи
	|ИМЕЮЩИЕ
	|	МАКСИМУМ(РаспределениеНебалансовых.ЕстьРаспределениеСуммы) = ИСТИНА
	|	И МАКСИМУМ(РаспределениеНебалансовых.РаспределяемыйРесурс) <> СУММА(РаспределениеНебалансовых.РаспределенныйРесурс)
	|ИНДЕКСИРОВАТЬ ПО
	|	НомерЗаписи,
	|	НомерСложнойЗаписи
	|";
	
	ВыполнитьЗаменуИменТаблицИРесурсовРаспределения(ТекстЗапроса, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс);
	ВыполнитьЗаменуИменНебалансовыхПолейРаспределения(ТекстЗапроса, РаспределенныйРесурс);
	
	ТекстыЗапросов.Добавить(ТекстЗапроса);
	
	ТекстЗапроса = "
	|ВЫБРАТЬ
	|	ОшибкиОкругления.НомерЗаписи КАК НомерЗаписи,
	|	ОшибкиОкругления.ОшибкаОкругления КАК ОшибкаОкругления,
	|	МИНИМУМ(РаспределениеНебалансовых.НомерСтроки) КАК НомерСтроки
	|ПОМЕСТИТЬ ТаблицаДляПомещения
	|ИЗ
	|	втОшибкиОкругления КАК ОшибкиОкругления
	|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ втРаспределениеНебалансовых КАК РаспределениеНебалансовых
	|		ПО ОшибкиОкругления.НомерЗаписи = РаспределениеНебалансовых.НомерЗаписи
	|		 И ОшибкиОкругления.НомерСложнойЗаписи = РаспределениеНебалансовых.НомерСложнойЗаписи
	|		 И ОшибкиОкругления.МаксимальныйРаспределенныйРесурс = РаспределениеНебалансовых.РаспределенныйРесурс
	|СГРУППИРОВАТЬ ПО
	|	ОшибкиОкругления.НомерЗаписи,
	|	ОшибкиОкругления.ОшибкаОкругления
	|ИНДЕКСИРОВАТЬ ПО
	|	НомерЗаписи,
	|	НомерСтроки
	|";
	
	ВыполнитьЗаменуИменТаблицИРесурсовРаспределения(ТекстЗапроса, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс);
	ВыполнитьЗаменуИменНебалансовыхПолейРаспределения(ТекстЗапроса, РаспределенныйРесурс);
	
	ТекстыЗапросов.Добавить(ТекстЗапроса);
	
	ДобавитьЗапросУдаленияВременнойТаблицы(ТекстыЗапросов, "втОшибкиОкругления");
	
КонецПроцедуры

Процедура ДобавитьТаблицуОшибокОкругленияКоличества(ТекстыЗапросов, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс) Экспорт
	
	ТекстЗапроса = "
	|ВЫБРАТЬ
	|	РаспределениеНебалансовых.НомерЗаписи КАК НомерЗаписи,
	|	РаспределениеНебалансовых.НомерСложнойЗаписи КАК НомерСложнойЗаписи,
	|	МАКСИМУМ(РаспределениеНебалансовых.РаспределяемыйРесурс) - СУММА(РаспределениеНебалансовых.РаспределенныйРесурс) КАК ОшибкаОкругления,
	|	МАКСИМУМ(РаспределениеНебалансовых.РаспределенныйРесурс) КАК МаксимальныйРаспределенныйРесурс
	|ПОМЕСТИТЬ втОшибкиОкругления
	|ИЗ
	|	втРаспределениеНебалансовых КАК РаспределениеНебалансовых
	|ГДЕ
	|	РаспределениеНебалансовых.ЕстьРаспределениеКоличества
	|СГРУППИРОВАТЬ ПО
	|	РаспределениеНебалансовых.НомерЗаписи,
	|	РаспределениеНебалансовых.НомерСложнойЗаписи
	|ИМЕЮЩИЕ
	|	МАКСИМУМ(РаспределениеНебалансовых.РаспределяемыйРесурс) <> СУММА(РаспределениеНебалансовых.РаспределенныйРесурс)
	|ИНДЕКСИРОВАТЬ ПО
	|	НомерЗаписи,
	|	НомерСложнойЗаписи
	|";
	
	ВыполнитьЗаменуИменТаблицИРесурсовРаспределения(ТекстЗапроса, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс);
	ВыполнитьЗаменуИменНебалансовыхПолейРаспределения(ТекстЗапроса, РаспределенныйРесурс);
	
	ТекстыЗапросов.Добавить(ТекстЗапроса);
		
	ТекстЗапроса = "
	|ВЫБРАТЬ
	|	ОшибкиОкругления.НомерЗаписи КАК НомерЗаписи,
	|	ОшибкиОкругления.ОшибкаОкругления КАК ОшибкаОкругления,
	|	МИНИМУМ(РаспределениеНебалансовых.НомерСтроки) КАК НомерСтроки
	|ПОМЕСТИТЬ ТаблицаДляПомещения
	|ИЗ
	|	втОшибкиОкругления КАК ОшибкиОкругления
	|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ втРаспределениеНебалансовых КАК РаспределениеНебалансовых
	|		ПО ОшибкиОкругления.НомерЗаписи = РаспределениеНебалансовых.НомерЗаписи
	|		 И ОшибкиОкругления.НомерСложнойЗаписи = РаспределениеНебалансовых.НомерСложнойЗаписи
	|		 И ОшибкиОкругления.МаксимальныйРаспределенныйРесурс = РаспределениеНебалансовых.РаспределенныйРесурс
	|СГРУППИРОВАТЬ ПО
	|	ОшибкиОкругления.НомерЗаписи,
	|	ОшибкиОкругления.ОшибкаОкругления
	|ИНДЕКСИРОВАТЬ ПО
	|	НомерЗаписи,
	|	НомерСтроки
	|";
	
	ВыполнитьЗаменуИменТаблицИРесурсовРаспределения(ТекстЗапроса, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс);
	ВыполнитьЗаменуИменНебалансовыхПолейРаспределения(ТекстЗапроса, РаспределенныйРесурс);
	
	ТекстыЗапросов.Добавить(ТекстЗапроса);
	
	ДобавитьЗапросУдаленияВременнойТаблицы(ТекстыЗапросов, "втОшибкиОкругления");
	
КонецПроцедуры

Процедура ДобавитьТаблицуОшибокОкругленияРесурса(ТекстыЗапросов, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс) Экспорт
	
	ТекстЗапроса = "
	|ВЫБРАТЬ
	|	РаспределениеПоДатамПогашения.НомерЗаписи КАК НомерЗаписи,
	|	МАКСИМУМ(РаспределениеПоДатамПогашения.РаспределяемыйРесурс) - СУММА(РаспределениеПоДатамПогашения.РаспределенныйРесурс) КАК ОшибкаОкругления,
	|	МАКСИМУМ(РаспределениеПоДатамПогашения.РаспределенныйРесурс) КАК МаксимальныйРаспределенныйРесурс
	|ПОМЕСТИТЬ втОшибкиОкругления
	|ИЗ
	|	втРаспределениеПоДатамПогашения КАК РаспределениеПоДатамПогашения
	|ГДЕ
	|	РаспределениеПоДатамПогашения.ЕстьРаспределение
	|СГРУППИРОВАТЬ ПО
	|	РаспределениеПоДатамПогашения.НомерЗаписи
	|ИМЕЮЩИЕ
	|	МАКСИМУМ(РаспределениеПоДатамПогашения.РаспределяемыйРесурс) <> СУММА(РаспределениеПоДатамПогашения.РаспределенныйРесурс)
	|ИНДЕКСИРОВАТЬ ПО
	|	НомерЗаписи
	|";
	
	ВыполнитьЗаменуИменТаблицИРесурсовРаспределения(ТекстЗапроса, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс);
	
	ТекстыЗапросов.Добавить(ТекстЗапроса);
	
	ТекстЗапроса = "
	|ВЫБРАТЬ
	|	ОшибкиОкругления.НомерЗаписи КАК НомерЗаписи,
	|	ОшибкиОкругления.ОшибкаОкругления КАК ОшибкаОкругления,
	|	МИНИМУМ(РаспределениеПоДатамПогашения.НомерСтроки) КАК НомерСтроки
	|ПОМЕСТИТЬ ТаблицаДляПомещения
	|ИЗ
	|	втОшибкиОкругления КАК ОшибкиОкругления
	|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ втРаспределениеПоДатамПогашения КАК РаспределениеПоДатамПогашения
	|		ПО ОшибкиОкругления.НомерЗаписи = РаспределениеПоДатамПогашения.НомерЗаписи
	|		 И ОшибкиОкругления.МаксимальныйРаспределенныйРесурс = РаспределениеПоДатамПогашения.РаспределенныйРесурс
	|СГРУППИРОВАТЬ ПО
	|	ОшибкиОкругления.НомерЗаписи,
	|	ОшибкиОкругления.ОшибкаОкругления
	|ИНДЕКСИРОВАТЬ ПО
	|	НомерЗаписи,
	|	НомерСтроки
	|";
	
	ВыполнитьЗаменуИменТаблицИРесурсовРаспределения(ТекстЗапроса, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс);
	
	ТекстыЗапросов.Добавить(ТекстЗапроса);
	
	ДобавитьЗапросУдаленияВременнойТаблицы(ТекстыЗапросов, "втОшибкиОкругления");
	
КонецПроцедуры

Процедура ВыполнитьЗаменуИменТаблицИРесурсовРаспределения(ТекстЗапроса, ТаблицаДляПомещения, РаспределяемыйРесурс, РаспределенныйРесурс) Экспорт
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ТаблицаДляПомещения",  ТаблицаДляПомещения);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "РаспределяемыйРесурс", РаспределяемыйРесурс);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "РаспределенныйРесурс", РаспределенныйРесурс);
	
КонецПроцедуры

Процедура ВыполнитьЗаменуИменНебалансовыхПолейРаспределения(ТекстЗапроса, РаспределенныйРесурс) Экспорт
	
	Если РаспределенныйРесурс = "СуммаУпрДт"
	 ИЛИ РаспределенныйРесурс = "СуммаВалДт"
	 ИЛИ РаспределенныйРесурс = "СуммаРеглДт"
	 ИЛИ РаспределенныйРесурс = "КоличествоДт" Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "НомерЗаписи", "НомерЗаписиДт");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "СложнаяПроводка", "СложнаяПроводкаДт");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Валюта", "ВалютаДт");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ЕстьРаспределениеСуммы", "ЕстьРаспределениеСуммыДт");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ЕстьРаспределениеКоличества", "ЕстьРаспределениеКоличестваДт");
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "НомерЗаписи", "НомерЗаписиКт");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "СложнаяПроводка", "СложнаяПроводкаКт");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Валюта", "ВалютаКт");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ЕстьРаспределениеСуммы", "ЕстьРаспределениеСуммыКт");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ЕстьРаспределениеКоличества", "ЕстьРаспределениеКоличестваКт");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ФормированиеПараметровЗапроса

Процедура ЗаполнитьКорИсточникиДанных(ПараметрыЗапроса, СтрокаОбъектаУчета) Экспорт
	
	Если СтрокаОбъектаУчета.КорИсточникиДанных.Количество() > 0 Тогда
		КорИсточникиДанных = ПараметрыЗапроса.КорИсточникиДанных; // ТаблицаЗначений -
		ИдентификаторИсточников = ХешСуммаИсточниковДанных(СтрокаОбъектаУчета.КорИсточникиДанных, ХешФункция.SHA256);
		КорИдентификаторИсточников = ХешСуммаИсточниковДанных(СтрокаОбъектаУчета.КорИсточникиДанных, ХешФункция.SHA1);
		
		Для Каждого КорИсточникДанных Из СтрокаОбъектаУчета.КорИсточникиДанных Цикл
			НоваяСтрока = КорИсточникиДанных.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, СтрокаОбъектаУчета);
			НоваяСтрока.КорИсточникДанных = КорИсточникДанных;
			НоваяСтрока.Идентификатор = ИдентификаторИсточников;
			НоваяСтрока.КорИдентификатор = КорИдентификаторИсточников;
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьИсточникиСоСрокамиОбращения(ПараметрыЗапроса, СтрокаОбъектаУчета, ПараметрыОтраженияДвижений) Экспорт
	
	ИсточникиСоСрокамиОбращения = ПараметрыЗапроса.ИсточникиСоСрокамиОбращения; // ТаблицаЗначений -
	
	Если ПараметрыЗапроса.ВыделениеДолгосрочныхАктивовОбязательств
		И ПараметрыОтраженияДвижений.ВыделениеДолгосрочныхАктивовОбязательств
		И СтрокаОбъектаУчета.ВыделениеДолгосрочныхАктивовОбязательств Тогда
		НоваяСтрока = ИсточникиСоСрокамиОбращения.Добавить();
		НоваяСтрока.ИсточникДанных = СтрокаОбъектаУчета.ИсточникДанных;
		НоваяСтрока.ОбъектУчета = СтрокаОбъектаУчета.ОбъектУчета;
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьПризнакиУчетаКорреспонденций(ПараметрыЗапроса, СтрокаОбъектаУчета) Экспорт
	
	ПризнакиУчетаКорреспонденций = ПараметрыЗапроса.ПризнакиУчетаКорреспонденций; // ТаблицаЗначений -
	
	Для Каждого ПризнакУчетаКорреспонденции Из СтрокаОбъектаУчета.ПризнакиУчетаКорреспонденций Цикл
		НоваяСтрока = ПризнакиУчетаКорреспонденций.Добавить();
		НоваяСтрока.ОбъектУчета = СтрокаОбъектаУчета.ОбъектУчета;
		НоваяСтрока.КорОбъектУчета = ПризнакУчетаКорреспонденции.ОбъектУчета;
		НоваяСтрока.ПризнакУчета = ПризнакУчетаКорреспонденции.ПризнакУчета;
		НоваяСтрока.Значение = ПризнакУчетаКорреспонденции.Значение;
	КонецЦикла;
	
КонецПроцедуры

//++ НЕ УТКА

Процедура ЗаполнитьОбъектыНесобственныхЦенностей(ПараметрыЗапроса, СтрокаОбъектаУчета) Экспорт
	
	ОбъектыНесобственныхЦенностей = ПараметрыЗапроса.ОбъектыНесобственныхЦенностей; // ТаблицаЗначений -
	
	Если ПараметрыЗапроса.СпособУчетаНесобственныхЦенностей <> Перечисления.СпособыУчетаНесобственныхПодконтрольныхЦенностей.НеОтражаютсяНаСчетах
	   И ПараметрыЗапроса.СпособУчетаНесобственныхЦенностей <> Перечисления.СпособыУчетаНесобственныхПодконтрольныхЦенностей.ПустаяСсылка()
	   И СтрокаОбъектаУчета.НесобственныеЦенности Тогда
		НоваяСтрока = ОбъектыНесобственныхЦенностей.Добавить();
		НоваяСтрока.ОбъектУчета = СтрокаОбъектаУчета.ОбъектУчета;
	КонецЕсли;
	
КонецПроцедуры

//-- НЕ УТКА

Процедура ЗаполнитьОбъектыПоПризнакамУчетаНДС(ПараметрыЗапроса, СтрокаОбъектаУчета) Экспорт
	
	ОбъектыУчетаНДС = ПараметрыЗапроса.ОбъектыУчетаНДС; // Массив -
	ОбъектыУчетаСНДС = ПараметрыЗапроса.ОбъектыУчетаСНДС; // Массив -
	
	Если СтрокаОбъектаУчета.Показатели.Количество() = 1 Тогда
		Если СтрокаОбъектаУчета.Показатели[Перечисления.ПоказателиАналитическихРегистров.СуммаНДС] <> Неопределено Тогда
			ОбъектыУчетаНДС.Добавить(СтрокаОбъектаУчета.ОбъектУчета);
		ИначеЕсли СтрокаОбъектаУчета.Показатели[Перечисления.ПоказателиАналитическихРегистров.СуммаСНДС] <> Неопределено Тогда
			ОбъектыУчетаСНДС.Добавить(СтрокаОбъектаУчета.ОбъектУчета);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ФормированиеТекстовЗапросов

Процедура УточнитьПараметрыОтраженияДвижений(ПараметрыОтраженияДвижений, МетаданныеРегистра) Экспорт
	
	ВидРегистраНакопления = Метаданные.СвойстваОбъектов.ВидРегистраНакопления;
	
	Если ПустаяСтрока(ПараметрыОтраженияДвижений.УсловиеДебет) Тогда
		Если МетаданныеРегистра.ВидРегистра = ВидРегистраНакопления.Обороты Тогда
			ПараметрыОтраженияДвижений.УсловиеДебет = "ЛОЖЬ";
		ИначеЕсли ПараметрыОтраженияДвижений.ИнверсияДвижений Тогда
			ПараметрыОтраженияДвижений.УсловиеДебет = "&ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)";
		Иначе
			ПараметрыОтраженияДвижений.УсловиеДебет = "&ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)";
		КонецЕсли;
	КонецЕсли;
	
	Если ПустаяСтрока(ПараметрыОтраженияДвижений.УсловиеКредит) Тогда
		Если МетаданныеРегистра.ВидРегистра = ВидРегистраНакопления.Обороты Тогда
			ПараметрыОтраженияДвижений.УсловиеКредит = "ЛОЖЬ";
		ИначеЕсли ПараметрыОтраженияДвижений.ИнверсияДвижений Тогда
			ПараметрыОтраженияДвижений.УсловиеКредит = "&ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)";
		Иначе
			ПараметрыОтраженияДвижений.УсловиеКредит = "&ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)";
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура УточнитьСвойстваОбъектаУчета(ПараметрыОтраженияДвижений, СтрокаОбъектаУчета, МетаданныеРегистра) Экспорт
	
	ВидРегистраНакопления = Метаданные.СвойстваОбъектов.ВидРегистраНакопления;
	
	Если СтрокаОбъектаУчета.ИнверсияДвижений И НЕ ПараметрыОтраженияДвижений.ИнверсияДвижений
		И МетаданныеРегистра.ВидРегистра = ВидРегистраНакопления.Остатки Тогда
		Если ПустаяСтрока(СтрокаОбъектаУчета.УсловиеДебет) Тогда
			СтрокаОбъектаУчета.УсловиеДебет = "&ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)";
		КонецЕсли;
		
		Если ПустаяСтрока(СтрокаОбъектаУчета.УсловиеКредит) Тогда
			СтрокаОбъектаУчета.УсловиеКредит = "&ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)";
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Функция КлючРесурсовОбъектаУчета(СтрокаОбъектаУчета) Экспорт
	
	ВсеРесурсы = Новый Массив();
	
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеРесурсы, СтрокаОбъектаУчета.РесурсыУпр.ВыгрузитьКолонку("ИмяРесурса"));
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеРесурсы, СтрокаОбъектаУчета.РесурсыРегл.ВыгрузитьКолонку("ИмяРесурса"));
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеРесурсы, СтрокаОбъектаУчета.РесурсыВал.ВыгрузитьКолонку("ИмяРесурса"));
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеРесурсы, СтрокаОбъектаУчета.РесурсыКоличество.ВыгрузитьКолонку("ИмяРесурса"));
	
	ОбщегоНазначенияУТКлиентСервер.СортироватьМассив(ВсеРесурсы);
	
	Возврат СтрСоединить(ВсеРесурсы, ",");
	
КонецФункции

Функция ПоляВыборкиПоОписаниюОбъектаУчета() Экспорт
	
	ПоляВыборки = Новый Массив();
	
	ПоляВыборки.Добавить("ОбъектУчета");
	ПоляВыборки.Добавить("НаправлениеДеятельности");
	ПоляВыборки.Добавить("ИдентификаторФинЗаписи");
	ПоляВыборки.Добавить("НастройкаХозОперации");
	ПоляВыборки.Добавить("ИдентификаторСтроки");
	ПоляВыборки.Добавить("СуммаУпр");
	ПоляВыборки.Добавить("СуммаРегл");
	ПоляВыборки.Добавить("СуммаВал");
	ПоляВыборки.Добавить("Количество");
	ПоляВыборки.Добавить("ЭтоДебет");
	ПоляВыборки.Добавить("ЭтоКредит");
	ПоляВыборки.Добавить("ЭтоСторно");
	ПоляВыборки.Добавить("УсловиеЗначенияРесурсов");
	
	Возврат ПоляВыборки;
	
КонецФункции

Функция СтрокиВыборкиПолейПоОписаниюОбъектаУчета(СтрокиОбъектовУчета, ПараметрыОтраженияДвижений, ПараметрыЗапроса, СписокПолей = Неопределено) Экспорт
	
	Если СписокПолей = Неопределено Тогда
		СписокПолей = СтрСоединить(ПоляВыборкиПоОписаниюОбъектаУчета(), ",");
	КонецЕсли;
	
	ПоляВыборкиОбъектаУчета = Новый Структура(СписокПолей);
	
	ПоляВыборкиОбъектаУчета.ОбъектУчета = СтрокаВыборкиОбъектаУчета(СтрокиОбъектовУчета, ПараметрыЗапроса);
	
	ПоляВыборкиОбъектаУчета.НаправлениеДеятельности = СтрокаВыборкиПоляОбъектаУчета("ПутьКДаннымНаправлениеДеятельности",
		СтрокиОбъектовУчета, ПараметрыОтраженияДвижений, ПараметрыЗапроса, Справочники.НаправленияДеятельности.ПустаяСсылка());
	
	ПоляВыборкиОбъектаУчета.ИдентификаторФинЗаписи = СтрокаВыборкиПоляОбъектаУчета("ПутьКДаннымИдентификаторФинЗаписи",
		СтрокиОбъектовУчета, ПараметрыОтраженияДвижений, ПараметрыЗапроса, ПроведениеДокументов.ИдентификаторНеиспользуемойФинЗаписи());
	
	ПоляВыборкиОбъектаУчета.НастройкаХозОперации = СтрокаВыборкиПоляОбъектаУчета("ПутьКДаннымНастройкаХозОперации",
		СтрокиОбъектовУчета, ПараметрыОтраженияДвижений, ПараметрыЗапроса, Справочники.НастройкиХозяйственныхОпераций.ПустаяСсылка());
	
	ПоляВыборкиОбъектаУчета.ИдентификаторСтроки = СтрокаВыборкиПоляОбъектаУчета("ПутьКДаннымИдентификаторСтроки",
		СтрокиОбъектовУчета, ПараметрыОтраженияДвижений, ПараметрыЗапроса, "");
	
	РесурсыУпр = СтрокиОбъектовУчета[0].РесурсыУпр;
	РесурсыРегл = СтрокиОбъектовУчета[0].РесурсыРегл;
	РесурсыВал = СтрокиОбъектовУчета[0].РесурсыВал;
	РесурсыКоличество = СтрокиОбъектовУчета[0].РесурсыКоличество;
	
	СтрокиВыборкиРесурсов = Новый Структура(СтрСоединить(ПоляВыборкиЧисловыхРесурсов(), ","));
	СтрокиВыборкиРесурсов.СуммаУпр = СтрокаВыборкиРесурса(РесурсыУпр);
	СтрокиВыборкиРесурсов.СуммаРегл = СтрокаВыборкиРесурса(РесурсыРегл);
	СтрокиВыборкиРесурсов.СуммаВал = СтрокаВыборкиРесурса(РесурсыВал);
	СтрокиВыборкиРесурсов.Количество = СтрокаВыборкиРесурса(РесурсыКоличество);
	
	ЭтоСторно = СтрокаВыборкиПризнакаСторно(СтрокиВыборкиРесурсов);
	УсловияНеНоль = Новый Массив();
	
	ШаблонВыборкиСуммы = "СУММА(ВЫБОР
	|		КОГДА &ЭтоСторно
	|			ТОГДА -&Ресурс
	|		ИНАЧЕ &Ресурс
	|	КОНЕЦ)";
	
	Для Каждого КлючИЗначение Из СтрокиВыборкиРесурсов Цикл
		ПоляВыборкиОбъектаУчета[КлючИЗначение.Ключ] = СтрокаВыборкиСуммыРесурса(
			КлючИЗначение.Значение, ШаблонВыборкиСуммы, ЭтоСторно, УсловияНеНоль);
	КонецЦикла;
	
	Если УсловияНеНоль.Количество() = 0 Тогда
		УсловияНеНоль.Добавить(СтрокаВыборкиЗначения(Ложь));
	КонецЕсли;
	
	ПоляВыборкиОбъектаУчета.ЭтоСторно = ЭтоСторно;
	ПоляВыборкиОбъектаУчета.УсловиеЗначенияРесурсов = "(" + СтрСоединить(УсловияНеНоль, " ИЛИ ") + ")";
	
	ЭтоДебет = "ВЫБОР
	|		КОГДА &ЭтоСторно
	|			ТОГДА &УсловиеКредит
	|		ИНАЧЕ &УсловиеДебет
	|	КОНЕЦ";
	
	ЭтоКредит = "ВЫБОР
	|		КОГДА &ЭтоСторно
	|			ТОГДА &УсловиеДебет
	|		ИНАЧЕ &УсловиеКредит
	|	КОНЕЦ";
	
	УсловиеДебет = СтрокаВыборкиПоляОбъектаУчета("УсловиеДебет",
		СтрокиОбъектовУчета, ПараметрыОтраженияДвижений, ПараметрыЗапроса, Ложь);
		
	УсловиеКредит = СтрокаВыборкиПоляОбъектаУчета("УсловиеКредит",
		СтрокиОбъектовУчета, ПараметрыОтраженияДвижений, ПараметрыЗапроса, Ложь);
	
	ЭтоДебет = СтрЗаменить(ЭтоДебет, "&ЭтоСторно", ЭтоСторно);
	ЭтоДебет = СтрЗаменить(ЭтоДебет, "&УсловиеДебет", УсловиеДебет);
	ЭтоДебет = СтрЗаменить(ЭтоДебет, "&УсловиеКредит", УсловиеКредит);
	
	ЭтоКредит = СтрЗаменить(ЭтоКредит, "&ЭтоСторно", ЭтоСторно);
	ЭтоКредит = СтрЗаменить(ЭтоКредит, "&УсловиеДебет", УсловиеДебет);
	ЭтоКредит = СтрЗаменить(ЭтоКредит, "&УсловиеКредит", УсловиеКредит);
	
	ПоляВыборкиОбъектаУчета.ЭтоДебет = ЭтоДебет;
	ПоляВыборкиОбъектаУчета.ЭтоКредит = ЭтоКредит;
	
	Возврат ПоляВыборкиОбъектаУчета;
	
КонецФункции

Функция СтрокаВыборкиОбъектаУчета(СтрокиОбъектовУчета, ПараметрыЗапроса) Экспорт
	
	Если СтрокиОбъектовУчета[0].ИсточникДанных = Метаданные.РегистрыНакопления.ДвиженияПоПрочимАктивамПассивам.Имя Тогда
		Возврат СтрокаВыборкиОбъектовПрочиеАктивыПассивы(СтрокиОбъектовУчета, ПараметрыЗапроса);
	КонецЕсли;
	
	СтрокиВыбора = Новый Массив();
	ОбъектыБезОтбора = Новый Массив();
	
	Для Каждого СтрокаОбъектаУчета Из СтрокиОбъектовУчета Цикл
		ОтборДанных = СтрокаОбъектаУчета.ОтборДанных; //ОтборКомпоновкиДанных -
		Если ОтборДанных.Элементы.Количество() > 0 Тогда
			СтрокиКогда = Новый Массив();
			СтрокиКогда.Добавить(Символы.Таб);
			СтрокиКогда.Добавить("КОГДА"); //@Query-part
			СтрокиКогда.Добавить(СтрокаУсловияВыборкиДанных(ОтборДанных, ПараметрыЗапроса));
			
			СтрокиТогда = Новый Массив();
			СтрокиТогда.Добавить(Символы.Таб);
			СтрокиТогда.Добавить("ТОГДА"); //@Query-part
			СтрокиТогда.Добавить(СтрокаВыборкиЗначения(СтрокаОбъектаУчета.ОбъектУчета));
			
			СтрокиВыбора.Добавить(СтрСоединить(СтрокиКогда, " "));
			СтрокиВыбора.Добавить(СтрСоединить(СтрокиТогда, " "));
		Иначе
			ОбъектыБезОтбора.Добавить(СтрокаОбъектаУчета);
		КонецЕсли;
	КонецЦикла;
	
	Если СтрокиВыбора.Количество() = 0 И ОбъектыБезОтбора.Количество() = 0 Тогда
		СтрокаВыборки = СтрокаВыборкиЗначения(ПланыВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка());
	ИначеЕсли ОбъектыБезОтбора.Количество() > 1 Тогда
		СтрокиИсключения = Новый Массив();
		СтрокиИсключения.Добавить("ru = 'Для регистра %1 описано более одного объекта финансового учета без указания отбора выборки данных:';
									|en = 'For the %1 register, more than one financial accounting item is described without the dataset filter specified:'");
		Для Каждого СтрокаОбъектаУчета Из ОбъектыБезОтбора Цикл
			СтрокиИсключения[0] = СтрЗаменить(СтрокиИсключения[0], "%1", СтрокаОбъектаУчета.ИсточникДанных);
			СтрокиИсключения.Добавить(СтрокаОбъектаУчета.ОбъектУчета);
		КонецЦикла;
		ВызватьИсключение СтрСоединить(СтрокиИсключения, Символы.ПС);
	ИначеЕсли СтрокиВыбора.Количество() = 0 Тогда
		СтрокаВыборки = СтрокаВыборкиЗначения(ОбъектыБезОтбора[0].ОбъектУчета);
	Иначе
		СтрокиИначе = Новый Массив();
		СтрокиИначе.Добавить(Символы.Таб);
		СтрокиИначе.Добавить("ИНАЧЕ"); //@Query-part
		Если ОбъектыБезОтбора.Количество() = 0 Тогда
			СтрокиИначе.Добавить(СтрокаВыборкиЗначения(ПланыВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка()));
		Иначе
			СтрокиИначе.Добавить(СтрокаВыборкиЗначения(ОбъектыБезОтбора[0].ОбъектУчета));
		КонецЕсли;
		
		СтрокиКонец = Новый Массив();
		СтрокиКонец.Добавить(Символы.Таб);
		СтрокиКонец.Добавить("КОНЕЦ"); //@Query-part
		
		СтрокиВыбора.Вставить(0, "ВЫБОР"); //@Query-part
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиИначе, " "));
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиКонец, ""));
		СтрокаВыборки = СтрСоединить(СтрокиВыбора, Символы.ПС);
	КонецЕсли;
	
	Возврат СтрокаВыборки;
	
КонецФункции

Функция СтрокаВыборкиОбъектовПрочиеАктивыПассивы(СтрокиОбъектовУчета, ПараметрыЗапроса) Экспорт
	
	СтрокиВыбора = Новый Массив();
	ПутьКДаннымОбъектУчета = ПутьКДаннымСПсевдонимомИсточника("Статья");
	
	Для Каждого СтрокаОбъектаУчета Из СтрокиОбъектовУчета Цикл
		ОтборДанных = СтрокаОбъектаУчета.ОтборДанных; //ОтборКомпоновкиДанных -
		КоличествоЭлементовОтбора = ОтборДанных.Элементы.Количество();
		ОтборТолькоНаРавенствоСтатье = Истина;
		
		Если КоличествоЭлементовОтбора = 1 Тогда
			ЭлементОтбора = ОтборДанных.Элементы[0];
			Если ТипЗнч(ЭлементОтбора) <> Тип("ЭлементОтбораКомпоновкиДанных")
			 ИЛИ ЭлементОтбора.ЛевоеЗначение <> Новый ПолеКомпоновкиДанных("Статья")
			 ИЛИ ЭлементОтбора.ВидСравнения <> ВидСравненияКомпоновкиДанных.Равно
			 ИЛИ ЭлементОтбора.ПравоеЗначение <> СтрокаОбъектаУчета.ОбъектУчета Тогда
				ОтборТолькоНаРавенствоСтатье = Ложь;
			КонецЕсли;
		ИначеЕсли КоличествоЭлементовОтбора > 1 Тогда
			ОтборТолькоНаРавенствоСтатье = Ложь;
		КонецЕсли;
		
		Если НЕ ОтборТолькоНаРавенствоСтатье Тогда
			СтрокиКогда = Новый Массив();
			СтрокиКогда.Добавить(Символы.Таб);
			СтрокиКогда.Добавить("КОГДА"); //@Query-part
			СтрокиКогда.Добавить(СтрокаУсловияВыборкиДанных(ОтборДанных, ПараметрыЗапроса));
			
			СтрокиТогда = Новый Массив();
			СтрокиТогда.Добавить(Символы.Таб);
			СтрокиТогда.Добавить("ТОГДА"); //@Query-part
			СтрокиТогда.Добавить(СтрокаВыборкиЗначения(СтрокаОбъектаУчета.ОбъектУчета));
			
			СтрокиВыбора.Добавить(СтрСоединить(СтрокиКогда, " "));
			СтрокиВыбора.Добавить(СтрСоединить(СтрокиТогда, " "));
		КонецЕсли;
	КонецЦикла;
	
	Если СтрокиВыбора.Количество() = 0 Тогда
		СтрокаВыборки = ПутьКДаннымОбъектУчета;
	Иначе
		СтрокиИначе = Новый Массив();
		СтрокиИначе.Добавить(Символы.Таб);
		СтрокиИначе.Добавить("ИНАЧЕ"); //@Query-part
		СтрокиИначе.Добавить(ПутьКДаннымОбъектУчета);
		
		СтрокиКонец = Новый Массив();
		СтрокиКонец.Добавить(Символы.Таб);
		СтрокиКонец.Добавить("КОНЕЦ"); //@Query-part
		
		СтрокиВыбора.Вставить(0, "ВЫБОР"); //@Query-part
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиИначе, " "));
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиКонец, ""));
		СтрокаВыборки = СтрСоединить(СтрокиВыбора, Символы.ПС);
	КонецЕсли;
	
	Возврат СтрокаВыборки;
	
КонецФункции

Функция СтрокаВыборкиПоляОбъектаУчета(СвойствоПутьКДанным, СтрокиОбъектовУчета,
	ПараметрыОтраженияДвижений, ПараметрыЗапроса, НеопределенноеЗначение) Экспорт
	
	ПутиКДаннымОбъектовУчета = Новый Соответствие();
	ПутьКДаннымБезОтбора = Неопределено;
	
	Для Каждого СтрокаОбъектаУчета Из СтрокиОбъектовУчета Цикл
		ПутьКДанным = СтрокаОбъектаУчета[СвойствоПутьКДанным];
		Если ПутиКДаннымОбъектовУчета[ПутьКДанным] = Неопределено Тогда
			ПутиКДаннымОбъектовУчета.Вставить(ПутьКДанным, Новый Массив());
		КонецЕсли;
		ПутиКДаннымОбъектовУчета[ПутьКДанным].Добавить(СтрокаОбъектаУчета);
		
		ОтборДанных = СтрокаОбъектаУчета.ОтборДанных; //ОтборКомпоновкиДанных -
		Если ОтборДанных.Элементы.Количество() = 0 Тогда
			ПутьКДаннымБезОтбора = ПутьКДанным;
		КонецЕсли;
	КонецЦикла;
	
	СтрокиВыбора = Новый Массив();
	ИгнорироватьЗначениеПоУмолчанию = ПараметрыОтраженияДвижений.СодержитНебалансовыеДанные
		И СвойствоПутьКДанным = "ПутьКДаннымИдентификаторФинЗаписи";
	
	Для Каждого ПутьКДаннымОбъектовУчета Из ПутиКДаннымОбъектовУчета Цикл
		Если ПутьКДаннымОбъектовУчета.Ключ = ПутьКДаннымБезОтбора Тогда
			Продолжить;
		ИначеЕсли ИгнорироватьЗначениеПоУмолчанию Тогда
		ИначеЕсли ПутьКДаннымБезОтбора = Неопределено И ПустаяСтрока(ПутьКДаннымОбъектовУчета.Ключ) Тогда
			Продолжить;
		КонецЕсли;
		
		ГруппаОтборов = Новый Массив();
		Для Каждого СтрокаОбъектаУчета Из ПутьКДаннымОбъектовУчета.Значение Цикл
			ГруппаОтборов.Добавить(СтрокаОбъектаУчета.ОтборДанных);
		КонецЦикла;
		
		СтрокиКогда = Новый Массив();
		СтрокиКогда.Добавить(Символы.Таб);
		СтрокиКогда.Добавить("КОГДА"); //@Query-part
		СтрокиКогда.Добавить(СтрокаУсловияВыборкиДанных(ГруппаОтборов, ПараметрыЗапроса));
		
		СтрокиТогда = Новый Массив();
		СтрокиТогда.Добавить(Символы.Таб);
		СтрокиТогда.Добавить("ТОГДА"); //@Query-part
		Если НЕ ПустаяСтрока(ПутьКДаннымОбъектовУчета.Ключ) Тогда
			СтрокиТогда.Добавить(ПутьКДаннымСПсевдонимомИсточника(ПутьКДаннымОбъектовУчета.Ключ));
		ИначеЕсли НЕ ПустаяСтрока(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]) Тогда
			СтрокиТогда.Добавить(ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]));
		Иначе
			СтрокиТогда.Добавить(СтрокаВыборкиЗначения(НеопределенноеЗначение));
		КонецЕсли;
		
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиКогда, " "));
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиТогда, " "));
	КонецЦикла;
	
	Если СтрокиВыбора.Количество() > 0 Тогда
		СтрокиИначе = Новый Массив();
		СтрокиИначе.Добавить(Символы.Таб);
		СтрокиИначе.Добавить("ИНАЧЕ"); //@Query-part
		Если ЗначениеЗаполнено(ПутьКДаннымБезОтбора) Тогда
			СтрокиИначе.Добавить(ПутьКДаннымСПсевдонимомИсточника(ПутьКДаннымБезОтбора));
		ИначеЕсли ИгнорироватьЗначениеПоУмолчанию Тогда
			СтрокиИначе.Добавить(СтрокаВыборкиЗначения(НеопределенноеЗначение));
		ИначеЕсли НЕ ПустаяСтрока(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]) Тогда
			СтрокиИначе.Добавить(ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]));
		Иначе
			СтрокиИначе.Добавить(СтрокаВыборкиЗначения(НеопределенноеЗначение));
		КонецЕсли;
		
		СтрокиКонец = Новый Массив();
		СтрокиКонец.Добавить(Символы.Таб);
		СтрокиКонец.Добавить("КОНЕЦ"); //@Query-part
		
		СтрокиВыбора.Вставить(0, "ВЫБОР"); //@Query-part
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиИначе, " "));
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиКонец, ""));
		СтрокаВыборки = СтрСоединить(СтрокиВыбора, Символы.ПС);
	ИначеЕсли ЗначениеЗаполнено(ПутьКДаннымБезОтбора) Тогда
		СтрокаВыборки = ПутьКДаннымСПсевдонимомИсточника(ПутьКДаннымБезОтбора);
	ИначеЕсли НЕ ПустаяСтрока(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]) Тогда
		СтрокаВыборки = ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]);
	Иначе
		СтрокаВыборки = СтрокаВыборкиЗначения(НеопределенноеЗначение);
	КонецЕсли;
	
	Возврат СтрокаВыборки;
	
КонецФункции

Функция СтрокаВыборкиПоляСПризнакомУчетаВОбъекте(СвойствоПутьКДанным, СвойствоПризнакУчета,
	СтрокиОбъектовУчета, ПараметрыОтраженияДвижений, ПараметрыЗапроса, НеопределенноеЗначение) Экспорт
	
	ЗначениеОбщегоПризнакаУчета = Неопределено;
	
	Если ПараметрыЗапроса.Свойство(СвойствоПризнакУчета, ЗначениеОбщегоПризнакаУчета)
		И НЕ Булево(ЗначениеОбщегоПризнакаУчета) Тогда
		Возврат СтрокаВыборкиЗначения(НеопределенноеЗначение);
	ИначеЕсли НЕ Булево(ПараметрыОтраженияДвижений[СвойствоПризнакУчета]) Тогда
		Возврат СтрокаВыборкиЗначения(НеопределенноеЗначение);
	ИначеЕсли ПустаяСтрока(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]) Тогда
		Возврат СтрокаВыборкиЗначения(НеопределенноеЗначение);
	КонецЕсли;
	
	ПризнакиУчетаОбъектовУчета = Новый Соответствие();
	ПризнакУчетаБезОтбора = Неопределено;
	
	Для Каждого СтрокаОбъектаУчета Из СтрокиОбъектовУчета Цикл
		ПризнакУчета = Булево(СтрокаОбъектаУчета[СвойствоПризнакУчета]);
		Если ПризнакиУчетаОбъектовУчета[ПризнакУчета] = Неопределено Тогда
			ПризнакиУчетаОбъектовУчета.Вставить(ПризнакУчета, Новый Массив());
		КонецЕсли;
		ПризнакиУчетаОбъектовУчета[ПризнакУчета].Добавить(СтрокаОбъектаУчета);
		
		ОтборДанных = СтрокаОбъектаУчета.ОтборДанных; //ОтборКомпоновкиДанных -
		Если ОтборДанных.Элементы.Количество() = 0 Тогда
			ПризнакУчетаБезОтбора = ПризнакУчета;
		КонецЕсли;
	КонецЦикла;
	
	СтрокиВыбора = Новый Массив();
	
	Для Каждого ПризнакУчетаОбъектовУчета Из ПризнакиУчетаОбъектовУчета Цикл
		Если ПризнакУчетаОбъектовУчета.Ключ = ПризнакУчетаБезОтбора Тогда
			Продолжить;
		КонецЕсли;
		
		ГруппаОтборов = Новый Массив();
		Для Каждого СтрокаОбъектаУчета Из ПризнакУчетаОбъектовУчета.Значение Цикл
			ГруппаОтборов.Добавить(СтрокаОбъектаУчета.ОтборДанных);
		КонецЦикла;
		
		СтрокиКогда = Новый Массив();
		СтрокиКогда.Добавить(Символы.Таб);
		СтрокиКогда.Добавить("КОГДА"); //@Query-part
		СтрокиКогда.Добавить(СтрокаУсловияВыборкиДанных(ГруппаОтборов, ПараметрыЗапроса));
		
		СтрокиТогда = Новый Массив();
		СтрокиТогда.Добавить(Символы.Таб);
		СтрокиТогда.Добавить("ТОГДА"); //@Query-part
		Если ПризнакУчетаОбъектовУчета.Ключ = Истина Тогда
			СтрокиТогда.Добавить(ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]));
		Иначе
			СтрокиТогда.Добавить(СтрокаВыборкиЗначения(НеопределенноеЗначение));
		КонецЕсли;
		
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиКогда, " "));
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиТогда, " "));
	КонецЦикла;
	
	Если СтрокиВыбора.Количество() > 0 Тогда
		СтрокиИначе = Новый Массив();
		СтрокиИначе.Добавить(Символы.Таб);
		СтрокиИначе.Добавить("ИНАЧЕ"); //@Query-part
		Если ПризнакУчетаБезОтбора = Истина Тогда
			СтрокиИначе.Добавить(ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]));
		Иначе
			СтрокиИначе.Добавить(СтрокаВыборкиЗначения(НеопределенноеЗначение));
		КонецЕсли;
		
		СтрокиКонец = Новый Массив();
		СтрокиКонец.Добавить(Символы.Таб);
		СтрокиКонец.Добавить("КОНЕЦ"); //@Query-part
		
		СтрокиВыбора.Вставить(0, "ВЫБОР"); //@Query-part
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиИначе, " "));
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиКонец, ""));
		СтрокаВыборки = СтрСоединить(СтрокиВыбора, Символы.ПС);
	ИначеЕсли ПризнакУчетаБезОтбора = Истина Тогда
		СтрокаВыборки = ПутьКДаннымСПсевдонимомИсточника(ПараметрыОтраженияДвижений[СвойствоПутьКДанным]);
	Иначе
		СтрокаВыборки = СтрокаВыборкиЗначения(НеопределенноеЗначение);
	КонецЕсли;
	
	Возврат СтрокаВыборки;
	
КонецФункции

Функция СтрокаВыборкиПризнакаСторно(СтрокиВыборкиРесурсов) Экспорт
	
	СтрокиУсловия = Новый Массив();
	СтрокиУсловия.Добавить("(&СуммаУпр < 0 И &СуммаРегл <= 0)");
	СтрокиУсловия.Добавить("(&СуммаУпр <= 0 И &СуммаРегл < 0)");
	
	Если СтрокиВыборкиРесурсов.СуммаВал <> "0" ИЛИ СтрокиВыборкиРесурсов.Количество <> "0" Тогда
		СтрокиУсловия.Добавить("(&СуммаУпр = 0 И &СуммаРегл = 0 И (&СуммаВал < 0 ИЛИ &Количество < 0))");
	КонецЕсли;
	
	СтрокаВыборкиУсловия = "(" + СтрСоединить(СтрокиУсловия, " ИЛИ ") + ")";
	
	Для Каждого КлючИЗначение Из СтрокиВыборкиРесурсов Цикл
		СтрокаВыборкиУсловия = СтрЗаменить(СтрокаВыборкиУсловия, "&" + КлючИЗначение.Ключ, КлючИЗначение.Значение);
	КонецЦикла;
	
	Возврат СтрокаВыборкиУсловия;
	
КонецФункции

Функция СтрокаВыборкиРесурса(СоставРесурса) Экспорт
	
	Если СоставРесурса.Количество() = 0 Тогда
		СтрокаВыборки = "0";
	Иначе
		ПутиКДанным = Новый Массив();
		
		Для Каждого ПолеРесурса Из СоставРесурса Цикл
			Если ТипЗнч(ПолеРесурса) = Тип("СтрокаТаблицыЗначений") Тогда
				ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника(ПолеРесурса.ИмяРесурса);
				Коэффициент = ПолеРесурса.Коэффициент;
			ИначеЕсли ПолеРесурса = "0" Тогда
				ПутьКДанным = ПолеРесурса;
				Коэффициент = 0;
			ИначеЕсли СтрНачинаетсяС(ПолеРесурса, "-") Тогда
				ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника(Сред(ПолеРесурса, 2));
				Коэффициент = -1;
			Иначе
				ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника(ПолеРесурса);
				Коэффициент = 1;
			КонецЕсли;
			
			Если Коэффициент = 0 Тогда
				ПутиКДанным.Добавить("0");
			Иначе
				ПутиКДанным.Добавить(?(Коэффициент < 0, "-", "") + ПутьКДанным);
			КонецЕсли;
		КонецЦикла;
		
		СтрокаВыборки = СтрСоединить(ПутиКДанным, " + ");
		
		Если ПутиКДанным.Количество() > 1 Тогда
			СтрокаВыборки = "(" + СтрокаВыборки + ")";
		КонецЕсли;
	КонецЕсли;
	
	Возврат СтрокаВыборки;
	
КонецФункции

Функция СтрокаВыборкиСуммыРесурса(СтрокаВыборкиРесурса, ШаблонВыборкиСуммы, СтрокаВыборкиСторно, УсловияНеНоль) Экспорт
	
	Если СтрокаВыборкиРесурса = "0" Тогда
		СтрокаВыборкиСуммы = "0";
	Иначе
		СтрокаВыборкиСуммы = СтрЗаменить(ШаблонВыборкиСуммы, "&Ресурс", СтрокаВыборкиРесурса);
		СтрокаВыборкиСуммы = СтрЗаменить(СтрокаВыборкиСуммы, "&ЭтоСторно", СтрокаВыборкиСторно);
		УсловияНеНоль.Добавить(СтрокаВыборкиРесурса + " <> 0");
	КонецЕсли;
	
	Возврат СтрокаВыборкиСуммы;
	
КонецФункции

Процедура ИзменитьСоставПолейВыборки(ТекстЗапроса, УдаляемыеПоля = Неопределено, ПоляИндекса = Неопределено, ТаблицаДляПомещения = "") Экспорт
	
	СхемаЗапроса = Новый СхемаЗапроса();
	ПакетЗапросов = СхемаЗапроса.ПакетЗапросов[0];
	ПакетЗапросов.УстановитьТекстЗапроса(ТекстЗапроса);
	
	Если УдаляемыеПоля <> Неопределено Тогда
		Для Каждого ПсевдонимПоля Из УдаляемыеПоля Цикл
			Колонка = ПакетЗапросов.Колонки.Найти(ПсевдонимПоля);
			Если Колонка <> Неопределено Тогда
				ИндексКолонки = ПакетЗапросов.Колонки.Индекс(Колонка);
				ПакетЗапросов.Колонки.Удалить(ИндексКолонки);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если ПоляИндекса <> Неопределено Тогда
		Для Каждого ПсевдонимПоля Из ПоляИндекса Цикл
			ПакетЗапросов.Индекс.Добавить(ПсевдонимПоля);
		КонецЦикла;
	КонецЕсли;
	
	Если НЕ ПустаяСтрока(ТаблицаДляПомещения) Тогда
		ПакетЗапросов.ТаблицаДляПомещения = ТаблицаДляПомещения;
	ИначеЕсли НЕ ПустаяСтрока(ПакетЗапросов.ТаблицаДляПомещения) Тогда
		ПакетЗапросов.ТаблицаДляПомещения = "";
	КонецЕсли;

	ТекстЗапроса = ПакетЗапросов.ПолучитьТекстЗапроса();
	
КонецПроцедуры

Функция ПутьКДаннымСПсевдонимомИсточника(ПутьКДанным, ПсевдонимИсточника = Неопределено) Экспорт
	
	Если ПсевдонимИсточника = Неопределено Тогда
		ПсевдонимИсточника = ПсевдонимИсточникаДанныхПоУмолчанию();
	КонецЕсли;
	
	ЭтоКонстанта = ПутьКДанным = "ЛОЖЬ"
		ИЛИ ПутьКДанным = "ИСТИНА"
		ИЛИ ПутьКДанным = "НЕОПРЕДЕЛЕНО"
		ИЛИ СтрНачинаетсяС(ПутьКДанным, "ТИП(")
		ИЛИ СтрНачинаетсяС(ПутьКДанным, "ЗНАЧЕНИЕ(")
		ИЛИ СтрНачинаетсяС(ПутьКДанным, "ДАТАВРЕМЯ(");
	
	ЭтоВыражение = СтрНайти(ПутьКДанным, " ") > 0
		ИЛИ СтрНайти(ПутьКДанным, Символы.ПС) > 0;
	
	ЭтоПараметр = СтрНачинаетсяС(ПутьКДанным, "&");
	
	Если ПустаяСтрока(ПсевдонимИсточника)
	 ИЛИ СтрНачинаетсяС(ПутьКДанным, ПсевдонимИсточника)
	 ИЛИ ЭтоКонстанта
	 ИЛИ ЭтоВыражение
	 ИЛИ ЭтоПараметр Тогда
		Возврат ПутьКДанным;
	Иначе
		Возврат ПсевдонимИсточника + "." + ПутьКДанным;
	КонецЕсли;
	
КонецФункции

Функция ПутьКДаннымБезПсевдонимаИсточника(ПутьКДанным, ПсевдонимИсточника = Неопределено) Экспорт
	
	Если ПсевдонимИсточника = Неопределено Тогда
		ПсевдонимИсточника = ПсевдонимИсточникаДанныхПоУмолчанию();
	КонецЕсли;
	
	Возврат СтрЗаменить(ПутьКДанным, ПсевдонимИсточника + ".", "");
	
КонецФункции

Функция СтрокаВыборкиЗначения(ЗначениеВыборки) Экспорт
	
	ТипЗначенияВыборки = ТипЗнч(ЗначениеВыборки);
	
	Если ТипЗначенияВыборки = Тип("Неопределено") Тогда
		СтрокаВыборки = "НЕОПРЕДЕЛЕНО";
	ИначеЕсли ТипЗначенияВыборки = Тип("Число") Тогда
		СтрокаВыборки = Формат(ЗначениеВыборки, "ЧРД=.; ЧН=0; ЧГ=0");
	ИначеЕсли ТипЗначенияВыборки = Тип("Дата") Тогда
		СтрокаВыборки = "ДАТАВРЕМЯ(" + Формат(ЗначениеВыборки, "ДФ=yyyy,MM,dd; ДП=0001,01,01") + ")";
	ИначеЕсли ТипЗначенияВыборки = Тип("Строка") Тогда
		СтрокаВыборки = """" + ЗначениеВыборки + """";
	ИначеЕсли ТипЗначенияВыборки = Тип("Булево") Тогда
		СтрокаВыборки = Формат(ЗначениеВыборки, "БЛ=ЛОЖЬ; БИ=ИСТИНА");
	ИначеЕсли ТипЗначенияВыборки = Тип("Тип") Тогда
		СтрокаВыборки = "ТИП(" + ПолноеИмяТипа(ЗначениеВыборки) + ")";
	ИначеЕсли Перечисления.ТипВсеСсылки().СодержитТип(ТипЗначенияВыборки) Тогда
		СтрокаВыборки = "ЗНАЧЕНИЕ(" + ПолноеИмяЗначенияПеречисления(ЗначениеВыборки) + ")";
	ИначеЕсли (Справочники.ТипВсеСсылки().СодержитТип(ТипЗначенияВыборки)
		ИЛИ ПланыВидовХарактеристик.ТипВсеСсылки().СодержитТип(ТипЗначенияВыборки)
		ИЛИ ПланыСчетов.ТипВсеСсылки().СодержитТип(ТипЗначенияВыборки))
		И (ЗначениеВыборки.Предопределенный ИЛИ ЗначениеВыборки.Пустая()) Тогда
		СтрокаВыборки = "ЗНАЧЕНИЕ(" + ПолноеИмяПредопределенногоЗначения(ЗначениеВыборки) + ")";
	ИначеЕсли Документы.ТипВсеСсылки().СодержитТип(ТипЗначенияВыборки)
		И ЗначениеВыборки.Пустая() Тогда
		СтрокаВыборки = "ЗНАЧЕНИЕ(" + ПолноеИмяПредопределенногоЗначения(ЗначениеВыборки) + ")";
	ИначеЕсли ТипЗначенияВыборки = Тип("СписокЗначений") Тогда
		СтрокаВыборки = СтрокаВыборкиЗначения(ЗначениеВыборки.ВыгрузитьЗначения());
	ИначеЕсли ТипЗначенияВыборки = Тип("Массив") Тогда
		СтрокиВыборкиЭлементов = Новый Массив();
		Для Каждого ЭлементВыборки Из ЗначениеВыборки Цикл
			СтрокаВыборкиЭлемента = СтрокаВыборкиЗначения(ЭлементВыборки);
			Если СтрНачинаетсяС(СтрокаВыборкиЭлемента, "Параметр") Тогда
				СтрокиВыборкиЭлементов.Очистить();
				Прервать;
			Иначе
				СтрокиВыборкиЭлементов.Добавить(СтрокаВыборкиЭлемента);
			КонецЕсли;
		КонецЦикла;
		Если СтрокиВыборкиЭлементов.Количество() > 0 Тогда
			Разделитель = "," + Символы.ПС + Символы.Таб + Символы.Таб;
			СтрокаВыборки = СтрСоединить(СтрокиВыборкиЭлементов, Разделитель);
		Иначе
			СтрокаВыборки = "Параметр" + СтрЗаменить(Строка(Новый УникальныйИдентификатор()), "-", "");
		КонецЕсли;
	Иначе
		СтрокаВыборки = "Параметр" + СтрЗаменить(Строка(Новый УникальныйИдентификатор()), "-", "");
	КонецЕсли;
	
	Возврат СтрокаВыборки;
	
КонецФункции

Функция СтрокаВыбораЗначенияПоля(СтрокиВыборкиПоля) Экспорт
	
	Если СтрокиВыборкиПоля.Количество() = 1 Тогда
		Для Каждого КлючИЗначение Из СтрокиВыборкиПоля Цикл
			СтрокаВыбораЗначения = КлючИЗначение.Ключ;
			Прервать;
		КонецЦикла;
	Иначе
		ЧастотнаяСтрокаВыборки = "";
		КоличествоИспользований = 0;
		
		Для Каждого КлючИЗначение Из СтрокиВыборкиПоля Цикл
			Если КлючИЗначение.Значение.Количество() > КоличествоИспользований Тогда
				ЧастотнаяСтрокаВыборки = КлючИЗначение.Ключ;
				КоличествоИспользований = КлючИЗначение.Значение.Количество();
			КонецЕсли;
		КонецЦикла;
		
		СтрокиВыбора = Новый Массив();
		
		Для Каждого КлючИЗначение Из СтрокиВыборкиПоля Цикл
			Если КлючИЗначение.Ключ = ЧастотнаяСтрокаВыборки Тогда
				Продолжить;
			КонецЕсли;
			
			Если КлючИЗначение.Значение.Количество() = 1 Тогда
				ЗначениеОтбора = КлючИЗначение.Значение[0];
			Иначе
				ЗначениеОтбора = КлючИЗначение.Значение;
			КонецЕсли;
			
			УсловиеОтбора = Новый Структура("НомерПовтора", ЗначениеОтбора);
			
			СтрокиКогда = Новый Массив();
			СтрокиКогда.Добавить(Символы.Таб);
			СтрокиКогда.Добавить("КОГДА"); //@Query-part
			СтрокиКогда.Добавить(СтрокаУсловияВыборкиДанных(УсловиеОтбора,, "Повторитель"));
			
			СтрокиТогда = Новый Массив();
			СтрокиТогда.Добавить(Символы.Таб);
			СтрокиТогда.Добавить("ТОГДА"); //@Query-part
			СтрокиТогда.Добавить(КлючИЗначение.Ключ);
			
			СтрокиВыбора.Добавить(СтрСоединить(СтрокиКогда, " "));
			СтрокиВыбора.Добавить(СтрСоединить(СтрокиТогда, " "));
		КонецЦикла;
		
		СтрокиИначе = Новый Массив();
		СтрокиИначе.Добавить(Символы.Таб);
		СтрокиИначе.Добавить("ИНАЧЕ"); //@Query-part
		СтрокиИначе.Добавить(ЧастотнаяСтрокаВыборки);
		
		СтрокиКонец = Новый Массив();
		СтрокиКонец.Добавить(Символы.Таб);
		СтрокиКонец.Добавить("КОНЕЦ"); //@Query-part
		
		СтрокиВыбора.Вставить(0, "ВЫБОР"); //@Query-part
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиИначе, " "));
		СтрокиВыбора.Добавить(СтрСоединить(СтрокиКонец, ""));
		
		СтрокаВыбораЗначения = СтрСоединить(СтрокиВыбора, Символы.ПС);
	КонецЕсли;
	
	Возврат СтрокаВыбораЗначения;
	
КонецФункции

Функция РесурсОтсутствуетИлиМожетБытьПустым(СоставРесурса) Экспорт
	
	Если СоставРесурса.Количество() = 0 Тогда
		РесурсОтсутствует = Истина;
		РесурсМожетБытьПустым = Истина;
	Иначе
		РесурсОтсутствует = Ложь;
		РесурсМожетБытьПустым = СоставРесурса[0] = "0";
	КонецЕсли;
	
	Возврат РесурсОтсутствует ИЛИ РесурсМожетБытьПустым;
	
КонецФункции

Процедура ДобавитьПовторительВИсточникДанных(ИсточникДанных, КоличествоПовторов) Экспорт
	
	СтрокиВыборки = Новый Массив();
	ШаблонВыборки = "
	|	ВЫБРАТЬ
	|		&НомерПовтора КАК НомерПовтора"; //@Query-part
	
	Для НомерПовтора = 1 По КоличествоПовторов Цикл
		СтрокиВыборки.Добавить(СтрЗаменить(ШаблонВыборки, "&НомерПовтора", СтрокаВыборкиЗначения(НомерПовтора)));
	КонецЦикла;
	
	Разделитель = "
	|	ОБЪЕДИНИТЬ ВСЕ"; //@Query-part
	
	ИсточникДанных = ИсточникДанных + СтрШаблон("
		|	,
		|	(%1) КАК Повторитель",
		СтрСоединить(СтрокиВыборки, Разделитель));
	
КонецПроцедуры

Процедура ДобавитьПовторительВПоляГруппировки(ТекстЗапроса) Экспорт
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса,
		"СГРУППИРОВАТЬ ПО",
		"СГРУППИРОВАТЬ ПО
		|	Повторитель.НомерПовтора,");
	
КонецПроцедуры

// Возвращает фрагмент текста запроса с условием на поле Период
//
// Параметры:
//  ПараметрыЗапроса - Структура - параметры запроса, в том числе:
//   * ДатаНачала - Дата - начало периода
//   * ДатаОкончания - Дата - конец периода
//
// Возвращаемое значение:
//  Строка - фрагмент текста запроса
//
Функция СтрокаУсловияПериод(ПараметрыЗапроса) Экспорт
	
	СтрокиУсловия = Новый Массив();
	
	Если ПараметрыЗапроса.ДатаНачала > '00010101' Тогда
		СтрокиУсловия.Добавить("&Период >= &ДатаНачала");
	КонецЕсли;
	
	Если ПараметрыЗапроса.ДатаОкончания > '00010101' Тогда
		СтрокиУсловия.Добавить("&Период <= &ДатаОкончания");
	КонецЕсли;
	
	Если СтрокиУсловия.Количество() = 0 Тогда
		Возврат "ИСТИНА";
	Иначе
		Возврат СтрСоединить(СтрокиУсловия, Символы.ПС + Символы.Таб + "И ");
	КонецЕсли;
	
КонецФункции

// Возвращает фрагмент текста запроса с условием на поле Регистратор
//
// Параметры:
//  ПараметрыЗапроса - Структура - параметры запроса, в том числе:
//   * Регистратор - ДокументСсылка - ссылка на регистратор
//
// Возвращаемое значение:
//  Строка - фрагмент текста запроса
//
Функция СтрокаУсловияРегистратор(ПараметрыЗапроса) Экспорт
	
	Если ТипЗнч(ПараметрыЗапроса.Регистратор) = Тип("Массив") Тогда
		Возврат "&Регистратор В (&ПараметрЗапросаРегистратор)";
	ИначеЕсли ЗначениеЗаполнено(ПараметрыЗапроса.Регистратор) Тогда
		Возврат "&Регистратор = &ПараметрЗапросаРегистратор";
	Иначе
		Возврат "ИСТИНА";
	КонецЕсли;
	
КонецФункции

Функция СтрокаУсловияОрганизация(ПараметрыЗапроса) Экспорт
	
	Если ТипЗнч(ПараметрыЗапроса.Организация) = Тип("Массив") Тогда
		Возврат "&Организация В (&ПараметрЗапросаОрганизация)";
	ИначеЕсли ЗначениеЗаполнено(ПараметрыЗапроса.Организация) Тогда
		Возврат "&Организация = &ПараметрЗапросаОрганизация";
	Иначе
		Возврат "ИСТИНА";
	КонецЕсли;
	
КонецФункции

Функция СтрокаУсловияУдаляемыеДокументы(МетаданныеРегистра) Экспорт
	
	ДокументыМетаданные = Метаданные.Документы;
	СтрокиУсловия = Новый Массив();
	
	Для Каждого ИмяДокумента Из ИменаНеиспользуемыхДокументов() Цикл
		Если ДокументыМетаданные[ИмяДокумента].Движения.Содержит(МетаданныеРегистра) Тогда
			СтрокиУсловия.Добавить("НЕ &Регистратор ССЫЛКА Документ." + ИмяДокумента);
		КонецЕсли;
	КонецЦикла;
	
	Если СтрокиУсловия.Количество() = 0 Тогда
		Возврат "ИСТИНА";
	Иначе
		Возврат СтрСоединить(СтрокиУсловия, Символы.ПС + Символы.Таб + "И ");
	КонецЕсли;
	
КонецФункции

Функция СтрокаДополнительныйОтбор(ПараметрыОтраженияДвижений) Экспорт
	
	Если ЗначениеЗаполнено(ПараметрыОтраженияДвижений.ДополнительныйОтбор) Тогда
		Возврат ПараметрыОтраженияДвижений.ДополнительныйОтбор;
	Иначе
		Возврат "ИСТИНА";
	КонецЕсли;
	
КонецФункции

Функция СтрокаУсловияРеквизитСторно() Экспорт
	
	Возврат "&РеквизитСторно = ЛОЖЬ";
	
КонецФункции

Функция ПолноеИмяТипа(ЗначениеТипа) Экспорт
	
	Если ЗначениеТипа = Тип("Дата") Тогда
		Возврат "ДАТА";
	ИначеЕсли ЗначениеТипа = Тип("Число") Тогда
		Возврат "ЧИСЛО";
	ИначеЕсли ЗначениеТипа = Тип("Строка") Тогда
		Возврат "СТРОКА";
	Иначе
		ОбъектМетаданных = Метаданные.НайтиПоТипу(ЗначениеТипа);
		Если ОбъектМетаданных <> Неопределено Тогда
			Возврат ОбъектМетаданных.ПолноеИмя();
		Иначе
			Возврат "БУЛЕВО";
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

Функция ИмяРегистраСлужебныхОбъектовУчета() Экспорт
	
	Возврат "ДвиженияСлужебныхОбъектовУчета";
	
КонецФункции

#КонецОбласти

#Область ПолучениеАналитикиКорреспонденций

Функция ТекстВыборкиПолей(ДоступныеПоля, ИсключаемыеПоля, ДобавляемыеПоля = Неопределено, Постфикс = "", ПсевдонимИсточника = Неопределено) Экспорт
	
	Если ДобавляемыеПоля = Неопределено Тогда
		ДобавляемыеПоля = ДоступныеПоля;
	КонецЕсли;
	
	СтрокиВыборкиПолей = Новый Массив();
	
	Для Каждого ИмяПоля Из ДобавляемыеПоля Цикл
		ПсевдонимПоля = ИмяПоля + Постфикс;
		
		Если ИсключаемыеПоля.Найти(ИмяПоля) <> Неопределено Тогда
			Продолжить;
		ИначеЕсли НЕ ПустаяСтрока(Постфикс) И ИсключаемыеПоля.Найти(ПсевдонимПоля) <> Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Если ИмяПоля = "ТипРегистратора" Тогда
			ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника("Регистратор", ПсевдонимИсточника);
			СтрокиВыборкиПолей.Добавить("ТИПЗНАЧЕНИЯ(" + ПутьКДанным+ ") КАК " + ПсевдонимПоля);
		ИначеЕсли ИмяПоля = "НастройкаХозяйственнойОперации" Тогда
			ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника("НастройкаХозОперации", ПсевдонимИсточника);
			СтрокиВыборкиПолей.Добавить(ПутьКДанным + " КАК " + ПсевдонимПоля);
		ИначеЕсли ДоступныеПоля.Найти(ИмяПоля) <> Неопределено Тогда
			ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника(ИмяПоля, ПсевдонимИсточника);
			СтрокиВыборкиПолей.Добавить(ПутьКДанным + " КАК " + ПсевдонимПоля);
		Иначе
			СтрокиВыборкиПолей.Добавить(СтрокаВыборкиЗначения(Неопределено) + " КАК " + ПсевдонимПоля);
		КонецЕсли;
	КонецЦикла;
	
	РазделительСтрок = "," + Символы.ПС + Символы.Таб;
	
	Если СтрокиВыборкиПолей.Количество() > 0 Тогда
		ТекстВыборкиПолей = СтрСоединить(СтрокиВыборкиПолей, РазделительСтрок);
	Иначе
		ТекстВыборкиПолей = СтрокаВыборкиЗначения(Неопределено);
	КонецЕсли;
	
	Возврат ТекстВыборкиПолей;
	
КонецФункции

Процедура ЗаполнитьСтрокиВыборкиАналитики(ВыборкаПолей, ГруппировкаПолей, ДоступныеПоля, ИсключаемыеПоля, ДобавляемыеПоля = Неопределено) Экспорт
	
	Если ДобавляемыеПоля = Неопределено Тогда
		ДобавляемыеПоля = ДоступныеПоля;
		ПроверятьДоступностьПолей = Ложь;
	Иначе
		ПроверятьДоступностьПолей = Истина;
	КонецЕсли;
	
	Для Каждого ИмяПоля Из ДобавляемыеПоля Цикл
		Если ИсключаемыеПоля.Найти(ИмяПоля) <> Неопределено Тогда
			Продолжить;
		ИначеЕсли ПроверятьДоступностьПолей И ДоступныеПоля.Найти(ИмяПоля) = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника(ИмяПоля);
		ВыборкаПолей.Добавить(ПутьКДанным + " КАК " + ИмяПоля);
		ГруппировкаПолей.Добавить(ПутьКДанным);
	КонецЦикла;
	
КонецПроцедуры

Процедура ДобавитьВыборкуАналитикиВТекстЗапроса(ВыборкаПолей, ГруппировкаПолей, ТекстЗапроса) Экспорт

	РазделительСтрок = "," + Символы.ПС + Символы.Таб;
	
	Если ВыборкаПолей.Количество() > 0 Тогда
		ТекстПолейАналитики = СтрСоединить(ВыборкаПолей, РазделительСтрок);
	Иначе
		ТекстПолейАналитики = "//ПоляВыбораАналитики";
	КонецЕсли;
	
	Если ГруппировкаПолей.Количество() > 0 Тогда
		ТекстГруппыАналитики = СтрСоединить(ГруппировкаПолей, РазделительСтрок);
	Иначе
		ТекстГруппыАналитики = "//ГруппаПолейАналитики";
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ПоляВыбораАналитики", ТекстПолейАналитики);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ГруппаПолейАналитики", ТекстГруппыАналитики);
	
	Если СтрНайти(ТекстЗапроса, "&ПоляАналитикиВложеннойТаблицы") > 0 Тогда
		Разделитель = " КАК ";
		ДлинаРазделителя = СтрДлина(Разделитель);
		
		ВыборкаПолейВложеннойТаблицы = Новый Массив();
		ГруппировкаПолейВложеннойТаблицы = Новый Массив();
		
		Для Каждого СтрокаВыборкиПоля Из ВыборкаПолей Цикл
			ПозицияРазделителя = СтрНайти(СтрокаВыборкиПоля, Разделитель, НаправлениеПоиска.СКонца);
			ПсевдонимПоля = Сред(СтрокаВыборкиПоля, ПозицияРазделителя + ДлинаРазделителя);
			ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника(ПсевдонимПоля);
			ВыборкаПолейВложеннойТаблицы.Добавить(ПутьКДанным + " КАК " + ПсевдонимПоля);
			ГруппировкаПолейВложеннойТаблицы.Добавить(ПутьКДанным);
		КонецЦикла;
	
		Если ВыборкаПолей.Количество() > 0 Тогда
			ТекстПолейАналитики = СтрСоединить(ВыборкаПолейВложеннойТаблицы, РазделительСтрок);
			ТекстГруппыАналитики = СтрСоединить(ГруппировкаПолейВложеннойТаблицы, РазделительСтрок);
		Иначе
			ТекстПолейАналитики = "//ПоляАналитикиВложеннойТаблицы";
			ТекстГруппыАналитики = "//ГруппаАналитикиВложеннойТаблицы";
		КонецЕсли;
		
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ПоляАналитикиВложеннойТаблицы", ТекстПолейАналитики);
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ГруппаАналитикиВложеннойТаблицы", ТекстГруппыАналитики);
	КонецЕсли;
	
КонецПроцедуры

Процедура ДобавитьВыборкуРесурсовВТекстЗапроса(ВыборкаРесурсов, ТекстЗапроса) Экспорт
	
	РазделительСтрок = "," + Символы.ПС + Символы.Таб;
	
	Если ВыборкаРесурсов.Количество() > 0 Тогда
		ТекстРесурсовИсточника = СтрСоединить(ВыборкаРесурсов, РазделительСтрок);
	Иначе
		ТекстРесурсовИсточника = "//РесурсыИсточникаДанных";
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&РесурсыИсточникаДанных", ТекстРесурсовИсточника);
	
	Если СтрНайти(ТекстЗапроса, "&ПустыеРесурсыИсточникаДанных") > 0
	 ИЛИ СтрНайти(ТекстЗапроса, "&РесурсыВложенногоИсточникаДанных") > 0
	 ИЛИ СтрНайти(ТекстЗапроса, "&СуммыРесурсовВложенногоИсточникаДанных") > 0 Тогда
		Разделитель = " КАК ";
		ДлинаРазделителя = СтрДлина(Разделитель);
		
		ВыборкаПустыхРесурсовИсточника = Новый Массив();
		ВыборкаРесурсовВложенногоИсточника = Новый Массив();
		ВыборкаСуммРесурсовВложенногоИсточника = Новый Массив();
		
		Для Каждого СтрокаВыборкиРесурса Из ВыборкаРесурсов Цикл
			ПозицияРазделителя = СтрНайти(СтрокаВыборкиРесурса, Разделитель, НаправлениеПоиска.СКонца);
			ПсевдонимРесурса = Сред(СтрокаВыборкиРесурса, ПозицияРазделителя + ДлинаРазделителя);
			ПутьКДанным = ПутьКДаннымСПсевдонимомИсточника(ПсевдонимРесурса);
			ВыборкаПустыхРесурсовИсточника.Добавить("0 КАК " + ПсевдонимРесурса);
			ВыборкаРесурсовВложенногоИсточника.Добавить(ПутьКДанным + " КАК " + ПсевдонимРесурса);
			ВыборкаСуммРесурсовВложенногоИсточника.Добавить(СтрШаблон("СУММА(%1) КАК %2", ПутьКДанным, ПсевдонимРесурса));
		КонецЦикла;
	
		Если ВыборкаРесурсов.Количество() > 0 Тогда
			ТекстПустыхРесурсов = СтрСоединить(ВыборкаПустыхРесурсовИсточника, РазделительСтрок);
			ТекстРесурсовИсточника = СтрСоединить(ВыборкаРесурсовВложенногоИсточника, РазделительСтрок);
			ТекстСуммРесурсовИсточника = СтрСоединить(ВыборкаСуммРесурсовВложенногоИсточника, РазделительСтрок);
		Иначе
			ТекстПустыхРесурсов = "//ПустыеРесурсыИсточникаДанных";
			ТекстРесурсовИсточника = "//РесурсыВложенногоИсточникаДанных";
			ТекстСуммРесурсовИсточника = "//СуммыРесурсовВложенногоИсточникаДанных";
		КонецЕсли;
		
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ПустыеРесурсыИсточникаДанных", ТекстПустыхРесурсов);
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&РесурсыВложенногоИсточникаДанных", ТекстРесурсовИсточника);
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&СуммыРесурсовВложенногоИсточникаДанных", ТекстСуммРесурсовИсточника);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОтборКорреспонденцийПоНастройкамСхемыКомпоновкиДанных

Функция НоваяСхемаКомпоновкиДанных(ТекстЗапроса) Экспорт
	
	СхемаКомпоновкиДанных = КомпоновкаДанныхСервер.ПустаяСхема();
	НаборДанных = КомпоновкаДанныхСервер.ДобавитьПустойНаборДанных(СхемаКомпоновкиДанных);
	НаборДанных.АвтоЗаполнениеДоступныхПолей = Истина;
	НаборДанных.Запрос = ТекстЗапроса;
	
	Возврат СхемаКомпоновкиДанных;
	
КонецФункции

Функция НовыйКомпоновщикНастроекКомпоновкиДанных(АдресСхемыКомпоновкиДанных, ПоляВыбораКомпоновкиДанных, ПараметрыЗапроса) Экспорт
	
	КомпоновщикНастроек = Новый КомпоновщикНастроекКомпоновкиДанных();
	КомпоновщикНастроек.Инициализировать(Новый ИсточникДоступныхНастроекКомпоновкиДанных(АдресСхемыКомпоновкиДанных));
	
	Для Каждого ПолеВыбора Из ПоляВыбораКомпоновкиДанных Цикл
		ВыбранноеПоле = КомпоновщикНастроек.Настройки.Выбор.Элементы.Добавить(Тип("ВыбранноеПолеКомпоновкиДанных"));
		ВыбранноеПоле.Поле = Новый ПолеКомпоновкиДанных(ПолеВыбора.Ключ);
	КонецЦикла;
	
	Для Каждого ДоступныйПараметр Из КомпоновщикНастроек.Настройки.ПараметрыДанных.ДоступныеПараметры.Элементы Цикл
		Параметр = ДоступныйПараметр.Параметр;
		КомпоновщикНастроек.Настройки.ПараметрыДанных.УстановитьЗначениеПараметра(Параметр, ПараметрыЗапроса[Строка(Параметр)]);
	КонецЦикла;
	
	ДетальныеЗаписи = КомпоновщикНастроек.Настройки.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
	ДетальныеЗаписи.Выбор.Элементы.Добавить(Тип("АвтоВыбранноеПолеКомпоновкиДанных"));
	
	Возврат КомпоновщикНастроек;
	
КонецФункции

Процедура ДобавитьРезультатКомпоновкиВТаблицу(СхемаКомпоновкиДанных, КомпоновщикНастроек, МенеджерВременныхТаблиц, ТаблицаРезультатовКомпоновки) Экспорт
	
	КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных();
	МакетКомпоновки = КомпоновщикМакета.Выполнить(СхемаКомпоновкиДанных, КомпоновщикНастроек.Настройки,,, Тип("ГенераторМакетаКомпоновкиДанныхДляКоллекцииЗначений"));
	
	ПроцессорКомпоновкиДанных = Новый ПроцессорКомпоновкиДанных();
	ПроцессорКомпоновкиДанных.Инициализировать(МакетКомпоновки,,, Истина,, МенеджерВременныхТаблиц);
	
	ПроцессорВыводаВТаблицуЗначений = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВКоллекциюЗначений();
	ТаблицаРезультатаКомпоновки = ПроцессорВыводаВТаблицуЗначений.Вывести(ПроцессорКомпоновкиДанных, Ложь);
	
	Для Каждого СтрокаРезультата Из ТаблицаРезультатаКомпоновки Цикл
		ЗаполнитьЗначенияСвойств(ТаблицаРезультатовКомпоновки.Добавить(), СтрокаРезультата);
	КонецЦикла;
	
КонецПроцедуры


// Проверяет наличие в переданном отборе используемыз полей.
// Возможен отбор полей верхнего уровня по значению постфикса.
// 
// Параметры:
//  ОтборИсточник - ОтборКомпоновкиДанных - отбор, в котором производится проверка наличия используемых полей.
//  Постфикс - Строка - постфикс, по значению которого производится отбор полей верхнего уровня.
// 
// Возвращаемое значение:
//  Булево - признак наличия используемых полей
//
Функция ЕстьЭлементыОтбора(ОтборИсточник, Постфикс = "") Экспорт
	
	ЕстьЭлементыОтбора = Ложь;
	
	Для Каждого ЭлементОтбора Из ОтборИсточник.Элементы Цикл
		Если НЕ ЭлементОтбора.Использование Тогда
			Продолжить;
		КонецЕсли;
		
		Если ТипЗнч(ЭлементОтбора) = Тип("ЭлементОтбораКомпоновкиДанных") Тогда
			ПутьКДанным = Строка(ЭлементОтбора.ЛевоеЗначение);
			ПолеОтбора = СтрРазделить(ПутьКДанным, ".")[0];
			
			Если НЕ ПустаяСтрока(Постфикс) Тогда
				ЕстьЭлементыОтбора = СтрЗаканчиваетсяНа(ПолеОтбора, Постфикс);
			Иначе
				ЕстьЭлементыОтбора = Истина;
			КонецЕсли;
		Иначе
			ЕстьЭлементыОтбора = ЕстьЭлементыОтбора(ЭлементОтбора, Постфикс);
		КонецЕсли;
		
		Если ЕстьЭлементыОтбора Тогда
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Возврат ЕстьЭлементыОтбора;
	
КонецФункции

// Добавляет в переданный массив поля верхнего уровня из переданного отбора.
// Возможен отбор полей по значению постфикса.
// Постфикс исключается из имен полей.
// 
// Параметры:
//  ПоляОтбора - Массив из Строка - заполняемый массив.
//  ОтборИсточник - ОтборКомпоновкиДанных - отбор, полями верхнего уровня которого заполняется массив.
//  Постфикс - Строка - постфикс, по значению которого производится отбор полей верхнего уровня.
//
Процедура ДобавитьПоляЭлементовОтбора(ПоляОтбора, ОтборИсточник, Постфикс = "") Экспорт
	
	Для Каждого ЭлементОтбора Из ОтборИсточник.Элементы Цикл
		Если НЕ ЭлементОтбора.Использование Тогда
			Продолжить;
		КонецЕсли;
		
		Если ТипЗнч(ЭлементОтбора) = Тип("ЭлементОтбораКомпоновкиДанных") Тогда
			ПутьКДанным = Строка(ЭлементОтбора.ЛевоеЗначение);
			ПолеОтбора = СтрРазделить(ПутьКДанным, ".")[0];
			
			Если НЕ ПустаяСтрока(Постфикс) Тогда
				Если СтрЗаканчиваетсяНа(ПолеОтбора, Постфикс) Тогда
					ПолеОтбора = Сред(ПолеОтбора, 1, СтрДлина(ПолеОтбора) - СтрДлина(Постфикс));
				Иначе
					Продолжить;
				КонецЕсли;
			КонецЕсли;
			
			Если ПоляОтбора.Найти(ПолеОтбора) = Неопределено Тогда
				ПоляОтбора.Добавить(ПолеОтбора);
			КонецЕсли;
		Иначе
			ДобавитьПоляЭлементовОтбора(ПоляОтбора, ЭлементОтбора, Постфикс);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ФормированиеПроводок

Функция ПроверкаРавенстваБалансовыхРесурсов(ВыборкаДвижения, СтрокиАналитики, ПараметрыЗапроса, ДтКт) Экспорт
	
	Если СтрокиАналитики.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ИмяРегистра = ВыборкаДвижения["ИмяРегистра" + ДтКт];
	
	Если ИмяРегистра = ИмяРегистраСлужебныхОбъектовУчета() Тогда
		Возврат Истина;
	КонецЕсли;
	
	СуммаУпр = 0;
	СуммаРегл = 0;
	
	Для Каждого СтрокаАналитики Из СтрокиАналитики Цикл
		СуммаУпр = СуммаУпр + СтрокаАналитики.СуммаУпр;
		СуммаРегл = СуммаРегл + СтрокаАналитики.СуммаРегл;
	КонецЦикла;
	
	Если ПараметрыЗапроса.РегистрыБезСуммыУпр.Найти(ИмяРегистра) <> Неопределено Тогда
		СуммаДвиженияУпрРавнаСуммеУпрАналитики = Истина;
	Иначе
		СуммаДвиженияУпрРавнаСуммеУпрАналитики = ВыборкаДвижения["СуммаДвиженияУпр" + ДтКт] = СуммаУпр;
	КонецЕсли;
	
	Если ПараметрыЗапроса.РегистрыБезСуммыРегл.Найти(ИмяРегистра) <> Неопределено Тогда
		СуммаДвиженияРеглРавнаСуммеРеглАналитики = Истина;
	Иначе
		СуммаДвиженияРеглРавнаСуммеРеглАналитики = ВыборкаДвижения["СуммаДвиженияРегл" + ДтКт] = СуммаРегл;
	КонецЕсли;
	
	Возврат СуммаДвиженияУпрРавнаСуммеУпрАналитики И СуммаДвиженияРеглРавнаСуммеРеглАналитики;
	
КонецФункции

Функция ВалютыОперативногоУчета(Организация) Экспорт
	
	ВалютыОперативногоУчета = Новый Структура();
	ВалютыОперативногоУчета.Вставить("ВалютаУпр", ЗначениеНастроекПовтИсп.ВалютаУправленческогоУчета());
	ВалютыОперативногоУчета.Вставить("ВалютаРегл", ЗначениеНастроекПовтИсп.ВалютаРегламентированногоУчетаОрганизации(Организация));
	
	Возврат ВалютыОперативногоУчета;
	
КонецФункции

Функция ПересчитанныеРесурсыМногихАналитик(ВыборкаДвижения, МногоАналитик, ПараметрыФормированияПроводок) Экспорт
	
	ОписаниеСуммы = ОбщегоНазначения.ОписаниеТипаЧисло(ПараметрыФормированияПроводок.ДлинаСуммы, ПараметрыФормированияПроводок.ТочностьСуммы);
	ОписаниеКоличества = ОбщегоНазначения.ОписаниеТипаЧисло(ПараметрыФормированияПроводок.ДлинаКоличества, ПараметрыФормированияПроводок.ТочностьКоличества);
	
	ТаблицаРесурсов = Новый ТаблицаЗначений();
	ТаблицаРесурсов.Колонки.Добавить("СуммаУпр", ОписаниеСуммы);
	ТаблицаРесурсов.Колонки.Добавить("СуммаРегл", ОписаниеСуммы);
	ТаблицаРесурсов.Колонки.Добавить("Валюта", Новый ОписаниеТипов("СправочникСсылка.Валюты"));
	ТаблицаРесурсов.Колонки.Добавить("СуммаВал", ОписаниеСуммы);
	ТаблицаРесурсов.Колонки.Добавить("Количество", ОписаниеКоличества);
	
	ПересчитатьБаланс = ВыборкаДвижения["СложнаяПроводка" + МногоАналитик.ДтКт];
	ПересчитатьСуммыВал = ПересчитатьБаланс
		И ВыборкаДвижения["Валютный" + МногоАналитик.ДтКт]
		И ВыборкаДвижения["СуммаВал" + МногоАналитик.ДтКт] <> 0;
	ПересчитатьКоличества = ПересчитатьБаланс
		И ВыборкаДвижения["Количественный" + МногоАналитик.ДтКт]
		И ВыборкаДвижения["Количество" + МногоАналитик.ДтКт] <> 0;
	
	СуммыУпр = Новый Массив();
	СуммыРегл = Новый Массив();
	СуммыВал = Новый Массив();
	Количества = Новый Массив();
	
	Для Каждого СтрокаАналитики Из МногоАналитик.СтрокиАналитики Цикл
		НоваяСтрока = ТаблицаРесурсов.Добавить();
		НоваяСтрока.Валюта = ВыборкаДвижения["Валюта" + МногоАналитик.ДтКт];
		
		Если ПересчитатьБаланс Тогда
			СуммыУпр.Добавить(СтрокаАналитики.СуммаУпр);
			СуммыРегл.Добавить(СтрокаАналитики.СуммаРегл);
			Если ПересчитатьСуммыВал Тогда
				СуммыВал.Добавить(СтрокаАналитики.СуммаВал);
			КонецЕсли;
			Если ПересчитатьКоличества Тогда
				Количества.Добавить(СтрокаАналитики.Количество);
			КонецЕсли;
		Иначе
			ЗаполнитьЗначенияСвойств(НоваяСтрока, СтрокаАналитики, "СуммаУпр,СуммаРегл,СуммаВал,Количество");
		КонецЕсли;
	КонецЦикла;
	
	Если ПересчитатьБаланс Тогда
		РаспределениеСуммыУпр = ОбщегоНазначенияКлиентСервер.РаспределитьСуммуПропорциональноКоэффициентам(
			ВыборкаДвижения.СуммаУпр, СуммыУпр, ПараметрыФормированияПроводок.ТочностьСуммы);
		Если РаспределениеСуммыУпр <> Неопределено Тогда
			ТаблицаРесурсов.ЗагрузитьКолонку(РаспределениеСуммыУпр, "СуммаУпр");
		КонецЕсли;
		
		РаспределениеСуммыРегл = ОбщегоНазначенияКлиентСервер.РаспределитьСуммуПропорциональноКоэффициентам(
			ВыборкаДвижения.СуммаРегл, СуммыРегл, ПараметрыФормированияПроводок.ТочностьСуммы);
		Если РаспределениеСуммыРегл <> Неопределено Тогда
			ТаблицаРесурсов.ЗагрузитьКолонку(РаспределениеСуммыРегл, "СуммаРегл");
		КонецЕсли;
		
		Если ПересчитатьСуммыВал Тогда
			РаспределениеСуммыВал = ОбщегоНазначенияКлиентСервер.РаспределитьСуммуПропорциональноКоэффициентам(
				ВыборкаДвижения["СуммаВал" + МногоАналитик.ДтКт], СуммыВал, ПараметрыФормированияПроводок.ТочностьСуммы);
			Если РаспределениеСуммыВал <> Неопределено Тогда
				ТаблицаРесурсов.ЗагрузитьКолонку(РаспределениеСуммыВал, "СуммаВал");
			КонецЕсли;
		КонецЕсли;
		
		Если ПересчитатьКоличества Тогда
			РаспределениеКоличества = ОбщегоНазначенияКлиентСервер.РаспределитьСуммуПропорциональноКоэффициентам(
				ВыборкаДвижения["Количество" + МногоАналитик.ДтКт], Количества, ПараметрыФормированияПроводок.ТочностьКоличества);
			Если РаспределениеКоличества <> Неопределено Тогда
				ТаблицаРесурсов.ЗагрузитьКолонку(РаспределениеКоличества, "Количество");
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ТаблицаРесурсов;
	
КонецФункции

Функция РаспределенныеРесурсыОднойАналитики(ВыборкаДвижения, ОднаАналитика, РесурсыМногихАналитик, ПараметрыФормированияПроводок) Экспорт
	
	ОписаниеСуммы = ОбщегоНазначения.ОписаниеТипаЧисло(ПараметрыФормированияПроводок.ДлинаСуммы, ПараметрыФормированияПроводок.ТочностьСуммы);
	ОписаниеКоличества = ОбщегоНазначения.ОписаниеТипаЧисло(ПараметрыФормированияПроводок.ДлинаКоличества, ПараметрыФормированияПроводок.ТочностьКоличества);
	
	ТаблицаРесурсов = Новый ТаблицаЗначений();
	ТаблицаРесурсов.Колонки.Добавить("Валюта", Новый ОписаниеТипов("СправочникСсылка.Валюты"));
	ТаблицаРесурсов.Колонки.Добавить("СуммаВал", ОписаниеСуммы);
	ТаблицаРесурсов.Колонки.Добавить("Количество", ОписаниеКоличества);
	
	РаспределитьСуммуВал = ВыборкаДвижения["Валютный" + ОднаАналитика.ДтКт]
		И ВыборкаДвижения["СуммаВал" + ОднаАналитика.ДтКт] <> 0;
	РаспределитьКоличество = ВыборкаДвижения["Количественный" + ОднаАналитика.ДтКт]
		И ВыборкаДвижения["Количество" + ОднаАналитика.ДтКт] <> 0;
	
	СуммыУпр = Новый Массив();
	СуммыРегл = Новый Массив();
	
	ВсеСуммыУпрРавныНулю = Истина;
	ВсеСуммыУпрНеРавныНулю = Истина;
	ВсеСуммыРеглРавныНулю = Истина;
	ВсеСуммыРеглНеРавныНулю = Истина;
	
	Для Каждого СтрокаРесурсовМногих Из РесурсыМногихАналитик Цикл
		НоваяСтрока = ТаблицаРесурсов.Добавить();
		НоваяСтрока.Валюта = ВыборкаДвижения["Валюта" + ОднаАналитика.ДтКт];
		
		Если РаспределитьСуммуВал ИЛИ РаспределитьКоличество Тогда
			СуммыУпр.Добавить(СтрокаРесурсовМногих.СуммаУпр);
			СуммыРегл.Добавить(СтрокаРесурсовМногих.СуммаРегл);
			Если СтрокаРесурсовМногих.СуммаУпр = 0 Тогда
				ВсеСуммыУпрНеРавныНулю = Ложь;
			Иначе
				ВсеСуммыУпрРавныНулю = Ложь;
			КонецЕсли;
			Если СтрокаРесурсовМногих.СуммаРегл = 0 Тогда
				ВсеСуммыРеглНеРавныНулю = Ложь;
			Иначе
				ВсеСуммыРеглРавныНулю = Ложь;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Если ВсеСуммыУпрНеРавныНулю Тогда
		Коэффициенты = СуммыУпр;
	ИначеЕсли ВсеСуммыРеглНеРавныНулю Тогда
		Коэффициенты = СуммыРегл;
	ИначеЕсли НЕ ВсеСуммыУпрРавныНулю Тогда
		Коэффициенты = СуммыУпр;
	ИначеЕсли НЕ ВсеСуммыРеглРавныНулю Тогда
		Коэффициенты = СуммыРегл;
	Иначе
		Возврат ТаблицаРесурсов;
	КонецЕсли;
	
	Если РаспределитьСуммуВал Тогда
		РаспределениеСуммыВал = ОбщегоНазначенияКлиентСервер.РаспределитьСуммуПропорциональноКоэффициентам(
			ВыборкаДвижения["СуммаВал" + ОднаАналитика.ДтКт], Коэффициенты, ПараметрыФормированияПроводок.ТочностьСуммы);
		Если РаспределениеСуммыВал <> Неопределено Тогда
			ТаблицаРесурсов.ЗагрузитьКолонку(РаспределениеСуммыВал, "СуммаВал");
		КонецЕсли;
	КонецЕсли;
	
	Если РаспределитьКоличество Тогда
		РаспределениеКоличества = ОбщегоНазначенияКлиентСервер.РаспределитьСуммуПропорциональноКоэффициентам(
			ВыборкаДвижения["Количество" + ОднаАналитика.ДтКт], Коэффициенты, ПараметрыФормированияПроводок.ТочностьКоличества);
		Если РаспределениеКоличества <> Неопределено Тогда
			ТаблицаРесурсов.ЗагрузитьКолонку(РаспределениеКоличества, "Количество");
		КонецЕсли;
	КонецЕсли;
	
	Возврат ТаблицаРесурсов;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область УправлениеВыполнениемОффлайновыхРасчетов

Процедура РассчитатьСуммыДокументовВВалютахУчета(ДатаОкончания, МассивДокументов, ДополнительныеПараметры) Экспорт
	
	Если НЕ ПривилегированныйРежим() Тогда
		УстановитьПривилегированныйРежим(Истина);
	КонецЕсли;
	
	РегистрыСведений.СуммыДокументовВВалютахУчета.РассчитатьСуммыДокументовВВалютахУчета(МассивДокументов);
	
	Если ДополнительныеПараметры.ПересчитатьРегистрыОборотов Тогда
		УправленческийУчетПроведениеСервер.ОбновитьДвиженияПоОборотнымРегистрам(МассивДокументов);
	КонецЕсли;
	
КонецПроцедуры
	
//++ НЕ УТ

Процедура СформироватьДвиженияПоВНА(ДатаОкончания, МассивДокументов, ДополнительныеПараметры) Экспорт
	
	Если НЕ ПривилегированныйРежим() Тогда
		УстановитьПривилегированныйРежим(Истина);
	КонецЕсли;
	
	ОтложенноеФормированиеДвиженийВНА.ВыполнитьОперациюПоДокументам(МассивДокументов);
	РасчетСтоимостиВНА.ВыполнитьОперациюПоДокументам(МассивДокументов);
	ЗакрытиеРасходовОтВыбытияОС.ВыполнитьОперациюПоДокументам(МассивДокументов);
	
КонецПроцедуры
	
//-- НЕ УТ

Процедура ОтразитьДокументыВУчетеНДС(ДатаОкончания, МассивДокументов, ДополнительныеПараметры) Экспорт
	
	Если НЕ ПривилегированныйРежим() Тогда
		УстановитьПривилегированныйРежим(Истина);
	КонецЕсли;
	
	ПериодРасчетаНДС = КонецМесяца(ДатаОкончания);
	УчетНДСУП.ОтразитьДокументыВУчетеНДС(ПериодРасчетаНДС, МассивДокументов);
	
КонецПроцедуры

Процедура ВыполнитьОффлайновыйРасчет(ПараметрыВыполнения, ПроцедураРасчета)
	
	ДокументовНаПорцию = 1000;
	КоличествоДокументов = ПараметрыВыполнения.ДокументыКРасчету.Количество();
	
	Если НЕ ПроцедураРасчета.МногопоточноеВыполнение Тогда
		КоличествоПотоков = 1;
	ИначеЕсли КоличествоДокументов <= ДокументовНаПорцию Тогда
		КоличествоПотоков = 1;
	Иначе
		КоличествоПотоков = ПараметрыВыполнения.КоличествоПотоков;
	КонецЕсли;
	
	Если КоличествоПотоков = 1 Тогда
		ВыполнитьОффлайновыйРасчетТекущимПотоком(ПараметрыВыполнения, ПроцедураРасчета);
	Иначе
		ТаблицаУправленияПотоками = Новый ТаблицаЗначений();
		ТаблицаУправленияПотоками.Колонки.Добавить("ИмяМетода", ОбщегоНазначения.ОписаниеТипаСтрока(250));
		ТаблицаУправленияПотоками.Колонки.Добавить("ДатаОкончания", ОбщегоНазначения.ОписаниеТипаДата(ЧастиДаты.ДатаВремя));
		ТаблицаУправленияПотоками.Колонки.Добавить("ДокументыКРасчету", Новый ОписаниеТипов("Массив"));
		ТаблицаУправленияПотоками.Колонки.Добавить("ДополнительныеПараметры", Новый ОписаниеТипов("Структура"));
		ТаблицаУправленияПотоками.Колонки.Добавить("Идентификатор", Новый ОписаниеТипов("УникальныйИдентификатор"));
		ТаблицаУправленияПотоками.Колонки.Добавить("КоличествоПопыток", ОбщегоНазначения.ОписаниеТипаЧисло(3, 0));
		
		КоличествоПорций = Цел(КоличествоДокументов / ДокументовНаПорцию) + 1;
		
		Для НомерПорции = 1 По КоличествоПорций Цикл
			ПорцияДокументов = Новый Массив();
			
			Для НомерДокумента = 1 По ДокументовНаПорцию Цикл
				ИндексДокумента = (НомерПорции - 1) * ДокументовНаПорцию + НомерДокумента - 1;
				Если ИндексДокумента < КоличествоДокументов Тогда
					ПорцияДокументов.Добавить(ПараметрыВыполнения.ДокументыКРасчету[ИндексДокумента]);
				Иначе
					Прервать;
				КонецЕсли;
			КонецЦикла;
			
			Если ПорцияДокументов.Количество() > 0 Тогда
				ПараметрыПотока = ТаблицаУправленияПотоками.Добавить();
				ПараметрыПотока.ИмяМетода = ПроцедураРасчета.ИмяМетода;
				ПараметрыПотока.ДатаОкончания = ПараметрыВыполнения.ДатаОкончания;
				ПараметрыПотока.ДокументыКРасчету = ПорцияДокументов;
				ПараметрыПотока.ДополнительныеПараметры = ПроцедураРасчета.ДополнительныеПараметры;
				
				НачатьВыполнениеОффлайновогоРасчетаНовымПотоком(ПараметрыПотока);
				
				Если ТаблицаУправленияПотоками.Количество() >= КоличествоПотоков Тогда
					ОжидатьЗавершенияОффлайновогоРасчета(ТаблицаУправленияПотоками, Ложь);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		Если ТаблицаУправленияПотоками.Количество() > 0 Тогда
			ОжидатьЗавершенияОффлайновогоРасчета(ТаблицаУправленияПотоками, Истина);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура ВыполнитьОффлайновыйРасчетТекущимПотоком(ПараметрыРасчета, ПроцедураРасчета)
	
	ОбщийМодуль = "ФинансовыйУчетПоДаннымБалансовыхРегистров";
	ПолноеИмяМетода = ОбщийМодуль + "." + ПроцедураРасчета.ИмяМетода;
	
	ПараметрыВыполнения = Новый Массив();
	ПараметрыВыполнения.Добавить(ПараметрыРасчета.ДатаОкончания);
	ПараметрыВыполнения.Добавить(ПараметрыРасчета.ДокументыКРасчету);
	ПараметрыВыполнения.Добавить(ПроцедураРасчета.ДополнительныеПараметры);
	
	ОбщегоНазначения.ВыполнитьМетодКонфигурации(ПолноеИмяМетода, ПараметрыВыполнения);
	
КонецПроцедуры

Процедура НачатьВыполнениеОффлайновогоРасчетаНовымПотоком(ПараметрыПотока)
	
	ОбщийМодуль = "ФинансовыйУчетПоДаннымБалансовыхРегистров";
	ПолноеИмяМетода = ОбщийМодуль + "." + ПараметрыПотока.ИмяМетода;
	
	Наименование = НСтр("ru = 'Доформирование движений документов в регистрах финансового учета';
						|en = 'Finalize the generation of document register records in financial accounting registers'");
	
	ПараметрыВыполнения = Новый Массив();
	ПараметрыВыполнения.Добавить(ПараметрыПотока.ДатаОкончания);
	ПараметрыВыполнения.Добавить(ПараметрыПотока.ДокументыКРасчету);
	ПараметрыВыполнения.Добавить(ПараметрыПотока.ДополнительныеПараметры);
	
	НовоеЗадание = ФоновыеЗадания.Выполнить(ПолноеИмяМетода, ПараметрыВыполнения,, Наименование);
	ПараметрыПотока.Идентификатор = НовоеЗадание.УникальныйИдентификатор;
	ПараметрыПотока.КоличествоПопыток = ПараметрыПотока.КоличествоПопыток + 1;
	
КонецПроцедуры

Процедура ОжидатьЗавершенияОффлайновогоРасчета(ТаблицаУправленияПотоками, ЗавершениеВсехПотоков)
	
	ВремяОжидания = 60;
	КоличествоПопыток = 3;
	
	АктивныеЗадания = Новый Массив();
	АварийныеЗадания = Новый Массив();
	ЗавершенныеЗадания = Новый Массив();
	
	Для Каждого ПараметрыПотока Из ТаблицаУправленияПотоками Цикл
		ФоновоеЗадание = ФоновыеЗадания.НайтиПоУникальномуИдентификатору(ПараметрыПотока.Идентификатор);
		Если ФоновоеЗадание = Неопределено Тогда
			АварийныеЗадания.Добавить(ПараметрыПотока.Идентификатор);
		ИначеЕсли ФоновоеЗадание.Состояние = СостояниеФоновогоЗадания.Активно Тогда
			АктивныеЗадания.Добавить(ФоновоеЗадание);
		ИначеЕсли ФоновоеЗадание.Состояние = СостояниеФоновогоЗадания.Завершено Тогда
			ЗавершенныеЗадания.Добавить(ПараметрыПотока.Идентификатор);
		Иначе
			АварийныеЗадания.Добавить(ПараметрыПотока.Идентификатор);
		КонецЕсли;
	КонецЦикла;
	
	Пока (ЗавершенныеЗадания.Количество() = 0 ИЛИ ЗавершениеВсехПотоков) И АктивныеЗадания.Количество() > 0 Цикл
		АктивныеЗадания = ФоновыеЗадания.ОжидатьЗавершенияВыполнения(АктивныеЗадания, ВремяОжидания);
		КоличествоЗаданий = АктивныеЗадания.Количество();
		
		Для НомерЗадания = 1 По КоличествоЗаданий Цикл
			ОбратныйИндекс = КоличествоЗаданий - НомерЗадания;
			ФоновоеЗадание = АктивныеЗадания[ОбратныйИндекс];
			
			Если ФоновоеЗадание.Состояние <> СостояниеФоновогоЗадания.Активно Тогда
				Если ФоновоеЗадание.Состояние = СостояниеФоновогоЗадания.Завершено Тогда
					ЗавершенныеЗадания.Добавить(ФоновоеЗадание.УникальныйИдентификатор);
				Иначе
					АварийныеЗадания.Добавить(ФоновоеЗадание.УникальныйИдентификатор);
				КонецЕсли;
				АктивныеЗадания.Удалить(ОбратныйИндекс);
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	Для Каждого ЗавершенноеЗадание Из ЗавершенныеЗадания Цикл
		ПараметрыПотока = ТаблицаУправленияПотоками.Найти(ЗавершенноеЗадание, "Идентификатор");
		Если ПараметрыПотока <> Неопределено Тогда
			ТаблицаУправленияПотоками.Удалить(ПараметрыПотока);
		КонецЕсли;
	КонецЦикла;
	
	Если АварийныеЗадания.Количество() > 0 Тогда
		ТекстыСообщений = Новый Массив();
		
		Для Каждого АварийноеЗадание Из АварийныеЗадания Цикл
			ПараметрыПотока = ТаблицаУправленияПотоками.Найти(АварийноеЗадание, "Идентификатор");
			
			Если ПараметрыПотока = Неопределено Тогда
			ИначеЕсли ПараметрыПотока.КоличествоПопыток < КоличествоПопыток Тогда
				НачатьВыполнениеОффлайновогоРасчетаНовымПотоком(ПараметрыПотока);
			Иначе
				ФоновоеЗадание = ФоновыеЗадания.НайтиПоУникальномуИдентификатору(АварийноеЗадание);
				Если ФоновоеЗадание = Неопределено Тогда
					ТекстСообщения = СтрШаблон(НСтр("ru = 'Фоновое задание с идентификатором %1 не найдено.';
													|en = 'A background job with the %1 ID is not found.'"), АварийноеЗадание);
				ИначеЕсли ФоновоеЗадание.Состояние = СостояниеФоновогоЗадания.Отменено Тогда
					ТекстСообщения = СтрШаблон(НСтр("ru = 'Фоновое задание с идентификатором %1 отменено администратором.';
													|en = 'The background job with the %1 ID is canceled by the administrator.'"), АварийноеЗадание);
				ИначеЕсли ФоновоеЗадание.Состояние = СостояниеФоновогоЗадания.ЗавершеноАварийно Тогда
					ТекстСообщения = СтрШаблон(НСтр("ru = 'Фоновое задание с идентификатором %1 завершено аварийно.';
													|en = 'The background job with the %1 ID crashed.'"), АварийноеЗадание)
						+ Символы.ПС + КраткоеПредставлениеОшибки(ФоновоеЗадание.ИнформацияОбОшибке);
				Иначе
					ТекстСообщения = СтрШаблон(НСтр("ru = 'Фоновое задание с идентификатором %1 не выполнено.';
													|en = 'The background job with the %1 ID is not performed.'"), АварийноеЗадание);
				КонецЕсли;
				
				ТекстыСообщений.Добавить(ТекстСообщения);
				
				Если ФоновоеЗадание = Неопределено ИЛИ ФоновоеЗадание.ИнформацияОбОшибке = Неопределено Тогда
					ПредставлениеОшибки = ТекстСообщения;
				Иначе
					ПредставлениеОшибки = СтрШаблон(НСтр("ru = 'Фоновое задание с идентификатором %1 завершено аварийно.';
														|en = 'The background job with the %1 ID crashed.'"), АварийноеЗадание)
						+ Символы.ПС + ПодробноеПредставлениеОшибки(ФоновоеЗадание.ИнформацияОбОшибке);
				КонецЕсли;
				
				ЗаписьЖурналаРегистрации(
					НСтр("ru = 'Доформирование движений документов в регистрах финансового учета';
						|en = 'Finalize the generation of document register records in financial accounting registers'", ОбщегоНазначения.КодОсновногоЯзыка()),
					УровеньЖурналаРегистрации.Ошибка,,,
					ПредставлениеОшибки);
			КонецЕсли;
		КонецЦикла;
		
		Если ТекстыСообщений.Количество() > 0 Тогда
			ТекстыСообщений.Вставить(0, НСтр("ru = 'Доформирование движений документов в регистрах финансового учета завершилось с ошибкой.';
											|en = 'Errors occurred when finalizing the generation of document register records in financial accounting registers.'"));
			ТекстСообщения = СтрСоединить(ОбщегоНазначенияКлиентСервер.СвернутьМассив(ТекстыСообщений), Символы.ПС);
			ВызватьИсключение ТекстСообщения;
		ИначеЕсли ЗавершенныеЗадания.Количество() = 0 ИЛИ ЗавершениеВсехПотоков Тогда
			ОжидатьЗавершенияОффлайновогоРасчета(ТаблицаУправленияПотоками, ЗавершениеВсехПотоков);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ФормированиеПараметровЗапроса

Функция ХешСуммаИсточниковДанных(ИсточникиДанных, ТипХешФункции)
	
	ТаблицаИсточников = Новый ТаблицаЗначений();
	ТаблицаИсточников.Колонки.Добавить("ИсточникДанных", ОбщегоНазначения.ОписаниеТипаСтрока(150));
	
	Для Каждого ИсточникДанных Из ИсточникиДанных Цикл
		НоваяСтрока = ТаблицаИсточников.Добавить();
		НоваяСтрока.ИсточникДанных = ИсточникДанных;
	КонецЦикла;
	
	ТаблицаИсточников.Свернуть("ИсточникДанных");
	ТаблицаИсточников.Сортировать("ИсточникДанных");
	
	ХешированиеДанных = Новый ХешированиеДанных(ТипХешФункции);
	ХешированиеДанных.Добавить(СтрСоединить(ТаблицаИсточников.ВыгрузитьКолонку("ИсточникДанных")));
	
	Возврат Base64Строка(ХешированиеДанных.ХешСумма);
	
КонецФункции

#КонецОбласти

#Область ФормированиеТекстовЗапросов

Функция ПоляВыборкиЧисловыхРесурсов()
	
	ПоляВыборки = Новый Массив();
	
	ПоляВыборки.Добавить("СуммаУпр");
	ПоляВыборки.Добавить("СуммаРегл");
	ПоляВыборки.Добавить("СуммаВал");
	ПоляВыборки.Добавить("Количество");
	
	Возврат ПоляВыборки;
	
КонецФункции

Функция ПолноеИмяЗначенияПеречисления(ЗначениеПеречисления)
	
	ОбъектМетаданных = ЗначениеПеречисления.Метаданные();
	
	Если ЗначениеПеречисления.Пустая() Тогда
		ИмяЗначения = "ПустаяСсылка";
	Иначе
		ИндексЗначения = Перечисления[ОбъектМетаданных.Имя].Индекс(ЗначениеПеречисления);
		ИмяЗначения = ОбъектМетаданных.ЗначенияПеречисления[ИндексЗначения].Имя;
	КонецЕсли;
	
	Возврат ОбъектМетаданных.ПолноеИмя() + "." + ИмяЗначения;
	
КонецФункции

Функция ПолноеИмяПредопределенногоЗначения(ПредопределенноеЗначение)
	
	ОбъектМетаданных = ПредопределенноеЗначение.Метаданные();
	
	Если ПредопределенноеЗначение.Пустая() Тогда
		ИмяЗначения = "ПустаяСсылка";
	Иначе
		ИмяЗначения = ПредопределенноеЗначение.ИмяПредопределенныхДанных;
	КонецЕсли;
	
	Возврат ОбъектМетаданных.ПолноеИмя() + "." + ИмяЗначения;
	
КонецФункции

Функция ТипыДокументовВводаОстатков()
	
	ТипыДокументов = Новый Массив();
	
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковВзаиморасчетов"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковДенежныхСредств"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковОПродажахЗаПрошлыеПериоды"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковПоФинансовымИнструментам"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковПрочиеРасходы"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковПрочихАктивовПассивов"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковРасчетовПоЭквайрингу"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковСПодотчетниками"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковТоваров"));
	
	//++ НЕ УТ
	
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковВзаиморасчетовПоАренде"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковВнеоборотныхАктивов2_4"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковИнвестицииВАренду"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВводОстатковУзловКомпонентовАмортизации"));
	
	//-- НЕ УТ
	
	//++ НЕ УТКА
	
	ТипыДокументов.Добавить(Тип("ДокументСсылка.НачальныеОстаткиНЗППоПартиямПроизводства"));
	
	//-- НЕ УТКА
	
	ФинансовыйУчетПоДаннымБалансовыхРегистровЛокализация.ДополнитьТипыДокументовВводаОстатков(ТипыДокументов);
	
	Возврат ТипыДокументов;
	
КонецФункции

Функция ТипыДокументовРозничныхПродаж()
	
	ТипыДокументов = Новый Массив();
	
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ОтчетОРозничныхПродажах"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.РеализацияПодарочныхСертификатов"));
	
	Возврат ТипыДокументов;
	
КонецФункции

Функция ТипыДокументовРозничныхВозвратов()
	
	ТипыДокументов = Новый Массив();
	
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ОтчетОРозничныхВозвратах"));
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВозвратПодарочныхСертификатов"));
	
	Возврат ТипыДокументов;
	
КонецФункции

Функция ТипыДокументовВзаимозачетаЗадолженности()
	
	ТипыДокументов = Новый Массив();
	
	ТипыДокументов.Добавить(Тип("ДокументСсылка.ВзаимозачетЗадолженности"));
	
	Возврат ТипыДокументов;
	
КонецФункции

Функция ХозяйственныеОперацииБезУчетаВВалюте()
	
	БезУчетаВВалюте = Новый Массив();
	
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыАрендаПрибыль);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыАрендаУбыток);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыДепозитыПрибыль);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыДепозитыУбыток);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыДСПрибыль);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыДСУбыток);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыЗаймыВыданныеПрибыль);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыЗаймыВыданныеУбыток);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыКлиентыПрибыль);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыКлиентыУбыток);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыКредитыИЗаймыПрибыль);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыКредитыИЗаймыУбыток);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыПоставщикиПрибыль);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыПоставщикиУбыток);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыРезервыПоДолгамПрибыль);
	БезУчетаВВалюте.Добавить(Справочники.НастройкиХозяйственныхОпераций.КурсовыеРазницыРезервыПоДолгамУбыток);
	
	Возврат БезУчетаВВалюте;
	
КонецФункции

Функция ТипыНалогообложенияБезУчетаНДС()
	
	БезУчетаНДС = Новый Массив();
	
	БезУчетаНДС.Добавить(Перечисления.ТипыНалогообложенияНДС.ПродажаНеОблагаетсяНДС);
	БезУчетаНДС.Добавить(Перечисления.ТипыНалогообложенияНДС.ПродажаОблагаетсяЕНВД);
	
	Возврат БезУчетаНДС;
	
КонецФункции

#КонецОбласти

#КонецОбласти